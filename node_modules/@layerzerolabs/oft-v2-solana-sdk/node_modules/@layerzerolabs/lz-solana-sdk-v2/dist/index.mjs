import * as web3108 from '@solana/web3.js';
import { PublicKey, TransactionInstruction, AddressLookupTableProgram, VersionedTransaction, TransactionMessage, AddressLookupTableAccount, Keypair, NONCE_ACCOUNT_LENGTH, Transaction, SystemProgram, NonceAccount } from '@solana/web3.js';
import BN from 'bn.js';
import invariant6 from 'tiny-invariant';
import { keccak_256, sha2_256 } from '@layerzerolabs/lz-foundation';
import { arrayify } from '@layerzerolabs/lz-utilities';
import { addressToBytes32, PacketV1Codec } from '@layerzerolabs/lz-v2-utilities';
import * as beet159 from '@metaplex-foundation/beet';
import { uniformFixedSizeArray, u8, FixableBeetArgsStruct, u32, u64, bytes, u16, BeetArgsStruct, bool, array } from '@metaplex-foundation/beet';
import * as beetSolana96 from '@metaplex-foundation/beet-solana';
import * as splToken from '@solana/spl-token';
import { TOKEN_PROGRAM_ID, ASSOCIATED_TOKEN_PROGRAM_ID, getAssociatedTokenAddress } from '@solana/spl-token';
import { Environment, EndpointId } from '@layerzerolabs/lz-definitions';
import base58 from 'bs58';
import { defaultPublicKey, deserializeAccount, publicKey, assertAccountExists, gpaBuilder, ProgramError, transactionBuilder, createNullRpc, some, createNoopSigner, isSome, isPublicKey, generateSigner, publicKeyBytes, isPda, isSigner } from '@metaplex-foundation/umi';
import { mapSerializer, struct, bytes as bytes$1, bool as bool$1, u8 as u8$1, u32 as u32$1, publicKey as publicKey$1, option, scalarEnum, u64 as u64$1, array as array$1, u16 as u16$1, dataEnum, tuple, u128, i64, unit, base58 as base58$1, Endian } from '@metaplex-foundation/umi/serializers';
import { createWeb3JsEddsa } from '@metaplex-foundation/umi-eddsa-web3js';
import { createDefaultProgramRepository } from '@metaplex-foundation/umi-program-repository';
import { toWeb3JsPublicKey, fromWeb3JsPublicKey, toWeb3JsInstruction } from '@metaplex-foundation/umi-web3js-adapters';
import { fetchAllAddressLookupTable } from '@metaplex-foundation/mpl-toolbox';

var __defProp = Object.defineProperty;
var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
  get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
}) : x)(function(x) {
  if (typeof require !== "undefined") return require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var ENDPOINT_SEED = "Endpoint";
var MESSAGE_LIB_SEED = "MessageLib";
var SEND_LIBRARY_CONFIG_SEED = "SendLibraryConfig";
var RECEIVE_LIBRARY_CONFIG_SEED = "ReceiveLibraryConfig";
var NONCE_SEED = "Nonce";
var PENDING_NONCE_SEED = "PendingNonce";
var PAYLOAD_HASH_SEED = "PayloadHash";
var COMPOSED_MESSAGE_HASH_SEED = "ComposedMessageHash";
var OAPP_SEED = "OApp";
var COUNT_SEED = "Count";
var REMOTE_SEED = "Remote";
var LZ_RECEIVE_TYPES_SEED = "LzReceiveTypes";
var LZ_COMPOSE_TYPES_SEED = "LzComposeTypes";
var ULN_SEED = MESSAGE_LIB_SEED;
var ULN_CONFIG_SEED = "UlnConfig";
var SEND_CONFIG_SEED = "SendConfig";
var RECEIVE_CONFIG_SEED = "ReceiveConfig";
var OPTIONS_SEED = "Options";
var CONFIRMATIONS_SEED = "Confirmations";
var WORKER_SEED = "Worker";
var DVN_CONFIG_SEED = "DvnConfig";
var EVENT_SEED = "__event_authority";
var EXECUTOR_CONFIG_SEED = "ExecutorConfig";
var PRICE_FEED_SEED = "PriceFeed";
var PEER_SEED = "Peer";
var MINT_SEED = "Mint";
var ENFORCED_OPTIONS_SEED = "EnforcedOptions";
var EndpointPDADeriver = class {
  /**
   * Creates an instance of the EndpointPDADeriver class.
   *
   * @param {PublicKey} program - The program public key.
   */
  constructor(program) {
    this.program = program;
  }
  /**
   * Derives the setting address.
   *
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  setting() {
    return PublicKey.findProgramAddressSync([Buffer.from(ENDPOINT_SEED, "utf8")], this.program);
  }
  /**
   * Derives the default send library configuration address.
   *
   * @param {number} dstEndpointId - The destination endpoint ID.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  defaultSendLibraryConfig(dstEndpointId) {
    return PublicKey.findProgramAddressSync(
      [
        Buffer.from(SEND_LIBRARY_CONFIG_SEED, "utf8"),
        // U32 to Uint8Array([0,0,0,0])
        new BN(dstEndpointId).toArrayLike(Buffer, "be", 4)
      ],
      this.program
    );
  }
  /**
   * Derives the send library configuration address.
   *
   * @param {PublicKey} sender - The sender public key.
   * @param {number} dstEndpointId - The destination endpoint ID.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  sendLibraryConfig(sender, dstEndpointId) {
    return PublicKey.findProgramAddressSync(
      [
        Buffer.from(SEND_LIBRARY_CONFIG_SEED, "utf8"),
        sender.toBytes(),
        // U32 to Uint8Array([0,0,0,0])
        new BN(dstEndpointId).toArrayLike(Buffer, "be", 4)
      ],
      this.program
    );
  }
  /**
   * Derives the message library information address.
   *
   * @param {PublicKey} messageLibrary - The message library public key, PDA(derive by message lib program)
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  messageLibraryInfo(messageLibrary) {
    return PublicKey.findProgramAddressSync(
      [Buffer.from(MESSAGE_LIB_SEED, "utf8"), messageLibrary.toBytes()],
      this.program
    );
  }
  /**
   * Derives the default receive library configuration address.
   *
   * @param {number} srcEndpointId - The source endpoint ID.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  defaultReceiveLibraryConfig(srcEndpointId) {
    return PublicKey.findProgramAddressSync(
      [
        Buffer.from(RECEIVE_LIBRARY_CONFIG_SEED, "utf8"),
        // U32 to Uint8Array([0,0,0,0])
        new BN(srcEndpointId).toArrayLike(Buffer, "be", 4)
      ],
      this.program
    );
  }
  /**
   * Derives the receive library configuration address.
   *
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} srcEndpointId - The source endpoint ID.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  receiveLibraryConfig(receiver, srcEndpointId) {
    return PublicKey.findProgramAddressSync(
      [
        Buffer.from(RECEIVE_LIBRARY_CONFIG_SEED, "utf8"),
        receiver.toBytes(),
        // U32 to Uint8Array([0,0,0,0])
        new BN(srcEndpointId).toArrayLike(Buffer, "be", 4)
      ],
      this.program
    );
  }
  receiveLibraryTimeout(_dstEndpointId) {
    return [this.program, 0];
  }
  defaultMessageLib() {
    return this.messageLibraryInfo(PublicKey.default);
  }
  blockMessageLib(blockMsgLib) {
    return PublicKey.findProgramAddressSync(
      [Buffer.from(MESSAGE_LIB_SEED, "utf8"), blockMsgLib.toBytes()],
      this.program
    );
  }
  /**
   * Derives the nonce address.
   *
   * @param {PublicKey} localOapp - The local OApp public key.
   * @param {number} remoteChainId - The remote chain ID.
   * @param {Uint8Array} remoteOapp - The remote OApp address.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  nonce(localOapp, remoteChainId, remoteOapp) {
    return PublicKey.findProgramAddressSync(
      [
        Buffer.from(NONCE_SEED, "utf8"),
        localOapp.toBytes(),
        // U32 to Uint8Array([0,0,0,0])
        new BN(remoteChainId).toArrayLike(Buffer, "be", 4),
        remoteOapp
      ],
      this.program
    );
  }
  /**
   * Derives the pending nonce address.
   *
   * @param {PublicKey} localOapp - The local OApp public key.
   * @param {number} remoteChainId - The remote chain ID.
   * @param {Uint8Array} remoteOapp - The remote OApp address.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  pendingNonce(localOapp, remoteChainId, remoteOapp) {
    return PublicKey.findProgramAddressSync(
      [
        Buffer.from(PENDING_NONCE_SEED, "utf8"),
        localOapp.toBytes(),
        new BN(remoteChainId).toArrayLike(Buffer, "be", 4),
        remoteOapp
      ],
      this.program
    );
  }
  /**
   * Derives the OApp registry address.
   *
   * @param {PublicKey} localOapp - The local OApp public key.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  oappRegistry(localOapp) {
    return PublicKey.findProgramAddressSync([Buffer.from(OAPP_SEED, "utf8"), localOapp.toBytes()], this.program);
  }
  /**
   * Derives the payload hash address.
   *
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {Uint8Array} sender - The sender address.
   * @param {number} nonce - The nonce.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  payloadHash(receiver, srcEid, sender, nonce) {
    return PublicKey.findProgramAddressSync(
      [
        Buffer.from(PAYLOAD_HASH_SEED, "utf8"),
        receiver.toBytes(),
        new BN(srcEid).toArrayLike(Buffer, "be", 4),
        sender,
        new BN(nonce).toArrayLike(Buffer, "be", 8)
      ],
      this.program
    );
  }
  /**
   * Derives the composed message address.
   *
   * @param {PublicKey} from - The sender public key.
   * @param {Uint8Array} guid - The GUID.
   * @param {number} index - The index.
   * @param {PublicKey} to - The receiver public key.
   * @param {Uint8Array} messageHash - The message hash.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  composedMessage(from, guid, index, to, messageHash) {
    return PublicKey.findProgramAddressSync(
      [
        Buffer.from(COMPOSED_MESSAGE_HASH_SEED, "utf8"),
        from.toBytes(),
        to.toBytes(),
        guid,
        new BN(index).toArrayLike(Buffer, "be", 2),
        messageHash
      ],
      this.program
    );
  }
};
var MessageLibPDADeriver = class {
  /**
   * Creates an instance of the MessageLibPDADeriver class.
   *
   * @param {PublicKey} program - The program public key.
   */
  constructor(program) {
    this.program = program;
  }
  /**
   * Derives the message library address.
   *
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  messageLib() {
    return PublicKey.findProgramAddressSync([Buffer.from(MESSAGE_LIB_SEED, "utf8")], this.program);
  }
  /**
   * Derives the send configuration address.
   *
   * @param {number} eid - The endpoint ID.
   * @param {PublicKey} oapp - The OApp public key.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  sendConfig(eid, oapp) {
    return PublicKey.findProgramAddressSync(
      [Buffer.from(SEND_CONFIG_SEED, "utf8"), new BN(eid).toArrayLike(Buffer, "be", 4), oapp.toBuffer()],
      this.program
    );
  }
  /**
   * Derives the receive configuration address.
   *
   * @param {number} eid - The endpoint ID.
   * @param {PublicKey} oapp - The OApp public key.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  receiveConfig(eid, oapp) {
    return PublicKey.findProgramAddressSync(
      [Buffer.from(RECEIVE_CONFIG_SEED, "utf8"), new BN(eid).toArrayLike(Buffer, "be", 4), oapp.toBuffer()],
      this.program
    );
  }
};
var UlnPDADeriver = class extends MessageLibPDADeriver {
  /**
   * Derives the setting address.
   *
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  setting() {
    return PublicKey.findProgramAddressSync([Buffer.from(ULN_SEED, "utf8")], this.program);
  }
  /**
   * Derives the configuration address.
   *
   * @param {number} eid - The endpoint ID.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  config(eid) {
    return PublicKey.findProgramAddressSync(
      [Buffer.from(ULN_CONFIG_SEED, "utf8"), new BN(eid).toArrayLike(Buffer, "be", 4)],
      this.program
    );
  }
  /**
   * Derives the default send configuration address.
   *
   * @param {number} eid - The endpoint ID.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  defaultSendConfig(eid) {
    return PublicKey.findProgramAddressSync(
      [Buffer.from(SEND_CONFIG_SEED, "utf8"), new BN(eid).toArrayLike(Buffer, "be", 4)],
      this.program
    );
  }
  /**
   * Derives the default receive configuration address.
   *
   * @param {number} eid - The endpoint ID.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  defaultReceiveConfig(eid) {
    return PublicKey.findProgramAddressSync(
      [Buffer.from(RECEIVE_CONFIG_SEED, "utf8"), new BN(eid).toArrayLike(Buffer, "be", 4)],
      this.program
    );
  }
  /**
   * Derives the options address.
   *
   * @param {number} eit - The endpoint ID.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  options(eit) {
    return PublicKey.findProgramAddressSync(
      [Buffer.from(OPTIONS_SEED, "utf8"), new BN(eit).toArrayLike(Buffer, "be", 4)],
      this.program
    );
  }
  /**
   * Derives the worker configuration address.
   *
   * @param {PublicKey} worker - The worker public key.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  workerConfig(worker) {
    return PublicKey.findProgramAddressSync([Buffer.from(WORKER_SEED, "utf8"), worker.toBuffer()], this.program);
  }
  /**
   * Derives the confirmations address.
   *
   * @param {Uint8Array} headerHash - The header hash.
   * @param {Uint8Array} payloadHash - The payload hash.
   * @param {PublicKey} dvn - The DVN public key.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  confirmations(headerHash, payloadHash, dvn) {
    return PublicKey.findProgramAddressSync(
      [Buffer.from(CONFIRMATIONS_SEED, "utf8"), headerHash, payloadHash, dvn.toBytes()],
      this.program
    );
  }
};
var OAppBasePDADeriver = class {
  /**
   * Creates an instance of the OAppBasePDADeriver class.
   *
   * @param {PublicKey} program - The program public key.
   */
  constructor(program) {
    this.program = program;
  }
  /**
   * Derives the remote address.
   *
   * @param {number} dstChainId - The destination chain ID.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  remote(dstChainId) {
    return PublicKey.findProgramAddressSync(
      [Buffer.from(REMOTE_SEED), new BN(dstChainId).toArrayLike(Buffer, "be", 4)],
      this.program
    );
  }
  /**
   * Derives the LzReceive types accounts address.
   *
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  lzReceiveTypesAccounts() {
    return PublicKey.findProgramAddressSync([Buffer.from(LZ_RECEIVE_TYPES_SEED, "utf8")], this.program);
  }
  /**
   * Derives the LzCompose types accounts address.
   *
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  lzComposeTypesAccounts() {
    return PublicKey.findProgramAddressSync([Buffer.from(LZ_COMPOSE_TYPES_SEED, "utf8")], this.program);
  }
};
var DVNDeriver = class {
  constructor(program) {
    this.program = program;
  }
  /**
   * Derives the authority address.
   *
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  authority() {
    return PublicKey.findProgramAddressSync([Buffer.from("dvn", "utf8")], this.program);
  }
  /**
   * Derives the configuration address.
   *
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  config() {
    return PublicKey.findProgramAddressSync([Buffer.from(DVN_CONFIG_SEED, "utf8")], this.program);
  }
  /**
   * Derives the execute hash address.
   *
   * @param {Buffer} digestHash - The digest hash.
   * @returns {[PublicKey, number]} The derived address and bump seed.
   */
  executeHash(digestHash) {
    return PublicKey.findProgramAddressSync([Buffer.from("ExecuteHash", "utf8"), digestHash], this.program);
  }
};
var EventPDADeriver = class {
  /**
   * Creates an instance of the EventPDADeriver class.
   *
   * @param {PublicKey} program - The program public key.
   */
  constructor(program) {
    this.program = program;
  }
  /**
   * Derives the event authority PDA.
   *
   * @returns {[PublicKey, number]} The event authority PDA and bump seed.
   */
  eventAuthority() {
    return PublicKey.findProgramAddressSync([Buffer.from(EVENT_SEED, "utf8")], this.program);
  }
};
var ExecutorPDADeriver = class {
  /**
   * Creates an instance of the ExecutorPDADeriver class.
   *
   * @param {PublicKey} program - The program public key.
   */
  constructor(program) {
    this.program = program;
  }
  /**
   * Derives the executor configuration PDA.
   *
   * @returns {[PublicKey, number]} The executor configuration PDA and bump seed.
   */
  config() {
    return PublicKey.findProgramAddressSync([Buffer.from(EXECUTOR_CONFIG_SEED, "utf8")], this.program);
  }
};
var PriceFeedPDADeriver = class {
  /**
   * Creates an instance of the PriceFeedPDADeriver class.
   *
   * @param {PublicKey} program - The program public key.
   */
  constructor(program) {
    this.program = program;
  }
  /**
   * Derives the price feed PDA.
   *
   * @returns {[PublicKey, number]} The price feed PDA and bump seed.
   */
  priceFeed() {
    return PublicKey.findProgramAddressSync([Buffer.from(PRICE_FEED_SEED, "utf8")], this.program);
  }
};

// src/solita/endpoint.ts
var endpoint_exports = {};
__export(endpoint_exports, {
  DefaultMessageLib: () => DefaultMessageLib,
  Endpoint: () => Endpoint,
  EventEmitDiscriminator: () => EventEmitDiscriminator,
  PROGRAM_ID: () => PROGRAM_ID,
  accounts: () => accounts_exports,
  errors: () => errors_exports,
  events: () => events_exports,
  instructions: () => instructions_exports,
  types: () => types_exports
});

// src/generated/solita/endpoint/accounts/index.ts
var accounts_exports = {};
__export(accounts_exports, {
  ComposeMessageState: () => ComposeMessageState,
  EndpointSettings: () => EndpointSettings,
  MessageLibInfo: () => MessageLibInfo,
  Nonce: () => Nonce,
  OAppRegistry: () => OAppRegistry,
  PayloadHash: () => PayloadHash,
  PendingInboundNonce: () => PendingInboundNonce,
  ReceiveLibraryConfig: () => ReceiveLibraryConfig,
  SendLibraryConfig: () => SendLibraryConfig,
  accountProviders: () => accountProviders,
  composeMessageStateBeet: () => composeMessageStateBeet,
  composeMessageStateDiscriminator: () => composeMessageStateDiscriminator,
  endpointSettingsBeet: () => endpointSettingsBeet,
  endpointSettingsDiscriminator: () => endpointSettingsDiscriminator,
  messageLibInfoBeet: () => messageLibInfoBeet,
  messageLibInfoDiscriminator: () => messageLibInfoDiscriminator,
  nonceBeet: () => nonceBeet,
  nonceDiscriminator: () => nonceDiscriminator,
  oAppRegistryBeet: () => oAppRegistryBeet,
  oAppRegistryDiscriminator: () => oAppRegistryDiscriminator,
  payloadHashBeet: () => payloadHashBeet,
  payloadHashDiscriminator: () => payloadHashDiscriminator,
  pendingInboundNonceBeet: () => pendingInboundNonceBeet,
  pendingInboundNonceDiscriminator: () => pendingInboundNonceDiscriminator,
  receiveLibraryConfigBeet: () => receiveLibraryConfigBeet,
  receiveLibraryConfigDiscriminator: () => receiveLibraryConfigDiscriminator,
  sendLibraryConfigBeet: () => sendLibraryConfigBeet,
  sendLibraryConfigDiscriminator: () => sendLibraryConfigDiscriminator
});
var composeMessageStateDiscriminator = [
  55,
  107,
  79,
  254,
  243,
  22,
  172,
  240
];
var ComposeMessageState = class _ComposeMessageState {
  constructor(received, bump) {
    this.received = received;
    this.bump = bump;
  }
  /**
   * Creates a {@link ComposeMessageState} instance from the provided args.
   */
  static fromArgs(args) {
    return new _ComposeMessageState(args.received, args.bump);
  }
  /**
   * Deserializes the {@link ComposeMessageState} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _ComposeMessageState.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link ComposeMessageState} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(
        `Unable to find ComposeMessageState account at ${address}`
      );
    }
    return _ComposeMessageState.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, composeMessageStateBeet);
  }
  /**
   * Deserializes the {@link ComposeMessageState} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return composeMessageStateBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link ComposeMessageState} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return composeMessageStateBeet.serialize({
      accountDiscriminator: composeMessageStateDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link ComposeMessageState}
   */
  static get byteSize() {
    return composeMessageStateBeet.byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link ComposeMessageState} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _ComposeMessageState.byteSize,
      commitment
    );
  }
  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link ComposeMessageState} data.
   */
  static hasCorrectByteSize(buf, offset = 0) {
    return buf.byteLength - offset === _ComposeMessageState.byteSize;
  }
  /**
   * Returns a readable version of {@link ComposeMessageState} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      received: this.received,
      bump: this.bump
    };
  }
};
var composeMessageStateBeet = new beet159.BeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["received", beet159.bool],
    ["bump", beet159.u8]
  ],
  ComposeMessageState.fromArgs,
  "ComposeMessageState"
);
var endpointSettingsDiscriminator = [221, 232, 73, 56, 10, 66, 72, 14];
var EndpointSettings = class _EndpointSettings {
  constructor(eid, bump, admin, lzTokenMint) {
    this.eid = eid;
    this.bump = bump;
    this.admin = admin;
    this.lzTokenMint = lzTokenMint;
  }
  /**
   * Creates a {@link EndpointSettings} instance from the provided args.
   */
  static fromArgs(args) {
    return new _EndpointSettings(
      args.eid,
      args.bump,
      args.admin,
      args.lzTokenMint
    );
  }
  /**
   * Deserializes the {@link EndpointSettings} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _EndpointSettings.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link EndpointSettings} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find EndpointSettings account at ${address}`);
    }
    return _EndpointSettings.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, endpointSettingsBeet);
  }
  /**
   * Deserializes the {@link EndpointSettings} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return endpointSettingsBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link EndpointSettings} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return endpointSettingsBeet.serialize({
      accountDiscriminator: endpointSettingsDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link EndpointSettings} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _EndpointSettings.fromArgs(args);
    return endpointSettingsBeet.toFixedFromValue({
      accountDiscriminator: endpointSettingsDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link EndpointSettings} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _EndpointSettings.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link EndpointSettings} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      eid: this.eid,
      bump: this.bump,
      admin: this.admin.toBase58(),
      lzTokenMint: this.lzTokenMint
    };
  }
};
var endpointSettingsBeet = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["eid", beet159.u32],
    ["bump", beet159.u8],
    ["admin", beetSolana96.publicKey],
    ["lzTokenMint", beet159.coption(beetSolana96.publicKey)]
  ],
  EndpointSettings.fromArgs,
  "EndpointSettings"
);
var MessageLibType = /* @__PURE__ */ ((MessageLibType6) => {
  MessageLibType6[MessageLibType6["Send"] = 0] = "Send";
  MessageLibType6[MessageLibType6["Receive"] = 1] = "Receive";
  MessageLibType6[MessageLibType6["SendAndReceive"] = 2] = "SendAndReceive";
  return MessageLibType6;
})(MessageLibType || {});
var messageLibTypeBeet = beet159.fixedScalarEnum(
  MessageLibType
);

// src/generated/solita/endpoint/accounts/MessageLibInfo.ts
var messageLibInfoDiscriminator = [103, 102, 218, 28, 204, 135, 71, 14];
var MessageLibInfo = class _MessageLibInfo {
  constructor(messageLibType, bump, messageLibBump) {
    this.messageLibType = messageLibType;
    this.bump = bump;
    this.messageLibBump = messageLibBump;
  }
  /**
   * Creates a {@link MessageLibInfo} instance from the provided args.
   */
  static fromArgs(args) {
    return new _MessageLibInfo(
      args.messageLibType,
      args.bump,
      args.messageLibBump
    );
  }
  /**
   * Deserializes the {@link MessageLibInfo} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _MessageLibInfo.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link MessageLibInfo} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find MessageLibInfo account at ${address}`);
    }
    return _MessageLibInfo.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, messageLibInfoBeet);
  }
  /**
   * Deserializes the {@link MessageLibInfo} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return messageLibInfoBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link MessageLibInfo} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return messageLibInfoBeet.serialize({
      accountDiscriminator: messageLibInfoDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link MessageLibInfo}
   */
  static get byteSize() {
    return messageLibInfoBeet.byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link MessageLibInfo} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _MessageLibInfo.byteSize,
      commitment
    );
  }
  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link MessageLibInfo} data.
   */
  static hasCorrectByteSize(buf, offset = 0) {
    return buf.byteLength - offset === _MessageLibInfo.byteSize;
  }
  /**
   * Returns a readable version of {@link MessageLibInfo} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      messageLibType: "MessageLibType." + MessageLibType[this.messageLibType],
      bump: this.bump,
      messageLibBump: this.messageLibBump
    };
  }
};
var messageLibInfoBeet = new beet159.BeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["messageLibType", messageLibTypeBeet],
    ["bump", beet159.u8],
    ["messageLibBump", beet159.u8]
  ],
  MessageLibInfo.fromArgs,
  "MessageLibInfo"
);
var nonceDiscriminator = [143, 197, 147, 95, 106, 165, 50, 43];
var Nonce = class _Nonce {
  constructor(bump, outboundNonce, inboundNonce) {
    this.bump = bump;
    this.outboundNonce = outboundNonce;
    this.inboundNonce = inboundNonce;
  }
  /**
   * Creates a {@link Nonce} instance from the provided args.
   */
  static fromArgs(args) {
    return new _Nonce(args.bump, args.outboundNonce, args.inboundNonce);
  }
  /**
   * Deserializes the {@link Nonce} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _Nonce.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Nonce} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find Nonce account at ${address}`);
    }
    return _Nonce.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, nonceBeet);
  }
  /**
   * Deserializes the {@link Nonce} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return nonceBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link Nonce} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return nonceBeet.serialize({
      accountDiscriminator: nonceDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Nonce}
   */
  static get byteSize() {
    return nonceBeet.byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Nonce} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _Nonce.byteSize,
      commitment
    );
  }
  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link Nonce} data.
   */
  static hasCorrectByteSize(buf, offset = 0) {
    return buf.byteLength - offset === _Nonce.byteSize;
  }
  /**
   * Returns a readable version of {@link Nonce} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      bump: this.bump,
      outboundNonce: (() => {
        const x = this.outboundNonce;
        if (typeof x.toNumber === "function") {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      inboundNonce: (() => {
        const x = this.inboundNonce;
        if (typeof x.toNumber === "function") {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })()
    };
  }
};
var nonceBeet = new beet159.BeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["bump", beet159.u8],
    ["outboundNonce", beet159.u64],
    ["inboundNonce", beet159.u64]
  ],
  Nonce.fromArgs,
  "Nonce"
);
var oAppRegistryDiscriminator = [6, 152, 199, 30, 217, 50, 69, 149];
var OAppRegistry = class _OAppRegistry {
  constructor(delegate, bump) {
    this.delegate = delegate;
    this.bump = bump;
  }
  /**
   * Creates a {@link OAppRegistry} instance from the provided args.
   */
  static fromArgs(args) {
    return new _OAppRegistry(args.delegate, args.bump);
  }
  /**
   * Deserializes the {@link OAppRegistry} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _OAppRegistry.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link OAppRegistry} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find OAppRegistry account at ${address}`);
    }
    return _OAppRegistry.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, oAppRegistryBeet);
  }
  /**
   * Deserializes the {@link OAppRegistry} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return oAppRegistryBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link OAppRegistry} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return oAppRegistryBeet.serialize({
      accountDiscriminator: oAppRegistryDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link OAppRegistry}
   */
  static get byteSize() {
    return oAppRegistryBeet.byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link OAppRegistry} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _OAppRegistry.byteSize,
      commitment
    );
  }
  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link OAppRegistry} data.
   */
  static hasCorrectByteSize(buf, offset = 0) {
    return buf.byteLength - offset === _OAppRegistry.byteSize;
  }
  /**
   * Returns a readable version of {@link OAppRegistry} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      delegate: this.delegate.toBase58(),
      bump: this.bump
    };
  }
};
var oAppRegistryBeet = new beet159.BeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["delegate", beetSolana96.publicKey],
    ["bump", beet159.u8]
  ],
  OAppRegistry.fromArgs,
  "OAppRegistry"
);
var payloadHashDiscriminator = [96, 28, 106, 145, 103, 32, 186, 70];
var PayloadHash = class _PayloadHash {
  constructor(hash, bump) {
    this.hash = hash;
    this.bump = bump;
  }
  /**
   * Creates a {@link PayloadHash} instance from the provided args.
   */
  static fromArgs(args) {
    return new _PayloadHash(args.hash, args.bump);
  }
  /**
   * Deserializes the {@link PayloadHash} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _PayloadHash.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link PayloadHash} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find PayloadHash account at ${address}`);
    }
    return _PayloadHash.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, payloadHashBeet);
  }
  /**
   * Deserializes the {@link PayloadHash} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return payloadHashBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link PayloadHash} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return payloadHashBeet.serialize({
      accountDiscriminator: payloadHashDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link PayloadHash}
   */
  static get byteSize() {
    return payloadHashBeet.byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link PayloadHash} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _PayloadHash.byteSize,
      commitment
    );
  }
  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link PayloadHash} data.
   */
  static hasCorrectByteSize(buf, offset = 0) {
    return buf.byteLength - offset === _PayloadHash.byteSize;
  }
  /**
   * Returns a readable version of {@link PayloadHash} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      hash: this.hash,
      bump: this.bump
    };
  }
};
var payloadHashBeet = new beet159.BeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["hash", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["bump", beet159.u8]
  ],
  PayloadHash.fromArgs,
  "PayloadHash"
);
var pendingInboundNonceDiscriminator = [
  170,
  176,
  95,
  240,
  120,
  231,
  241,
  218
];
var PendingInboundNonce = class _PendingInboundNonce {
  constructor(nonces, bump) {
    this.nonces = nonces;
    this.bump = bump;
  }
  /**
   * Creates a {@link PendingInboundNonce} instance from the provided args.
   */
  static fromArgs(args) {
    return new _PendingInboundNonce(args.nonces, args.bump);
  }
  /**
   * Deserializes the {@link PendingInboundNonce} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _PendingInboundNonce.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link PendingInboundNonce} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(
        `Unable to find PendingInboundNonce account at ${address}`
      );
    }
    return _PendingInboundNonce.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, pendingInboundNonceBeet);
  }
  /**
   * Deserializes the {@link PendingInboundNonce} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return pendingInboundNonceBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link PendingInboundNonce} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return pendingInboundNonceBeet.serialize({
      accountDiscriminator: pendingInboundNonceDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link PendingInboundNonce} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _PendingInboundNonce.fromArgs(args);
    return pendingInboundNonceBeet.toFixedFromValue({
      accountDiscriminator: pendingInboundNonceDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link PendingInboundNonce} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _PendingInboundNonce.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link PendingInboundNonce} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      nonces: this.nonces,
      bump: this.bump
    };
  }
};
var pendingInboundNonceBeet = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["nonces", beet159.array(beet159.u64)],
    ["bump", beet159.u8]
  ],
  PendingInboundNonce.fromArgs,
  "PendingInboundNonce"
);
var receiveLibraryTimeoutBeet = new beet159.BeetArgsStruct(
  [
    ["messageLib", beetSolana96.publicKey],
    ["expiry", beet159.u64]
  ],
  "ReceiveLibraryTimeout"
);

// src/generated/solita/endpoint/accounts/ReceiveLibraryConfig.ts
var receiveLibraryConfigDiscriminator = [
  142,
  226,
  251,
  138,
  1,
  206,
  91,
  193
];
var ReceiveLibraryConfig = class _ReceiveLibraryConfig {
  constructor(messageLib, timeout, bump) {
    this.messageLib = messageLib;
    this.timeout = timeout;
    this.bump = bump;
  }
  /**
   * Creates a {@link ReceiveLibraryConfig} instance from the provided args.
   */
  static fromArgs(args) {
    return new _ReceiveLibraryConfig(args.messageLib, args.timeout, args.bump);
  }
  /**
   * Deserializes the {@link ReceiveLibraryConfig} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _ReceiveLibraryConfig.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link ReceiveLibraryConfig} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(
        `Unable to find ReceiveLibraryConfig account at ${address}`
      );
    }
    return _ReceiveLibraryConfig.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, receiveLibraryConfigBeet);
  }
  /**
   * Deserializes the {@link ReceiveLibraryConfig} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return receiveLibraryConfigBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link ReceiveLibraryConfig} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return receiveLibraryConfigBeet.serialize({
      accountDiscriminator: receiveLibraryConfigDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link ReceiveLibraryConfig} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _ReceiveLibraryConfig.fromArgs(args);
    return receiveLibraryConfigBeet.toFixedFromValue({
      accountDiscriminator: receiveLibraryConfigDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link ReceiveLibraryConfig} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _ReceiveLibraryConfig.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link ReceiveLibraryConfig} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      messageLib: this.messageLib.toBase58(),
      timeout: this.timeout,
      bump: this.bump
    };
  }
};
var receiveLibraryConfigBeet = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["messageLib", beetSolana96.publicKey],
    ["timeout", beet159.coption(receiveLibraryTimeoutBeet)],
    ["bump", beet159.u8]
  ],
  ReceiveLibraryConfig.fromArgs,
  "ReceiveLibraryConfig"
);
var sendLibraryConfigDiscriminator = [
  61,
  238,
  31,
  72,
  251,
  117,
  66,
  176
];
var SendLibraryConfig = class _SendLibraryConfig {
  constructor(messageLib, bump) {
    this.messageLib = messageLib;
    this.bump = bump;
  }
  /**
   * Creates a {@link SendLibraryConfig} instance from the provided args.
   */
  static fromArgs(args) {
    return new _SendLibraryConfig(args.messageLib, args.bump);
  }
  /**
   * Deserializes the {@link SendLibraryConfig} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _SendLibraryConfig.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link SendLibraryConfig} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find SendLibraryConfig account at ${address}`);
    }
    return _SendLibraryConfig.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, sendLibraryConfigBeet);
  }
  /**
   * Deserializes the {@link SendLibraryConfig} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return sendLibraryConfigBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link SendLibraryConfig} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return sendLibraryConfigBeet.serialize({
      accountDiscriminator: sendLibraryConfigDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link SendLibraryConfig}
   */
  static get byteSize() {
    return sendLibraryConfigBeet.byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link SendLibraryConfig} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _SendLibraryConfig.byteSize,
      commitment
    );
  }
  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link SendLibraryConfig} data.
   */
  static hasCorrectByteSize(buf, offset = 0) {
    return buf.byteLength - offset === _SendLibraryConfig.byteSize;
  }
  /**
   * Returns a readable version of {@link SendLibraryConfig} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      messageLib: this.messageLib.toBase58(),
      bump: this.bump
    };
  }
};
var sendLibraryConfigBeet = new beet159.BeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["messageLib", beetSolana96.publicKey],
    ["bump", beet159.u8]
  ],
  SendLibraryConfig.fromArgs,
  "SendLibraryConfig"
);

// src/generated/solita/endpoint/accounts/index.ts
var accountProviders = {
  ComposeMessageState,
  EndpointSettings,
  OAppRegistry,
  MessageLibInfo,
  ReceiveLibraryConfig,
  SendLibraryConfig,
  Nonce,
  PayloadHash,
  PendingInboundNonce
};

// src/generated/solita/endpoint/errors/index.ts
var errors_exports = {};
__export(errors_exports, {
  AccountNotFoundError: () => AccountNotFoundError,
  ComposeNotFoundError: () => ComposeNotFoundError,
  InvalidAmountError: () => InvalidAmountError,
  InvalidExpiryError: () => InvalidExpiryError,
  InvalidMessageLibError: () => InvalidMessageLibError,
  InvalidNonceError: () => InvalidNonceError,
  InvalidPayloadHashError: () => InvalidPayloadHashError,
  InvalidReceiveLibraryError: () => InvalidReceiveLibraryError,
  InvalidSendLibraryError: () => InvalidSendLibraryError,
  LzTokenUnavailableError: () => LzTokenUnavailableError,
  OnlyNonDefaultLibError: () => OnlyNonDefaultLibError,
  OnlyReceiveLibError: () => OnlyReceiveLibError,
  OnlySendLibError: () => OnlySendLibError,
  PayloadHashNotFoundError: () => PayloadHashNotFoundError,
  ReadOnlyAccountError: () => ReadOnlyAccountError,
  SameValueError: () => SameValueError,
  UnauthorizedError: () => UnauthorizedError,
  WritableAccountNotAllowedError: () => WritableAccountNotAllowedError,
  errorFromCode: () => errorFromCode,
  errorFromName: () => errorFromName
});
var createErrorFromCodeLookup = /* @__PURE__ */ new Map();
var createErrorFromNameLookup = /* @__PURE__ */ new Map();
var InvalidSendLibraryError = class _InvalidSendLibraryError extends Error {
  constructor() {
    super("");
    this.code = 6e3;
    this.name = "InvalidSendLibrary";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidSendLibraryError);
    }
  }
};
createErrorFromCodeLookup.set(6e3, () => new InvalidSendLibraryError());
createErrorFromNameLookup.set(
  "InvalidSendLibrary",
  () => new InvalidSendLibraryError()
);
var InvalidReceiveLibraryError = class _InvalidReceiveLibraryError extends Error {
  constructor() {
    super("");
    this.code = 6001;
    this.name = "InvalidReceiveLibrary";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidReceiveLibraryError);
    }
  }
};
createErrorFromCodeLookup.set(6001, () => new InvalidReceiveLibraryError());
createErrorFromNameLookup.set(
  "InvalidReceiveLibrary",
  () => new InvalidReceiveLibraryError()
);
var SameValueError = class _SameValueError extends Error {
  constructor() {
    super("");
    this.code = 6002;
    this.name = "SameValue";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _SameValueError);
    }
  }
};
createErrorFromCodeLookup.set(6002, () => new SameValueError());
createErrorFromNameLookup.set("SameValue", () => new SameValueError());
var AccountNotFoundError = class _AccountNotFoundError extends Error {
  constructor() {
    super("");
    this.code = 6003;
    this.name = "AccountNotFound";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _AccountNotFoundError);
    }
  }
};
createErrorFromCodeLookup.set(6003, () => new AccountNotFoundError());
createErrorFromNameLookup.set(
  "AccountNotFound",
  () => new AccountNotFoundError()
);
var OnlySendLibError = class _OnlySendLibError extends Error {
  constructor() {
    super("");
    this.code = 6004;
    this.name = "OnlySendLib";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _OnlySendLibError);
    }
  }
};
createErrorFromCodeLookup.set(6004, () => new OnlySendLibError());
createErrorFromNameLookup.set("OnlySendLib", () => new OnlySendLibError());
var OnlyReceiveLibError = class _OnlyReceiveLibError extends Error {
  constructor() {
    super("");
    this.code = 6005;
    this.name = "OnlyReceiveLib";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _OnlyReceiveLibError);
    }
  }
};
createErrorFromCodeLookup.set(6005, () => new OnlyReceiveLibError());
createErrorFromNameLookup.set("OnlyReceiveLib", () => new OnlyReceiveLibError());
var InvalidExpiryError = class _InvalidExpiryError extends Error {
  constructor() {
    super("");
    this.code = 6006;
    this.name = "InvalidExpiry";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidExpiryError);
    }
  }
};
createErrorFromCodeLookup.set(6006, () => new InvalidExpiryError());
createErrorFromNameLookup.set("InvalidExpiry", () => new InvalidExpiryError());
var OnlyNonDefaultLibError = class _OnlyNonDefaultLibError extends Error {
  constructor() {
    super("");
    this.code = 6007;
    this.name = "OnlyNonDefaultLib";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _OnlyNonDefaultLibError);
    }
  }
};
createErrorFromCodeLookup.set(6007, () => new OnlyNonDefaultLibError());
createErrorFromNameLookup.set(
  "OnlyNonDefaultLib",
  () => new OnlyNonDefaultLibError()
);
var InvalidAmountError = class _InvalidAmountError extends Error {
  constructor() {
    super("");
    this.code = 6008;
    this.name = "InvalidAmount";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidAmountError);
    }
  }
};
createErrorFromCodeLookup.set(6008, () => new InvalidAmountError());
createErrorFromNameLookup.set("InvalidAmount", () => new InvalidAmountError());
var InvalidNonceError = class _InvalidNonceError extends Error {
  constructor() {
    super("");
    this.code = 6009;
    this.name = "InvalidNonce";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidNonceError);
    }
  }
};
createErrorFromCodeLookup.set(6009, () => new InvalidNonceError());
createErrorFromNameLookup.set("InvalidNonce", () => new InvalidNonceError());
var UnauthorizedError = class _UnauthorizedError extends Error {
  constructor() {
    super("");
    this.code = 6010;
    this.name = "Unauthorized";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _UnauthorizedError);
    }
  }
};
createErrorFromCodeLookup.set(6010, () => new UnauthorizedError());
createErrorFromNameLookup.set("Unauthorized", () => new UnauthorizedError());
var PayloadHashNotFoundError = class _PayloadHashNotFoundError extends Error {
  constructor() {
    super("");
    this.code = 6011;
    this.name = "PayloadHashNotFound";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _PayloadHashNotFoundError);
    }
  }
};
createErrorFromCodeLookup.set(6011, () => new PayloadHashNotFoundError());
createErrorFromNameLookup.set(
  "PayloadHashNotFound",
  () => new PayloadHashNotFoundError()
);
var ComposeNotFoundError = class _ComposeNotFoundError extends Error {
  constructor() {
    super("");
    this.code = 6012;
    this.name = "ComposeNotFound";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _ComposeNotFoundError);
    }
  }
};
createErrorFromCodeLookup.set(6012, () => new ComposeNotFoundError());
createErrorFromNameLookup.set(
  "ComposeNotFound",
  () => new ComposeNotFoundError()
);
var InvalidPayloadHashError = class _InvalidPayloadHashError extends Error {
  constructor() {
    super("");
    this.code = 6013;
    this.name = "InvalidPayloadHash";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidPayloadHashError);
    }
  }
};
createErrorFromCodeLookup.set(6013, () => new InvalidPayloadHashError());
createErrorFromNameLookup.set(
  "InvalidPayloadHash",
  () => new InvalidPayloadHashError()
);
var LzTokenUnavailableError = class _LzTokenUnavailableError extends Error {
  constructor() {
    super("");
    this.code = 6014;
    this.name = "LzTokenUnavailable";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _LzTokenUnavailableError);
    }
  }
};
createErrorFromCodeLookup.set(6014, () => new LzTokenUnavailableError());
createErrorFromNameLookup.set(
  "LzTokenUnavailable",
  () => new LzTokenUnavailableError()
);
var ReadOnlyAccountError = class _ReadOnlyAccountError extends Error {
  constructor() {
    super("");
    this.code = 6015;
    this.name = "ReadOnlyAccount";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _ReadOnlyAccountError);
    }
  }
};
createErrorFromCodeLookup.set(6015, () => new ReadOnlyAccountError());
createErrorFromNameLookup.set(
  "ReadOnlyAccount",
  () => new ReadOnlyAccountError()
);
var InvalidMessageLibError = class _InvalidMessageLibError extends Error {
  constructor() {
    super("");
    this.code = 6016;
    this.name = "InvalidMessageLib";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidMessageLibError);
    }
  }
};
createErrorFromCodeLookup.set(6016, () => new InvalidMessageLibError());
createErrorFromNameLookup.set(
  "InvalidMessageLib",
  () => new InvalidMessageLibError()
);
var WritableAccountNotAllowedError = class _WritableAccountNotAllowedError extends Error {
  constructor() {
    super("");
    this.code = 6017;
    this.name = "WritableAccountNotAllowed";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _WritableAccountNotAllowedError);
    }
  }
};
createErrorFromCodeLookup.set(
  6017,
  () => new WritableAccountNotAllowedError()
);
createErrorFromNameLookup.set(
  "WritableAccountNotAllowed",
  () => new WritableAccountNotAllowedError()
);
function errorFromCode(code) {
  const createError = createErrorFromCodeLookup.get(code);
  return createError != null ? createError() : null;
}
function errorFromName(name) {
  const createError = createErrorFromNameLookup.get(name);
  return createError != null ? createError() : null;
}

// src/generated/solita/endpoint/instructions/index.ts
var instructions_exports = {};
__export(instructions_exports, {
  burnInstructionDiscriminator: () => burnInstructionDiscriminator,
  burnStruct: () => burnStruct,
  clearComposeInstructionDiscriminator: () => clearComposeInstructionDiscriminator,
  clearComposeStruct: () => clearComposeStruct,
  clearInstructionDiscriminator: () => clearInstructionDiscriminator,
  clearStruct: () => clearStruct,
  createBurnInstruction: () => createBurnInstruction,
  createBurnInstructionAccounts: () => createBurnInstructionAccounts,
  createClearComposeInstruction: () => createClearComposeInstruction,
  createClearComposeInstructionAccounts: () => createClearComposeInstructionAccounts,
  createClearInstruction: () => createClearInstruction,
  createClearInstructionAccounts: () => createClearInstructionAccounts,
  createInitConfigInstruction: () => createInitConfigInstruction,
  createInitConfigInstructionAccounts: () => createInitConfigInstructionAccounts,
  createInitDefaultReceiveLibraryInstruction: () => createInitDefaultReceiveLibraryInstruction,
  createInitDefaultReceiveLibraryInstructionAccounts: () => createInitDefaultReceiveLibraryInstructionAccounts,
  createInitDefaultSendLibraryInstruction: () => createInitDefaultSendLibraryInstruction,
  createInitDefaultSendLibraryInstructionAccounts: () => createInitDefaultSendLibraryInstructionAccounts,
  createInitEndpointInstruction: () => createInitEndpointInstruction,
  createInitEndpointInstructionAccounts: () => createInitEndpointInstructionAccounts,
  createInitNonceInstruction: () => createInitNonceInstruction,
  createInitNonceInstructionAccounts: () => createInitNonceInstructionAccounts,
  createInitReceiveLibraryInstruction: () => createInitReceiveLibraryInstruction,
  createInitReceiveLibraryInstructionAccounts: () => createInitReceiveLibraryInstructionAccounts,
  createInitSendLibraryInstruction: () => createInitSendLibraryInstruction,
  createInitSendLibraryInstructionAccounts: () => createInitSendLibraryInstructionAccounts,
  createInitVerifyInstruction: () => createInitVerifyInstruction,
  createInitVerifyInstructionAccounts: () => createInitVerifyInstructionAccounts,
  createLzComposeAlertInstruction: () => createLzComposeAlertInstruction,
  createLzComposeAlertInstructionAccounts: () => createLzComposeAlertInstructionAccounts,
  createLzReceiveAlertInstruction: () => createLzReceiveAlertInstruction,
  createLzReceiveAlertInstructionAccounts: () => createLzReceiveAlertInstructionAccounts,
  createNilifyInstruction: () => createNilifyInstruction,
  createNilifyInstructionAccounts: () => createNilifyInstructionAccounts,
  createQuoteInstruction: () => createQuoteInstruction,
  createQuoteInstructionAccounts: () => createQuoteInstructionAccounts,
  createRegisterLibraryInstruction: () => createRegisterLibraryInstruction,
  createRegisterLibraryInstructionAccounts: () => createRegisterLibraryInstructionAccounts,
  createRegisterOappInstruction: () => createRegisterOappInstruction,
  createRegisterOappInstructionAccounts: () => createRegisterOappInstructionAccounts,
  createSendComposeInstruction: () => createSendComposeInstruction,
  createSendComposeInstructionAccounts: () => createSendComposeInstructionAccounts,
  createSendInstruction: () => createSendInstruction,
  createSendInstructionAccounts: () => createSendInstructionAccounts,
  createSetConfigInstruction: () => createSetConfigInstruction,
  createSetConfigInstructionAccounts: () => createSetConfigInstructionAccounts,
  createSetDefaultReceiveLibraryInstruction: () => createSetDefaultReceiveLibraryInstruction,
  createSetDefaultReceiveLibraryInstructionAccounts: () => createSetDefaultReceiveLibraryInstructionAccounts,
  createSetDefaultReceiveLibraryTimeoutInstruction: () => createSetDefaultReceiveLibraryTimeoutInstruction,
  createSetDefaultReceiveLibraryTimeoutInstructionAccounts: () => createSetDefaultReceiveLibraryTimeoutInstructionAccounts,
  createSetDefaultSendLibraryInstruction: () => createSetDefaultSendLibraryInstruction,
  createSetDefaultSendLibraryInstructionAccounts: () => createSetDefaultSendLibraryInstructionAccounts,
  createSetDelegateInstruction: () => createSetDelegateInstruction,
  createSetDelegateInstructionAccounts: () => createSetDelegateInstructionAccounts,
  createSetLzTokenInstruction: () => createSetLzTokenInstruction,
  createSetLzTokenInstructionAccounts: () => createSetLzTokenInstructionAccounts,
  createSetReceiveLibraryInstruction: () => createSetReceiveLibraryInstruction,
  createSetReceiveLibraryInstructionAccounts: () => createSetReceiveLibraryInstructionAccounts,
  createSetReceiveLibraryTimeoutInstruction: () => createSetReceiveLibraryTimeoutInstruction,
  createSetReceiveLibraryTimeoutInstructionAccounts: () => createSetReceiveLibraryTimeoutInstructionAccounts,
  createSetSendLibraryInstruction: () => createSetSendLibraryInstruction,
  createSetSendLibraryInstructionAccounts: () => createSetSendLibraryInstructionAccounts,
  createSkipInstruction: () => createSkipInstruction,
  createSkipInstructionAccounts: () => createSkipInstructionAccounts,
  createTransferAdminInstruction: () => createTransferAdminInstruction,
  createTransferAdminInstructionAccounts: () => createTransferAdminInstructionAccounts,
  createVerifyInstruction: () => createVerifyInstruction,
  createVerifyInstructionAccounts: () => createVerifyInstructionAccounts,
  createWithdrawRentInstruction: () => createWithdrawRentInstruction,
  createWithdrawRentInstructionAccounts: () => createWithdrawRentInstructionAccounts,
  initConfigInstructionDiscriminator: () => initConfigInstructionDiscriminator,
  initConfigStruct: () => initConfigStruct,
  initDefaultReceiveLibraryInstructionDiscriminator: () => initDefaultReceiveLibraryInstructionDiscriminator,
  initDefaultReceiveLibraryStruct: () => initDefaultReceiveLibraryStruct,
  initDefaultSendLibraryInstructionDiscriminator: () => initDefaultSendLibraryInstructionDiscriminator,
  initDefaultSendLibraryStruct: () => initDefaultSendLibraryStruct,
  initEndpointInstructionDiscriminator: () => initEndpointInstructionDiscriminator,
  initEndpointStruct: () => initEndpointStruct,
  initNonceInstructionDiscriminator: () => initNonceInstructionDiscriminator,
  initNonceStruct: () => initNonceStruct,
  initReceiveLibraryInstructionDiscriminator: () => initReceiveLibraryInstructionDiscriminator,
  initReceiveLibraryStruct: () => initReceiveLibraryStruct,
  initSendLibraryInstructionDiscriminator: () => initSendLibraryInstructionDiscriminator,
  initSendLibraryStruct: () => initSendLibraryStruct,
  initVerifyInstructionDiscriminator: () => initVerifyInstructionDiscriminator,
  initVerifyStruct: () => initVerifyStruct,
  lzComposeAlertInstructionDiscriminator: () => lzComposeAlertInstructionDiscriminator,
  lzComposeAlertStruct: () => lzComposeAlertStruct,
  lzReceiveAlertInstructionDiscriminator: () => lzReceiveAlertInstructionDiscriminator,
  lzReceiveAlertStruct: () => lzReceiveAlertStruct,
  nilifyInstructionDiscriminator: () => nilifyInstructionDiscriminator,
  nilifyStruct: () => nilifyStruct,
  quoteInstructionDiscriminator: () => quoteInstructionDiscriminator,
  quoteStruct: () => quoteStruct,
  registerLibraryInstructionDiscriminator: () => registerLibraryInstructionDiscriminator,
  registerLibraryStruct: () => registerLibraryStruct,
  registerOappInstructionDiscriminator: () => registerOappInstructionDiscriminator,
  registerOappStruct: () => registerOappStruct,
  sendComposeInstructionDiscriminator: () => sendComposeInstructionDiscriminator,
  sendComposeStruct: () => sendComposeStruct,
  sendInstructionDiscriminator: () => sendInstructionDiscriminator,
  sendStruct: () => sendStruct,
  setConfigInstructionDiscriminator: () => setConfigInstructionDiscriminator,
  setConfigStruct: () => setConfigStruct,
  setDefaultReceiveLibraryInstructionDiscriminator: () => setDefaultReceiveLibraryInstructionDiscriminator,
  setDefaultReceiveLibraryStruct: () => setDefaultReceiveLibraryStruct,
  setDefaultReceiveLibraryTimeoutInstructionDiscriminator: () => setDefaultReceiveLibraryTimeoutInstructionDiscriminator,
  setDefaultReceiveLibraryTimeoutStruct: () => setDefaultReceiveLibraryTimeoutStruct,
  setDefaultSendLibraryInstructionDiscriminator: () => setDefaultSendLibraryInstructionDiscriminator,
  setDefaultSendLibraryStruct: () => setDefaultSendLibraryStruct,
  setDelegateInstructionDiscriminator: () => setDelegateInstructionDiscriminator,
  setDelegateStruct: () => setDelegateStruct,
  setLzTokenInstructionDiscriminator: () => setLzTokenInstructionDiscriminator,
  setLzTokenStruct: () => setLzTokenStruct,
  setReceiveLibraryInstructionDiscriminator: () => setReceiveLibraryInstructionDiscriminator,
  setReceiveLibraryStruct: () => setReceiveLibraryStruct,
  setReceiveLibraryTimeoutInstructionDiscriminator: () => setReceiveLibraryTimeoutInstructionDiscriminator,
  setReceiveLibraryTimeoutStruct: () => setReceiveLibraryTimeoutStruct,
  setSendLibraryInstructionDiscriminator: () => setSendLibraryInstructionDiscriminator,
  setSendLibraryStruct: () => setSendLibraryStruct,
  skipInstructionDiscriminator: () => skipInstructionDiscriminator,
  skipStruct: () => skipStruct,
  transferAdminInstructionDiscriminator: () => transferAdminInstructionDiscriminator,
  transferAdminStruct: () => transferAdminStruct,
  verifyInstructionDiscriminator: () => verifyInstructionDiscriminator,
  verifyStruct: () => verifyStruct,
  withdrawRentInstructionDiscriminator: () => withdrawRentInstructionDiscriminator,
  withdrawRentStruct: () => withdrawRentStruct
});
var burnParamsBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["nonce", beet159.u64],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)]
  ],
  "BurnParams"
);

// src/generated/solita/endpoint/instructions/burn.ts
var burnStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", burnParamsBeet]
  ],
  "BurnInstructionArgs"
);
var burnInstructionDiscriminator = [116, 110, 29, 56, 107, 219, 42, 93];
function createBurnInstruction(accounts, args, programId) {
  const [data] = burnStruct.serialize({
    instructionDiscriminator: burnInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createBurnInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var clearParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["nonce", beet159.u64],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["message", beet159.bytes]
  ],
  "ClearParams"
);

// src/generated/solita/endpoint/instructions/clear.ts
var clearStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", clearParamsBeet]
  ],
  "ClearInstructionArgs"
);
var clearInstructionDiscriminator = [
  250,
  39,
  28,
  213,
  123,
  163,
  133,
  5
];
function createClearInstruction(accounts, args, programId) {
  const [data] = clearStruct.serialize({
    instructionDiscriminator: clearInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createClearInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var clearComposeParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["from", beetSolana96.publicKey],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["index", beet159.u16],
    ["message", beet159.bytes]
  ],
  "ClearComposeParams"
);

// src/generated/solita/endpoint/instructions/clearCompose.ts
var clearComposeStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", clearComposeParamsBeet]
  ],
  "ClearComposeInstructionArgs"
);
var clearComposeInstructionDiscriminator = [
  118,
  1,
  18,
  142,
  95,
  175,
  21,
  125
];
function createClearComposeInstruction(accounts, args, programId) {
  const [data] = clearComposeStruct.serialize({
    instructionDiscriminator: clearComposeInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.to,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.composeMessage,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createClearComposeInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.to,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.composeMessage,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initConfigParamsBeet = new beet159.BeetArgsStruct(
  [
    ["oapp", beetSolana96.publicKey],
    ["eid", beet159.u32]
  ],
  "InitConfigParams"
);

// src/generated/solita/endpoint/instructions/initConfig.ts
var initConfigStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initConfigParamsBeet]
  ],
  "InitConfigInstructionArgs"
);
var initConfigInstructionDiscriminator = [
  23,
  235,
  115,
  232,
  168,
  96,
  1,
  231
];
function createInitConfigInstruction(accounts, args, programId) {
  const [data] = initConfigStruct.serialize({
    instructionDiscriminator: initConfigInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.delegate,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibProgram,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitConfigInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.delegate,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibProgram,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initDefaultReceiveLibraryParamsBeet = new beet159.BeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["newLib", beetSolana96.publicKey]
  ],
  "InitDefaultReceiveLibraryParams"
);

// src/generated/solita/endpoint/instructions/initDefaultReceiveLibrary.ts
var initDefaultReceiveLibraryStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initDefaultReceiveLibraryParamsBeet]
  ],
  "InitDefaultReceiveLibraryInstructionArgs"
);
var initDefaultReceiveLibraryInstructionDiscriminator = [
  32,
  202,
  76,
  22,
  42,
  249,
  227,
  109
];
function createInitDefaultReceiveLibraryInstruction(accounts, args, programId) {
  const [data] = initDefaultReceiveLibraryStruct.serialize({
    instructionDiscriminator: initDefaultReceiveLibraryInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitDefaultReceiveLibraryInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initDefaultSendLibraryParamsBeet = new beet159.BeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["newLib", beetSolana96.publicKey]
  ],
  "InitDefaultSendLibraryParams"
);

// src/generated/solita/endpoint/instructions/initDefaultSendLibrary.ts
var initDefaultSendLibraryStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initDefaultSendLibraryParamsBeet]
  ],
  "InitDefaultSendLibraryInstructionArgs"
);
var initDefaultSendLibraryInstructionDiscriminator = [
  120,
  187,
  15,
  31,
  174,
  97,
  138,
  58
];
function createInitDefaultSendLibraryInstruction(accounts, args, programId) {
  const [data] = initDefaultSendLibraryStruct.serialize({
    instructionDiscriminator: initDefaultSendLibraryInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitDefaultSendLibraryInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initEndpointParamsBeet = new beet159.BeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["admin", beetSolana96.publicKey]
  ],
  "InitEndpointParams"
);

// src/generated/solita/endpoint/instructions/initEndpoint.ts
var initEndpointStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initEndpointParamsBeet]
  ],
  "InitEndpointInstructionArgs"
);
var initEndpointInstructionDiscriminator = [
  178,
  30,
  29,
  207,
  120,
  225,
  246,
  134
];
function createInitEndpointInstruction(accounts, args, programId) {
  const [data] = initEndpointStruct.serialize({
    instructionDiscriminator: initEndpointInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitEndpointInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initNonceParamsBeet = new beet159.BeetArgsStruct(
  [
    ["localOapp", beetSolana96.publicKey],
    ["remoteEid", beet159.u32],
    ["remoteOapp", beet159.uniformFixedSizeArray(beet159.u8, 32)]
  ],
  "InitNonceParams"
);

// src/generated/solita/endpoint/instructions/initNonce.ts
var initNonceStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initNonceParamsBeet]
  ],
  "InitNonceInstructionArgs"
);
var initNonceInstructionDiscriminator = [
  204,
  171,
  16,
  214,
  182,
  191,
  27,
  196
];
function createInitNonceInstruction(accounts, args, programId) {
  const [data] = initNonceStruct.serialize({
    instructionDiscriminator: initNonceInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.delegate,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.pendingInboundNonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitNonceInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.delegate,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.pendingInboundNonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initReceiveLibraryParamsBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["eid", beet159.u32]
  ],
  "InitReceiveLibraryParams"
);

// src/generated/solita/endpoint/instructions/initReceiveLibrary.ts
var initReceiveLibraryStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initReceiveLibraryParamsBeet]
  ],
  "InitReceiveLibraryInstructionArgs"
);
var initReceiveLibraryInstructionDiscriminator = [
  197,
  114,
  81,
  100,
  45,
  233,
  36,
  230
];
function createInitReceiveLibraryInstruction(accounts, args, programId) {
  const [data] = initReceiveLibraryStruct.serialize({
    instructionDiscriminator: initReceiveLibraryInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.delegate,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.receiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitReceiveLibraryInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.delegate,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.receiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initSendLibraryParamsBeet = new beet159.BeetArgsStruct(
  [
    ["sender", beetSolana96.publicKey],
    ["eid", beet159.u32]
  ],
  "InitSendLibraryParams"
);

// src/generated/solita/endpoint/instructions/initSendLibrary.ts
var initSendLibraryStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initSendLibraryParamsBeet]
  ],
  "InitSendLibraryInstructionArgs"
);
var initSendLibraryInstructionDiscriminator = [
  156,
  24,
  235,
  120,
  73,
  193,
  144,
  19
];
function createInitSendLibraryInstruction(accounts, args, programId) {
  const [data] = initSendLibraryStruct.serialize({
    instructionDiscriminator: initSendLibraryInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.delegate,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitSendLibraryInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.delegate,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initVerifyParamsBeet = new beet159.BeetArgsStruct(
  [
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["receiver", beetSolana96.publicKey],
    ["nonce", beet159.u64]
  ],
  "InitVerifyParams"
);

// src/generated/solita/endpoint/instructions/initVerify.ts
var initVerifyStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initVerifyParamsBeet]
  ],
  "InitVerifyInstructionArgs"
);
var initVerifyInstructionDiscriminator = [
  76,
  246,
  244,
  124,
  115,
  17,
  235,
  91
];
function createInitVerifyInstruction(accounts, args, programId) {
  const [data] = initVerifyStruct.serialize({
    instructionDiscriminator: initVerifyInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitVerifyInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var lzComposeAlertParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["from", beetSolana96.publicKey],
    ["to", beetSolana96.publicKey],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["index", beet159.u16],
    ["computeUnits", beet159.u64],
    ["value", beet159.u64],
    ["message", beet159.bytes],
    ["extraData", beet159.bytes],
    ["reason", beet159.bytes]
  ],
  "LzComposeAlertParams"
);

// src/generated/solita/endpoint/instructions/lzComposeAlert.ts
var lzComposeAlertStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", lzComposeAlertParamsBeet]
  ],
  "LzComposeAlertInstructionArgs"
);
var lzComposeAlertInstructionDiscriminator = [
  27,
  91,
  198,
  77,
  66,
  92,
  122,
  167
];
function createLzComposeAlertInstruction(accounts, args, programId) {
  const [data] = lzComposeAlertStruct.serialize({
    instructionDiscriminator: lzComposeAlertInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.executor,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createLzComposeAlertInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.executor,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var lzReceiveAlertParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["nonce", beet159.u64],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["computeUnits", beet159.u64],
    ["value", beet159.u64],
    ["message", beet159.bytes],
    ["extraData", beet159.bytes],
    ["reason", beet159.bytes]
  ],
  "LzReceiveAlertParams"
);

// src/generated/solita/endpoint/instructions/lzReceiveAlert.ts
var lzReceiveAlertStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", lzReceiveAlertParamsBeet]
  ],
  "LzReceiveAlertInstructionArgs"
);
var lzReceiveAlertInstructionDiscriminator = [
  131,
  141,
  48,
  222,
  15,
  235,
  141,
  160
];
function createLzReceiveAlertInstruction(accounts, args, programId) {
  const [data] = lzReceiveAlertStruct.serialize({
    instructionDiscriminator: lzReceiveAlertInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.executor,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createLzReceiveAlertInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.executor,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var nilifyParamsBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["nonce", beet159.u64],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)]
  ],
  "NilifyParams"
);

// src/generated/solita/endpoint/instructions/nilify.ts
var nilifyStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", nilifyParamsBeet]
  ],
  "NilifyInstructionArgs"
);
var nilifyInstructionDiscriminator = [
  143,
  136,
  129,
  199,
  36,
  35,
  160,
  85
];
function createNilifyInstruction(accounts, args, programId) {
  const [data] = nilifyStruct.serialize({
    instructionDiscriminator: nilifyInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.pendingInboundNonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createNilifyInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.pendingInboundNonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var quoteParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["sender", beetSolana96.publicKey],
    ["dstEid", beet159.u32],
    ["receiver", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["message", beet159.bytes],
    ["options", beet159.bytes],
    ["payInLzToken", beet159.bool]
  ],
  "QuoteParams"
);

// src/generated/solita/endpoint/instructions/quote.ts
var quoteStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", quoteParamsBeet]
  ],
  "QuoteInstructionArgs"
);
var quoteInstructionDiscriminator = [
  149,
  42,
  109,
  247,
  134,
  146,
  213,
  123
];
function createQuoteInstruction(accounts, args, programId) {
  const [data] = quoteStruct.serialize({
    instructionDiscriminator: quoteInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.sendLibraryProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createQuoteInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.sendLibraryProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var registerLibraryParamsBeet = new beet159.BeetArgsStruct(
  [
    ["libProgram", beetSolana96.publicKey],
    ["libType", messageLibTypeBeet]
  ],
  "RegisterLibraryParams"
);

// src/generated/solita/endpoint/instructions/registerLibrary.ts
var registerLibraryStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", registerLibraryParamsBeet]
  ],
  "RegisterLibraryInstructionArgs"
);
var registerLibraryInstructionDiscriminator = [
  23,
  171,
  28,
  116,
  111,
  193,
  238,
  142
];
function createRegisterLibraryInstruction(accounts, args, programId) {
  const [data] = registerLibraryStruct.serialize({
    instructionDiscriminator: registerLibraryInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createRegisterLibraryInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var registerOAppParamsBeet = new beet159.BeetArgsStruct(
  [["delegate", beetSolana96.publicKey]],
  "RegisterOAppParams"
);

// src/generated/solita/endpoint/instructions/registerOapp.ts
var registerOappStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", registerOAppParamsBeet]
  ],
  "RegisterOappInstructionArgs"
);
var registerOappInstructionDiscriminator = [
  129,
  89,
  71,
  68,
  11,
  82,
  210,
  125
];
function createRegisterOappInstruction(accounts, args, programId) {
  const [data] = registerOappStruct.serialize({
    instructionDiscriminator: registerOappInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.oapp,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createRegisterOappInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.oapp,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var sendParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["dstEid", beet159.u32],
    ["receiver", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["message", beet159.bytes],
    ["options", beet159.bytes],
    ["nativeFee", beet159.u64],
    ["lzTokenFee", beet159.u64]
  ],
  "SendParams"
);

// src/generated/solita/endpoint/instructions/send.ts
var sendStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", sendParamsBeet]
  ],
  "SendInstructionArgs"
);
var sendInstructionDiscriminator = [102, 251, 20, 187, 65, 75, 12, 69];
function createSendInstruction(accounts, args, programId) {
  const [data] = sendStruct.serialize({
    instructionDiscriminator: sendInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.sender,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.sendLibraryProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSendInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.sender,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.sendLibraryProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var sendComposeParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["to", beetSolana96.publicKey],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["index", beet159.u16],
    ["message", beet159.bytes]
  ],
  "SendComposeParams"
);

// src/generated/solita/endpoint/instructions/sendCompose.ts
var sendComposeStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", sendComposeParamsBeet]
  ],
  "SendComposeInstructionArgs"
);
var sendComposeInstructionDiscriminator = [
  75,
  38,
  228,
  168,
  43,
  39,
  238,
  229
];
function createSendComposeInstruction(accounts, args, programId) {
  const [data] = sendComposeStruct.serialize({
    instructionDiscriminator: sendComposeInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.from,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.composeMessage,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSendComposeInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.from,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.composeMessage,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setConfigParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["oapp", beetSolana96.publicKey],
    ["eid", beet159.u32],
    ["configType", beet159.u32],
    ["config", beet159.bytes]
  ],
  "SetConfigParams"
);

// src/generated/solita/endpoint/instructions/setConfig.ts
var setConfigStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setConfigParamsBeet]
  ],
  "SetConfigInstructionArgs"
);
var setConfigInstructionDiscriminator = [
  108,
  158,
  154,
  175,
  212,
  98,
  52,
  66
];
function createSetConfigInstruction(accounts, args, programId) {
  const [data] = setConfigStruct.serialize({
    instructionDiscriminator: setConfigInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibProgram,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetConfigInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibProgram,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setDefaultReceiveLibraryParamsBeet = new beet159.BeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["newLib", beetSolana96.publicKey],
    ["gracePeriod", beet159.u64]
  ],
  "SetDefaultReceiveLibraryParams"
);

// src/generated/solita/endpoint/instructions/setDefaultReceiveLibrary.ts
var setDefaultReceiveLibraryStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setDefaultReceiveLibraryParamsBeet]
  ],
  "SetDefaultReceiveLibraryInstructionArgs"
);
var setDefaultReceiveLibraryInstructionDiscriminator = [
  14,
  162,
  167,
  212,
  13,
  20,
  151,
  129
];
function createSetDefaultReceiveLibraryInstruction(accounts, args, programId) {
  const [data] = setDefaultReceiveLibraryStruct.serialize({
    instructionDiscriminator: setDefaultReceiveLibraryInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetDefaultReceiveLibraryInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setDefaultReceiveLibraryTimeoutParamsBeet = new beet159.BeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["lib", beetSolana96.publicKey],
    ["expiry", beet159.u64]
  ],
  "SetDefaultReceiveLibraryTimeoutParams"
);

// src/generated/solita/endpoint/instructions/setDefaultReceiveLibraryTimeout.ts
var setDefaultReceiveLibraryTimeoutStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setDefaultReceiveLibraryTimeoutParamsBeet]
  ],
  "SetDefaultReceiveLibraryTimeoutInstructionArgs"
);
var setDefaultReceiveLibraryTimeoutInstructionDiscriminator = [
  118,
  32,
  94,
  51,
  25,
  247,
  16,
  232
];
function createSetDefaultReceiveLibraryTimeoutInstruction(accounts, args, programId) {
  const [data] = setDefaultReceiveLibraryTimeoutStruct.serialize({
    instructionDiscriminator: setDefaultReceiveLibraryTimeoutInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetDefaultReceiveLibraryTimeoutInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setDefaultSendLibraryParamsBeet = new beet159.BeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["newLib", beetSolana96.publicKey]
  ],
  "SetDefaultSendLibraryParams"
);

// src/generated/solita/endpoint/instructions/setDefaultSendLibrary.ts
var setDefaultSendLibraryStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setDefaultSendLibraryParamsBeet]
  ],
  "SetDefaultSendLibraryInstructionArgs"
);
var setDefaultSendLibraryInstructionDiscriminator = [
  220,
  215,
  110,
  127,
  237,
  178,
  215,
  170
];
function createSetDefaultSendLibraryInstruction(accounts, args, programId) {
  const [data] = setDefaultSendLibraryStruct.serialize({
    instructionDiscriminator: setDefaultSendLibraryInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetDefaultSendLibraryInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setDelegateParamsBeet = new beet159.BeetArgsStruct(
  [["delegate", beetSolana96.publicKey]],
  "SetDelegateParams"
);

// src/generated/solita/endpoint/instructions/setDelegate.ts
var setDelegateStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setDelegateParamsBeet]
  ],
  "SetDelegateInstructionArgs"
);
var setDelegateInstructionDiscriminator = [
  242,
  30,
  46,
  76,
  108,
  235,
  128,
  181
];
function createSetDelegateInstruction(accounts, args, programId) {
  const [data] = setDelegateStruct.serialize({
    instructionDiscriminator: setDelegateInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.oapp,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetDelegateInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.oapp,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setLzTokenParamsBeet = new beet159.FixableBeetArgsStruct(
  [["lzToken", beet159.coption(beetSolana96.publicKey)]],
  "SetLzTokenParams"
);

// src/generated/solita/endpoint/instructions/setLzToken.ts
var setLzTokenStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setLzTokenParamsBeet]
  ],
  "SetLzTokenInstructionArgs"
);
var setLzTokenInstructionDiscriminator = [
  22,
  151,
  112,
  174,
  213,
  225,
  223,
  72
];
function createSetLzTokenInstruction(accounts, args, programId) {
  const [data] = setLzTokenStruct.serialize({
    instructionDiscriminator: setLzTokenInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetLzTokenInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setReceiveLibraryParamsBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["eid", beet159.u32],
    ["newLib", beetSolana96.publicKey],
    ["gracePeriod", beet159.u64]
  ],
  "SetReceiveLibraryParams"
);

// src/generated/solita/endpoint/instructions/setReceiveLibrary.ts
var setReceiveLibraryStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setReceiveLibraryParamsBeet]
  ],
  "SetReceiveLibraryInstructionArgs"
);
var setReceiveLibraryInstructionDiscriminator = [
  223,
  172,
  180,
  105,
  165,
  161,
  147,
  228
];
function createSetReceiveLibraryInstruction(accounts, args, programId) {
  const [data] = setReceiveLibraryStruct.serialize({
    instructionDiscriminator: setReceiveLibraryInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.receiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo ?? programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetReceiveLibraryInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.receiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo ?? programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setReceiveLibraryTimeoutParamsBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["eid", beet159.u32],
    ["lib", beetSolana96.publicKey],
    ["expiry", beet159.u64]
  ],
  "SetReceiveLibraryTimeoutParams"
);

// src/generated/solita/endpoint/instructions/setReceiveLibraryTimeout.ts
var setReceiveLibraryTimeoutStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setReceiveLibraryTimeoutParamsBeet]
  ],
  "SetReceiveLibraryTimeoutInstructionArgs"
);
var setReceiveLibraryTimeoutInstructionDiscriminator = [
  33,
  159,
  29,
  53,
  218,
  156,
  41,
  192
];
function createSetReceiveLibraryTimeoutInstruction(accounts, args, programId) {
  const [data] = setReceiveLibraryTimeoutStruct.serialize({
    instructionDiscriminator: setReceiveLibraryTimeoutInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.receiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetReceiveLibraryTimeoutInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.receiveLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setSendLibraryParamsBeet = new beet159.BeetArgsStruct(
  [
    ["sender", beetSolana96.publicKey],
    ["eid", beet159.u32],
    ["newLib", beetSolana96.publicKey]
  ],
  "SetSendLibraryParams"
);

// src/generated/solita/endpoint/instructions/setSendLibrary.ts
var setSendLibraryStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setSendLibraryParamsBeet]
  ],
  "SetSendLibraryInstructionArgs"
);
var setSendLibraryInstructionDiscriminator = [
  251,
  118,
  78,
  158,
  134,
  149,
  129,
  5
];
function createSetSendLibraryInstruction(accounts, args, programId) {
  const [data] = setSendLibraryStruct.serialize({
    instructionDiscriminator: setSendLibraryInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo ?? programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetSendLibraryInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendLibraryConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibInfo ?? programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var skipParamsBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["nonce", beet159.u64]
  ],
  "SkipParams"
);

// src/generated/solita/endpoint/instructions/skip.ts
var skipStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", skipParamsBeet]
  ],
  "SkipInstructionArgs"
);
var skipInstructionDiscriminator = [154, 63, 181, 53, 19, 26, 117, 45];
function createSkipInstruction(accounts, args, programId) {
  const [data] = skipStruct.serialize({
    instructionDiscriminator: skipInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.pendingInboundNonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSkipInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.oappRegistry,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.pendingInboundNonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var transferAdminParamsBeet = new beet159.BeetArgsStruct(
  [["admin", beetSolana96.publicKey]],
  "TransferAdminParams"
);

// src/generated/solita/endpoint/instructions/transferAdmin.ts
var transferAdminStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", transferAdminParamsBeet]
  ],
  "TransferAdminInstructionArgs"
);
var transferAdminInstructionDiscriminator = [
  42,
  242,
  66,
  106,
  228,
  10,
  111,
  156
];
function createTransferAdminInstruction(accounts, args, programId) {
  const [data] = transferAdminStruct.serialize({
    instructionDiscriminator: transferAdminInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createTransferAdminInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var verifyParamsBeet = new beet159.BeetArgsStruct(
  [
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["receiver", beetSolana96.publicKey],
    ["nonce", beet159.u64],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)]
  ],
  "VerifyParams"
);

// src/generated/solita/endpoint/instructions/verify.ts
var verifyStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", verifyParamsBeet]
  ],
  "VerifyInstructionArgs"
);
var verifyInstructionDiscriminator = [
  133,
  161,
  141,
  48,
  120,
  198,
  88,
  150
];
function createVerifyInstruction(accounts, args, programId) {
  const [data] = verifyStruct.serialize({
    instructionDiscriminator: verifyInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.receiveLibrary,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.receiveLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.pendingInboundNonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createVerifyInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.receiveLibrary,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.receiveLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveLibraryConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.nonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.pendingInboundNonce,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var withdrawRentParamsBeet = new beet159.BeetArgsStruct(
  [["amount", beet159.u64]],
  "WithdrawRentParams"
);

// src/generated/solita/endpoint/instructions/withdrawRent.ts
var withdrawRentStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", withdrawRentParamsBeet]
  ],
  "WithdrawRentInstructionArgs"
);
var withdrawRentInstructionDiscriminator = [
  226,
  7,
  41,
  158,
  173,
  111,
  192,
  107
];
function createWithdrawRentInstruction(accounts, args, programId) {
  const [data] = withdrawRentStruct.serialize({
    instructionDiscriminator: withdrawRentInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiver,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createWithdrawRentInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.endpoint,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiver,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}

// src/generated/solita/endpoint/types/index.ts
var types_exports = {};
__export(types_exports, {
  MessageLibType: () => MessageLibType,
  burnParamsBeet: () => burnParamsBeet,
  clearComposeParamsBeet: () => clearComposeParamsBeet,
  clearParamsBeet: () => clearParamsBeet,
  initConfigParamsBeet: () => initConfigParamsBeet,
  initDefaultReceiveLibraryParamsBeet: () => initDefaultReceiveLibraryParamsBeet,
  initDefaultSendLibraryParamsBeet: () => initDefaultSendLibraryParamsBeet,
  initEndpointParamsBeet: () => initEndpointParamsBeet,
  initNonceParamsBeet: () => initNonceParamsBeet,
  initReceiveLibraryParamsBeet: () => initReceiveLibraryParamsBeet,
  initSendLibraryParamsBeet: () => initSendLibraryParamsBeet,
  initVerifyParamsBeet: () => initVerifyParamsBeet,
  lzComposeAlertParamsBeet: () => lzComposeAlertParamsBeet,
  lzReceiveAlertParamsBeet: () => lzReceiveAlertParamsBeet,
  messageLibTypeBeet: () => messageLibTypeBeet,
  messagingFeeBeet: () => messagingFeeBeet,
  messagingReceiptBeet: () => messagingReceiptBeet,
  nilifyParamsBeet: () => nilifyParamsBeet,
  quoteParamsBeet: () => quoteParamsBeet,
  receiveLibraryTimeoutBeet: () => receiveLibraryTimeoutBeet,
  registerLibraryParamsBeet: () => registerLibraryParamsBeet,
  registerOAppParamsBeet: () => registerOAppParamsBeet,
  sendComposeParamsBeet: () => sendComposeParamsBeet,
  sendParamsBeet: () => sendParamsBeet,
  setConfigParamsBeet: () => setConfigParamsBeet,
  setDefaultReceiveLibraryParamsBeet: () => setDefaultReceiveLibraryParamsBeet,
  setDefaultReceiveLibraryTimeoutParamsBeet: () => setDefaultReceiveLibraryTimeoutParamsBeet,
  setDefaultSendLibraryParamsBeet: () => setDefaultSendLibraryParamsBeet,
  setDelegateParamsBeet: () => setDelegateParamsBeet,
  setLzTokenParamsBeet: () => setLzTokenParamsBeet,
  setReceiveLibraryParamsBeet: () => setReceiveLibraryParamsBeet,
  setReceiveLibraryTimeoutParamsBeet: () => setReceiveLibraryTimeoutParamsBeet,
  setSendLibraryParamsBeet: () => setSendLibraryParamsBeet,
  skipParamsBeet: () => skipParamsBeet,
  transferAdminParamsBeet: () => transferAdminParamsBeet,
  verifyParamsBeet: () => verifyParamsBeet,
  withdrawRentParamsBeet: () => withdrawRentParamsBeet
});
var messagingFeeBeet = new beet159.BeetArgsStruct(
  [
    ["nativeFee", beet159.u64],
    ["lzTokenFee", beet159.u64]
  ],
  "MessagingFee"
);
var messagingReceiptBeet = new beet159.BeetArgsStruct(
  [
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["nonce", beet159.u64],
    ["fee", messagingFeeBeet]
  ],
  "MessagingReceipt"
);

// src/generated/solita/endpoint/index.ts
var PROGRAM_ADDRESS = "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6";
var PROGRAM_ID = new PublicKey(PROGRAM_ADDRESS);

// src/generated/solita/endpoint/events/index.ts
var events_exports = {};
__export(events_exports, {
  adminTransferredEventBeet: () => adminTransferredEventBeet,
  composeDeliveredEventBeet: () => composeDeliveredEventBeet,
  composeSentEventBeet: () => composeSentEventBeet,
  defaultReceiveLibrarySetEventBeet: () => defaultReceiveLibrarySetEventBeet,
  defaultReceiveLibraryTimeoutSetEventBeet: () => defaultReceiveLibraryTimeoutSetEventBeet,
  defaultSendLibrarySetEventBeet: () => defaultSendLibrarySetEventBeet,
  delegateSetEventBeet: () => delegateSetEventBeet,
  inboundNonceSkippedEventBeet: () => inboundNonceSkippedEventBeet,
  libraryRegisteredEventBeet: () => libraryRegisteredEventBeet,
  lzComposeAlertEventBeet: () => lzComposeAlertEventBeet,
  lzReceiveAlertEventBeet: () => lzReceiveAlertEventBeet,
  lzTokenSetEventBeet: () => lzTokenSetEventBeet,
  oAppRegisteredEventBeet: () => oAppRegisteredEventBeet,
  packetBurntEventBeet: () => packetBurntEventBeet,
  packetDeliveredEventBeet: () => packetDeliveredEventBeet,
  packetNilifiedEventBeet: () => packetNilifiedEventBeet,
  packetSentEventBeet: () => packetSentEventBeet,
  packetVerifiedEventBeet: () => packetVerifiedEventBeet,
  receiveLibrarySetEventBeet: () => receiveLibrarySetEventBeet,
  receiveLibraryTimeoutSetEventBeet: () => receiveLibraryTimeoutSetEventBeet,
  rentWithdrawnEventBeet: () => rentWithdrawnEventBeet,
  sendLibrarySetEventBeet: () => sendLibrarySetEventBeet
});
var adminTransferredEventBeet = new beet159.BeetArgsStruct(
  [["newAdmin", beetSolana96.publicKey]],
  "AdminTransferredEvent"
);
var composeDeliveredEventBeet = new beet159.BeetArgsStruct(
  [
    ["from", beetSolana96.publicKey],
    ["to", beetSolana96.publicKey],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["index", beet159.u16]
  ],
  "ComposeDeliveredEvent"
);
var composeSentEventBeet = new beet159.FixableBeetArgsStruct(
  [
    ["from", beetSolana96.publicKey],
    ["to", beetSolana96.publicKey],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["index", beet159.u16],
    ["message", beet159.bytes]
  ],
  "ComposeSentEvent"
);
var defaultReceiveLibrarySetEventBeet = new beet159.BeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["newLib", beetSolana96.publicKey]
  ],
  "DefaultReceiveLibrarySetEvent"
);
var defaultReceiveLibraryTimeoutSetEventBeet = new beet159.FixableBeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["timeout", beet159.coption(receiveLibraryTimeoutBeet)]
  ],
  "DefaultReceiveLibraryTimeoutSetEvent"
);
var defaultSendLibrarySetEventBeet = new beet159.BeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["newLib", beetSolana96.publicKey]
  ],
  "DefaultSendLibrarySetEvent"
);
var delegateSetEventBeet = new beet159.BeetArgsStruct(
  [["newDelegate", beetSolana96.publicKey]],
  "DelegateSetEvent"
);
var inboundNonceSkippedEventBeet = new beet159.BeetArgsStruct(
  [
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["receiver", beetSolana96.publicKey],
    ["nonce", beet159.u64]
  ],
  "InboundNonceSkippedEvent"
);
var libraryRegisteredEventBeet = new beet159.BeetArgsStruct(
  [
    ["newLib", beetSolana96.publicKey],
    ["newLibProgram", beetSolana96.publicKey]
  ],
  "LibraryRegisteredEvent"
);
var lzComposeAlertEventBeet = new beet159.FixableBeetArgsStruct(
  [
    ["executor", beetSolana96.publicKey],
    ["from", beetSolana96.publicKey],
    ["to", beetSolana96.publicKey],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["index", beet159.u16],
    ["computeUnits", beet159.u64],
    ["value", beet159.u64],
    ["message", beet159.bytes],
    ["extraData", beet159.bytes],
    ["reason", beet159.bytes]
  ],
  "LzComposeAlertEvent"
);
var lzReceiveAlertEventBeet = new beet159.FixableBeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["executor", beetSolana96.publicKey],
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["nonce", beet159.u64],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["computeUnits", beet159.u64],
    ["value", beet159.u64],
    ["message", beet159.bytes],
    ["extraData", beet159.bytes],
    ["reason", beet159.bytes]
  ],
  "LzReceiveAlertEvent"
);
var lzTokenSetEventBeet = new beet159.FixableBeetArgsStruct(
  [["token", beet159.coption(beetSolana96.publicKey)]],
  "LzTokenSetEvent"
);
var oAppRegisteredEventBeet = new beet159.BeetArgsStruct(
  [
    ["oapp", beetSolana96.publicKey],
    ["delegate", beetSolana96.publicKey]
  ],
  "OAppRegisteredEvent"
);
var packetBurntEventBeet = new beet159.BeetArgsStruct(
  [
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["receiver", beetSolana96.publicKey],
    ["nonce", beet159.u64],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)]
  ],
  "PacketBurntEvent"
);
var packetDeliveredEventBeet = new beet159.BeetArgsStruct(
  [
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["receiver", beetSolana96.publicKey],
    ["nonce", beet159.u64]
  ],
  "PacketDeliveredEvent"
);
var packetNilifiedEventBeet = new beet159.BeetArgsStruct(
  [
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["receiver", beetSolana96.publicKey],
    ["nonce", beet159.u64],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)]
  ],
  "PacketNilifiedEvent"
);
var packetSentEventBeet = new beet159.FixableBeetArgsStruct(
  [
    ["encodedPacket", beet159.bytes],
    ["options", beet159.bytes],
    ["sendLibrary", beetSolana96.publicKey]
  ],
  "PacketSentEvent"
);
var packetVerifiedEventBeet = new beet159.BeetArgsStruct(
  [
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["receiver", beetSolana96.publicKey],
    ["nonce", beet159.u64],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)]
  ],
  "PacketVerifiedEvent"
);
var receiveLibrarySetEventBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["eid", beet159.u32],
    ["newLib", beetSolana96.publicKey]
  ],
  "ReceiveLibrarySetEvent"
);
var receiveLibraryTimeoutSetEventBeet = new beet159.FixableBeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["eid", beet159.u32],
    ["timeout", beet159.coption(receiveLibraryTimeoutBeet)]
  ],
  "ReceiveLibraryTimeoutSetEvent"
);
var rentWithdrawnEventBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["amount", beet159.u64]
  ],
  "RentWithdrawnEvent"
);
var sendLibrarySetEventBeet = new beet159.BeetArgsStruct(
  [
    ["sender", beetSolana96.publicKey],
    ["eid", beet159.u32],
    ["newLib", beetSolana96.publicKey]
  ],
  "SendLibrarySetEvent"
);

// src/solita/endpoint.ts
var EventEmitDiscriminator = "e445a52e51cb9a1d";
var DefaultMessageLib = PublicKey.default;
var Endpoint = class _Endpoint {
  /**
   * Creates an instance of the Endpoint class.
   *
   * @param {PublicKey} program - The program public key.
   */
  constructor(program) {
    this.program = program;
    this.deriver = new EndpointPDADeriver(program);
    const [eventAuthorityPDA] = new EventPDADeriver(program).eventAuthority();
    this.eventAuthorityPDA = eventAuthorityPDA;
  }
  /**
   * Initializes the endpoint settings, including eid and admin, and registers the blocked message library.
   *
   * @param {number} endpointId - The endpoint ID.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} admin - The admin public key.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  initEndpoint(endpointId, payer, admin) {
    const [settingPDA] = this.deriver.setting();
    return createInitEndpointInstruction(
      {
        payer,
        endpoint: settingPDA
      },
      {
        params: {
          eid: endpointId,
          admin
        }
      },
      this.program
    );
  }
  // async initOrUpdateConfig(connection:Connection,)
  /**
   * Registers a library.
   * call this function after endpoint initialized. Only admin can call this function.
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {types.MessageLibType} [libType=types.MessageLibType.SendAndReceive] - The library type.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  registerLibrary(admin, messageLibProgram, libType = 2 /* SendAndReceive */) {
    const [msgLibPda] = new MessageLibPDADeriver(messageLibProgram).messageLib();
    const [msgLibInfoPda] = this.deriver.messageLibraryInfo(msgLibPda);
    const [settingPDA] = this.deriver.setting();
    return createRegisterLibraryInstruction(
      {
        admin,
        endpoint: settingPDA,
        messageLibInfo: msgLibInfoPda,
        eventAuthority: this.eventAuthorityPDA,
        program: this.program
      },
      {
        params: {
          libProgram: messageLibProgram,
          libType
        }
      },
      this.program
    );
  }
  /**
   * Sets the default send library.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} admin - The admin public key.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<TransactionInstruction | null>} A promise that resolves to the transaction instruction or null if not needed.
   */
  async setDefaultSendLibrary(connection, admin, messageLibProgram, dstEid, commitmentOrConfig) {
    const [msgLib] = new MessageLibPDADeriver(messageLibProgram).messageLib();
    const [defaultSendLibConfig] = this.deriver.defaultSendLibraryConfig(dstEid);
    const [settingPDA] = this.deriver.setting();
    const [msgLibInfo] = this.deriver.messageLibraryInfo(msgLib);
    const info = await connection.getAccountInfo(defaultSendLibConfig, commitmentOrConfig);
    if (!info) {
      return createInitDefaultSendLibraryInstruction(
        {
          admin,
          endpoint: settingPDA,
          defaultSendLibraryConfig: defaultSendLibConfig,
          messageLibInfo: msgLibInfo,
          program: this.program,
          eventAuthority: this.eventAuthorityPDA
        },
        {
          params: {
            eid: dstEid,
            newLib: msgLib
          }
        },
        this.program
      );
    } else {
      const sendLibConfig = await SendLibraryConfig.fromAccountAddress(
        connection,
        defaultSendLibConfig,
        commitmentOrConfig
      );
      if (sendLibConfig.messageLib.toBase58() === msgLib.toBase58()) {
        return null;
      }
      return createSetDefaultSendLibraryInstruction(
        {
          admin,
          endpoint: settingPDA,
          defaultSendLibraryConfig: defaultSendLibConfig,
          messageLibInfo: msgLibInfo,
          program: this.program,
          eventAuthority: this.eventAuthorityPDA
        },
        {
          params: {
            eid: dstEid,
            newLib: msgLib
          }
        },
        this.program
      );
    }
  }
  /**
   * Sets the default receive library.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} admin - The admin public key.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<TransactionInstruction | null>} A promise that resolves to the transaction instruction or null if not needed.
   */
  async setDefaultReceiveLibrary(connection, admin, messageLibProgram, srcEid, commitmentOrConfig) {
    const [msgLib] = new MessageLibPDADeriver(messageLibProgram).messageLib();
    const [defaultReceiveLibConfig] = this.deriver.defaultReceiveLibraryConfig(srcEid);
    const [settingPDA] = this.deriver.setting();
    const [defaultMsgLibInfo] = this.deriver.messageLibraryInfo(msgLib);
    const info = await connection.getAccountInfo(defaultReceiveLibConfig, commitmentOrConfig);
    if (!info) {
      return createInitDefaultReceiveLibraryInstruction(
        {
          admin,
          endpoint: settingPDA,
          defaultReceiveLibraryConfig: defaultReceiveLibConfig,
          messageLibInfo: defaultMsgLibInfo,
          program: this.program,
          eventAuthority: this.eventAuthorityPDA
        },
        {
          params: {
            eid: srcEid,
            newLib: msgLib
          }
        },
        this.program
      );
    } else {
      const receiveLibConfig = await ReceiveLibraryConfig.fromAccountAddress(
        connection,
        defaultReceiveLibConfig,
        commitmentOrConfig
      );
      if (receiveLibConfig.messageLib.toBase58() === msgLib.toBase58()) {
        return null;
      }
      return createSetDefaultReceiveLibraryInstruction(
        {
          admin,
          endpoint: settingPDA,
          defaultReceiveLibraryConfig: defaultReceiveLibConfig,
          messageLibInfo: defaultMsgLibInfo,
          program: this.program,
          eventAuthority: this.eventAuthorityPDA
        },
        {
          params: {
            eid: srcEid,
            newLib: msgLib,
            gracePeriod: 0
            // TODO: set grace period
          }
        },
        this.program
      );
    }
  }
  /**
   * Initializes the OApp configuration.
   *
   * @param {PublicKey} delegate - The delegate public key.
   * @param {MessageLibInterface} msgLibSDK - The message library SDK.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} oappID - The OApp ID public key.
   * @param {number} eid - The endpoint ID.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  initOAppConfig(delegate, msgLibSDK, payer, oappID, eid) {
    const [msgLib] = new MessageLibPDADeriver(msgLibSDK.program).messageLib();
    const [oappRegistry] = this.deriver.oappRegistry(oappID);
    const [msgLibInfo] = this.deriver.messageLibraryInfo(msgLib);
    return createInitConfigInstruction(
      {
        delegate,
        oappRegistry,
        messageLib: msgLib,
        messageLibInfo: msgLibInfo,
        messageLibProgram: msgLibSDK.program,
        anchorRemainingAccounts: msgLibSDK.getInitConfigIXAccountMetaForCPI(payer, oappID, eid)
      },
      {
        params: {
          eid,
          oapp: oappID
        }
      },
      this.program
    );
  }
  /**
   * Initializes the OApp nonce.
   *
   * @param {PublicKey} delegate - The delegate public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {PublicKey} oappIDPDA - The OApp ID PDA.
   * @param {Uint8Array} remoteOappAddr - The remote OApp address.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  initOAppNonce(delegate, dstEid, oappIDPDA2, remoteOappAddr) {
    const [nonce] = this.deriver.nonce(oappIDPDA2, dstEid, remoteOappAddr);
    const [pendingNonce] = this.deriver.pendingNonce(oappIDPDA2, dstEid, remoteOappAddr);
    const [oappRegistry] = this.deriver.oappRegistry(oappIDPDA2);
    return createInitNonceInstruction(
      {
        nonce,
        pendingInboundNonce: pendingNonce,
        oappRegistry,
        delegate
      },
      {
        params: {
          localOapp: oappIDPDA2,
          remoteOapp: Array.from(remoteOappAddr),
          remoteEid: dstEid
        }
      },
      this.program
    );
  }
  /**
   * Initializes the send library.
   *
   * @param {PublicKey} delegate - The delegate public key.
   * @param {PublicKey} sender - The sender public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  initSendLibrary(delegate, sender, dstEid) {
    const [oappRegistry] = this.deriver.oappRegistry(sender);
    const [sendLibraryConfig] = this.deriver.sendLibraryConfig(sender, dstEid);
    return createInitSendLibraryInstruction(
      {
        delegate,
        oappRegistry,
        sendLibraryConfig
      },
      {
        params: {
          sender,
          eid: dstEid
        }
      },
      this.program
    );
  }
  /**
   * Sets the send library.
   *
   * @param {PublicKey} oappAdmin - The OApp admin public key.
   * @param {PublicKey} oappIDPDA - The OApp ID PDA.
   * @param {PublicKey} newSendLibProgram - The new send library program public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setSendLibrary(oappAdmin, oappIDPDA2, newSendLibProgram, dstEid) {
    const [newSendLib] = new MessageLibPDADeriver(newSendLibProgram).messageLib();
    const [sendLibraryConfig] = this.deriver.sendLibraryConfig(oappIDPDA2, dstEid);
    const [sendLibraryInfo] = this.deriver.messageLibraryInfo(newSendLib);
    const [oappRegistry] = this.deriver.oappRegistry(oappIDPDA2);
    const ix = createSetSendLibraryInstruction(
      {
        signer: oappAdmin,
        sendLibraryConfig,
        messageLibInfo: sendLibraryInfo,
        oappRegistry,
        program: this.program,
        eventAuthority: this.eventAuthorityPDA
      },
      {
        params: {
          sender: oappIDPDA2,
          eid: dstEid,
          newLib: newSendLib
        }
      },
      this.program
    );
    return ix;
  }
  /**
   * Initializes the receive library.
   *
   * @param {PublicKey} delegate - The delegate public key.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} srcEid - The source endpoint ID.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  initReceiveLibrary(delegate, receiver, srcEid) {
    const [oappRegistry] = this.deriver.oappRegistry(receiver);
    const [receiveLibraryConfig] = this.deriver.receiveLibraryConfig(receiver, srcEid);
    return createInitReceiveLibraryInstruction(
      {
        delegate,
        oappRegistry,
        receiveLibraryConfig
      },
      {
        params: {
          receiver,
          eid: srcEid
        }
      },
      this.program
    );
  }
  /**
   * Sets the receive library.
   *
   * @param {PublicKey} oappAdmin - The OApp admin public key.
   * @param {PublicKey} oappIDPDA - The OApp ID PDA.
   * @param {PublicKey} newReceiveLibProgram - The new receive library program public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {bigint | number} [gracePeriod] - The grace period.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setReceiveLibrary(oappAdmin, oappIDPDA2, newReceiveLibProgram, srcEid, gracePeriod) {
    const [newReceiveLib] = new MessageLibPDADeriver(newReceiveLibProgram).messageLib();
    const [receiveLibraryConfig] = this.deriver.receiveLibraryConfig(oappIDPDA2, srcEid);
    const [receiveLibraryInfo] = this.deriver.messageLibraryInfo(newReceiveLib);
    const [oappRegistry] = this.deriver.oappRegistry(oappIDPDA2);
    const ix = createSetReceiveLibraryInstruction(
      {
        signer: oappAdmin,
        receiveLibraryConfig,
        messageLibInfo: receiveLibraryInfo,
        oappRegistry,
        program: this.program,
        eventAuthority: this.eventAuthorityPDA
      },
      {
        params: {
          receiver: oappIDPDA2,
          eid: srcEid,
          newLib: newReceiveLib,
          gracePeriod: gracePeriod === void 0 ? 0 : new BN(gracePeriod.toString())
        }
      },
      this.program
    );
    return ix;
  }
  /**
   * Sets the OApp configuration.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} oappDelegate - The OApp delegate public key.
   * @param {PublicKey} oappID - The OApp ID public key.
   * @param {PublicKey} msgLibProgram - The message library program public key.
   * @param {number} eid - The endpoint ID.
   * @param {object} config - The configuration object.
   * @param {SetConfigType} config.configType - The configuration type.
   * @param {UlnProgram.types.ExecutorConfig | UlnProgram.types.UlnConfig} config.value - The configuration value.
   * @param {Commitment} [commitment='confirmed'] - The commitment level.
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   * @throws {Error} If the message library version is unsupported.
   */
  async setOappConfig(connection, oappDelegate, oappID, msgLibProgram, eid, config2, commitment = "confirmed") {
    const [msgLib] = new MessageLibPDADeriver(msgLibProgram).messageLib();
    const [msgLibInfo] = this.deriver.messageLibraryInfo(msgLib);
    const msgLibVersion = await this.getMessageLibVersion(connection, oappDelegate, msgLibProgram, commitment);
    let msgLibClient;
    if (msgLibVersion?.major.toString() === "0" && msgLibVersion.minor == 0 && msgLibVersion.endpointVersion == 2) {
      msgLibClient = new simple_message_lib_exports.SimpleMessageLib(msgLibProgram);
    } else if (msgLibVersion?.major.toString() === "3" && msgLibVersion.minor == 0 && msgLibVersion.endpointVersion == 2) {
      msgLibClient = new uln_exports.Uln(msgLibProgram);
    } else {
      throw new Error(
        `unsupported message lib version ${msgLibVersion?.major.toString()}.${msgLibVersion?.minor.toString()}`
      );
    }
    const [oappRegistry] = this.deriver.oappRegistry(oappID);
    const ix = createSetConfigInstruction(
      {
        signer: oappDelegate,
        messageLibInfo: msgLibInfo,
        messageLib: msgLib,
        messageLibProgram: msgLibProgram,
        oappRegistry,
        anchorRemainingAccounts: await msgLibClient.getSetConfigIXAccountMetaForCPI(this.program, oappID, eid)
      },
      {
        params: {
          eid,
          configType: config2.configType,
          config: msgLibProgram.toBase58() === simple_message_lib_exports.PROGRAM_ID.toBase58() ? new Uint8Array(10) : uln_exports.Uln.constructSetConfigData(config2.configType, config2.value),
          oapp: oappID
        }
      },
      this.program
    );
    return ix;
  }
  /// send a simulated transaction to the endpoint to get the fee for sending a message
  /**
   *
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the quote instruction.
   *
   * caculate the fee for sending a message with ULN:
   *  1. executorFee: feeForGas + feeForOptionType
   *      * feeForGas: ((gas + gasForCalldata) * gasPriceInUnit * priceRatio / priceRatioDenominator) * multiplier_bps / 10000
   *      * feeForOptionType: nativeDrop/lzReceive/lzCompose
   *  2. oracleFee: feeForGas
   *      * feeForGas: same above
   *
   *  The priceRatioDenominator is 10^20
   *  totalFee = executorFee + oracleFee * numOracles(requiredOracles + optionalOracles)
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PacketPath} path - The packet path.
   * @param {MessageLibInterface} msgLibProgram - The message library program.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   * @throws {Error} If the default send library is not initialized or the message library is blocked.
   */
  async getQuoteIXAccountMetaForCPI(connection, payer, path, msgLibProgram) {
    const { sender: sender_, dstEid, receiver: receiver_ } = path;
    const sender = new PublicKey(arrayify(sender_));
    const receiver = addressToBytes32(receiver_);
    const sendLibInfo = await this.getSendLibrary(connection, sender, dstEid);
    const { msgLib, programId: owner } = sendLibInfo;
    const [sendLibraryInfo] = this.deriver.messageLibraryInfo(msgLib);
    const remainingAccounts = await msgLibProgram.getQuoteIXAccountMetaForCPI(connection, payer, path);
    const [setting] = this.deriver.setting();
    const [defaultSendLibraryConfig] = this.deriver.defaultSendLibraryConfig(dstEid);
    const [sendLibraryConfig] = this.deriver.sendLibraryConfig(sender, dstEid);
    const [nonce] = this.deriver.nonce(sender, dstEid, receiver);
    const accounts = createQuoteInstructionAccounts(
      {
        sendLibraryConfig,
        sendLibraryProgram: owner,
        defaultSendLibraryConfig,
        endpoint: setting,
        nonce,
        sendLibraryInfo,
        anchorRemainingAccounts: remainingAccounts
      },
      this.program
    );
    return Promise.resolve(
      [
        {
          pubkey: this.program,
          isSigner: false,
          isWritable: false
        }
      ].concat(accounts)
    );
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the send instruction.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PacketPath} path - The packet path.
   * @param {MessageLibInterface} msgLibProgram - The message library program.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   * @throws {Error} If the default send library is not initialized or the message library is blocked.
   */
  async getSendIXAccountMetaForCPI(connection, payer, path, msgLibProgram, commitmentOrConfig) {
    const { sender: sender_, dstEid, receiver: receiver_ } = path;
    const sender = new PublicKey(arrayify(sender_));
    const receiver = addressToBytes32(receiver_);
    const info = await this.getSendLibrary(connection, sender, dstEid, commitmentOrConfig);
    const sendLibrary = info.msgLib;
    const [sendLibraryInfo] = this.deriver.messageLibraryInfo(sendLibrary);
    const remainingAccounts = await msgLibProgram.getSendIXAccountMetaForCPI(connection, payer, path);
    const [defaultSendLibraryConfig] = this.deriver.defaultSendLibraryConfig(dstEid);
    const [sendLibraryConfig] = this.deriver.sendLibraryConfig(sender, dstEid);
    const [nonce] = this.deriver.nonce(sender, dstEid, receiver);
    const accounts = createSendInstructionAccounts(
      {
        sender,
        //signer
        /// this account should be derived from message lib
        sendLibraryProgram: info.programId,
        sendLibraryConfig,
        defaultSendLibraryConfig,
        sendLibraryInfo,
        endpoint: this.deriver.setting()[0],
        program: this.program,
        nonce,
        eventAuthority: this.eventAuthorityPDA,
        anchorRemainingAccounts: remainingAccounts
      },
      this.program
    );
    accounts.forEach((item) => {
      item.isSigner = false;
    });
    return [
      {
        pubkey: this.program,
        isSigner: false,
        isWritable: false
      }
    ].concat(accounts);
  }
  /**
   * Skips a message.
   *
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} sender - The sender public key.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {string} nonce - The nonce.
   * @returns {Promise<TransactionInstruction | null>} A promise that resolves to the transaction instruction or null if not needed.
   */
  async skip(payer, sender, receiver, srcEid, nonce) {
    const [nonceAccount] = this.deriver.nonce(receiver, srcEid, addressToBytes32(sender.toBase58()));
    const [payloadHash] = this.deriver.payloadHash(receiver, srcEid, sender.toBytes(), parseInt(nonce));
    const [pendingInboundNonce] = this.deriver.pendingNonce(receiver, srcEid, sender.toBytes());
    const [oAppRegistry] = PublicKey.findProgramAddressSync(
      [Buffer.from(OAPP_SEED, "utf-8"), receiver.toBuffer()],
      this.program
    );
    const ix = createSkipInstruction(
      {
        signer: payer,
        oappRegistry: oAppRegistry,
        nonce: nonceAccount,
        pendingInboundNonce,
        payloadHash,
        endpoint: this.deriver.setting()[0],
        eventAuthority: this.eventAuthorityPDA,
        program: this.program
      },
      {
        params: {
          receiver,
          srcEid,
          sender: Array.from(sender.toBytes()),
          nonce: new BN(nonce)
        }
      },
      this.program
    );
    return Promise.resolve(ix);
  }
  /**
   * Initializes the verification process.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} sender - The sender public key.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {string} nonce - The nonce.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<TransactionInstruction | null>} A promise that resolves to the transaction instruction or null if not needed.
   */
  async initVerify(connection, payer, sender, receiver, srcEid, nonce, commitmentOrConfig) {
    const [nonceAccount] = this.deriver.nonce(receiver, srcEid, addressToBytes32(sender.toBase58()));
    const [payloadHash] = this.deriver.payloadHash(receiver, srcEid, sender.toBytes(), parseInt(nonce));
    const payloadHashInfo = await connection.getAccountInfo(payloadHash, commitmentOrConfig);
    if (payloadHashInfo) {
      return null;
    }
    return createInitVerifyInstruction(
      {
        payer,
        nonce: nonceAccount,
        payloadHash
      },
      {
        params: {
          srcEid,
          sender: Array.from(sender.toBytes()),
          receiver,
          nonce: new BN(nonce)
        }
      },
      this.program
    );
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the verify instruction.
   *
   * @param {PacketV1Codec} packet - The packet.
   * @param {PublicKey} receiveLibrary - The receive library public key.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getVerifyIXAccountMetaForCPI(packet, receiveLibrary) {
    const receiver = new PublicKey(Buffer.from(packet.receiver().slice(2), "hex"));
    const srcEid = packet.srcEid();
    const sender = addressToBytes32(packet.sender());
    const [payloadHash] = this.deriver.payloadHash(receiver, srcEid, sender, parseInt(packet.nonce()));
    const [defaultReceiveLibraryConfig] = this.deriver.defaultReceiveLibraryConfig(srcEid);
    const [receiveLibraryConfig] = this.deriver.receiveLibraryConfig(receiver, srcEid);
    const [nonce] = this.deriver.nonce(receiver, srcEid, sender);
    const [pendingNonce] = this.deriver.pendingNonce(receiver, srcEid, sender);
    const accounts = createVerifyInstructionAccounts(
      {
        receiveLibrary,
        receiveLibraryConfig,
        defaultReceiveLibraryConfig,
        nonce,
        pendingInboundNonce: pendingNonce,
        payloadHash,
        program: this.program,
        eventAuthority: this.eventAuthorityPDA
      },
      this.program
    );
    accounts.forEach((key) => {
      key.isSigner = false;
    });
    const program = {
      pubkey: this.program,
      isSigner: false,
      isWritable: false
    };
    return [program, ...accounts];
  }
  /***
   * resetConfig is only supported by ULN
   */
  // async getResetConfigIXAccountMetaForCPI(
  //     connection: Connection,
  //     msgLibProgram: PublicKey,
  //     oappID: PublicKey,
  //     eid: number,
  //     commitmentOrConfig: Commitment | GetAccountInfoConfig = 'confirmed'
  // ): Promise<AccountMeta[]> {
  //     const uln = new UlnProgram.Uln(msgLibProgram)
  //     const [msgLib] = uln.ulnDeriver.messageLib()
  //     const [msgLibInfo] = this.endpointDeriver.messageLibraryAuthority(msgLib)
  //     const ix = instructions.createResetConfigInstruction(
  //         {
  //             oapp: oappID,
  //             messageLibAuthority: msgLibInfo,
  //             messageLib: msgLib,
  //             anchorRemainingAccounts: await uln.getResetConfigIXAccountMetaForCPI(
  //                 connection,
  //                 this.program,
  //                 oappID,
  //                 eid,
  //                 commitmentOrConfig
  //             ),
  //         } as instructions.ResetConfigInstructionAccounts,
  //         // Fake params. Just to get the ix.keys
  //         {
  //             params: {
  //                 eid: eid,
  //                 messageLib: msgLib,
  //             } as types.ResetConfigParams,
  //         } as instructions.ResetConfigInstructionArgs
  //     )
  //     ix.keys.forEach((key) => {
  //         key.isSigner = false
  //     })
  //     return [
  //         {
  //             pubkey: this.program,
  //             isWritable: false,
  //             isSigner: false,
  //         } as AccountMeta,
  //     ].concat(ix.keys)
  // }
  /**
   * snapshotConfig is only supported by ULN
   */
  // async getSnapshotConfigIXAccountMetaForCPI(
  //     connection: Connection,
  //     msgLibProgram: PublicKey,
  //     oappID: PublicKey,
  //     eid: number,
  //     commitmentOrConfig: Commitment | GetAccountInfoConfig = 'confirmed'
  // ): Promise<AccountMeta[]> {
  //     const uln = new UlnProgram.Uln(msgLibProgram)
  //     const [msgLib] = uln.ulnDeriver.messageLib()
  //     const [msgLibInfo] = this.endpointDeriver.messageLibraryAuthority(msgLib)
  //     const ix = instructions.createSnapshotConfigInstruction(
  //         {
  //             oapp: oappID,
  //             messageLibAuthority: msgLibInfo,
  //             messageLib: msgLib,
  //             anchorRemainingAccounts: await uln.getSnapshotConfigIXAccountMetaForCPI(
  //                 connection,
  //                 this.program,
  //                 oappID,
  //                 eid,
  //                 commitmentOrConfig
  //             ),
  //         } satisfies instructions.SnapshotConfigInstructionAccounts,
  //         // Fake params. Just to get the ix.keys
  //         {
  //             params: {
  //                 eid: eid,
  //                 messageLib: msgLib,
  //             } as types.SnapshotConfigParams,
  //         } as instructions.SnapshotConfigInstructionArgs
  //     )
  //     ix.keys.forEach((key) => {
  //         key.isSigner = false
  //     })
  //     return [
  //         {
  //             pubkey: this.program,
  //             isWritable: false,
  //             isSigner: false,
  //         },
  //     ].concat(ix.keys)
  // }
  /**
   * @param composer. The composer is usually the PDA of the app's ID. It depends on the oapp's implementation.
   */
  // getDeliverComposedMessageIXAccountMetaForCPI(
  //     payer: PublicKey,
  //     composer: PublicKey,
  //     packet: PacketV1Codec
  // ): AccountMeta[] {
  //     const receiver = new PublicKey(Buffer.from(packet.receiver().slice(2), 'hex'))
  //     const guid = Uint8Array.from(Buffer.from(packet.guid().slice(2), 'hex'))
  //     const message = ethers.utils.arrayify(ethers.utils.keccak256(packet.message()))
  //     const ix = instructions.createDeliverComposedMessageInstruction(
  //         {
  //             payer,
  //             receiver,
  //             composedMessage: this.endpointDeriver.composedMessage(receiver, guid, composer, message)[0],
  //             endpoint: this.endpointDeriver.setting()[0],
  //             endpointProgram: this.program,
  //         } satisfies instructions.DeliverComposedMessageInstructionAccounts,
  //         // Fake params. Just to get the ix.keys
  //         {
  //             params: {
  //                 composer: payer,
  //                 guid: Array.from(guid),
  //                 message: Uint8Array.from([]),
  //             } as types.DeliverComposedMessageParams,
  //         } as instructions.DeliverComposedMessageInstructionArgs
  //     )
  //     ix.keys.forEach((key) => {
  //         key.isSigner = false
  //     })
  //     return [
  //         {
  //             pubkey: this.program,
  //             isWritable: false,
  //             isSigner: false,
  //         },
  //     ].concat(ix.keys)
  // }
  /**
   * @param composer. The composer is usually the PDA of the app's ID. It depends on the oapp's implementation.
   */
  // getClearComposedMessageIXAccountMetaForCPI(composer: PublicKey, packet: PacketV1Codec): AccountMeta[] {
  //     const receiver = new PublicKey(Buffer.from(packet.receiver().slice(2), 'hex'))
  //     const guid = Uint8Array.from(Buffer.from(packet.guid().slice(2), 'hex'))
  //     const message = ethers.utils.arrayify(ethers.utils.keccak256(packet.message()))
  //     const ix = instructions.createClearComposedMessageInstruction(
  //         {
  //             composer,
  //             composedMessage: this.endpointDeriver.composedMessage(receiver, guid, composer, message)[0],
  //             endpoint: this.endpointDeriver.setting()[0],
  //             endpointProgram: this.program,
  //         } satisfies instructions.ClearComposedMessageInstructionAccounts,
  //         // Fake params. Just to get the ix.keys
  //         {
  //             params: {
  //                 receiver: composer,
  //                 guid: Array.from(guid),
  //                 message: Uint8Array.from([]),
  //             } as types.ClearComposedMessageParams,
  //         } as instructions.ClearComposedMessageInstructionArgs
  //     )
  //     ix.keys.forEach((key) => {
  //         key.isSigner = false
  //     })
  //     return [
  //         {
  //             pubkey: this.program,
  //             isWritable: false,
  //             isSigner: false,
  //         } as AccountMeta,
  //     ].concat(ix.keys)
  // }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the register OApp instruction.
   *
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} oapp - The OApp public key.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getRegisterOappIxAccountMetaForCPI(payer, oapp) {
    const [oappRegistry] = this.deriver.oappRegistry(oapp);
    const eventAuthority = this.eventAuthorityPDA;
    const keys = createRegisterOappInstructionAccounts(
      {
        payer,
        oapp,
        oappRegistry,
        eventAuthority,
        program: this.program
      },
      this.program
    );
    keys.forEach((key) => {
      key.isSigner = false;
    });
    return [
      {
        pubkey: this.program,
        isSigner: false,
        isWritable: false
      }
    ].concat(keys);
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the skip instruction.
   *
   * @param {PublicKey} receiver - The receiver public key.
   * @param {Uint8Array} sender - The sender address.
   * @param {number} srcEid - The source endpoint ID.
   * @param {number} nonce - The nonce.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getSkipIxAccountMetaForCPI(receiver, sender, srcEid, nonce) {
    const [noncePDA] = this.deriver.nonce(receiver, srcEid, sender);
    const [pendingNonce] = this.deriver.pendingNonce(receiver, srcEid, sender);
    const [payloadHash] = this.deriver.payloadHash(receiver, srcEid, sender, nonce);
    const [oappRegistry] = this.deriver.oappRegistry(receiver);
    const keys = createSkipInstructionAccounts(
      {
        signer: receiver,
        oappRegistry,
        nonce: noncePDA,
        pendingInboundNonce: pendingNonce,
        payloadHash,
        endpoint: this.deriver.setting()[0],
        program: this.program,
        eventAuthority: this.eventAuthorityPDA
      },
      this.program
    );
    keys.forEach((key) => {
      key.isSigner = false;
    });
    return [
      {
        pubkey: this.program,
        isSigner: false,
        isWritable: false
      }
    ].concat(keys);
  }
  /**
   * Checks if the given message library program is the default send library.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<boolean>} A promise that resolves to true if the message library program is the default send library, false otherwise.
   */
  async isDefaultSendLibrary(connection, messageLibProgram, dstEid, commitmentOrConfig) {
    const [msgLib] = new MessageLibPDADeriver(messageLibProgram).messageLib();
    const info = await this.getDefaultSendLibrary(connection, dstEid, commitmentOrConfig);
    if (info) {
      return info.msgLib.equals(msgLib);
    }
    return false;
  }
  /**
   * Checks if the given message library program is the default receive library.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<boolean>} A promise that resolves to true if the message library program is the default receive library, false otherwise.
   */
  async isDefaultReceiveLibrary(connection, messageLibProgram, srcEid, commitmentOrConfig) {
    const [msgLib] = new MessageLibPDADeriver(messageLibProgram).messageLib();
    const info = await this.getDefaultReceiveLibrary(connection, srcEid, commitmentOrConfig);
    if (info) {
      return info.msgLib.equals(msgLib);
    }
    return false;
  }
  // all of below functions are retrieving accounts state
  /**
   * Gets the endpoint settings.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<EndpointSettings | null>} A promise that resolves to the endpoint settings or null if not found.
   */
  async getSetting(connection, commitmentOrConfig) {
    const [setting] = this.deriver.setting();
    try {
      return await EndpointSettings.fromAccountAddress(connection, setting, commitmentOrConfig);
    } catch (e) {
      return null;
    }
  }
  /**
   * Gets the message library information.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} messageLibProgram - The message library program public key, It is a PDA of the message library program
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<MessageLibInfo | null>} A promise that resolves to the message library information or null if not found.
   */
  async getMessageLibInfo(connection, messageLibProgram, commitmentOrConfig) {
    const [msgLib] = new MessageLibPDADeriver(messageLibProgram).messageLib();
    const [messageLibInfo] = this.deriver.messageLibraryInfo(msgLib);
    try {
      return await MessageLibInfo.fromAccountAddress(connection, messageLibInfo, commitmentOrConfig);
    } catch (e) {
      return null;
    }
  }
  /**
   * Gets the default receive library.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {number} srcEid - The source endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<{ msgLib: PublicKey; owner: PublicKey } | null>} A promise that resolves to the default receive library or null if not found.
   */
  async getDefaultReceiveLibrary(connection, srcEid, commitmentOrConfig) {
    const [defaultReceiveLibConfig] = this.deriver.defaultReceiveLibraryConfig(srcEid);
    try {
      const defaultInfo = await ReceiveLibraryConfig.fromAccountAddress(
        connection,
        defaultReceiveLibConfig,
        commitmentOrConfig
      );
      let msgLibProgram;
      if (_Endpoint.checkIfBlockedMessageLib(defaultInfo.messageLib)) {
        msgLibProgram = blocked_messagelib_exports.PROGRAM_ID;
      } else {
        const messageLibInfo = await connection.getAccountInfo(defaultInfo.messageLib, commitmentOrConfig);
        invariant6(messageLibInfo, "messageLibInfo should not be null");
        msgLibProgram = messageLibInfo.owner;
      }
      return {
        owner: msgLibProgram,
        msgLib: defaultInfo.messageLib
      };
    } catch (e) {
      return null;
    }
  }
  /**
   * Gets the default send library.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<{ msgLib: PublicKey; owner: PublicKey } | null>} A promise that resolves to the default send library or null if not found.
   */
  async getDefaultSendLibrary(connection, dstEid, commitmentOrConfig) {
    const [defaultSendLibConfig] = this.deriver.defaultSendLibraryConfig(dstEid);
    try {
      const defaultInfo = await SendLibraryConfig.fromAccountAddress(
        connection,
        defaultSendLibConfig,
        commitmentOrConfig
      );
      let msgLibProgram;
      if (_Endpoint.checkIfBlockedMessageLib(defaultInfo.messageLib)) {
        msgLibProgram = blocked_messagelib_exports.PROGRAM_ID;
      } else {
        const messageLibInfo = await connection.getAccountInfo(defaultInfo.messageLib, commitmentOrConfig);
        invariant6(messageLibInfo, "messageLibInfo should not be null");
        msgLibProgram = messageLibInfo.owner;
      }
      return {
        owner: msgLibProgram,
        msgLib: defaultInfo.messageLib
      };
    } catch (e) {
      return null;
    }
  }
  /**
   * Gets the configured send library for the app.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} oappPda - The OApp PDA.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<{ msgLib: PublicKey; programId: PublicKey; isDefault: boolean } | null>} A promise that resolves to the configured send library or null if not found.
   */
  async getSendLibrary(connection, oappPda, dstEid, commitmentOrConfig = "confirmed") {
    const [sendLibConfig] = this.deriver.sendLibraryConfig(oappPda, dstEid);
    const [defaultSendLibConfig] = this.deriver.defaultSendLibraryConfig(dstEid);
    const [defaultSendLibConfigBuf, sendLibConfigBuf] = await connection.getMultipleAccountsInfo(
      [defaultSendLibConfig, sendLibConfig],
      commitmentOrConfig
    );
    if (!defaultSendLibConfigBuf || !sendLibConfigBuf) {
      throw new Error(
        `Unable to find defaultSendLibraryConfig/sendLibraryConfig account at ${defaultSendLibConfig.toString()}/${sendLibConfig.toString()}`
      );
    }
    const [sendLibConfigInfo] = SendLibraryConfig.fromAccountInfo(sendLibConfigBuf, 0);
    const [defaultSendLibConfigInfo] = SendLibraryConfig.fromAccountInfo(defaultSendLibConfigBuf, 0);
    const msgLib = sendLibConfigInfo.messageLib.toString() === DefaultMessageLib.toString() ? defaultSendLibConfigInfo.messageLib : sendLibConfigInfo.messageLib;
    const isDefault = sendLibConfigInfo.messageLib.toString() === DefaultMessageLib.toString();
    let msgLibProgram;
    if (_Endpoint.checkIfBlockedMessageLib(msgLib)) {
      msgLibProgram = blocked_messagelib_exports.PROGRAM_ID;
    } else {
      const msgLibAccountInfo = await connection.getAccountInfo(msgLib, commitmentOrConfig);
      invariant6(msgLibAccountInfo, "msgLibAccountInfo should not be null");
      msgLibProgram = msgLibAccountInfo.owner;
    }
    return {
      programId: msgLibProgram,
      msgLib,
      isDefault
    };
  }
  /**
   * Gets the configured receive library for the app.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} oappPda - The OApp PDA.
   * @param {number} srcEid - The source endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<{ msgLib: PublicKey; programId?: PublicKey; isDefault: boolean; timeout: { msgLib: PublicKey; expiry: bigint } | null } | null>} A promise that resolves to the configured receive library or null if not found.
   */
  async getReceiveLibrary(connection, oappPda, srcEid, commitmentOrConfig) {
    const [receiveLibConfig] = this.deriver.receiveLibraryConfig(oappPda, srcEid);
    const [defaultReceiveLibConfig] = this.deriver.defaultReceiveLibraryConfig(srcEid);
    const [defaultReceiveLibConfigBuf, receiveLibConfigBuf] = await connection.getMultipleAccountsInfo(
      [defaultReceiveLibConfig, receiveLibConfig],
      commitmentOrConfig
    );
    if (!defaultReceiveLibConfigBuf || !receiveLibConfigBuf) {
      throw new Error(
        `Unable to find defaultReceiveLibraryConfig/receiveLibraryConfig account at ${defaultReceiveLibConfig.toString()}/${receiveLibConfig.toString()}`
      );
    }
    const [receiveLibConfigInfo] = ReceiveLibraryConfig.fromAccountInfo(receiveLibConfigBuf, 0);
    const [defaultReceiveLibConfigInfo] = ReceiveLibraryConfig.fromAccountInfo(
      defaultReceiveLibConfigBuf,
      0
    );
    const finalReceiveLibConfigInfo = receiveLibConfigInfo.messageLib.toString() === DefaultMessageLib.toString() ? defaultReceiveLibConfigInfo : receiveLibConfigInfo;
    const { messageLib, timeout } = finalReceiveLibConfigInfo;
    let msgLibProgram;
    if (_Endpoint.checkIfBlockedMessageLib(messageLib)) {
      msgLibProgram = blocked_messagelib_exports.PROGRAM_ID;
    } else {
      const msgLibAccountInfo = await connection.getAccountInfo(messageLib, commitmentOrConfig);
      invariant6(msgLibAccountInfo, "msgLibAccountInfo should not be null");
      msgLibProgram = msgLibAccountInfo.owner;
    }
    return {
      programId: msgLibProgram,
      msgLib: messageLib,
      isDefault: receiveLibConfigInfo.messageLib.toString() === DefaultMessageLib.toString(),
      timeout: timeout === null ? void 0 : { msgLib: timeout.messageLib, expiry: BigInt(timeout.expiry.toString()) }
    };
  }
  // rename to a more generic name
  /**
   * Gets the inbound payload hash.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {Uint8Array} sender - The sender address.
   * @param {number} nonce - The nonce.
   * @param {Uint8Array} _payloadHash - The payload hash.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<PayloadHash | null>} A promise that resolves to the payload hash or null if not found.
   */
  async getInboundPayloadHash(connection, receiver, srcEid, sender, nonce, _payloadHash, commitmentOrConfig) {
    const [payloadHashPDA] = this.deriver.payloadHash(receiver, srcEid, sender, nonce);
    const accountInfo = await connection.getAccountInfo(payloadHashPDA, commitmentOrConfig);
    if (!accountInfo) {
      return null;
    }
    return PayloadHash.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Gets the composed message state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} from - The sender public key.
   * @param {types.SendComposeParams} params - The send compose parameters.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<ComposeMessageState | null>} A promise that resolves to the composed message state or null if not found.
   */
  async getComposedMessageState(connection, from, params, commitmentOrConfig) {
    const message = keccak_256(params.message);
    const [composedMessagePDA] = this.deriver.composedMessage(
      from,
      Uint8Array.from(params.guid),
      params.index,
      params.to,
      message
    );
    const accountInfo = await connection.getAccountInfo(composedMessagePDA, commitmentOrConfig);
    if (!accountInfo) {
      return null;
    }
    return ComposeMessageState.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Gets the nonce.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} oappIDPDA - The OApp ID PDA.
   * @param {number} remoteEid - The remote endpoint ID.
   * @param {Uint8Array} remoteOappAddr - The remote OApp address.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.Nonce | null>} A promise that resolves to the nonce or null if not found.
   */
  async getNonce(connection, oappIDPDA2, remoteEid, remoteOappAddr, commitmentOrConfig) {
    const [nonce] = this.deriver.nonce(oappIDPDA2, remoteEid, remoteOappAddr);
    try {
      return await Nonce.fromAccountAddress(connection, nonce, commitmentOrConfig);
    } catch (e) {
      return null;
    }
  }
  /**
   * Gets the pending inbound nonce.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} oappIDPDA - The OApp ID PDA.
   * @param {number} remoteEid - The remote endpoint ID.
   * @param {Uint8Array} remoteOappAddr - The remote OApp address.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.PendingInboundNonce | null>} A promise that resolves to the pending inbound nonce or null if not found.
   */
  async getPendingInboundNonce(connection, oappIDPDA2, remoteEid, remoteOappAddr, commitmentOrConfig) {
    const [nonce] = this.deriver.pendingNonce(oappIDPDA2, remoteEid, remoteOappAddr);
    try {
      return await PendingInboundNonce.fromAccountAddress(connection, nonce, commitmentOrConfig);
    } catch (e) {
      return null;
    }
  }
  /**
   * Gets the message library version.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {Commitment} [commitment='confirmed'] - The commitment level.
   * @returns {Promise<Version | null>} A promise that resolves to the version or null if not found.
   */
  async getMessageLibVersion(connection, payer, messageLibProgram, commitment = "confirmed") {
    const ix = simple_message_lib_exports.instructions.createVersionInstruction(messageLibProgram);
    const simulateResp = await simulateTransaction(connection, [ix], messageLibProgram, payer, commitment);
    const version5 = simple_message_lib_exports.types.versionBeet.read(simulateResp, 0);
    return version5;
  }
  /**
   * Transfers the admin role to a new admin.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} admin - The current admin public key.
   * @param {PublicKey} newAdmin - The new admin public key.
   * @param {Commitment} [commitment='confirmed'] - The commitment level.
   * @returns {Promise<TransactionInstruction | null>} A promise that resolves to the transaction instruction or null if not needed.
   */
  async transferAdmin(connection, admin, newAdmin, commitment = "confirmed") {
    const [settingPDA] = this.deriver.setting();
    const endpointSettings = await EndpointSettings.fromAccountAddress(connection, settingPDA, commitment);
    if (endpointSettings.admin.toBase58() === newAdmin.toBase58()) {
      console.warn("endpoint admin not need change");
      return null;
    }
    return createTransferAdminInstruction(
      {
        admin,
        endpoint: settingPDA,
        eventAuthority: this.eventAuthorityPDA,
        program: this.program
      },
      {
        params: {
          admin: newAdmin
        }
      },
      this.program
    );
  }
  static checkIfBlockedMessageLib(msgLib) {
    const [blockedMessageLib] = new MessageLibPDADeriver(blocked_messagelib_exports.PROGRAM_ID).messageLib();
    return msgLib.toString() === blockedMessageLib.toString();
  }
};

// src/solita/simple-message-lib.ts
var simple_message_lib_exports = {};
__export(simple_message_lib_exports, {
  PROGRAM_ID: () => PROGRAM_ID2,
  SimpleMessageLib: () => SimpleMessageLib,
  accounts: () => accounts_exports2,
  errors: () => errors_exports2,
  instructions: () => instructions_exports2,
  types: () => types_exports2
});

// src/generated/solita/simple_messagelib/accounts/index.ts
var accounts_exports2 = {};
__export(accounts_exports2, {
  MessageLib: () => MessageLib,
  ReceiveConfigStore: () => ReceiveConfigStore,
  SendConfigStore: () => SendConfigStore,
  accountProviders: () => accountProviders2,
  messageLibBeet: () => messageLibBeet,
  messageLibDiscriminator: () => messageLibDiscriminator,
  receiveConfigStoreBeet: () => receiveConfigStoreBeet,
  receiveConfigStoreDiscriminator: () => receiveConfigStoreDiscriminator,
  sendConfigStoreBeet: () => sendConfigStoreBeet,
  sendConfigStoreDiscriminator: () => sendConfigStoreDiscriminator
});
var messageLibDiscriminator = [141, 191, 244, 48, 52, 174, 199, 209];
var MessageLib = class _MessageLib {
  constructor(eid, endpoint, endpointProgram, bump, admin, fee, lzTokenFee, wlCaller) {
    this.eid = eid;
    this.endpoint = endpoint;
    this.endpointProgram = endpointProgram;
    this.bump = bump;
    this.admin = admin;
    this.fee = fee;
    this.lzTokenFee = lzTokenFee;
    this.wlCaller = wlCaller;
  }
  /**
   * Creates a {@link MessageLib} instance from the provided args.
   */
  static fromArgs(args) {
    return new _MessageLib(
      args.eid,
      args.endpoint,
      args.endpointProgram,
      args.bump,
      args.admin,
      args.fee,
      args.lzTokenFee,
      args.wlCaller
    );
  }
  /**
   * Deserializes the {@link MessageLib} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _MessageLib.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link MessageLib} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find MessageLib account at ${address}`);
    }
    return _MessageLib.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, messageLibBeet);
  }
  /**
   * Deserializes the {@link MessageLib} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return messageLibBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link MessageLib} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return messageLibBeet.serialize({
      accountDiscriminator: messageLibDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link MessageLib}
   */
  static get byteSize() {
    return messageLibBeet.byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link MessageLib} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _MessageLib.byteSize,
      commitment
    );
  }
  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link MessageLib} data.
   */
  static hasCorrectByteSize(buf, offset = 0) {
    return buf.byteLength - offset === _MessageLib.byteSize;
  }
  /**
   * Returns a readable version of {@link MessageLib} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      eid: this.eid,
      endpoint: this.endpoint.toBase58(),
      endpointProgram: this.endpointProgram.toBase58(),
      bump: this.bump,
      admin: this.admin.toBase58(),
      fee: (() => {
        const x = this.fee;
        if (typeof x.toNumber === "function") {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      lzTokenFee: (() => {
        const x = this.lzTokenFee;
        if (typeof x.toNumber === "function") {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      wlCaller: this.wlCaller.toBase58()
    };
  }
};
var messageLibBeet = new beet159.BeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["eid", beet159.u32],
    ["endpoint", beetSolana96.publicKey],
    ["endpointProgram", beetSolana96.publicKey],
    ["bump", beet159.u8],
    ["admin", beetSolana96.publicKey],
    ["fee", beet159.u64],
    ["lzTokenFee", beet159.u64],
    ["wlCaller", beetSolana96.publicKey]
  ],
  MessageLib.fromArgs,
  "MessageLib"
);
var receiveConfigStoreDiscriminator = [70, 73, 30, 1, 248, 48, 93, 125];
var ReceiveConfigStore = class _ReceiveConfigStore {
  constructor(bump, data) {
    this.bump = bump;
    this.data = data;
  }
  /**
   * Creates a {@link ReceiveConfigStore} instance from the provided args.
   */
  static fromArgs(args) {
    return new _ReceiveConfigStore(args.bump, args.data);
  }
  /**
   * Deserializes the {@link ReceiveConfigStore} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _ReceiveConfigStore.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link ReceiveConfigStore} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find ReceiveConfigStore account at ${address}`);
    }
    return _ReceiveConfigStore.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, receiveConfigStoreBeet);
  }
  /**
   * Deserializes the {@link ReceiveConfigStore} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return receiveConfigStoreBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link ReceiveConfigStore} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return receiveConfigStoreBeet.serialize({
      accountDiscriminator: receiveConfigStoreDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link ReceiveConfigStore} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _ReceiveConfigStore.fromArgs(args);
    return receiveConfigStoreBeet.toFixedFromValue({
      accountDiscriminator: receiveConfigStoreDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link ReceiveConfigStore} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _ReceiveConfigStore.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link ReceiveConfigStore} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      bump: this.bump,
      data: this.data
    };
  }
};
var receiveConfigStoreBeet = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["bump", beet159.u8],
    ["data", beet159.bytes]
  ],
  ReceiveConfigStore.fromArgs,
  "ReceiveConfigStore"
);
var sendConfigStoreDiscriminator = [
  246,
  55,
  104,
  200,
  90,
  164,
  50,
  196
];
var SendConfigStore = class _SendConfigStore {
  constructor(bump, data) {
    this.bump = bump;
    this.data = data;
  }
  /**
   * Creates a {@link SendConfigStore} instance from the provided args.
   */
  static fromArgs(args) {
    return new _SendConfigStore(args.bump, args.data);
  }
  /**
   * Deserializes the {@link SendConfigStore} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _SendConfigStore.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link SendConfigStore} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find SendConfigStore account at ${address}`);
    }
    return _SendConfigStore.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, sendConfigStoreBeet);
  }
  /**
   * Deserializes the {@link SendConfigStore} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return sendConfigStoreBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link SendConfigStore} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return sendConfigStoreBeet.serialize({
      accountDiscriminator: sendConfigStoreDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link SendConfigStore} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _SendConfigStore.fromArgs(args);
    return sendConfigStoreBeet.toFixedFromValue({
      accountDiscriminator: sendConfigStoreDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link SendConfigStore} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _SendConfigStore.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link SendConfigStore} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      bump: this.bump,
      data: this.data
    };
  }
};
var sendConfigStoreBeet = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["bump", beet159.u8],
    ["data", beet159.bytes]
  ],
  SendConfigStore.fromArgs,
  "SendConfigStore"
);

// src/generated/solita/simple_messagelib/accounts/index.ts
var accountProviders2 = {
  MessageLib,
  ReceiveConfigStore,
  SendConfigStore
};

// src/generated/solita/simple_messagelib/errors/index.ts
var errors_exports2 = {};
__export(errors_exports2, {
  InsufficientFeeError: () => InsufficientFeeError,
  InvalidAmountError: () => InvalidAmountError2,
  InvalidConfigTypeError: () => InvalidConfigTypeError,
  InvalidLzTokenMintError: () => InvalidLzTokenMintError,
  LzTokenUnavailableError: () => LzTokenUnavailableError2,
  OnlyRevertError: () => OnlyRevertError,
  OnlyWhitelistedCallerError: () => OnlyWhitelistedCallerError,
  SendReentrancyError: () => SendReentrancyError,
  errorFromCode: () => errorFromCode2,
  errorFromName: () => errorFromName2
});
var createErrorFromCodeLookup2 = /* @__PURE__ */ new Map();
var createErrorFromNameLookup2 = /* @__PURE__ */ new Map();
var OnlyWhitelistedCallerError = class _OnlyWhitelistedCallerError extends Error {
  constructor() {
    super("");
    this.code = 6e3;
    this.name = "OnlyWhitelistedCaller";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _OnlyWhitelistedCallerError);
    }
  }
};
createErrorFromCodeLookup2.set(6e3, () => new OnlyWhitelistedCallerError());
createErrorFromNameLookup2.set(
  "OnlyWhitelistedCaller",
  () => new OnlyWhitelistedCallerError()
);
var InsufficientFeeError = class _InsufficientFeeError extends Error {
  constructor() {
    super("");
    this.code = 6001;
    this.name = "InsufficientFee";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InsufficientFeeError);
    }
  }
};
createErrorFromCodeLookup2.set(6001, () => new InsufficientFeeError());
createErrorFromNameLookup2.set(
  "InsufficientFee",
  () => new InsufficientFeeError()
);
var InvalidAmountError2 = class _InvalidAmountError extends Error {
  constructor() {
    super("");
    this.code = 6002;
    this.name = "InvalidAmount";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidAmountError);
    }
  }
};
createErrorFromCodeLookup2.set(6002, () => new InvalidAmountError2());
createErrorFromNameLookup2.set("InvalidAmount", () => new InvalidAmountError2());
var InvalidConfigTypeError = class _InvalidConfigTypeError extends Error {
  constructor() {
    super("");
    this.code = 6003;
    this.name = "InvalidConfigType";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidConfigTypeError);
    }
  }
};
createErrorFromCodeLookup2.set(6003, () => new InvalidConfigTypeError());
createErrorFromNameLookup2.set(
  "InvalidConfigType",
  () => new InvalidConfigTypeError()
);
var InvalidLzTokenMintError = class _InvalidLzTokenMintError extends Error {
  constructor() {
    super("");
    this.code = 6004;
    this.name = "InvalidLzTokenMint";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidLzTokenMintError);
    }
  }
};
createErrorFromCodeLookup2.set(6004, () => new InvalidLzTokenMintError());
createErrorFromNameLookup2.set(
  "InvalidLzTokenMint",
  () => new InvalidLzTokenMintError()
);
var LzTokenUnavailableError2 = class _LzTokenUnavailableError extends Error {
  constructor() {
    super("");
    this.code = 6005;
    this.name = "LzTokenUnavailable";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _LzTokenUnavailableError);
    }
  }
};
createErrorFromCodeLookup2.set(6005, () => new LzTokenUnavailableError2());
createErrorFromNameLookup2.set(
  "LzTokenUnavailable",
  () => new LzTokenUnavailableError2()
);
var SendReentrancyError = class _SendReentrancyError extends Error {
  constructor() {
    super("");
    this.code = 6006;
    this.name = "SendReentrancy";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _SendReentrancyError);
    }
  }
};
createErrorFromCodeLookup2.set(6006, () => new SendReentrancyError());
createErrorFromNameLookup2.set("SendReentrancy", () => new SendReentrancyError());
var OnlyRevertError = class _OnlyRevertError extends Error {
  constructor() {
    super("");
    this.code = 6007;
    this.name = "OnlyRevert";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _OnlyRevertError);
    }
  }
};
createErrorFromCodeLookup2.set(6007, () => new OnlyRevertError());
createErrorFromNameLookup2.set("OnlyRevert", () => new OnlyRevertError());
function errorFromCode2(code) {
  const createError = createErrorFromCodeLookup2.get(code);
  return createError != null ? createError() : null;
}
function errorFromName2(name) {
  const createError = createErrorFromNameLookup2.get(name);
  return createError != null ? createError() : null;
}

// src/generated/solita/simple_messagelib/instructions/index.ts
var instructions_exports2 = {};
__export(instructions_exports2, {
  createInitConfigInstruction: () => createInitConfigInstruction2,
  createInitConfigInstructionAccounts: () => createInitConfigInstructionAccounts2,
  createInitDefaultConfigInstruction: () => createInitDefaultConfigInstruction,
  createInitDefaultConfigInstructionAccounts: () => createInitDefaultConfigInstructionAccounts,
  createInitMessageLibInstruction: () => createInitMessageLibInstruction,
  createInitMessageLibInstructionAccounts: () => createInitMessageLibInstructionAccounts,
  createQuoteInstruction: () => createQuoteInstruction2,
  createQuoteInstructionAccounts: () => createQuoteInstructionAccounts2,
  createRevertCallInstruction: () => createRevertCallInstruction,
  createRevertCallInstructionAccounts: () => createRevertCallInstructionAccounts,
  createSendInstruction: () => createSendInstruction2,
  createSendInstructionAccounts: () => createSendInstructionAccounts2,
  createSendWithLzTokenInstruction: () => createSendWithLzTokenInstruction,
  createSendWithLzTokenInstructionAccounts: () => createSendWithLzTokenInstructionAccounts,
  createSetConfigInstruction: () => createSetConfigInstruction2,
  createSetConfigInstructionAccounts: () => createSetConfigInstructionAccounts2,
  createSetDefaultConfigInstruction: () => createSetDefaultConfigInstruction,
  createSetDefaultConfigInstructionAccounts: () => createSetDefaultConfigInstructionAccounts,
  createSetFeeInstruction: () => createSetFeeInstruction,
  createSetFeeInstructionAccounts: () => createSetFeeInstructionAccounts,
  createSetWlCallerInstruction: () => createSetWlCallerInstruction,
  createSetWlCallerInstructionAccounts: () => createSetWlCallerInstructionAccounts,
  createTransferAdminInstruction: () => createTransferAdminInstruction2,
  createTransferAdminInstructionAccounts: () => createTransferAdminInstructionAccounts2,
  createValidatePacketInstruction: () => createValidatePacketInstruction,
  createValidatePacketInstructionAccounts: () => createValidatePacketInstructionAccounts,
  createVersionInstruction: () => createVersionInstruction,
  createVersionInstructionAccounts: () => createVersionInstructionAccounts,
  createWithdrawFeesInstruction: () => createWithdrawFeesInstruction,
  createWithdrawFeesInstructionAccounts: () => createWithdrawFeesInstructionAccounts,
  initConfigInstructionDiscriminator: () => initConfigInstructionDiscriminator2,
  initConfigStruct: () => initConfigStruct2,
  initDefaultConfigInstructionDiscriminator: () => initDefaultConfigInstructionDiscriminator,
  initDefaultConfigStruct: () => initDefaultConfigStruct,
  initMessageLibInstructionDiscriminator: () => initMessageLibInstructionDiscriminator,
  initMessageLibStruct: () => initMessageLibStruct,
  quoteInstructionDiscriminator: () => quoteInstructionDiscriminator2,
  quoteStruct: () => quoteStruct2,
  revertCallInstructionDiscriminator: () => revertCallInstructionDiscriminator,
  revertCallStruct: () => revertCallStruct,
  sendInstructionDiscriminator: () => sendInstructionDiscriminator2,
  sendStruct: () => sendStruct2,
  sendWithLzTokenInstructionDiscriminator: () => sendWithLzTokenInstructionDiscriminator,
  sendWithLzTokenStruct: () => sendWithLzTokenStruct,
  setConfigInstructionDiscriminator: () => setConfigInstructionDiscriminator2,
  setConfigStruct: () => setConfigStruct2,
  setDefaultConfigInstructionDiscriminator: () => setDefaultConfigInstructionDiscriminator,
  setDefaultConfigStruct: () => setDefaultConfigStruct,
  setFeeInstructionDiscriminator: () => setFeeInstructionDiscriminator,
  setFeeStruct: () => setFeeStruct,
  setWlCallerInstructionDiscriminator: () => setWlCallerInstructionDiscriminator,
  setWlCallerStruct: () => setWlCallerStruct,
  transferAdminInstructionDiscriminator: () => transferAdminInstructionDiscriminator2,
  transferAdminStruct: () => transferAdminStruct2,
  validatePacketInstructionDiscriminator: () => validatePacketInstructionDiscriminator,
  validatePacketStruct: () => validatePacketStruct,
  versionInstructionDiscriminator: () => versionInstructionDiscriminator,
  versionStruct: () => versionStruct,
  withdrawFeesInstructionDiscriminator: () => withdrawFeesInstructionDiscriminator,
  withdrawFeesStruct: () => withdrawFeesStruct
});
var initConfigParamsBeet2 = new beet159.BeetArgsStruct(
  [
    ["oapp", beetSolana96.publicKey],
    ["eid", beet159.u32]
  ],
  "InitConfigParams"
);

// src/generated/solita/simple_messagelib/instructions/initConfig.ts
var initConfigStruct2 = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initConfigParamsBeet2]
  ],
  "InitConfigInstructionArgs"
);
var initConfigInstructionDiscriminator2 = [
  23,
  235,
  115,
  232,
  168,
  96,
  1,
  231
];
function createInitConfigInstruction2(accounts, args, programId) {
  const [data] = initConfigStruct2.serialize({
    instructionDiscriminator: initConfigInstructionDiscriminator2,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitConfigInstructionAccounts2(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initDefaultConfigParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["sendConfig", beet159.coption(beet159.bytes)],
    ["receiveConfig", beet159.coption(beet159.bytes)]
  ],
  "InitDefaultConfigParams"
);

// src/generated/solita/simple_messagelib/instructions/initDefaultConfig.ts
var initDefaultConfigStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initDefaultConfigParamsBeet]
  ],
  "InitDefaultConfigInstructionArgs"
);
var initDefaultConfigInstructionDiscriminator = [
  98,
  218,
  197,
  194,
  173,
  179,
  112,
  21
];
function createInitDefaultConfigInstruction(accounts, args, programId) {
  const [data] = initDefaultConfigStruct.serialize({
    instructionDiscriminator: initDefaultConfigInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitDefaultConfigInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initMessageLibParamsBeet = new beet159.BeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["endpoint", beetSolana96.publicKey],
    ["endpointProgram", beetSolana96.publicKey],
    ["admin", beetSolana96.publicKey],
    ["fee", beet159.u64],
    ["lzTokenFee", beet159.u64]
  ],
  "InitMessageLibParams"
);

// src/generated/solita/simple_messagelib/instructions/initMessageLib.ts
var initMessageLibStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initMessageLibParamsBeet]
  ],
  "InitMessageLibInstructionArgs"
);
var initMessageLibInstructionDiscriminator = [
  184,
  93,
  59,
  206,
  98,
  238,
  225,
  54
];
function createInitMessageLibInstruction(accounts, args, programId) {
  const [data] = initMessageLibStruct.serialize({
    instructionDiscriminator: initMessageLibInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitMessageLibInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var packetBeet = new beet159.FixableBeetArgsStruct(
  [
    ["nonce", beet159.u64],
    ["srcEid", beet159.u32],
    ["sender", beetSolana96.publicKey],
    ["dstEid", beet159.u32],
    ["receiver", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["message", beet159.bytes]
  ],
  "Packet"
);

// src/generated/solita/simple_messagelib/types/QuoteParams.ts
var quoteParamsBeet2 = new beet159.FixableBeetArgsStruct(
  [
    ["packet", packetBeet],
    ["options", beet159.bytes],
    ["payInLzToken", beet159.bool]
  ],
  "QuoteParams"
);

// src/generated/solita/simple_messagelib/instructions/quote.ts
var quoteStruct2 = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", quoteParamsBeet2]
  ],
  "QuoteInstructionArgs"
);
var quoteInstructionDiscriminator2 = [
  149,
  42,
  109,
  247,
  134,
  146,
  213,
  123
];
function createQuoteInstruction2(accounts, args, programId) {
  const [data] = quoteStruct2.serialize({
    instructionDiscriminator: quoteInstructionDiscriminator2,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createQuoteInstructionAccounts2(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var revertCallStruct = new beet159.BeetArgsStruct(
  [["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)]],
  "RevertCallInstructionArgs"
);
var revertCallInstructionDiscriminator = [
  124,
  243,
  145,
  116,
  106,
  203,
  93,
  114
];
function createRevertCallInstruction(programId) {
  const [data] = revertCallStruct.serialize({
    instructionDiscriminator: revertCallInstructionDiscriminator
  });
  const keys = [];
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createRevertCallInstructionAccounts(programId) {
  const keys = [];
  return keys;
}
var sendParamsBeet2 = new beet159.FixableBeetArgsStruct(
  [
    ["packet", packetBeet],
    ["options", beet159.bytes],
    ["nativeFee", beet159.u64]
  ],
  "SendParams"
);

// src/generated/solita/simple_messagelib/instructions/send.ts
var sendStruct2 = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", sendParamsBeet2]
  ],
  "SendInstructionArgs"
);
var sendInstructionDiscriminator2 = [102, 251, 20, 187, 65, 75, 12, 69];
function createSendInstruction2(accounts, args, programId) {
  const [data] = sendStruct2.serialize({
    instructionDiscriminator: sendInstructionDiscriminator2,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSendInstructionAccounts2(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var sendWithLzTokenParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["packet", packetBeet],
    ["options", beet159.bytes],
    ["nativeFee", beet159.u64],
    ["lzTokenFee", beet159.u64],
    ["lzTokenMint", beetSolana96.publicKey]
  ],
  "SendWithLzTokenParams"
);

// src/generated/solita/simple_messagelib/instructions/sendWithLzToken.ts
var sendWithLzTokenStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", sendWithLzTokenParamsBeet]
  ],
  "SendWithLzTokenInstructionArgs"
);
var sendWithLzTokenInstructionDiscriminator = [
  165,
  161,
  84,
  48,
  129,
  26,
  193,
  19
];
function createSendWithLzTokenInstruction(accounts, args, programId) {
  const [data] = sendWithLzTokenStruct.serialize({
    instructionDiscriminator: sendWithLzTokenInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibLzToken,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.lzTokenSource,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.lzTokenMint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.tokenProgram ?? splToken.TOKEN_PROGRAM_ID,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSendWithLzTokenInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.messageLibLzToken,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.lzTokenSource,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.lzTokenMint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.tokenProgram ?? splToken.TOKEN_PROGRAM_ID,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setConfigParamsBeet2 = new beet159.FixableBeetArgsStruct(
  [
    ["oapp", beetSolana96.publicKey],
    ["eid", beet159.u32],
    ["configType", beet159.u32],
    ["config", beet159.bytes]
  ],
  "SetConfigParams"
);

// src/generated/solita/simple_messagelib/instructions/setConfig.ts
var setConfigStruct2 = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setConfigParamsBeet2]
  ],
  "SetConfigInstructionArgs"
);
var setConfigInstructionDiscriminator2 = [
  108,
  158,
  154,
  175,
  212,
  98,
  52,
  66
];
function createSetConfigInstruction2(accounts, args, programId) {
  const [data] = setConfigStruct2.serialize({
    instructionDiscriminator: setConfigInstructionDiscriminator2,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetConfigInstructionAccounts2(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setDefaultConfigParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["sendConfig", beet159.coption(beet159.bytes)],
    ["receiveConfig", beet159.coption(beet159.bytes)]
  ],
  "SetDefaultConfigParams"
);

// src/generated/solita/simple_messagelib/instructions/setDefaultConfig.ts
var setDefaultConfigStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setDefaultConfigParamsBeet]
  ],
  "SetDefaultConfigInstructionArgs"
);
var setDefaultConfigInstructionDiscriminator = [
  111,
  228,
  17,
  75,
  5,
  76,
  213,
  169
];
function createSetDefaultConfigInstruction(accounts, args, programId) {
  const [data] = setDefaultConfigStruct.serialize({
    instructionDiscriminator: setDefaultConfigInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetDefaultConfigInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setFeeParamsBeet = new beet159.BeetArgsStruct(
  [
    ["fee", beet159.u64],
    ["lzTokenFee", beet159.u64]
  ],
  "SetFeeParams"
);

// src/generated/solita/simple_messagelib/instructions/setFee.ts
var setFeeStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setFeeParamsBeet]
  ],
  "SetFeeInstructionArgs"
);
var setFeeInstructionDiscriminator = [
  18,
  154,
  24,
  18,
  237,
  214,
  19,
  80
];
function createSetFeeInstruction(accounts, args, programId) {
  const [data] = setFeeStruct.serialize({
    instructionDiscriminator: setFeeInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetFeeInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setWlCallerParamsBeet = new beet159.BeetArgsStruct(
  [["newCaller", beetSolana96.publicKey]],
  "SetWlCallerParams"
);

// src/generated/solita/simple_messagelib/instructions/setWlCaller.ts
var setWlCallerStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setWlCallerParamsBeet]
  ],
  "SetWlCallerInstructionArgs"
);
var setWlCallerInstructionDiscriminator = [
  153,
  41,
  240,
  37,
  126,
  107,
  14,
  253
];
function createSetWlCallerInstruction(accounts, args, programId) {
  const [data] = setWlCallerStruct.serialize({
    instructionDiscriminator: setWlCallerInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetWlCallerInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var transferAdminParamsBeet2 = new beet159.BeetArgsStruct(
  [["admin", beetSolana96.publicKey]],
  "TransferAdminParams"
);

// src/generated/solita/simple_messagelib/instructions/transferAdmin.ts
var transferAdminStruct2 = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", transferAdminParamsBeet2]
  ],
  "TransferAdminInstructionArgs"
);
var transferAdminInstructionDiscriminator2 = [
  42,
  242,
  66,
  106,
  228,
  10,
  111,
  156
];
function createTransferAdminInstruction2(accounts, args, programId) {
  const [data] = transferAdminStruct2.serialize({
    instructionDiscriminator: transferAdminInstructionDiscriminator2,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createTransferAdminInstructionAccounts2(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var validatePacketParamsBeet = new beet159.FixableBeetArgsStruct(
  [["packet", beet159.bytes]],
  "ValidatePacketParams"
);

// src/generated/solita/simple_messagelib/instructions/validatePacket.ts
var validatePacketStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", validatePacketParamsBeet]
  ],
  "ValidatePacketInstructionArgs"
);
var validatePacketInstructionDiscriminator = [
  34,
  146,
  107,
  76,
  157,
  98,
  105,
  211
];
function createValidatePacketInstruction(accounts, args, programId) {
  const [data] = validatePacketStruct.serialize({
    instructionDiscriminator: validatePacketInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.receiveLibrary,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createValidatePacketInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.receiveLibrary,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var versionStruct = new beet159.BeetArgsStruct(
  [["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)]],
  "VersionInstructionArgs"
);
var versionInstructionDiscriminator = [
  118,
  65,
  195,
  198,
  129,
  216,
  252,
  192
];
function createVersionInstruction(programId) {
  const [data] = versionStruct.serialize({
    instructionDiscriminator: versionInstructionDiscriminator
  });
  const keys = [];
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createVersionInstructionAccounts(programId) {
  const keys = [];
  return keys;
}
var withdrawFeesParamsBeet = new beet159.BeetArgsStruct(
  [["amount", beet159.u64]],
  "WithdrawFeesParams"
);

// src/generated/solita/simple_messagelib/instructions/withdrawFees.ts
var withdrawFeesStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", withdrawFeesParamsBeet]
  ],
  "WithdrawFeesInstructionArgs"
);
var withdrawFeesInstructionDiscriminator = [
  198,
  212,
  171,
  109,
  144,
  215,
  174,
  89
];
function createWithdrawFeesInstruction(accounts, args, programId) {
  const [data] = withdrawFeesStruct.serialize({
    instructionDiscriminator: withdrawFeesInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiver,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createWithdrawFeesInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.messageLib,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiver,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}

// src/generated/solita/simple_messagelib/types/index.ts
var types_exports2 = {};
__export(types_exports2, {
  initConfigParamsBeet: () => initConfigParamsBeet2,
  initDefaultConfigParamsBeet: () => initDefaultConfigParamsBeet,
  initMessageLibParamsBeet: () => initMessageLibParamsBeet,
  messagingFeeBeet: () => messagingFeeBeet2,
  packetBeet: () => packetBeet,
  quoteParamsBeet: () => quoteParamsBeet2,
  sendParamsBeet: () => sendParamsBeet2,
  sendWithLzTokenParamsBeet: () => sendWithLzTokenParamsBeet,
  setConfigParamsBeet: () => setConfigParamsBeet2,
  setDefaultConfigParamsBeet: () => setDefaultConfigParamsBeet,
  setFeeParamsBeet: () => setFeeParamsBeet,
  setWlCallerParamsBeet: () => setWlCallerParamsBeet,
  transferAdminParamsBeet: () => transferAdminParamsBeet2,
  validatePacketParamsBeet: () => validatePacketParamsBeet,
  versionBeet: () => versionBeet,
  withdrawFeesParamsBeet: () => withdrawFeesParamsBeet
});
var messagingFeeBeet2 = new beet159.BeetArgsStruct(
  [
    ["nativeFee", beet159.u64],
    ["lzTokenFee", beet159.u64]
  ],
  "MessagingFee"
);
var versionBeet = new beet159.BeetArgsStruct(
  [
    ["major", beet159.u64],
    ["minor", beet159.u8],
    ["endpointVersion", beet159.u8]
  ],
  "Version"
);
var PROGRAM_ADDRESS2 = "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy";
var PROGRAM_ID2 = new PublicKey(PROGRAM_ADDRESS2);

// src/solita/simple-message-lib.ts
var SimpleMessageLib = class {
  /**
   * Creates an instance of the SimpleMessageLib class.
   *
   * @param {PublicKey} program - The program public key.
   */
  constructor(program) {
    this.program = program;
    this.deriver = new MessageLibPDADeriver(program);
  }
  /**
   * Initializes the SimpleMessageLib.
   *
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} admin - The admin public key.
   * @param {number} eid - The endpoint ID.
   * @param {number} nativeFee - The native fee.
   * @param {number} [lzTokenFee=0] - The LayerZero token fee.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  initSimpleMessageLib(endpointProgram, payer, admin, eid, nativeFee, lzTokenFee = 0) {
    const [messageLibPda] = this.deriver.messageLib();
    const [endpointAuth] = new EndpointPDADeriver(endpointProgram).messageLibraryInfo(messageLibPda);
    return createInitMessageLibInstruction(
      {
        payer,
        messageLib: messageLibPda
      },
      {
        params: {
          endpointProgram,
          endpoint: endpointAuth,
          eid,
          admin,
          fee: nativeFee,
          lzTokenFee
        }
      },
      this.program
    );
  }
  /**
   * Sets the whitelist caller.
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {PublicKey} newCaller - The new caller public key.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setWhitelistCaller(admin, newCaller) {
    const [messageLibPDA] = this.deriver.messageLib();
    return createSetWlCallerInstruction(
      {
        admin,
        messageLib: messageLibPDA
      },
      {
        params: {
          newCaller
        }
      },
      this.program
    );
  }
  /**
   * Checks if a caller is whitelisted.
   *
   * @param {Connection} connection - The connection object.
   * @param {PublicKey} caller - The caller public key.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment or config.
   *
   * @returns {Promise<boolean>} A promise that resolves to true if the caller is whitelisted, false otherwise.
   */
  async isWhiteListed(connection, caller, commitmentOrConfig) {
    try {
      const exceptedCaller = await this.getWhiteListCaller(connection, commitmentOrConfig);
      return exceptedCaller.equals(caller);
    } catch (e) {
      return false;
    }
  }
  /**
   * Gets the whitelisted caller.
   *
   * @param {Connection} connection - The connection object.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment or config.
   *
   * @returns {Promise<PublicKey>} A promise that resolves to the whitelisted caller public key.
   */
  async getWhiteListCaller(connection, commitmentOrConfig) {
    const [messageLibPDA] = this.deriver.messageLib();
    const messageLibInfo = await MessageLib.fromAccountAddress(
      connection,
      messageLibPDA,
      commitmentOrConfig
    );
    return messageLibInfo.wlCaller;
  }
  /**
   * Validates a packet.
   *
   * @param {Connection} _connection - The connection object.
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {PublicKey} payer - The payer public key.
   * @param {Uint8Array} encodedPacket - The encoded packet.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  validatePacket(_connection, endpointProgram, payer, encodedPacket) {
    const packet = PacketV1Codec.fromBytes(encodedPacket);
    const [receiveLibrary] = this.deriver.messageLib();
    const endpoint = new endpoint_exports.Endpoint(endpointProgram);
    return createValidatePacketInstruction(
      {
        payer,
        receiveLibrary,
        anchorRemainingAccounts: endpoint.getVerifyIXAccountMetaForCPI(packet, receiveLibrary)
      },
      {
        params: { packet: encodedPacket }
      },
      this.program
    );
  }
  /**
   * Gets the account meta of the send instruction for CPI (Cross-Program Invocation).
   *
   * @param {Connection} _connection - The connection object.
   * @param {PublicKey} payer - The payer public key.
   * @param {PacketPath} _path - The packet path.
   *
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account meta.
   */
  async getSendIXAccountMetaForCPI(_connection, payer, _path) {
    const [msgLib] = this.deriver.messageLib();
    const cpiAccounts = createSendInstructionAccounts2(
      {
        endpoint: PublicKey.default,
        // useless
        messageLib: msgLib,
        payer
      },
      this.program
    );
    cpiAccounts.forEach((key) => {
      if (!payer.equals(key.pubkey)) {
        key.isSigner = false;
      }
    });
    return Promise.resolve(cpiAccounts.slice(1));
  }
  /**
   * Gets the account meta of the quote instruction for CPI (Cross-Program Invocation).
   *
   * @param {Connection} _connection - The connection object.
   * @param {PublicKey} _payer - The payer public key.
   * @param {PacketPath} _path - The packet path.
   * @param {Commitment} [commitment] - The commitment.
   *
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account meta.
   */
  async getQuoteIXAccountMetaForCPI(_connection, _payer, _path, _commitment) {
    const [msgLib] = this.deriver.messageLib();
    const keys = createQuoteInstructionAccounts2(
      {
        endpoint: PublicKey.default,
        // useless
        messageLib: msgLib
      },
      this.program
    );
    return Promise.resolve(keys.slice(1));
  }
  /**
   * Gets the account meta of the init config instruction for CPI (Cross-Program Invocation).
   *
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} oappID - The oapp ID public key.
   * @param {number} eid - The endpoint ID.
   *
   * @returns {AccountMeta[]} An array of account meta.
   */
  getInitConfigIXAccountMetaForCPI(payer, oappID, eid) {
    const [sendConfig] = this.deriver.sendConfig(eid, oappID);
    const [receiveConfig] = this.deriver.receiveConfig(eid, oappID);
    const [messageLib] = this.deriver.messageLib();
    const keys = createInitConfigInstructionAccounts2(
      {
        endpoint: PublicKey.default,
        // useless
        payer,
        messageLib,
        sendConfig,
        receiveConfig
      },
      this.program
    );
    keys.forEach((key) => {
      if (!payer.equals(key.pubkey)) {
        key.isSigner = false;
      }
    });
    return keys.slice(1);
  }
  /**
   * Gets the account meta of the set config instruction for CPI (Cross-Program Invocation).
   *
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {PublicKey} oappID - The oapp ID public key.
   * @param {number} eid - The endpoint ID.
   *
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account meta.
   */
  async getSetConfigIXAccountMetaForCPI(endpointProgram, oappID, eid) {
    const [sendConfig] = this.deriver.sendConfig(eid, oappID);
    const [receiveConfig] = this.deriver.receiveConfig(eid, oappID);
    const [msgLib] = this.deriver.messageLib();
    const accounts = createSetConfigInstructionAccounts2(
      {
        endpoint: PublicKey.default,
        // useless
        sendConfig,
        receiveConfig,
        messageLib: msgLib
      },
      this.program
    );
    return Promise.resolve(accounts.slice(1));
  }
};

// src/solita/uln.ts
var uln_exports = {};
__export(uln_exports, {
  AtLeastOneDVNError: () => AtLeastOneDVNError,
  Confirmations: () => Confirmations,
  ExceededMaxMessageSizeError: () => ExceededMaxMessageSizeError,
  ExceededU128Error: () => ExceededU128Error,
  InsufficientFeeError: () => InsufficientFeeError2,
  InvalidAccountLengthError: () => InvalidAccountLengthError,
  InvalidAmountError: () => InvalidAmountError4,
  InvalidBpsError: () => InvalidBpsError,
  InvalidConfigTypeError: () => InvalidConfigTypeError2,
  InvalidConfirmationError: () => InvalidConfirmationError,
  InvalidConfirmationsError: () => InvalidConfirmationsError,
  InvalidDvnError: () => InvalidDvnError,
  InvalidDvnProgramError: () => InvalidDvnProgramError,
  InvalidEidError: () => InvalidEidError,
  InvalidExecutorError: () => InvalidExecutorError,
  InvalidExecutorProgramError: () => InvalidExecutorProgramError,
  InvalidLzTokenMintError: () => InvalidLzTokenMintError2,
  InvalidOptionTypeError: () => InvalidOptionTypeError,
  InvalidOptionalDVNCountError: () => InvalidOptionalDVNCountError,
  InvalidOptionalDVNThresholdError: () => InvalidOptionalDVNThresholdError,
  InvalidPacketVersionError: () => InvalidPacketVersionError,
  InvalidPayerError: () => InvalidPayerError,
  InvalidRequiredDVNCountError: () => InvalidRequiredDVNCountError,
  InvalidTreasuryError: () => InvalidTreasuryError,
  InvalidTreasuryFeeCapError: () => InvalidTreasuryFeeCapError,
  InvalidType1SizeError: () => InvalidType1SizeError,
  InvalidType2SizeError: () => InvalidType2SizeError,
  InvalidWorkerIdError: () => InvalidWorkerIdError,
  LzTokenUnavailableError: () => LzTokenUnavailableError3,
  NonSignerError: () => NonSignerError,
  PROGRAM_ADDRESS: () => PROGRAM_ADDRESS5,
  PROGRAM_ID: () => PROGRAM_ID5,
  ReceiveConfig: () => ReceiveConfig2,
  SendConfig: () => SendConfig,
  Uln: () => Uln,
  UlnSettings: () => UlnSettings,
  UnauthorizedError: () => UnauthorizedError2,
  UnsortedError: () => UnsortedError,
  VerifyingError: () => VerifyingError,
  ZeroMessageSizeError: () => ZeroMessageSizeError,
  accountProviders: () => accountProviders5,
  accounts: () => accounts_exports5,
  closeVerifyInstructionDiscriminator: () => closeVerifyInstructionDiscriminator,
  closeVerifyParamsBeet: () => closeVerifyParamsBeet,
  closeVerifyStruct: () => closeVerifyStruct,
  commitVerificationInstructionDiscriminator: () => commitVerificationInstructionDiscriminator,
  commitVerificationParamsBeet: () => commitVerificationParamsBeet,
  commitVerificationStruct: () => commitVerificationStruct,
  configBeet: () => configBeet,
  confirmationsBeet: () => confirmationsBeet,
  confirmationsDiscriminator: () => confirmationsDiscriminator,
  createCloseVerifyInstruction: () => createCloseVerifyInstruction,
  createCloseVerifyInstructionAccounts: () => createCloseVerifyInstructionAccounts,
  createCommitVerificationInstruction: () => createCommitVerificationInstruction,
  createCommitVerificationInstructionAccounts: () => createCommitVerificationInstructionAccounts,
  createInitConfigInstruction: () => createInitConfigInstruction3,
  createInitConfigInstructionAccounts: () => createInitConfigInstructionAccounts3,
  createInitDefaultConfigInstruction: () => createInitDefaultConfigInstruction2,
  createInitDefaultConfigInstructionAccounts: () => createInitDefaultConfigInstructionAccounts2,
  createInitUlnInstruction: () => createInitUlnInstruction,
  createInitUlnInstructionAccounts: () => createInitUlnInstructionAccounts,
  createInitVerifyInstruction: () => createInitVerifyInstruction2,
  createInitVerifyInstructionAccounts: () => createInitVerifyInstructionAccounts2,
  createQuoteInstruction: () => createQuoteInstruction3,
  createQuoteInstructionAccounts: () => createQuoteInstructionAccounts3,
  createSendInstruction: () => createSendInstruction3,
  createSendInstructionAccounts: () => createSendInstructionAccounts3,
  createSendWithLzTokenInstruction: () => createSendWithLzTokenInstruction2,
  createSendWithLzTokenInstructionAccounts: () => createSendWithLzTokenInstructionAccounts2,
  createSetConfigInstruction: () => createSetConfigInstruction4,
  createSetConfigInstructionAccounts: () => createSetConfigInstructionAccounts4,
  createSetDefaultConfigInstruction: () => createSetDefaultConfigInstruction2,
  createSetDefaultConfigInstructionAccounts: () => createSetDefaultConfigInstructionAccounts2,
  createSetTreasuryInstruction: () => createSetTreasuryInstruction,
  createSetTreasuryInstructionAccounts: () => createSetTreasuryInstructionAccounts,
  createTransferAdminInstruction: () => createTransferAdminInstruction3,
  createTransferAdminInstructionAccounts: () => createTransferAdminInstructionAccounts3,
  createVerifyInstruction: () => createVerifyInstruction2,
  createVerifyInstructionAccounts: () => createVerifyInstructionAccounts2,
  createVersionInstruction: () => createVersionInstruction2,
  createVersionInstructionAccounts: () => createVersionInstructionAccounts2,
  createWithdrawRentInstruction: () => createWithdrawRentInstruction2,
  createWithdrawRentInstructionAccounts: () => createWithdrawRentInstructionAccounts2,
  errorFromCode: () => errorFromCode5,
  errorFromName: () => errorFromName5,
  errors: () => errors_exports4,
  events: () => events_exports3,
  executorConfigBeet: () => executorConfigBeet,
  initConfigInstructionDiscriminator: () => initConfigInstructionDiscriminator3,
  initConfigParamsBeet: () => initConfigParamsBeet3,
  initConfigStruct: () => initConfigStruct3,
  initDefaultConfigInstructionDiscriminator: () => initDefaultConfigInstructionDiscriminator2,
  initDefaultConfigParamsBeet: () => initDefaultConfigParamsBeet2,
  initDefaultConfigStruct: () => initDefaultConfigStruct2,
  initUlnInstructionDiscriminator: () => initUlnInstructionDiscriminator,
  initUlnParamsBeet: () => initUlnParamsBeet,
  initUlnStruct: () => initUlnStruct,
  initVerifyInstructionDiscriminator: () => initVerifyInstructionDiscriminator2,
  initVerifyParamsBeet: () => initVerifyParamsBeet2,
  initVerifyStruct: () => initVerifyStruct2,
  instructions: () => instructions_exports5,
  isConfigExecutor: () => isConfigExecutor,
  isConfigReceiveUln: () => isConfigReceiveUln,
  isConfigSendUln: () => isConfigSendUln,
  lzTokenTreasuryBeet: () => lzTokenTreasuryBeet,
  messagingFeeBeet: () => messagingFeeBeet3,
  packetBeet: () => packetBeet2,
  quoteInstructionDiscriminator: () => quoteInstructionDiscriminator3,
  quoteParamsBeet: () => quoteParamsBeet3,
  quoteStruct: () => quoteStruct3,
  receiveConfigBeet: () => receiveConfigBeet2,
  receiveConfigDiscriminator: () => receiveConfigDiscriminator2,
  sendConfigBeet: () => sendConfigBeet,
  sendConfigDiscriminator: () => sendConfigDiscriminator,
  sendInstructionDiscriminator: () => sendInstructionDiscriminator3,
  sendParamsBeet: () => sendParamsBeet3,
  sendStruct: () => sendStruct3,
  sendWithLzTokenInstructionDiscriminator: () => sendWithLzTokenInstructionDiscriminator2,
  sendWithLzTokenParamsBeet: () => sendWithLzTokenParamsBeet2,
  sendWithLzTokenStruct: () => sendWithLzTokenStruct2,
  setConfigInstructionDiscriminator: () => setConfigInstructionDiscriminator4,
  setConfigParamsBeet: () => setConfigParamsBeet4,
  setConfigStruct: () => setConfigStruct4,
  setDefaultConfigInstructionDiscriminator: () => setDefaultConfigInstructionDiscriminator2,
  setDefaultConfigParamsBeet: () => setDefaultConfigParamsBeet2,
  setDefaultConfigStruct: () => setDefaultConfigStruct2,
  setTreasuryInstructionDiscriminator: () => setTreasuryInstructionDiscriminator,
  setTreasuryParamsBeet: () => setTreasuryParamsBeet,
  setTreasuryStruct: () => setTreasuryStruct,
  transferAdminInstructionDiscriminator: () => transferAdminInstructionDiscriminator3,
  transferAdminParamsBeet: () => transferAdminParamsBeet3,
  transferAdminStruct: () => transferAdminStruct3,
  treasuryBeet: () => treasuryBeet,
  treasuryFeeBeet: () => treasuryFeeBeet,
  types: () => types_exports5,
  ulnConfigBeet: () => ulnConfigBeet2,
  ulnSettingsBeet: () => ulnSettingsBeet,
  ulnSettingsDiscriminator: () => ulnSettingsDiscriminator,
  verifyInstructionDiscriminator: () => verifyInstructionDiscriminator2,
  verifyParamsBeet: () => verifyParamsBeet2,
  verifyStruct: () => verifyStruct2,
  versionBeet: () => versionBeet2,
  versionInstructionDiscriminator: () => versionInstructionDiscriminator2,
  versionStruct: () => versionStruct2,
  withdrawRentInstructionDiscriminator: () => withdrawRentInstructionDiscriminator2,
  withdrawRentParamsBeet: () => withdrawRentParamsBeet2,
  withdrawRentStruct: () => withdrawRentStruct2,
  workerFeeBeet: () => workerFeeBeet
});

// src/solita/dvn.ts
var dvn_exports2 = {};
__export(dvn_exports2, {
  DVN: () => DVN,
  PROGRAM_ID: () => PROGRAM_ID3,
  SolanaSignerExt: () => SolanaSignerExt,
  accounts: () => accounts_exports3,
  errors: () => errors_exports3,
  events: () => events_exports2,
  instructions: () => instructions_exports3,
  types: () => types_exports3
});

// src/generated/solita/dvn/index.ts
var dvn_exports = {};
__export(dvn_exports, {
  DuplicateSignatureError: () => DuplicateSignatureError,
  DvnConfig: () => DvnConfig,
  EidNotSupportedError: () => EidNotSupportedError,
  ExecuteHash: () => ExecuteHash,
  ExpiredError: () => ExpiredError,
  InvalidAmountError: () => InvalidAmountError3,
  InvalidQuorumError: () => InvalidQuorumError,
  InvalidSignatureLenError: () => InvalidSignatureLenError,
  InvalidSignersLenError: () => InvalidSignersLenError,
  InvalidVidError: () => InvalidVidError,
  MsgLibNotAllowedError: () => MsgLibNotAllowedError,
  NotAdminError: () => NotAdminError,
  PROGRAM_ADDRESS: () => PROGRAM_ADDRESS3,
  PROGRAM_ID: () => PROGRAM_ID3,
  PausedError: () => PausedError,
  ReceiveConfig: () => ReceiveConfig,
  SignatureErrorError: () => SignatureErrorError,
  SignerNotInCommitteeError: () => SignerNotInCommitteeError,
  TooManyAdminsError: () => TooManyAdminsError,
  TooManyOptionTypesError: () => TooManyOptionTypesError,
  UnexpiredExecuteHashError: () => UnexpiredExecuteHashError,
  UniqueOwnersError: () => UniqueOwnersError,
  VerificationState: () => VerificationState,
  accountProviders: () => accountProviders3,
  aclBeet: () => aclBeet,
  adminConfigBeet: () => adminConfigBeet,
  closeExecuteInstructionDiscriminator: () => closeExecuteInstructionDiscriminator,
  closeExecuteParamsBeet: () => closeExecuteParamsBeet,
  closeExecuteStruct: () => closeExecuteStruct,
  createCloseExecuteInstruction: () => createCloseExecuteInstruction,
  createCloseExecuteInstructionAccounts: () => createCloseExecuteInstructionAccounts,
  createInitDvnInstruction: () => createInitDvnInstruction,
  createInitDvnInstructionAccounts: () => createInitDvnInstructionAccounts,
  createInvokeInstruction: () => createInvokeInstruction,
  createInvokeInstructionAccounts: () => createInvokeInstructionAccounts,
  createQuoteDvnInstruction: () => createQuoteDvnInstruction,
  createQuoteDvnInstructionAccounts: () => createQuoteDvnInstructionAccounts,
  createSetConfigInstruction: () => createSetConfigInstruction3,
  createSetConfigInstructionAccounts: () => createSetConfigInstructionAccounts3,
  createVerifiableInstruction: () => createVerifiableInstruction,
  createVerifiableInstructionAccounts: () => createVerifiableInstructionAccounts,
  createWithdrawFeeInstruction: () => createWithdrawFeeInstruction,
  createWithdrawFeeInstructionAccounts: () => createWithdrawFeeInstructionAccounts,
  dstConfigBeet: () => dstConfigBeet,
  dvnConfigBeet: () => dvnConfigBeet,
  dvnConfigDiscriminator: () => dvnConfigDiscriminator,
  errorFromCode: () => errorFromCode3,
  errorFromName: () => errorFromName3,
  executeHashBeet: () => executeHashBeet,
  executeHashDiscriminator: () => executeHashDiscriminator,
  executeTransactionDigestBeet: () => executeTransactionDigestBeet,
  initDvnInstructionDiscriminator: () => initDvnInstructionDiscriminator,
  initDvnParamsBeet: () => initDvnParamsBeet,
  initDvnStruct: () => initDvnStruct,
  invokeInstructionDiscriminator: () => invokeInstructionDiscriminator,
  invokeParamsBeet: () => invokeParamsBeet,
  invokeStruct: () => invokeStruct,
  isAdminConfigAdmins: () => isAdminConfigAdmins,
  isAdminConfigDefaultMultiplierBps: () => isAdminConfigDefaultMultiplierBps,
  isAdminConfigDstConfigs: () => isAdminConfigDstConfigs,
  isAdminConfigPriceFeed: () => isAdminConfigPriceFeed,
  isAdminConfigRemoveDstConfigs: () => isAdminConfigRemoveDstConfigs,
  isMultisigConfigAdmins: () => isMultisigConfigAdmins,
  isMultisigConfigAllowlist: () => isMultisigConfigAllowlist,
  isMultisigConfigDenylist: () => isMultisigConfigDenylist,
  isMultisigConfigMsglibs: () => isMultisigConfigMsglibs,
  isMultisigConfigPaused: () => isMultisigConfigPaused,
  isMultisigConfigQuorum: () => isMultisigConfigQuorum,
  isMultisigConfigSigners: () => isMultisigConfigSigners,
  lzOptionBeet: () => lzOptionBeet,
  multisigBeet: () => multisigBeet,
  multisigConfigBeet: () => multisigConfigBeet,
  quoteDvnInstructionDiscriminator: () => quoteDvnInstructionDiscriminator,
  quoteDvnParamsBeet: () => quoteDvnParamsBeet,
  quoteDvnStruct: () => quoteDvnStruct,
  receiveConfigBeet: () => receiveConfigBeet,
  receiveConfigDiscriminator: () => receiveConfigDiscriminator,
  setConfigInstructionDiscriminator: () => setConfigInstructionDiscriminator3,
  setConfigParamsBeet: () => setConfigParamsBeet3,
  setConfigStruct: () => setConfigStruct3,
  transactionAccountBeet: () => transactionAccountBeet,
  ulnConfigBeet: () => ulnConfigBeet,
  verifiableInstructionDiscriminator: () => verifiableInstructionDiscriminator,
  verifiableParamsBeet: () => verifiableParamsBeet,
  verifiableStruct: () => verifiableStruct,
  verificationStateBeet: () => verificationStateBeet,
  withdrawFeeInstructionDiscriminator: () => withdrawFeeInstructionDiscriminator,
  withdrawFeeParamsBeet: () => withdrawFeeParamsBeet,
  withdrawFeeStruct: () => withdrawFeeStruct
});

// src/generated/solita/dvn/accounts/index.ts
var accounts_exports3 = {};
__export(accounts_exports3, {
  DvnConfig: () => DvnConfig,
  ExecuteHash: () => ExecuteHash,
  ReceiveConfig: () => ReceiveConfig,
  accountProviders: () => accountProviders3,
  dvnConfigBeet: () => dvnConfigBeet,
  dvnConfigDiscriminator: () => dvnConfigDiscriminator,
  executeHashBeet: () => executeHashBeet,
  executeHashDiscriminator: () => executeHashDiscriminator,
  receiveConfigBeet: () => receiveConfigBeet,
  receiveConfigDiscriminator: () => receiveConfigDiscriminator
});
var multisigBeet = new beet159.FixableBeetArgsStruct(
  [
    ["signers", beet159.array(beet159.uniformFixedSizeArray(beet159.u8, 64))],
    ["quorum", beet159.u8]
  ],
  "Multisig"
);
var aclBeet = new beet159.FixableBeetArgsStruct(
  [
    ["allowList", beet159.array(beetSolana96.publicKey)],
    ["denyList", beet159.array(beetSolana96.publicKey)]
  ],
  "Acl"
);
var dstConfigBeet = new beet159.FixableBeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["dstGas", beet159.u32],
    ["multiplierBps", beet159.coption(beet159.u16)],
    ["floorMarginUsd", beet159.coption(beet159.u128)]
  ],
  "DstConfig"
);

// src/generated/solita/dvn/accounts/DvnConfig.ts
var dvnConfigDiscriminator = [49, 81, 33, 98, 234, 168, 93, 73];
var DvnConfig = class _DvnConfig {
  constructor(vid, bump, multisig, acl, paused, msglibs, admins, priceFeed, dstConfigs, defaultMultiplierBps) {
    this.vid = vid;
    this.bump = bump;
    this.multisig = multisig;
    this.acl = acl;
    this.paused = paused;
    this.msglibs = msglibs;
    this.admins = admins;
    this.priceFeed = priceFeed;
    this.dstConfigs = dstConfigs;
    this.defaultMultiplierBps = defaultMultiplierBps;
  }
  /**
   * Creates a {@link DvnConfig} instance from the provided args.
   */
  static fromArgs(args) {
    return new _DvnConfig(
      args.vid,
      args.bump,
      args.multisig,
      args.acl,
      args.paused,
      args.msglibs,
      args.admins,
      args.priceFeed,
      args.dstConfigs,
      args.defaultMultiplierBps
    );
  }
  /**
   * Deserializes the {@link DvnConfig} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _DvnConfig.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link DvnConfig} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find DvnConfig account at ${address}`);
    }
    return _DvnConfig.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, dvnConfigBeet);
  }
  /**
   * Deserializes the {@link DvnConfig} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return dvnConfigBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link DvnConfig} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return dvnConfigBeet.serialize({
      accountDiscriminator: dvnConfigDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link DvnConfig} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _DvnConfig.fromArgs(args);
    return dvnConfigBeet.toFixedFromValue({
      accountDiscriminator: dvnConfigDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link DvnConfig} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _DvnConfig.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link DvnConfig} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      vid: this.vid,
      bump: this.bump,
      multisig: this.multisig,
      acl: this.acl,
      paused: this.paused,
      msglibs: this.msglibs,
      admins: this.admins,
      priceFeed: this.priceFeed.toBase58(),
      dstConfigs: this.dstConfigs,
      defaultMultiplierBps: this.defaultMultiplierBps
    };
  }
};
var dvnConfigBeet = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["vid", beet159.u32],
    ["bump", beet159.u8],
    ["multisig", multisigBeet],
    ["acl", aclBeet],
    ["paused", beet159.bool],
    ["msglibs", beet159.array(beetSolana96.publicKey)],
    ["admins", beet159.array(beetSolana96.publicKey)],
    ["priceFeed", beetSolana96.publicKey],
    ["dstConfigs", beet159.array(dstConfigBeet)],
    ["defaultMultiplierBps", beet159.u16]
  ],
  DvnConfig.fromArgs,
  "DvnConfig"
);
var executeHashDiscriminator = [34, 17, 137, 123, 62, 25, 205, 155];
var ExecuteHash = class _ExecuteHash {
  constructor(expiration, bump) {
    this.expiration = expiration;
    this.bump = bump;
  }
  /**
   * Creates a {@link ExecuteHash} instance from the provided args.
   */
  static fromArgs(args) {
    return new _ExecuteHash(args.expiration, args.bump);
  }
  /**
   * Deserializes the {@link ExecuteHash} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _ExecuteHash.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link ExecuteHash} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find ExecuteHash account at ${address}`);
    }
    return _ExecuteHash.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, executeHashBeet);
  }
  /**
   * Deserializes the {@link ExecuteHash} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return executeHashBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link ExecuteHash} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return executeHashBeet.serialize({
      accountDiscriminator: executeHashDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link ExecuteHash}
   */
  static get byteSize() {
    return executeHashBeet.byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link ExecuteHash} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _ExecuteHash.byteSize,
      commitment
    );
  }
  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link ExecuteHash} data.
   */
  static hasCorrectByteSize(buf, offset = 0) {
    return buf.byteLength - offset === _ExecuteHash.byteSize;
  }
  /**
   * Returns a readable version of {@link ExecuteHash} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      expiration: (() => {
        const x = this.expiration;
        if (typeof x.toNumber === "function") {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      bump: this.bump
    };
  }
};
var executeHashBeet = new beet159.BeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["expiration", beet159.i64],
    ["bump", beet159.u8]
  ],
  ExecuteHash.fromArgs,
  "ExecuteHash"
);
var ulnConfigBeet = new beet159.FixableBeetArgsStruct(
  [
    ["confirmations", beet159.u64],
    ["requiredDvnCount", beet159.u8],
    ["optionalDvnCount", beet159.u8],
    ["optionalDvnThreshold", beet159.u8],
    ["requiredDvns", beet159.array(beetSolana96.publicKey)],
    ["optionalDvns", beet159.array(beetSolana96.publicKey)]
  ],
  "UlnConfig"
);

// src/generated/solita/dvn/accounts/ReceiveConfig.ts
var receiveConfigDiscriminator = [162, 159, 153, 188, 56, 65, 245, 58];
var ReceiveConfig = class _ReceiveConfig {
  constructor(bump, uln) {
    this.bump = bump;
    this.uln = uln;
  }
  /**
   * Creates a {@link ReceiveConfig} instance from the provided args.
   */
  static fromArgs(args) {
    return new _ReceiveConfig(args.bump, args.uln);
  }
  /**
   * Deserializes the {@link ReceiveConfig} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _ReceiveConfig.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link ReceiveConfig} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find ReceiveConfig account at ${address}`);
    }
    return _ReceiveConfig.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, receiveConfigBeet);
  }
  /**
   * Deserializes the {@link ReceiveConfig} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return receiveConfigBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link ReceiveConfig} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return receiveConfigBeet.serialize({
      accountDiscriminator: receiveConfigDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link ReceiveConfig} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _ReceiveConfig.fromArgs(args);
    return receiveConfigBeet.toFixedFromValue({
      accountDiscriminator: receiveConfigDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link ReceiveConfig} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _ReceiveConfig.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link ReceiveConfig} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      bump: this.bump,
      uln: this.uln
    };
  }
};
var receiveConfigBeet = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["bump", beet159.u8],
    ["uln", ulnConfigBeet]
  ],
  ReceiveConfig.fromArgs,
  "ReceiveConfig"
);

// src/generated/solita/dvn/accounts/index.ts
var accountProviders3 = { DvnConfig, ExecuteHash, ReceiveConfig };

// src/generated/solita/dvn/errors/index.ts
var errors_exports3 = {};
__export(errors_exports3, {
  DuplicateSignatureError: () => DuplicateSignatureError,
  EidNotSupportedError: () => EidNotSupportedError,
  ExpiredError: () => ExpiredError,
  InvalidAmountError: () => InvalidAmountError3,
  InvalidQuorumError: () => InvalidQuorumError,
  InvalidSignatureLenError: () => InvalidSignatureLenError,
  InvalidSignersLenError: () => InvalidSignersLenError,
  InvalidVidError: () => InvalidVidError,
  MsgLibNotAllowedError: () => MsgLibNotAllowedError,
  NotAdminError: () => NotAdminError,
  PausedError: () => PausedError,
  SignatureErrorError: () => SignatureErrorError,
  SignerNotInCommitteeError: () => SignerNotInCommitteeError,
  TooManyAdminsError: () => TooManyAdminsError,
  TooManyOptionTypesError: () => TooManyOptionTypesError,
  UnexpiredExecuteHashError: () => UnexpiredExecuteHashError,
  UniqueOwnersError: () => UniqueOwnersError,
  errorFromCode: () => errorFromCode3,
  errorFromName: () => errorFromName3
});
var createErrorFromCodeLookup3 = /* @__PURE__ */ new Map();
var createErrorFromNameLookup3 = /* @__PURE__ */ new Map();
var InvalidSignatureLenError = class _InvalidSignatureLenError extends Error {
  constructor() {
    super("");
    this.code = 6e3;
    this.name = "InvalidSignatureLen";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidSignatureLenError);
    }
  }
};
createErrorFromCodeLookup3.set(6e3, () => new InvalidSignatureLenError());
createErrorFromNameLookup3.set(
  "InvalidSignatureLen",
  () => new InvalidSignatureLenError()
);
var NotAdminError = class _NotAdminError extends Error {
  constructor() {
    super("");
    this.code = 6001;
    this.name = "NotAdmin";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _NotAdminError);
    }
  }
};
createErrorFromCodeLookup3.set(6001, () => new NotAdminError());
createErrorFromNameLookup3.set("NotAdmin", () => new NotAdminError());
var MsgLibNotAllowedError = class _MsgLibNotAllowedError extends Error {
  constructor() {
    super("");
    this.code = 6002;
    this.name = "MsgLibNotAllowed";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _MsgLibNotAllowedError);
    }
  }
};
createErrorFromCodeLookup3.set(6002, () => new MsgLibNotAllowedError());
createErrorFromNameLookup3.set(
  "MsgLibNotAllowed",
  () => new MsgLibNotAllowedError()
);
var InvalidQuorumError = class _InvalidQuorumError extends Error {
  constructor() {
    super("");
    this.code = 6003;
    this.name = "InvalidQuorum";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidQuorumError);
    }
  }
};
createErrorFromCodeLookup3.set(6003, () => new InvalidQuorumError());
createErrorFromNameLookup3.set("InvalidQuorum", () => new InvalidQuorumError());
var InvalidSignersLenError = class _InvalidSignersLenError extends Error {
  constructor() {
    super("");
    this.code = 6004;
    this.name = "InvalidSignersLen";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidSignersLenError);
    }
  }
};
createErrorFromCodeLookup3.set(6004, () => new InvalidSignersLenError());
createErrorFromNameLookup3.set(
  "InvalidSignersLen",
  () => new InvalidSignersLenError()
);
var UniqueOwnersError = class _UniqueOwnersError extends Error {
  constructor() {
    super("");
    this.code = 6005;
    this.name = "UniqueOwners";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _UniqueOwnersError);
    }
  }
};
createErrorFromCodeLookup3.set(6005, () => new UniqueOwnersError());
createErrorFromNameLookup3.set("UniqueOwners", () => new UniqueOwnersError());
var SignatureErrorError = class _SignatureErrorError extends Error {
  constructor() {
    super("");
    this.code = 6006;
    this.name = "SignatureError";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _SignatureErrorError);
    }
  }
};
createErrorFromCodeLookup3.set(6006, () => new SignatureErrorError());
createErrorFromNameLookup3.set("SignatureError", () => new SignatureErrorError());
var SignerNotInCommitteeError = class _SignerNotInCommitteeError extends Error {
  constructor() {
    super("");
    this.code = 6007;
    this.name = "SignerNotInCommittee";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _SignerNotInCommitteeError);
    }
  }
};
createErrorFromCodeLookup3.set(6007, () => new SignerNotInCommitteeError());
createErrorFromNameLookup3.set(
  "SignerNotInCommittee",
  () => new SignerNotInCommitteeError()
);
var TooManyAdminsError = class _TooManyAdminsError extends Error {
  constructor() {
    super("");
    this.code = 6008;
    this.name = "TooManyAdmins";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _TooManyAdminsError);
    }
  }
};
createErrorFromCodeLookup3.set(6008, () => new TooManyAdminsError());
createErrorFromNameLookup3.set("TooManyAdmins", () => new TooManyAdminsError());
var TooManyOptionTypesError = class _TooManyOptionTypesError extends Error {
  constructor() {
    super("");
    this.code = 6009;
    this.name = "TooManyOptionTypes";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _TooManyOptionTypesError);
    }
  }
};
createErrorFromCodeLookup3.set(6009, () => new TooManyOptionTypesError());
createErrorFromNameLookup3.set(
  "TooManyOptionTypes",
  () => new TooManyOptionTypesError()
);
var DuplicateSignatureError = class _DuplicateSignatureError extends Error {
  constructor() {
    super("");
    this.code = 6010;
    this.name = "DuplicateSignature";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _DuplicateSignatureError);
    }
  }
};
createErrorFromCodeLookup3.set(6010, () => new DuplicateSignatureError());
createErrorFromNameLookup3.set(
  "DuplicateSignature",
  () => new DuplicateSignatureError()
);
var ExpiredError = class _ExpiredError extends Error {
  constructor() {
    super("");
    this.code = 6011;
    this.name = "Expired";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _ExpiredError);
    }
  }
};
createErrorFromCodeLookup3.set(6011, () => new ExpiredError());
createErrorFromNameLookup3.set("Expired", () => new ExpiredError());
var InvalidVidError = class _InvalidVidError extends Error {
  constructor() {
    super("");
    this.code = 6012;
    this.name = "InvalidVid";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidVidError);
    }
  }
};
createErrorFromCodeLookup3.set(6012, () => new InvalidVidError());
createErrorFromNameLookup3.set("InvalidVid", () => new InvalidVidError());
var PausedError = class _PausedError extends Error {
  constructor() {
    super("");
    this.code = 6013;
    this.name = "Paused";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _PausedError);
    }
  }
};
createErrorFromCodeLookup3.set(6013, () => new PausedError());
createErrorFromNameLookup3.set("Paused", () => new PausedError());
var UnexpiredExecuteHashError = class _UnexpiredExecuteHashError extends Error {
  constructor() {
    super("");
    this.code = 6014;
    this.name = "UnexpiredExecuteHash";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _UnexpiredExecuteHashError);
    }
  }
};
createErrorFromCodeLookup3.set(6014, () => new UnexpiredExecuteHashError());
createErrorFromNameLookup3.set(
  "UnexpiredExecuteHash",
  () => new UnexpiredExecuteHashError()
);
var InvalidAmountError3 = class _InvalidAmountError extends Error {
  constructor() {
    super("");
    this.code = 6015;
    this.name = "InvalidAmount";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidAmountError);
    }
  }
};
createErrorFromCodeLookup3.set(6015, () => new InvalidAmountError3());
createErrorFromNameLookup3.set("InvalidAmount", () => new InvalidAmountError3());
var EidNotSupportedError = class _EidNotSupportedError extends Error {
  constructor() {
    super("");
    this.code = 6016;
    this.name = "EidNotSupported";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _EidNotSupportedError);
    }
  }
};
createErrorFromCodeLookup3.set(6016, () => new EidNotSupportedError());
createErrorFromNameLookup3.set(
  "EidNotSupported",
  () => new EidNotSupportedError()
);
function errorFromCode3(code) {
  const createError = createErrorFromCodeLookup3.get(code);
  return createError != null ? createError() : null;
}
function errorFromName3(name) {
  const createError = createErrorFromNameLookup3.get(name);
  return createError != null ? createError() : null;
}

// src/generated/solita/dvn/instructions/index.ts
var instructions_exports3 = {};
__export(instructions_exports3, {
  closeExecuteInstructionDiscriminator: () => closeExecuteInstructionDiscriminator,
  closeExecuteStruct: () => closeExecuteStruct,
  createCloseExecuteInstruction: () => createCloseExecuteInstruction,
  createCloseExecuteInstructionAccounts: () => createCloseExecuteInstructionAccounts,
  createInitDvnInstruction: () => createInitDvnInstruction,
  createInitDvnInstructionAccounts: () => createInitDvnInstructionAccounts,
  createInvokeInstruction: () => createInvokeInstruction,
  createInvokeInstructionAccounts: () => createInvokeInstructionAccounts,
  createQuoteDvnInstruction: () => createQuoteDvnInstruction,
  createQuoteDvnInstructionAccounts: () => createQuoteDvnInstructionAccounts,
  createSetConfigInstruction: () => createSetConfigInstruction3,
  createSetConfigInstructionAccounts: () => createSetConfigInstructionAccounts3,
  createVerifiableInstruction: () => createVerifiableInstruction,
  createVerifiableInstructionAccounts: () => createVerifiableInstructionAccounts,
  createWithdrawFeeInstruction: () => createWithdrawFeeInstruction,
  createWithdrawFeeInstructionAccounts: () => createWithdrawFeeInstructionAccounts,
  initDvnInstructionDiscriminator: () => initDvnInstructionDiscriminator,
  initDvnStruct: () => initDvnStruct,
  invokeInstructionDiscriminator: () => invokeInstructionDiscriminator,
  invokeStruct: () => invokeStruct,
  quoteDvnInstructionDiscriminator: () => quoteDvnInstructionDiscriminator,
  quoteDvnStruct: () => quoteDvnStruct,
  setConfigInstructionDiscriminator: () => setConfigInstructionDiscriminator3,
  setConfigStruct: () => setConfigStruct3,
  verifiableInstructionDiscriminator: () => verifiableInstructionDiscriminator,
  verifiableStruct: () => verifiableStruct,
  withdrawFeeInstructionDiscriminator: () => withdrawFeeInstructionDiscriminator,
  withdrawFeeStruct: () => withdrawFeeStruct
});
var closeExecuteParamsBeet = new beet159.BeetArgsStruct(
  [["digestHash", beet159.uniformFixedSizeArray(beet159.u8, 32)]],
  "CloseExecuteParams"
);

// src/generated/solita/dvn/instructions/closeExecute.ts
var closeExecuteStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", closeExecuteParamsBeet]
  ],
  "CloseExecuteInstructionArgs"
);
var closeExecuteInstructionDiscriminator = [
  253,
  220,
  23,
  127,
  15,
  171,
  189,
  226
];
function createCloseExecuteInstruction(accounts, args, programId) {
  const [data] = closeExecuteStruct.serialize({
    instructionDiscriminator: closeExecuteInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.executeHash,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createCloseExecuteInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.executeHash,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initDvnParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["vid", beet159.u32],
    ["msglibs", beet159.array(beetSolana96.publicKey)],
    ["priceFeed", beetSolana96.publicKey],
    ["signers", beet159.array(beet159.uniformFixedSizeArray(beet159.u8, 64))],
    ["quorum", beet159.u8],
    ["admins", beet159.array(beetSolana96.publicKey)]
  ],
  "InitDvnParams"
);

// src/generated/solita/dvn/instructions/initDvn.ts
var initDvnStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initDvnParamsBeet]
  ],
  "InitDvnInstructionArgs"
);
var initDvnInstructionDiscriminator = [
  58,
  108,
  132,
  12,
  20,
  10,
  215,
  210
];
function createInitDvnInstruction(accounts, args, programId) {
  const [data] = initDvnStruct.serialize({
    instructionDiscriminator: initDvnInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitDvnInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var transactionAccountBeet = new beet159.BeetArgsStruct(
  [
    ["pubkey", beetSolana96.publicKey],
    ["isSigner", beet159.bool],
    ["isWritable", beet159.bool]
  ],
  "TransactionAccount"
);

// src/generated/solita/dvn/types/ExecuteTransactionDigest.ts
var executeTransactionDigestBeet = new beet159.FixableBeetArgsStruct(
  [
    ["vid", beet159.u32],
    ["programId", beetSolana96.publicKey],
    ["accounts", beet159.array(transactionAccountBeet)],
    ["data", beet159.bytes],
    ["expiration", beet159.i64]
  ],
  "ExecuteTransactionDigest"
);

// src/generated/solita/dvn/types/InvokeParams.ts
var invokeParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["digest", executeTransactionDigestBeet],
    ["signatures", beet159.array(beet159.uniformFixedSizeArray(beet159.u8, 65))]
  ],
  "InvokeParams"
);

// src/generated/solita/dvn/instructions/invoke.ts
var invokeStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", invokeParamsBeet]
  ],
  "InvokeInstructionArgs"
);
var invokeInstructionDiscriminator = [26, 16, 169, 7, 21, 202, 242, 25];
function createInvokeInstruction(accounts, args, programId) {
  const [data] = invokeStruct.serialize({
    instructionDiscriminator: invokeInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.executeHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInvokeInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.executeHash,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var lzOptionBeet = new beet159.FixableBeetArgsStruct(
  [
    ["optionType", beet159.u8],
    ["params", beet159.bytes]
  ],
  "LzOption"
);

// src/generated/solita/dvn/types/QuoteDvnParams.ts
var quoteDvnParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["msglib", beetSolana96.publicKey],
    ["dstEid", beet159.u32],
    ["sender", beetSolana96.publicKey],
    ["packetHeader", beet159.bytes],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["confirmations", beet159.u64],
    ["options", beet159.array(lzOptionBeet)]
  ],
  "QuoteDvnParams"
);

// src/generated/solita/dvn/instructions/quoteDvn.ts
var quoteDvnStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", quoteDvnParamsBeet]
  ],
  "QuoteDvnInstructionArgs"
);
var quoteDvnInstructionDiscriminator = [
  223,
  98,
  51,
  200,
  123,
  133,
  132,
  194
];
function createQuoteDvnInstruction(accounts, args, programId) {
  const [data] = quoteDvnStruct.serialize({
    instructionDiscriminator: quoteDvnInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.dvnConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.priceFeedProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.priceFeedConfig,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createQuoteDvnInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.dvnConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.priceFeedProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.priceFeedConfig,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var isAdminConfigAdmins = (x) => x.__kind === "Admins";
var isAdminConfigDefaultMultiplierBps = (x) => x.__kind === "DefaultMultiplierBps";
var isAdminConfigDstConfigs = (x) => x.__kind === "DstConfigs";
var isAdminConfigPriceFeed = (x) => x.__kind === "PriceFeed";
var isAdminConfigRemoveDstConfigs = (x) => x.__kind === "RemoveDstConfigs";
var adminConfigBeet = beet159.dataEnum([
  [
    "Admins",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(beetSolana96.publicKey)])]],
      'AdminConfigRecord["Admins"]'
    )
  ],
  [
    "DefaultMultiplierBps",
    new beet159.BeetArgsStruct(
      [["fields", beet159.fixedSizeTuple([beet159.u16])]],
      'AdminConfigRecord["DefaultMultiplierBps"]'
    )
  ],
  [
    "DstConfigs",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(dstConfigBeet)])]],
      'AdminConfigRecord["DstConfigs"]'
    )
  ],
  [
    "PriceFeed",
    new beet159.BeetArgsStruct(
      [["fields", beet159.fixedSizeTuple([beetSolana96.publicKey])]],
      'AdminConfigRecord["PriceFeed"]'
    )
  ],
  [
    "RemoveDstConfigs",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(beet159.u32)])]],
      'AdminConfigRecord["RemoveDstConfigs"]'
    )
  ]
]);

// src/generated/solita/dvn/types/SetConfigParams.ts
var setConfigParamsBeet3 = new beet159.FixableBeetArgsStruct(
  [["config", adminConfigBeet]],
  "SetConfigParams"
);

// src/generated/solita/dvn/instructions/setConfig.ts
var setConfigStruct3 = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setConfigParamsBeet3]
  ],
  "SetConfigInstructionArgs"
);
var setConfigInstructionDiscriminator3 = [
  108,
  158,
  154,
  175,
  212,
  98,
  52,
  66
];
function createSetConfigInstruction3(accounts, args, programId) {
  const [data] = setConfigStruct3.serialize({
    instructionDiscriminator: setConfigInstructionDiscriminator3,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetConfigInstructionAccounts3(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var verifiableParamsBeet = new beet159.BeetArgsStruct(
  [
    ["packetHeader", beet159.uniformFixedSizeArray(beet159.u8, 81)],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)]
  ],
  "VerifiableParams"
);

// src/generated/solita/dvn/instructions/verifiable.ts
var verifiableStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", verifiableParamsBeet]
  ],
  "VerifiableInstructionArgs"
);
var verifiableInstructionDiscriminator = [
  249,
  50,
  227,
  157,
  238,
  249,
  211,
  90
];
function createVerifiableInstruction(accounts, args, programId) {
  const [data] = verifiableStruct.serialize({
    instructionDiscriminator: verifiableInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveConfig,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createVerifiableInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveConfig,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var withdrawFeeParamsBeet = new beet159.BeetArgsStruct(
  [["amount", beet159.u64]],
  "WithdrawFeeParams"
);

// src/generated/solita/dvn/instructions/withdrawFee.ts
var withdrawFeeStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", withdrawFeeParamsBeet]
  ],
  "WithdrawFeeInstructionArgs"
);
var withdrawFeeInstructionDiscriminator = [
  14,
  122,
  231,
  218,
  31,
  238,
  223,
  150
];
function createWithdrawFeeInstruction(accounts, args, programId) {
  const [data] = withdrawFeeStruct.serialize({
    instructionDiscriminator: withdrawFeeInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiver,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createWithdrawFeeInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiver,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}

// src/generated/solita/dvn/types/index.ts
var types_exports3 = {};
__export(types_exports3, {
  VerificationState: () => VerificationState,
  aclBeet: () => aclBeet,
  adminConfigBeet: () => adminConfigBeet,
  closeExecuteParamsBeet: () => closeExecuteParamsBeet,
  dstConfigBeet: () => dstConfigBeet,
  executeTransactionDigestBeet: () => executeTransactionDigestBeet,
  initDvnParamsBeet: () => initDvnParamsBeet,
  invokeParamsBeet: () => invokeParamsBeet,
  isAdminConfigAdmins: () => isAdminConfigAdmins,
  isAdminConfigDefaultMultiplierBps: () => isAdminConfigDefaultMultiplierBps,
  isAdminConfigDstConfigs: () => isAdminConfigDstConfigs,
  isAdminConfigPriceFeed: () => isAdminConfigPriceFeed,
  isAdminConfigRemoveDstConfigs: () => isAdminConfigRemoveDstConfigs,
  isMultisigConfigAdmins: () => isMultisigConfigAdmins,
  isMultisigConfigAllowlist: () => isMultisigConfigAllowlist,
  isMultisigConfigDenylist: () => isMultisigConfigDenylist,
  isMultisigConfigMsglibs: () => isMultisigConfigMsglibs,
  isMultisigConfigPaused: () => isMultisigConfigPaused,
  isMultisigConfigQuorum: () => isMultisigConfigQuorum,
  isMultisigConfigSigners: () => isMultisigConfigSigners,
  lzOptionBeet: () => lzOptionBeet,
  multisigBeet: () => multisigBeet,
  multisigConfigBeet: () => multisigConfigBeet,
  quoteDvnParamsBeet: () => quoteDvnParamsBeet,
  setConfigParamsBeet: () => setConfigParamsBeet3,
  transactionAccountBeet: () => transactionAccountBeet,
  ulnConfigBeet: () => ulnConfigBeet,
  verifiableParamsBeet: () => verifiableParamsBeet,
  verificationStateBeet: () => verificationStateBeet,
  withdrawFeeParamsBeet: () => withdrawFeeParamsBeet
});
var isMultisigConfigAdmins = (x) => x.__kind === "Admins";
var isMultisigConfigAllowlist = (x) => x.__kind === "Allowlist";
var isMultisigConfigDenylist = (x) => x.__kind === "Denylist";
var isMultisigConfigMsglibs = (x) => x.__kind === "Msglibs";
var isMultisigConfigPaused = (x) => x.__kind === "Paused";
var isMultisigConfigQuorum = (x) => x.__kind === "Quorum";
var isMultisigConfigSigners = (x) => x.__kind === "Signers";
var multisigConfigBeet = beet159.dataEnum([
  [
    "Admins",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(beetSolana96.publicKey)])]],
      'MultisigConfigRecord["Admins"]'
    )
  ],
  [
    "Allowlist",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(beetSolana96.publicKey)])]],
      'MultisigConfigRecord["Allowlist"]'
    )
  ],
  [
    "Denylist",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(beetSolana96.publicKey)])]],
      'MultisigConfigRecord["Denylist"]'
    )
  ],
  [
    "Msglibs",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(beetSolana96.publicKey)])]],
      'MultisigConfigRecord["Msglibs"]'
    )
  ],
  [
    "Paused",
    new beet159.BeetArgsStruct(
      [["fields", beet159.fixedSizeTuple([beet159.bool])]],
      'MultisigConfigRecord["Paused"]'
    )
  ],
  [
    "Quorum",
    new beet159.BeetArgsStruct(
      [["fields", beet159.fixedSizeTuple([beet159.u8])]],
      'MultisigConfigRecord["Quorum"]'
    )
  ],
  [
    "Signers",
    new beet159.FixableBeetArgsStruct(
      [
        [
          "fields",
          beet159.tuple([beet159.array(beet159.uniformFixedSizeArray(beet159.u8, 64))])
        ]
      ],
      'MultisigConfigRecord["Signers"]'
    )
  ]
]);
var VerificationState = /* @__PURE__ */ ((VerificationState3) => {
  VerificationState3[VerificationState3["Verifying"] = 0] = "Verifying";
  VerificationState3[VerificationState3["Verifiable"] = 1] = "Verifiable";
  VerificationState3[VerificationState3["Verified"] = 2] = "Verified";
  VerificationState3[VerificationState3["NotInitializable"] = 3] = "NotInitializable";
  VerificationState3[VerificationState3["VerifiableButCapExceeded"] = 4] = "VerifiableButCapExceeded";
  return VerificationState3;
})(VerificationState || {});
var verificationStateBeet = beet159.fixedScalarEnum(
  VerificationState
);

// src/generated/solita/dvn/index.ts
var PROGRAM_ADDRESS3 = "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW";
var PROGRAM_ID3 = new PublicKey(PROGRAM_ADDRESS3);

// src/generated/solita/dvn/events/index.ts
var events_exports2 = {};
__export(events_exports2, {
  adminConfigSetEventBeet: () => adminConfigSetEventBeet,
  feeWithdrawnEventBeet: () => feeWithdrawnEventBeet,
  multisigConfigSetEventBeet: () => multisigConfigSetEventBeet
});
var adminConfigSetEventBeet = new beet159.FixableBeetArgsStruct(
  [["config", adminConfigBeet]],
  "AdminConfigSetEvent"
);
var feeWithdrawnEventBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["amount", beet159.u64]
  ],
  "FeeWithdrawnEvent"
);
var multisigConfigSetEventBeet = new beet159.FixableBeetArgsStruct(
  [["config", multisigConfigBeet]],
  "MultisigConfigSetEvent"
);

// src/solita/dvn.ts
var SolanaSignerExt = class _SolanaSignerExt {
  constructor(signers) {
    this.signers = signers;
  }
  /**
   * Creates an instance of the SolanaSignerExt class.
   *
   * @param {HashSigner[]} signers - The signers.
   * @returns {SolanaSignerExt} The SolanaSignerExt instance.
   */
  static from(signers) {
    return new _SolanaSignerExt(signers);
  }
  /**
   * Signs a message.
   *
   * @param {Buffer} message - The message to sign.
   * @returns {Promise<{ signature: Uint8Array; recoveryId: number }[]>} A promise that resolves to an array of signature objects.
   */
  async sign(message) {
    return Promise.all(
      this.signers.map(async (s) => {
        const rv = await s.signHash(message);
        return { signature: rv.slice(0, 64), recoveryId: rv[64] };
      })
    );
  }
};
var DVN = class {
  /**
   * Creates an instance of the DVN class.
   *
   * @param {PublicKey} programId - The program ID.
   * @param {EndpointId} [endpointId=EndpointId.SOLANA_V2_SANDBOX] - The endpoint ID.
   */
  constructor(programId, endpointId = EndpointId.SOLANA_V2_SANDBOX) {
    this.programId = programId;
    this.dvnDeriver = new DVNDeriver(programId);
    this.vid = endpointId % 3e4;
    this.eventAuthority = new EventPDADeriver(programId).eventAuthority()[0];
  }
  /**
   * Initializes the DVN.
   *
   * @param {Connection} connection - The connection.
   * @param {PublicKey} payer - The payer.
   * @param {InitDvnParams} params - The initialization parameters.
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   * @throws {Error} If the DVN is already initialized.
   */
  async initDVN(connection, payer, params) {
    const [config2] = this.dvnDeriver.config();
    const info = await connection.getAccountInfo(config2);
    if (info) {
      throw new Error("DVN already initialized");
    }
    return createInitDvnInstruction(
      {
        payer,
        config: config2
      },
      {
        params
      },
      this.programId
    );
  }
  /**
   * Gets the digest for a transaction.
   *
   * @param {number} vid - The validator ID.
   * @param {TransactionInstruction} instruction - The transaction instruction.
   * @param {number} expiration - The expiration time.
   * @returns {types.ExecuteTransactionDigest} The transaction digest.
   */
  getDigest(vid, instruction, expiration) {
    return {
      vid,
      programId: instruction.programId,
      accounts: instruction.keys.map((key) => {
        return {
          pubkey: key.pubkey,
          isSigner: key.isSigner,
          isWritable: key.isWritable
        };
      }),
      data: instruction.data,
      expiration
    };
  }
  /**
   * Gets the execute hash for a given hash bytes.
   *
   * @param {Buffer} hashBytes - The hash bytes.
   * @returns {PublicKey} The execute hash.
   */
  getExecuteHash(hashBytes) {
    const [executeHash] = this.dvnDeriver.executeHash(hashBytes);
    return executeHash;
  }
  /**
   * Gets the hash bytes for a given digest.
   *
   * @param {types.ExecuteTransactionDigest} digest - The transaction digest.
   * @returns {Buffer} The hash bytes.
   */
  getHashBytes(digest) {
    const [digestBytes] = executeTransactionDigestBeet.serialize(digest);
    const hash = keccak_256(digestBytes);
    return Buffer.from(hash);
  }
  /**
   * Invokes a transaction.
   *
   * @param {Connection} connection - The connection.
   * @param {number} vid - The validator ID.
   * @param {PublicKey} payer - The payer.
   * @param {TransactionInstruction} instruction - The transaction instruction.
   * @param {number} expiration - The expiration time.
   * @param {SignFunc} sign - The sign function.
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   * @throws {Error} If the DVN is not initialized.
   */
  async invoke(connection, vid, payer, instruction, expiration, sign) {
    const configState = await this.getConfigState(connection, "confirmed");
    if (!configState) {
      throw new Error("DVN not initialized");
    }
    const [config2] = this.dvnDeriver.config();
    const digest = this.getDigest(vid, instruction, expiration);
    const hashBytes = this.getHashBytes(digest);
    const executeHash = this.getExecuteHash(hashBytes);
    const signatures = [];
    const signResult = await sign.sign(hashBytes);
    signatures.push(...signResult.map((s) => Array.from(s.signature).concat([s.recoveryId])));
    const param = {
      digest,
      signatures
    };
    const remainingAccounts = instruction.keys.map((key) => {
      key.isSigner = false;
      return key;
    });
    return createInvokeInstruction(
      {
        signer: payer,
        config: config2,
        executeHash,
        program: this.programId,
        eventAuthority: this.eventAuthority,
        anchorRemainingAccounts: [
          {
            pubkey: instruction.programId,
            isSigner: false,
            isWritable: false
          }
        ].concat(remainingAccounts)
      },
      {
        params: param
      },
      this.programId
    );
  }
  /**
   * Creates a transaction instruction to set the quorum.
   *
   * @param {number} quorum - The quorum.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createSetQuorumInstruction(quorum) {
    const params = {
      fields: [quorum],
      __kind: "Quorum"
    };
    const fixedBeet = multisigConfigBeet.toFixedFromValue(params);
    const buffer = Buffer.alloc(fixedBeet.byteSize);
    fixedBeet.write(buffer, 0, params);
    return new TransactionInstruction({
      programId: this.programId,
      keys: [],
      data: buffer
    });
  }
  /**
   * Creates a transaction instruction to set the admins.
   *
   * @param {PublicKey[]} admins - The admin public keys.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createSetAdminsInstruction(admins) {
    const params = {
      fields: [admins],
      __kind: "Admins"
    };
    const fixedBeet = multisigConfigBeet.toFixedFromValue(params);
    const buffer = Buffer.alloc(fixedBeet.byteSize);
    fixedBeet.write(buffer, 0, params);
    return new TransactionInstruction({
      programId: this.programId,
      keys: [],
      data: buffer
    });
  }
  /**
   * Creates a transaction instruction to set the signers.
   *
   * @param {Uint8Array[]} signers - The signer public keys.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createSetSignersInstruction(signers) {
    const params = {
      fields: [signers.map((s) => Array.from(s))],
      __kind: "Signers"
    };
    const fixedBeet = multisigConfigBeet.toFixedFromValue(params);
    const buffer = Buffer.alloc(fixedBeet.byteSize);
    fixedBeet.write(buffer, 0, params);
    return new TransactionInstruction({
      programId: this.programId,
      keys: [],
      data: buffer
    });
  }
  /**
   * Creates a transaction instruction to set the allowlist.
   *
   * @param {PublicKey[]} allowlist - The allowlist public keys.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createSetAllowlistInstruction(allowlist) {
    const params = {
      fields: [allowlist],
      __kind: "Allowlist"
    };
    const fixedBeet = multisigConfigBeet.toFixedFromValue(params);
    const buffer = Buffer.alloc(fixedBeet.byteSize);
    fixedBeet.write(buffer, 0, params);
    return new TransactionInstruction({
      programId: this.programId,
      keys: [],
      data: buffer
    });
  }
  /**
   * Creates a transaction instruction to set the denylist.
   *
   * @param {PublicKey[]} denylist - The denylist public keys.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createSetDenylistInstruction(denylist) {
    const params = {
      fields: [denylist],
      __kind: "Denylist"
    };
    const fixedBeet = multisigConfigBeet.toFixedFromValue(params);
    const buffer = Buffer.alloc(fixedBeet.byteSize);
    fixedBeet.write(buffer, 0, params);
    return new TransactionInstruction({
      programId: this.programId,
      keys: [],
      data: buffer
    });
  }
  /**
   * Creates a transaction instruction to set the pause state.
   *
   * @param {boolean} pause - The pause state.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createSetPauseInstruction(pause) {
    const params = {
      fields: [pause],
      __kind: "Paused"
    };
    const fixedBeet = multisigConfigBeet.toFixedFromValue(params);
    const buffer = Buffer.alloc(fixedBeet.byteSize);
    fixedBeet.write(buffer, 0, params);
    return new TransactionInstruction({
      programId: this.programId,
      keys: [],
      data: buffer
    });
  }
  /**
   * Creates a transaction instruction to set the default multiplier basis points.
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {number} defaultMultiplierBps - The default multiplier basis points.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createSetDefaultMultiplierBpsInstruction(admin, defaultMultiplierBps) {
    const [configAccount] = this.dvnDeriver.config();
    return createSetConfigInstruction3(
      {
        admin,
        config: configAccount,
        program: this.programId,
        eventAuthority: this.eventAuthority
      },
      {
        params: {
          config: {
            fields: [defaultMultiplierBps],
            __kind: "DefaultMultiplierBps"
          }
        }
      },
      this.programId
    );
  }
  /**
   * Creates a transaction instruction to change the admins.
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {PublicKey[]} admins - The new admin public keys.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createChangeAdminsInstruction(admin, admins) {
    const [configAccount] = this.dvnDeriver.config();
    return createSetConfigInstruction3(
      {
        admin,
        config: configAccount,
        program: this.programId,
        eventAuthority: this.eventAuthority
      },
      {
        params: {
          config: {
            fields: [admins],
            __kind: "Admins"
          }
        }
      },
      this.programId
    );
  }
  /**
   * Creates a transaction instruction to set the destination configurations.
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {types.DstConfig[]} dstConfigs - The destination configurations.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createSetDstConfigInstruction(admin, dstConfigs) {
    const [configAccount] = this.dvnDeriver.config();
    return createSetConfigInstruction3(
      {
        admin,
        config: configAccount,
        program: this.programId,
        eventAuthority: this.eventAuthority
      },
      {
        params: {
          config: {
            fields: [dstConfigs],
            __kind: "DstConfigs"
          }
        }
      },
      this.programId
    );
  }
  /**
   * Creates a transaction instruction to remove the destination configurations.
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {number[]} removeEids - The endpoint IDs to remove.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createRemoveDstConfigInstruction(admin, removeEids) {
    const [configAccount] = this.dvnDeriver.config();
    return createSetConfigInstruction3(
      {
        admin,
        config: configAccount,
        program: this.programId,
        eventAuthority: this.eventAuthority
      },
      {
        params: {
          config: {
            fields: [removeEids],
            __kind: "RemoveDstConfigs"
          }
        }
      },
      this.programId
    );
  }
  /**
   * Creates a transaction instruction to set the message libraries.
   *
   * @param {PublicKey[]} msglibPrograms - The message library public keys.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createSetMsgLibsInstruction(msglibPrograms) {
    const params = {
      fields: [msglibPrograms],
      __kind: "Msglibs"
    };
    const fixedBeet = multisigConfigBeet.toFixedFromValue(params);
    const buffer = Buffer.alloc(fixedBeet.byteSize);
    fixedBeet.write(buffer, 0, params);
    return new TransactionInstruction({
      programId: this.programId,
      keys: [],
      data: buffer
    });
  }
  /**
   * Gets the expiration time.
   *
   * @returns {number} The expiration time.
   */
  _getExpiration() {
    return (/* @__PURE__ */ new Date()).getTime() / 1e3 + 120;
  }
  /**
   * Sets the message libraries.
   *
   * @param {Connection} connection - The connection.
   * @param {PublicKey} payer - The payer.
   * @param {PublicKey[]} msgLibs - The message library public keys.
   * @param {SignFunc} sign - The sign function.
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   */
  async setMsgLibs(connection, payer, msgLibs, sign) {
    const expiration = this._getExpiration();
    return this.invoke(connection, this.vid, payer, this.createSetMsgLibsInstruction(msgLibs), expiration, sign);
  }
  /**
   * Creates a transaction instruction to set the price feed.
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {PublicKey} priceFeedProgram - The price feed program public key.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  createSetPriceFeedInstruction(admin, priceFeedProgram) {
    const [configAccount] = this.dvnDeriver.config();
    const [priceFeedPda] = new PriceFeedPDADeriver(priceFeedProgram).priceFeed();
    return createSetConfigInstruction3(
      {
        admin,
        config: configAccount,
        program: this.programId,
        eventAuthority: this.eventAuthority
      },
      {
        params: {
          config: {
            fields: [priceFeedPda],
            __kind: "PriceFeed"
          }
        }
      },
      this.programId
    );
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the quote instruction.
   *
   * @param {PublicKey} priceFeedConfig - The price feed configuration public key.
   * @param {PublicKey} priceFeedProgram - The price feed program public key.
   * @param {boolean} payment - Indicates if payment is required.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getQuoteIXAccountMetaForCPI(priceFeedConfig, priceFeedProgram, payment) {
    const [config2] = this.dvnDeriver.config();
    const keys = createQuoteDvnInstructionAccounts(
      {
        dvnConfig: config2,
        priceFeedConfig,
        priceFeedProgram
      },
      this.programId
    );
    if (payment) {
      keys[0].isWritable = true;
    }
    return [
      {
        pubkey: this.programId,
        isWritable: false,
        isSigner: false
      }
    ].concat(keys);
  }
  /**
   * Gets the configuration state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {Commitment | GetAccountInfoConfig} [commitment='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<accounts.DvnConfig | null>} A promise that resolves to the configuration state or null if not found.
   */
  async getConfigState(connection, commitment = "confirmed") {
    const [config2] = this.dvnDeriver.config();
    try {
      return await DvnConfig.fromAccountAddress(connection, config2, commitment);
    } catch (e) {
      return null;
    }
  }
};

// src/solita/executor.ts
var executor_exports = {};
__export(executor_exports, {
  EidNotSupportedError: () => EidNotSupportedError2,
  ExecutionState: () => ExecutionState,
  Executor: () => Executor,
  ExecutorConfig: () => ExecutorConfig3,
  ExecutorIsAdminError: () => ExecutorIsAdminError,
  InsufficientBalanceError: () => InsufficientBalanceError,
  InvalidNativeDropReceiverError: () => InvalidNativeDropReceiverError,
  InvalidNativeDropRequestsLengthError: () => InvalidNativeDropRequestsLengthError,
  InvalidOwnerError: () => InvalidOwnerError,
  InvalidSizeError: () => InvalidSizeError,
  MsgLibNotAllowedError: () => MsgLibNotAllowedError2,
  NativeAmountExceedsCapError: () => NativeAmountExceedsCapError,
  Nonce: () => Nonce2,
  NotAdminError: () => NotAdminError2,
  NotExecutorError: () => NotExecutorError,
  PROGRAM_ADDRESS: () => PROGRAM_ADDRESS4,
  PROGRAM_ID: () => PROGRAM_ID4,
  PausedError: () => PausedError2,
  TooManyAdminsError: () => TooManyAdminsError2,
  TooManyExecutorsError: () => TooManyExecutorsError,
  TooManyOptionTypesError: () => TooManyOptionTypesError2,
  UnsupportedOptionTypeError: () => UnsupportedOptionTypeError,
  ZeroLzComposeGasProvidedError: () => ZeroLzComposeGasProvidedError,
  ZeroLzReceiveGasProvidedError: () => ZeroLzReceiveGasProvidedError,
  accountProviders: () => accountProviders4,
  accounts: () => accounts_exports4,
  aclBeet: () => aclBeet2,
  adminSetConfigInstructionDiscriminator: () => adminSetConfigInstructionDiscriminator,
  adminSetConfigParamsBeet: () => adminSetConfigParamsBeet,
  adminSetConfigStruct: () => adminSetConfigStruct,
  composeInstructionDiscriminator: () => composeInstructionDiscriminator,
  composeParamsBeet: () => composeParamsBeet,
  composeStruct: () => composeStruct,
  createAdminSetConfigInstruction: () => createAdminSetConfigInstruction,
  createAdminSetConfigInstructionAccounts: () => createAdminSetConfigInstructionAccounts,
  createComposeInstruction: () => createComposeInstruction,
  createComposeInstructionAccounts: () => createComposeInstructionAccounts,
  createExecutableInstruction: () => createExecutableInstruction,
  createExecutableInstructionAccounts: () => createExecutableInstructionAccounts,
  createExecuteInstruction: () => createExecuteInstruction,
  createExecuteInstructionAccounts: () => createExecuteInstructionAccounts,
  createInitExecutorInstruction: () => createInitExecutorInstruction,
  createInitExecutorInstructionAccounts: () => createInitExecutorInstructionAccounts,
  createNativeDropInstruction: () => createNativeDropInstruction,
  createNativeDropInstructionAccounts: () => createNativeDropInstructionAccounts,
  createOwnerSetConfigInstruction: () => createOwnerSetConfigInstruction,
  createOwnerSetConfigInstructionAccounts: () => createOwnerSetConfigInstructionAccounts,
  createQuoteExecutorInstruction: () => createQuoteExecutorInstruction,
  createQuoteExecutorInstructionAccounts: () => createQuoteExecutorInstructionAccounts,
  dstConfigBeet: () => dstConfigBeet2,
  errorFromCode: () => errorFromCode4,
  errorFromName: () => errorFromName4,
  events: () => events_exports4,
  executableInstructionDiscriminator: () => executableInstructionDiscriminator,
  executableParamsBeet: () => executableParamsBeet,
  executableStruct: () => executableStruct,
  executeInstructionDiscriminator: () => executeInstructionDiscriminator,
  executeParamsBeet: () => executeParamsBeet,
  executeStruct: () => executeStruct,
  executionStateBeet: () => executionStateBeet,
  executorConfigBeet: () => executorConfigBeet2,
  executorConfigDiscriminator: () => executorConfigDiscriminator,
  initExecutorInstructionDiscriminator: () => initExecutorInstructionDiscriminator,
  initExecutorParamsBeet: () => initExecutorParamsBeet,
  initExecutorStruct: () => initExecutorStruct,
  instructions: () => instructions_exports4,
  isAdminSetConfigParamsDefaultMultiplierBps: () => isAdminSetConfigParamsDefaultMultiplierBps,
  isAdminSetConfigParamsDstConfigs: () => isAdminSetConfigParamsDstConfigs,
  isAdminSetConfigParamsPriceFeed: () => isAdminSetConfigParamsPriceFeed,
  isOwnerSetConfigParamsAdmins: () => isOwnerSetConfigParamsAdmins,
  isOwnerSetConfigParamsAllowlist: () => isOwnerSetConfigParamsAllowlist,
  isOwnerSetConfigParamsDenylist: () => isOwnerSetConfigParamsDenylist,
  isOwnerSetConfigParamsExecutors: () => isOwnerSetConfigParamsExecutors,
  isOwnerSetConfigParamsMsglibs: () => isOwnerSetConfigParamsMsglibs,
  isOwnerSetConfigParamsOwner: () => isOwnerSetConfigParamsOwner,
  isOwnerSetConfigParamsPaused: () => isOwnerSetConfigParamsPaused,
  lzComposeParamsBeet: () => lzComposeParamsBeet,
  lzOptionBeet: () => lzOptionBeet2,
  lzReceiveParamsBeet: () => lzReceiveParamsBeet,
  nativeDropInstructionDiscriminator: () => nativeDropInstructionDiscriminator,
  nativeDropParamsBeet: () => nativeDropParamsBeet,
  nativeDropRequestBeet: () => nativeDropRequestBeet,
  nativeDropStruct: () => nativeDropStruct,
  nonceBeet: () => nonceBeet2,
  nonceDiscriminator: () => nonceDiscriminator2,
  ownerSetConfigInstructionDiscriminator: () => ownerSetConfigInstructionDiscriminator,
  ownerSetConfigParamsBeet: () => ownerSetConfigParamsBeet,
  ownerSetConfigStruct: () => ownerSetConfigStruct,
  quoteExecutorInstructionDiscriminator: () => quoteExecutorInstructionDiscriminator,
  quoteExecutorParamsBeet: () => quoteExecutorParamsBeet,
  quoteExecutorStruct: () => quoteExecutorStruct,
  types: () => types_exports4
});
var AddressType = uniformFixedSizeArray(u8, 32);
var MSG_TYPE_OFFSET = 0;
var MessageType = /* @__PURE__ */ ((MessageType3) => {
  MessageType3[MessageType3["VANILLA"] = 1] = "VANILLA";
  MessageType3[MessageType3["COMPOSED_TYPE"] = 2] = "COMPOSED_TYPE";
  return MessageType3;
})(MessageType || {});
var LzReceiveParamsBeet = new FixableBeetArgsStruct(
  [
    ["srcEid", u32],
    ["sender", uniformFixedSizeArray(u8, 32)],
    ["nonce", u64],
    ["guid", uniformFixedSizeArray(u8, 32)],
    ["message", bytes],
    ["callerParams", bytes]
  ],
  "LzReceiveParams"
);
var LzComposeParamsBeet = new FixableBeetArgsStruct(
  [
    ["from", beetSolana96.publicKey],
    ["to", beetSolana96.publicKey],
    ["guid", uniformFixedSizeArray(u8, 32)],
    ["index", u16],
    ["message", bytes],
    ["extraData", bytes]
  ],
  "LzComposeParams"
);
var LzReceiveAccountBeet = new BeetArgsStruct(
  [
    ["pubkey", beetSolana96.publicKey],
    ["isSigner", bool],
    ["isWritable", bool]
  ],
  "LzReceiveAccount"
);
var ExecutorOptionType = /* @__PURE__ */ ((ExecutorOptionType3) => {
  ExecutorOptionType3[ExecutorOptionType3["PlaceHolder"] = 0] = "PlaceHolder";
  ExecutorOptionType3[ExecutorOptionType3["LzReceive"] = 1] = "LzReceive";
  ExecutorOptionType3[ExecutorOptionType3["NativeDrop"] = 2] = "NativeDrop";
  ExecutorOptionType3[ExecutorOptionType3["LzCompose"] = 3] = "LzCompose";
  ExecutorOptionType3[ExecutorOptionType3["OrderExecution"] = 4] = "OrderExecution";
  return ExecutorOptionType3;
})(ExecutorOptionType || {});
var MaxExecutorOptionTypeLength = 10;

// src/generated/solita/uln/events/index.ts
var events_exports3 = {};
__export(events_exports3, {
  adminTransferredEventBeet: () => adminTransferredEventBeet2,
  configSetEventBeet: () => configSetEventBeet,
  defaultConfigSetEventBeet: () => defaultConfigSetEventBeet,
  feesPaidEventBeet: () => feesPaidEventBeet,
  payloadVerifiedEventBeet: () => payloadVerifiedEventBeet,
  rentWithdrawnEventBeet: () => rentWithdrawnEventBeet2,
  treasurySetEventBeet: () => treasurySetEventBeet
});
var adminTransferredEventBeet2 = new beet159.BeetArgsStruct(
  [["newAdmin", beetSolana96.publicKey]],
  "AdminTransferredEvent"
);
var ulnConfigBeet2 = new beet159.FixableBeetArgsStruct(
  [
    ["confirmations", beet159.u64],
    ["requiredDvnCount", beet159.u8],
    ["optionalDvnCount", beet159.u8],
    ["optionalDvnThreshold", beet159.u8],
    ["requiredDvns", beet159.array(beetSolana96.publicKey)],
    ["optionalDvns", beet159.array(beetSolana96.publicKey)]
  ],
  "UlnConfig"
);
var executorConfigBeet = new beet159.BeetArgsStruct(
  [
    ["maxMessageSize", beet159.u32],
    ["executor", beetSolana96.publicKey]
  ],
  "ExecutorConfig"
);

// src/generated/solita/uln/types/Config.ts
var isConfigSendUln = (x) => x.__kind === "SendUln";
var isConfigReceiveUln = (x) => x.__kind === "ReceiveUln";
var isConfigExecutor = (x) => x.__kind === "Executor";
var configBeet = beet159.dataEnum([
  [
    "SendUln",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([ulnConfigBeet2])]],
      'ConfigRecord["SendUln"]'
    )
  ],
  [
    "ReceiveUln",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([ulnConfigBeet2])]],
      'ConfigRecord["ReceiveUln"]'
    )
  ],
  [
    "Executor",
    new beet159.BeetArgsStruct(
      [["fields", beet159.fixedSizeTuple([executorConfigBeet])]],
      'ConfigRecord["Executor"]'
    )
  ]
]);

// src/generated/solita/uln/events/ConfigSetEvent.ts
var configSetEventBeet = new beet159.FixableBeetArgsStruct(
  [
    ["oapp", beetSolana96.publicKey],
    ["eid", beet159.u32],
    ["config", configBeet]
  ],
  "ConfigSetEvent"
);
var defaultConfigSetEventBeet = new beet159.FixableBeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["sendUlnConfig", beet159.coption(ulnConfigBeet2)],
    ["receiveUlnConfig", beet159.coption(ulnConfigBeet2)],
    ["executorConfig", beet159.coption(executorConfigBeet)]
  ],
  "DefaultConfigSetEvent"
);
var workerFeeBeet = new beet159.BeetArgsStruct(
  [
    ["worker", beetSolana96.publicKey],
    ["fee", beet159.u64]
  ],
  "WorkerFee"
);
var treasuryFeeBeet = new beet159.BeetArgsStruct(
  [
    ["treasury", beetSolana96.publicKey],
    ["fee", beet159.u64],
    ["payInLzToken", beet159.bool]
  ],
  "TreasuryFee"
);

// src/generated/solita/uln/events/FeesPaidEvent.ts
var feesPaidEventBeet = new beet159.FixableBeetArgsStruct(
  [
    ["executor", workerFeeBeet],
    ["dvns", beet159.array(workerFeeBeet)],
    ["treasury", beet159.coption(treasuryFeeBeet)]
  ],
  "FeesPaidEvent"
);
var payloadVerifiedEventBeet = new beet159.BeetArgsStruct(
  [
    ["dvn", beetSolana96.publicKey],
    ["header", beet159.uniformFixedSizeArray(beet159.u8, 81)],
    ["confirmations", beet159.u64],
    ["proofHash", beet159.uniformFixedSizeArray(beet159.u8, 32)]
  ],
  "PayloadVerifiedEvent"
);
var rentWithdrawnEventBeet2 = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["amount", beet159.u64]
  ],
  "RentWithdrawnEvent"
);
var lzTokenTreasuryBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["fee", beet159.u64]
  ],
  "LzTokenTreasury"
);

// src/generated/solita/uln/types/Treasury.ts
var treasuryBeet = new beet159.FixableBeetArgsStruct(
  [
    ["admin", beet159.coption(beetSolana96.publicKey)],
    ["nativeReceiver", beetSolana96.publicKey],
    ["nativeFeeBps", beet159.u64],
    ["lzToken", beet159.coption(lzTokenTreasuryBeet)]
  ],
  "Treasury"
);

// src/generated/solita/uln/events/TreasurySetEvent.ts
var treasurySetEventBeet = new beet159.FixableBeetArgsStruct(
  [["treasury", beet159.coption(treasuryBeet)]],
  "TreasurySetEvent"
);

// src/solita/utility.ts
function getEventDiscriminator(event) {
  return Buffer.from(sha2_256(`event:${event}`)).subarray(0, 8).toString("hex");
}
async function extractSentPacketEventByTxHash(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature(
    connection,
    program,
    signature,
    packetSentEventBeet,
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractVerifiedPacketEventByTxHash(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature(
    connection,
    program,
    signature,
    packetVerifiedEventBeet,
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractReceivedPacketEventByTxHash(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature(
    connection,
    program,
    signature,
    packetDeliveredEventBeet,
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractComposeSentEventByTxHash(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature(
    connection,
    program,
    signature,
    composeSentEventBeet,
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractComposeDeliveredEventByTxHash(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature(
    connection,
    program,
    signature,
    composeDeliveredEventBeet,
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractWorkerFeePaidEventByTxHash(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature(
    connection,
    program,
    signature,
    feesPaidEventBeet,
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractEventFromTransactionSignature(connection, program, signature, eventBeet, commitment, unsafeParseErr = false) {
  const tx = typeof signature === "string" ? await connection.getParsedTransaction(signature, commitment) : signature;
  if (tx == null) return null;
  const events = new Array();
  if (tx.meta?.err !== null && !unsafeParseErr) {
    return null;
  }
  for (const instruction of tx.meta?.innerInstructions ?? []) {
    for (const innerInstruction of instruction.instructions) {
      const inst = innerInstruction;
      if (!inst.programId.equals(program)) {
        continue;
      }
      const decoded = base58.decode(inst.data);
      const discriminator = Buffer.from(decoded.subarray(0, 8)).toString("hex");
      if (discriminator !== EventEmitDiscriminator) {
        continue;
      }
      const eventDiscriminator = Buffer.from(decoded.subarray(8, 16)).toString("hex");
      if (eventDiscriminator !== getEventDiscriminator(eventBeet.description)) {
        continue;
      }
      const dataBuffer = Buffer.from(decoded.subarray(16, decoded.length));
      if (eventBeet instanceof beet159.FixableBeetArgsStruct) {
        const beet260 = eventBeet.toFixedFromData(dataBuffer, 0);
        events.push(beet260.read(dataBuffer, 0));
      } else {
        events.push(eventBeet.read(dataBuffer, 0));
      }
    }
  }
  return events.length > 0 ? events : null;
}
async function generateAddressLookupTable(connection, payer, authority, addresses) {
  const slot = await connection.getSlot("finalized");
  const [createInstruction, lookupTableAddress] = AddressLookupTableProgram.createLookupTable({
    payer,
    authority,
    recentSlot: slot
  });
  const extendInstruction = AddressLookupTableProgram.extendLookupTable({
    payer,
    authority,
    lookupTable: lookupTableAddress,
    addresses
  });
  return {
    instructions: [createInstruction, extendInstruction],
    address: lookupTableAddress
  };
}
function deactivateLookupTable(authority, lookupTable) {
  return AddressLookupTableProgram.deactivateLookupTable({
    authority,
    lookupTable
  });
}
function closeLookupTable(recipient, authority, lookupTable) {
  return AddressLookupTableProgram.closeLookupTable({
    authority,
    recipient,
    lookupTable
  });
}
async function txWithAddressLookupTable(connection, payer, instructions, recentBlockHash, _tableAddrs) {
  recentBlockHash = recentBlockHash ?? (await connection.getLatestBlockhash()).blockhash;
  if (!_tableAddrs) {
    return new VersionedTransaction(
      new TransactionMessage({
        instructions,
        payerKey: payer,
        recentBlockhash: recentBlockHash
      }).compileToV0Message()
    );
  }
  const tableAddrs = Array.isArray(_tableAddrs) ? _tableAddrs : [_tableAddrs];
  const lookupTableAccountsResult = await connection.getMultipleAccountsInfo(tableAddrs);
  const lookupTableAccounts = lookupTableAccountsResult.map((result, index) => {
    if (result === null) return null;
    return new AddressLookupTableAccount({
      key: tableAddrs[index],
      state: AddressLookupTableAccount.deserialize(result.data)
    });
  }).filter((account) => account !== null);
  return new VersionedTransaction(
    new TransactionMessage({
      instructions,
      payerKey: payer,
      recentBlockhash: recentBlockHash
    }).compileToV0Message(lookupTableAccounts.length > 0 ? lookupTableAccounts : void 0)
  );
}
async function createNonceAccountTX(connection, auth, lamportsForRent) {
  const nonceAccount = Keypair.generate();
  const lamports = lamportsForRent ?? await connection.getMinimumBalanceForRentExemption(NONCE_ACCOUNT_LENGTH);
  const tx = new Transaction();
  tx.add(
    // create nonce account
    SystemProgram.createAccount({
      fromPubkey: auth,
      newAccountPubkey: nonceAccount.publicKey,
      lamports,
      space: NONCE_ACCOUNT_LENGTH,
      programId: SystemProgram.programId
    }),
    // init nonce account
    SystemProgram.nonceInitialize({
      noncePubkey: nonceAccount.publicKey,
      // nonce account pubkey
      authorizedPubkey: auth
      // nonce account auth
    })
  );
  return { tx, nonceAccount };
}
async function txWithNonce(connection, noncePubkey, instructions, nonceInfo) {
  const tx = new Transaction();
  if (!nonceInfo) {
    const accountInfo = await connection.getAccountInfo(noncePubkey);
    if (accountInfo) {
      nonceInfo = NonceAccount.fromAccountData(accountInfo.data);
    } else {
      return null;
    }
  }
  tx.add(
    // nonce advance must be the first insturction
    SystemProgram.nonceAdvance({
      noncePubkey,
      authorizedPubkey: nonceInfo.authorizedPubkey
    })
  );
  tx.add(...instructions);
  tx.recentBlockhash = nonceInfo.nonce;
  return tx;
}
async function isAccountInitialized(connection, account, commitmentOrConfig) {
  return connection.getAccountInfo(account, commitmentOrConfig).then((res) => {
    return res !== null;
  });
}
async function buildMessageV0(connection, payerKey, instructions, commitmentOrConfig = "confirmed", blockhash) {
  return new TransactionMessage({
    payerKey,
    recentBlockhash: blockhash ?? (await connection.getLatestBlockhash(commitmentOrConfig)).blockhash,
    instructions
  }).compileToV0Message();
}
async function buildVersionedTransaction(connection, payerKey, instructions, commitmentOrConfig = "confirmed", blockhash, lookupTableAddresses) {
  if (lookupTableAddresses) {
    return txWithAddressLookupTable(connection, payerKey, instructions, blockhash, lookupTableAddresses);
  }
  return new VersionedTransaction(
    await buildMessageV0(connection, payerKey, instructions, commitmentOrConfig, blockhash)
  );
}
function instructionDiscriminator(method) {
  return Buffer.from(sha2_256(Buffer.from(`global:${method}`))).subarray(0, 8);
}
async function simulateTransaction(connection, instructions, programId, payer, commitment = "confirmed", blockhash, lookupTableAddresses) {
  const tx = await buildVersionedTransaction(
    connection,
    payer,
    instructions,
    commitment,
    blockhash,
    lookupTableAddresses
  );
  const simulateResp = await connection.simulateTransaction(tx, { sigVerify: false, commitment });
  const returnPrefix = `Program return: ${programId.toBase58()} `;
  const returnLog = simulateResp.value.logs?.find((l) => l.startsWith(returnPrefix));
  if (returnLog === void 0 || simulateResp.value.returnData?.programId !== programId.toBase58()) {
    throw new Error(`Simulate Fail: ${JSON.stringify(simulateResp)}`);
  } else {
    return Buffer.from(returnLog.slice(returnPrefix.length), "base64");
  }
}

// src/solita/recevie.ts
async function lzReceive(connection, payer, packet, callerParams = Uint8Array.from([0, 0]), commitmentOrConfig = "confirmed") {
  const { message: message_, sender, srcEid, guid, receiver: receiver_ } = packet;
  const receiver = new PublicKey(addressToBytes32(receiver_));
  const message = arrayify(message_);
  const params = {
    srcEid,
    sender: Array.from(arrayify(sender)),
    guid: Array.from(arrayify(guid)),
    message,
    callerParams,
    nonce: parseInt(packet.nonce)
  };
  const receiverInfo = await connection.getParsedAccountInfo(receiver, commitmentOrConfig);
  const receiverProgram = new PublicKey(receiverInfo.value.owner);
  const accounts = await getLzReceiveAccounts(
    connection,
    payer,
    receiver,
    receiverProgram,
    params,
    commitmentOrConfig
  );
  const [data] = LzReceiveParamsBeet.serialize(params);
  return new TransactionInstruction({
    programId: receiverProgram,
    keys: accounts,
    data: Buffer.concat([instructionDiscriminator("lz_receive"), data])
  });
}
async function lzCompose(connection, payer, event, extraData = Uint8Array.from([0, 0]), commitmentOrConfig) {
  const { to, from, guid, index, message } = event;
  const params = {
    from,
    to,
    guid,
    index,
    message,
    extraData
  };
  const accountInfo = await connection.getAccountInfo(to, commitmentOrConfig);
  if (!accountInfo) {
    throw new Error(`Account not found: ${to.toBase58()}`);
  }
  const programId = accountInfo.owner;
  const accounts = await getLzComposeAccountMeta(connection, payer, to, programId, params, commitmentOrConfig);
  const [data] = LzComposeParamsBeet.serialize(params);
  return new TransactionInstruction({
    programId,
    keys: accounts,
    data: Buffer.concat([instructionDiscriminator("lz_compose"), data])
  });
}
async function getLzReceiveAccounts(connection, payer, receiver, receiverProgram, params, commitmentOrConfig = "confirmed") {
  const lzReceiveTypesAccounts = await (async () => {
    const [lzReceiveTypesAccountsPDA] = deriveLzReceiveTypesAccountsPDA(receiverProgram, receiver);
    const info = await connection.getAccountInfo(lzReceiveTypesAccountsPDA, commitmentOrConfig);
    const accounts = [];
    if (info) {
      const buffer = Buffer.from(info.data);
      const len = buffer.length - 8;
      if (len % 32 !== 0) {
        throw new Error(
          `Invalid length of AccountInfo.data. The length must be a multiple of 32 plus 8.(n*32+8). Current length is ${buffer.length}`
        );
      }
      for (let i = 8; i < len; i += 32) {
        const address = AddressType.read(buffer, i);
        accounts.push({
          pubkey: new PublicKey(address),
          isSigner: false,
          isWritable: false
        });
      }
    }
    return accounts;
  })();
  const [data] = LzReceiveParamsBeet.serialize(params);
  const lzReceiveTypesIx = new TransactionInstruction({
    programId: receiverProgram,
    keys: lzReceiveTypesAccounts,
    data: Buffer.concat([instructionDiscriminator("lz_receive_types"), data])
  });
  const response = await simulateTransaction(connection, [lzReceiveTypesIx], receiverProgram, payer);
  const keys = retrieveAccountFromSimulatedResp(response, payer);
  return keys;
}
async function getLzComposeAccountMeta(connection, payer, to, composerProgram, params, commitmentOrConfig = "confirmed") {
  const lzComposeTypesAccounts = await (async () => {
    const [lzComposeTypesAccountsPDA] = deriveLzComposeTypesAccountsPDA(composerProgram, to);
    const info = await connection.getAccountInfo(lzComposeTypesAccountsPDA, commitmentOrConfig);
    const accounts = [];
    if (info) {
      const buffer = Buffer.from(info.data);
      const len = buffer.length - 8;
      if (len % 32 !== 0) {
        throw new Error(
          `Invalid length of AccountInfo.data. The length must be a multiple of 32 plus 8.(n*32+8). Current length is ${buffer.length}`
        );
      }
      for (let i = 8; i < len; i += 32) {
        const address = AddressType.read(buffer, i);
        accounts.push({
          pubkey: new PublicKey(address),
          isSigner: false,
          isWritable: false
        });
      }
    }
    return accounts;
  })();
  const [data] = LzComposeParamsBeet.serialize(params);
  const lzComposeTypesIx = new TransactionInstruction({
    programId: composerProgram,
    keys: lzComposeTypesAccounts,
    data: Buffer.concat([instructionDiscriminator("lz_compose_types"), data])
  });
  const response = await simulateTransaction(connection, [lzComposeTypesIx], composerProgram, payer);
  const keys = retrieveAccountFromSimulatedResp(response, payer);
  return keys;
}
function retrieveAccountFromSimulatedResp(resp, payer) {
  const result = array(LzReceiveAccountBeet);
  const fixedBeet = result.toFixedFromData(resp, 0);
  const results = fixedBeet.read(resp, 0);
  return results.map((r) => {
    if (r.pubkey.toBase58() == PublicKey.default.toBase58() && r.isSigner) {
      if (!payer) throw new Error("payer is required");
      return {
        pubkey: payer,
        isSigner: true,
        isWritable: r.isWritable
      };
    } else {
      return {
        pubkey: r.pubkey,
        isSigner: r.isSigner,
        isWritable: r.isWritable
      };
    }
  });
}

// src/generated/solita/executor/accounts/index.ts
var accounts_exports4 = {};
__export(accounts_exports4, {
  ExecutorConfig: () => ExecutorConfig3,
  Nonce: () => Nonce2,
  accountProviders: () => accountProviders4,
  executorConfigBeet: () => executorConfigBeet2,
  executorConfigDiscriminator: () => executorConfigDiscriminator,
  nonceBeet: () => nonceBeet2,
  nonceDiscriminator: () => nonceDiscriminator2
});
var aclBeet2 = new beet159.FixableBeetArgsStruct(
  [
    ["allowList", beet159.array(beetSolana96.publicKey)],
    ["denyList", beet159.array(beetSolana96.publicKey)]
  ],
  "Acl"
);
var dstConfigBeet2 = new beet159.FixableBeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["lzReceiveBaseGas", beet159.u32],
    ["lzComposeBaseGas", beet159.u32],
    ["multiplierBps", beet159.coption(beet159.u16)],
    ["floorMarginUsd", beet159.coption(beet159.u128)],
    ["nativeDropCap", beet159.u128]
  ],
  "DstConfig"
);

// src/generated/solita/executor/accounts/ExecutorConfig.ts
var executorConfigDiscriminator = [134, 17, 226, 24, 10, 173, 157, 78];
var ExecutorConfig3 = class _ExecutorConfig {
  constructor(bump, owner, acl, admins, executors, msglibs, paused, defaultMultiplierBps, priceFeed, dstConfigs) {
    this.bump = bump;
    this.owner = owner;
    this.acl = acl;
    this.admins = admins;
    this.executors = executors;
    this.msglibs = msglibs;
    this.paused = paused;
    this.defaultMultiplierBps = defaultMultiplierBps;
    this.priceFeed = priceFeed;
    this.dstConfigs = dstConfigs;
  }
  /**
   * Creates a {@link ExecutorConfig} instance from the provided args.
   */
  static fromArgs(args) {
    return new _ExecutorConfig(
      args.bump,
      args.owner,
      args.acl,
      args.admins,
      args.executors,
      args.msglibs,
      args.paused,
      args.defaultMultiplierBps,
      args.priceFeed,
      args.dstConfigs
    );
  }
  /**
   * Deserializes the {@link ExecutorConfig} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _ExecutorConfig.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link ExecutorConfig} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find ExecutorConfig account at ${address}`);
    }
    return _ExecutorConfig.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, executorConfigBeet2);
  }
  /**
   * Deserializes the {@link ExecutorConfig} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return executorConfigBeet2.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link ExecutorConfig} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return executorConfigBeet2.serialize({
      accountDiscriminator: executorConfigDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link ExecutorConfig} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _ExecutorConfig.fromArgs(args);
    return executorConfigBeet2.toFixedFromValue({
      accountDiscriminator: executorConfigDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link ExecutorConfig} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _ExecutorConfig.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link ExecutorConfig} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      bump: this.bump,
      owner: this.owner.toBase58(),
      acl: this.acl,
      admins: this.admins,
      executors: this.executors,
      msglibs: this.msglibs,
      paused: this.paused,
      defaultMultiplierBps: this.defaultMultiplierBps,
      priceFeed: this.priceFeed.toBase58(),
      dstConfigs: this.dstConfigs
    };
  }
};
var executorConfigBeet2 = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["bump", beet159.u8],
    ["owner", beetSolana96.publicKey],
    ["acl", aclBeet2],
    ["admins", beet159.array(beetSolana96.publicKey)],
    ["executors", beet159.array(beetSolana96.publicKey)],
    ["msglibs", beet159.array(beetSolana96.publicKey)],
    ["paused", beet159.bool],
    ["defaultMultiplierBps", beet159.u16],
    ["priceFeed", beetSolana96.publicKey],
    ["dstConfigs", beet159.array(dstConfigBeet2)]
  ],
  ExecutorConfig3.fromArgs,
  "ExecutorConfig"
);
var nonceDiscriminator2 = [143, 197, 147, 95, 106, 165, 50, 43];
var Nonce2 = class _Nonce {
  constructor(bump, outboundNonce, inboundNonce) {
    this.bump = bump;
    this.outboundNonce = outboundNonce;
    this.inboundNonce = inboundNonce;
  }
  /**
   * Creates a {@link Nonce} instance from the provided args.
   */
  static fromArgs(args) {
    return new _Nonce(args.bump, args.outboundNonce, args.inboundNonce);
  }
  /**
   * Deserializes the {@link Nonce} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _Nonce.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Nonce} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find Nonce account at ${address}`);
    }
    return _Nonce.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, nonceBeet2);
  }
  /**
   * Deserializes the {@link Nonce} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return nonceBeet2.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link Nonce} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return nonceBeet2.serialize({
      accountDiscriminator: nonceDiscriminator2,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Nonce}
   */
  static get byteSize() {
    return nonceBeet2.byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Nonce} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _Nonce.byteSize,
      commitment
    );
  }
  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link Nonce} data.
   */
  static hasCorrectByteSize(buf, offset = 0) {
    return buf.byteLength - offset === _Nonce.byteSize;
  }
  /**
   * Returns a readable version of {@link Nonce} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      bump: this.bump,
      outboundNonce: (() => {
        const x = this.outboundNonce;
        if (typeof x.toNumber === "function") {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      inboundNonce: (() => {
        const x = this.inboundNonce;
        if (typeof x.toNumber === "function") {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })()
    };
  }
};
var nonceBeet2 = new beet159.BeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["bump", beet159.u8],
    ["outboundNonce", beet159.u64],
    ["inboundNonce", beet159.u64]
  ],
  Nonce2.fromArgs,
  "Nonce"
);

// src/generated/solita/executor/accounts/index.ts
var accountProviders4 = { Nonce: Nonce2, ExecutorConfig: ExecutorConfig3 };

// src/generated/solita/executor/events/index.ts
var events_exports4 = {};
__export(events_exports4, {
  nativeDropAppliedEventBeet: () => nativeDropAppliedEventBeet
});
var nativeDropRequestBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["amount", beet159.u64]
  ],
  "NativeDropRequest"
);

// src/generated/solita/executor/events/NativeDropAppliedEvent.ts
var nativeDropAppliedEventBeet = new beet159.FixableBeetArgsStruct(
  [
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["nonce", beet159.u64],
    ["dstEid", beet159.u32],
    ["oapp", beetSolana96.publicKey],
    ["nativeDropRequests", beet159.array(nativeDropRequestBeet)],
    ["successes", beet159.array(beet159.bool)]
  ],
  "NativeDropAppliedEvent"
);

// src/generated/solita/executor/instructions/index.ts
var instructions_exports4 = {};
__export(instructions_exports4, {
  adminSetConfigInstructionDiscriminator: () => adminSetConfigInstructionDiscriminator,
  adminSetConfigStruct: () => adminSetConfigStruct,
  composeInstructionDiscriminator: () => composeInstructionDiscriminator,
  composeStruct: () => composeStruct,
  createAdminSetConfigInstruction: () => createAdminSetConfigInstruction,
  createAdminSetConfigInstructionAccounts: () => createAdminSetConfigInstructionAccounts,
  createComposeInstruction: () => createComposeInstruction,
  createComposeInstructionAccounts: () => createComposeInstructionAccounts,
  createExecutableInstruction: () => createExecutableInstruction,
  createExecutableInstructionAccounts: () => createExecutableInstructionAccounts,
  createExecuteInstruction: () => createExecuteInstruction,
  createExecuteInstructionAccounts: () => createExecuteInstructionAccounts,
  createInitExecutorInstruction: () => createInitExecutorInstruction,
  createInitExecutorInstructionAccounts: () => createInitExecutorInstructionAccounts,
  createNativeDropInstruction: () => createNativeDropInstruction,
  createNativeDropInstructionAccounts: () => createNativeDropInstructionAccounts,
  createOwnerSetConfigInstruction: () => createOwnerSetConfigInstruction,
  createOwnerSetConfigInstructionAccounts: () => createOwnerSetConfigInstructionAccounts,
  createQuoteExecutorInstruction: () => createQuoteExecutorInstruction,
  createQuoteExecutorInstructionAccounts: () => createQuoteExecutorInstructionAccounts,
  executableInstructionDiscriminator: () => executableInstructionDiscriminator,
  executableStruct: () => executableStruct,
  executeInstructionDiscriminator: () => executeInstructionDiscriminator,
  executeStruct: () => executeStruct,
  initExecutorInstructionDiscriminator: () => initExecutorInstructionDiscriminator,
  initExecutorStruct: () => initExecutorStruct,
  nativeDropInstructionDiscriminator: () => nativeDropInstructionDiscriminator,
  nativeDropStruct: () => nativeDropStruct,
  ownerSetConfigInstructionDiscriminator: () => ownerSetConfigInstructionDiscriminator,
  ownerSetConfigStruct: () => ownerSetConfigStruct,
  quoteExecutorInstructionDiscriminator: () => quoteExecutorInstructionDiscriminator,
  quoteExecutorStruct: () => quoteExecutorStruct
});
var isAdminSetConfigParamsPriceFeed = (x) => x.__kind === "PriceFeed";
var isAdminSetConfigParamsDefaultMultiplierBps = (x) => x.__kind === "DefaultMultiplierBps";
var isAdminSetConfigParamsDstConfigs = (x) => x.__kind === "DstConfigs";
var adminSetConfigParamsBeet = beet159.dataEnum([
  [
    "PriceFeed",
    new beet159.BeetArgsStruct(
      [["fields", beet159.fixedSizeTuple([beetSolana96.publicKey])]],
      'AdminSetConfigParamsRecord["PriceFeed"]'
    )
  ],
  [
    "DefaultMultiplierBps",
    new beet159.BeetArgsStruct(
      [["fields", beet159.fixedSizeTuple([beet159.u16])]],
      'AdminSetConfigParamsRecord["DefaultMultiplierBps"]'
    )
  ],
  [
    "DstConfigs",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(dstConfigBeet2)])]],
      'AdminSetConfigParamsRecord["DstConfigs"]'
    )
  ]
]);

// src/generated/solita/executor/instructions/adminSetConfig.ts
var adminSetConfigStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", adminSetConfigParamsBeet]
  ],
  "AdminSetConfigInstructionArgs"
);
var adminSetConfigInstructionDiscriminator = [
  35,
  56,
  160,
  84,
  132,
  156,
  27,
  79
];
function createAdminSetConfigInstruction(accounts, args, programId) {
  const [data] = adminSetConfigStruct.serialize({
    instructionDiscriminator: adminSetConfigInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createAdminSetConfigInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var lzComposeParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["from", beetSolana96.publicKey],
    ["to", beetSolana96.publicKey],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["index", beet159.u16],
    ["message", beet159.bytes],
    ["extraData", beet159.bytes]
  ],
  "LzComposeParams"
);

// src/generated/solita/executor/types/ComposeParams.ts
var composeParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["lzCompose", lzComposeParamsBeet],
    ["computeUnits", beet159.u64],
    ["value", beet159.u64]
  ],
  "ComposeParams"
);

// src/generated/solita/executor/instructions/compose.ts
var composeStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", composeParamsBeet]
  ],
  "ComposeInstructionArgs"
);
var composeInstructionDiscriminator = [
  106,
  64,
  131,
  142,
  7,
  159,
  42,
  15
];
function createComposeInstruction(accounts, args, programId) {
  const [data] = composeStruct.serialize({
    instructionDiscriminator: composeInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.executor,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpointProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpointEventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createComposeInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.executor,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpointProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpointEventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var executableParamsBeet = new beet159.BeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["nonce", beet159.u64]
  ],
  "ExecutableParams"
);

// src/generated/solita/executor/instructions/executable.ts
var executableStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", executableParamsBeet]
  ],
  "ExecutableInstructionArgs"
);
var executableInstructionDiscriminator = [
  57,
  165,
  171,
  234,
  157,
  191,
  156,
  25
];
function createExecutableInstruction(accounts, args, programId) {
  const [data] = executableStruct.serialize({
    instructionDiscriminator: executableInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createExecutableInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.nonce,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payloadHash,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var lzReceiveParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["nonce", beet159.u64],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["message", beet159.bytes],
    ["extraData", beet159.bytes]
  ],
  "LzReceiveParams"
);

// src/generated/solita/executor/types/ExecuteParams.ts
var executeParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["receiver", beetSolana96.publicKey],
    ["lzReceive", lzReceiveParamsBeet],
    ["value", beet159.u64],
    ["computeUnits", beet159.u64]
  ],
  "ExecuteParams"
);

// src/generated/solita/executor/instructions/execute.ts
var executeStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", executeParamsBeet]
  ],
  "ExecuteInstructionArgs"
);
var executeInstructionDiscriminator = [
  130,
  221,
  242,
  154,
  13,
  193,
  189,
  29
];
function createExecuteInstruction(accounts, args, programId) {
  const [data] = executeStruct.serialize({
    instructionDiscriminator: executeInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.executor,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpointProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpointEventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createExecuteInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.executor,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpointProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.endpointEventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initExecutorParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["owner", beetSolana96.publicKey],
    ["admins", beet159.array(beetSolana96.publicKey)],
    ["executors", beet159.array(beetSolana96.publicKey)],
    ["msglibs", beet159.array(beetSolana96.publicKey)],
    ["priceFeed", beetSolana96.publicKey]
  ],
  "InitExecutorParams"
);

// src/generated/solita/executor/instructions/initExecutor.ts
var initExecutorStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initExecutorParamsBeet]
  ],
  "InitExecutorInstructionArgs"
);
var initExecutorInstructionDiscriminator = [
  195,
  68,
  116,
  173,
  55,
  173,
  159,
  31
];
function createInitExecutorInstruction(accounts, args, programId) {
  const [data] = initExecutorStruct.serialize({
    instructionDiscriminator: initExecutorInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitExecutorInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var nativeDropParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["srcEid", beet159.u32],
    ["sender", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["nonce", beet159.u64],
    ["dstEid", beet159.u32],
    ["oapp", beetSolana96.publicKey],
    ["nativeDropRequests", beet159.array(nativeDropRequestBeet)]
  ],
  "NativeDropParams"
);

// src/generated/solita/executor/instructions/nativeDrop.ts
var nativeDropStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", nativeDropParamsBeet]
  ],
  "NativeDropInstructionArgs"
);
var nativeDropInstructionDiscriminator = [
  105,
  30,
  218,
  121,
  155,
  234,
  85,
  12
];
function createNativeDropInstruction(accounts, args, programId) {
  const [data] = nativeDropStruct.serialize({
    instructionDiscriminator: nativeDropInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.executor,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createNativeDropInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.executor,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var isOwnerSetConfigParamsAdmins = (x) => x.__kind === "Admins";
var isOwnerSetConfigParamsExecutors = (x) => x.__kind === "Executors";
var isOwnerSetConfigParamsMsglibs = (x) => x.__kind === "Msglibs";
var isOwnerSetConfigParamsOwner = (x) => x.__kind === "Owner";
var isOwnerSetConfigParamsPaused = (x) => x.__kind === "Paused";
var isOwnerSetConfigParamsAllowlist = (x) => x.__kind === "Allowlist";
var isOwnerSetConfigParamsDenylist = (x) => x.__kind === "Denylist";
var ownerSetConfigParamsBeet = beet159.dataEnum([
  [
    "Admins",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(beetSolana96.publicKey)])]],
      'OwnerSetConfigParamsRecord["Admins"]'
    )
  ],
  [
    "Executors",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(beetSolana96.publicKey)])]],
      'OwnerSetConfigParamsRecord["Executors"]'
    )
  ],
  [
    "Msglibs",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(beetSolana96.publicKey)])]],
      'OwnerSetConfigParamsRecord["Msglibs"]'
    )
  ],
  [
    "Owner",
    new beet159.BeetArgsStruct(
      [["fields", beet159.fixedSizeTuple([beetSolana96.publicKey])]],
      'OwnerSetConfigParamsRecord["Owner"]'
    )
  ],
  [
    "Paused",
    new beet159.BeetArgsStruct(
      [["fields", beet159.fixedSizeTuple([beet159.bool])]],
      'OwnerSetConfigParamsRecord["Paused"]'
    )
  ],
  [
    "Allowlist",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(beetSolana96.publicKey)])]],
      'OwnerSetConfigParamsRecord["Allowlist"]'
    )
  ],
  [
    "Denylist",
    new beet159.FixableBeetArgsStruct(
      [["fields", beet159.tuple([beet159.array(beetSolana96.publicKey)])]],
      'OwnerSetConfigParamsRecord["Denylist"]'
    )
  ]
]);

// src/generated/solita/executor/instructions/ownerSetConfig.ts
var ownerSetConfigStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", ownerSetConfigParamsBeet]
  ],
  "OwnerSetConfigInstructionArgs"
);
var ownerSetConfigInstructionDiscriminator = [
  99,
  197,
  129,
  19,
  164,
  164,
  183,
  123
];
function createOwnerSetConfigInstruction(accounts, args, programId) {
  const [data] = ownerSetConfigStruct.serialize({
    instructionDiscriminator: ownerSetConfigInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.owner,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createOwnerSetConfigInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.owner,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.config,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var lzOptionBeet2 = new beet159.FixableBeetArgsStruct(
  [
    ["optionType", beet159.u8],
    ["params", beet159.bytes]
  ],
  "LzOption"
);

// src/generated/solita/executor/types/QuoteExecutorParams.ts
var quoteExecutorParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["msglib", beetSolana96.publicKey],
    ["dstEid", beet159.u32],
    ["sender", beetSolana96.publicKey],
    ["calldataSize", beet159.u64],
    ["options", beet159.array(lzOptionBeet2)]
  ],
  "QuoteExecutorParams"
);

// src/generated/solita/executor/instructions/quoteExecutor.ts
var quoteExecutorStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", quoteExecutorParamsBeet]
  ],
  "QuoteExecutorInstructionArgs"
);
var quoteExecutorInstructionDiscriminator = [
  84,
  246,
  255,
  191,
  82,
  65,
  164,
  92
];
function createQuoteExecutorInstruction(accounts, args, programId) {
  const [data] = quoteExecutorStruct.serialize({
    instructionDiscriminator: quoteExecutorInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.executorConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.priceFeedProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.priceFeedConfig,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createQuoteExecutorInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.executorConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.priceFeedProgram,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.priceFeedConfig,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}

// src/generated/solita/executor/types/index.ts
var types_exports4 = {};
__export(types_exports4, {
  ExecutionState: () => ExecutionState,
  aclBeet: () => aclBeet2,
  adminSetConfigParamsBeet: () => adminSetConfigParamsBeet,
  composeParamsBeet: () => composeParamsBeet,
  dstConfigBeet: () => dstConfigBeet2,
  executableParamsBeet: () => executableParamsBeet,
  executeParamsBeet: () => executeParamsBeet,
  executionStateBeet: () => executionStateBeet,
  initExecutorParamsBeet: () => initExecutorParamsBeet,
  isAdminSetConfigParamsDefaultMultiplierBps: () => isAdminSetConfigParamsDefaultMultiplierBps,
  isAdminSetConfigParamsDstConfigs: () => isAdminSetConfigParamsDstConfigs,
  isAdminSetConfigParamsPriceFeed: () => isAdminSetConfigParamsPriceFeed,
  isOwnerSetConfigParamsAdmins: () => isOwnerSetConfigParamsAdmins,
  isOwnerSetConfigParamsAllowlist: () => isOwnerSetConfigParamsAllowlist,
  isOwnerSetConfigParamsDenylist: () => isOwnerSetConfigParamsDenylist,
  isOwnerSetConfigParamsExecutors: () => isOwnerSetConfigParamsExecutors,
  isOwnerSetConfigParamsMsglibs: () => isOwnerSetConfigParamsMsglibs,
  isOwnerSetConfigParamsOwner: () => isOwnerSetConfigParamsOwner,
  isOwnerSetConfigParamsPaused: () => isOwnerSetConfigParamsPaused,
  lzComposeParamsBeet: () => lzComposeParamsBeet,
  lzOptionBeet: () => lzOptionBeet2,
  lzReceiveParamsBeet: () => lzReceiveParamsBeet,
  nativeDropParamsBeet: () => nativeDropParamsBeet,
  nativeDropRequestBeet: () => nativeDropRequestBeet,
  ownerSetConfigParamsBeet: () => ownerSetConfigParamsBeet,
  quoteExecutorParamsBeet: () => quoteExecutorParamsBeet
});
var ExecutionState = /* @__PURE__ */ ((ExecutionState3) => {
  ExecutionState3[ExecutionState3["NotExecutable"] = 0] = "NotExecutable";
  ExecutionState3[ExecutionState3["VerifiedButNotExecutable"] = 1] = "VerifiedButNotExecutable";
  ExecutionState3[ExecutionState3["Executable"] = 2] = "Executable";
  ExecutionState3[ExecutionState3["Executed"] = 3] = "Executed";
  return ExecutionState3;
})(ExecutionState || {});
var executionStateBeet = beet159.fixedScalarEnum(
  ExecutionState
);

// src/generated/solita/executor/errors/index.ts
var createErrorFromCodeLookup4 = /* @__PURE__ */ new Map();
var createErrorFromNameLookup4 = /* @__PURE__ */ new Map();
var InvalidSizeError = class _InvalidSizeError extends Error {
  constructor() {
    super("");
    this.code = 6e3;
    this.name = "InvalidSize";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidSizeError);
    }
  }
};
createErrorFromCodeLookup4.set(6e3, () => new InvalidSizeError());
createErrorFromNameLookup4.set("InvalidSize", () => new InvalidSizeError());
var PausedError2 = class _PausedError extends Error {
  constructor() {
    super("");
    this.code = 6001;
    this.name = "Paused";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _PausedError);
    }
  }
};
createErrorFromCodeLookup4.set(6001, () => new PausedError2());
createErrorFromNameLookup4.set("Paused", () => new PausedError2());
var UnsupportedOptionTypeError = class _UnsupportedOptionTypeError extends Error {
  constructor() {
    super("");
    this.code = 6002;
    this.name = "UnsupportedOptionType";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _UnsupportedOptionTypeError);
    }
  }
};
createErrorFromCodeLookup4.set(6002, () => new UnsupportedOptionTypeError());
createErrorFromNameLookup4.set(
  "UnsupportedOptionType",
  () => new UnsupportedOptionTypeError()
);
var ZeroLzComposeGasProvidedError = class _ZeroLzComposeGasProvidedError extends Error {
  constructor() {
    super("");
    this.code = 6003;
    this.name = "ZeroLzComposeGasProvided";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _ZeroLzComposeGasProvidedError);
    }
  }
};
createErrorFromCodeLookup4.set(6003, () => new ZeroLzComposeGasProvidedError());
createErrorFromNameLookup4.set(
  "ZeroLzComposeGasProvided",
  () => new ZeroLzComposeGasProvidedError()
);
var ZeroLzReceiveGasProvidedError = class _ZeroLzReceiveGasProvidedError extends Error {
  constructor() {
    super("");
    this.code = 6004;
    this.name = "ZeroLzReceiveGasProvided";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _ZeroLzReceiveGasProvidedError);
    }
  }
};
createErrorFromCodeLookup4.set(6004, () => new ZeroLzReceiveGasProvidedError());
createErrorFromNameLookup4.set(
  "ZeroLzReceiveGasProvided",
  () => new ZeroLzReceiveGasProvidedError()
);
var NativeAmountExceedsCapError = class _NativeAmountExceedsCapError extends Error {
  constructor() {
    super("");
    this.code = 6005;
    this.name = "NativeAmountExceedsCap";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _NativeAmountExceedsCapError);
    }
  }
};
createErrorFromCodeLookup4.set(6005, () => new NativeAmountExceedsCapError());
createErrorFromNameLookup4.set(
  "NativeAmountExceedsCap",
  () => new NativeAmountExceedsCapError()
);
var NotAdminError2 = class _NotAdminError extends Error {
  constructor() {
    super("");
    this.code = 6006;
    this.name = "NotAdmin";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _NotAdminError);
    }
  }
};
createErrorFromCodeLookup4.set(6006, () => new NotAdminError2());
createErrorFromNameLookup4.set("NotAdmin", () => new NotAdminError2());
var NotExecutorError = class _NotExecutorError extends Error {
  constructor() {
    super("");
    this.code = 6007;
    this.name = "NotExecutor";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _NotExecutorError);
    }
  }
};
createErrorFromCodeLookup4.set(6007, () => new NotExecutorError());
createErrorFromNameLookup4.set("NotExecutor", () => new NotExecutorError());
var MsgLibNotAllowedError2 = class _MsgLibNotAllowedError extends Error {
  constructor() {
    super("");
    this.code = 6008;
    this.name = "MsgLibNotAllowed";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _MsgLibNotAllowedError);
    }
  }
};
createErrorFromCodeLookup4.set(6008, () => new MsgLibNotAllowedError2());
createErrorFromNameLookup4.set(
  "MsgLibNotAllowed",
  () => new MsgLibNotAllowedError2()
);
var TooManyAdminsError2 = class _TooManyAdminsError extends Error {
  constructor() {
    super("");
    this.code = 6009;
    this.name = "TooManyAdmins";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _TooManyAdminsError);
    }
  }
};
createErrorFromCodeLookup4.set(6009, () => new TooManyAdminsError2());
createErrorFromNameLookup4.set("TooManyAdmins", () => new TooManyAdminsError2());
var TooManyExecutorsError = class _TooManyExecutorsError extends Error {
  constructor() {
    super("");
    this.code = 6010;
    this.name = "TooManyExecutors";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _TooManyExecutorsError);
    }
  }
};
createErrorFromCodeLookup4.set(6010, () => new TooManyExecutorsError());
createErrorFromNameLookup4.set(
  "TooManyExecutors",
  () => new TooManyExecutorsError()
);
var TooManyOptionTypesError2 = class _TooManyOptionTypesError extends Error {
  constructor() {
    super("");
    this.code = 6011;
    this.name = "TooManyOptionTypes";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _TooManyOptionTypesError);
    }
  }
};
createErrorFromCodeLookup4.set(6011, () => new TooManyOptionTypesError2());
createErrorFromNameLookup4.set(
  "TooManyOptionTypes",
  () => new TooManyOptionTypesError2()
);
var InvalidNativeDropRequestsLengthError = class _InvalidNativeDropRequestsLengthError extends Error {
  constructor() {
    super("");
    this.code = 6012;
    this.name = "InvalidNativeDropRequestsLength";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidNativeDropRequestsLengthError);
    }
  }
};
createErrorFromCodeLookup4.set(
  6012,
  () => new InvalidNativeDropRequestsLengthError()
);
createErrorFromNameLookup4.set(
  "InvalidNativeDropRequestsLength",
  () => new InvalidNativeDropRequestsLengthError()
);
var InvalidNativeDropReceiverError = class _InvalidNativeDropReceiverError extends Error {
  constructor() {
    super("");
    this.code = 6013;
    this.name = "InvalidNativeDropReceiver";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidNativeDropReceiverError);
    }
  }
};
createErrorFromCodeLookup4.set(
  6013,
  () => new InvalidNativeDropReceiverError()
);
createErrorFromNameLookup4.set(
  "InvalidNativeDropReceiver",
  () => new InvalidNativeDropReceiverError()
);
var InsufficientBalanceError = class _InsufficientBalanceError extends Error {
  constructor() {
    super("");
    this.code = 6014;
    this.name = "InsufficientBalance";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InsufficientBalanceError);
    }
  }
};
createErrorFromCodeLookup4.set(6014, () => new InsufficientBalanceError());
createErrorFromNameLookup4.set(
  "InsufficientBalance",
  () => new InsufficientBalanceError()
);
var EidNotSupportedError2 = class _EidNotSupportedError extends Error {
  constructor() {
    super("");
    this.code = 6015;
    this.name = "EidNotSupported";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _EidNotSupportedError);
    }
  }
};
createErrorFromCodeLookup4.set(6015, () => new EidNotSupportedError2());
createErrorFromNameLookup4.set(
  "EidNotSupported",
  () => new EidNotSupportedError2()
);
var ExecutorIsAdminError = class _ExecutorIsAdminError extends Error {
  constructor() {
    super("");
    this.code = 6016;
    this.name = "ExecutorIsAdmin";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _ExecutorIsAdminError);
    }
  }
};
createErrorFromCodeLookup4.set(6016, () => new ExecutorIsAdminError());
createErrorFromNameLookup4.set(
  "ExecutorIsAdmin",
  () => new ExecutorIsAdminError()
);
var InvalidOwnerError = class _InvalidOwnerError extends Error {
  constructor() {
    super("");
    this.code = 6017;
    this.name = "InvalidOwner";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidOwnerError);
    }
  }
};
createErrorFromCodeLookup4.set(6017, () => new InvalidOwnerError());
createErrorFromNameLookup4.set("InvalidOwner", () => new InvalidOwnerError());
function errorFromCode4(code) {
  const createError = createErrorFromCodeLookup4.get(code);
  return createError != null ? createError() : null;
}
function errorFromName4(name) {
  const createError = createErrorFromNameLookup4.get(name);
  return createError != null ? createError() : null;
}

// src/generated/solita/executor/index.ts
var PROGRAM_ADDRESS4 = "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn";
var PROGRAM_ID4 = new PublicKey(PROGRAM_ADDRESS4);

// src/solita/executor.ts
var Executor = class {
  /**
   * Creates an instance of the Executor class.
   *
   * @param {PublicKey} program - The program public key.
   */
  constructor(program) {
    this.program = program;
    this.deriver = new ExecutorPDADeriver(this.program);
  }
  // owner methods
  /**
   * Initializes the executor.
   *
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} owner - The owner public key.
   * @param {PublicKey[]} admins - The admin public keys.
   * @param {PublicKey[]} executors - The executor public keys.
   * @param {PublicKey[]} msglibs - The message library public keys.
   * @param {PublicKey} priceFeed - The price feed public key.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  initExecutor(payer, owner, admins, executors, msglibs, priceFeed) {
    const [configAccount] = this.deriver.config();
    return createInitExecutorInstruction(
      {
        payer,
        config: configAccount
      },
      {
        params: {
          owner,
          admins,
          executors,
          msglibs,
          priceFeed
        }
      },
      this.program
    );
  }
  /**
   * Sets the owner.
   *
   * @param {PublicKey} owner - The current owner public key.
   * @param {PublicKey} newOwner - The new owner public key.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setOwner(owner, newOwner) {
    const [configAccount] = this.deriver.config();
    return createOwnerSetConfigInstruction(
      {
        owner,
        config: configAccount
      },
      {
        params: {
          fields: [newOwner],
          __kind: "Owner"
        }
      },
      this.program
    );
  }
  /**
   * Sets the admins.
   *
   * @param {PublicKey} owner - The owner public key.
   * @param {PublicKey[]} admins - The admin public keys.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setAdmins(owner, admins) {
    const [configAccount] = this.deriver.config();
    return createOwnerSetConfigInstruction(
      {
        owner,
        config: configAccount
      },
      {
        params: {
          fields: [admins],
          __kind: "Admins"
        }
      },
      this.program
    );
  }
  /**
   * Sets the allow list.
   *
   * @param {PublicKey} owner - The owner public key.
   * @param {PublicKey[]} allowlist - The allow list public keys.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setAllowList(owner, allowlist) {
    const [configAccount] = this.deriver.config();
    return createOwnerSetConfigInstruction(
      {
        owner,
        config: configAccount
      },
      {
        params: {
          fields: [allowlist],
          __kind: "Allowlist"
        }
      },
      this.program
    );
  }
  /**
   * Sets the deny list.
   *
   * @param {PublicKey} owner - The owner public key.
   * @param {PublicKey[]} denylist - The deny list public keys.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setDenyList(owner, denylist) {
    const [configAccount] = this.deriver.config();
    return createOwnerSetConfigInstruction(
      {
        owner,
        config: configAccount
      },
      {
        params: {
          fields: [denylist],
          __kind: "Denylist"
        }
      },
      this.program
    );
  }
  /**
   * Sets the paused state.
   *
   * @param {PublicKey} owner - The owner public key.
   * @param {boolean} paused - The paused state.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setPaused(owner, paused) {
    const [configAccount] = this.deriver.config();
    return createOwnerSetConfigInstruction(
      {
        owner,
        config: configAccount
      },
      {
        params: {
          fields: [paused],
          __kind: "Paused"
        }
      },
      this.program
    );
  }
  /**
   * Sets the executors.
   *
   * @param {PublicKey} owner - The owner public key.
   * @param {PublicKey[]} executors - The executor public keys.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setExecutors(owner, executors) {
    const [configAccount] = this.deriver.config();
    return createOwnerSetConfigInstruction(
      {
        owner,
        config: configAccount
      },
      {
        params: {
          fields: [executors],
          __kind: "Executors"
        }
      },
      this.program
    );
  }
  /**
   * Sets the message libraries by programs.
   *
   * @param {PublicKey} owner - The owner public key.
   * @param {PublicKey[]} msglibPrograms - The message library program public keys.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setMsglibByPrograms(owner, msglibPrograms) {
    this.deriver.config();
    const msglibPdas = msglibPrograms.map((program) => {
      return new MessageLibPDADeriver(program).messageLib()[0];
    });
    return this.setMsglibByPDAs(owner, msglibPdas);
  }
  /**
   * Sets the message libraries by PDAs.
   *
   * @param {PublicKey} owner - The owner public key.
   * @param {PublicKey[]} msglibPDAs - The message library PDA public keys.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setMsglibByPDAs(owner, msglibPDAs) {
    const [configAccount] = this.deriver.config();
    return createOwnerSetConfigInstruction(
      {
        owner,
        config: configAccount
      },
      {
        params: {
          fields: [msglibPDAs],
          __kind: "Msglibs"
        }
      },
      this.program
    );
  }
  // admin methods
  /**
   * Sets the default multiplier BPS.
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {number} defaultMultiplierBps - The default multiplier BPS.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setDefaultMultiplierBps(admin, defaultMultiplierBps) {
    const [configAccount] = this.deriver.config();
    return createAdminSetConfigInstruction(
      {
        admin,
        config: configAccount
      },
      {
        params: {
          fields: [defaultMultiplierBps],
          __kind: "DefaultMultiplierBps"
        }
      },
      this.program
    );
  }
  /**
   * Sets the destination configurations.
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {types.DstConfig[]} dstConfigs - The destination configurations.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setDstConfig(admin, dstConfigs) {
    const [configAccount] = this.deriver.config();
    return createAdminSetConfigInstruction(
      {
        admin,
        config: configAccount
      },
      {
        params: {
          fields: [dstConfigs],
          __kind: "DstConfigs"
        }
      },
      this.program
    );
  }
  /**
   * Sets the price feed.
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {PublicKey} priceFeedProgram - The price feed program public key.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setPriceFeed(admin, priceFeedProgram) {
    const [configAccount] = this.deriver.config();
    const [priceFeed] = new PriceFeedPDADeriver(priceFeedProgram).priceFeed();
    return createAdminSetConfigInstruction(
      {
        admin,
        config: configAccount
      },
      {
        params: {
          fields: [priceFeed],
          __kind: "PriceFeed"
        }
      },
      this.program
    );
  }
  /**
   * Gets the executor configuration.
   *
   * @param {Connection} connection - The connection.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment or configuration.
   *
   * @returns {Promise<accounts.ExecutorConfig | null>} A promise that resolves to the executor configuration or null.
   */
  async getExecutorConfig(connection, commitmentOrConfig) {
    const [configAccount] = this.deriver.config();
    try {
      const config2 = await ExecutorConfig3.fromAccountAddress(
        connection,
        configAccount,
        commitmentOrConfig
      );
      return config2;
    } catch (error) {
      return null;
    }
  }
  /**
   * Gets the quote instruction account meta for CPI.
   *
   * @param {PublicKey} priceFeedConfig - The price feed configuration public key.
   * @param {PublicKey} priceFeedProgram - The price feed program public key.
   * @param {boolean} payment - Whether payment is required.
   *
   * @returns {AccountMeta[]} The account meta array.
   */
  getQuoteIXAccountMetaForCPI(priceFeedConfig, priceFeedProgram, payment) {
    const [config2] = this.deriver.config();
    const ixAccounts = createQuoteExecutorInstructionAccounts(
      {
        executorConfig: config2,
        priceFeedConfig,
        priceFeedProgram
      },
      this.program
    );
    if (payment) {
      ixAccounts[0].isWritable = true;
    }
    return [
      {
        pubkey: this.program,
        isWritable: false,
        isSigner: false
      }
    ].concat(ixAccounts);
  }
  /**
   * Composes a transaction.
   *
   * @param {Connection} connection - The connection.
   * @param {PublicKey} executor - The executor public key.
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {ComposeSentEvent} event - The compose sent event.
   * @param {Uint8Array} extraData - The extra data.
   * @param {BN} [value] - The value.
   * @param {number} [computeUnits] - The compute units.
   * @param {Commitment} [commitmentOrConfig] - The commitment or configuration.
   *
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   */
  async compose(connection, executor, endpointProgram, event, extraData, value = new BN(0), computeUnits = 4e5, commitmentOrConfig) {
    const [config2] = this.deriver.config();
    const endpointEventDeriver = new EventPDADeriver(endpointProgram);
    const executorEventDeriver = new EventPDADeriver(this.program);
    const { to, from, guid, index, message } = event;
    const receiverInfo = await connection.getParsedAccountInfo(to, commitmentOrConfig);
    const receiverProgram = new PublicKey(receiverInfo.value.owner);
    const accounts = await getLzComposeAccountMeta(
      connection,
      executor,
      to,
      receiverProgram,
      {
        from,
        to,
        guid,
        index,
        message,
        extraData
      },
      commitmentOrConfig
    );
    return createComposeInstruction(
      {
        executor,
        config: config2,
        endpointProgram,
        endpointEventAuthority: endpointEventDeriver.eventAuthority()[0],
        program: this.program,
        eventAuthority: executorEventDeriver.eventAuthority()[0],
        anchorRemainingAccounts: [
          {
            pubkey: receiverProgram,
            isWritable: false,
            isSigner: false
          },
          ...accounts
        ]
      },
      {
        params: {
          lzCompose: {
            to,
            from,
            guid,
            index,
            message,
            extraData
          },
          computeUnits,
          value
        }
      },
      this.program
    );
  }
};

// src/generated/solita/uln/accounts/index.ts
var accounts_exports5 = {};
__export(accounts_exports5, {
  Confirmations: () => Confirmations,
  ReceiveConfig: () => ReceiveConfig2,
  SendConfig: () => SendConfig,
  UlnSettings: () => UlnSettings,
  accountProviders: () => accountProviders5,
  confirmationsBeet: () => confirmationsBeet,
  confirmationsDiscriminator: () => confirmationsDiscriminator,
  receiveConfigBeet: () => receiveConfigBeet2,
  receiveConfigDiscriminator: () => receiveConfigDiscriminator2,
  sendConfigBeet: () => sendConfigBeet,
  sendConfigDiscriminator: () => sendConfigDiscriminator,
  ulnSettingsBeet: () => ulnSettingsBeet,
  ulnSettingsDiscriminator: () => ulnSettingsDiscriminator
});
var confirmationsDiscriminator = [206, 57, 50, 8, 124, 133, 138, 112];
var Confirmations = class _Confirmations {
  constructor(value, bump) {
    this.value = value;
    this.bump = bump;
  }
  /**
   * Creates a {@link Confirmations} instance from the provided args.
   */
  static fromArgs(args) {
    return new _Confirmations(args.value, args.bump);
  }
  /**
   * Deserializes the {@link Confirmations} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _Confirmations.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Confirmations} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find Confirmations account at ${address}`);
    }
    return _Confirmations.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, confirmationsBeet);
  }
  /**
   * Deserializes the {@link Confirmations} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return confirmationsBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link Confirmations} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return confirmationsBeet.serialize({
      accountDiscriminator: confirmationsDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Confirmations} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _Confirmations.fromArgs(args);
    return confirmationsBeet.toFixedFromValue({
      accountDiscriminator: confirmationsDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Confirmations} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _Confirmations.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link Confirmations} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      value: this.value,
      bump: this.bump
    };
  }
};
var confirmationsBeet = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["value", beet159.coption(beet159.u64)],
    ["bump", beet159.u8]
  ],
  Confirmations.fromArgs,
  "Confirmations"
);
var receiveConfigDiscriminator2 = [162, 159, 153, 188, 56, 65, 245, 58];
var ReceiveConfig2 = class _ReceiveConfig {
  constructor(bump, uln) {
    this.bump = bump;
    this.uln = uln;
  }
  /**
   * Creates a {@link ReceiveConfig} instance from the provided args.
   */
  static fromArgs(args) {
    return new _ReceiveConfig(args.bump, args.uln);
  }
  /**
   * Deserializes the {@link ReceiveConfig} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _ReceiveConfig.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link ReceiveConfig} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find ReceiveConfig account at ${address}`);
    }
    return _ReceiveConfig.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, receiveConfigBeet2);
  }
  /**
   * Deserializes the {@link ReceiveConfig} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return receiveConfigBeet2.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link ReceiveConfig} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return receiveConfigBeet2.serialize({
      accountDiscriminator: receiveConfigDiscriminator2,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link ReceiveConfig} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _ReceiveConfig.fromArgs(args);
    return receiveConfigBeet2.toFixedFromValue({
      accountDiscriminator: receiveConfigDiscriminator2,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link ReceiveConfig} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _ReceiveConfig.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link ReceiveConfig} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      bump: this.bump,
      uln: this.uln
    };
  }
};
var receiveConfigBeet2 = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["bump", beet159.u8],
    ["uln", ulnConfigBeet2]
  ],
  ReceiveConfig2.fromArgs,
  "ReceiveConfig"
);
var sendConfigDiscriminator = [91, 221, 135, 137, 131, 199, 174, 25];
var SendConfig = class _SendConfig {
  constructor(bump, uln, executor) {
    this.bump = bump;
    this.uln = uln;
    this.executor = executor;
  }
  /**
   * Creates a {@link SendConfig} instance from the provided args.
   */
  static fromArgs(args) {
    return new _SendConfig(args.bump, args.uln, args.executor);
  }
  /**
   * Deserializes the {@link SendConfig} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _SendConfig.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link SendConfig} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find SendConfig account at ${address}`);
    }
    return _SendConfig.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, sendConfigBeet);
  }
  /**
   * Deserializes the {@link SendConfig} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return sendConfigBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link SendConfig} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return sendConfigBeet.serialize({
      accountDiscriminator: sendConfigDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link SendConfig} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _SendConfig.fromArgs(args);
    return sendConfigBeet.toFixedFromValue({
      accountDiscriminator: sendConfigDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link SendConfig} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _SendConfig.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link SendConfig} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      bump: this.bump,
      uln: this.uln,
      executor: this.executor
    };
  }
};
var sendConfigBeet = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["bump", beet159.u8],
    ["uln", ulnConfigBeet2],
    ["executor", executorConfigBeet]
  ],
  SendConfig.fromArgs,
  "SendConfig"
);
var ulnSettingsDiscriminator = [226, 199, 100, 253, 38, 115, 167, 154];
var UlnSettings = class _UlnSettings {
  constructor(eid, endpoint, endpointProgram, bump, admin, treasury) {
    this.eid = eid;
    this.endpoint = endpoint;
    this.endpointProgram = endpointProgram;
    this.bump = bump;
    this.admin = admin;
    this.treasury = treasury;
  }
  /**
   * Creates a {@link UlnSettings} instance from the provided args.
   */
  static fromArgs(args) {
    return new _UlnSettings(
      args.eid,
      args.endpoint,
      args.endpointProgram,
      args.bump,
      args.admin,
      args.treasury
    );
  }
  /**
   * Deserializes the {@link UlnSettings} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _UlnSettings.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link UlnSettings} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find UlnSettings account at ${address}`);
    }
    return _UlnSettings.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, ulnSettingsBeet);
  }
  /**
   * Deserializes the {@link UlnSettings} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return ulnSettingsBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link UlnSettings} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return ulnSettingsBeet.serialize({
      accountDiscriminator: ulnSettingsDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link UlnSettings} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _UlnSettings.fromArgs(args);
    return ulnSettingsBeet.toFixedFromValue({
      accountDiscriminator: ulnSettingsDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link UlnSettings} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _UlnSettings.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link UlnSettings} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      eid: this.eid,
      endpoint: this.endpoint.toBase58(),
      endpointProgram: this.endpointProgram.toBase58(),
      bump: this.bump,
      admin: this.admin.toBase58(),
      treasury: this.treasury
    };
  }
};
var ulnSettingsBeet = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["eid", beet159.u32],
    ["endpoint", beetSolana96.publicKey],
    ["endpointProgram", beetSolana96.publicKey],
    ["bump", beet159.u8],
    ["admin", beetSolana96.publicKey],
    ["treasury", beet159.coption(treasuryBeet)]
  ],
  UlnSettings.fromArgs,
  "UlnSettings"
);

// src/generated/solita/uln/accounts/index.ts
var accountProviders5 = {
  Confirmations,
  ReceiveConfig: ReceiveConfig2,
  SendConfig,
  UlnSettings
};

// src/generated/solita/uln/errors/index.ts
var errors_exports4 = {};
__export(errors_exports4, {
  AtLeastOneDVNError: () => AtLeastOneDVNError,
  ExceededMaxMessageSizeError: () => ExceededMaxMessageSizeError,
  ExceededU128Error: () => ExceededU128Error,
  InsufficientFeeError: () => InsufficientFeeError2,
  InvalidAccountLengthError: () => InvalidAccountLengthError,
  InvalidAmountError: () => InvalidAmountError4,
  InvalidBpsError: () => InvalidBpsError,
  InvalidConfigTypeError: () => InvalidConfigTypeError2,
  InvalidConfirmationError: () => InvalidConfirmationError,
  InvalidConfirmationsError: () => InvalidConfirmationsError,
  InvalidDvnError: () => InvalidDvnError,
  InvalidDvnProgramError: () => InvalidDvnProgramError,
  InvalidEidError: () => InvalidEidError,
  InvalidExecutorError: () => InvalidExecutorError,
  InvalidExecutorProgramError: () => InvalidExecutorProgramError,
  InvalidLzTokenMintError: () => InvalidLzTokenMintError2,
  InvalidOptionTypeError: () => InvalidOptionTypeError,
  InvalidOptionalDVNCountError: () => InvalidOptionalDVNCountError,
  InvalidOptionalDVNThresholdError: () => InvalidOptionalDVNThresholdError,
  InvalidPacketVersionError: () => InvalidPacketVersionError,
  InvalidPayerError: () => InvalidPayerError,
  InvalidRequiredDVNCountError: () => InvalidRequiredDVNCountError,
  InvalidTreasuryError: () => InvalidTreasuryError,
  InvalidTreasuryFeeCapError: () => InvalidTreasuryFeeCapError,
  InvalidType1SizeError: () => InvalidType1SizeError,
  InvalidType2SizeError: () => InvalidType2SizeError,
  InvalidWorkerIdError: () => InvalidWorkerIdError,
  LzTokenUnavailableError: () => LzTokenUnavailableError3,
  NonSignerError: () => NonSignerError,
  UnauthorizedError: () => UnauthorizedError2,
  UnsortedError: () => UnsortedError,
  VerifyingError: () => VerifyingError,
  ZeroMessageSizeError: () => ZeroMessageSizeError,
  errorFromCode: () => errorFromCode5,
  errorFromName: () => errorFromName5
});
var createErrorFromCodeLookup5 = /* @__PURE__ */ new Map();
var createErrorFromNameLookup5 = /* @__PURE__ */ new Map();
var UnauthorizedError2 = class _UnauthorizedError extends Error {
  constructor() {
    super("");
    this.code = 6e3;
    this.name = "Unauthorized";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _UnauthorizedError);
    }
  }
};
createErrorFromCodeLookup5.set(6e3, () => new UnauthorizedError2());
createErrorFromNameLookup5.set("Unauthorized", () => new UnauthorizedError2());
var InvalidAmountError4 = class _InvalidAmountError extends Error {
  constructor() {
    super("");
    this.code = 6001;
    this.name = "InvalidAmount";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidAmountError);
    }
  }
};
createErrorFromCodeLookup5.set(6001, () => new InvalidAmountError4());
createErrorFromNameLookup5.set("InvalidAmount", () => new InvalidAmountError4());
var InvalidExecutorError = class _InvalidExecutorError extends Error {
  constructor() {
    super("");
    this.code = 6002;
    this.name = "InvalidExecutor";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidExecutorError);
    }
  }
};
createErrorFromCodeLookup5.set(6002, () => new InvalidExecutorError());
createErrorFromNameLookup5.set(
  "InvalidExecutor",
  () => new InvalidExecutorError()
);
var ZeroMessageSizeError = class _ZeroMessageSizeError extends Error {
  constructor() {
    super("");
    this.code = 6003;
    this.name = "ZeroMessageSize";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _ZeroMessageSizeError);
    }
  }
};
createErrorFromCodeLookup5.set(6003, () => new ZeroMessageSizeError());
createErrorFromNameLookup5.set(
  "ZeroMessageSize",
  () => new ZeroMessageSizeError()
);
var InvalidRequiredDVNCountError = class _InvalidRequiredDVNCountError extends Error {
  constructor() {
    super("");
    this.code = 6004;
    this.name = "InvalidRequiredDVNCount";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidRequiredDVNCountError);
    }
  }
};
createErrorFromCodeLookup5.set(6004, () => new InvalidRequiredDVNCountError());
createErrorFromNameLookup5.set(
  "InvalidRequiredDVNCount",
  () => new InvalidRequiredDVNCountError()
);
var InvalidOptionalDVNCountError = class _InvalidOptionalDVNCountError extends Error {
  constructor() {
    super("");
    this.code = 6005;
    this.name = "InvalidOptionalDVNCount";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidOptionalDVNCountError);
    }
  }
};
createErrorFromCodeLookup5.set(6005, () => new InvalidOptionalDVNCountError());
createErrorFromNameLookup5.set(
  "InvalidOptionalDVNCount",
  () => new InvalidOptionalDVNCountError()
);
var InvalidOptionalDVNThresholdError = class _InvalidOptionalDVNThresholdError extends Error {
  constructor() {
    super("");
    this.code = 6006;
    this.name = "InvalidOptionalDVNThreshold";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidOptionalDVNThresholdError);
    }
  }
};
createErrorFromCodeLookup5.set(
  6006,
  () => new InvalidOptionalDVNThresholdError()
);
createErrorFromNameLookup5.set(
  "InvalidOptionalDVNThreshold",
  () => new InvalidOptionalDVNThresholdError()
);
var InvalidConfirmationsError = class _InvalidConfirmationsError extends Error {
  constructor() {
    super("");
    this.code = 6007;
    this.name = "InvalidConfirmations";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidConfirmationsError);
    }
  }
};
createErrorFromCodeLookup5.set(6007, () => new InvalidConfirmationsError());
createErrorFromNameLookup5.set(
  "InvalidConfirmations",
  () => new InvalidConfirmationsError()
);
var AtLeastOneDVNError = class _AtLeastOneDVNError extends Error {
  constructor() {
    super("");
    this.code = 6008;
    this.name = "AtLeastOneDVN";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _AtLeastOneDVNError);
    }
  }
};
createErrorFromCodeLookup5.set(6008, () => new AtLeastOneDVNError());
createErrorFromNameLookup5.set("AtLeastOneDVN", () => new AtLeastOneDVNError());
var UnsortedError = class _UnsortedError extends Error {
  constructor() {
    super("");
    this.code = 6009;
    this.name = "Unsorted";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _UnsortedError);
    }
  }
};
createErrorFromCodeLookup5.set(6009, () => new UnsortedError());
createErrorFromNameLookup5.set("Unsorted", () => new UnsortedError());
var InvalidTreasuryFeeCapError = class _InvalidTreasuryFeeCapError extends Error {
  constructor() {
    super("");
    this.code = 6010;
    this.name = "InvalidTreasuryFeeCap";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidTreasuryFeeCapError);
    }
  }
};
createErrorFromCodeLookup5.set(6010, () => new InvalidTreasuryFeeCapError());
createErrorFromNameLookup5.set(
  "InvalidTreasuryFeeCap",
  () => new InvalidTreasuryFeeCapError()
);
var InvalidPacketVersionError = class _InvalidPacketVersionError extends Error {
  constructor() {
    super("");
    this.code = 6011;
    this.name = "InvalidPacketVersion";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidPacketVersionError);
    }
  }
};
createErrorFromCodeLookup5.set(6011, () => new InvalidPacketVersionError());
createErrorFromNameLookup5.set(
  "InvalidPacketVersion",
  () => new InvalidPacketVersionError()
);
var InvalidEidError = class _InvalidEidError extends Error {
  constructor() {
    super("");
    this.code = 6012;
    this.name = "InvalidEid";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidEidError);
    }
  }
};
createErrorFromCodeLookup5.set(6012, () => new InvalidEidError());
createErrorFromNameLookup5.set("InvalidEid", () => new InvalidEidError());
var VerifyingError = class _VerifyingError extends Error {
  constructor() {
    super("");
    this.code = 6013;
    this.name = "Verifying";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _VerifyingError);
    }
  }
};
createErrorFromCodeLookup5.set(6013, () => new VerifyingError());
createErrorFromNameLookup5.set("Verifying", () => new VerifyingError());
var InvalidWorkerIdError = class _InvalidWorkerIdError extends Error {
  constructor() {
    super("");
    this.code = 6014;
    this.name = "InvalidWorkerId";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidWorkerIdError);
    }
  }
};
createErrorFromCodeLookup5.set(6014, () => new InvalidWorkerIdError());
createErrorFromNameLookup5.set(
  "InvalidWorkerId",
  () => new InvalidWorkerIdError()
);
var InvalidOptionTypeError = class _InvalidOptionTypeError extends Error {
  constructor() {
    super("");
    this.code = 6015;
    this.name = "InvalidOptionType";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidOptionTypeError);
    }
  }
};
createErrorFromCodeLookup5.set(6015, () => new InvalidOptionTypeError());
createErrorFromNameLookup5.set(
  "InvalidOptionType",
  () => new InvalidOptionTypeError()
);
var InvalidBpsError = class _InvalidBpsError extends Error {
  constructor() {
    super("");
    this.code = 6016;
    this.name = "InvalidBps";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidBpsError);
    }
  }
};
createErrorFromCodeLookup5.set(6016, () => new InvalidBpsError());
createErrorFromNameLookup5.set("InvalidBps", () => new InvalidBpsError());
var ExceededMaxMessageSizeError = class _ExceededMaxMessageSizeError extends Error {
  constructor() {
    super("");
    this.code = 6017;
    this.name = "ExceededMaxMessageSize";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _ExceededMaxMessageSizeError);
    }
  }
};
createErrorFromCodeLookup5.set(6017, () => new ExceededMaxMessageSizeError());
createErrorFromNameLookup5.set(
  "ExceededMaxMessageSize",
  () => new ExceededMaxMessageSizeError()
);
var InvalidExecutorProgramError = class _InvalidExecutorProgramError extends Error {
  constructor() {
    super("");
    this.code = 6018;
    this.name = "InvalidExecutorProgram";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidExecutorProgramError);
    }
  }
};
createErrorFromCodeLookup5.set(6018, () => new InvalidExecutorProgramError());
createErrorFromNameLookup5.set(
  "InvalidExecutorProgram",
  () => new InvalidExecutorProgramError()
);
var InvalidAccountLengthError = class _InvalidAccountLengthError extends Error {
  constructor() {
    super("");
    this.code = 6019;
    this.name = "InvalidAccountLength";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidAccountLengthError);
    }
  }
};
createErrorFromCodeLookup5.set(6019, () => new InvalidAccountLengthError());
createErrorFromNameLookup5.set(
  "InvalidAccountLength",
  () => new InvalidAccountLengthError()
);
var InvalidDvnProgramError = class _InvalidDvnProgramError extends Error {
  constructor() {
    super("");
    this.code = 6020;
    this.name = "InvalidDvnProgram";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidDvnProgramError);
    }
  }
};
createErrorFromCodeLookup5.set(6020, () => new InvalidDvnProgramError());
createErrorFromNameLookup5.set(
  "InvalidDvnProgram",
  () => new InvalidDvnProgramError()
);
var InvalidDvnError = class _InvalidDvnError extends Error {
  constructor() {
    super("");
    this.code = 6021;
    this.name = "InvalidDvn";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidDvnError);
    }
  }
};
createErrorFromCodeLookup5.set(6021, () => new InvalidDvnError());
createErrorFromNameLookup5.set("InvalidDvn", () => new InvalidDvnError());
var LzTokenUnavailableError3 = class _LzTokenUnavailableError extends Error {
  constructor() {
    super("");
    this.code = 6022;
    this.name = "LzTokenUnavailable";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _LzTokenUnavailableError);
    }
  }
};
createErrorFromCodeLookup5.set(6022, () => new LzTokenUnavailableError3());
createErrorFromNameLookup5.set(
  "LzTokenUnavailable",
  () => new LzTokenUnavailableError3()
);
var InsufficientFeeError2 = class _InsufficientFeeError extends Error {
  constructor() {
    super("");
    this.code = 6023;
    this.name = "InsufficientFee";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InsufficientFeeError);
    }
  }
};
createErrorFromCodeLookup5.set(6023, () => new InsufficientFeeError2());
createErrorFromNameLookup5.set(
  "InsufficientFee",
  () => new InsufficientFeeError2()
);
var InvalidTreasuryError = class _InvalidTreasuryError extends Error {
  constructor() {
    super("");
    this.code = 6024;
    this.name = "InvalidTreasury";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidTreasuryError);
    }
  }
};
createErrorFromCodeLookup5.set(6024, () => new InvalidTreasuryError());
createErrorFromNameLookup5.set(
  "InvalidTreasury",
  () => new InvalidTreasuryError()
);
var InvalidLzTokenMintError2 = class _InvalidLzTokenMintError extends Error {
  constructor() {
    super("");
    this.code = 6025;
    this.name = "InvalidLzTokenMint";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidLzTokenMintError);
    }
  }
};
createErrorFromCodeLookup5.set(6025, () => new InvalidLzTokenMintError2());
createErrorFromNameLookup5.set(
  "InvalidLzTokenMint",
  () => new InvalidLzTokenMintError2()
);
var InvalidConfigTypeError2 = class _InvalidConfigTypeError extends Error {
  constructor() {
    super("");
    this.code = 6026;
    this.name = "InvalidConfigType";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidConfigTypeError);
    }
  }
};
createErrorFromCodeLookup5.set(6026, () => new InvalidConfigTypeError2());
createErrorFromNameLookup5.set(
  "InvalidConfigType",
  () => new InvalidConfigTypeError2()
);
var InvalidConfirmationError = class _InvalidConfirmationError extends Error {
  constructor() {
    super("");
    this.code = 6027;
    this.name = "InvalidConfirmation";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidConfirmationError);
    }
  }
};
createErrorFromCodeLookup5.set(6027, () => new InvalidConfirmationError());
createErrorFromNameLookup5.set(
  "InvalidConfirmation",
  () => new InvalidConfirmationError()
);
var InvalidType1SizeError = class _InvalidType1SizeError extends Error {
  constructor() {
    super("");
    this.code = 6028;
    this.name = "InvalidType1Size";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidType1SizeError);
    }
  }
};
createErrorFromCodeLookup5.set(6028, () => new InvalidType1SizeError());
createErrorFromNameLookup5.set(
  "InvalidType1Size",
  () => new InvalidType1SizeError()
);
var InvalidType2SizeError = class _InvalidType2SizeError extends Error {
  constructor() {
    super("");
    this.code = 6029;
    this.name = "InvalidType2Size";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidType2SizeError);
    }
  }
};
createErrorFromCodeLookup5.set(6029, () => new InvalidType2SizeError());
createErrorFromNameLookup5.set(
  "InvalidType2Size",
  () => new InvalidType2SizeError()
);
var ExceededU128Error = class _ExceededU128Error extends Error {
  constructor() {
    super("");
    this.code = 6030;
    this.name = "ExceededU128";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _ExceededU128Error);
    }
  }
};
createErrorFromCodeLookup5.set(6030, () => new ExceededU128Error());
createErrorFromNameLookup5.set("ExceededU128", () => new ExceededU128Error());
var NonSignerError = class _NonSignerError extends Error {
  constructor() {
    super("");
    this.code = 6031;
    this.name = "NonSigner";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _NonSignerError);
    }
  }
};
createErrorFromCodeLookup5.set(6031, () => new NonSignerError());
createErrorFromNameLookup5.set("NonSigner", () => new NonSignerError());
var InvalidPayerError = class _InvalidPayerError extends Error {
  constructor() {
    super("");
    this.code = 6032;
    this.name = "InvalidPayer";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidPayerError);
    }
  }
};
createErrorFromCodeLookup5.set(6032, () => new InvalidPayerError());
createErrorFromNameLookup5.set("InvalidPayer", () => new InvalidPayerError());
function errorFromCode5(code) {
  const createError = createErrorFromCodeLookup5.get(code);
  return createError != null ? createError() : null;
}
function errorFromName5(name) {
  const createError = createErrorFromNameLookup5.get(name);
  return createError != null ? createError() : null;
}

// src/generated/solita/uln/instructions/index.ts
var instructions_exports5 = {};
__export(instructions_exports5, {
  closeVerifyInstructionDiscriminator: () => closeVerifyInstructionDiscriminator,
  closeVerifyStruct: () => closeVerifyStruct,
  commitVerificationInstructionDiscriminator: () => commitVerificationInstructionDiscriminator,
  commitVerificationStruct: () => commitVerificationStruct,
  createCloseVerifyInstruction: () => createCloseVerifyInstruction,
  createCloseVerifyInstructionAccounts: () => createCloseVerifyInstructionAccounts,
  createCommitVerificationInstruction: () => createCommitVerificationInstruction,
  createCommitVerificationInstructionAccounts: () => createCommitVerificationInstructionAccounts,
  createInitConfigInstruction: () => createInitConfigInstruction3,
  createInitConfigInstructionAccounts: () => createInitConfigInstructionAccounts3,
  createInitDefaultConfigInstruction: () => createInitDefaultConfigInstruction2,
  createInitDefaultConfigInstructionAccounts: () => createInitDefaultConfigInstructionAccounts2,
  createInitUlnInstruction: () => createInitUlnInstruction,
  createInitUlnInstructionAccounts: () => createInitUlnInstructionAccounts,
  createInitVerifyInstruction: () => createInitVerifyInstruction2,
  createInitVerifyInstructionAccounts: () => createInitVerifyInstructionAccounts2,
  createQuoteInstruction: () => createQuoteInstruction3,
  createQuoteInstructionAccounts: () => createQuoteInstructionAccounts3,
  createSendInstruction: () => createSendInstruction3,
  createSendInstructionAccounts: () => createSendInstructionAccounts3,
  createSendWithLzTokenInstruction: () => createSendWithLzTokenInstruction2,
  createSendWithLzTokenInstructionAccounts: () => createSendWithLzTokenInstructionAccounts2,
  createSetConfigInstruction: () => createSetConfigInstruction4,
  createSetConfigInstructionAccounts: () => createSetConfigInstructionAccounts4,
  createSetDefaultConfigInstruction: () => createSetDefaultConfigInstruction2,
  createSetDefaultConfigInstructionAccounts: () => createSetDefaultConfigInstructionAccounts2,
  createSetTreasuryInstruction: () => createSetTreasuryInstruction,
  createSetTreasuryInstructionAccounts: () => createSetTreasuryInstructionAccounts,
  createTransferAdminInstruction: () => createTransferAdminInstruction3,
  createTransferAdminInstructionAccounts: () => createTransferAdminInstructionAccounts3,
  createVerifyInstruction: () => createVerifyInstruction2,
  createVerifyInstructionAccounts: () => createVerifyInstructionAccounts2,
  createVersionInstruction: () => createVersionInstruction2,
  createVersionInstructionAccounts: () => createVersionInstructionAccounts2,
  createWithdrawRentInstruction: () => createWithdrawRentInstruction2,
  createWithdrawRentInstructionAccounts: () => createWithdrawRentInstructionAccounts2,
  initConfigInstructionDiscriminator: () => initConfigInstructionDiscriminator3,
  initConfigStruct: () => initConfigStruct3,
  initDefaultConfigInstructionDiscriminator: () => initDefaultConfigInstructionDiscriminator2,
  initDefaultConfigStruct: () => initDefaultConfigStruct2,
  initUlnInstructionDiscriminator: () => initUlnInstructionDiscriminator,
  initUlnStruct: () => initUlnStruct,
  initVerifyInstructionDiscriminator: () => initVerifyInstructionDiscriminator2,
  initVerifyStruct: () => initVerifyStruct2,
  quoteInstructionDiscriminator: () => quoteInstructionDiscriminator3,
  quoteStruct: () => quoteStruct3,
  sendInstructionDiscriminator: () => sendInstructionDiscriminator3,
  sendStruct: () => sendStruct3,
  sendWithLzTokenInstructionDiscriminator: () => sendWithLzTokenInstructionDiscriminator2,
  sendWithLzTokenStruct: () => sendWithLzTokenStruct2,
  setConfigInstructionDiscriminator: () => setConfigInstructionDiscriminator4,
  setConfigStruct: () => setConfigStruct4,
  setDefaultConfigInstructionDiscriminator: () => setDefaultConfigInstructionDiscriminator2,
  setDefaultConfigStruct: () => setDefaultConfigStruct2,
  setTreasuryInstructionDiscriminator: () => setTreasuryInstructionDiscriminator,
  setTreasuryStruct: () => setTreasuryStruct,
  transferAdminInstructionDiscriminator: () => transferAdminInstructionDiscriminator3,
  transferAdminStruct: () => transferAdminStruct3,
  verifyInstructionDiscriminator: () => verifyInstructionDiscriminator2,
  verifyStruct: () => verifyStruct2,
  versionInstructionDiscriminator: () => versionInstructionDiscriminator2,
  versionStruct: () => versionStruct2,
  withdrawRentInstructionDiscriminator: () => withdrawRentInstructionDiscriminator2,
  withdrawRentStruct: () => withdrawRentStruct2
});
var closeVerifyParamsBeet = new beet159.BeetArgsStruct(
  [
    ["packetHeaderHash", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)]
  ],
  "CloseVerifyParams"
);

// src/generated/solita/uln/instructions/closeVerify.ts
var closeVerifyStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", closeVerifyParamsBeet]
  ],
  "CloseVerifyInstructionArgs"
);
var closeVerifyInstructionDiscriminator = [
  44,
  133,
  228,
  138,
  169,
  242,
  53,
  36
];
function createCloseVerifyInstruction(accounts, args, programId) {
  const [data] = closeVerifyStruct.serialize({
    instructionDiscriminator: closeVerifyInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.dvn,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.receiver,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.confirmations,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createCloseVerifyInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.dvn,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.receiver,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.confirmations,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var commitVerificationParamsBeet = new beet159.BeetArgsStruct(
  [
    ["packetHeader", beet159.uniformFixedSizeArray(beet159.u8, 81)],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)]
  ],
  "CommitVerificationParams"
);

// src/generated/solita/uln/instructions/commitVerification.ts
var commitVerificationStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", commitVerificationParamsBeet]
  ],
  "CommitVerificationInstructionArgs"
);
var commitVerificationInstructionDiscriminator = [
  194,
  120,
  44,
  252,
  140,
  215,
  139,
  103
];
function createCommitVerificationInstruction(accounts, args, programId) {
  const [data] = commitVerificationStruct.serialize({
    instructionDiscriminator: commitVerificationInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.receiveConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createCommitVerificationInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.receiveConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initConfigParamsBeet3 = new beet159.BeetArgsStruct(
  [
    ["oapp", beetSolana96.publicKey],
    ["eid", beet159.u32]
  ],
  "InitConfigParams"
);

// src/generated/solita/uln/instructions/initConfig.ts
var initConfigStruct3 = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initConfigParamsBeet3]
  ],
  "InitConfigInstructionArgs"
);
var initConfigInstructionDiscriminator3 = [
  23,
  235,
  115,
  232,
  168,
  96,
  1,
  231
];
function createInitConfigInstruction3(accounts, args, programId) {
  const [data] = initConfigStruct3.serialize({
    instructionDiscriminator: initConfigInstructionDiscriminator3,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitConfigInstructionAccounts3(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initDefaultConfigParamsBeet2 = new beet159.FixableBeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["sendUlnConfig", ulnConfigBeet2],
    ["receiveUlnConfig", ulnConfigBeet2],
    ["executorConfig", executorConfigBeet]
  ],
  "InitDefaultConfigParams"
);

// src/generated/solita/uln/instructions/initDefaultConfig.ts
var initDefaultConfigStruct2 = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initDefaultConfigParamsBeet2]
  ],
  "InitDefaultConfigInstructionArgs"
);
var initDefaultConfigInstructionDiscriminator2 = [
  98,
  218,
  197,
  194,
  173,
  179,
  112,
  21
];
function createInitDefaultConfigInstruction2(accounts, args, programId) {
  const [data] = initDefaultConfigStruct2.serialize({
    instructionDiscriminator: initDefaultConfigInstructionDiscriminator2,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitDefaultConfigInstructionAccounts2(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initUlnParamsBeet = new beet159.BeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["endpoint", beetSolana96.publicKey],
    ["endpointProgram", beetSolana96.publicKey],
    ["admin", beetSolana96.publicKey]
  ],
  "InitUlnParams"
);

// src/generated/solita/uln/instructions/initUln.ts
var initUlnStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initUlnParamsBeet]
  ],
  "InitUlnInstructionArgs"
);
var initUlnInstructionDiscriminator = [
  19,
  215,
  207,
  92,
  197,
  112,
  119,
  240
];
function createInitUlnInstruction(accounts, args, programId) {
  const [data] = initUlnStruct.serialize({
    instructionDiscriminator: initUlnInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitUlnInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initVerifyParamsBeet2 = new beet159.BeetArgsStruct(
  [
    ["packetHeader", beet159.uniformFixedSizeArray(beet159.u8, 81)],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["dvn", beetSolana96.publicKey]
  ],
  "InitVerifyParams"
);

// src/generated/solita/uln/instructions/initVerify.ts
var initVerifyStruct2 = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initVerifyParamsBeet2]
  ],
  "InitVerifyInstructionArgs"
);
var initVerifyInstructionDiscriminator2 = [
  76,
  246,
  244,
  124,
  115,
  17,
  235,
  91
];
function createInitVerifyInstruction2(accounts, args, programId) {
  const [data] = initVerifyStruct2.serialize({
    instructionDiscriminator: initVerifyInstructionDiscriminator2,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.confirmations,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitVerifyInstructionAccounts2(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.confirmations,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var packetBeet2 = new beet159.FixableBeetArgsStruct(
  [
    ["nonce", beet159.u64],
    ["srcEid", beet159.u32],
    ["sender", beetSolana96.publicKey],
    ["dstEid", beet159.u32],
    ["receiver", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["guid", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["message", beet159.bytes]
  ],
  "Packet"
);

// src/generated/solita/uln/types/QuoteParams.ts
var quoteParamsBeet3 = new beet159.FixableBeetArgsStruct(
  [
    ["packet", packetBeet2],
    ["options", beet159.bytes],
    ["payInLzToken", beet159.bool]
  ],
  "QuoteParams"
);

// src/generated/solita/uln/instructions/quote.ts
var quoteStruct3 = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", quoteParamsBeet3]
  ],
  "QuoteInstructionArgs"
);
var quoteInstructionDiscriminator3 = [
  149,
  42,
  109,
  247,
  134,
  146,
  213,
  123
];
function createQuoteInstruction3(accounts, args, programId) {
  const [data] = quoteStruct3.serialize({
    instructionDiscriminator: quoteInstructionDiscriminator3,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendConfig,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createQuoteInstructionAccounts3(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendConfig,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var sendParamsBeet3 = new beet159.FixableBeetArgsStruct(
  [
    ["packet", packetBeet2],
    ["options", beet159.bytes],
    ["nativeFee", beet159.u64]
  ],
  "SendParams"
);

// src/generated/solita/uln/instructions/send.ts
var sendStruct3 = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", sendParamsBeet3]
  ],
  "SendInstructionArgs"
);
var sendInstructionDiscriminator3 = [102, 251, 20, 187, 65, 75, 12, 69];
function createSendInstruction3(accounts, args, programId) {
  const [data] = sendStruct3.serialize({
    instructionDiscriminator: sendInstructionDiscriminator3,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.treasury ?? programId,
      isWritable: accounts.treasury != null,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSendInstructionAccounts3(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.treasury ?? programId,
      isWritable: accounts.treasury != null,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var sendWithLzTokenParamsBeet2 = new beet159.FixableBeetArgsStruct(
  [
    ["packet", packetBeet2],
    ["options", beet159.bytes],
    ["nativeFee", beet159.u64],
    ["lzTokenFee", beet159.u64],
    ["lzTokenMint", beetSolana96.publicKey]
  ],
  "SendWithLzTokenParams"
);

// src/generated/solita/uln/instructions/sendWithLzToken.ts
var sendWithLzTokenStruct2 = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", sendWithLzTokenParamsBeet2]
  ],
  "SendWithLzTokenInstructionArgs"
);
var sendWithLzTokenInstructionDiscriminator2 = [
  165,
  161,
  84,
  48,
  129,
  26,
  193,
  19
];
function createSendWithLzTokenInstruction2(accounts, args, programId) {
  const [data] = sendWithLzTokenStruct2.serialize({
    instructionDiscriminator: sendWithLzTokenInstructionDiscriminator2,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.lzTokenSource,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.lzTokenTreasury,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.lzTokenMint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.tokenProgram ?? splToken.TOKEN_PROGRAM_ID,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSendWithLzTokenInstructionAccounts2(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.lzTokenSource,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.lzTokenTreasury,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.lzTokenMint,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.tokenProgram ?? splToken.TOKEN_PROGRAM_ID,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setConfigParamsBeet4 = new beet159.FixableBeetArgsStruct(
  [
    ["oapp", beetSolana96.publicKey],
    ["eid", beet159.u32],
    ["configType", beet159.u32],
    ["config", beet159.bytes]
  ],
  "SetConfigParams"
);

// src/generated/solita/uln/instructions/setConfig.ts
var setConfigStruct4 = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setConfigParamsBeet4]
  ],
  "SetConfigInstructionArgs"
);
var setConfigInstructionDiscriminator4 = [
  108,
  158,
  154,
  175,
  212,
  98,
  52,
  66
];
function createSetConfigInstruction4(accounts, args, programId) {
  const [data] = setConfigStruct4.serialize({
    instructionDiscriminator: setConfigInstructionDiscriminator4,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetConfigInstructionAccounts4(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.endpoint,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.defaultSendConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.defaultReceiveConfig,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setDefaultConfigParamsBeet2 = new beet159.FixableBeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["sendUlnConfig", beet159.coption(ulnConfigBeet2)],
    ["receiveUlnConfig", beet159.coption(ulnConfigBeet2)],
    ["executorConfig", beet159.coption(executorConfigBeet)]
  ],
  "SetDefaultConfigParams"
);

// src/generated/solita/uln/instructions/setDefaultConfig.ts
var setDefaultConfigStruct2 = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setDefaultConfigParamsBeet2]
  ],
  "SetDefaultConfigInstructionArgs"
);
var setDefaultConfigInstructionDiscriminator2 = [
  111,
  228,
  17,
  75,
  5,
  76,
  213,
  169
];
function createSetDefaultConfigInstruction2(accounts, args, programId) {
  const [data] = setDefaultConfigStruct2.serialize({
    instructionDiscriminator: setDefaultConfigInstructionDiscriminator2,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetDefaultConfigInstructionAccounts2(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.sendConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiveConfig,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setTreasuryParamsBeet = new beet159.FixableBeetArgsStruct(
  [["treasury", beet159.coption(treasuryBeet)]],
  "SetTreasuryParams"
);

// src/generated/solita/uln/instructions/setTreasury.ts
var setTreasuryStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setTreasuryParamsBeet]
  ],
  "SetTreasuryInstructionArgs"
);
var setTreasuryInstructionDiscriminator = [
  57,
  97,
  196,
  95,
  195,
  206,
  106,
  136
];
function createSetTreasuryInstruction(accounts, args, programId) {
  const [data] = setTreasuryStruct.serialize({
    instructionDiscriminator: setTreasuryInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetTreasuryInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.signer,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var transferAdminParamsBeet3 = new beet159.BeetArgsStruct(
  [["admin", beetSolana96.publicKey]],
  "TransferAdminParams"
);

// src/generated/solita/uln/instructions/transferAdmin.ts
var transferAdminStruct3 = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", transferAdminParamsBeet3]
  ],
  "TransferAdminInstructionArgs"
);
var transferAdminInstructionDiscriminator3 = [
  42,
  242,
  66,
  106,
  228,
  10,
  111,
  156
];
function createTransferAdminInstruction3(accounts, args, programId) {
  const [data] = transferAdminStruct3.serialize({
    instructionDiscriminator: transferAdminInstructionDiscriminator3,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createTransferAdminInstructionAccounts3(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var verifyParamsBeet2 = new beet159.BeetArgsStruct(
  [
    ["packetHeader", beet159.uniformFixedSizeArray(beet159.u8, 81)],
    ["payloadHash", beet159.uniformFixedSizeArray(beet159.u8, 32)],
    ["confirmations", beet159.u64]
  ],
  "VerifyParams"
);

// src/generated/solita/uln/instructions/verify.ts
var verifyStruct2 = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", verifyParamsBeet2]
  ],
  "VerifyInstructionArgs"
);
var verifyInstructionDiscriminator2 = [
  133,
  161,
  141,
  48,
  120,
  198,
  88,
  150
];
function createVerifyInstruction2(accounts, args, programId) {
  const [data] = verifyStruct2.serialize({
    instructionDiscriminator: verifyInstructionDiscriminator2,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.dvn,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.confirmations,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createVerifyInstructionAccounts2(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.dvn,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.confirmations,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var versionStruct2 = new beet159.BeetArgsStruct(
  [["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)]],
  "VersionInstructionArgs"
);
var versionInstructionDiscriminator2 = [
  118,
  65,
  195,
  198,
  129,
  216,
  252,
  192
];
function createVersionInstruction2(programId) {
  const [data] = versionStruct2.serialize({
    instructionDiscriminator: versionInstructionDiscriminator2
  });
  const keys = [];
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createVersionInstructionAccounts2(programId) {
  const keys = [];
  return keys;
}
var withdrawRentParamsBeet2 = new beet159.BeetArgsStruct(
  [["amount", beet159.u64]],
  "WithdrawRentParams"
);

// src/generated/solita/uln/instructions/withdrawRent.ts
var withdrawRentStruct2 = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", withdrawRentParamsBeet2]
  ],
  "WithdrawRentInstructionArgs"
);
var withdrawRentInstructionDiscriminator2 = [
  226,
  7,
  41,
  158,
  173,
  111,
  192,
  107
];
function createWithdrawRentInstruction2(accounts, args, programId) {
  const [data] = withdrawRentStruct2.serialize({
    instructionDiscriminator: withdrawRentInstructionDiscriminator2,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiver,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createWithdrawRentInstructionAccounts2(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.uln,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.receiver,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.eventAuthority,
      isWritable: false,
      isSigner: false
    },
    {
      pubkey: accounts.program,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}

// src/generated/solita/uln/types/index.ts
var types_exports5 = {};
__export(types_exports5, {
  closeVerifyParamsBeet: () => closeVerifyParamsBeet,
  commitVerificationParamsBeet: () => commitVerificationParamsBeet,
  configBeet: () => configBeet,
  executorConfigBeet: () => executorConfigBeet,
  initConfigParamsBeet: () => initConfigParamsBeet3,
  initDefaultConfigParamsBeet: () => initDefaultConfigParamsBeet2,
  initUlnParamsBeet: () => initUlnParamsBeet,
  initVerifyParamsBeet: () => initVerifyParamsBeet2,
  isConfigExecutor: () => isConfigExecutor,
  isConfigReceiveUln: () => isConfigReceiveUln,
  isConfigSendUln: () => isConfigSendUln,
  lzTokenTreasuryBeet: () => lzTokenTreasuryBeet,
  messagingFeeBeet: () => messagingFeeBeet3,
  packetBeet: () => packetBeet2,
  quoteParamsBeet: () => quoteParamsBeet3,
  sendParamsBeet: () => sendParamsBeet3,
  sendWithLzTokenParamsBeet: () => sendWithLzTokenParamsBeet2,
  setConfigParamsBeet: () => setConfigParamsBeet4,
  setDefaultConfigParamsBeet: () => setDefaultConfigParamsBeet2,
  setTreasuryParamsBeet: () => setTreasuryParamsBeet,
  transferAdminParamsBeet: () => transferAdminParamsBeet3,
  treasuryBeet: () => treasuryBeet,
  treasuryFeeBeet: () => treasuryFeeBeet,
  ulnConfigBeet: () => ulnConfigBeet2,
  verifyParamsBeet: () => verifyParamsBeet2,
  versionBeet: () => versionBeet2,
  withdrawRentParamsBeet: () => withdrawRentParamsBeet2,
  workerFeeBeet: () => workerFeeBeet
});
var messagingFeeBeet3 = new beet159.BeetArgsStruct(
  [
    ["nativeFee", beet159.u64],
    ["lzTokenFee", beet159.u64]
  ],
  "MessagingFee"
);
var versionBeet2 = new beet159.BeetArgsStruct(
  [
    ["major", beet159.u64],
    ["minor", beet159.u8],
    ["endpointVersion", beet159.u8]
  ],
  "Version"
);
var PROGRAM_ADDRESS5 = "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH";
var PROGRAM_ID5 = new PublicKey(PROGRAM_ADDRESS5);

// src/solita/uln.ts
var Uln = class {
  /**
   * Creates an instance of the Uln class.
   *
   * @param {PublicKey} program - The program public key.
   */
  constructor(program) {
    this.program = program;
    this.deriver = new UlnPDADeriver(program);
    const [eventAuthorityPDA] = new EventPDADeriver(program).eventAuthority();
    this.eventAuthorityPDA = eventAuthorityPDA;
  }
  /**
   * Initializes the ULN.
   *
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} admin - The admin public key.
   * @param {number} eid - The endpoint ID.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  initUln(endpointProgram, payer, admin, eid) {
    const [setting] = this.deriver.setting();
    const [msgLib] = this.deriver.messageLib();
    const [endpointAuth] = new EndpointPDADeriver(endpointProgram).messageLibraryInfo(msgLib);
    return createInitUlnInstruction(
      {
        payer,
        uln: setting
      },
      {
        params: {
          eid,
          admin,
          endpoint: endpointAuth,
          endpointProgram
        }
      },
      this.program
    );
  }
  /**
   * Initializes or updates the default configuration.
   * before calling this function, you should call initUln to initialize the uln
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} admin - The admin public key.
   * @param {number} eid - The endpoint ID.
   * @param {types.UlnConfig} sendUlnConfig - The send ULN configuration.
   * @param {types.UlnConfig} receiveUlnConfig - The receive ULN configuration.
   * @param {types.ExecutorConfig} executorConfig - The executor configuration.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   */
  async initOrUpdateDefaultConfig(connection, admin, eid, sendUlnConfig, receiveUlnConfig, executorConfig, commitmentOrConfig) {
    const [setting] = this.deriver.setting();
    const [sendConfigPDA] = this.deriver.defaultSendConfig(eid);
    const [receiveConfigPDA] = this.deriver.defaultReceiveConfig(eid);
    const configInfo = await connection.getAccountInfo(sendConfigPDA, commitmentOrConfig);
    if (configInfo) {
      return createSetDefaultConfigInstruction2(
        {
          admin,
          uln: setting,
          sendConfig: sendConfigPDA,
          receiveConfig: receiveConfigPDA,
          eventAuthority: this.eventAuthorityPDA,
          program: this.program
        },
        {
          params: {
            eid,
            sendUlnConfig,
            receiveUlnConfig,
            executorConfig
          }
        },
        this.program
      );
    } else {
      return createInitDefaultConfigInstruction2(
        {
          admin,
          uln: setting,
          sendConfig: sendConfigPDA,
          receiveConfig: receiveConfigPDA,
          eventAuthority: this.eventAuthorityPDA,
          program: this.program
        },
        {
          params: {
            eid,
            sendUlnConfig,
            receiveUlnConfig,
            executorConfig
          }
        },
        this.program
      );
    }
  }
  // msg_lib -> msg_lib_program ->
  /**
   * Sets the treasury.
   * before calling this function, you should call initUln to initialize the uln
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {object} treasury - The treasury object.
   * @param {PublicKey} treasury.admin - The treasury admin public key.
   * @param {PublicKey} treasury.nativeReceiver - The native receiver public key.
   * @param {number} treasury.nativeFeeBps - The native fee basis points.
   * @param {object} treasury.lzToken - The LZ token object.
   * @param {PublicKey} treasury.lzToken.receiver - The LZ token receiver public key.
   * @param {number} treasury.lzToken.fee - The LZ token fee.
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   */
  async setTreasury(admin, treasury) {
    const [setting] = this.deriver.setting();
    const ix = createSetTreasuryInstruction(
      {
        signer: admin,
        uln: setting,
        eventAuthority: this.eventAuthorityPDA,
        program: this.program
      },
      {
        params: {
          treasury
        }
      },
      this.program
    );
    return Promise.resolve(ix);
  }
  /**
   * Transfers the admin role to a new admin.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} admin - The current admin public key.
   * @param {PublicKey} newAdmin - The new admin public key.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   * @throws {Error} If the ULN is not initialized.
   */
  async transferAdmin(connection, admin, newAdmin, commitmentOrConfig) {
    const [setting] = this.deriver.setting();
    const info = await connection.getAccountInfo(setting, commitmentOrConfig);
    if (!info) {
      throw new Error("uln not initialized");
    }
    return createTransferAdminInstruction3(
      {
        admin,
        uln: setting,
        eventAuthority: this.eventAuthorityPDA,
        program: this.program
      },
      {
        params: {
          admin: newAdmin
        }
      },
      this.program
    );
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the quote instruction.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} _payer - The payer public key.
   * @param {PacketPath} path - The packet path.
   * @param {Commitment} [commitment] - The commitment level.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  async getQuoteIXAccountMetaForCPI(connection, _payer, path, commitment) {
    const { sender: sender_, dstEid } = path;
    const sender = new PublicKey(arrayify(sender_));
    const [defaultSendConfig] = this.deriver.defaultSendConfig(dstEid);
    const [sendConfig] = this.deriver.sendConfig(dstEid, sender);
    const [setting] = this.deriver.setting();
    const remainingAccounts = await this.getSendIxRemainingAccounts(connection, sender, dstEid, false, commitment);
    const accounts = createQuoteInstructionAccounts3(
      {
        endpoint: PublicKey.default,
        // useless
        uln: setting,
        sendConfig,
        defaultSendConfig,
        anchorRemainingAccounts: remainingAccounts
      },
      this.program
    );
    return accounts.slice(1);
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the send instruction.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PacketPath} path - The packet path.
   * @param {Commitment} [commitment='confirmed'] - The commitment level.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  async getSendIXAccountMetaForCPI(connection, payer, path, commitment = "confirmed") {
    const { sender: sender_, dstEid } = path;
    const sender = new PublicKey(arrayify(sender_));
    const [sendConfig] = this.deriver.sendConfig(dstEid, sender);
    const [defaultSendConfig] = this.deriver.defaultSendConfig(dstEid);
    const [setting] = this.deriver.setting();
    const ulnState = await this.getSetting(connection, commitment);
    const treasury = ulnState?.treasury?.nativeReceiver;
    const remainingAccounts = await this.getSendIxRemainingAccounts(connection, sender, dstEid, true, commitment);
    const accounts = createSendInstructionAccounts3(
      {
        endpoint: PublicKey.default,
        // useless
        uln: setting,
        sendConfig,
        defaultSendConfig,
        payer,
        treasury,
        eventAuthority: this.eventAuthorityPDA,
        program: this.program,
        anchorRemainingAccounts: remainingAccounts
      },
      this.program
    );
    accounts.forEach((key) => {
      if (!payer.equals(key.pubkey)) {
        key.isSigner = false;
      }
    });
    if (treasury) {
      const treasuryIndex = accounts.findIndex((k) => k.pubkey.toBase58() == treasury.toBase58());
      accounts[treasuryIndex].isWritable = true;
    }
    return accounts.slice(1);
  }
  // 3 RPC requests
  /**
   * Gets the remaining accounts for the send instruction.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} sender - The sender public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {boolean} payment - Indicates if payment is required.
   * @param {Commitment | GetAccountInfoConfig} [commitment='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  async getSendIxRemainingAccounts(connection, sender, dstEid, payment, commitment = "confirmed") {
    const { executor, dvns } = await this.getWorkers(connection, sender, dstEid, commitment);
    const priceFeeds = new Array();
    [executor.config, ...dvns.map((dvn) => dvn.config)].forEach((config2) => {
      if (!priceFeeds.find((p) => p.equals(config2.priceFeed))) {
        priceFeeds.push(config2.priceFeed);
      }
    });
    const priceFeedInfos = await connection.getMultipleAccountsInfo(priceFeeds, commitment);
    const priceFeedDict = priceFeedInfos.reduce((acc, info, i) => {
      invariant6(info, `priceFeed:${priceFeeds[i].toBase58()} not initialized`);
      acc.set(priceFeeds[i].toBase58(), info.owner);
      return acc;
    }, /* @__PURE__ */ new Map());
    const executorAccounts = new Executor(executor.owner).getQuoteIXAccountMetaForCPI(
      executor.config.priceFeed,
      // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
      priceFeedDict.get(executor.config.priceFeed.toBase58()),
      payment
    );
    const dvnAccounts = dvns.map((p, i) => {
      return new DVN(p.owner).getQuoteIXAccountMetaForCPI(
        p.config.priceFeed,
        // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
        priceFeedDict.get(p.config.priceFeed.toBase58()),
        payment
      );
    });
    return executorAccounts.concat(dvnAccounts.flat());
  }
  /**
   * Gets all workers (executor and DVN).
   * 2 RPC requests
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} sender - The sender public key.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitment='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<{ executor: { config: ExecutorAccounts.ExecutorConfig; owner: PublicKey }; dvns: { config: DvnAccounts.DvnConfig; owner: PublicKey }[] }>} A promise that resolves to an object containing the executor and DVN configurations.
   */
  async getWorkers(connection, sender, eid, commitment = "confirmed") {
    const [defaultSendConfig] = this.deriver.defaultSendConfig(eid);
    const [sendConfig] = this.deriver.sendConfig(eid, sender);
    const [defaultSendConfigBuf, sendConfigBuf] = await connection.getMultipleAccountsInfo(
      [defaultSendConfig, sendConfig],
      commitment
    );
    invariant6(defaultSendConfigBuf, "defaultSendConfig not initialized");
    const [defaultSendConfigState] = SendConfig.fromAccountInfo(defaultSendConfigBuf);
    const sendConfigState = sendConfigBuf ? SendConfig.fromAccountInfo(sendConfigBuf)[0] : null;
    const finalConfigState = this.getUlnConfigState(defaultSendConfigState, sendConfigState);
    const finalExecutorConfigState = this.getExecutorConfigState(defaultSendConfigState, sendConfigState);
    const { executor } = finalExecutorConfigState;
    const { requiredDvns, optionalDvns } = finalConfigState;
    const dvns = requiredDvns.concat(optionalDvns);
    const [executorBuf, ...dvnBuf] = await connection.getMultipleAccountsInfo([executor, ...dvns], commitment);
    invariant6(executorBuf, `executor:${executor.toBase58()} not initialized`);
    return {
      executor: {
        config: accounts_exports4.ExecutorConfig.fromAccountInfo(executorBuf)[0],
        owner: executorBuf.owner
      },
      dvns: dvnBuf.map((dvn, i) => {
        invariant6(dvn, `dvn:${dvns[i].toBase58()} not initialized`);
        return {
          config: accounts_exports3.DvnConfig.fromAccountInfo(dvn)[0],
          owner: dvn.owner
        };
      })
    };
  }
  /**
   * Initializes the verification process.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} dvn - The DVN public key.
   * @param {Uint8Array} packetBytes - The packet bytes.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<TransactionInstruction | null>} A promise that resolves to the transaction instruction or null if not needed.
   */
  async initVerify(connection, payer, dvn, packetBytes, commitmentOrConfig) {
    const packet = PacketV1Codec.fromBytes(packetBytes);
    const headerHash = packet.headerHash();
    const headerHashBytes = Uint8Array.from(Buffer.from(headerHash.slice(2), "hex"));
    const payloadHash = packet.payloadHash();
    const payloadHashBytes = Uint8Array.from(Buffer.from(payloadHash.slice(2), "hex"));
    const [confirmationsPDA] = this.deriver.confirmations(headerHashBytes, payloadHashBytes, dvn);
    const confirmationsInfo = await connection.getAccountInfo(confirmationsPDA, commitmentOrConfig);
    if (confirmationsInfo) {
      return null;
    }
    return createInitVerifyInstruction2(
      {
        payer,
        confirmations: confirmationsPDA
      },
      {
        params: {
          packetHeader: Array.from(Uint8Array.from(Buffer.from(packet.header().slice(2), "hex"))),
          payloadHash: Array.from(payloadHashBytes),
          dvn
        }
      },
      this.program
    );
  }
  /**
   * Verifies the packet.
   *
   * @param {PublicKey} dvn - The DVN public key.
   * @param {Uint8Array} packetBytes - The packet bytes.
   * @param {number | string} confirmations - The number of confirmations.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  verify(dvn, packetBytes, confirmations) {
    const packet = PacketV1Codec.fromBytes(packetBytes);
    const headerHash = packet.headerHash();
    const headerHashBytes = Uint8Array.from(Buffer.from(headerHash.slice(2), "hex"));
    const payloadHash = packet.payloadHash();
    const payloadHashBytes = Uint8Array.from(Buffer.from(payloadHash.slice(2), "hex"));
    const [confirmationsPDA] = this.deriver.confirmations(headerHashBytes, payloadHashBytes, dvn);
    return createVerifyInstruction2(
      {
        dvn,
        confirmations: confirmationsPDA,
        eventAuthority: this.eventAuthorityPDA,
        program: this.program
      },
      {
        params: {
          packetHeader: Array.from(Uint8Array.from(Buffer.from(packet.header().slice(2), "hex"))),
          payloadHash: Array.from(payloadHashBytes),
          confirmations: typeof confirmations === "string" ? new BN(confirmations) : confirmations
        }
      },
      this.program
    );
  }
  /**
   * Closes the verification process.
   *
   * @param {PublicKey} dvn - The DVN public key.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {Uint8Array} packetBytes - The packet bytes.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  closeVerify(dvn, receiver, packetBytes) {
    const packet = PacketV1Codec.fromBytes(packetBytes);
    const headerHash = packet.headerHash();
    const headerHashBytes = Uint8Array.from(Buffer.from(headerHash.slice(2), "hex"));
    const payloadHash = packet.payloadHash();
    const payloadHashBytes = Uint8Array.from(Buffer.from(payloadHash.slice(2), "hex"));
    const [confirmationsPDA] = this.deriver.confirmations(headerHashBytes, payloadHashBytes, dvn);
    return createCloseVerifyInstruction(
      {
        dvn,
        receiver,
        confirmations: confirmationsPDA
      },
      {
        params: {
          packetHeaderHash: Array.from(headerHashBytes),
          payloadHash: Array.from(payloadHashBytes)
        }
      },
      this.program
    );
  }
  /**
   * Checks if the DVN is verified.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {PublicKey} dvn - The DVN public key.
   * @param {Uint8Array} packetBytes - The packet bytes.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<boolean>} A promise that resolves to true if the DVN is verified, false otherwise.
   */
  async isDvnVerified(connection, endpointProgram, dvn, packetBytes, commitmentOrConfig = "confirmed") {
    const packet = PacketV1Codec.fromBytes(packetBytes);
    const headerHash = packet.headerHash();
    const headerHashBytes = Uint8Array.from(Buffer.from(headerHash.slice(2), "hex"));
    const payloadHash = packet.payloadHash();
    const payloadHashBytes = Uint8Array.from(Buffer.from(payloadHash.slice(2), "hex"));
    const [confirmationsPDA] = this.deriver.confirmations(headerHashBytes, payloadHashBytes, dvn);
    const endpoint = new endpoint_exports.Endpoint(endpointProgram);
    const receiver = new PublicKey(arrayify(packet.receiver()));
    const sender = arrayify(packet.sender());
    const nonce = await endpoint.getNonce(connection, receiver, packet.srcEid(), sender, commitmentOrConfig);
    const pendingNonce = await endpoint.getPendingInboundNonce(
      connection,
      receiver,
      packet.srcEid(),
      sender,
      commitmentOrConfig
    );
    if (!nonce || !pendingNonce) {
      return false;
    }
    if (parseInt(nonce.inboundNonce.toString()) >= parseInt(packet.nonce())) {
      return true;
    }
    if (pendingNonce.nonces.find((n) => n.toString() === packet.nonce()) !== void 0) {
      return true;
    }
    try {
      const confirmation = await Confirmations.fromAccountAddress(
        connection,
        confirmationsPDA,
        commitmentOrConfig
      );
      if (confirmation.value === null) {
        return false;
      } else {
        return true;
      }
    } catch (e) {
      return false;
    }
  }
  /**
   * Commits the verification process.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {Uint8Array} packetBytes - The packet bytes.
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   */
  async commitVerification(connection, endpointProgram, packetBytes) {
    const packet = PacketV1Codec.fromBytes(packetBytes);
    const payloadHash = packet.payloadHash();
    const srcEid = packet.srcEid();
    const receiver = new PublicKey(arrayify(packet.receiver()));
    const payloadHashBytes = Uint8Array.from(Buffer.from(payloadHash.slice(2), "hex"));
    const [defaultReceiveConfig] = this.deriver.defaultReceiveConfig(srcEid);
    const [receiveConfig] = this.deriver.receiveConfig(srcEid, receiver);
    const [setting] = this.deriver.setting();
    const [msgLib] = this.deriver.messageLib();
    const receiveConfigState = await this.getFinalReceiveConfigState(connection, receiver, srcEid);
    const headerHash = packet.headerHash();
    const headerHashBytes = Uint8Array.from(Buffer.from(headerHash.slice(2), "hex"));
    const confirmations = receiveConfigState.uln.requiredDvns.concat(receiveConfigState.uln.optionalDvns).map((p) => {
      const [confirmationsPDA] = this.deriver.confirmations(headerHashBytes, payloadHashBytes, p);
      return {
        pubkey: confirmationsPDA,
        isSigner: false,
        isWritable: true
      };
    });
    const endpoint = new endpoint_exports.Endpoint(endpointProgram);
    const verifyAccounts = endpoint.getVerifyIXAccountMetaForCPI(packet, msgLib);
    return createCommitVerificationInstruction(
      {
        uln: setting,
        defaultReceiveConfig,
        receiveConfig,
        anchorRemainingAccounts: confirmations.concat(verifyAccounts)
      },
      {
        params: {
          packetHeader: Array.from(arrayify(packet.header())),
          payloadHash: Array.from(payloadHashBytes)
        }
      },
      this.program
    );
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the init config instruction.
   *
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} oappID - The OApp ID public key.
   * @param {number} eid - The endpoint ID.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getInitConfigIXAccountMetaForCPI(payer, oappID, eid) {
    const [sendConfig] = this.deriver.sendConfig(eid, oappID);
    const [receiveConfig] = this.deriver.receiveConfig(eid, oappID);
    const [setting] = this.deriver.setting();
    const accounts = createInitConfigInstructionAccounts3(
      {
        endpoint: PublicKey.default,
        // useless
        payer,
        uln: setting,
        sendConfig,
        receiveConfig
      },
      this.program
    );
    accounts.forEach((key) => {
      if (!payer.equals(key.pubkey)) {
        key.isSigner = false;
      }
    });
    return accounts.slice(1);
  }
  /**
   * Constructs the set config data.
   *
   * @param {SetConfigType} configType - The configuration type.
   * @param {types.ExecutorConfig | types.UlnConfig} configData - The configuration data.
   * @returns {Uint8Array} The constructed set config data.
   * @throws {Error} If the configuration type is invalid.
   */
  static constructSetConfigData(configType, configData) {
    switch (configType) {
      case 1 /* EXECUTOR */: {
        const [data] = executorConfigBeet.serialize(configData);
        return Uint8Array.from(data);
      }
      case 2 /* SEND_ULN */:
      case 3 /* RECEIVE_ULN */: {
        const [data] = ulnConfigBeet2.serialize(configData);
        return Uint8Array.from(data);
      }
      default:
        throw new Error(`invalid config type: ${configType}`);
    }
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the set config instruction.
   *
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {PublicKey} oappID - The OApp ID public key.
   * @param {number} eid - The endpoint ID.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  async getSetConfigIXAccountMetaForCPI(endpointProgram, oappID, eid) {
    const [sendConfig] = this.deriver.sendConfig(eid, oappID);
    const [receiveConfig] = this.deriver.receiveConfig(eid, oappID);
    const [defaultSendConfig] = this.deriver.defaultSendConfig(eid);
    const [defaultReceiveConfig] = this.deriver.defaultReceiveConfig(eid);
    const [setting] = this.deriver.setting();
    const [msgLib] = this.deriver.messageLib();
    const [msgLibInfo] = new EndpointPDADeriver(endpointProgram).messageLibraryInfo(msgLib);
    const accounts = createSetConfigInstructionAccounts4(
      {
        endpoint: msgLibInfo,
        uln: setting,
        sendConfig,
        defaultSendConfig,
        defaultReceiveConfig,
        receiveConfig,
        eventAuthority: this.eventAuthorityPDA,
        program: this.program
      },
      this.program
    );
    return Promise.resolve(accounts.slice(1));
  }
  //
  // all of below functions are retrieving accounts state
  /**
   * Gets the ULN settings.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.UlnSettings | null>} A promise that resolves to the ULN settings or null if not found.
   */
  async getSetting(connection, commitmentOrConfig) {
    const [setting] = this.deriver.setting();
    try {
      return await UlnSettings.fromAccountAddress(connection, setting, commitmentOrConfig);
    } catch (e) {
      return null;
    }
  }
  /**
   * Gets the default send configuration state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.SendConfig | null>} A promise that resolves to the default send configuration state or null if not found.
   */
  async getDefaultSendConfigState(connection, eid, commitmentOrConfig) {
    const [config2] = this.deriver.defaultSendConfig(eid);
    try {
      return await SendConfig.fromAccountAddress(connection, config2, commitmentOrConfig);
    } catch (e) {
      return null;
    }
  }
  /**
   * Gets the send configuration state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} sender - The sender public key.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.SendConfig | null>} A promise that resolves to the send configuration state or null if not found.
   */
  async getSendConfigState(connection, sender, eid, commitmentOrConfig) {
    const [config2] = this.deriver.sendConfig(eid, sender);
    try {
      return await SendConfig.fromAccountAddress(connection, config2, commitmentOrConfig);
    } catch (e) {
      return null;
    }
  }
  /**
   * Gets the default receive configuration state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.ReceiveConfig | null>} A promise that resolves to the default receive configuration state or null if not found.
   */
  async getDefaultReceiveConfigState(connection, eid, commitmentOrConfig) {
    const [config2] = this.deriver.defaultReceiveConfig(eid);
    try {
      return await ReceiveConfig2.fromAccountAddress(connection, config2, commitmentOrConfig);
    } catch (e) {
      return null;
    }
  }
  /**
   * Gets the receive configuration state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.ReceiveConfig | null>} A promise that resolves to the receive configuration state or null if not found.
   */
  async getReceiveConfigState(connection, receiver, eid, commitmentOrConfig) {
    const [config2] = this.deriver.receiveConfig(eid, receiver);
    try {
      return await ReceiveConfig2.fromAccountAddress(connection, config2, commitmentOrConfig);
    } catch (e) {
      return null;
    }
  }
  /**
   * Gets the final receive configuration state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<accounts.ReceiveConfig>} A promise that resolves to the final receive configuration state.
   * @throws {Error} If no DVN is found.
   */
  async getFinalReceiveConfigState(connection, receiver, eid, commitmentOrConfig = "confirmed") {
    const [defaultConfig] = this.deriver.defaultReceiveConfig(eid);
    const [customConfig] = this.deriver.receiveConfig(eid, receiver);
    const [defaultConfigInfo, customConfigInfo] = await connection.getMultipleAccountsInfo(
      [defaultConfig, customConfig],
      commitmentOrConfig
    );
    if (defaultConfigInfo == null) {
      throw new Error(`please init default receive config first. ${defaultConfig.toBase58()}`);
    }
    const defaultConfigState = ReceiveConfig2.fromAccountInfo(defaultConfigInfo)[0];
    const customConfigState = customConfigInfo ? ReceiveConfig2.fromAccountInfo(customConfigInfo)[0] : null;
    const ulnConfigState = this.getUlnConfigState(defaultConfigState, customConfigState);
    return ReceiveConfig2.fromArgs({
      bump: defaultConfigState.bump,
      uln: ulnConfigState
    });
  }
  getUlnConfigState(defaultConfigState, customConfigState) {
    const NIL_CONFIRMATIONS = "18446744073709551615";
    const NIL_DVN_COUNT = "255";
    const rtn_config = {
      confirmations: 0,
      requiredDvnCount: 0,
      optionalDvnCount: 0,
      optionalDvnThreshold: 0,
      requiredDvns: [],
      optionalDvns: []
    };
    if (customConfigState == null || customConfigState.uln.confirmations.toString() === "0") {
      rtn_config.confirmations = defaultConfigState.uln.confirmations;
    } else if (customConfigState.uln.confirmations.toString() !== NIL_CONFIRMATIONS) {
      rtn_config.confirmations = customConfigState.uln.confirmations;
    }
    if (customConfigState == null || customConfigState.uln.requiredDvnCount == 0) {
      if (defaultConfigState.uln.requiredDvnCount > 0) {
        rtn_config.requiredDvns = defaultConfigState.uln.requiredDvns;
        rtn_config.requiredDvnCount = defaultConfigState.uln.requiredDvnCount;
      }
    } else if (customConfigState.uln.requiredDvnCount.toString() !== NIL_DVN_COUNT) {
      rtn_config.requiredDvns = customConfigState.uln.requiredDvns;
      rtn_config.requiredDvnCount = customConfigState.uln.requiredDvnCount;
    }
    if (customConfigState == null || customConfigState.uln.optionalDvnCount == 0) {
      if (defaultConfigState.uln.optionalDvnCount > 0) {
        rtn_config.optionalDvns = defaultConfigState.uln.optionalDvns;
        rtn_config.optionalDvnCount = defaultConfigState.uln.optionalDvnCount;
        rtn_config.optionalDvnThreshold = defaultConfigState.uln.optionalDvnThreshold;
      }
    } else if (customConfigState.uln.optionalDvnCount.toString() !== NIL_DVN_COUNT) {
      rtn_config.optionalDvns = customConfigState.uln.optionalDvns;
      rtn_config.optionalDvnCount = customConfigState.uln.optionalDvnCount;
      rtn_config.optionalDvnThreshold = customConfigState.uln.optionalDvnThreshold;
    }
    if (rtn_config.requiredDvnCount === 0 && rtn_config.optionalDvnCount === 0) {
      throw new Error("no dvn");
    }
    return rtn_config;
  }
  getExecutorConfigState(defaultConfigState, customConfigState) {
    const rtn_config = {
      maxMessageSize: 0,
      executor: PublicKey.default
    };
    if (customConfigState == null || customConfigState.executor.executor.equals(PublicKey.default)) {
      rtn_config.executor = defaultConfigState.executor.executor;
    } else {
      rtn_config.executor = customConfigState.executor.executor;
    }
    if (customConfigState == null || customConfigState.executor.maxMessageSize === 0) {
      rtn_config.maxMessageSize = defaultConfigState.executor.maxMessageSize;
    } else {
      rtn_config.maxMessageSize = customConfigState.executor.maxMessageSize;
    }
    return rtn_config;
  }
};

// src/solita/pricefeed.ts
var pricefeed_exports = {};
__export(pricefeed_exports, {
  PROGRAM_ID: () => PROGRAM_ID6,
  PriceFeed: () => PriceFeed2,
  accounts: () => accounts_exports6,
  instructions: () => instructions_exports6,
  types: () => types_exports6
});

// src/generated/solita/pricefeed/accounts/index.ts
var accounts_exports6 = {};
__export(accounts_exports6, {
  PriceFeed: () => PriceFeed,
  accountProviders: () => accountProviders6,
  priceFeedBeet: () => priceFeedBeet,
  priceFeedDiscriminator: () => priceFeedDiscriminator
});
var isModelTypeArbitrum = (x) => x.__kind === "Arbitrum";
var isModelTypeOptimism = (x) => x.__kind === "Optimism";
var modelTypeBeet = beet159.dataEnum([
  [
    "Arbitrum",
    new beet159.BeetArgsStruct(
      [
        ["gasPerL2Tx", beet159.u64],
        ["gasPerL1CalldataByte", beet159.u32]
      ],
      'ModelTypeRecord["Arbitrum"]'
    )
  ],
  [
    "Optimism",
    new beet159.BeetArgsStruct(
      [["l1Eid", beet159.u32]],
      'ModelTypeRecord["Optimism"]'
    )
  ]
]);

// src/generated/solita/pricefeed/types/Price.ts
var priceBeet = new beet159.FixableBeetArgsStruct(
  [
    ["eid", beet159.u32],
    ["priceRatio", beet159.u128],
    ["gasPriceInUnit", beet159.u64],
    ["gasPerByte", beet159.u32],
    ["modelType", beet159.coption(modelTypeBeet)]
  ],
  "Price"
);

// src/generated/solita/pricefeed/accounts/PriceFeed.ts
var priceFeedDiscriminator = [189, 103, 252, 23, 152, 35, 243, 156];
var PriceFeed = class _PriceFeed {
  constructor(admin, updaters, priceRatioDenominator, arbitrumCompressionPercent, nativeTokenPriceUsd, prices, bump) {
    this.admin = admin;
    this.updaters = updaters;
    this.priceRatioDenominator = priceRatioDenominator;
    this.arbitrumCompressionPercent = arbitrumCompressionPercent;
    this.nativeTokenPriceUsd = nativeTokenPriceUsd;
    this.prices = prices;
    this.bump = bump;
  }
  /**
   * Creates a {@link PriceFeed} instance from the provided args.
   */
  static fromArgs(args) {
    return new _PriceFeed(
      args.admin,
      args.updaters,
      args.priceRatioDenominator,
      args.arbitrumCompressionPercent,
      args.nativeTokenPriceUsd,
      args.prices,
      args.bump
    );
  }
  /**
   * Deserializes the {@link PriceFeed} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo, offset = 0) {
    return _PriceFeed.deserialize(accountInfo.data, offset);
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link PriceFeed} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(connection, address, commitmentOrConfig) {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    );
    if (accountInfo == null) {
      throw new Error(`Unable to find PriceFeed account at ${address}`);
    }
    return _PriceFeed.fromAccountInfo(accountInfo, 0)[0];
  }
  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId = new web3108.PublicKey(
    "8ahPGPjEbpgGaZx2NV1iG5Shj7TDwvsjkEDcGWjt94TP"
  )) {
    return beetSolana96.GpaBuilder.fromStruct(programId, priceFeedBeet);
  }
  /**
   * Deserializes the {@link PriceFeed} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf, offset = 0) {
    return priceFeedBeet.deserialize(buf, offset);
  }
  /**
   * Serializes the {@link PriceFeed} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize() {
    return priceFeedBeet.serialize({
      accountDiscriminator: priceFeedDiscriminator,
      ...this
    });
  }
  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link PriceFeed} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args) {
    const instance = _PriceFeed.fromArgs(args);
    return priceFeedBeet.toFixedFromValue({
      accountDiscriminator: priceFeedDiscriminator,
      ...instance
    }).byteSize;
  }
  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link PriceFeed} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(args, connection, commitment) {
    return connection.getMinimumBalanceForRentExemption(
      _PriceFeed.byteSize(args),
      commitment
    );
  }
  /**
   * Returns a readable version of {@link PriceFeed} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      admin: this.admin.toBase58(),
      updaters: this.updaters,
      priceRatioDenominator: (() => {
        const x = this.priceRatioDenominator;
        if (typeof x.toNumber === "function") {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      arbitrumCompressionPercent: (() => {
        const x = this.arbitrumCompressionPercent;
        if (typeof x.toNumber === "function") {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      nativeTokenPriceUsd: this.nativeTokenPriceUsd,
      prices: this.prices,
      bump: this.bump
    };
  }
};
var priceFeedBeet = new beet159.FixableBeetStruct(
  [
    ["accountDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["admin", beetSolana96.publicKey],
    ["updaters", beet159.array(beetSolana96.publicKey)],
    ["priceRatioDenominator", beet159.u128],
    ["arbitrumCompressionPercent", beet159.u128],
    ["nativeTokenPriceUsd", beet159.coption(beet159.u128)],
    ["prices", beet159.array(priceBeet)],
    ["bump", beet159.u8]
  ],
  PriceFeed.fromArgs,
  "PriceFeed"
);

// src/generated/solita/pricefeed/accounts/index.ts
var accountProviders6 = { PriceFeed };

// src/generated/solita/pricefeed/instructions/index.ts
var instructions_exports6 = {};
__export(instructions_exports6, {
  createGetFeeInstruction: () => createGetFeeInstruction,
  createGetFeeInstructionAccounts: () => createGetFeeInstructionAccounts,
  createInitPriceFeedInstruction: () => createInitPriceFeedInstruction,
  createInitPriceFeedInstructionAccounts: () => createInitPriceFeedInstructionAccounts,
  createSetPriceFeedInstruction: () => createSetPriceFeedInstruction,
  createSetPriceFeedInstructionAccounts: () => createSetPriceFeedInstructionAccounts,
  createSetPriceInstruction: () => createSetPriceInstruction,
  createSetPriceInstructionAccounts: () => createSetPriceInstructionAccounts,
  createSetSolPriceInstruction: () => createSetSolPriceInstruction,
  createSetSolPriceInstructionAccounts: () => createSetSolPriceInstructionAccounts,
  createTransferAdminInstruction: () => createTransferAdminInstruction4,
  createTransferAdminInstructionAccounts: () => createTransferAdminInstructionAccounts4,
  getFeeInstructionDiscriminator: () => getFeeInstructionDiscriminator,
  getFeeStruct: () => getFeeStruct,
  initPriceFeedInstructionDiscriminator: () => initPriceFeedInstructionDiscriminator,
  initPriceFeedStruct: () => initPriceFeedStruct,
  setPriceFeedInstructionDiscriminator: () => setPriceFeedInstructionDiscriminator,
  setPriceFeedStruct: () => setPriceFeedStruct,
  setPriceInstructionDiscriminator: () => setPriceInstructionDiscriminator,
  setPriceStruct: () => setPriceStruct,
  setSolPriceInstructionDiscriminator: () => setSolPriceInstructionDiscriminator,
  setSolPriceStruct: () => setSolPriceStruct,
  transferAdminInstructionDiscriminator: () => transferAdminInstructionDiscriminator4,
  transferAdminStruct: () => transferAdminStruct4
});
var getFeeParamsBeet = new beet159.BeetArgsStruct(
  [
    ["dstEid", beet159.u32],
    ["calldataSize", beet159.u64],
    ["totalGas", beet159.u128]
  ],
  "GetFeeParams"
);

// src/generated/solita/pricefeed/instructions/getFee.ts
var getFeeStruct = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", getFeeParamsBeet]
  ],
  "GetFeeInstructionArgs"
);
var getFeeInstructionDiscriminator = [
  115,
  195,
  235,
  161,
  25,
  219,
  60,
  29
];
function createGetFeeInstruction(accounts, args, programId) {
  const [data] = getFeeStruct.serialize({
    instructionDiscriminator: getFeeInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.priceFeed,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createGetFeeInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.priceFeed,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var initPriceFeedParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["admin", beetSolana96.publicKey],
    ["updaters", beet159.array(beetSolana96.publicKey)]
  ],
  "InitPriceFeedParams"
);

// src/generated/solita/pricefeed/instructions/initPriceFeed.ts
var initPriceFeedStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", initPriceFeedParamsBeet]
  ],
  "InitPriceFeedInstructionArgs"
);
var initPriceFeedInstructionDiscriminator = [
  27,
  209,
  184,
  5,
  152,
  116,
  136,
  16
];
function createInitPriceFeedInstruction(accounts, args, programId) {
  const [data] = initPriceFeedStruct.serialize({
    instructionDiscriminator: initPriceFeedInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.priceFeed,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createInitPriceFeedInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.payer,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.priceFeed,
      isWritable: true,
      isSigner: false
    },
    {
      pubkey: accounts.systemProgram ?? web3108.SystemProgram.programId,
      isWritable: false,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var priceParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["priceRatio", beet159.u128],
    ["gasPriceInUnit", beet159.u64],
    ["gasPerByte", beet159.u32],
    ["modelType", beet159.coption(modelTypeBeet)]
  ],
  "PriceParams"
);

// src/generated/solita/pricefeed/types/SetPriceParams.ts
var setPriceParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["dstEid", beet159.u32],
    ["params", beet159.coption(priceParamsBeet)]
  ],
  "SetPriceParams"
);

// src/generated/solita/pricefeed/instructions/setPrice.ts
var setPriceStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setPriceParamsBeet]
  ],
  "SetPriceInstructionArgs"
);
var setPriceInstructionDiscriminator = [
  16,
  19,
  182,
  8,
  149,
  83,
  72,
  181
];
function createSetPriceInstruction(accounts, args, programId) {
  const [data] = setPriceStruct.serialize({
    instructionDiscriminator: setPriceInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.updater,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.priceFeed,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetPriceInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.updater,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.priceFeed,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setPriceFeedParamsBeet = new beet159.FixableBeetArgsStruct(
  [
    ["updaters", beet159.array(beetSolana96.publicKey)],
    ["priceRatioDenominator", beet159.u128],
    ["arbitrumCompressionPercent", beet159.u128]
  ],
  "SetPriceFeedParams"
);

// src/generated/solita/pricefeed/instructions/setPriceFeed.ts
var setPriceFeedStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setPriceFeedParamsBeet]
  ],
  "SetPriceFeedInstructionArgs"
);
var setPriceFeedInstructionDiscriminator = [
  13,
  15,
  231,
  129,
  61,
  7,
  28,
  122
];
function createSetPriceFeedInstruction(accounts, args, programId) {
  const [data] = setPriceFeedStruct.serialize({
    instructionDiscriminator: setPriceFeedInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.priceFeed,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetPriceFeedInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: true,
      isSigner: true
    },
    {
      pubkey: accounts.priceFeed,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var setSolPriceParamsBeet = new beet159.FixableBeetArgsStruct(
  [["nativeTokenPriceUsd", beet159.coption(beet159.u128)]],
  "SetSolPriceParams"
);

// src/generated/solita/pricefeed/instructions/setSolPrice.ts
var setSolPriceStruct = new beet159.FixableBeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", setSolPriceParamsBeet]
  ],
  "SetSolPriceInstructionArgs"
);
var setSolPriceInstructionDiscriminator = [
  151,
  237,
  70,
  191,
  232,
  81,
  138,
  214
];
function createSetSolPriceInstruction(accounts, args, programId) {
  const [data] = setSolPriceStruct.serialize({
    instructionDiscriminator: setSolPriceInstructionDiscriminator,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.updater,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.priceFeed,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createSetSolPriceInstructionAccounts(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.updater,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.priceFeed,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}
var transferAdminParamsBeet4 = new beet159.BeetArgsStruct(
  [["admin", beetSolana96.publicKey]],
  "TransferAdminParams"
);

// src/generated/solita/pricefeed/instructions/transferAdmin.ts
var transferAdminStruct4 = new beet159.BeetArgsStruct(
  [
    ["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)],
    ["params", transferAdminParamsBeet4]
  ],
  "TransferAdminInstructionArgs"
);
var transferAdminInstructionDiscriminator4 = [
  42,
  242,
  66,
  106,
  228,
  10,
  111,
  156
];
function createTransferAdminInstruction4(accounts, args, programId) {
  const [data] = transferAdminStruct4.serialize({
    instructionDiscriminator: transferAdminInstructionDiscriminator4,
    ...args
  });
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.priceFeed,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createTransferAdminInstructionAccounts4(accounts, programId) {
  const keys = [
    {
      pubkey: accounts.admin,
      isWritable: false,
      isSigner: true
    },
    {
      pubkey: accounts.priceFeed,
      isWritable: true,
      isSigner: false
    }
  ];
  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc);
    }
  }
  return keys;
}

// src/generated/solita/pricefeed/types/index.ts
var types_exports6 = {};
__export(types_exports6, {
  getFeeParamsBeet: () => getFeeParamsBeet,
  initPriceFeedParamsBeet: () => initPriceFeedParamsBeet,
  isModelTypeArbitrum: () => isModelTypeArbitrum,
  isModelTypeOptimism: () => isModelTypeOptimism,
  modelTypeBeet: () => modelTypeBeet,
  priceBeet: () => priceBeet,
  priceParamsBeet: () => priceParamsBeet,
  setPriceFeedParamsBeet: () => setPriceFeedParamsBeet,
  setPriceParamsBeet: () => setPriceParamsBeet,
  setSolPriceParamsBeet: () => setSolPriceParamsBeet,
  transferAdminParamsBeet: () => transferAdminParamsBeet4
});

// src/generated/solita/pricefeed/errors/index.ts
var createErrorFromCodeLookup6 = /* @__PURE__ */ new Map();
var createErrorFromNameLookup6 = /* @__PURE__ */ new Map();
var TooManyUpdatersError = class _TooManyUpdatersError extends Error {
  constructor() {
    super("");
    this.code = 6e3;
    this.name = "TooManyUpdaters";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _TooManyUpdatersError);
    }
  }
};
createErrorFromCodeLookup6.set(6e3, () => new TooManyUpdatersError());
createErrorFromNameLookup6.set(
  "TooManyUpdaters",
  () => new TooManyUpdatersError()
);
var InvalidUpdaterError = class _InvalidUpdaterError extends Error {
  constructor() {
    super("");
    this.code = 6001;
    this.name = "InvalidUpdater";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidUpdaterError);
    }
  }
};
createErrorFromCodeLookup6.set(6001, () => new InvalidUpdaterError());
createErrorFromNameLookup6.set("InvalidUpdater", () => new InvalidUpdaterError());
var NotFoundError = class _NotFoundError extends Error {
  constructor() {
    super("");
    this.code = 6002;
    this.name = "NotFound";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _NotFoundError);
    }
  }
};
createErrorFromCodeLookup6.set(6002, () => new NotFoundError());
createErrorFromNameLookup6.set("NotFound", () => new NotFoundError());
var InvalidSizeError2 = class _InvalidSizeError extends Error {
  constructor() {
    super("");
    this.code = 6003;
    this.name = "InvalidSize";
    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, _InvalidSizeError);
    }
  }
};
createErrorFromCodeLookup6.set(6003, () => new InvalidSizeError2());
createErrorFromNameLookup6.set("InvalidSize", () => new InvalidSizeError2());

// src/generated/solita/pricefeed/index.ts
var PROGRAM_ADDRESS6 = "8ahPGPjEbpgGaZx2NV1iG5Shj7TDwvsjkEDcGWjt94TP";
var PROGRAM_ID6 = new PublicKey(PROGRAM_ADDRESS6);

// src/solita/pricefeed.ts
var PriceFeed2 = class {
  /**
   * Creates an instance of the PriceFeed class.
   *
   * @param {PublicKey} program - The public key of the program.
   */
  constructor(program) {
    this.program = program;
    this.deriver = new PriceFeedPDADeriver(this.program);
  }
  /**
   * Initializes the price feed.
   *
   * @param {Connection} connection - The Solana connection object.
   * @param {PublicKey} payer - The public key of the payer.
   * @param {PublicKey} admin - The public key of the admin.
   * @param {PublicKey[]} updaters - The list of public keys of the updaters.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - Optional commitment or configuration.
   *
   * @returns {Promise<TransactionInstruction>} The transaction instruction.
   */
  async initPriceFeed(connection, payer, admin, updaters, commitmentOrConfig) {
    const [priceFeed] = this.deriver.priceFeed();
    const info = await connection.getAccountInfo(priceFeed, commitmentOrConfig);
    if (info) {
      throw new Error("Pricefeed already initialized");
    }
    return createInitPriceFeedInstruction(
      {
        payer,
        priceFeed
      },
      {
        params: {
          admin,
          updaters
        }
      },
      this.program
    );
  }
  /**
   * Sets the price feed.
   *
   * @param {PublicKey} admin - The public key of the admin.
   * @param {PublicKey[]} updaters - The list of public keys of the updaters.
   * @param {bignum} priceRatioDenominator - The price ratio denominator.
   * @param {number} arbitrumCompressionPercent - The Arbitrum compression percentage.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setPriceFeed(admin, updaters, priceRatioDenominator, arbitrumCompressionPercent) {
    const [priceFeed] = this.deriver.priceFeed();
    return createSetPriceFeedInstruction(
      {
        admin,
        priceFeed
      },
      {
        params: {
          updaters,
          priceRatioDenominator,
          arbitrumCompressionPercent
        }
      },
      this.program
    );
  }
  /**
   * Sets the price.
   *
   * @param {PublicKey} updater - The public key of the updater.
   * @param {number} dstEid - The destination EID.
   * @param {bignum} priceRatio - The price ratio.
   * @param {bignum} gasPriceInUnit - The gas price in unit.
   * @param {number} gasPerByte - The gas per byte.
   * @param {types.ModelType | null} modelType - The model type.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setPrice(updater, dstEid, priceRatio, gasPriceInUnit, gasPerByte, modelType2) {
    const EID_MODULUS = 3e4;
    dstEid = dstEid % EID_MODULUS;
    const [priceFeed] = this.deriver.priceFeed();
    return createSetPriceInstruction(
      {
        updater,
        priceFeed
      },
      {
        params: {
          dstEid,
          params: {
            priceRatio,
            gasPriceInUnit,
            gasPerByte,
            modelType: modelType2
          }
        }
      },
      this.program
    );
  }
  /**
   * Sets the SOL price.
   *
   * @param {PublicKey} updater - The public key of the updater.
   * @param {bignum | null} nativeTokenPriceUsd - The native token price in USD.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setSolPrice(updater, nativeTokenPriceUsd) {
    const [priceFeed] = this.deriver.priceFeed();
    return createSetSolPriceInstruction(
      {
        priceFeed,
        updater
      },
      {
        params: {
          nativeTokenPriceUsd
        }
      },
      this.program
    );
  }
  /**
   * Transfers the admin role.
   *
   * @param {PublicKey} admin - The public key of the current admin.
   * @param {PublicKey} newAdmin - The public key of the new admin.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  transferAdmin(admin, newAdmin) {
    const [priceFeed] = this.deriver.priceFeed();
    return createTransferAdminInstruction4(
      {
        admin,
        priceFeed
      },
      {
        params: {
          admin: newAdmin
        }
      },
      this.program
    );
  }
  /**
   * Retrieves the price feed account.
   *
   * @param {Connection} connection - The Solana connection object.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - Optional commitment or configuration.
   *
   * @returns {Promise<accounts.PriceFeed | null>} The price feed account or null if not found.
   */
  async getPriceFeed(connection, commitmentOrConfig) {
    const [priceFeed] = this.deriver.priceFeed();
    try {
      return await PriceFeed.fromAccountAddress(connection, priceFeed, commitmentOrConfig);
    } catch (e) {
      return null;
    }
  }
};
function oappIDPDA(program, seed = COUNT_SEED, id) {
  if (id != void 0) {
    return PublicKey.findProgramAddressSync(
      [Buffer.from(seed, "utf8"), new BN(id).toArrayLike(Buffer, "be", 1)],
      program
    );
  } else {
    return PublicKey.findProgramAddressSync([Buffer.from(seed, "utf8")], program);
  }
}
function deriveLzReceiveTypesAccountsPDA(program, oappId) {
  if (oappId != void 0) {
    return PublicKey.findProgramAddressSync([Buffer.from(LZ_RECEIVE_TYPES_SEED, "utf8"), oappId.toBytes()], program);
  }
  return PublicKey.findProgramAddressSync([Buffer.from(LZ_RECEIVE_TYPES_SEED, "utf8")], program);
}
function deriveLzComposeTypesAccountsPDA(program, oappId) {
  if (oappId != void 0) {
    return PublicKey.findProgramAddressSync([Buffer.from(LZ_COMPOSE_TYPES_SEED, "utf8"), oappId.toBytes()], program);
  }
  return PublicKey.findProgramAddressSync([Buffer.from(LZ_COMPOSE_TYPES_SEED, "utf8")], program);
}
var BaseOApp = class {
  /**
   * Creates an instance of the BaseOApp class.
   *
   * @param {PublicKey} program - The program public key.
   */
  constructor(program) {
    this.program = program;
    this.oappBaseDeriver = new OAppBasePDADeriver(program);
  }
  /**
   * Queries the ID PDA info.
   *
   * @param {Connection} connection - The connection.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment or config.
   * @returns {Promise<AccountInfo<Buffer> | null>} The account info.
   */
  async queryIDPDAInfo(connection, commitmentOrConfig) {
    return this.queryPDAInfo(connection, this.idPDA()[0], commitmentOrConfig);
  }
  /**
   * Queries the PDA info.
   *
   * @param {Connection} connection - The connection.
   * @param {PublicKey} pda - The PDA public key.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment or config.
   * @returns {Promise<AccountInfo<Buffer> | null>} The account info.
   */
  async queryPDAInfo(connection, pda, commitmentOrConfig) {
    return connection.getAccountInfo(pda, commitmentOrConfig);
  }
  /**
   * Gets the ID PDA.
   *
   * @returns {[PublicKey, number]} The ID PDA and bump.
   */
  idPDA() {
    return oappIDPDA(this.program);
  }
  /**
   * Gets the remote.
   *
   * @param {Connection} connection - The connection.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment or config.
   * @returns {Promise<Uint8Array | null>} The remote data.
   */
  async getRemote(connection, dstEid, commitmentOrConfig) {
    const [remotePDA] = this.oappBaseDeriver.remote(dstEid);
    const info = await this.queryPDAInfo(connection, remotePDA, commitmentOrConfig);
    if (info) {
      const result = AddressType.read(info.data, 8);
      return Uint8Array.from(result);
    }
    return null;
  }
};
var idlTypes = [
  "MessageLibQuoteParams",
  "MessageLibSendParams",
  "MessagingFee",
  "MessagingReceipt",
  "PacketForQuote",
  "Packet",
  "InitConfigParams",
  "MessageLibSetConfigParams",
  "MessageLibSnapshotConfigParams",
  "MessageLibResetConfigParams"
];
var messageLibs = ["uln", "simple_messagelib"];
var FAUCET_URL = {
  [Environment.MAINNET]: "",
  [Environment.TESTNET]: "",
  [Environment.DEVNET]: "",
  [Environment.LOCAL]: "http://127.0.0.1:9900"
};
function getProgramKeypair(network, program) {
  network = network === "default" ? "solana-sandbox-local" : network;
  const deployment = __require(`@layerzerolabs/lz-solana-sdk-v2/deployments/${network}/${program}.json`);
  return new PublicKey(deployment.address);
}
function getEndpointProgramId(network) {
  return getProgramKeypair(network, "endpoint");
}
function getSimpleMessageLibProgramId(network) {
  return getProgramKeypair(network, "simple_messagelib");
}
function getULNProgramId(network) {
  return getProgramKeypair(network, "uln");
}
function getDVNProgramId(network) {
  return getProgramKeypair(network, "dvn");
}
function getBlockedMessageLibProgramId(network) {
  return getProgramKeypair(network, "blocked_messagelib");
}
function getExecutorProgramId(network) {
  return getProgramKeypair(network, "executor");
}
function getPricefeedProgramId(network) {
  return getProgramKeypair(network, "pricefeed");
}

// src/generated/solita/blocked_messagelib/index.ts
var blocked_messagelib_exports = {};
__export(blocked_messagelib_exports, {
  PROGRAM_ADDRESS: () => PROGRAM_ADDRESS7,
  PROGRAM_ID: () => PROGRAM_ID7,
  createVersionInstruction: () => createVersionInstruction3,
  createVersionInstructionAccounts: () => createVersionInstructionAccounts3,
  versionInstructionDiscriminator: () => versionInstructionDiscriminator3,
  versionStruct: () => versionStruct3
});
var versionStruct3 = new beet159.BeetArgsStruct(
  [["instructionDiscriminator", beet159.uniformFixedSizeArray(beet159.u8, 8)]],
  "VersionInstructionArgs"
);
var versionInstructionDiscriminator3 = [
  118,
  65,
  195,
  198,
  129,
  216,
  252,
  192
];
function createVersionInstruction3(programId) {
  const [data] = versionStruct3.serialize({
    instructionDiscriminator: versionInstructionDiscriminator3
  });
  const keys = [];
  const ix = new web3108.TransactionInstruction({
    programId,
    keys,
    data
  });
  return ix;
}
function createVersionInstructionAccounts3(programId) {
  const keys = [];
  return keys;
}

// src/generated/solita/blocked_messagelib/index.ts
var PROGRAM_ADDRESS7 = "2XrYqmhBMPJgDsb4SVbjV1PnJBprurd5bzRCkHwiFCJB";
var PROGRAM_ID7 = new PublicKey(PROGRAM_ADDRESS7);
var SendHelper = class {
  /**
   * Creates an instance of the SendHelper class.
   *
   * @param {PublicKey} [endpointProgram=EndpointProgram.PROGRAM_ID] - The endpoint program public key.
   * @param {PublicKey} [ulnProgram=UlnProgram.PROGRAM_ID] - The ULN program public key.
   * @param {PublicKey} [simpleMsgLibProgram=SimpleMessageLibProgram.PROGRAM_ID] - The simple message library program public key.
   */
  constructor(endpointProgram = endpoint_exports.PROGRAM_ID, ulnProgram = uln_exports.PROGRAM_ID, simpleMsgLibProgram = simple_message_lib_exports.PROGRAM_ID) {
    this.endpointProgram = endpointProgram;
    this.ulnProgram = ulnProgram;
    this.simpleMsgLibProgram = simpleMsgLibProgram;
    this.accounts = /* @__PURE__ */ new Map();
    this.endpoint = new endpoint_exports.Endpoint(endpointProgram);
    this.uln = new uln_exports.Uln(ulnProgram);
    this.simpleMsgLib = new simple_message_lib_exports.SimpleMessageLib(simpleMsgLibProgram);
  }
  /**
   * Fetches multiple account information.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey[]} keys - The public keys of the accounts.
   * @param {Commitment | GetAccountInfoConfig} [commitment] - The commitment level or account info configuration.
   * @returns {Promise<(AccountInfo<Buffer> | null)[]>} A promise that resolves to an array of account information.
   */
  async getMultipleAccountsInfo(connection, keys, commitment) {
    const missingKeys = keys.filter((key) => !this.accounts.has(key.toBase58()));
    if (missingKeys.length > 0) {
      const infos = await connection.getMultipleAccountsInfo(missingKeys, commitment);
      infos.forEach((info, i) => {
        this.accounts.set(missingKeys[i].toBase58(), info);
      });
    }
    return keys.map((key) => this.accounts.get(key.toBase58()));
  }
  /**
   * Gets the account metadata for the quote instruction.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} sender - The sender public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {string} receiver - The receiver address.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  async getQuoteAccounts(connection, payer, sender, dstEid, receiver, commitmentOrConfig = "confirmed") {
    const [sendLibConfig] = this.endpoint.deriver.sendLibraryConfig(sender, dstEid);
    const [defaultSendLibConfig] = this.endpoint.deriver.defaultSendLibraryConfig(dstEid);
    const [simpleMsgLib] = this.simpleMsgLib.deriver.messageLib();
    const [uln] = this.uln.deriver.messageLib();
    const [ulnDefaultSendConfig] = this.uln.deriver.defaultSendConfig(dstEid);
    const [ulnSendConfig] = this.uln.deriver.sendConfig(dstEid, sender);
    const [
      sendLibConfigBuf,
      defaultSendLibConfigBuf,
      _simpleMsgLibBuf,
      ulnBuf,
      ulnDefaultSendConfigBuf,
      ulnSendConfigBuf
    ] = await this.getMultipleAccountsInfo(
      connection,
      [sendLibConfig, defaultSendLibConfig, simpleMsgLib, uln, ulnDefaultSendConfig, ulnSendConfig],
      commitmentOrConfig
    );
    invariant6(defaultSendLibConfigBuf && sendLibConfigBuf, "endpoint send library not initialized");
    const [sendLibConfigInfo] = endpoint_exports.accounts.SendLibraryConfig.fromAccountInfo(sendLibConfigBuf, 0);
    const [defaultSendLibConfigInfo] = endpoint_exports.accounts.SendLibraryConfig.fromAccountInfo(
      defaultSendLibConfigBuf,
      0
    );
    const msgLib = sendLibConfigInfo.messageLib.toString() === DefaultMessageLib.toString() ? defaultSendLibConfigInfo.messageLib : sendLibConfigInfo.messageLib;
    let msgLibProgram = this.ulnProgram;
    if (msgLib.toBase58() === this.simpleMsgLib.deriver.messageLib()[0].toBase58()) {
      msgLibProgram = this.simpleMsgLibProgram;
      return this.getEndpointAccounts(msgLibProgram, msgLib, sender, dstEid, receiver, 0 /* Quote */).concat(
        this.getSimpleMsgLibAccounts(payer, 0 /* Quote */)
      );
    }
    if (msgLib.toBase58() !== this.uln.deriver.messageLib()[0].toBase58()) {
      throw new Error("Invalid message library");
    }
    return this.getEndpointAccounts(msgLibProgram, msgLib, sender, dstEid, receiver, 0 /* Quote */).concat(
      await this.getUlnAccounts(
        connection,
        payer,
        {
          accountId: uln,
          accountInfo: ulnBuf
        },
        {
          accountId: ulnDefaultSendConfig,
          accountInfo: ulnDefaultSendConfigBuf
        },
        {
          accountId: ulnSendConfig,
          accountInfo: ulnSendConfigBuf
        },
        0 /* Quote */,
        commitmentOrConfig
      )
    );
  }
  /**
   * Gets the account metadata for the send instruction.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} sender - The sender public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {string} receiver - The receiver address.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  async getSendAccounts(connection, payer, sender, dstEid, receiver, commitmentOrConfig = "confirmed") {
    const [sendLibConfig] = this.endpoint.deriver.sendLibraryConfig(sender, dstEid);
    const [defaultSendLibConfig] = this.endpoint.deriver.defaultSendLibraryConfig(dstEid);
    const [simpleMsgLib] = this.simpleMsgLib.deriver.messageLib();
    const [uln] = this.uln.deriver.messageLib();
    const [ulnDefaultSendConfig] = this.uln.deriver.defaultSendConfig(dstEid);
    const [ulnSendConfig] = this.uln.deriver.sendConfig(dstEid, sender);
    const [
      sendLibConfigBuf,
      defaultSendLibConfigBuf,
      _simpleMsgLibBuf,
      ulnBuf,
      ulnDefaultSendConfigBuf,
      ulnSendConfigBuf
    ] = await this.getMultipleAccountsInfo(
      connection,
      [sendLibConfig, defaultSendLibConfig, simpleMsgLib, uln, ulnDefaultSendConfig, ulnSendConfig],
      commitmentOrConfig
    );
    invariant6(defaultSendLibConfigBuf && sendLibConfigBuf, "endpoint send library not initialized");
    const [sendLibConfigInfo] = endpoint_exports.accounts.SendLibraryConfig.fromAccountInfo(sendLibConfigBuf, 0);
    const [defaultSendLibConfigInfo] = endpoint_exports.accounts.SendLibraryConfig.fromAccountInfo(
      defaultSendLibConfigBuf,
      0
    );
    const msgLib = sendLibConfigInfo.messageLib.toString() === DefaultMessageLib.toString() ? defaultSendLibConfigInfo.messageLib : sendLibConfigInfo.messageLib;
    let msgLibProgram = this.ulnProgram;
    if (msgLib.toBase58() === this.simpleMsgLib.deriver.messageLib()[0].toBase58()) {
      msgLibProgram = this.simpleMsgLibProgram;
      return this.getEndpointAccounts(msgLibProgram, msgLib, sender, dstEid, receiver, 1 /* Send */).concat(
        this.getSimpleMsgLibAccounts(payer, 1 /* Send */)
      );
    }
    if (msgLib.toBase58() !== this.uln.deriver.messageLib()[0].toBase58()) {
      throw new Error("Invalid message library");
    }
    return this.getEndpointAccounts(msgLibProgram, msgLib, sender, dstEid, receiver, 1 /* Send */).concat(
      await this.getUlnAccounts(
        connection,
        payer,
        {
          accountId: uln,
          accountInfo: ulnBuf
        },
        {
          accountId: ulnDefaultSendConfig,
          accountInfo: ulnDefaultSendConfigBuf
        },
        {
          accountId: ulnSendConfig,
          accountInfo: ulnSendConfigBuf
        },
        1 /* Send */,
        commitmentOrConfig
      )
    );
  }
  /**
   * Gets the account metadata for the endpoint.
   *
   * @param {PublicKey} msgLibProgram - The message library program public key.
   * @param {PublicKey} msgLib - The message library public key.
   * @param {PublicKey} sender - The sender public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {string} receiver - The receiver address.
   * @param {QuoteOrSend} quoteOrSend - The quote or send action.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getEndpointAccounts(msgLibProgram, msgLib, sender, dstEid, receiver, quoteOrSend) {
    const [sendLibraryConfig] = this.endpoint.deriver.sendLibraryConfig(sender, dstEid);
    const [defaultSendLibraryConfig] = this.endpoint.deriver.defaultSendLibraryConfig(dstEid);
    const [sendLibraryInfo] = this.endpoint.deriver.messageLibraryInfo(msgLib);
    const [nonce] = this.endpoint.deriver.nonce(sender, dstEid, addressToBytes32(receiver));
    let accounts;
    if (quoteOrSend === 0 /* Quote */) {
      accounts = endpoint_exports.instructions.createQuoteInstructionAccounts(
        {
          sendLibraryProgram: msgLibProgram,
          sendLibraryConfig,
          defaultSendLibraryConfig,
          sendLibraryInfo,
          endpoint: this.endpoint.deriver.setting()[0],
          nonce
        },
        this.endpointProgram
      );
    } else {
      accounts = endpoint_exports.instructions.createSendInstructionAccounts(
        {
          sender,
          //signer
          sendLibraryProgram: msgLibProgram,
          sendLibraryConfig,
          defaultSendLibraryConfig,
          sendLibraryInfo,
          endpoint: this.endpoint.deriver.setting()[0],
          nonce,
          program: this.endpointProgram,
          eventAuthority: this.endpoint.eventAuthorityPDA
        },
        this.endpoint.program
      );
    }
    accounts.forEach((item) => {
      item.isSigner = false;
    });
    return [
      {
        pubkey: this.endpoint.program,
        isSigner: false,
        isWritable: false
      }
    ].concat(accounts);
  }
  /**
   * Gets the account metadata for the simple message library.
   *
   * @param {PublicKey} payer - The payer public key.
   * @param {QuoteOrSend} quoteOrSend - The quote or send action.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getSimpleMsgLibAccounts(payer, quoteOrSend) {
    const [msgLib] = this.simpleMsgLib.deriver.messageLib();
    let accounts;
    if (quoteOrSend === 0 /* Quote */) {
      accounts = simple_message_lib_exports.instructions.createQuoteInstructionAccounts(
        {
          endpoint: PublicKey.default,
          // useless
          messageLib: msgLib
        },
        this.simpleMsgLibProgram
      );
    } else {
      accounts = simple_message_lib_exports.instructions.createSendInstructionAccounts(
        {
          endpoint: PublicKey.default,
          // useless
          messageLib: msgLib,
          payer
        },
        this.simpleMsgLibProgram
      );
    }
    accounts.forEach((key) => {
      if (!payer.equals(key.pubkey)) {
        key.isSigner = false;
      }
    });
    return accounts.slice(1);
  }
  /**
   * Gets the account metadata for the ULN.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {KeyedAccountInfo} ulnInfo - The ULN account information.
   * @param {KeyedAccountInfo} ulnDefaultSendConfigInfo - The ULN default send configuration account information.
   * @param {KeyedAccountInfo} ulnSendConfigInfo - The ULN send configuration account information.
   * @param {QuoteOrSend} quoteOrSend - The quote or send action.
   * @param {Commitment | GetAccountInfoConfig} [commitment] - The commitment level or account info configuration.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  async getUlnAccounts(connection, payer, ulnInfo, ulnDefaultSendConfigInfo, ulnSendConfigInfo, quoteOrSend, commitment) {
    invariant6(ulnInfo.accountInfo && ulnDefaultSendConfigInfo.accountInfo, "uln send library not initialized");
    const [ulnState] = uln_exports.accounts.UlnSettings.fromAccountInfo(ulnInfo.accountInfo, 0);
    const [defaultSendConfigState] = uln_exports.accounts.SendConfig.fromAccountInfo(
      ulnDefaultSendConfigInfo.accountInfo,
      0
    );
    const sendConfigState = ulnSendConfigInfo.accountInfo ? uln_exports.accounts.SendConfig.fromAccountInfo(ulnSendConfigInfo.accountInfo)[0] : null;
    let {
      executor,
      uln: { requiredDvns, optionalDvns }
    } = defaultSendConfigState;
    if (sendConfigState?.executor && !sendConfigState.executor.executor.equals(PublicKey.default)) {
      ({ executor } = sendConfigState);
    }
    if (sendConfigState && sendConfigState.uln.requiredDvns.length > 0) {
      requiredDvns = sendConfigState.uln.requiredDvns.filter((p) => {
        return !p.equals(PublicKey.default);
      });
    }
    if (sendConfigState && sendConfigState.uln.optionalDvns.length > 0) {
      optionalDvns = sendConfigState.uln.optionalDvns.filter((p) => {
        return !p.equals(PublicKey.default);
      });
    }
    const dvnsKey = requiredDvns.concat(optionalDvns);
    const [executorBuf, ...dvnBuf] = await this.getMultipleAccountsInfo(
      connection,
      [executor.executor, ...dvnsKey],
      commitment
    );
    invariant6(executorBuf, `executor:${executor.executor.toBase58()} not initialized`);
    let executorAccounts, dvnAccounts;
    {
      const executor2 = {
        config: accounts_exports4.ExecutorConfig.fromAccountInfo(executorBuf)[0],
        owner: executorBuf.owner
      };
      const dvns = dvnBuf.map((dvn, i) => {
        invariant6(dvn, `dvn:${dvnsKey[i].toBase58()} not initialized`);
        return {
          config: accounts_exports3.DvnConfig.fromAccountInfo(dvn)[0],
          owner: dvn.owner
        };
      });
      const priceFeeds = new Array();
      [executor2.config, ...dvns.map((dvn) => dvn.config)].forEach((config2) => {
        priceFeeds.push(config2.priceFeed);
      });
      const priceFeedInfos = await this.getMultipleAccountsInfo(connection, priceFeeds, commitment);
      priceFeedInfos.forEach((info, i) => {
        invariant6(info, `priceFeed:${priceFeeds[i].toBase58()} not initialized`);
      });
      executorAccounts = new Executor(executor2.owner).getQuoteIXAccountMetaForCPI(
        executor2.config.priceFeed,
        // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
        (await this.getMultipleAccountsInfo(connection, [executor2.config.priceFeed], commitment))[0].owner,
        quoteOrSend === 1 /* Send */
      );
      dvnAccounts = (await Promise.all(
        dvns.map(async (p, i) => {
          return new DVN(p.owner).getQuoteIXAccountMetaForCPI(
            p.config.priceFeed,
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            (await this.getMultipleAccountsInfo(connection, [p.config.priceFeed], commitment))[0].owner,
            quoteOrSend === 1 /* Send */
          );
        })
      )).flat();
    }
    const treasury = ulnState.treasury?.nativeReceiver;
    let accounts;
    if (quoteOrSend === 0 /* Quote */) {
      accounts = uln_exports.instructions.createQuoteInstructionAccounts(
        {
          endpoint: PublicKey.default,
          // useless
          uln: this.uln.deriver.setting()[0],
          sendConfig: ulnSendConfigInfo.accountId,
          defaultSendConfig: ulnDefaultSendConfigInfo.accountId,
          anchorRemainingAccounts: executorAccounts.concat(dvnAccounts)
        },
        this.ulnProgram
      );
    } else {
      accounts = uln_exports.instructions.createSendInstructionAccounts(
        {
          endpoint: PublicKey.default,
          // useless
          uln: this.uln.deriver.setting()[0],
          sendConfig: ulnSendConfigInfo.accountId,
          defaultSendConfig: ulnDefaultSendConfigInfo.accountId,
          payer,
          treasury,
          eventAuthority: this.uln.eventAuthorityPDA,
          program: this.uln.program,
          anchorRemainingAccounts: executorAccounts.concat(dvnAccounts)
        },
        this.ulnProgram
      );
      if (treasury) {
        const treasuryIndex = accounts.findIndex((k) => k.pubkey.toBase58() == treasury.toBase58());
        accounts[treasuryIndex].isWritable = true;
      }
    }
    accounts.forEach((key) => {
      if (!payer.equals(key.pubkey)) {
        key.isSigner = false;
      }
    });
    return accounts.slice(1);
  }
};

// src/index.ts
var src_exports = {};
__export(src_exports, {
  BlockedMessageLibProgram: () => blocked_messagelib_exports2,
  DVNProgram: () => dvn_exports4,
  DvnPDA: () => DvnPDA,
  DvnProgram: () => dvn_exports3,
  EDDSA: () => EDDSA,
  ENFORCED_OPTIONS_SEED: () => ENFORCED_OPTIONS_SEED2,
  EndpointPDA: () => EndpointPDA,
  EndpointProgram: () => endpoint_exports2,
  EventPDA: () => EventPDA,
  ExecutorOptionType: () => ExecutorOptionType2,
  ExecutorPDA: () => ExecutorPDA,
  ExecutorProgram: () => executor_exports2,
  FAUCET_URL: () => FAUCET_URL2,
  MINT_SEED: () => MINT_SEED2,
  MSG_TYPE_OFFSET: () => MSG_TYPE_OFFSET2,
  MaxExecutorOptionTypeLength: () => MaxExecutorOptionTypeLength2,
  MessageLibPDA: () => MessageLibPDA,
  MessageType: () => MessageType2,
  OmniAppPDA: () => OmniAppPDA,
  PEER_SEED: () => PEER_SEED2,
  PriceFeedPDA: () => PriceFeedPDA,
  PriceFeedProgram: () => pricefeed_exports2,
  SendHelper: () => SendHelper2,
  SetConfigType: () => SetConfigType4,
  SimpleMessageLibProgram: () => simple_message_lib_exports2,
  UlnPDA: () => UlnPDA,
  UlnProgram: () => uln_exports2,
  addressLocator: () => addressLocator,
  buildLzComposeExecutionPlan: () => buildLzComposeExecutionPlan,
  buildLzReceiveExecutionPlan: () => buildLzReceiveExecutionPlan,
  buildMessageV0: () => buildMessageV02,
  buildVersionedTransaction: () => buildVersionedTransaction2,
  closeLookupTable: () => closeLookupTable2,
  createNonceAccountTX: () => createNonceAccountTX2,
  deactivateLookupTable: () => deactivateLookupTable2,
  extractComposeDeliveredEventByTxHash: () => extractComposeDeliveredEventByTxHash2,
  extractComposeSentEventByTxHash: () => extractComposeSentEventByTxHash2,
  extractEventFromTransactionSignature: () => extractEventFromTransactionSignature2,
  extractPacketSentEventByTxHash: () => extractPacketSentEventByTxHash,
  extractReceivedPacketEventByTxHash: () => extractReceivedPacketEventByTxHash2,
  extractVerifiedPacketEventByTxHash: () => extractVerifiedPacketEventByTxHash2,
  extractWorkerFeePaidEventByTxHash: () => extractWorkerFeePaidEventByTxHash2,
  generateAddressLookupTable: () => generateAddressLookupTable2,
  getAccountMetaRefSerializer: () => getAccountMetaRefSerializer,
  getAddressLocatorSerializer: () => getAddressLocatorSerializer,
  getBlockedMessageLibProgramId: () => getBlockedMessageLibProgramId2,
  getDVNProgramId: () => getDVNProgramId2,
  getEndpointProgramId: () => getEndpointProgramId2,
  getExecutorProgramId: () => getExecutorProgramId2,
  getInstructionForLzComposeSerializer: () => getInstructionForLzComposeSerializer,
  getInstructionForLzReceiveSerializer: () => getInstructionForLzReceiveSerializer,
  getLzComposeAccountsFromTypesV1: () => getLzComposeAccountsFromTypesV1,
  getLzComposeParamsSerializer: () => getLzComposeParamsSerializer2,
  getLzComposeTypesInfo: () => getLzComposeTypesInfo,
  getLzComposeTypesV2AccountsSerializer: () => getLzComposeTypesV2AccountsSerializer,
  getLzComposeTypesV2ResultSerializer: () => getLzComposeTypesV2ResultSerializer,
  getLzReceiveAccountSerializer: () => getLzReceiveAccountSerializer,
  getLzReceiveAccountsFromTypesV1: () => getLzReceiveAccountsFromTypesV1,
  getLzReceiveParamsSerializer: () => getLzReceiveParamsSerializer2,
  getLzReceiveTypesInfo: () => getLzReceiveTypesInfo,
  getLzReceiveTypesV2AccountsSerializer: () => getLzReceiveTypesV2AccountsSerializer,
  getLzReceiveTypesV2ResultSerializer: () => getLzReceiveTypesV2ResultSerializer,
  getNextNonceInstructionDataSerializer: () => getNextNonceInstructionDataSerializer,
  getPricefeedProgramId: () => getPricefeedProgramId2,
  getProgramKeypair: () => getProgramKeypair2,
  getSimpleMessageLibProgramId: () => getSimpleMessageLibProgramId2,
  getULNProgramId: () => getULNProgramId2,
  idlTypes: () => idlTypes2,
  instructionDiscriminator: () => instructionDiscriminator2,
  isAccountInitialized: () => isAccountInitialized2,
  isAddressLocator: () => isAddressLocator,
  lzCompose: () => lzCompose2,
  lzReceive: () => lzReceive2,
  messageLibs: () => messageLibs2,
  nextNonce: () => nextNonce,
  simulateWeb3JsTransaction: () => simulateWeb3JsTransaction,
  toWeb3Connection: () => toWeb3Connection,
  txWithAddressLookupTable: () => txWithAddressLookupTable2,
  txWithNonce: () => txWithNonce2
});
var PEER_SEED2 = "Peer";
var MINT_SEED2 = "Mint";
var ENFORCED_OPTIONS_SEED2 = "EnforcedOptions";
var EDDSA = createWeb3JsEddsa();
var _EndpointPDA = class _EndpointPDA {
  constructor(program) {
    this.program = program;
  }
  setting() {
    return EDDSA.findPda(this.program, [Buffer.from(_EndpointPDA.ENDPOINT_SEED, "utf8")]);
  }
  defaultSendLibraryConfig(dstEndpointId) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_EndpointPDA.SEND_LIBRARY_CONFIG_SEED, "utf8"),
      u32$1({ endian: Endian.Big }).serialize(dstEndpointId)
      // new BN(dstEndpointId).toArrayLike(Buffer, 'be', 4),
    ]);
  }
  sendLibraryConfig(sender, dstEndpointId) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_EndpointPDA.SEND_LIBRARY_CONFIG_SEED, "utf8"),
      publicKeyBytes(sender),
      u32$1({ endian: Endian.Big }).serialize(dstEndpointId)
      // new BN(dstEndpointId).toArrayLike(Buffer, 'be', 4),
    ]);
  }
  /**
   * @param messageLibrary PDA(derive by message lib program)
   */
  messageLibraryInfo(messageLibrary) {
    return EDDSA.findPda(this.program, [
      Buffer.from(MessageLibPDA.MESSAGE_LIB_SEED, "utf8"),
      publicKeyBytes(messageLibrary)
    ]);
  }
  defaultReceiveLibraryConfig(srcEndpointId) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_EndpointPDA.RECEIVE_LIBRARY_CONFIG_SEED, "utf8"),
      u32$1({ endian: Endian.Big }).serialize(srcEndpointId)
      // new BN(srcEndpointId).toArrayLike(Buffer, 'be', 4),
    ]);
  }
  receiveLibraryConfig(receiver, srcEndpointId) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_EndpointPDA.RECEIVE_LIBRARY_CONFIG_SEED, "utf8"),
      publicKeyBytes(receiver),
      u32$1({ endian: Endian.Big }).serialize(srcEndpointId)
      // new BN(srcEndpointId).toArrayLike(Buffer, 'be', 4),
    ]);
  }
  receiveLibraryTimeout(_dstEndpointId) {
    return [this.program, 0];
  }
  defaultMessageLib() {
    return this.messageLibraryInfo(defaultPublicKey());
  }
  blockMessageLib(blockMsgLib) {
    return EDDSA.findPda(this.program, [
      Buffer.from(MessageLibPDA.MESSAGE_LIB_SEED, "utf8"),
      publicKeyBytes(blockMsgLib)
    ]);
  }
  /**
   * @param oapp
   * @param remoteChainId
   * @param remoteOapp
   */
  nonce(oapp, remoteChainId, remoteOapp) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_EndpointPDA.NONCE_SEED, "utf8"),
      publicKeyBytes(oapp),
      u32$1({ endian: Endian.Big }).serialize(remoteChainId),
      // new BN(remoteChainId).toArrayLike(Buffer, 'be', 4),
      remoteOapp
    ]);
  }
  pendingNonce(oapp, remoteChainId, remoteOapp) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_EndpointPDA.PENDING_NONCE_SEED, "utf8"),
      publicKeyBytes(oapp),
      u32$1({ endian: Endian.Big }).serialize(remoteChainId),
      remoteOapp
    ]);
  }
  oappRegistry(oapp) {
    return EDDSA.findPda(this.program, [Buffer.from(_EndpointPDA.OAPP_SEED, "utf8"), publicKeyBytes(oapp)]);
  }
  /***
   * @param receiver
   * @param srcEid
   * @param sender
   * @param nonce
   * @param payloadHash
   * @srcChainId u32 to big endian Uint8Array
   * @sender [u8; 32] sender Address
   * @nonce u64 to big endian Uint8Array
   * @payloadHash [u8; 32]
   */
  payloadHash(receiver, srcEid, sender, nonce) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_EndpointPDA.PAYLOAD_HASH_SEED, "utf8"),
      publicKeyBytes(receiver),
      u32$1({ endian: Endian.Big }).serialize(srcEid),
      sender,
      u64$1({ endian: Endian.Big }).serialize(nonce)
    ]);
  }
  composedMessage(from, guid, index, to, messageHash) {
    return EDDSA.findPda(
      this.program,
      [
        Buffer.from(_EndpointPDA.COMPOSED_MESSAGE_HASH_SEED, "utf8"),
        publicKeyBytes(from),
        publicKeyBytes(to),
        guid,
        u16$1({ endian: Endian.Big }).serialize(index),
        messageHash
      ]
      // this.program
    );
  }
};
_EndpointPDA.ENDPOINT_SEED = "Endpoint";
_EndpointPDA.SEND_LIBRARY_CONFIG_SEED = "SendLibraryConfig";
_EndpointPDA.RECEIVE_LIBRARY_CONFIG_SEED = "ReceiveLibraryConfig";
_EndpointPDA.NONCE_SEED = "Nonce";
_EndpointPDA.PENDING_NONCE_SEED = "PendingNonce";
_EndpointPDA.OAPP_SEED = "OApp";
_EndpointPDA.PAYLOAD_HASH_SEED = "PayloadHash";
_EndpointPDA.COMPOSED_MESSAGE_HASH_SEED = "ComposedMessageHash";
var EndpointPDA = _EndpointPDA;
var _MessageLibPDA = class _MessageLibPDA {
  constructor(program) {
    this.program = program;
  }
  messageLib() {
    return EDDSA.findPda(this.program, [Buffer.from(_MessageLibPDA.MESSAGE_LIB_SEED, "utf8")]);
  }
  sendConfig(eid, oapp) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_MessageLibPDA.SEND_CONFIG_SEED, "utf8"),
      u32$1({ endian: Endian.Big }).serialize(eid),
      publicKeyBytes(oapp)
      // oapp.toBuffer(),
    ]);
  }
  receiveConfig(eid, oapp) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_MessageLibPDA.RECEIVE_CONFIG_SEED, "utf8"),
      u32$1({ endian: Endian.Big }).serialize(eid),
      publicKeyBytes(oapp)
    ]);
  }
};
_MessageLibPDA.SEND_CONFIG_SEED = "SendConfig";
_MessageLibPDA.RECEIVE_CONFIG_SEED = "ReceiveConfig";
_MessageLibPDA.MESSAGE_LIB_SEED = "MessageLib";
var MessageLibPDA = _MessageLibPDA;
var _UlnPDA = class _UlnPDA extends MessageLibPDA {
  setting() {
    return EDDSA.findPda(this.program, [Buffer.from(_UlnPDA.ULN_SEED, "utf8")]);
  }
  config(eid) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_UlnPDA.ULN_CONFIG_SEED, "utf8"),
      u32$1({ endian: Endian.Big }).serialize(eid)
    ]);
  }
  defaultSendConfig(eid) {
    return EDDSA.findPda(this.program, [
      Buffer.from(MessageLibPDA.SEND_CONFIG_SEED, "utf8"),
      u32$1({ endian: Endian.Big }).serialize(eid)
    ]);
  }
  defaultReceiveConfig(eid) {
    return EDDSA.findPda(this.program, [
      Buffer.from(MessageLibPDA.RECEIVE_CONFIG_SEED, "utf8"),
      u32$1({ endian: Endian.Big }).serialize(eid)
    ]);
  }
  options(eit) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_UlnPDA.OPTIONS_SEED, "utf8"),
      u32$1({ endian: Endian.Big }).serialize(eit)
    ]);
  }
  workerConfig(worker) {
    return EDDSA.findPda(this.program, [Buffer.from(_UlnPDA.WORKER_SEED, "utf8"), publicKeyBytes(worker)]);
  }
  confirmations(headerHash, payloadHash, dvn) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_UlnPDA.CONFIRMATIONS_SEED, "utf8"),
      headerHash,
      payloadHash,
      publicKeyBytes(dvn)
    ]);
  }
};
_UlnPDA.ULN_SEED = MessageLibPDA.MESSAGE_LIB_SEED;
_UlnPDA.ULN_CONFIG_SEED = "UlnConfig";
_UlnPDA.OPTIONS_SEED = "Options";
_UlnPDA.CONFIRMATIONS_SEED = "Confirmations";
_UlnPDA.WORKER_SEED = "Worker";
var UlnPDA = _UlnPDA;
var _OmniAppPDA = class _OmniAppPDA {
  constructor(programId) {
    this.programId = programId;
  }
  oapp() {
    throw new Error("Method not implemented.");
  }
  lzReceiveTypesAccounts(receiver) {
    return EDDSA.findPda(this.programId, [
      Buffer.from(_OmniAppPDA.LZ_RECEIVE_TYPES_SEED, "utf8"),
      publicKeyBytes(receiver ?? this.oapp()[0])
    ]);
  }
  lzComposeTypesAccounts(to) {
    return EDDSA.findPda(this.programId, [
      Buffer.from(_OmniAppPDA.LZ_COMPOSE_TYPES_SEED, "utf8"),
      publicKeyBytes(to ?? this.oapp()[0])
    ]);
  }
};
_OmniAppPDA.PEER_SEED = "Peer";
_OmniAppPDA.LZ_RECEIVE_TYPES_SEED = "LzReceiveTypes";
_OmniAppPDA.LZ_COMPOSE_TYPES_SEED = "LzComposeTypes";
var OmniAppPDA = _OmniAppPDA;
var _DvnPDA = class _DvnPDA {
  constructor(program) {
    this.program = program;
  }
  authority() {
    return EDDSA.findPda(this.program, [Buffer.from("dvn", "utf8")]);
  }
  config() {
    return EDDSA.findPda(this.program, [Buffer.from(_DvnPDA.DVN_CONFIG_SEED, "utf8")]);
  }
  executeHash(digestHash) {
    return EDDSA.findPda(this.program, [Buffer.from("ExecuteHash", "utf8"), digestHash]);
  }
};
_DvnPDA.DVN_CONFIG_SEED = "DvnConfig";
var DvnPDA = _DvnPDA;
var _EventPDA = class _EventPDA {
  constructor(program) {
    this.program = program;
  }
  eventAuthority() {
    return EDDSA.findPda(this.program, [Buffer.from(_EventPDA.EVENT_SEED, "utf8")]);
  }
};
_EventPDA.EVENT_SEED = "__event_authority";
var EventPDA = _EventPDA;
var _ExecutorPDA = class _ExecutorPDA {
  constructor(program) {
    this.program = program;
  }
  config() {
    return EDDSA.findPda(this.program, [Buffer.from(_ExecutorPDA.EXECUTOR_CONFIG_SEED, "utf8")]);
  }
  context(executor, version5) {
    return EDDSA.findPda(this.program, [
      Buffer.from(_ExecutorPDA.EXECUTION_CONTEXT_SEED, "utf8"),
      publicKeyBytes(executor),
      u8$1().serialize(version5)
    ]);
  }
};
_ExecutorPDA.EXECUTOR_CONFIG_SEED = "ExecutorConfig";
_ExecutorPDA.EXECUTION_CONTEXT_SEED = "ExecutionContext";
var ExecutorPDA = _ExecutorPDA;
var _PriceFeedPDA = class _PriceFeedPDA {
  constructor(program) {
    this.program = program;
  }
  priceFeed() {
    return EDDSA.findPda(this.program, [Buffer.from(_PriceFeedPDA.PRICE_FEED_SEED, "utf8")]);
  }
};
_PriceFeedPDA.PRICE_FEED_SEED = "PriceFeed";
var PriceFeedPDA = _PriceFeedPDA;

// src/endpoint.ts
var endpoint_exports2 = {};
__export(endpoint_exports2, {
  DefaultMessageLib: () => DefaultMessageLib2,
  ENDPOINT_PROGRAM_ID: () => ENDPOINT_PROGRAM_ID,
  Endpoint: () => Endpoint2,
  EventEmitDiscriminator: () => EventEmitDiscriminator2,
  accounts: () => accounts_exports7,
  errors: () => errors_exports5,
  events: () => events_exports5,
  instructions: () => instructions_exports7,
  types: () => types_exports7
});

// src/generated/kinobi/endpoint/accounts/index.ts
var accounts_exports7 = {};
__export(accounts_exports7, {
  deserializeComposeMessageState: () => deserializeComposeMessageState,
  deserializeEndpointSettings: () => deserializeEndpointSettings,
  deserializeMessageLibInfo: () => deserializeMessageLibInfo,
  deserializeNonce: () => deserializeNonce,
  deserializeOAppRegistry: () => deserializeOAppRegistry,
  deserializePayloadHash: () => deserializePayloadHash,
  deserializePendingInboundNonce: () => deserializePendingInboundNonce,
  deserializeReceiveLibraryConfig: () => deserializeReceiveLibraryConfig,
  deserializeSendLibraryConfig: () => deserializeSendLibraryConfig,
  fetchAllComposeMessageState: () => fetchAllComposeMessageState,
  fetchAllEndpointSettings: () => fetchAllEndpointSettings,
  fetchAllMessageLibInfo: () => fetchAllMessageLibInfo,
  fetchAllNonce: () => fetchAllNonce,
  fetchAllOAppRegistry: () => fetchAllOAppRegistry,
  fetchAllPayloadHash: () => fetchAllPayloadHash,
  fetchAllPendingInboundNonce: () => fetchAllPendingInboundNonce,
  fetchAllReceiveLibraryConfig: () => fetchAllReceiveLibraryConfig,
  fetchAllSendLibraryConfig: () => fetchAllSendLibraryConfig,
  fetchComposeMessageState: () => fetchComposeMessageState,
  fetchEndpointSettings: () => fetchEndpointSettings,
  fetchMessageLibInfo: () => fetchMessageLibInfo,
  fetchNonce: () => fetchNonce,
  fetchOAppRegistry: () => fetchOAppRegistry,
  fetchPayloadHash: () => fetchPayloadHash,
  fetchPendingInboundNonce: () => fetchPendingInboundNonce,
  fetchReceiveLibraryConfig: () => fetchReceiveLibraryConfig,
  fetchSendLibraryConfig: () => fetchSendLibraryConfig,
  getComposeMessageStateAccountDataSerializer: () => getComposeMessageStateAccountDataSerializer,
  getComposeMessageStateGpaBuilder: () => getComposeMessageStateGpaBuilder,
  getComposeMessageStateSize: () => getComposeMessageStateSize,
  getEndpointSettingsAccountDataSerializer: () => getEndpointSettingsAccountDataSerializer,
  getEndpointSettingsGpaBuilder: () => getEndpointSettingsGpaBuilder,
  getMessageLibInfoAccountDataSerializer: () => getMessageLibInfoAccountDataSerializer,
  getMessageLibInfoGpaBuilder: () => getMessageLibInfoGpaBuilder,
  getMessageLibInfoSize: () => getMessageLibInfoSize,
  getNonceAccountDataSerializer: () => getNonceAccountDataSerializer,
  getNonceGpaBuilder: () => getNonceGpaBuilder,
  getNonceSize: () => getNonceSize,
  getOAppRegistryAccountDataSerializer: () => getOAppRegistryAccountDataSerializer,
  getOAppRegistryGpaBuilder: () => getOAppRegistryGpaBuilder,
  getOAppRegistrySize: () => getOAppRegistrySize,
  getPayloadHashAccountDataSerializer: () => getPayloadHashAccountDataSerializer,
  getPayloadHashGpaBuilder: () => getPayloadHashGpaBuilder,
  getPayloadHashSize: () => getPayloadHashSize,
  getPendingInboundNonceAccountDataSerializer: () => getPendingInboundNonceAccountDataSerializer,
  getPendingInboundNonceGpaBuilder: () => getPendingInboundNonceGpaBuilder,
  getReceiveLibraryConfigAccountDataSerializer: () => getReceiveLibraryConfigAccountDataSerializer,
  getReceiveLibraryConfigGpaBuilder: () => getReceiveLibraryConfigGpaBuilder,
  getSendLibraryConfigAccountDataSerializer: () => getSendLibraryConfigAccountDataSerializer,
  getSendLibraryConfigGpaBuilder: () => getSendLibraryConfigGpaBuilder,
  getSendLibraryConfigSize: () => getSendLibraryConfigSize,
  safeFetchAllComposeMessageState: () => safeFetchAllComposeMessageState,
  safeFetchAllEndpointSettings: () => safeFetchAllEndpointSettings,
  safeFetchAllMessageLibInfo: () => safeFetchAllMessageLibInfo,
  safeFetchAllNonce: () => safeFetchAllNonce,
  safeFetchAllOAppRegistry: () => safeFetchAllOAppRegistry,
  safeFetchAllPayloadHash: () => safeFetchAllPayloadHash,
  safeFetchAllPendingInboundNonce: () => safeFetchAllPendingInboundNonce,
  safeFetchAllReceiveLibraryConfig: () => safeFetchAllReceiveLibraryConfig,
  safeFetchAllSendLibraryConfig: () => safeFetchAllSendLibraryConfig,
  safeFetchComposeMessageState: () => safeFetchComposeMessageState,
  safeFetchEndpointSettings: () => safeFetchEndpointSettings,
  safeFetchMessageLibInfo: () => safeFetchMessageLibInfo,
  safeFetchNonce: () => safeFetchNonce,
  safeFetchOAppRegistry: () => safeFetchOAppRegistry,
  safeFetchPayloadHash: () => safeFetchPayloadHash,
  safeFetchPendingInboundNonce: () => safeFetchPendingInboundNonce,
  safeFetchReceiveLibraryConfig: () => safeFetchReceiveLibraryConfig,
  safeFetchSendLibraryConfig: () => safeFetchSendLibraryConfig
});
function getComposeMessageStateAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["received", bool$1()],
        ["bump", u8$1()]
      ],
      { description: "ComposeMessageStateAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([55, 107, 79, 254, 243, 22, 172, 240])
    })
  );
}
function deserializeComposeMessageState(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getComposeMessageStateAccountDataSerializer()
  );
}
async function fetchComposeMessageState(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "ComposeMessageState");
  return deserializeComposeMessageState(maybeAccount);
}
async function safeFetchComposeMessageState(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeComposeMessageState(maybeAccount) : null;
}
async function fetchAllComposeMessageState(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "ComposeMessageState");
    return deserializeComposeMessageState(maybeAccount);
  });
}
async function safeFetchAllComposeMessageState(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeComposeMessageState(maybeAccount)
  );
}
function getComposeMessageStateGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    received: [8, bool$1()],
    bump: [9, u8$1()]
  }).deserializeUsing(
    (account) => deserializeComposeMessageState(account)
  ).whereField(
    "discriminator",
    new Uint8Array([55, 107, 79, 254, 243, 22, 172, 240])
  );
}
function getComposeMessageStateSize() {
  return 10;
}
function getEndpointSettingsAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["bump", u8$1()],
        ["admin", publicKey$1()],
        ["lzTokenMint", option(publicKey$1())]
      ],
      { description: "EndpointSettingsAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([221, 232, 73, 56, 10, 66, 72, 14])
    })
  );
}
function deserializeEndpointSettings(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getEndpointSettingsAccountDataSerializer()
  );
}
async function fetchEndpointSettings(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "EndpointSettings");
  return deserializeEndpointSettings(maybeAccount);
}
async function safeFetchEndpointSettings(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeEndpointSettings(maybeAccount) : null;
}
async function fetchAllEndpointSettings(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "EndpointSettings");
    return deserializeEndpointSettings(maybeAccount);
  });
}
async function safeFetchAllEndpointSettings(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeEndpointSettings(maybeAccount)
  );
}
function getEndpointSettingsGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    eid: [8, u32$1()],
    bump: [12, u8$1()],
    admin: [13, publicKey$1()],
    lzTokenMint: [45, option(publicKey$1())]
  }).deserializeUsing(
    (account) => deserializeEndpointSettings(account)
  ).whereField(
    "discriminator",
    new Uint8Array([221, 232, 73, 56, 10, 66, 72, 14])
  );
}

// src/generated/kinobi/endpoint/types/index.ts
var types_exports7 = {};
__export(types_exports7, {
  MessageLibType: () => MessageLibType3,
  getMessageLibTypeSerializer: () => getMessageLibTypeSerializer,
  getMessagingFeeSerializer: () => getMessagingFeeSerializer,
  getMessagingReceiptSerializer: () => getMessagingReceiptSerializer,
  getReceiveLibraryTimeoutSerializer: () => getReceiveLibraryTimeoutSerializer
});
var MessageLibType3 = /* @__PURE__ */ ((MessageLibType6) => {
  MessageLibType6[MessageLibType6["Send"] = 0] = "Send";
  MessageLibType6[MessageLibType6["Receive"] = 1] = "Receive";
  MessageLibType6[MessageLibType6["SendAndReceive"] = 2] = "SendAndReceive";
  return MessageLibType6;
})(MessageLibType3 || {});
function getMessageLibTypeSerializer() {
  return scalarEnum(MessageLibType3, {
    description: "MessageLibType"
  });
}
function getMessagingFeeSerializer() {
  return struct(
    [
      ["nativeFee", u64$1()],
      ["lzTokenFee", u64$1()]
    ],
    { description: "MessagingFee" }
  );
}
function getMessagingReceiptSerializer() {
  return struct(
    [
      ["guid", bytes$1({ size: 32 })],
      ["nonce", u64$1()],
      ["fee", getMessagingFeeSerializer()]
    ],
    { description: "MessagingReceipt" }
  );
}
function getReceiveLibraryTimeoutSerializer() {
  return struct(
    [
      ["messageLib", publicKey$1()],
      ["expiry", u64$1()]
    ],
    { description: "ReceiveLibraryTimeout" }
  );
}

// src/generated/kinobi/endpoint/accounts/messageLibInfo.ts
function getMessageLibInfoAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["messageLibType", getMessageLibTypeSerializer()],
        ["bump", u8$1()],
        ["messageLibBump", u8$1()]
      ],
      { description: "MessageLibInfoAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([103, 102, 218, 28, 204, 135, 71, 14])
    })
  );
}
function deserializeMessageLibInfo(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getMessageLibInfoAccountDataSerializer()
  );
}
async function fetchMessageLibInfo(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "MessageLibInfo");
  return deserializeMessageLibInfo(maybeAccount);
}
async function safeFetchMessageLibInfo(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeMessageLibInfo(maybeAccount) : null;
}
async function fetchAllMessageLibInfo(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "MessageLibInfo");
    return deserializeMessageLibInfo(maybeAccount);
  });
}
async function safeFetchAllMessageLibInfo(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeMessageLibInfo(maybeAccount)
  );
}
function getMessageLibInfoGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    messageLibType: [8, getMessageLibTypeSerializer()],
    bump: [9, u8$1()],
    messageLibBump: [10, u8$1()]
  }).deserializeUsing(
    (account) => deserializeMessageLibInfo(account)
  ).whereField(
    "discriminator",
    new Uint8Array([103, 102, 218, 28, 204, 135, 71, 14])
  );
}
function getMessageLibInfoSize() {
  return 11;
}
function getNonceAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["bump", u8$1()],
        ["outboundNonce", u64$1()],
        ["inboundNonce", u64$1()]
      ],
      { description: "NonceAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([143, 197, 147, 95, 106, 165, 50, 43])
    })
  );
}
function deserializeNonce(rawAccount) {
  return deserializeAccount(rawAccount, getNonceAccountDataSerializer());
}
async function fetchNonce(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "Nonce");
  return deserializeNonce(maybeAccount);
}
async function safeFetchNonce(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeNonce(maybeAccount) : null;
}
async function fetchAllNonce(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "Nonce");
    return deserializeNonce(maybeAccount);
  });
}
async function safeFetchAllNonce(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map((maybeAccount) => deserializeNonce(maybeAccount));
}
function getNonceGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    bump: [8, u8$1()],
    outboundNonce: [9, u64$1()],
    inboundNonce: [17, u64$1()]
  }).deserializeUsing((account) => deserializeNonce(account)).whereField(
    "discriminator",
    new Uint8Array([143, 197, 147, 95, 106, 165, 50, 43])
  );
}
function getNonceSize() {
  return 25;
}
function getOAppRegistryAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["delegate", publicKey$1()],
        ["bump", u8$1()]
      ],
      { description: "OAppRegistryAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([6, 152, 199, 30, 217, 50, 69, 149])
    })
  );
}
function deserializeOAppRegistry(rawAccount) {
  return deserializeAccount(rawAccount, getOAppRegistryAccountDataSerializer());
}
async function fetchOAppRegistry(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "OAppRegistry");
  return deserializeOAppRegistry(maybeAccount);
}
async function safeFetchOAppRegistry(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeOAppRegistry(maybeAccount) : null;
}
async function fetchAllOAppRegistry(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "OAppRegistry");
    return deserializeOAppRegistry(maybeAccount);
  });
}
async function safeFetchAllOAppRegistry(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map((maybeAccount) => deserializeOAppRegistry(maybeAccount));
}
function getOAppRegistryGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    delegate: [8, publicKey$1()],
    bump: [40, u8$1()]
  }).deserializeUsing(
    (account) => deserializeOAppRegistry(account)
  ).whereField(
    "discriminator",
    new Uint8Array([6, 152, 199, 30, 217, 50, 69, 149])
  );
}
function getOAppRegistrySize() {
  return 41;
}
function getPayloadHashAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["hash", bytes$1({ size: 32 })],
        ["bump", u8$1()]
      ],
      { description: "PayloadHashAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([96, 28, 106, 145, 103, 32, 186, 70])
    })
  );
}
function deserializePayloadHash(rawAccount) {
  return deserializeAccount(rawAccount, getPayloadHashAccountDataSerializer());
}
async function fetchPayloadHash(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "PayloadHash");
  return deserializePayloadHash(maybeAccount);
}
async function safeFetchPayloadHash(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializePayloadHash(maybeAccount) : null;
}
async function fetchAllPayloadHash(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "PayloadHash");
    return deserializePayloadHash(maybeAccount);
  });
}
async function safeFetchAllPayloadHash(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map((maybeAccount) => deserializePayloadHash(maybeAccount));
}
function getPayloadHashGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    hash: [8, bytes$1({ size: 32 })],
    bump: [40, u8$1()]
  }).deserializeUsing((account) => deserializePayloadHash(account)).whereField(
    "discriminator",
    new Uint8Array([96, 28, 106, 145, 103, 32, 186, 70])
  );
}
function getPayloadHashSize() {
  return 41;
}
function getPendingInboundNonceAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["nonces", array$1(u64$1())],
        ["bump", u8$1()]
      ],
      { description: "PendingInboundNonceAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([170, 176, 95, 240, 120, 231, 241, 218])
    })
  );
}
function deserializePendingInboundNonce(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getPendingInboundNonceAccountDataSerializer()
  );
}
async function fetchPendingInboundNonce(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "PendingInboundNonce");
  return deserializePendingInboundNonce(maybeAccount);
}
async function safeFetchPendingInboundNonce(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializePendingInboundNonce(maybeAccount) : null;
}
async function fetchAllPendingInboundNonce(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "PendingInboundNonce");
    return deserializePendingInboundNonce(maybeAccount);
  });
}
async function safeFetchAllPendingInboundNonce(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializePendingInboundNonce(maybeAccount)
  );
}
function getPendingInboundNonceGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    nonces: [8, array$1(u64$1())],
    bump: [null, u8$1()]
  }).deserializeUsing(
    (account) => deserializePendingInboundNonce(account)
  ).whereField(
    "discriminator",
    new Uint8Array([170, 176, 95, 240, 120, 231, 241, 218])
  );
}
function getReceiveLibraryConfigAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["messageLib", publicKey$1()],
        ["timeout", option(getReceiveLibraryTimeoutSerializer())],
        ["bump", u8$1()]
      ],
      { description: "ReceiveLibraryConfigAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([142, 226, 251, 138, 1, 206, 91, 193])
    })
  );
}
function deserializeReceiveLibraryConfig(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getReceiveLibraryConfigAccountDataSerializer()
  );
}
async function fetchReceiveLibraryConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "ReceiveLibraryConfig");
  return deserializeReceiveLibraryConfig(maybeAccount);
}
async function safeFetchReceiveLibraryConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeReceiveLibraryConfig(maybeAccount) : null;
}
async function fetchAllReceiveLibraryConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "ReceiveLibraryConfig");
    return deserializeReceiveLibraryConfig(maybeAccount);
  });
}
async function safeFetchAllReceiveLibraryConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeReceiveLibraryConfig(maybeAccount)
  );
}
function getReceiveLibraryConfigGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    messageLib: [8, publicKey$1()],
    timeout: [40, option(getReceiveLibraryTimeoutSerializer())],
    bump: [null, u8$1()]
  }).deserializeUsing(
    (account) => deserializeReceiveLibraryConfig(account)
  ).whereField(
    "discriminator",
    new Uint8Array([142, 226, 251, 138, 1, 206, 91, 193])
  );
}
function getSendLibraryConfigAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["messageLib", publicKey$1()],
        ["bump", u8$1()]
      ],
      { description: "SendLibraryConfigAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([61, 238, 31, 72, 251, 117, 66, 176])
    })
  );
}
function deserializeSendLibraryConfig(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getSendLibraryConfigAccountDataSerializer()
  );
}
async function fetchSendLibraryConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "SendLibraryConfig");
  return deserializeSendLibraryConfig(maybeAccount);
}
async function safeFetchSendLibraryConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeSendLibraryConfig(maybeAccount) : null;
}
async function fetchAllSendLibraryConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "SendLibraryConfig");
    return deserializeSendLibraryConfig(maybeAccount);
  });
}
async function safeFetchAllSendLibraryConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeSendLibraryConfig(maybeAccount)
  );
}
function getSendLibraryConfigGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    messageLib: [8, publicKey$1()],
    bump: [40, u8$1()]
  }).deserializeUsing(
    (account) => deserializeSendLibraryConfig(account)
  ).whereField(
    "discriminator",
    new Uint8Array([61, 238, 31, 72, 251, 117, 66, 176])
  );
}
function getSendLibraryConfigSize() {
  return 41;
}

// src/generated/kinobi/endpoint/errors/index.ts
var errors_exports5 = {};
__export(errors_exports5, {
  AccountNotFoundError: () => AccountNotFoundError2,
  ComposeNotFoundError: () => ComposeNotFoundError2,
  InvalidAmountError: () => InvalidAmountError5,
  InvalidExpiryError: () => InvalidExpiryError2,
  InvalidMessageLibError: () => InvalidMessageLibError2,
  InvalidNonceError: () => InvalidNonceError2,
  InvalidPayloadHashError: () => InvalidPayloadHashError2,
  InvalidReceiveLibraryError: () => InvalidReceiveLibraryError2,
  InvalidSendLibraryError: () => InvalidSendLibraryError2,
  LzTokenUnavailableError: () => LzTokenUnavailableError4,
  OnlyNonDefaultLibError: () => OnlyNonDefaultLibError2,
  OnlyReceiveLibError: () => OnlyReceiveLibError2,
  OnlySendLibError: () => OnlySendLibError2,
  PayloadHashNotFoundError: () => PayloadHashNotFoundError2,
  ReadOnlyAccountError: () => ReadOnlyAccountError2,
  SameValueError: () => SameValueError2,
  UnauthorizedError: () => UnauthorizedError3,
  WritableAccountNotAllowedError: () => WritableAccountNotAllowedError2,
  getEndpointErrorFromCode: () => getEndpointErrorFromCode,
  getEndpointErrorFromName: () => getEndpointErrorFromName
});
var codeToErrorMap = /* @__PURE__ */ new Map();
var nameToErrorMap = /* @__PURE__ */ new Map();
var InvalidSendLibraryError2 = class extends ProgramError {
  // 6000
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidSendLibrary";
    this.code = 6e3;
  }
};
codeToErrorMap.set(6e3, InvalidSendLibraryError2);
nameToErrorMap.set("InvalidSendLibrary", InvalidSendLibraryError2);
var InvalidReceiveLibraryError2 = class extends ProgramError {
  // 6001
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidReceiveLibrary";
    this.code = 6001;
  }
};
codeToErrorMap.set(6001, InvalidReceiveLibraryError2);
nameToErrorMap.set("InvalidReceiveLibrary", InvalidReceiveLibraryError2);
var SameValueError2 = class extends ProgramError {
  // 6002
  constructor(program, cause) {
    super("", program, cause);
    this.name = "SameValue";
    this.code = 6002;
  }
};
codeToErrorMap.set(6002, SameValueError2);
nameToErrorMap.set("SameValue", SameValueError2);
var AccountNotFoundError2 = class extends ProgramError {
  // 6003
  constructor(program, cause) {
    super("", program, cause);
    this.name = "AccountNotFound";
    this.code = 6003;
  }
};
codeToErrorMap.set(6003, AccountNotFoundError2);
nameToErrorMap.set("AccountNotFound", AccountNotFoundError2);
var OnlySendLibError2 = class extends ProgramError {
  // 6004
  constructor(program, cause) {
    super("", program, cause);
    this.name = "OnlySendLib";
    this.code = 6004;
  }
};
codeToErrorMap.set(6004, OnlySendLibError2);
nameToErrorMap.set("OnlySendLib", OnlySendLibError2);
var OnlyReceiveLibError2 = class extends ProgramError {
  // 6005
  constructor(program, cause) {
    super("", program, cause);
    this.name = "OnlyReceiveLib";
    this.code = 6005;
  }
};
codeToErrorMap.set(6005, OnlyReceiveLibError2);
nameToErrorMap.set("OnlyReceiveLib", OnlyReceiveLibError2);
var InvalidExpiryError2 = class extends ProgramError {
  // 6006
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidExpiry";
    this.code = 6006;
  }
};
codeToErrorMap.set(6006, InvalidExpiryError2);
nameToErrorMap.set("InvalidExpiry", InvalidExpiryError2);
var OnlyNonDefaultLibError2 = class extends ProgramError {
  // 6007
  constructor(program, cause) {
    super("", program, cause);
    this.name = "OnlyNonDefaultLib";
    this.code = 6007;
  }
};
codeToErrorMap.set(6007, OnlyNonDefaultLibError2);
nameToErrorMap.set("OnlyNonDefaultLib", OnlyNonDefaultLibError2);
var InvalidAmountError5 = class extends ProgramError {
  // 6008
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidAmount";
    this.code = 6008;
  }
};
codeToErrorMap.set(6008, InvalidAmountError5);
nameToErrorMap.set("InvalidAmount", InvalidAmountError5);
var InvalidNonceError2 = class extends ProgramError {
  // 6009
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidNonce";
    this.code = 6009;
  }
};
codeToErrorMap.set(6009, InvalidNonceError2);
nameToErrorMap.set("InvalidNonce", InvalidNonceError2);
var UnauthorizedError3 = class extends ProgramError {
  // 6010
  constructor(program, cause) {
    super("", program, cause);
    this.name = "Unauthorized";
    this.code = 6010;
  }
};
codeToErrorMap.set(6010, UnauthorizedError3);
nameToErrorMap.set("Unauthorized", UnauthorizedError3);
var PayloadHashNotFoundError2 = class extends ProgramError {
  // 6011
  constructor(program, cause) {
    super("", program, cause);
    this.name = "PayloadHashNotFound";
    this.code = 6011;
  }
};
codeToErrorMap.set(6011, PayloadHashNotFoundError2);
nameToErrorMap.set("PayloadHashNotFound", PayloadHashNotFoundError2);
var ComposeNotFoundError2 = class extends ProgramError {
  // 6012
  constructor(program, cause) {
    super("", program, cause);
    this.name = "ComposeNotFound";
    this.code = 6012;
  }
};
codeToErrorMap.set(6012, ComposeNotFoundError2);
nameToErrorMap.set("ComposeNotFound", ComposeNotFoundError2);
var InvalidPayloadHashError2 = class extends ProgramError {
  // 6013
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidPayloadHash";
    this.code = 6013;
  }
};
codeToErrorMap.set(6013, InvalidPayloadHashError2);
nameToErrorMap.set("InvalidPayloadHash", InvalidPayloadHashError2);
var LzTokenUnavailableError4 = class extends ProgramError {
  // 6014
  constructor(program, cause) {
    super("", program, cause);
    this.name = "LzTokenUnavailable";
    this.code = 6014;
  }
};
codeToErrorMap.set(6014, LzTokenUnavailableError4);
nameToErrorMap.set("LzTokenUnavailable", LzTokenUnavailableError4);
var ReadOnlyAccountError2 = class extends ProgramError {
  // 6015
  constructor(program, cause) {
    super("", program, cause);
    this.name = "ReadOnlyAccount";
    this.code = 6015;
  }
};
codeToErrorMap.set(6015, ReadOnlyAccountError2);
nameToErrorMap.set("ReadOnlyAccount", ReadOnlyAccountError2);
var InvalidMessageLibError2 = class extends ProgramError {
  // 6016
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidMessageLib";
    this.code = 6016;
  }
};
codeToErrorMap.set(6016, InvalidMessageLibError2);
nameToErrorMap.set("InvalidMessageLib", InvalidMessageLibError2);
var WritableAccountNotAllowedError2 = class extends ProgramError {
  // 6017
  constructor(program, cause) {
    super("", program, cause);
    this.name = "WritableAccountNotAllowed";
    this.code = 6017;
  }
};
codeToErrorMap.set(6017, WritableAccountNotAllowedError2);
nameToErrorMap.set("WritableAccountNotAllowed", WritableAccountNotAllowedError2);
function getEndpointErrorFromCode(code, program, cause) {
  const constructor = codeToErrorMap.get(code);
  return constructor ? new constructor(program, cause) : null;
}
function getEndpointErrorFromName(name, program, cause) {
  const constructor = nameToErrorMap.get(name);
  return constructor ? new constructor(program, cause) : null;
}

// src/generated/kinobi/endpoint/instructions/index.ts
var instructions_exports7 = {};
__export(instructions_exports7, {
  burn: () => burn,
  clear: () => clear,
  clearCompose: () => clearCompose,
  getBurnInstructionDataSerializer: () => getBurnInstructionDataSerializer,
  getClearComposeInstructionDataSerializer: () => getClearComposeInstructionDataSerializer,
  getClearInstructionDataSerializer: () => getClearInstructionDataSerializer,
  getInitConfigInstructionDataSerializer: () => getInitConfigInstructionDataSerializer,
  getInitDefaultReceiveLibraryInstructionDataSerializer: () => getInitDefaultReceiveLibraryInstructionDataSerializer,
  getInitDefaultSendLibraryInstructionDataSerializer: () => getInitDefaultSendLibraryInstructionDataSerializer,
  getInitEndpointInstructionDataSerializer: () => getInitEndpointInstructionDataSerializer,
  getInitNonceInstructionDataSerializer: () => getInitNonceInstructionDataSerializer,
  getInitReceiveLibraryInstructionDataSerializer: () => getInitReceiveLibraryInstructionDataSerializer,
  getInitSendLibraryInstructionDataSerializer: () => getInitSendLibraryInstructionDataSerializer,
  getInitVerifyInstructionDataSerializer: () => getInitVerifyInstructionDataSerializer,
  getLzComposeAlertInstructionDataSerializer: () => getLzComposeAlertInstructionDataSerializer,
  getLzReceiveAlertInstructionDataSerializer: () => getLzReceiveAlertInstructionDataSerializer,
  getNilifyInstructionDataSerializer: () => getNilifyInstructionDataSerializer,
  getQuoteInstructionDataSerializer: () => getQuoteInstructionDataSerializer,
  getRegisterLibraryInstructionDataSerializer: () => getRegisterLibraryInstructionDataSerializer,
  getRegisterOappInstructionDataSerializer: () => getRegisterOappInstructionDataSerializer,
  getSendComposeInstructionDataSerializer: () => getSendComposeInstructionDataSerializer,
  getSendInstructionDataSerializer: () => getSendInstructionDataSerializer,
  getSetConfigInstructionDataSerializer: () => getSetConfigInstructionDataSerializer,
  getSetDefaultReceiveLibraryInstructionDataSerializer: () => getSetDefaultReceiveLibraryInstructionDataSerializer,
  getSetDefaultReceiveLibraryTimeoutInstructionDataSerializer: () => getSetDefaultReceiveLibraryTimeoutInstructionDataSerializer,
  getSetDefaultSendLibraryInstructionDataSerializer: () => getSetDefaultSendLibraryInstructionDataSerializer,
  getSetDelegateInstructionDataSerializer: () => getSetDelegateInstructionDataSerializer,
  getSetLzTokenInstructionDataSerializer: () => getSetLzTokenInstructionDataSerializer,
  getSetReceiveLibraryInstructionDataSerializer: () => getSetReceiveLibraryInstructionDataSerializer,
  getSetReceiveLibraryTimeoutInstructionDataSerializer: () => getSetReceiveLibraryTimeoutInstructionDataSerializer,
  getSetSendLibraryInstructionDataSerializer: () => getSetSendLibraryInstructionDataSerializer,
  getSkipInstructionDataSerializer: () => getSkipInstructionDataSerializer,
  getTransferAdminInstructionDataSerializer: () => getTransferAdminInstructionDataSerializer,
  getVerifyInstructionDataSerializer: () => getVerifyInstructionDataSerializer,
  getWithdrawRentInstructionDataSerializer: () => getWithdrawRentInstructionDataSerializer,
  initConfig: () => initConfig,
  initDefaultReceiveLibrary: () => initDefaultReceiveLibrary,
  initDefaultSendLibrary: () => initDefaultSendLibrary,
  initEndpoint: () => initEndpoint,
  initNonce: () => initNonce,
  initReceiveLibrary: () => initReceiveLibrary,
  initSendLibrary: () => initSendLibrary,
  initVerify: () => initVerify,
  lzComposeAlert: () => lzComposeAlert,
  lzReceiveAlert: () => lzReceiveAlert,
  nilify: () => nilify,
  quote: () => quote,
  registerLibrary: () => registerLibrary,
  registerOapp: () => registerOapp,
  send: () => send,
  sendCompose: () => sendCompose,
  setConfig: () => setConfig,
  setDefaultReceiveLibrary: () => setDefaultReceiveLibrary,
  setDefaultReceiveLibraryTimeout: () => setDefaultReceiveLibraryTimeout,
  setDefaultSendLibrary: () => setDefaultSendLibrary,
  setDelegate: () => setDelegate,
  setLzToken: () => setLzToken,
  setReceiveLibrary: () => setReceiveLibrary,
  setReceiveLibraryTimeout: () => setReceiveLibraryTimeout,
  setSendLibrary: () => setSendLibrary,
  skip: () => skip,
  transferAdmin: () => transferAdmin,
  verify: () => verify,
  withdrawRent: () => withdrawRent
});
function getAccountMetasAndSigners(accounts, optionalAccountStrategy, programId) {
  const keys = [];
  const signers = [];
  accounts.forEach((account) => {
    if (!account.value) {
      keys.push({ pubkey: programId, isSigner: false, isWritable: false });
      return;
    }
    if (isSigner(account.value)) {
      signers.push(account.value);
    }
    keys.push({
      pubkey: publicKey(account.value, false),
      isSigner: isSigner(account.value),
      isWritable: account.isWritable
    });
  });
  return [keys, signers];
}

// src/generated/kinobi/endpoint/instructions/burn.ts
function getBurnInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["receiver", publicKey$1()],
        ["srcEid", u32$1()],
        ["sender", bytes$1({ size: 32 })],
        ["nonce", u64$1()],
        ["payloadHash", bytes$1({ size: 32 })]
      ],
      { description: "BurnInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([116, 110, 29, 56, 107, 219, 42, 93])
    })
  );
}
function burn(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    signer: {
      index: 0,
      isWritable: false,
      value: accounts.signer ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: accounts.oappRegistry ?? null
    },
    nonce: {
      index: 2,
      isWritable: false,
      value: accounts.nonce ?? null
    },
    payloadHash: {
      index: 3,
      isWritable: true,
      value: accounts.payloadHash ?? null
    },
    endpoint: {
      index: 4,
      isWritable: true,
      value: accounts.endpoint ?? null
    },
    eventAuthority: {
      index: 5,
      isWritable: false,
      value: accounts.eventAuthority ?? null
    },
    program: {
      index: 6,
      isWritable: false,
      value: accounts.program ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getBurnInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getClearInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["receiver", publicKey$1()],
        ["srcEid", u32$1()],
        ["sender", bytes$1({ size: 32 })],
        ["nonce", u64$1()],
        ["guid", bytes$1({ size: 32 })],
        ["message", bytes$1({ size: u32$1() })]
      ],
      { description: "ClearInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([250, 39, 28, 213, 123, 163, 133, 5])
    })
  );
}
function clear(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    signer: {
      index: 0,
      isWritable: false,
      value: accounts.signer ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: accounts.oappRegistry ?? null
    },
    nonce: {
      index: 2,
      isWritable: false,
      value: accounts.nonce ?? null
    },
    payloadHash: {
      index: 3,
      isWritable: true,
      value: accounts.payloadHash ?? null
    },
    endpoint: {
      index: 4,
      isWritable: true,
      value: accounts.endpoint ?? null
    },
    eventAuthority: {
      index: 5,
      isWritable: false,
      value: accounts.eventAuthority ?? null
    },
    program: {
      index: 6,
      isWritable: false,
      value: accounts.program ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getClearInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getClearComposeInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["from", publicKey$1()],
        ["guid", bytes$1({ size: 32 })],
        ["index", u16$1()],
        ["message", bytes$1({ size: u32$1() })]
      ],
      { description: "ClearComposeInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([118, 1, 18, 142, 95, 175, 21, 125])
    })
  );
}
function clearCompose(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    to: { index: 0, isWritable: false, value: input.to ?? null },
    composeMessage: {
      index: 1,
      isWritable: true,
      value: input.composeMessage ?? null
    },
    eventAuthority: {
      index: 2,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 3,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getClearComposeInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitConfigInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["oapp", publicKey$1()],
        ["eid", u32$1()]
      ],
      { description: "InitConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([23, 235, 115, 232, 168, 96, 1, 231])
    })
  );
}
function initConfig(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    delegate: {
      index: 0,
      isWritable: false,
      value: input.delegate ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: input.oappRegistry ?? null
    },
    messageLibInfo: {
      index: 2,
      isWritable: false,
      value: input.messageLibInfo ?? null
    },
    messageLib: {
      index: 3,
      isWritable: false,
      value: input.messageLib ?? null
    },
    messageLibProgram: {
      index: 4,
      isWritable: false,
      value: input.messageLibProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitConfigInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitDefaultReceiveLibraryInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["newLib", publicKey$1()]
      ],
      { description: "InitDefaultReceiveLibraryInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([32, 202, 76, 22, 42, 249, 227, 109])
    })
  );
}
function initDefaultReceiveLibrary(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true,
      value: input.admin ?? null
    },
    endpoint: {
      index: 1,
      isWritable: false,
      value: input.endpoint ?? null
    },
    defaultReceiveLibraryConfig: {
      index: 2,
      isWritable: true,
      value: input.defaultReceiveLibraryConfig ?? null
    },
    messageLibInfo: {
      index: 3,
      isWritable: false,
      value: input.messageLibInfo ?? null
    },
    systemProgram: {
      index: 4,
      isWritable: false,
      value: input.systemProgram ?? null
    },
    eventAuthority: {
      index: 5,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 6,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitDefaultReceiveLibraryInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitDefaultSendLibraryInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["newLib", publicKey$1()]
      ],
      { description: "InitDefaultSendLibraryInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([120, 187, 15, 31, 174, 97, 138, 58])
    })
  );
}
function initDefaultSendLibrary(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true,
      value: input.admin ?? null
    },
    endpoint: {
      index: 1,
      isWritable: false,
      value: input.endpoint ?? null
    },
    defaultSendLibraryConfig: {
      index: 2,
      isWritable: true,
      value: input.defaultSendLibraryConfig ?? null
    },
    messageLibInfo: {
      index: 3,
      isWritable: false,
      value: input.messageLibInfo ?? null
    },
    systemProgram: {
      index: 4,
      isWritable: false,
      value: input.systemProgram ?? null
    },
    eventAuthority: {
      index: 5,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 6,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitDefaultSendLibraryInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitEndpointInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["admin", publicKey$1()]
      ],
      { description: "InitEndpointInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([178, 30, 29, 207, 120, 225, 246, 134])
    })
  );
}
function initEndpoint(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true,
      value: input.payer ?? null
    },
    endpoint: {
      index: 1,
      isWritable: true,
      value: input.endpoint ?? null
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitEndpointInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitNonceInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["localOapp", publicKey$1()],
        ["remoteEid", u32$1()],
        ["remoteOapp", bytes$1({ size: 32 })]
      ],
      { description: "InitNonceInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([204, 171, 16, 214, 182, 191, 27, 196])
    })
  );
}
function initNonce(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    delegate: {
      index: 0,
      isWritable: true,
      value: input.delegate ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: input.oappRegistry ?? null
    },
    nonce: {
      index: 2,
      isWritable: true,
      value: input.nonce ?? null
    },
    pendingInboundNonce: {
      index: 3,
      isWritable: true,
      value: input.pendingInboundNonce ?? null
    },
    systemProgram: {
      index: 4,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitNonceInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitReceiveLibraryInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["receiver", publicKey$1()],
        ["eid", u32$1()]
      ],
      { description: "InitReceiveLibraryInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([197, 114, 81, 100, 45, 233, 36, 230])
    })
  );
}
function initReceiveLibrary(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    delegate: {
      index: 0,
      isWritable: true,
      value: input.delegate ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: input.oappRegistry ?? null
    },
    receiveLibraryConfig: {
      index: 2,
      isWritable: true,
      value: input.receiveLibraryConfig ?? null
    },
    systemProgram: {
      index: 3,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitReceiveLibraryInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitSendLibraryInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["sender", publicKey$1()],
        ["eid", u32$1()]
      ],
      { description: "InitSendLibraryInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([156, 24, 235, 120, 73, 193, 144, 19])
    })
  );
}
function initSendLibrary(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    delegate: {
      index: 0,
      isWritable: true,
      value: input.delegate ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: input.oappRegistry ?? null
    },
    sendLibraryConfig: {
      index: 2,
      isWritable: true,
      value: input.sendLibraryConfig ?? null
    },
    systemProgram: {
      index: 3,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitSendLibraryInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitVerifyInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["srcEid", u32$1()],
        ["sender", bytes$1({ size: 32 })],
        ["receiver", publicKey$1()],
        ["nonce", u64$1()]
      ],
      { description: "InitVerifyInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([76, 246, 244, 124, 115, 17, 235, 91])
    })
  );
}
function initVerify(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true,
      value: accounts.payer ?? null
    },
    nonce: {
      index: 1,
      isWritable: false,
      value: accounts.nonce ?? null
    },
    payloadHash: {
      index: 2,
      isWritable: true,
      value: accounts.payloadHash ?? null
    },
    systemProgram: {
      index: 3,
      isWritable: false,
      value: accounts.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...args };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitVerifyInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getLzComposeAlertInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["from", publicKey$1()],
        ["to", publicKey$1()],
        ["guid", bytes$1({ size: 32 })],
        ["index", u16$1()],
        ["computeUnits", u64$1()],
        ["value", u64$1()],
        ["message", bytes$1({ size: u32$1() })],
        ["extraData", bytes$1({ size: u32$1() })],
        ["reason", bytes$1({ size: u32$1() })]
      ],
      { description: "LzComposeAlertInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([27, 91, 198, 77, 66, 92, 122, 167])
    })
  );
}
function lzComposeAlert(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    executor: {
      index: 0,
      isWritable: false,
      value: input.executor ?? null
    },
    eventAuthority: {
      index: 1,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 2,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getLzComposeAlertInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getLzReceiveAlertInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["receiver", publicKey$1()],
        ["srcEid", u32$1()],
        ["sender", bytes$1({ size: 32 })],
        ["nonce", u64$1()],
        ["guid", bytes$1({ size: 32 })],
        ["computeUnits", u64$1()],
        ["value", u64$1()],
        ["message", bytes$1({ size: u32$1() })],
        ["extraData", bytes$1({ size: u32$1() })],
        ["reason", bytes$1({ size: u32$1() })]
      ],
      { description: "LzReceiveAlertInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([131, 141, 48, 222, 15, 235, 141, 160])
    })
  );
}
function lzReceiveAlert(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    executor: {
      index: 0,
      isWritable: false,
      value: input.executor ?? null
    },
    eventAuthority: {
      index: 1,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 2,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getLzReceiveAlertInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getNilifyInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["receiver", publicKey$1()],
        ["srcEid", u32$1()],
        ["sender", bytes$1({ size: 32 })],
        ["nonce", u64$1()],
        ["payloadHash", bytes$1({ size: 32 })]
      ],
      { description: "NilifyInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([143, 136, 129, 199, 36, 35, 160, 85])
    })
  );
}
function nilify(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    signer: {
      index: 0,
      isWritable: false,
      value: accounts.signer ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: accounts.oappRegistry ?? null
    },
    nonce: {
      index: 2,
      isWritable: true,
      value: accounts.nonce ?? null
    },
    pendingInboundNonce: {
      index: 3,
      isWritable: true,
      value: accounts.pendingInboundNonce ?? null
    },
    payloadHash: {
      index: 4,
      isWritable: true,
      value: accounts.payloadHash ?? null
    },
    eventAuthority: {
      index: 5,
      isWritable: false,
      value: accounts.eventAuthority ?? null
    },
    program: {
      index: 6,
      isWritable: false,
      value: accounts.program ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getNilifyInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getQuoteInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["sender", publicKey$1()],
        ["dstEid", u32$1()],
        ["receiver", bytes$1({ size: 32 })],
        ["message", bytes$1({ size: u32$1() })],
        ["options", bytes$1({ size: u32$1() })],
        ["payInLzToken", bool$1()]
      ],
      { description: "QuoteInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([149, 42, 109, 247, 134, 146, 213, 123])
    })
  );
}
function quote(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    sendLibraryProgram: {
      index: 0,
      isWritable: false,
      value: input.sendLibraryProgram ?? null
    },
    sendLibraryConfig: {
      index: 1,
      isWritable: false,
      value: input.sendLibraryConfig ?? null
    },
    defaultSendLibraryConfig: {
      index: 2,
      isWritable: false,
      value: input.defaultSendLibraryConfig ?? null
    },
    sendLibraryInfo: {
      index: 3,
      isWritable: false,
      value: input.sendLibraryInfo ?? null
    },
    endpoint: {
      index: 4,
      isWritable: false,
      value: input.endpoint ?? null
    },
    nonce: {
      index: 5,
      isWritable: false,
      value: input.nonce ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getQuoteInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getRegisterLibraryInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["libProgram", publicKey$1()],
        ["libType", getMessageLibTypeSerializer()]
      ],
      { description: "RegisterLibraryInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([23, 171, 28, 116, 111, 193, 238, 142])
    })
  );
}
function registerLibrary(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true,
      value: input.admin ?? null
    },
    endpoint: {
      index: 1,
      isWritable: false,
      value: input.endpoint ?? null
    },
    messageLibInfo: {
      index: 2,
      isWritable: true,
      value: input.messageLibInfo ?? null
    },
    systemProgram: {
      index: 3,
      isWritable: false,
      value: input.systemProgram ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getRegisterLibraryInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getRegisterOappInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["delegate", publicKey$1()]
      ],
      { description: "RegisterOappInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([129, 89, 71, 68, 11, 82, 210, 125])
    })
  );
}
function registerOapp(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true,
      value: input.payer ?? null
    },
    oapp: { index: 1, isWritable: false, value: input.oapp ?? null },
    oappRegistry: {
      index: 2,
      isWritable: true,
      value: input.oappRegistry ?? null
    },
    systemProgram: {
      index: 3,
      isWritable: false,
      value: input.systemProgram ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getRegisterOappInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSendInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["dstEid", u32$1()],
        ["receiver", bytes$1({ size: 32 })],
        ["message", bytes$1({ size: u32$1() })],
        ["options", bytes$1({ size: u32$1() })],
        ["nativeFee", u64$1()],
        ["lzTokenFee", u64$1()]
      ],
      { description: "SendInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([102, 251, 20, 187, 65, 75, 12, 69])
    })
  );
}
function send(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    sender: {
      index: 0,
      isWritable: false,
      value: input.sender ?? null
    },
    sendLibraryProgram: {
      index: 1,
      isWritable: false,
      value: input.sendLibraryProgram ?? null
    },
    sendLibraryConfig: {
      index: 2,
      isWritable: false,
      value: input.sendLibraryConfig ?? null
    },
    defaultSendLibraryConfig: {
      index: 3,
      isWritable: false,
      value: input.defaultSendLibraryConfig ?? null
    },
    sendLibraryInfo: {
      index: 4,
      isWritable: false,
      value: input.sendLibraryInfo ?? null
    },
    endpoint: {
      index: 5,
      isWritable: false,
      value: input.endpoint ?? null
    },
    nonce: {
      index: 6,
      isWritable: true,
      value: input.nonce ?? null
    },
    eventAuthority: {
      index: 7,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 8,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSendInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSendComposeInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["to", publicKey$1()],
        ["guid", bytes$1({ size: 32 })],
        ["index", u16$1()],
        ["message", bytes$1({ size: u32$1() })]
      ],
      { description: "SendComposeInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([75, 38, 228, 168, 43, 39, 238, 229])
    })
  );
}
function sendCompose(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    from: { index: 0, isWritable: false, value: input.from ?? null },
    payer: {
      index: 1,
      isWritable: true,
      value: input.payer ?? null
    },
    composeMessage: {
      index: 2,
      isWritable: true,
      value: input.composeMessage ?? null
    },
    systemProgram: {
      index: 3,
      isWritable: false,
      value: input.systemProgram ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSendComposeInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetConfigInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["oapp", publicKey$1()],
        ["eid", u32$1()],
        ["configType", u32$1()],
        ["config", bytes$1({ size: u32$1() })]
      ],
      { description: "SetConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([108, 158, 154, 175, 212, 98, 52, 66])
    })
  );
}
function setConfig(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    signer: {
      index: 0,
      isWritable: false,
      value: input.signer ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: input.oappRegistry ?? null
    },
    messageLibInfo: {
      index: 2,
      isWritable: false,
      value: input.messageLibInfo ?? null
    },
    messageLib: {
      index: 3,
      isWritable: false,
      value: input.messageLib ?? null
    },
    messageLibProgram: {
      index: 4,
      isWritable: false,
      value: input.messageLibProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetConfigInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetDefaultReceiveLibraryInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["newLib", publicKey$1()],
        ["gracePeriod", u64$1()]
      ],
      { description: "SetDefaultReceiveLibraryInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([14, 162, 167, 212, 13, 20, 151, 129])
    })
  );
}
function setDefaultReceiveLibrary(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    endpoint: {
      index: 1,
      isWritable: false,
      value: input.endpoint ?? null
    },
    defaultReceiveLibraryConfig: {
      index: 2,
      isWritable: true,
      value: input.defaultReceiveLibraryConfig ?? null
    },
    messageLibInfo: {
      index: 3,
      isWritable: false,
      value: input.messageLibInfo ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetDefaultReceiveLibraryInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetDefaultReceiveLibraryTimeoutInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["lib", publicKey$1()],
        ["expiry", u64$1()]
      ],
      { description: "SetDefaultReceiveLibraryTimeoutInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([118, 32, 94, 51, 25, 247, 16, 232])
    })
  );
}
function setDefaultReceiveLibraryTimeout(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    endpoint: {
      index: 1,
      isWritable: false,
      value: input.endpoint ?? null
    },
    defaultReceiveLibraryConfig: {
      index: 2,
      isWritable: true,
      value: input.defaultReceiveLibraryConfig ?? null
    },
    messageLibInfo: {
      index: 3,
      isWritable: false,
      value: input.messageLibInfo ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = {
    ...input
  };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetDefaultReceiveLibraryTimeoutInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetDefaultSendLibraryInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["newLib", publicKey$1()]
      ],
      { description: "SetDefaultSendLibraryInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([220, 215, 110, 127, 237, 178, 215, 170])
    })
  );
}
function setDefaultSendLibrary(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    endpoint: {
      index: 1,
      isWritable: false,
      value: input.endpoint ?? null
    },
    defaultSendLibraryConfig: {
      index: 2,
      isWritable: true,
      value: input.defaultSendLibraryConfig ?? null
    },
    messageLibInfo: {
      index: 3,
      isWritable: false,
      value: input.messageLibInfo ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetDefaultSendLibraryInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetDelegateInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["delegate", publicKey$1()]
      ],
      { description: "SetDelegateInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([242, 30, 46, 76, 108, 235, 128, 181])
    })
  );
}
function setDelegate(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    oapp: { index: 0, isWritable: false, value: input.oapp ?? null },
    oappRegistry: {
      index: 1,
      isWritable: true,
      value: input.oappRegistry ?? null
    },
    eventAuthority: {
      index: 2,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 3,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetDelegateInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetLzTokenInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["lzToken", option(publicKey$1())]
      ],
      { description: "SetLzTokenInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([22, 151, 112, 174, 213, 225, 223, 72])
    })
  );
}
function setLzToken(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    endpoint: {
      index: 1,
      isWritable: true,
      value: input.endpoint ?? null
    },
    eventAuthority: {
      index: 2,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 3,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetLzTokenInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetReceiveLibraryInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["receiver", publicKey$1()],
        ["eid", u32$1()],
        ["newLib", publicKey$1()],
        ["gracePeriod", u64$1()]
      ],
      { description: "SetReceiveLibraryInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([223, 172, 180, 105, 165, 161, 147, 228])
    })
  );
}
function setReceiveLibrary(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    signer: {
      index: 0,
      isWritable: false,
      value: input.signer ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: input.oappRegistry ?? null
    },
    receiveLibraryConfig: {
      index: 2,
      isWritable: true,
      value: input.receiveLibraryConfig ?? null
    },
    messageLibInfo: {
      index: 3,
      isWritable: false,
      value: input.messageLibInfo ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetReceiveLibraryInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetReceiveLibraryTimeoutInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["receiver", publicKey$1()],
        ["eid", u32$1()],
        ["lib", publicKey$1()],
        ["expiry", u64$1()]
      ],
      { description: "SetReceiveLibraryTimeoutInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([33, 159, 29, 53, 218, 156, 41, 192])
    })
  );
}
function setReceiveLibraryTimeout(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    signer: {
      index: 0,
      isWritable: false,
      value: input.signer ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: input.oappRegistry ?? null
    },
    receiveLibraryConfig: {
      index: 2,
      isWritable: true,
      value: input.receiveLibraryConfig ?? null
    },
    messageLibInfo: {
      index: 3,
      isWritable: false,
      value: input.messageLibInfo ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetReceiveLibraryTimeoutInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetSendLibraryInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["sender", publicKey$1()],
        ["eid", u32$1()],
        ["newLib", publicKey$1()]
      ],
      { description: "SetSendLibraryInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([251, 118, 78, 158, 134, 149, 129, 5])
    })
  );
}
function setSendLibrary(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    signer: {
      index: 0,
      isWritable: false,
      value: input.signer ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: input.oappRegistry ?? null
    },
    sendLibraryConfig: {
      index: 2,
      isWritable: true,
      value: input.sendLibraryConfig ?? null
    },
    messageLibInfo: {
      index: 3,
      isWritable: false,
      value: input.messageLibInfo ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetSendLibraryInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSkipInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["receiver", publicKey$1()],
        ["srcEid", u32$1()],
        ["sender", bytes$1({ size: 32 })],
        ["nonce", u64$1()]
      ],
      { description: "SkipInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([154, 63, 181, 53, 19, 26, 117, 45])
    })
  );
}
function skip(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    signer: {
      index: 0,
      isWritable: false,
      value: accounts.signer ?? null
    },
    oappRegistry: {
      index: 1,
      isWritable: false,
      value: accounts.oappRegistry ?? null
    },
    nonce: {
      index: 2,
      isWritable: true,
      value: accounts.nonce ?? null
    },
    pendingInboundNonce: {
      index: 3,
      isWritable: true,
      value: accounts.pendingInboundNonce ?? null
    },
    payloadHash: {
      index: 4,
      isWritable: true,
      value: accounts.payloadHash ?? null
    },
    endpoint: {
      index: 5,
      isWritable: true,
      value: accounts.endpoint ?? null
    },
    eventAuthority: {
      index: 6,
      isWritable: false,
      value: accounts.eventAuthority ?? null
    },
    program: {
      index: 7,
      isWritable: false,
      value: accounts.program ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSkipInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getTransferAdminInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["admin", publicKey$1()]
      ],
      { description: "TransferAdminInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([42, 242, 66, 106, 228, 10, 111, 156])
    })
  );
}
function transferAdmin(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: accounts.admin ?? null
    },
    endpoint: {
      index: 1,
      isWritable: true,
      value: accounts.endpoint ?? null
    },
    eventAuthority: {
      index: 2,
      isWritable: false,
      value: accounts.eventAuthority ?? null
    },
    program: {
      index: 3,
      isWritable: false,
      value: accounts.program ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getTransferAdminInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getVerifyInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["srcEid", u32$1()],
        ["sender", bytes$1({ size: 32 })],
        ["receiver", publicKey$1()],
        ["nonce", u64$1()],
        ["payloadHash", bytes$1({ size: 32 })]
      ],
      { description: "VerifyInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([133, 161, 141, 48, 120, 198, 88, 150])
    })
  );
}
function verify(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    receiveLibrary: {
      index: 0,
      isWritable: false,
      value: accounts.receiveLibrary ?? null
    },
    receiveLibraryConfig: {
      index: 1,
      isWritable: false,
      value: accounts.receiveLibraryConfig ?? null
    },
    defaultReceiveLibraryConfig: {
      index: 2,
      isWritable: false,
      value: accounts.defaultReceiveLibraryConfig ?? null
    },
    nonce: {
      index: 3,
      isWritable: true,
      value: accounts.nonce ?? null
    },
    pendingInboundNonce: {
      index: 4,
      isWritable: true,
      value: accounts.pendingInboundNonce ?? null
    },
    payloadHash: {
      index: 5,
      isWritable: true,
      value: accounts.payloadHash ?? null
    },
    eventAuthority: {
      index: 6,
      isWritable: false,
      value: accounts.eventAuthority ?? null
    },
    program: {
      index: 7,
      isWritable: false,
      value: accounts.program ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getVerifyInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getWithdrawRentInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["amount", u64$1()]
      ],
      { description: "WithdrawRentInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([226, 7, 41, 158, 173, 111, 192, 107])
    })
  );
}
function withdrawRent(context, input) {
  const programId = context.programs.getPublicKey(
    "endpoint",
    "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    endpoint: {
      index: 1,
      isWritable: true,
      value: input.endpoint ?? null
    },
    receiver: {
      index: 2,
      isWritable: true,
      value: input.receiver ?? null
    },
    eventAuthority: {
      index: 3,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 4,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getWithdrawRentInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}

// src/generated/kinobi/endpoint/programs/endpoint.ts
var ENDPOINT_PROGRAM_ID = "76y77prsiCMvXMjuoZ5VRrhG5qYBrUMYTE5WgHqgjEn6";

// src/generated/kinobi/endpoint/events/index.ts
var events_exports5 = {};
__export(events_exports5, {
  getAdminTransferredEventSerializer: () => getAdminTransferredEventSerializer,
  getComposeDeliveredEventSerializer: () => getComposeDeliveredEventSerializer,
  getComposeSentEventSerializer: () => getComposeSentEventSerializer,
  getDefaultReceiveLibrarySetEventSerializer: () => getDefaultReceiveLibrarySetEventSerializer,
  getDefaultReceiveLibraryTimeoutSetEventSerializer: () => getDefaultReceiveLibraryTimeoutSetEventSerializer,
  getDefaultSendLibrarySetEventSerializer: () => getDefaultSendLibrarySetEventSerializer,
  getDelegateSetEventSerializer: () => getDelegateSetEventSerializer,
  getInboundNonceSkippedEventSerializer: () => getInboundNonceSkippedEventSerializer,
  getLibraryRegisteredEventSerializer: () => getLibraryRegisteredEventSerializer,
  getLzComposeAlertEventSerializer: () => getLzComposeAlertEventSerializer,
  getLzReceiveAlertEventSerializer: () => getLzReceiveAlertEventSerializer,
  getLzTokenSetEventSerializer: () => getLzTokenSetEventSerializer,
  getOAppRegisteredEventSerializer: () => getOAppRegisteredEventSerializer,
  getPacketBurntEventSerializer: () => getPacketBurntEventSerializer,
  getPacketDeliveredEventSerializer: () => getPacketDeliveredEventSerializer,
  getPacketNilifiedEventSerializer: () => getPacketNilifiedEventSerializer,
  getPacketSentEventSerializer: () => getPacketSentEventSerializer,
  getPacketVerifiedEventSerializer: () => getPacketVerifiedEventSerializer,
  getReceiveLibrarySetEventSerializer: () => getReceiveLibrarySetEventSerializer,
  getReceiveLibraryTimeoutSetEventSerializer: () => getReceiveLibraryTimeoutSetEventSerializer,
  getRentWithdrawnEventSerializer: () => getRentWithdrawnEventSerializer,
  getSendLibrarySetEventSerializer: () => getSendLibrarySetEventSerializer
});
function getAdminTransferredEventSerializer() {
  return struct([["newAdmin", publicKey$1()]], {
    description: "AdminTransferredEvent"
  });
}
function getComposeDeliveredEventSerializer() {
  return struct(
    [
      ["from", publicKey$1()],
      ["to", publicKey$1()],
      ["guid", bytes$1({ size: 32 })],
      ["index", u16$1()]
    ],
    { description: "ComposeDeliveredEvent" }
  );
}
function getComposeSentEventSerializer() {
  return struct(
    [
      ["from", publicKey$1()],
      ["to", publicKey$1()],
      ["guid", bytes$1({ size: 32 })],
      ["index", u16$1()],
      ["message", bytes$1({ size: u32$1() })]
    ],
    { description: "ComposeSentEvent" }
  );
}
function getDefaultReceiveLibrarySetEventSerializer() {
  return struct(
    [
      ["eid", u32$1()],
      ["newLib", publicKey$1()]
    ],
    { description: "DefaultReceiveLibrarySetEvent" }
  );
}
function getDefaultReceiveLibraryTimeoutSetEventSerializer() {
  return struct(
    [
      ["eid", u32$1()],
      ["timeout", option(getReceiveLibraryTimeoutSerializer())]
    ],
    { description: "DefaultReceiveLibraryTimeoutSetEvent" }
  );
}
function getDefaultSendLibrarySetEventSerializer() {
  return struct(
    [
      ["eid", u32$1()],
      ["newLib", publicKey$1()]
    ],
    { description: "DefaultSendLibrarySetEvent" }
  );
}
function getDelegateSetEventSerializer() {
  return struct([["newDelegate", publicKey$1()]], {
    description: "DelegateSetEvent"
  });
}
function getInboundNonceSkippedEventSerializer() {
  return struct(
    [
      ["srcEid", u32$1()],
      ["sender", bytes$1({ size: 32 })],
      ["receiver", publicKey$1()],
      ["nonce", u64$1()]
    ],
    { description: "InboundNonceSkippedEvent" }
  );
}
function getLibraryRegisteredEventSerializer() {
  return struct(
    [
      ["newLib", publicKey$1()],
      ["newLibProgram", publicKey$1()]
    ],
    { description: "LibraryRegisteredEvent" }
  );
}
function getLzComposeAlertEventSerializer() {
  return struct(
    [
      ["executor", publicKey$1()],
      ["from", publicKey$1()],
      ["to", publicKey$1()],
      ["guid", bytes$1({ size: 32 })],
      ["index", u16$1()],
      ["computeUnits", u64$1()],
      ["value", u64$1()],
      ["message", bytes$1({ size: u32$1() })],
      ["extraData", bytes$1({ size: u32$1() })],
      ["reason", bytes$1({ size: u32$1() })]
    ],
    { description: "LzComposeAlertEvent" }
  );
}
function getLzReceiveAlertEventSerializer() {
  return struct(
    [
      ["receiver", publicKey$1()],
      ["executor", publicKey$1()],
      ["srcEid", u32$1()],
      ["sender", bytes$1({ size: 32 })],
      ["nonce", u64$1()],
      ["guid", bytes$1({ size: 32 })],
      ["computeUnits", u64$1()],
      ["value", u64$1()],
      ["message", bytes$1({ size: u32$1() })],
      ["extraData", bytes$1({ size: u32$1() })],
      ["reason", bytes$1({ size: u32$1() })]
    ],
    { description: "LzReceiveAlertEvent" }
  );
}
function getLzTokenSetEventSerializer() {
  return struct([["token", option(publicKey$1())]], {
    description: "LzTokenSetEvent"
  });
}
function getOAppRegisteredEventSerializer() {
  return struct(
    [
      ["oapp", publicKey$1()],
      ["delegate", publicKey$1()]
    ],
    { description: "OAppRegisteredEvent" }
  );
}
function getPacketBurntEventSerializer() {
  return struct(
    [
      ["srcEid", u32$1()],
      ["sender", bytes$1({ size: 32 })],
      ["receiver", publicKey$1()],
      ["nonce", u64$1()],
      ["payloadHash", bytes$1({ size: 32 })]
    ],
    { description: "PacketBurntEvent" }
  );
}
function getPacketDeliveredEventSerializer() {
  return struct(
    [
      ["srcEid", u32$1()],
      ["sender", bytes$1({ size: 32 })],
      ["receiver", publicKey$1()],
      ["nonce", u64$1()]
    ],
    { description: "PacketDeliveredEvent" }
  );
}
function getPacketNilifiedEventSerializer() {
  return struct(
    [
      ["srcEid", u32$1()],
      ["sender", bytes$1({ size: 32 })],
      ["receiver", publicKey$1()],
      ["nonce", u64$1()],
      ["payloadHash", bytes$1({ size: 32 })]
    ],
    { description: "PacketNilifiedEvent" }
  );
}
function getPacketSentEventSerializer() {
  return struct(
    [
      ["encodedPacket", bytes$1({ size: u32$1() })],
      ["options", bytes$1({ size: u32$1() })],
      ["sendLibrary", publicKey$1()]
    ],
    { description: "PacketSentEvent" }
  );
}
function getPacketVerifiedEventSerializer() {
  return struct(
    [
      ["srcEid", u32$1()],
      ["sender", bytes$1({ size: 32 })],
      ["receiver", publicKey$1()],
      ["nonce", u64$1()],
      ["payloadHash", bytes$1({ size: 32 })]
    ],
    { description: "PacketVerifiedEvent" }
  );
}
function getReceiveLibrarySetEventSerializer() {
  return struct(
    [
      ["receiver", publicKey$1()],
      ["eid", u32$1()],
      ["newLib", publicKey$1()]
    ],
    { description: "ReceiveLibrarySetEvent" }
  );
}
function getReceiveLibraryTimeoutSetEventSerializer() {
  return struct(
    [
      ["receiver", publicKey$1()],
      ["eid", u32$1()],
      ["timeout", option(getReceiveLibraryTimeoutSerializer())]
    ],
    { description: "ReceiveLibraryTimeoutSetEvent" }
  );
}
function getRentWithdrawnEventSerializer() {
  return struct(
    [
      ["receiver", publicKey$1()],
      ["amount", u64$1()]
    ],
    { description: "RentWithdrawnEvent" }
  );
}
function getSendLibrarySetEventSerializer() {
  return struct(
    [
      ["sender", publicKey$1()],
      ["eid", u32$1()],
      ["newLib", publicKey$1()]
    ],
    { description: "SendLibrarySetEvent" }
  );
}

// src/generated/kinobi/simple_messagelib/accounts/index.ts
var accounts_exports8 = {};
__export(accounts_exports8, {
  deserializeMessageLib: () => deserializeMessageLib,
  deserializeReceiveConfigStore: () => deserializeReceiveConfigStore,
  deserializeSendConfigStore: () => deserializeSendConfigStore,
  fetchAllMessageLib: () => fetchAllMessageLib,
  fetchAllReceiveConfigStore: () => fetchAllReceiveConfigStore,
  fetchAllSendConfigStore: () => fetchAllSendConfigStore,
  fetchMessageLib: () => fetchMessageLib,
  fetchReceiveConfigStore: () => fetchReceiveConfigStore,
  fetchSendConfigStore: () => fetchSendConfigStore,
  getMessageLibAccountDataSerializer: () => getMessageLibAccountDataSerializer,
  getMessageLibGpaBuilder: () => getMessageLibGpaBuilder,
  getMessageLibSize: () => getMessageLibSize,
  getReceiveConfigStoreAccountDataSerializer: () => getReceiveConfigStoreAccountDataSerializer,
  getReceiveConfigStoreGpaBuilder: () => getReceiveConfigStoreGpaBuilder,
  getSendConfigStoreAccountDataSerializer: () => getSendConfigStoreAccountDataSerializer,
  getSendConfigStoreGpaBuilder: () => getSendConfigStoreGpaBuilder,
  safeFetchAllMessageLib: () => safeFetchAllMessageLib,
  safeFetchAllReceiveConfigStore: () => safeFetchAllReceiveConfigStore,
  safeFetchAllSendConfigStore: () => safeFetchAllSendConfigStore,
  safeFetchMessageLib: () => safeFetchMessageLib,
  safeFetchReceiveConfigStore: () => safeFetchReceiveConfigStore,
  safeFetchSendConfigStore: () => safeFetchSendConfigStore
});
function getMessageLibAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["endpoint", publicKey$1()],
        ["endpointProgram", publicKey$1()],
        ["bump", u8$1()],
        ["admin", publicKey$1()],
        ["fee", u64$1()],
        ["lzTokenFee", u64$1()],
        ["wlCaller", publicKey$1()]
      ],
      { description: "MessageLibAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([141, 191, 244, 48, 52, 174, 199, 209])
    })
  );
}
function deserializeMessageLib(rawAccount) {
  return deserializeAccount(rawAccount, getMessageLibAccountDataSerializer());
}
async function fetchMessageLib(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "MessageLib");
  return deserializeMessageLib(maybeAccount);
}
async function safeFetchMessageLib(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeMessageLib(maybeAccount) : null;
}
async function fetchAllMessageLib(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "MessageLib");
    return deserializeMessageLib(maybeAccount);
  });
}
async function safeFetchAllMessageLib(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map((maybeAccount) => deserializeMessageLib(maybeAccount));
}
function getMessageLibGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    eid: [8, u32$1()],
    endpoint: [12, publicKey$1()],
    endpointProgram: [44, publicKey$1()],
    bump: [76, u8$1()],
    admin: [77, publicKey$1()],
    fee: [109, u64$1()],
    lzTokenFee: [117, u64$1()],
    wlCaller: [125, publicKey$1()]
  }).deserializeUsing((account) => deserializeMessageLib(account)).whereField(
    "discriminator",
    new Uint8Array([141, 191, 244, 48, 52, 174, 199, 209])
  );
}
function getMessageLibSize() {
  return 157;
}
function getReceiveConfigStoreAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["bump", u8$1()],
        ["data", bytes$1({ size: u32$1() })]
      ],
      { description: "ReceiveConfigStoreAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([70, 73, 30, 1, 248, 48, 93, 125])
    })
  );
}
function deserializeReceiveConfigStore(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getReceiveConfigStoreAccountDataSerializer()
  );
}
async function fetchReceiveConfigStore(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "ReceiveConfigStore");
  return deserializeReceiveConfigStore(maybeAccount);
}
async function safeFetchReceiveConfigStore(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeReceiveConfigStore(maybeAccount) : null;
}
async function fetchAllReceiveConfigStore(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "ReceiveConfigStore");
    return deserializeReceiveConfigStore(maybeAccount);
  });
}
async function safeFetchAllReceiveConfigStore(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeReceiveConfigStore(maybeAccount)
  );
}
function getReceiveConfigStoreGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    bump: [8, u8$1()],
    data: [9, bytes$1({ size: u32$1() })]
  }).deserializeUsing(
    (account) => deserializeReceiveConfigStore(account)
  ).whereField(
    "discriminator",
    new Uint8Array([70, 73, 30, 1, 248, 48, 93, 125])
  );
}
function getSendConfigStoreAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["bump", u8$1()],
        ["data", bytes$1({ size: u32$1() })]
      ],
      { description: "SendConfigStoreAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([246, 55, 104, 200, 90, 164, 50, 196])
    })
  );
}
function deserializeSendConfigStore(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getSendConfigStoreAccountDataSerializer()
  );
}
async function fetchSendConfigStore(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "SendConfigStore");
  return deserializeSendConfigStore(maybeAccount);
}
async function safeFetchSendConfigStore(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeSendConfigStore(maybeAccount) : null;
}
async function fetchAllSendConfigStore(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "SendConfigStore");
    return deserializeSendConfigStore(maybeAccount);
  });
}
async function safeFetchAllSendConfigStore(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeSendConfigStore(maybeAccount)
  );
}
function getSendConfigStoreGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    bump: [8, u8$1()],
    data: [9, bytes$1({ size: u32$1() })]
  }).deserializeUsing(
    (account) => deserializeSendConfigStore(account)
  ).whereField(
    "discriminator",
    new Uint8Array([246, 55, 104, 200, 90, 164, 50, 196])
  );
}

// src/generated/kinobi/simple_messagelib/errors/index.ts
var errors_exports6 = {};
__export(errors_exports6, {
  InsufficientFeeError: () => InsufficientFeeError3,
  InvalidAmountError: () => InvalidAmountError6,
  InvalidConfigTypeError: () => InvalidConfigTypeError3,
  InvalidLzTokenMintError: () => InvalidLzTokenMintError3,
  LzTokenUnavailableError: () => LzTokenUnavailableError5,
  OnlyRevertError: () => OnlyRevertError2,
  OnlyWhitelistedCallerError: () => OnlyWhitelistedCallerError2,
  SendReentrancyError: () => SendReentrancyError2,
  getSimpleMessagelibErrorFromCode: () => getSimpleMessagelibErrorFromCode,
  getSimpleMessagelibErrorFromName: () => getSimpleMessagelibErrorFromName
});
var codeToErrorMap2 = /* @__PURE__ */ new Map();
var nameToErrorMap2 = /* @__PURE__ */ new Map();
var OnlyWhitelistedCallerError2 = class extends ProgramError {
  // 6000
  constructor(program, cause) {
    super("", program, cause);
    this.name = "OnlyWhitelistedCaller";
    this.code = 6e3;
  }
};
codeToErrorMap2.set(6e3, OnlyWhitelistedCallerError2);
nameToErrorMap2.set("OnlyWhitelistedCaller", OnlyWhitelistedCallerError2);
var InsufficientFeeError3 = class extends ProgramError {
  // 6001
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InsufficientFee";
    this.code = 6001;
  }
};
codeToErrorMap2.set(6001, InsufficientFeeError3);
nameToErrorMap2.set("InsufficientFee", InsufficientFeeError3);
var InvalidAmountError6 = class extends ProgramError {
  // 6002
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidAmount";
    this.code = 6002;
  }
};
codeToErrorMap2.set(6002, InvalidAmountError6);
nameToErrorMap2.set("InvalidAmount", InvalidAmountError6);
var InvalidConfigTypeError3 = class extends ProgramError {
  // 6003
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidConfigType";
    this.code = 6003;
  }
};
codeToErrorMap2.set(6003, InvalidConfigTypeError3);
nameToErrorMap2.set("InvalidConfigType", InvalidConfigTypeError3);
var InvalidLzTokenMintError3 = class extends ProgramError {
  // 6004
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidLzTokenMint";
    this.code = 6004;
  }
};
codeToErrorMap2.set(6004, InvalidLzTokenMintError3);
nameToErrorMap2.set("InvalidLzTokenMint", InvalidLzTokenMintError3);
var LzTokenUnavailableError5 = class extends ProgramError {
  // 6005
  constructor(program, cause) {
    super("", program, cause);
    this.name = "LzTokenUnavailable";
    this.code = 6005;
  }
};
codeToErrorMap2.set(6005, LzTokenUnavailableError5);
nameToErrorMap2.set("LzTokenUnavailable", LzTokenUnavailableError5);
var SendReentrancyError2 = class extends ProgramError {
  // 6006
  constructor(program, cause) {
    super("", program, cause);
    this.name = "SendReentrancy";
    this.code = 6006;
  }
};
codeToErrorMap2.set(6006, SendReentrancyError2);
nameToErrorMap2.set("SendReentrancy", SendReentrancyError2);
var OnlyRevertError2 = class extends ProgramError {
  // 6007
  constructor(program, cause) {
    super("", program, cause);
    this.name = "OnlyRevert";
    this.code = 6007;
  }
};
codeToErrorMap2.set(6007, OnlyRevertError2);
nameToErrorMap2.set("OnlyRevert", OnlyRevertError2);
function getSimpleMessagelibErrorFromCode(code, program, cause) {
  const constructor = codeToErrorMap2.get(code);
  return constructor ? new constructor(program, cause) : null;
}
function getSimpleMessagelibErrorFromName(name, program, cause) {
  const constructor = nameToErrorMap2.get(name);
  return constructor ? new constructor(program, cause) : null;
}

// src/generated/kinobi/simple_messagelib/instructions/index.ts
var instructions_exports8 = {};
__export(instructions_exports8, {
  getInitConfigInstructionDataSerializer: () => getInitConfigInstructionDataSerializer2,
  getInitDefaultConfigInstructionDataSerializer: () => getInitDefaultConfigInstructionDataSerializer,
  getInitMessageLibInstructionDataSerializer: () => getInitMessageLibInstructionDataSerializer,
  getQuoteInstructionDataSerializer: () => getQuoteInstructionDataSerializer2,
  getRevertCallInstructionDataSerializer: () => getRevertCallInstructionDataSerializer,
  getSendInstructionDataSerializer: () => getSendInstructionDataSerializer2,
  getSendWithLzTokenInstructionDataSerializer: () => getSendWithLzTokenInstructionDataSerializer,
  getSetConfigInstructionDataSerializer: () => getSetConfigInstructionDataSerializer2,
  getSetDefaultConfigInstructionDataSerializer: () => getSetDefaultConfigInstructionDataSerializer,
  getSetFeeInstructionDataSerializer: () => getSetFeeInstructionDataSerializer,
  getSetWlCallerInstructionDataSerializer: () => getSetWlCallerInstructionDataSerializer,
  getTransferAdminInstructionDataSerializer: () => getTransferAdminInstructionDataSerializer2,
  getValidatePacketInstructionDataSerializer: () => getValidatePacketInstructionDataSerializer,
  getVersionInstructionDataSerializer: () => getVersionInstructionDataSerializer,
  getWithdrawFeesInstructionDataSerializer: () => getWithdrawFeesInstructionDataSerializer,
  initConfig: () => initConfig2,
  initDefaultConfig: () => initDefaultConfig,
  initMessageLib: () => initMessageLib,
  quote: () => quote2,
  revertCall: () => revertCall,
  send: () => send2,
  sendWithLzToken: () => sendWithLzToken,
  setConfig: () => setConfig2,
  setDefaultConfig: () => setDefaultConfig,
  setFee: () => setFee,
  setWlCaller: () => setWlCaller,
  transferAdmin: () => transferAdmin2,
  validatePacket: () => validatePacket,
  version: () => version,
  withdrawFees: () => withdrawFees
});
function getAccountMetasAndSigners2(accounts, optionalAccountStrategy, programId) {
  const keys = [];
  const signers = [];
  accounts.forEach((account) => {
    if (!account.value) {
      keys.push({ pubkey: programId, isSigner: false, isWritable: false });
      return;
    }
    if (isSigner(account.value)) {
      signers.push(account.value);
    }
    keys.push({
      pubkey: publicKey(account.value, false),
      isSigner: isSigner(account.value),
      isWritable: account.isWritable
    });
  });
  return [keys, signers];
}

// src/generated/kinobi/simple_messagelib/instructions/initConfig.ts
function getInitConfigInstructionDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["oapp", publicKey$1()],
        ["eid", u32$1()]
      ],
      { description: "InitConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([23, 235, 115, 232, 168, 96, 1, 231])
    })
  );
}
function initConfig2(context, input) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    endpoint: {
      index: 0,
      isWritable: false,
      value: input.endpoint ?? null
    },
    payer: {
      index: 1,
      isWritable: true,
      value: input.payer ?? null
    },
    messageLib: {
      index: 2,
      isWritable: false,
      value: input.messageLib ?? null
    },
    sendConfig: {
      index: 3,
      isWritable: true,
      value: input.sendConfig ?? null
    },
    receiveConfig: {
      index: 4,
      isWritable: true,
      value: input.receiveConfig ?? null
    },
    systemProgram: {
      index: 5,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitConfigInstructionDataSerializer2().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitDefaultConfigInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["sendConfig", option(bytes$1({ size: u32$1() }))],
        ["receiveConfig", option(bytes$1({ size: u32$1() }))]
      ],
      { description: "InitDefaultConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([98, 218, 197, 194, 173, 179, 112, 21])
    })
  );
}
function initDefaultConfig(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true,
      value: accounts.admin ?? null
    },
    messageLib: {
      index: 1,
      isWritable: false,
      value: accounts.messageLib ?? null
    },
    sendConfig: {
      index: 2,
      isWritable: true,
      value: accounts.sendConfig ?? null
    },
    receiveConfig: {
      index: 3,
      isWritable: true,
      value: accounts.receiveConfig ?? null
    },
    systemProgram: {
      index: 4,
      isWritable: false,
      value: accounts.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...args };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitDefaultConfigInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitMessageLibInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["endpoint", publicKey$1()],
        ["endpointProgram", publicKey$1()],
        ["admin", publicKey$1()],
        ["fee", u64$1()],
        ["lzTokenFee", u64$1()]
      ],
      { description: "InitMessageLibInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([184, 93, 59, 206, 98, 238, 225, 54])
    })
  );
}
function initMessageLib(context, input) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true,
      value: input.payer ?? null
    },
    messageLib: {
      index: 1,
      isWritable: true,
      value: input.messageLib ?? null
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitMessageLibInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}

// src/generated/kinobi/simple_messagelib/types/index.ts
var types_exports8 = {};
__export(types_exports8, {
  getMessagingFeeSerializer: () => getMessagingFeeSerializer2,
  getPacketSerializer: () => getPacketSerializer,
  getVersionSerializer: () => getVersionSerializer
});
function getMessagingFeeSerializer2() {
  return struct(
    [
      ["nativeFee", u64$1()],
      ["lzTokenFee", u64$1()]
    ],
    { description: "MessagingFee" }
  );
}
function getPacketSerializer() {
  return struct(
    [
      ["nonce", u64$1()],
      ["srcEid", u32$1()],
      ["sender", publicKey$1()],
      ["dstEid", u32$1()],
      ["receiver", bytes$1({ size: 32 })],
      ["guid", bytes$1({ size: 32 })],
      ["message", bytes$1({ size: u32$1() })]
    ],
    { description: "Packet" }
  );
}
function getVersionSerializer() {
  return struct(
    [
      ["major", u64$1()],
      ["minor", u8$1()],
      ["endpointVersion", u8$1()]
    ],
    { description: "Version" }
  );
}

// src/generated/kinobi/simple_messagelib/instructions/quote.ts
function getQuoteInstructionDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packet", getPacketSerializer()],
        ["options", bytes$1({ size: u32$1() })],
        ["payInLzToken", bool$1()]
      ],
      { description: "QuoteInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([149, 42, 109, 247, 134, 146, 213, 123])
    })
  );
}
function quote2(context, input) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    endpoint: {
      index: 0,
      isWritable: false,
      value: input.endpoint ?? null
    },
    messageLib: {
      index: 1,
      isWritable: false,
      value: input.messageLib ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getQuoteInstructionDataSerializer2().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getRevertCallInstructionDataSerializer() {
  return mapSerializer(
    struct([["discriminator", bytes$1({ size: 8 })]], {
      description: "RevertCallInstructionData"
    }),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([124, 243, 145, 116, 106, 203, 93, 114])
    })
  );
}
function revertCall(context) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {};
  const orderedAccounts = Object.values(
    resolvedAccounts
  );
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getRevertCallInstructionDataSerializer().serialize({});
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSendInstructionDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packet", getPacketSerializer()],
        ["options", bytes$1({ size: u32$1() })],
        ["nativeFee", u64$1()]
      ],
      { description: "SendInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([102, 251, 20, 187, 65, 75, 12, 69])
    })
  );
}
function send2(context, input) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    endpoint: {
      index: 0,
      isWritable: false,
      value: input.endpoint ?? null
    },
    messageLib: {
      index: 1,
      isWritable: true,
      value: input.messageLib ?? null
    },
    payer: {
      index: 2,
      isWritable: true,
      value: input.payer ?? null
    },
    systemProgram: {
      index: 3,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSendInstructionDataSerializer2().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSendWithLzTokenInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packet", getPacketSerializer()],
        ["options", bytes$1({ size: u32$1() })],
        ["nativeFee", u64$1()],
        ["lzTokenFee", u64$1()],
        ["lzTokenMint", publicKey$1()]
      ],
      { description: "SendWithLzTokenInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([165, 161, 84, 48, 129, 26, 193, 19])
    })
  );
}
function sendWithLzToken(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    endpoint: {
      index: 0,
      isWritable: false,
      value: accounts.endpoint ?? null
    },
    messageLib: {
      index: 1,
      isWritable: true,
      value: accounts.messageLib ?? null
    },
    messageLibLzToken: {
      index: 2,
      isWritable: true,
      value: accounts.messageLibLzToken ?? null
    },
    payer: {
      index: 3,
      isWritable: true,
      value: accounts.payer ?? null
    },
    lzTokenSource: {
      index: 4,
      isWritable: true,
      value: accounts.lzTokenSource ?? null
    },
    lzTokenMint: {
      index: 5,
      isWritable: false,
      value: accounts.lzTokenMint ?? null
    },
    tokenProgram: {
      index: 6,
      isWritable: false,
      value: accounts.tokenProgram ?? null
    },
    systemProgram: {
      index: 7,
      isWritable: false,
      value: accounts.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...args };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      "splToken",
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSendWithLzTokenInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetConfigInstructionDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["oapp", publicKey$1()],
        ["eid", u32$1()],
        ["configType", u32$1()],
        ["config", bytes$1({ size: u32$1() })]
      ],
      { description: "SetConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([108, 158, 154, 175, 212, 98, 52, 66])
    })
  );
}
function setConfig2(context, input) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    endpoint: {
      index: 0,
      isWritable: false,
      value: input.endpoint ?? null
    },
    messageLib: {
      index: 1,
      isWritable: false,
      value: input.messageLib ?? null
    },
    sendConfig: {
      index: 2,
      isWritable: true,
      value: input.sendConfig ?? null
    },
    receiveConfig: {
      index: 3,
      isWritable: true,
      value: input.receiveConfig ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetConfigInstructionDataSerializer2().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetDefaultConfigInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["sendConfig", option(bytes$1({ size: u32$1() }))],
        ["receiveConfig", option(bytes$1({ size: u32$1() }))]
      ],
      { description: "SetDefaultConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([111, 228, 17, 75, 5, 76, 213, 169])
    })
  );
}
function setDefaultConfig(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: accounts.admin ?? null
    },
    messageLib: {
      index: 1,
      isWritable: false,
      value: accounts.messageLib ?? null
    },
    sendConfig: {
      index: 2,
      isWritable: true,
      value: accounts.sendConfig ?? null
    },
    receiveConfig: {
      index: 3,
      isWritable: true,
      value: accounts.receiveConfig ?? null
    },
    systemProgram: {
      index: 4,
      isWritable: false,
      value: accounts.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...args };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetDefaultConfigInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetFeeInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["fee", u64$1()],
        ["lzTokenFee", u64$1()]
      ],
      { description: "SetFeeInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([18, 154, 24, 18, 237, 214, 19, 80])
    })
  );
}
function setFee(context, input) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    messageLib: {
      index: 1,
      isWritable: true,
      value: input.messageLib ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetFeeInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetWlCallerInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["newCaller", publicKey$1()]
      ],
      { description: "SetWlCallerInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([153, 41, 240, 37, 126, 107, 14, 253])
    })
  );
}
function setWlCaller(context, input) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    messageLib: {
      index: 1,
      isWritable: true,
      value: input.messageLib ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetWlCallerInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getTransferAdminInstructionDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["admin", publicKey$1()]
      ],
      { description: "TransferAdminInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([42, 242, 66, 106, 228, 10, 111, 156])
    })
  );
}
function transferAdmin2(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: accounts.admin ?? null
    },
    messageLib: {
      index: 1,
      isWritable: true,
      value: accounts.messageLib ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getTransferAdminInstructionDataSerializer2().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getValidatePacketInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packet", bytes$1({ size: u32$1() })]
      ],
      { description: "ValidatePacketInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([34, 146, 107, 76, 157, 98, 105, 211])
    })
  );
}
function validatePacket(context, input) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: false,
      value: input.payer ?? null
    },
    receiveLibrary: {
      index: 1,
      isWritable: false,
      value: input.receiveLibrary ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getValidatePacketInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getVersionInstructionDataSerializer() {
  return mapSerializer(
    struct([["discriminator", bytes$1({ size: 8 })]], {
      description: "VersionInstructionData"
    }),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([118, 65, 195, 198, 129, 216, 252, 192])
    })
  );
}
function version(context) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {};
  const orderedAccounts = Object.values(
    resolvedAccounts
  );
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getVersionInstructionDataSerializer().serialize({});
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getWithdrawFeesInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["amount", u64$1()]
      ],
      { description: "WithdrawFeesInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([198, 212, 171, 109, 144, 215, 174, 89])
    })
  );
}
function withdrawFees(context, input) {
  const programId = context.programs.getPublicKey(
    "simpleMessagelib",
    "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    messageLib: {
      index: 1,
      isWritable: true,
      value: input.messageLib ?? null
    },
    receiver: {
      index: 2,
      isWritable: true,
      value: input.receiver ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners2(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getWithdrawFeesInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}

// src/generated/kinobi/simple_messagelib/programs/simpleMessagelib.ts
var SIMPLE_MESSAGELIB_PROGRAM_ID = "6GsmxMTHAAiFKfemuM4zBjumTjNSX5CAiw4xSSXM2Toy";

// src/endpoint.ts
var EventEmitDiscriminator2 = "e445a52e51cb9a1d";
var DefaultMessageLib2 = defaultPublicKey();
var Endpoint2 = class _Endpoint {
  constructor(programId = ENDPOINT_PROGRAM_ID, rpc) {
    this.programId = programId;
    this.pda = new EndpointPDA(programId);
    if (rpc === void 0) {
      rpc = createNullRpc();
      rpc.getCluster = () => "custom";
    }
    this.programRepo = createDefaultProgramRepository({ rpc }, [
      {
        name: "endpoint",
        publicKey: programId,
        getErrorFromCode(code, cause) {
          return getEndpointErrorFromCode(code, this, cause);
        },
        getErrorFromName(name, cause) {
          return getEndpointErrorFromName(name, this, cause);
        },
        isOnCluster() {
          return true;
        }
      }
    ]);
    this.eventAuthority = new EventPDA(programId).eventAuthority()[0];
  }
  getProgram(clusterFilter = "custom") {
    return this.programRepo.get("endpoint", clusterFilter);
  }
  /**
   * Initializes the endpoint settings, including eid and admin, and registers the blocked message library.
   *
   * @param {number} endpointId - The endpoint ID.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} admin - The admin public key.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  initEndpoint(payer, params) {
    const txBuilder = initEndpoint(
      {
        programs: this.programRepo,
        payer
      },
      {
        payer,
        endpoint: this.pda.setting(),
        eid: params.eid,
        admin: params.admin
      }
    );
    return txBuilder.items[0];
  }
  /**
   * Registers a library.
   * call this function after endpoint initialized. Only admin can call this function.
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {types.MessageLibType} [libType=types.MessageLibType.SendAndReceive] - The library type.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  registerLibrary(admin, params) {
    const [msgLib] = new MessageLibPDA(params.messageLibProgram).messageLib();
    return registerLibrary(
      {
        programs: this.programRepo
      },
      {
        admin,
        endpoint: this.pda.setting(),
        messageLibInfo: this.pda.messageLibraryInfo(msgLib),
        eventAuthority: this.eventAuthority,
        program: this.programId,
        libProgram: params.messageLibProgram,
        libType: params.libType ?? 2 /* SendAndReceive */
      }
    ).items[0];
  }
  /**
   * Sets the default send library for a given remote endpoint.
   * This function configures the default message library for sending messages to a specified remote endpoint.
   * It requires administrative privileges to execute.
   *
   * @param {RpcInterface} rpc - The connection to the Solana cluster.
   * @param {Signer} admin - The admin public key.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<TransactionInstruction | null>} A promise that resolves to the transaction instruction or null if not needed.
   * This method initializes or updates the default send library configuration for a specified remote endpoint.
   * If the configuration does not exist, it initializes it. Otherwise, it updates the existing configuration.
   */
  async setDefaultSendLibrary(rpc, admin, params) {
    const [msgLib] = new MessageLibPDA(params.messageLibProgram).messageLib();
    const [defaultSendLibConfig] = this.pda.defaultSendLibraryConfig(params.remote);
    const [setting] = this.pda.setting();
    const [msgLibInfo] = this.pda.messageLibraryInfo(msgLib);
    const defaultSendLibConfigInfo = await rpc.getAccount(defaultSendLibConfig, {
      commitment: "confirmed"
    });
    let txBuilder;
    if (!defaultSendLibConfigInfo.exists) {
      txBuilder = initDefaultSendLibrary(
        { programs: this.programRepo },
        {
          admin,
          endpoint: setting,
          defaultSendLibraryConfig: defaultSendLibConfig,
          messageLibInfo: msgLibInfo,
          program: this.programId,
          eventAuthority: this.eventAuthority,
          // params
          eid: params.remote,
          newLib: msgLib
        }
      );
    } else {
      txBuilder = setDefaultSendLibrary(
        {
          programs: this.programRepo
        },
        {
          admin,
          endpoint: setting,
          defaultSendLibraryConfig: defaultSendLibConfig,
          messageLibInfo: msgLibInfo,
          program: this.programId,
          eventAuthority: this.eventAuthority,
          // params
          eid: params.remote,
          newLib: msgLib
        }
      );
    }
    return txBuilder.items[0];
  }
  /**
   * Sets the default receive library for a given endpoint.
   * This function configures the default message library for receiving messages at a specified endpoint.
   * It requires administrative privileges to execute.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} admin - The admin public key.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<TransactionInstruction | null>} A promise that resolves to the transaction instruction or null if not needed.
   * This method initializes or updates the default receive library configuration for a specified remote endpoint.
   * If the configuration does not exist, it initializes it. Otherwise, it updates the existing configuration.
   */
  async setDefaultReceiveLibrary(rpc, admin, params) {
    const [msgLib] = new MessageLibPDA(params.messageLibProgram).messageLib();
    const [defaultReceiveLibConfig] = this.pda.defaultReceiveLibraryConfig(params.remote);
    const [setting] = this.pda.setting();
    const [defaultMsgLibInfo] = this.pda.messageLibraryInfo(msgLib);
    const defaultReceiveLibConfigInfo = await rpc.getAccount(defaultReceiveLibConfig, { commitment: "confirmed" });
    let txBuilder;
    if (!defaultReceiveLibConfigInfo.exists) {
      txBuilder = initDefaultReceiveLibrary(
        { programs: this.programRepo },
        {
          admin,
          endpoint: setting,
          defaultReceiveLibraryConfig: defaultReceiveLibConfig,
          messageLibInfo: defaultMsgLibInfo,
          program: this.programId,
          eventAuthority: this.eventAuthority,
          // param
          eid: params.remote,
          newLib: msgLib
        }
      );
    } else {
      txBuilder = setDefaultReceiveLibrary(
        { programs: this.programRepo },
        {
          admin,
          endpoint: setting,
          defaultReceiveLibraryConfig: defaultReceiveLibConfig,
          messageLibInfo: defaultMsgLibInfo,
          program: this.programId,
          eventAuthority: this.eventAuthority,
          // param
          eid: params.remote,
          newLib: msgLib,
          gracePeriod: params.gracePeriod ?? 0
        }
      );
    }
    return txBuilder.items[0];
  }
  /**
   * Sets the default receive library timeout for a given message library program and remote endpoint.
   *
   * @param admin - The signer who has administrative privileges to set the timeout.
   * @param param - An object containing the following properties:
   *   @param param.msgLibProgram - The public key of the message library program.
   *   @param param.remote - The remote endpoint identifier.
   *   @param param.expiry - The expiry time for the timeout, can be a bigint or number.
   * @returns A WrappedInstruction object representing the instruction to set the default receive library timeout.
   */
  setDefaultReceiveLibraryTimeout(admin, params) {
    const [msgLib] = new MessageLibPDA(params.msgLibProgram).messageLib();
    return setDefaultReceiveLibraryTimeout(
      { programs: this.programRepo },
      {
        admin,
        endpoint: this.pda.setting(),
        program: this.programId,
        eventAuthority: this.eventAuthority,
        defaultReceiveLibraryConfig: this.pda.defaultReceiveLibraryConfig(params.remote),
        messageLibInfo: this.pda.messageLibraryInfo(msgLib),
        // param
        eid: params.remote,
        lib: msgLib,
        expiry: params.expiry
      }
    ).items[0];
  }
  setLzToken(admin, lzToken) {
    return setLzToken(
      { programs: this.programRepo },
      {
        admin,
        endpoint: this.pda.setting(),
        eventAuthority: this.eventAuthority,
        program: this.programId,
        lzToken: lzToken == null ? null : some(lzToken)
      }
    ).items[0];
  }
  withdrawRent(admin, params) {
    const { amount, receiver } = params;
    return withdrawRent(
      { programs: this.programRepo },
      {
        admin,
        endpoint: this.pda.setting(),
        eventAuthority: this.eventAuthority,
        program: this.programId,
        // param
        amount,
        receiver
      }
    ).items[0];
  }
  /**
   * Initializes the OApp configuration.
   *
   * @param accounts - The accounts object.
   * @param accounts.delegate - The delegate public key.
   * @param accounts.payer - The payer public key.
   * @param params - The parameters object.
   *  @param params.msgLibSDK - The message library SDK.
   *  @param params.oapp - The OApp ID public key.
   *  @param params.remote - The endpoint ID.
   * @returns A WrappedInstruction object representing the instruction to initialize the OApp configuration.
   */
  initOAppConfig(accounts, params) {
    const [msgLib] = new MessageLibPDA(params.msgLibSDK.programId).messageLib();
    const txBuilder = initConfig(
      { programs: this.programRepo },
      {
        delegate: accounts.delegate,
        oappRegistry: this.pda.oappRegistry(params.oapp),
        messageLib: msgLib,
        messageLibInfo: this.pda.messageLibraryInfo(msgLib),
        messageLibProgram: params.msgLibSDK.programId,
        // param
        eid: params.remote,
        oapp: params.oapp
      }
    );
    return txBuilder.addRemainingAccounts(
      params.msgLibSDK.getInitConfigIXAccountMetaForCPI(accounts.payer, params.oapp, params.remote)
    ).items[0];
  }
  /**
   * Initializes the OApp nonce.
   *
   * @param {PublicKey} delegate - The delegate public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {PublicKey} oappIDPDA - The OApp ID PDA.
   * @param {Uint8Array} remoteOappAddr - The remote OApp address.
   * @returns {TransactionInstruction} The transaction instruction.
   */
  initOAppNonce(delegate, params) {
    const txBuilder = initNonce(
      { programs: this.programRepo },
      {
        nonce: this.pda.nonce(params.localOApp, params.remote, params.remoteOApp),
        pendingInboundNonce: this.pda.pendingNonce(params.localOApp, params.remote, params.remoteOApp),
        oappRegistry: this.pda.oappRegistry(params.localOApp),
        delegate,
        // param
        localOapp: params.localOApp,
        remoteOapp: params.remoteOApp,
        remoteEid: params.remote
      }
    );
    return txBuilder.items[0];
  }
  initOAppSendLibrary(delegate, params) {
    return initSendLibrary(
      { programs: this.programRepo },
      {
        delegate,
        oappRegistry: this.pda.oappRegistry(params.sender),
        sendLibraryConfig: this.pda.sendLibraryConfig(params.sender, params.remote),
        // param
        sender: params.sender,
        eid: params.remote
      }
    ).items[0];
  }
  setOAppSendLibrary(delegate, params) {
    const { sender, remote, msgLibProgram } = params;
    const [newSendLib] = new MessageLibPDA(msgLibProgram).messageLib();
    return setSendLibrary(
      { programs: this.programRepo },
      {
        signer: delegate,
        sendLibraryConfig: this.pda.sendLibraryConfig(sender, remote),
        messageLibInfo: msgLibProgram === DefaultMessageLib2 ? void 0 : this.pda.messageLibraryInfo(newSendLib),
        oappRegistry: this.pda.oappRegistry(sender),
        program: this.programId,
        eventAuthority: this.eventAuthority,
        //param
        sender,
        eid: remote,
        newLib: msgLibProgram === DefaultMessageLib2 ? DefaultMessageLib2 : newSendLib
      }
    ).items[0];
  }
  initOAppReceiveLibrary(delegate, params) {
    const { receiver, remote } = params;
    return initReceiveLibrary(
      { programs: this.programRepo },
      {
        delegate,
        oappRegistry: this.pda.oappRegistry(receiver),
        receiveLibraryConfig: this.pda.receiveLibraryConfig(receiver, remote),
        // param
        receiver,
        eid: remote
      }
    ).items[0];
  }
  setOAppReceiveLibrary(delegate, params) {
    const { receiver, remote, msgLibProgram, gracePeriod } = params;
    const [newReceiveLib] = new MessageLibPDA(msgLibProgram).messageLib();
    return setReceiveLibrary(
      { programs: this.programRepo },
      {
        signer: delegate,
        receiveLibraryConfig: this.pda.receiveLibraryConfig(receiver, remote),
        messageLibInfo: msgLibProgram === DefaultMessageLib2 ? void 0 : this.pda.messageLibraryInfo(newReceiveLib),
        oappRegistry: this.pda.oappRegistry(receiver),
        program: this.programId,
        eventAuthority: this.eventAuthority,
        // param
        receiver,
        eid: remote,
        newLib: msgLibProgram === DefaultMessageLib2 ? DefaultMessageLib2 : newReceiveLib,
        gracePeriod: gracePeriod ?? 0
      }
    ).items[0];
  }
  setOAppReceiveLibraryTimeout(delegate, params) {
    const { receiver, remote, msgLibProgram, expiry } = params;
    const [receiveLib] = new MessageLibPDA(msgLibProgram).messageLib();
    return setReceiveLibraryTimeout(
      { programs: this.programRepo },
      {
        signer: delegate,
        oappRegistry: this.pda.oappRegistry(receiver),
        receiveLibraryConfig: this.pda.receiveLibraryConfig(receiver, remote),
        messageLibInfo: this.pda.messageLibraryInfo(receiveLib),
        eventAuthority: this.eventAuthority,
        program: this.programId,
        // param
        receiver,
        eid: remote,
        lib: receiveLib,
        expiry
      }
    ).items[0];
  }
  async setOAppConfig(rpc, delegate, params, commitment = "confirmed") {
    const { oapp, eid, config: config2, msgLibProgram } = params;
    const [msgLib] = new MessageLibPDA(msgLibProgram).messageLib();
    const msgLibVersion = params.msgLibVersion ?? await this.getMessageLibVersion(rpc, delegate.publicKey, msgLibProgram, commitment);
    let msgLibClient;
    if (msgLibVersion.major === 0n && msgLibVersion.minor == 0 && msgLibVersion.endpointVersion == 2) {
      msgLibClient = new simple_message_lib_exports2.SimpleMessageLib(msgLibProgram);
    } else if (msgLibVersion.major === 3n && msgLibVersion.minor == 0 && msgLibVersion.endpointVersion == 2) {
      msgLibClient = new uln_exports2.Uln(msgLibProgram);
    } else {
      throw new Error(
        `unsupported message lib version ${msgLibVersion.major.toString()}.${msgLibVersion.minor.toString()}`
      );
    }
    const txBuilder = setConfig(
      { programs: this.programRepo },
      {
        signer: delegate,
        messageLibInfo: this.pda.messageLibraryInfo(msgLib),
        messageLib: msgLib,
        messageLibProgram: msgLibProgram,
        oappRegistry: this.pda.oappRegistry(oapp),
        // param
        eid,
        configType: config2.configType,
        config: msgLibProgram === SIMPLE_MESSAGELIB_PROGRAM_ID ? new Uint8Array(10).fill(0) : uln_exports2.Uln.constructSetConfigData(config2.configType, config2.value),
        oapp
      }
    );
    return txBuilder.addRemainingAccounts(msgLibClient.getSetConfigIXAccountMetaForCPI(oapp, eid)).items[0];
  }
  oAppNilify(delegate, args) {
    const { receiver, srcEid, sender, nonce } = args;
    return nilify(
      { programs: this.programRepo },
      {
        signer: delegate,
        eventAuthority: this.eventAuthority,
        program: this.programId,
        oappRegistry: this.pda.oappRegistry(receiver),
        nonce: this.pda.nonce(receiver, srcEid, sender),
        pendingInboundNonce: this.pda.pendingNonce(receiver, srcEid, sender),
        payloadHash: this.pda.payloadHash(receiver, srcEid, sender, nonce)
      },
      args
    ).items[0];
  }
  oAppBurnNonce(delegate, args) {
    const { receiver, srcEid, sender, nonce } = args;
    return burn(
      { programs: this.programRepo },
      {
        signer: delegate,
        eventAuthority: this.eventAuthority,
        program: this.programId,
        oappRegistry: this.pda.oappRegistry(receiver),
        nonce: this.pda.nonce(receiver, srcEid, sender),
        payloadHash: this.pda.payloadHash(receiver, srcEid, sender, nonce),
        endpoint: this.pda.setting()
      },
      args
    ).items[0];
  }
  /// send a simulated transaction to the endpoint to get the fee for sending a message
  /**
   *
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the quote instruction.
   *
   * caculate the fee for sending a message with ULN:
   *  1. executorFee: feeForGas + feeForOptionType
   *      * feeForGas: ((gas + gasForCalldata) * gasPriceInUnit * priceRatio / priceRatioDenominator) * multiplier_bps / 10000
   *      * feeForOptionType: nativeDrop/lzReceive/lzCompose
   *  2. oracleFee: feeForGas
   *      * feeForGas: same above
   *
   *  The priceRatioDenominator is 10^20
   *  totalFee = executorFee + oracleFee * numOracles(requiredOracles + optionalOracles)
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PacketPath} path - The packet path.
   * @param {MessageLibInterface} msgLibProgram - The message library program.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   * @throws {Error} If the default send library is not initialized or the message library is blocked.
   */
  async getQuoteIXAccountMetaForCPI(rpc, payer, params, commitment = "confirmed") {
    const { path, msgLibProgram } = params;
    const { sender, dstEid, receiver } = path;
    const sendLibInfo = await this.getSendLibrary(rpc, sender, dstEid, commitment);
    const { msgLib, programId: owner } = sendLibInfo;
    const txBuilder = quote(
      { programs: this.programRepo },
      {
        sendLibraryConfig: this.pda.sendLibraryConfig(sender, dstEid),
        sendLibraryProgram: owner,
        defaultSendLibraryConfig: this.pda.defaultSendLibraryConfig(dstEid),
        endpoint: this.pda.setting(),
        nonce: this.pda.nonce(sender, dstEid, receiver),
        sendLibraryInfo: this.pda.messageLibraryInfo(msgLib),
        // param
        sender: defaultPublicKey(),
        dstEid: 0,
        receiver: new Uint8Array(32),
        message: new Uint8Array(32),
        options: new Uint8Array(32),
        payInLzToken: false
      }
    );
    const remainingAccounts = await msgLibProgram.getQuoteIXAccountMetaForCPI(rpc, payer, path);
    return [
      { pubkey: this.programId, isSigner: false, isWritable: false },
      ...txBuilder.addRemainingAccounts(remainingAccounts).getInstructions()[0].keys
    ];
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the send instruction.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PacketPath} path - The packet path.
   * @param {MessageLibInterface} msgLibProgram - The message library program.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   * @throws {Error} If the default send library is not initialized or the message library is blocked.
   */
  async getSendIXAccountMetaForCPI(rpc, payerOrAccounts, params, commitment = "confirmed") {
    const { path, msgLibProgram } = params;
    const { sender, dstEid, receiver } = path;
    const info = await this.getSendLibrary(rpc, sender, dstEid, commitment);
    const sendLibrary = info.msgLib;
    const txBuilder = send(
      { programs: this.programRepo },
      {
        sender: createNoopSigner(sender),
        /// this account should be derived from message lib
        sendLibraryProgram: info.programId,
        sendLibraryConfig: this.pda.sendLibraryConfig(sender, dstEid),
        defaultSendLibraryConfig: this.pda.defaultSendLibraryConfig(dstEid),
        sendLibraryInfo: this.pda.messageLibraryInfo(sendLibrary),
        endpoint: this.pda.setting(),
        program: this.programId,
        nonce: this.pda.nonce(sender, dstEid, receiver),
        eventAuthority: this.eventAuthority,
        // param
        dstEid: 0,
        receiver: new Uint8Array(32),
        message: new Uint8Array(32),
        options: new Uint8Array(32),
        nativeFee: 0,
        lzTokenFee: 0
      }
    );
    let remainingAccounts;
    if (typeof payerOrAccounts === "object") {
      remainingAccounts = await msgLibProgram.getSendWithLzTokenIXAccountMetaForCPI(
        rpc,
        payerOrAccounts,
        path,
        {
          tokenProgram: params.tokenProgram,
          associatedTokenProgram: params.associatedTokenProgram
        },
        commitment
      );
    } else {
      remainingAccounts = await msgLibProgram.getSendIXAccountMetaForCPI(rpc, payerOrAccounts, path);
    }
    return [
      { pubkey: this.programId, isSigner: false, isWritable: false },
      ...txBuilder.addRemainingAccounts(remainingAccounts).getInstructions()[0].keys.map((key) => {
        key.isSigner = false;
        return key;
      })
    ];
  }
  /**
   * Skips a message.
   *
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} sender - The sender public key.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {string} nonce - The nonce.
   * @returns {Promise<TransactionInstruction | null>} A promise that resolves to the transaction instruction or null if not needed.
   */
  skip(payer, params) {
    const { sender, receiver, srcEid, nonce } = params;
    return skip(
      { programs: this.programRepo },
      {
        signer: payer,
        oappRegistry: this.pda.oappRegistry(receiver),
        nonce: this.pda.nonce(receiver, srcEid, sender),
        pendingInboundNonce: this.pda.pendingNonce(receiver, srcEid, sender),
        payloadHash: this.pda.payloadHash(receiver, srcEid, sender, nonce),
        endpoint: this.pda.setting()[0],
        eventAuthority: this.eventAuthority,
        program: this.programId
      },
      params
    ).items[0];
  }
  /**
   * Initializes the verification process.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} sender - The sender public key.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {string} nonce - The nonce.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<TransactionInstruction | null>} A promise that resolves to the transaction instruction or null if not needed.
   */
  initVerify(payer, params) {
    const { sender, receiver, srcEid, nonce } = params;
    return initVerify(
      { programs: this.programRepo, payer },
      {
        payer,
        nonce: this.pda.nonce(receiver, srcEid, sender),
        payloadHash: this.pda.payloadHash(receiver, srcEid, sender, parseInt(nonce.toString()))
      },
      params
    ).items[0];
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the verify instruction.
   *
   * @param {PacketV1Codec} packet - The packet.
   * @param {PublicKey} receiveLibrary - The receive library public key.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getVerifyIXAccountMetaForCPI(packet, receiveLibrary) {
    const receiver = publicKey(Buffer.from(packet.receiver().slice(2), "hex"));
    const srcEid = packet.srcEid();
    const sender = addressToBytes32(packet.sender());
    const txBuilder = verify(
      { programs: this.programRepo },
      {
        receiveLibrary: createNoopSigner(receiveLibrary),
        receiveLibraryConfig: this.pda.receiveLibraryConfig(receiver, srcEid),
        defaultReceiveLibraryConfig: this.pda.defaultReceiveLibraryConfig(srcEid),
        nonce: this.pda.nonce(receiver, srcEid, sender),
        pendingInboundNonce: this.pda.pendingNonce(receiver, srcEid, sender),
        payloadHash: this.pda.payloadHash(receiver, srcEid, sender, parseInt(packet.nonce())),
        program: this.programId,
        eventAuthority: this.eventAuthority
      },
      {
        srcEid: 0,
        sender: new Uint8Array(32),
        receiver: defaultPublicKey(),
        nonce: 0,
        payloadHash: new Uint8Array(32)
      }
    );
    const program = {
      pubkey: this.programId,
      isSigner: false,
      isWritable: false
    };
    return [program, ...txBuilder.getInstructions()[0].keys.map((key) => ({ ...key, isSigner: false }))];
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the register OApp instruction.
   *
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} oapp - The OApp public key.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getRegisterOappIxAccountMetaForCPI(payer, oapp) {
    const txBuilder = registerOapp(
      { programs: this.programRepo, payer: createNoopSigner(payer) },
      {
        payer: createNoopSigner(payer),
        oapp: createNoopSigner(oapp),
        oappRegistry: this.pda.oappRegistry(oapp),
        eventAuthority: this.eventAuthority,
        program: this.programId,
        // param
        delegate: payer
      }
    );
    return [
      {
        pubkey: this.programId,
        isSigner: false,
        isWritable: false
      },
      ...txBuilder.getInstructions()[0].keys.map((key) => ({ ...key, isSigner: false }))
    ];
  }
  getSetDelegateIxAccountMetaForCPI(oapp) {
    const txBuilder = setDelegate(
      { programs: this.programRepo },
      {
        oapp: createNoopSigner(oapp),
        oappRegistry: this.pda.oappRegistry(oapp),
        eventAuthority: this.eventAuthority,
        program: this.programId,
        // param
        delegate: defaultPublicKey()
      }
    );
    return [
      {
        pubkey: this.programId,
        isSigner: false,
        isWritable: false
      },
      ...txBuilder.getInstructions()[0].keys.map((key) => ({ ...key, isSigner: false }))
    ];
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the skip instruction.
   *
   * @param {PublicKey} receiver - The receiver public key.
   * @param {Uint8Array} sender - The sender address.
   * @param {number} srcEid - The source endpoint ID.
   * @param {number} nonce - The nonce.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getSkipIxAccountMetaForCPI(receiver, sender, srcEid, nonce) {
    const txBuilder = skip(
      { programs: this.programRepo },
      {
        signer: createNoopSigner(receiver),
        oappRegistry: this.pda.oappRegistry(receiver),
        nonce: this.pda.nonce(receiver, srcEid, sender),
        pendingInboundNonce: this.pda.pendingNonce(receiver, srcEid, sender),
        payloadHash: this.pda.payloadHash(receiver, srcEid, sender, nonce),
        endpoint: this.pda.setting(),
        program: this.programId,
        eventAuthority: this.eventAuthority
      },
      {
        receiver: defaultPublicKey(),
        srcEid: 0,
        sender: new Uint8Array(32),
        nonce: 0
      }
    );
    return [
      {
        pubkey: this.programId,
        isSigner: false,
        isWritable: false
      },
      ...txBuilder.getInstructions()[0].keys.map((key) => ({ ...key, isSigner: false }))
    ];
  }
  /**
   * Checks if the given message library program is the default send library.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<boolean>} A promise that resolves to true if the message library program is the default send library, false otherwise.
   */
  async isDefaultSendLibrary(rpc, messageLibProgram, dstEid, commitment = "confirmed") {
    const [msgLib] = new MessageLibPDA(messageLibProgram).messageLib();
    const info = await this.getDefaultSendLibrary(rpc, dstEid, commitment);
    if (info) {
      return info.msgLib === msgLib;
    }
    return false;
  }
  /**
   * Checks if the given message library program is the default receive library.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<boolean>} A promise that resolves to true if the message library program is the default receive library, false otherwise.
   */
  async isDefaultReceiveLibrary(rpc, messageLibProgram, srcEid, commitment = "confirmed") {
    const [msgLib] = new MessageLibPDA(messageLibProgram).messageLib();
    const info = await this.getDefaultReceiveLibrary(rpc, srcEid, commitment);
    if (info) {
      return info.msgLib === msgLib;
    }
    return false;
  }
  // all of below functions are retrieving accounts state
  /**
   * Gets the endpoint settings.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<EndpointSettings | null>} A promise that resolves to the endpoint settings or null if not found.
   */
  async getSetting(rpc, commitment = "confirmed") {
    const [setting] = this.pda.setting();
    return safeFetchEndpointSettings({ rpc }, setting, { commitment });
  }
  /**
   * Gets the message library information.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} messageLibProgram - The message library program public key, It is a PDA of the message library program
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<MessageLibInfo | null>} A promise that resolves to the message library information or null if not found.
   */
  async getMessageLibInfo(rpc, messageLibProgram, commitment = "confirmed") {
    const [msgLib] = new MessageLibPDA(messageLibProgram).messageLib();
    const [messageLibInfo] = this.pda.messageLibraryInfo(msgLib);
    return safeFetchMessageLibInfo({ rpc }, messageLibInfo, { commitment });
  }
  /**
   * Gets the default receive library.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {number} srcEid - The source endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<{ msgLib: PublicKey; owner: PublicKey } | null>} A promise that resolves to the default receive library or null if not found.
   */
  async getDefaultReceiveLibrary(rpc, srcEid, commitment = "confirmed") {
    const [defaultReceiveLibConfig] = this.pda.defaultReceiveLibraryConfig(srcEid);
    const defaultInfo = await safeFetchReceiveLibraryConfig({ rpc }, defaultReceiveLibConfig, {
      commitment
    });
    if (defaultInfo == null) {
      return null;
    }
    let msgLibProgram;
    if (_Endpoint.checkIfBlockedMessageLib(defaultInfo.messageLib)) {
      msgLibProgram = blocked_messagelib_exports2.BLOCKED_MESSAGELIB_PROGRAM_ID;
    } else {
      const messageLibInfo = await rpc.getAccount(defaultInfo.messageLib, { commitment });
      invariant6(messageLibInfo.exists, "messageLibInfo should not be null");
      msgLibProgram = messageLibInfo.owner;
    }
    return {
      owner: msgLibProgram,
      msgLib: defaultInfo.messageLib
    };
  }
  /**
   * Gets the default send library.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<{ msgLib: PublicKey; owner: PublicKey } | null>} A promise that resolves to the default send library or null if not found.
   */
  async getDefaultSendLibrary(rpc, dstEid, commitment = "confirmed") {
    const [defaultSendLibConfig] = this.pda.defaultSendLibraryConfig(dstEid);
    const defaultInfo = await safeFetchSendLibraryConfig({ rpc }, defaultSendLibConfig, { commitment });
    if (defaultInfo == null) {
      return null;
    }
    let msgLibProgram;
    if (_Endpoint.checkIfBlockedMessageLib(defaultInfo.messageLib)) {
      msgLibProgram = blocked_messagelib_exports2.BLOCKED_MESSAGELIB_PROGRAM_ID;
    } else {
      const messageLibInfo = await rpc.getAccount(defaultInfo.messageLib, { commitment });
      invariant6(messageLibInfo.exists, "messageLibInfo should not be null");
      msgLibProgram = messageLibInfo.owner;
    }
    return {
      owner: msgLibProgram,
      msgLib: defaultInfo.messageLib
    };
  }
  /**
   * Gets the configured send library for the app.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} oappPda - The OApp PDA.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<{ msgLib: PublicKey; programId: PublicKey; isDefault: boolean } | null>} A promise that resolves to the configured send library or null if not found.
   */
  async getSendLibrary(rpc, oappPda, dstEid, commitment = "confirmed") {
    const [sendLibConfig] = this.pda.sendLibraryConfig(oappPda, dstEid);
    const [defaultSendLibConfig] = this.pda.defaultSendLibraryConfig(dstEid);
    const [defaultSendLibConfigBuf, sendLibConfigBuf] = await rpc.getAccounts(
      [defaultSendLibConfig, sendLibConfig],
      { commitment }
    );
    if (!defaultSendLibConfigBuf.exists || !sendLibConfigBuf.exists) {
      throw new Error(
        `Unable to find defaultSendLibraryConfig/sendLibraryConfig account at ${defaultSendLibConfig}/${sendLibConfig}`
      );
    }
    const sendLibConfigInfo = deserializeSendLibraryConfig(sendLibConfigBuf);
    const defaultSendLibConfigInfo = deserializeSendLibraryConfig(defaultSendLibConfigBuf);
    const msgLib = sendLibConfigInfo.messageLib.toString() === DefaultMessageLib2.toString() ? defaultSendLibConfigInfo.messageLib : sendLibConfigInfo.messageLib;
    const isDefault = sendLibConfigInfo.messageLib.toString() === DefaultMessageLib2.toString();
    let msgLibProgram;
    if (_Endpoint.checkIfBlockedMessageLib(msgLib)) {
      msgLibProgram = blocked_messagelib_exports2.BLOCKED_MESSAGELIB_PROGRAM_ID;
    } else {
      const msgLibInfo = await rpc.getAccount(msgLib, { commitment });
      invariant6(msgLibInfo.exists, "msgLibInfo should not be null");
      msgLibProgram = msgLibInfo.owner;
    }
    return {
      programId: msgLibProgram,
      msgLib,
      isDefault
    };
  }
  /**
   * Gets the configured receive library for the app.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} oappPda - The OApp PDA.
   * @param {number} srcEid - The source endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<{ msgLib: PublicKey; programId: PublicKey; isDefault: boolean; timeout: { msgLib: PublicKey; expiry: bigint } | null } | null>} A promise that resolves to the configured receive library or null if not found.
   */
  async getReceiveLibrary(rpc, oappPda, srcEid, commitment = "confirmed") {
    const [receiveLibConfig] = this.pda.receiveLibraryConfig(oappPda, srcEid);
    const [defaultReceiveLibConfig] = this.pda.defaultReceiveLibraryConfig(srcEid);
    const [defaultReceiveLibConfigBuf, receiveLibConfigBuf] = await rpc.getAccounts(
      [defaultReceiveLibConfig, receiveLibConfig],
      { commitment }
    );
    if (!defaultReceiveLibConfigBuf.exists || !receiveLibConfigBuf.exists) {
      throw new Error(
        `Unable to find defaultReceiveLibraryConfig/receiveLibraryConfig account at ${defaultReceiveLibConfig}/${receiveLibConfig}`
      );
    }
    const receiveLibConfigInfo = deserializeReceiveLibraryConfig(receiveLibConfigBuf);
    const defaultReceiveLibConfigInfo = deserializeReceiveLibraryConfig(defaultReceiveLibConfigBuf);
    const finalReceiveLibConfigInfo = receiveLibConfigInfo.messageLib.toString() === DefaultMessageLib2.toString() ? defaultReceiveLibConfigInfo : receiveLibConfigInfo;
    const msgLib = finalReceiveLibConfigInfo.messageLib;
    const isDefault = receiveLibConfigInfo.messageLib.toString() === DefaultMessageLib2.toString();
    let msgLibProgram;
    if (_Endpoint.checkIfBlockedMessageLib(msgLib)) {
      msgLibProgram = blocked_messagelib_exports2.BLOCKED_MESSAGELIB_PROGRAM_ID;
    } else {
      const msgLibInfo = await rpc.getAccount(msgLib, { commitment });
      invariant6(msgLibInfo.exists, "msgLibInfo should not be null");
      msgLibProgram = msgLibInfo.owner;
    }
    const receiveTimeout = isSome(finalReceiveLibConfigInfo.timeout) ? {
      msgLib: finalReceiveLibConfigInfo.timeout.value.messageLib,
      expiry: finalReceiveLibConfigInfo.timeout.value.expiry
    } : void 0;
    return {
      programId: msgLibProgram,
      msgLib,
      isDefault,
      timeout: receiveTimeout
    };
  }
  // rename to a more generic name
  /**
   * Gets the inbound payload hash.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} srcEid - The source endpoint ID.
   * @param {Uint8Array} sender - The sender address.
   * @param {number} nonce - The nonce.
   * @param {Uint8Array} _payloadHash - The payload hash.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<PayloadHash | null>} A promise that resolves to the payload hash or null if not found.
   */
  async getInboundPayloadHash(rpc, receiver, srcEid, sender, nonce, _payloadHash, commitment = "confirmed") {
    const [payloadHashPDA] = this.pda.payloadHash(receiver, srcEid, sender, nonce);
    return safeFetchPayloadHash({ rpc }, payloadHashPDA, { commitment });
  }
  /**
   * Gets the composed message state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} from - The sender public key.
   * @param {types.SendComposeParams} params - The send compose parameters.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<ComposeMessageState | null>} A promise that resolves to the composed message state or null if not found.
   */
  async getComposedMessageState(rpc, from, params, commitment = "confirmed") {
    const message = arrayify(keccak_256(params.message));
    const [composedMessagePDA] = this.pda.composedMessage(from, params.guid, params.index, params.to, message);
    return safeFetchComposeMessageState({ rpc }, composedMessagePDA, { commitment });
  }
  /**
   * Gets the nonce.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} oappIDPDA - The OApp ID PDA.
   * @param {number} remoteEid - The remote endpoint ID.
   * @param {Uint8Array} remoteOappAddr - The remote OApp address.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.Nonce | null>} A promise that resolves to the nonce or null if not found.
   */
  async getNonce(rpc, oappIDPDA2, remoteEid, remoteOappAddr, commitment = "confirmed") {
    const [nonce] = this.pda.nonce(oappIDPDA2, remoteEid, remoteOappAddr);
    return safeFetchNonce({ rpc }, nonce, { commitment });
  }
  /**
   * Gets the pending inbound nonce.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} oappIDPDA - The OApp ID PDA.
   * @param {number} remoteEid - The remote endpoint ID.
   * @param {Uint8Array} remoteOappAddr - The remote OApp address.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.PendingInboundNonce | null>} A promise that resolves to the pending inbound nonce or null if not found.
   */
  async getPendingInboundNonce(rpc, oappIDPDA2, remoteEid, remoteOappAddr, commitment = "confirmed") {
    const [nonce] = this.pda.pendingNonce(oappIDPDA2, remoteEid, remoteOappAddr);
    return safeFetchPendingInboundNonce({ rpc }, nonce, { commitment });
  }
  /**
   * Gets the message library version.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} messageLibProgram - The message library program public key.
   * @param {Commitment} [commitment='confirmed'] - The commitment level.
   * @returns {Promise<Version | null>} A promise that resolves to the version or null if not found.
   */
  async getMessageLibVersion(rpc, payer, messageLibProgram, commitment = "confirmed") {
    const msgLibProgramInWeb3Js = toWeb3JsPublicKey(messageLibProgram);
    const version5 = await simulateWeb3JsTransaction(
      rpc.getEndpoint(),
      [
        new TransactionInstruction({
          keys: [{ pubkey: msgLibProgramInWeb3Js, isSigner: false, isWritable: false }],
          data: Buffer.from(
            simple_message_lib_exports2.instructions.getVersionInstructionDataSerializer().serialize({})
          ),
          programId: msgLibProgramInWeb3Js
        })
      ],
      msgLibProgramInWeb3Js,
      toWeb3JsPublicKey(payer),
      simple_message_lib_exports2.types.getVersionSerializer(),
      commitment
    );
    return version5;
  }
  /**
   * Transfers the admin role to a new admin.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} admin - The current admin public key.
   * @param {PublicKey} newAdmin - The new admin public key.
   * @param {Commitment} [commitment='confirmed'] - The commitment level.
   * @returns {Promise<TransactionInstruction | null>} A promise that resolves to the transaction instruction or null if not needed.
   */
  transferAdmin(admin, newAdmin) {
    return transferAdmin(
      { programs: this.programRepo },
      {
        admin,
        endpoint: this.pda.setting(),
        eventAuthority: this.eventAuthority,
        program: this.programId
      },
      {
        admin: newAdmin
      }
    ).items[0];
  }
  static checkIfBlockedMessageLib(msgLib) {
    const [blockedMessageLib] = new MessageLibPDA(
      blocked_messagelib_exports2.BLOCKED_MESSAGELIB_PROGRAM_ID
    ).messageLib();
    return msgLib.toString() === blockedMessageLib.toString();
  }
};

// src/simple-message-lib.ts
var simple_message_lib_exports2 = {};
__export(simple_message_lib_exports2, {
  SIMPLE_MESSAGELIB_PROGRAM_ID: () => SIMPLE_MESSAGELIB_PROGRAM_ID,
  SimpleMessageLib: () => SimpleMessageLib2,
  accounts: () => accounts_exports8,
  errors: () => errors_exports6,
  instructions: () => instructions_exports8,
  types: () => types_exports8
});
var SimpleMessageLib2 = class {
  constructor(programId = SIMPLE_MESSAGELIB_PROGRAM_ID, rpc) {
    this.programId = programId;
    this.pda = new MessageLibPDA(programId);
    if (rpc === void 0) {
      rpc = createNullRpc();
      rpc.getCluster = () => "custom";
    }
    this.programRepo = createDefaultProgramRepository({ rpc }, [
      {
        name: "simpleMessagelib",
        publicKey: programId,
        getErrorFromCode(code, cause) {
          return getSimpleMessagelibErrorFromCode(code, this, cause);
        },
        getErrorFromName(name, cause) {
          return getSimpleMessagelibErrorFromName(name, this, cause);
        },
        isOnCluster() {
          return true;
        }
      }
    ]);
  }
  getProgram(clusterFilter = "custom") {
    return this.programRepo.get("simpleMessagelib", clusterFilter);
  }
  /**
   * Initializes the SimpleMessageLib.
   *
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} admin - The admin public key.
   * @param {number} eid - The endpoint ID.
   * @param {number} nativeFee - The native fee.
   * @param {number} [lzTokenFee=0] - The LayerZero token fee.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  initSimpleMessageLib(payer, param) {
    const [messageLib] = this.pda.messageLib();
    const { endpointProgram, admin, eid, nativeFee, lzTokenFee } = param;
    const [endpointAuth] = new EndpointPDA(endpointProgram ?? ENDPOINT_PROGRAM_ID).messageLibraryInfo(messageLib);
    return initMessageLib(
      { payer, programs: this.programRepo },
      {
        payer,
        messageLib,
        // param
        endpointProgram: endpointProgram ?? ENDPOINT_PROGRAM_ID,
        endpoint: endpointAuth,
        eid,
        admin,
        fee: nativeFee,
        lzTokenFee
      }
    ).items[0];
  }
  /**
   * Sets the whitelist caller.
   *
   * @param {Signer} admin - The admin public key.
   * @param {PublicKey} newCaller - The new caller public key.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  setWhitelistCaller(admin, newCaller) {
    const [messageLibPDA] = this.pda.messageLib();
    return setWlCaller(
      { programs: this.programRepo },
      {
        admin,
        messageLib: messageLibPDA,
        newCaller
      }
    ).items[0];
  }
  /**
   * Checks if a caller is whitelisted.
   *
   * @param {RpcInterface} rpc - The connection object.
   * @param {PublicKey} caller - The caller public key.
   *
   * @returns {Promise<boolean>} A promise that resolves to true if the caller is whitelisted, false otherwise.
   */
  async isWhiteListed(rpc, caller) {
    try {
      const exceptedCaller = await this.getWhiteListCaller(rpc);
      return exceptedCaller === caller;
    } catch (e) {
      return false;
    }
  }
  /**
   * Gets the whitelisted caller.
   *
   * @param {RpcInterface} rpc - The connection object.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment or config.
   *
   * @returns {Promise<PublicKey>} A promise that resolves to the whitelisted caller public key.
   */
  async getWhiteListCaller(rpc) {
    const [messageLib] = this.pda.messageLib();
    const messageLibInfo = await fetchMessageLib({ rpc }, messageLib, {});
    return messageLibInfo.wlCaller;
  }
  /**
   * Validates a packet.
   *
   * @param {Signer} payer - The payer public key.
   * @param {Uint8Array} encodedPacket - The encoded packet.
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   *
   * @returns {TransactionInstruction} The transaction instruction.
   */
  validatePacket(payer, encodedPacket, endpointProgram) {
    const packet = PacketV1Codec.fromBytes(encodedPacket);
    const [receiveLibrary] = this.pda.messageLib();
    const endpoint = new endpoint_exports2.Endpoint(endpointProgram ?? ENDPOINT_PROGRAM_ID);
    const txBuilder = validatePacket(
      { programs: this.programRepo, payer },
      {
        receiveLibrary,
        packet: encodedPacket
      }
    );
    return txBuilder.addRemainingAccounts(endpoint.getVerifyIXAccountMetaForCPI(packet, receiveLibrary)).items[0];
  }
  /**
   * Gets the account meta of the send instruction for CPI (Cross-Program Invocation).
   *
   * @param {RpcInterface} _rpc - The connection object.
   * @param {PublicKey} payer - The payer public key.
   * @param {SolanaPacketPath} _path - The packet path.
   *
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account meta.
   */
  async getSendIXAccountMetaForCPI(_rpc, payer, _path) {
    const [msgLib] = this.pda.messageLib();
    const txBuilder = send2(
      { payer: createNoopSigner(payer), programs: this.programRepo },
      {
        endpoint: createNoopSigner(defaultPublicKey()),
        // useless
        messageLib: msgLib,
        payer: createNoopSigner(payer),
        //param
        packet: {
          nonce: 0,
          srcEid: 0,
          sender: defaultPublicKey(),
          dstEid: 0,
          receiver: new Uint8Array(32),
          guid: new Uint8Array(32),
          message: new Uint8Array(0)
        },
        options: new Uint8Array([0, 0, 0, 0]),
        nativeFee: 0
      }
    );
    const cpiAccounts = txBuilder.getInstructions()[0].keys.map((key) => {
      if (payer !== key.pubkey) {
        key.isSigner = false;
      }
      return key;
    });
    return Promise.resolve(cpiAccounts.slice(1));
  }
  /***
   * Get the account meta of the send instruction for CPI(Cross-Program Invocation ) with lzToken
   */
  async getSendWithLzTokenIXAccountMetaForCPI(_rpc, accounts, _path, programs = {
    tokenProgram: fromWeb3JsPublicKey(TOKEN_PROGRAM_ID),
    associatedTokenProgram: fromWeb3JsPublicKey(ASSOCIATED_TOKEN_PROGRAM_ID)
  }, _commitment = "confirmed") {
    const { payer, tokenMint, tokenSource } = accounts;
    const [msgLib] = this.pda.messageLib();
    const {
      tokenProgram = fromWeb3JsPublicKey(TOKEN_PROGRAM_ID),
      associatedTokenProgram = fromWeb3JsPublicKey(ASSOCIATED_TOKEN_PROGRAM_ID)
    } = programs;
    const messageLibLzToken = await getAssociatedTokenAddress(
      toWeb3JsPublicKey(tokenMint),
      toWeb3JsPublicKey(msgLib),
      true,
      toWeb3JsPublicKey(tokenProgram),
      toWeb3JsPublicKey(associatedTokenProgram)
    );
    const txBuilder = sendWithLzToken(
      { payer: createNoopSigner(payer), programs: this.programRepo },
      {
        endpoint: createNoopSigner(defaultPublicKey()),
        messageLib: msgLib,
        payer: createNoopSigner(payer),
        messageLibLzToken: fromWeb3JsPublicKey(messageLibLzToken),
        lzTokenSource: tokenSource,
        lzTokenMint: tokenMint
      },
      {
        //param
        packet: {
          nonce: 0,
          srcEid: 0,
          sender: defaultPublicKey(),
          dstEid: 0,
          receiver: new Uint8Array(32),
          guid: new Uint8Array(32),
          message: new Uint8Array(0)
        },
        options: new Uint8Array([0, 0, 0, 0]),
        nativeFee: 0,
        lzTokenFee: 0,
        lzTokenMint: tokenMint
      }
    );
    const cpiAccounts = txBuilder.getInstructions()[0].keys.map((key) => {
      if (payer !== key.pubkey) {
        key.isSigner = false;
      }
      return key;
    });
    return Promise.resolve(cpiAccounts.slice(1));
  }
  /**
   * Gets the account meta of the quote instruction for CPI (Cross-Program Invocation).
   *
   * @param {Connection} _connection - The connection object.
   * @param {PublicKey} _payer - The payer public key.
   * @param {PacketPath} _path - The packet path.
   * @param {Commitment} [commitment] - The commitment.
   *
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account meta.
   */
  async getQuoteIXAccountMetaForCPI(_rpc, _payer, _path, _commitment = "confirmed") {
    const [msgLib] = this.pda.messageLib();
    const txBuilder = quote2(
      { programs: this.programRepo },
      {
        endpoint: createNoopSigner(defaultPublicKey()),
        // useless
        messageLib: msgLib,
        //param
        packet: {
          nonce: 0,
          srcEid: 0,
          sender: defaultPublicKey(),
          dstEid: 0,
          receiver: new Uint8Array(32),
          guid: new Uint8Array(32),
          message: new Uint8Array(0)
        },
        options: new Uint8Array([0, 0, 0, 0]),
        payInLzToken: false
      }
    );
    return Promise.resolve(txBuilder.getInstructions()[0].keys.slice(1));
  }
  /**
   * Gets the account meta of the init config instruction for CPI (Cross-Program Invocation).
   *
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} oapp - The oapp ID public key.
   * @param {number} eid - The endpoint ID.
   *
   * @returns {AccountMeta[]} An array of account meta.
   */
  getInitConfigIXAccountMetaForCPI(payer, oapp, eid) {
    const [sendConfig] = this.pda.sendConfig(eid, oapp);
    const [receiveConfig] = this.pda.receiveConfig(eid, oapp);
    const [messageLib] = this.pda.messageLib();
    const txBuilder = initConfig2(
      { payer: createNoopSigner(payer), programs: this.programRepo },
      {
        endpoint: createNoopSigner(defaultPublicKey()),
        payer: createNoopSigner(payer),
        messageLib,
        sendConfig,
        receiveConfig,
        // param
        oapp,
        eid
      }
    );
    const cpiAccounts = txBuilder.getInstructions()[0].keys.map((key) => {
      if (payer !== key.pubkey) {
        key.isSigner = false;
      }
      return key;
    });
    return cpiAccounts.slice(1);
  }
  /**
   * Gets the account meta of the set config instruction for CPI (Cross-Program Invocation).
   *
   * @param {PublicKey} oapp - The oapp ID public key.
   * @param {number} eid - The endpoint ID.
   * @param {PublicKey} _endpointProgram - The endpoint program public key.
   *
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account meta.
   */
  getSetConfigIXAccountMetaForCPI(oapp, eid, _endpointProgram = ENDPOINT_PROGRAM_ID) {
    const [sendConfig] = this.pda.sendConfig(eid, oapp);
    const [receiveConfig] = this.pda.receiveConfig(eid, oapp);
    const [msgLib] = this.pda.messageLib();
    const txBuilder = setConfig2(
      { programs: this.programRepo },
      {
        endpoint: createNoopSigner(ENDPOINT_PROGRAM_ID),
        // useless
        sendConfig,
        receiveConfig,
        messageLib: msgLib,
        // param
        oapp,
        eid: 0,
        configType: 0,
        config: new Uint8Array(0)
      }
    );
    return txBuilder.getInstructions()[0].keys.slice(1);
  }
};

// src/uln.ts
var uln_exports2 = {};
__export(uln_exports2, {
  ULN_PROGRAM_ID: () => ULN_PROGRAM_ID,
  Uln: () => Uln2,
  accounts: () => accounts_exports11,
  errors: () => errors_exports9,
  events: () => events_exports8,
  instructions: () => instructions_exports11,
  types: () => types_exports11
});

// src/dvn.ts
var dvn_exports4 = {};
__export(dvn_exports4, {
  DVN: () => DVN2,
  DVN_PROGRAM_ID: () => DVN_PROGRAM_ID,
  SolanaSignerExt: () => SolanaSignerExt2,
  accounts: () => accounts_exports9,
  errors: () => errors_exports7,
  events: () => events_exports6,
  instructions: () => instructions_exports9,
  types: () => types_exports9
});

// src/generated/kinobi/dvn/accounts/index.ts
var accounts_exports9 = {};
__export(accounts_exports9, {
  deserializeDvnConfig: () => deserializeDvnConfig,
  deserializeExecuteHash: () => deserializeExecuteHash,
  deserializeReceiveConfig: () => deserializeReceiveConfig,
  fetchAllDvnConfig: () => fetchAllDvnConfig,
  fetchAllExecuteHash: () => fetchAllExecuteHash,
  fetchAllReceiveConfig: () => fetchAllReceiveConfig,
  fetchDvnConfig: () => fetchDvnConfig,
  fetchExecuteHash: () => fetchExecuteHash,
  fetchReceiveConfig: () => fetchReceiveConfig,
  getDvnConfigAccountDataSerializer: () => getDvnConfigAccountDataSerializer,
  getDvnConfigGpaBuilder: () => getDvnConfigGpaBuilder,
  getExecuteHashAccountDataSerializer: () => getExecuteHashAccountDataSerializer,
  getExecuteHashGpaBuilder: () => getExecuteHashGpaBuilder,
  getExecuteHashSize: () => getExecuteHashSize,
  getReceiveConfigAccountDataSerializer: () => getReceiveConfigAccountDataSerializer,
  getReceiveConfigGpaBuilder: () => getReceiveConfigGpaBuilder,
  safeFetchAllDvnConfig: () => safeFetchAllDvnConfig,
  safeFetchAllExecuteHash: () => safeFetchAllExecuteHash,
  safeFetchAllReceiveConfig: () => safeFetchAllReceiveConfig,
  safeFetchDvnConfig: () => safeFetchDvnConfig,
  safeFetchExecuteHash: () => safeFetchExecuteHash,
  safeFetchReceiveConfig: () => safeFetchReceiveConfig
});

// src/generated/kinobi/dvn/types/index.ts
var types_exports9 = {};
__export(types_exports9, {
  VerificationState: () => VerificationState2,
  adminConfig: () => adminConfig,
  getAclSerializer: () => getAclSerializer,
  getAdminConfigSerializer: () => getAdminConfigSerializer,
  getDstConfigSerializer: () => getDstConfigSerializer,
  getExecuteTransactionDigestSerializer: () => getExecuteTransactionDigestSerializer,
  getLzOptionSerializer: () => getLzOptionSerializer,
  getMultisigConfigSerializer: () => getMultisigConfigSerializer,
  getMultisigSerializer: () => getMultisigSerializer,
  getTransactionAccountSerializer: () => getTransactionAccountSerializer,
  getUlnConfigSerializer: () => getUlnConfigSerializer,
  getVerificationStateSerializer: () => getVerificationStateSerializer,
  isAdminConfig: () => isAdminConfig,
  isMultisigConfig: () => isMultisigConfig,
  multisigConfig: () => multisigConfig
});
function getAclSerializer() {
  return struct(
    [
      ["allowList", array$1(publicKey$1())],
      ["denyList", array$1(publicKey$1())]
    ],
    { description: "Acl" }
  );
}
function getAdminConfigSerializer() {
  return dataEnum(
    [
      [
        "Admins",
        struct([
          ["fields", tuple([array$1(publicKey$1())])]
        ])
      ],
      [
        "DefaultMultiplierBps",
        struct([
          ["fields", tuple([u16$1()])]
        ])
      ],
      [
        "DstConfigs",
        struct([
          ["fields", tuple([array$1(getDstConfigSerializer())])]
        ])
      ],
      [
        "PriceFeed",
        struct([
          ["fields", tuple([publicKey$1()])]
        ])
      ],
      [
        "RemoveDstConfigs",
        struct([
          ["fields", tuple([array$1(u32$1())])]
        ])
      ]
    ],
    { description: "AdminConfig" }
  );
}
function adminConfig(kind, data) {
  return Array.isArray(data) ? { __kind: kind, fields: data } : { __kind: kind, ...data ?? {} };
}
function isAdminConfig(kind, value) {
  return value.__kind === kind;
}
function getDstConfigSerializer() {
  return struct(
    [
      ["eid", u32$1()],
      ["dstGas", u32$1()],
      ["multiplierBps", option(u16$1())],
      ["floorMarginUsd", option(u128())]
    ],
    { description: "DstConfig" }
  );
}
function getExecuteTransactionDigestSerializer() {
  return struct(
    [
      ["vid", u32$1()],
      ["programId", publicKey$1()],
      ["accounts", array$1(getTransactionAccountSerializer())],
      ["data", bytes$1({ size: u32$1() })],
      ["expiration", i64()]
    ],
    { description: "ExecuteTransactionDigest" }
  );
}
function getLzOptionSerializer() {
  return struct(
    [
      ["optionType", u8$1()],
      ["params", bytes$1({ size: u32$1() })]
    ],
    { description: "LzOption" }
  );
}
function getMultisigSerializer() {
  return struct(
    [
      ["signers", array$1(bytes$1({ size: 64 }))],
      ["quorum", u8$1()]
    ],
    { description: "Multisig" }
  );
}
function getMultisigConfigSerializer() {
  return dataEnum(
    [
      [
        "Admins",
        struct([
          ["fields", tuple([array$1(publicKey$1())])]
        ])
      ],
      [
        "Allowlist",
        struct([
          ["fields", tuple([array$1(publicKey$1())])]
        ])
      ],
      [
        "Denylist",
        struct([
          ["fields", tuple([array$1(publicKey$1())])]
        ])
      ],
      [
        "Msglibs",
        struct([
          ["fields", tuple([array$1(publicKey$1())])]
        ])
      ],
      [
        "Paused",
        struct([
          ["fields", tuple([bool$1()])]
        ])
      ],
      [
        "Quorum",
        struct([
          ["fields", tuple([u8$1()])]
        ])
      ],
      [
        "Signers",
        struct([
          ["fields", tuple([array$1(bytes$1({ size: 64 }))])]
        ])
      ]
    ],
    { description: "MultisigConfig" }
  );
}
function multisigConfig(kind, data) {
  return Array.isArray(data) ? { __kind: kind, fields: data } : { __kind: kind, ...data ?? {} };
}
function isMultisigConfig(kind, value) {
  return value.__kind === kind;
}
function getTransactionAccountSerializer() {
  return struct(
    [
      ["pubkey", publicKey$1()],
      ["isSigner", bool$1()],
      ["isWritable", bool$1()]
    ],
    { description: "TransactionAccount" }
  );
}
function getUlnConfigSerializer() {
  return struct(
    [
      ["confirmations", u64$1()],
      ["requiredDvnCount", u8$1()],
      ["optionalDvnCount", u8$1()],
      ["optionalDvnThreshold", u8$1()],
      ["requiredDvns", array$1(publicKey$1())],
      ["optionalDvns", array$1(publicKey$1())]
    ],
    { description: "UlnConfig" }
  );
}
var VerificationState2 = /* @__PURE__ */ ((VerificationState3) => {
  VerificationState3[VerificationState3["Verifying"] = 0] = "Verifying";
  VerificationState3[VerificationState3["Verifiable"] = 1] = "Verifiable";
  VerificationState3[VerificationState3["Verified"] = 2] = "Verified";
  VerificationState3[VerificationState3["NotInitializable"] = 3] = "NotInitializable";
  VerificationState3[VerificationState3["VerifiableButCapExceeded"] = 4] = "VerifiableButCapExceeded";
  return VerificationState3;
})(VerificationState2 || {});
function getVerificationStateSerializer() {
  return scalarEnum(VerificationState2, {
    description: "VerificationState"
  });
}

// src/generated/kinobi/dvn/accounts/dvnConfig.ts
function getDvnConfigAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["vid", u32$1()],
        ["bump", u8$1()],
        ["multisig", getMultisigSerializer()],
        ["acl", getAclSerializer()],
        ["paused", bool$1()],
        ["msglibs", array$1(publicKey$1())],
        ["admins", array$1(publicKey$1())],
        ["priceFeed", publicKey$1()],
        ["dstConfigs", array$1(getDstConfigSerializer())],
        ["defaultMultiplierBps", u16$1()]
      ],
      { description: "DvnConfigAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([49, 81, 33, 98, 234, 168, 93, 73])
    })
  );
}
function deserializeDvnConfig(rawAccount) {
  return deserializeAccount(rawAccount, getDvnConfigAccountDataSerializer());
}
async function fetchDvnConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "DvnConfig");
  return deserializeDvnConfig(maybeAccount);
}
async function safeFetchDvnConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeDvnConfig(maybeAccount) : null;
}
async function fetchAllDvnConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "DvnConfig");
    return deserializeDvnConfig(maybeAccount);
  });
}
async function safeFetchAllDvnConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map((maybeAccount) => deserializeDvnConfig(maybeAccount));
}
function getDvnConfigGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "dvn",
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    vid: [8, u32$1()],
    bump: [12, u8$1()],
    multisig: [13, getMultisigSerializer()],
    acl: [null, getAclSerializer()],
    paused: [null, bool$1()],
    msglibs: [null, array$1(publicKey$1())],
    admins: [null, array$1(publicKey$1())],
    priceFeed: [null, publicKey$1()],
    dstConfigs: [null, array$1(getDstConfigSerializer())],
    defaultMultiplierBps: [null, u16$1()]
  }).deserializeUsing((account) => deserializeDvnConfig(account)).whereField(
    "discriminator",
    new Uint8Array([49, 81, 33, 98, 234, 168, 93, 73])
  );
}
function getExecuteHashAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["expiration", i64()],
        ["bump", u8$1()]
      ],
      { description: "ExecuteHashAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([34, 17, 137, 123, 62, 25, 205, 155])
    })
  );
}
function deserializeExecuteHash(rawAccount) {
  return deserializeAccount(rawAccount, getExecuteHashAccountDataSerializer());
}
async function fetchExecuteHash(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "ExecuteHash");
  return deserializeExecuteHash(maybeAccount);
}
async function safeFetchExecuteHash(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeExecuteHash(maybeAccount) : null;
}
async function fetchAllExecuteHash(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "ExecuteHash");
    return deserializeExecuteHash(maybeAccount);
  });
}
async function safeFetchAllExecuteHash(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map((maybeAccount) => deserializeExecuteHash(maybeAccount));
}
function getExecuteHashGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "dvn",
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    expiration: [8, i64()],
    bump: [16, u8$1()]
  }).deserializeUsing((account) => deserializeExecuteHash(account)).whereField(
    "discriminator",
    new Uint8Array([34, 17, 137, 123, 62, 25, 205, 155])
  );
}
function getExecuteHashSize() {
  return 17;
}
function getReceiveConfigAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["bump", u8$1()],
        ["uln", getUlnConfigSerializer()]
      ],
      { description: "ReceiveConfigAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([162, 159, 153, 188, 56, 65, 245, 58])
    })
  );
}
function deserializeReceiveConfig(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getReceiveConfigAccountDataSerializer()
  );
}
async function fetchReceiveConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "ReceiveConfig");
  return deserializeReceiveConfig(maybeAccount);
}
async function safeFetchReceiveConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeReceiveConfig(maybeAccount) : null;
}
async function fetchAllReceiveConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "ReceiveConfig");
    return deserializeReceiveConfig(maybeAccount);
  });
}
async function safeFetchAllReceiveConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeReceiveConfig(maybeAccount)
  );
}
function getReceiveConfigGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "dvn",
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    bump: [8, u8$1()],
    uln: [9, getUlnConfigSerializer()]
  }).deserializeUsing(
    (account) => deserializeReceiveConfig(account)
  ).whereField(
    "discriminator",
    new Uint8Array([162, 159, 153, 188, 56, 65, 245, 58])
  );
}

// src/generated/kinobi/dvn/errors/index.ts
var errors_exports7 = {};
__export(errors_exports7, {
  DuplicateSignatureError: () => DuplicateSignatureError2,
  EidNotSupportedError: () => EidNotSupportedError3,
  ExpiredError: () => ExpiredError2,
  InvalidAmountError: () => InvalidAmountError7,
  InvalidQuorumError: () => InvalidQuorumError2,
  InvalidSignatureLenError: () => InvalidSignatureLenError2,
  InvalidSignersLenError: () => InvalidSignersLenError2,
  InvalidVidError: () => InvalidVidError2,
  MsgLibNotAllowedError: () => MsgLibNotAllowedError3,
  NotAdminError: () => NotAdminError3,
  PausedError: () => PausedError3,
  SignatureErrorError: () => SignatureErrorError2,
  SignerNotInCommitteeError: () => SignerNotInCommitteeError2,
  TooManyAdminsError: () => TooManyAdminsError3,
  TooManyOptionTypesError: () => TooManyOptionTypesError3,
  UnexpiredExecuteHashError: () => UnexpiredExecuteHashError2,
  UniqueOwnersError: () => UniqueOwnersError2,
  getDvnErrorFromCode: () => getDvnErrorFromCode,
  getDvnErrorFromName: () => getDvnErrorFromName
});
var codeToErrorMap3 = /* @__PURE__ */ new Map();
var nameToErrorMap3 = /* @__PURE__ */ new Map();
var InvalidSignatureLenError2 = class extends ProgramError {
  // 6000
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidSignatureLen";
    this.code = 6e3;
  }
};
codeToErrorMap3.set(6e3, InvalidSignatureLenError2);
nameToErrorMap3.set("InvalidSignatureLen", InvalidSignatureLenError2);
var NotAdminError3 = class extends ProgramError {
  // 6001
  constructor(program, cause) {
    super("", program, cause);
    this.name = "NotAdmin";
    this.code = 6001;
  }
};
codeToErrorMap3.set(6001, NotAdminError3);
nameToErrorMap3.set("NotAdmin", NotAdminError3);
var MsgLibNotAllowedError3 = class extends ProgramError {
  // 6002
  constructor(program, cause) {
    super("", program, cause);
    this.name = "MsgLibNotAllowed";
    this.code = 6002;
  }
};
codeToErrorMap3.set(6002, MsgLibNotAllowedError3);
nameToErrorMap3.set("MsgLibNotAllowed", MsgLibNotAllowedError3);
var InvalidQuorumError2 = class extends ProgramError {
  // 6003
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidQuorum";
    this.code = 6003;
  }
};
codeToErrorMap3.set(6003, InvalidQuorumError2);
nameToErrorMap3.set("InvalidQuorum", InvalidQuorumError2);
var InvalidSignersLenError2 = class extends ProgramError {
  // 6004
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidSignersLen";
    this.code = 6004;
  }
};
codeToErrorMap3.set(6004, InvalidSignersLenError2);
nameToErrorMap3.set("InvalidSignersLen", InvalidSignersLenError2);
var UniqueOwnersError2 = class extends ProgramError {
  // 6005
  constructor(program, cause) {
    super("", program, cause);
    this.name = "UniqueOwners";
    this.code = 6005;
  }
};
codeToErrorMap3.set(6005, UniqueOwnersError2);
nameToErrorMap3.set("UniqueOwners", UniqueOwnersError2);
var SignatureErrorError2 = class extends ProgramError {
  // 6006
  constructor(program, cause) {
    super("", program, cause);
    this.name = "SignatureError";
    this.code = 6006;
  }
};
codeToErrorMap3.set(6006, SignatureErrorError2);
nameToErrorMap3.set("SignatureError", SignatureErrorError2);
var SignerNotInCommitteeError2 = class extends ProgramError {
  // 6007
  constructor(program, cause) {
    super("", program, cause);
    this.name = "SignerNotInCommittee";
    this.code = 6007;
  }
};
codeToErrorMap3.set(6007, SignerNotInCommitteeError2);
nameToErrorMap3.set("SignerNotInCommittee", SignerNotInCommitteeError2);
var TooManyAdminsError3 = class extends ProgramError {
  // 6008
  constructor(program, cause) {
    super("", program, cause);
    this.name = "TooManyAdmins";
    this.code = 6008;
  }
};
codeToErrorMap3.set(6008, TooManyAdminsError3);
nameToErrorMap3.set("TooManyAdmins", TooManyAdminsError3);
var TooManyOptionTypesError3 = class extends ProgramError {
  // 6009
  constructor(program, cause) {
    super("", program, cause);
    this.name = "TooManyOptionTypes";
    this.code = 6009;
  }
};
codeToErrorMap3.set(6009, TooManyOptionTypesError3);
nameToErrorMap3.set("TooManyOptionTypes", TooManyOptionTypesError3);
var DuplicateSignatureError2 = class extends ProgramError {
  // 6010
  constructor(program, cause) {
    super("", program, cause);
    this.name = "DuplicateSignature";
    this.code = 6010;
  }
};
codeToErrorMap3.set(6010, DuplicateSignatureError2);
nameToErrorMap3.set("DuplicateSignature", DuplicateSignatureError2);
var ExpiredError2 = class extends ProgramError {
  // 6011
  constructor(program, cause) {
    super("", program, cause);
    this.name = "Expired";
    this.code = 6011;
  }
};
codeToErrorMap3.set(6011, ExpiredError2);
nameToErrorMap3.set("Expired", ExpiredError2);
var InvalidVidError2 = class extends ProgramError {
  // 6012
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidVid";
    this.code = 6012;
  }
};
codeToErrorMap3.set(6012, InvalidVidError2);
nameToErrorMap3.set("InvalidVid", InvalidVidError2);
var PausedError3 = class extends ProgramError {
  // 6013
  constructor(program, cause) {
    super("", program, cause);
    this.name = "Paused";
    this.code = 6013;
  }
};
codeToErrorMap3.set(6013, PausedError3);
nameToErrorMap3.set("Paused", PausedError3);
var UnexpiredExecuteHashError2 = class extends ProgramError {
  // 6014
  constructor(program, cause) {
    super("", program, cause);
    this.name = "UnexpiredExecuteHash";
    this.code = 6014;
  }
};
codeToErrorMap3.set(6014, UnexpiredExecuteHashError2);
nameToErrorMap3.set("UnexpiredExecuteHash", UnexpiredExecuteHashError2);
var InvalidAmountError7 = class extends ProgramError {
  // 6015
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidAmount";
    this.code = 6015;
  }
};
codeToErrorMap3.set(6015, InvalidAmountError7);
nameToErrorMap3.set("InvalidAmount", InvalidAmountError7);
var EidNotSupportedError3 = class extends ProgramError {
  // 6016
  constructor(program, cause) {
    super("", program, cause);
    this.name = "EidNotSupported";
    this.code = 6016;
  }
};
codeToErrorMap3.set(6016, EidNotSupportedError3);
nameToErrorMap3.set("EidNotSupported", EidNotSupportedError3);
function getDvnErrorFromCode(code, program, cause) {
  const constructor = codeToErrorMap3.get(code);
  return constructor ? new constructor(program, cause) : null;
}
function getDvnErrorFromName(name, program, cause) {
  const constructor = nameToErrorMap3.get(name);
  return constructor ? new constructor(program, cause) : null;
}

// src/generated/kinobi/dvn/events/index.ts
var events_exports6 = {};
__export(events_exports6, {
  getAdminConfigSetEventSerializer: () => getAdminConfigSetEventSerializer,
  getFeeWithdrawnEventSerializer: () => getFeeWithdrawnEventSerializer,
  getMultisigConfigSetEventSerializer: () => getMultisigConfigSetEventSerializer
});
function getAdminConfigSetEventSerializer() {
  return struct([["config", getAdminConfigSerializer()]], {
    description: "AdminConfigSetEvent"
  });
}
function getFeeWithdrawnEventSerializer() {
  return struct(
    [
      ["receiver", publicKey$1()],
      ["amount", u64$1()]
    ],
    { description: "FeeWithdrawnEvent" }
  );
}
function getMultisigConfigSetEventSerializer() {
  return struct(
    [["config", getMultisigConfigSerializer()]],
    { description: "MultisigConfigSetEvent" }
  );
}

// src/generated/kinobi/dvn/instructions/index.ts
var instructions_exports9 = {};
__export(instructions_exports9, {
  closeExecute: () => closeExecute,
  extendDvnConfig: () => extendDvnConfig,
  getCloseExecuteInstructionDataSerializer: () => getCloseExecuteInstructionDataSerializer,
  getExtendDvnConfigInstructionDataSerializer: () => getExtendDvnConfigInstructionDataSerializer,
  getInitDvnInstructionDataSerializer: () => getInitDvnInstructionDataSerializer,
  getInvokeInstructionDataSerializer: () => getInvokeInstructionDataSerializer,
  getQuoteDvnInstructionDataSerializer: () => getQuoteDvnInstructionDataSerializer,
  getSetConfigInstructionDataSerializer: () => getSetConfigInstructionDataSerializer3,
  getVerifiableInstructionDataSerializer: () => getVerifiableInstructionDataSerializer,
  getWithdrawFeeInstructionDataSerializer: () => getWithdrawFeeInstructionDataSerializer,
  initDvn: () => initDvn,
  invoke: () => invoke,
  quoteDvn: () => quoteDvn,
  setConfig: () => setConfig3,
  verifiable: () => verifiable,
  withdrawFee: () => withdrawFee
});
function expectSome(value) {
  if (value == null) {
    throw new Error("Expected a value but received null or undefined.");
  }
  return value;
}
function expectPublicKey(value) {
  if (!value) {
    throw new Error("Expected a PublicKey.");
  }
  return publicKey(value, false);
}
function expectPda(value) {
  if (!value || !Array.isArray(value) || !isPda(value)) {
    throw new Error("Expected a PDA.");
  }
  return value;
}
function getAccountMetasAndSigners3(accounts, optionalAccountStrategy, programId) {
  const keys = [];
  const signers = [];
  accounts.forEach((account) => {
    if (!account.value) {
      if (optionalAccountStrategy === "omitted") return;
      keys.push({ pubkey: programId, isSigner: false, isWritable: false });
      return;
    }
    if (isSigner(account.value)) {
      signers.push(account.value);
    }
    keys.push({
      pubkey: publicKey(account.value, false),
      isSigner: isSigner(account.value),
      isWritable: account.isWritable
    });
  });
  return [keys, signers];
}

// src/generated/kinobi/dvn/instructions/closeExecute.ts
function getCloseExecuteInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["digestHash", bytes$1({ size: 32 })]
      ],
      { description: "CloseExecuteInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([253, 220, 23, 127, 15, 171, 189, 226])
    })
  );
}
function closeExecute(context, input) {
  const programId = context.programs.getPublicKey(
    "dvn",
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    config: {
      index: 1,
      isWritable: true,
      value: input.config ?? null
    },
    executeHash: {
      index: 2,
      isWritable: true,
      value: input.executeHash ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners3(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getCloseExecuteInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getExtendDvnConfigInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [["discriminator", bytes$1({ size: 8 })]],
      { description: "ExtendDvnConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([183, 248, 24, 49, 110, 229, 169, 66])
    })
  );
}
function extendDvnConfig(context, input) {
  const programId = context.programs.getPublicKey(
    "dvn",
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true,
      value: input.admin ?? null
    },
    config: {
      index: 1,
      isWritable: true,
      value: input.config ?? null
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners3(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getExtendDvnConfigInstructionDataSerializer().serialize({});
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitDvnInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["vid", u32$1()],
        ["msglibs", array$1(publicKey$1())],
        ["priceFeed", publicKey$1()],
        ["signers", array$1(bytes$1({ size: 64 }))],
        ["quorum", u8$1()],
        ["admins", array$1(publicKey$1())]
      ],
      { description: "InitDvnInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([58, 108, 132, 12, 20, 10, 215, 210])
    })
  );
}
function initDvn(context, input) {
  const programId = context.programs.getPublicKey(
    "dvn",
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  );
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true,
      value: input.payer ?? null
    },
    config: {
      index: 1,
      isWritable: true,
      value: input.config ?? null
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners3(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitDvnInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInvokeInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["digest", getExecuteTransactionDigestSerializer()],
        ["signatures", array$1(array$1(u8$1(), { size: 65 }))]
      ],
      { description: "InvokeInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([26, 16, 169, 7, 21, 202, 242, 25])
    })
  );
}
function invoke(context, input) {
  const programId = context.programs.getPublicKey(
    "dvn",
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  );
  const resolvedAccounts = {
    signer: {
      index: 0,
      isWritable: true,
      value: input.signer ?? null
    },
    config: {
      index: 1,
      isWritable: true,
      value: input.config ?? null
    },
    executeHash: {
      index: 2,
      isWritable: true,
      value: input.executeHash ?? null
    },
    systemProgram: {
      index: 3,
      isWritable: false,
      value: input.systemProgram ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners3(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInvokeInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getQuoteDvnInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["msglib", publicKey$1()],
        ["dstEid", u32$1()],
        ["sender", publicKey$1()],
        ["packetHeader", bytes$1({ size: u32$1() })],
        ["payloadHash", bytes$1({ size: 32 })],
        ["confirmations", u64$1()],
        ["options", array$1(getLzOptionSerializer())]
      ],
      { description: "QuoteDvnInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([223, 98, 51, 200, 123, 133, 132, 194])
    })
  );
}
function quoteDvn(context, input) {
  const programId = context.programs.getPublicKey(
    "dvn",
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  );
  const resolvedAccounts = {
    dvnConfig: {
      index: 0,
      isWritable: false,
      value: input.dvnConfig ?? null
    },
    priceFeedProgram: {
      index: 1,
      isWritable: false,
      value: input.priceFeedProgram ?? null
    },
    priceFeedConfig: {
      index: 2,
      isWritable: false,
      value: input.priceFeedConfig ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners3(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getQuoteDvnInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetConfigInstructionDataSerializer3() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["config", getAdminConfigSerializer()]
      ],
      { description: "SetConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([108, 158, 154, 175, 212, 98, 52, 66])
    })
  );
}
function setConfig3(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "dvn",
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: accounts.admin ?? null
    },
    config: {
      index: 1,
      isWritable: true,
      value: accounts.config ?? null
    },
    eventAuthority: {
      index: 2,
      isWritable: false,
      value: accounts.eventAuthority ?? null
    },
    program: {
      index: 3,
      isWritable: false,
      value: accounts.program ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners3(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetConfigInstructionDataSerializer3().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getVerifiableInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packetHeader", array$1(u8$1(), { size: 81 })],
        ["payloadHash", bytes$1({ size: 32 })]
      ],
      { description: "VerifiableInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([249, 50, 227, 157, 238, 249, 211, 90])
    })
  );
}
function verifiable(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "dvn",
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  );
  const resolvedAccounts = {
    nonce: {
      index: 0,
      isWritable: false,
      value: accounts.nonce ?? null
    },
    payloadHash: {
      index: 1,
      isWritable: false,
      value: accounts.payloadHash ?? null
    },
    receiveConfig: {
      index: 2,
      isWritable: false,
      value: accounts.receiveConfig ?? null
    },
    defaultReceiveConfig: {
      index: 3,
      isWritable: false,
      value: accounts.defaultReceiveConfig ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners3(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getVerifiableInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getWithdrawFeeInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["amount", u64$1()]
      ],
      { description: "WithdrawFeeInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([14, 122, 231, 218, 31, 238, 223, 150])
    })
  );
}
function withdrawFee(context, input) {
  const programId = context.programs.getPublicKey(
    "dvn",
    "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    config: {
      index: 1,
      isWritable: true,
      value: input.config ?? null
    },
    receiver: {
      index: 2,
      isWritable: true,
      value: input.receiver ?? null
    },
    eventAuthority: {
      index: 3,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 4,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners3(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getWithdrawFeeInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}

// src/generated/kinobi/dvn/index.ts
var dvn_exports3 = {};
__export(dvn_exports3, {
  DVN_PROGRAM_ID: () => DVN_PROGRAM_ID,
  DuplicateSignatureError: () => DuplicateSignatureError2,
  EidNotSupportedError: () => EidNotSupportedError3,
  ExpiredError: () => ExpiredError2,
  InvalidAmountError: () => InvalidAmountError7,
  InvalidQuorumError: () => InvalidQuorumError2,
  InvalidSignatureLenError: () => InvalidSignatureLenError2,
  InvalidSignersLenError: () => InvalidSignersLenError2,
  InvalidVidError: () => InvalidVidError2,
  MsgLibNotAllowedError: () => MsgLibNotAllowedError3,
  NotAdminError: () => NotAdminError3,
  PausedError: () => PausedError3,
  SignatureErrorError: () => SignatureErrorError2,
  SignerNotInCommitteeError: () => SignerNotInCommitteeError2,
  TooManyAdminsError: () => TooManyAdminsError3,
  TooManyOptionTypesError: () => TooManyOptionTypesError3,
  UnexpiredExecuteHashError: () => UnexpiredExecuteHashError2,
  UniqueOwnersError: () => UniqueOwnersError2,
  VerificationState: () => VerificationState2,
  adminConfig: () => adminConfig,
  closeExecute: () => closeExecute,
  createDvnProgram: () => createDvnProgram,
  deserializeDvnConfig: () => deserializeDvnConfig,
  deserializeExecuteHash: () => deserializeExecuteHash,
  deserializeReceiveConfig: () => deserializeReceiveConfig,
  expectPda: () => expectPda,
  expectPublicKey: () => expectPublicKey,
  expectSome: () => expectSome,
  extendDvnConfig: () => extendDvnConfig,
  fetchAllDvnConfig: () => fetchAllDvnConfig,
  fetchAllExecuteHash: () => fetchAllExecuteHash,
  fetchAllReceiveConfig: () => fetchAllReceiveConfig,
  fetchDvnConfig: () => fetchDvnConfig,
  fetchExecuteHash: () => fetchExecuteHash,
  fetchReceiveConfig: () => fetchReceiveConfig,
  getAccountMetasAndSigners: () => getAccountMetasAndSigners3,
  getAclSerializer: () => getAclSerializer,
  getAdminConfigSerializer: () => getAdminConfigSerializer,
  getCloseExecuteInstructionDataSerializer: () => getCloseExecuteInstructionDataSerializer,
  getDstConfigSerializer: () => getDstConfigSerializer,
  getDvnConfigAccountDataSerializer: () => getDvnConfigAccountDataSerializer,
  getDvnConfigGpaBuilder: () => getDvnConfigGpaBuilder,
  getDvnErrorFromCode: () => getDvnErrorFromCode,
  getDvnErrorFromName: () => getDvnErrorFromName,
  getDvnProgram: () => getDvnProgram,
  getDvnProgramId: () => getDvnProgramId,
  getExecuteHashAccountDataSerializer: () => getExecuteHashAccountDataSerializer,
  getExecuteHashGpaBuilder: () => getExecuteHashGpaBuilder,
  getExecuteHashSize: () => getExecuteHashSize,
  getExecuteTransactionDigestSerializer: () => getExecuteTransactionDigestSerializer,
  getExtendDvnConfigInstructionDataSerializer: () => getExtendDvnConfigInstructionDataSerializer,
  getInitDvnInstructionDataSerializer: () => getInitDvnInstructionDataSerializer,
  getInvokeInstructionDataSerializer: () => getInvokeInstructionDataSerializer,
  getLzOptionSerializer: () => getLzOptionSerializer,
  getMultisigConfigSerializer: () => getMultisigConfigSerializer,
  getMultisigSerializer: () => getMultisigSerializer,
  getQuoteDvnInstructionDataSerializer: () => getQuoteDvnInstructionDataSerializer,
  getReceiveConfigAccountDataSerializer: () => getReceiveConfigAccountDataSerializer,
  getReceiveConfigGpaBuilder: () => getReceiveConfigGpaBuilder,
  getSetConfigInstructionDataSerializer: () => getSetConfigInstructionDataSerializer3,
  getTransactionAccountSerializer: () => getTransactionAccountSerializer,
  getUlnConfigSerializer: () => getUlnConfigSerializer,
  getVerifiableInstructionDataSerializer: () => getVerifiableInstructionDataSerializer,
  getVerificationStateSerializer: () => getVerificationStateSerializer,
  getWithdrawFeeInstructionDataSerializer: () => getWithdrawFeeInstructionDataSerializer,
  initDvn: () => initDvn,
  invoke: () => invoke,
  isAdminConfig: () => isAdminConfig,
  isMultisigConfig: () => isMultisigConfig,
  multisigConfig: () => multisigConfig,
  quoteDvn: () => quoteDvn,
  safeFetchAllDvnConfig: () => safeFetchAllDvnConfig,
  safeFetchAllExecuteHash: () => safeFetchAllExecuteHash,
  safeFetchAllReceiveConfig: () => safeFetchAllReceiveConfig,
  safeFetchDvnConfig: () => safeFetchDvnConfig,
  safeFetchExecuteHash: () => safeFetchExecuteHash,
  safeFetchReceiveConfig: () => safeFetchReceiveConfig,
  setConfig: () => setConfig3,
  verifiable: () => verifiable,
  withdrawFee: () => withdrawFee
});

// src/generated/kinobi/dvn/programs/dvn.ts
var DVN_PROGRAM_ID = "HtEYV4xB4wvsj5fgTkcfuChYpvGYzgzwvNhgDZQNh7wW";
function createDvnProgram() {
  return {
    name: "dvn",
    publicKey: DVN_PROGRAM_ID,
    getErrorFromCode(code, cause) {
      return getDvnErrorFromCode(code, this, cause);
    },
    getErrorFromName(name, cause) {
      return getDvnErrorFromName(name, this, cause);
    },
    isOnCluster() {
      return true;
    }
  };
}
function getDvnProgram(context, clusterFilter) {
  return context.programs.get("dvn", clusterFilter);
}
function getDvnProgramId(context, clusterFilter) {
  return context.programs.getPublicKey("dvn", DVN_PROGRAM_ID, clusterFilter);
}

// src/dvn.ts
var SolanaSignerExt2 = class _SolanaSignerExt {
  constructor(signers) {
    this.signers = signers;
  }
  static from(signers) {
    return new _SolanaSignerExt(signers);
  }
  async sign(message) {
    return Promise.all(
      this.signers.map(async (s) => {
        const rv = await s.signHash(message);
        return { signature: rv.slice(0, 64), recoveryId: rv[64] };
      })
    );
  }
};
var DVN2 = class {
  constructor(programId, rpc) {
    this.programId = programId;
    this.pda = new DvnPDA(programId);
    this.eventAuthority = new EventPDA(programId).eventAuthority()[0];
    if (rpc === void 0) {
      rpc = createNullRpc();
      rpc.getCluster = () => "custom";
    }
    this.programRepo = createDefaultProgramRepository({ rpc }, [
      {
        name: "dvn",
        publicKey: programId,
        getErrorFromCode(code, cause) {
          return getDvnErrorFromCode(code, this, cause);
        },
        getErrorFromName(name, cause) {
          return getDvnErrorFromName(name, this, cause);
        },
        isOnCluster() {
          return true;
        }
      }
    ]);
  }
  getProgram(clusterFilter = "custom") {
    return this.programRepo.get("dvn", clusterFilter);
  }
  async initDVN(rpc, payer, params) {
    const [config2] = this.pda.config();
    const info = await rpc.getAccount(config2);
    if (info.exists) {
      throw new Error("DVN already initialized");
    }
    return initDvn(
      { programs: this.programRepo, payer },
      {
        payer,
        config: config2,
        //
        ...params
      }
    ).items[0];
  }
  getDigest(vid, instruction, expiration) {
    return {
      vid,
      programId: instruction.programId,
      accounts: instruction.keys.map((key) => {
        return {
          pubkey: key.pubkey,
          isSigner: key.isSigner,
          isWritable: key.isWritable
        };
      }),
      data: instruction.data,
      expiration
    };
  }
  getExecuteHash(hashBytes) {
    return this.pda.executeHash(hashBytes)[0];
  }
  getHashBytes(digest) {
    const digestBytes = getExecuteTransactionDigestSerializer().serialize(digest);
    const hash = keccak_256(digestBytes);
    return Buffer.from(hash);
  }
  async invoke(rpc, payer, params, sign) {
    const configState = await this.getConfigState(rpc, "confirmed");
    if (!configState) {
      throw new Error("DVN not initialized");
    }
    const { vid, instruction, expiration } = params;
    const digest = this.getDigest(vid, instruction, expiration);
    const hashBytes = this.getHashBytes(digest);
    const executeHash = this.getExecuteHash(hashBytes);
    const signatures = [];
    const signResult = await sign.sign(hashBytes);
    signatures.push(...signResult.map((s) => Array.from(s.signature).concat([s.recoveryId])));
    const txBuilder = invoke(
      { programs: this.programRepo },
      {
        signer: payer,
        config: this.pda.config(),
        executeHash,
        program: this.programId,
        eventAuthority: this.eventAuthority,
        digest,
        signatures
      }
    );
    return txBuilder.addRemainingAccounts([
      {
        pubkey: instruction.programId,
        isSigner: false,
        isWritable: false
      },
      ...instruction.keys.map((key) => {
        key.isSigner = false;
        return key;
      })
    ]).items[0];
  }
  async setMultisigConfig(rpc, payer, params, sign) {
    const { vid, expiration } = params;
    const innerInstruction = {
      programId: this.programId,
      keys: [],
      data: Buffer.from(getMultisigConfigSerializer().serialize(params.config))
    };
    const digest = this.getDigest(vid, innerInstruction, expiration);
    return [
      await this.invoke(
        rpc,
        payer,
        {
          vid,
          instruction: innerInstruction,
          expiration
        },
        sign
      ),
      this.getHashBytes(digest)
    ];
  }
  // set admin config, can call by one of the admins
  setConfig(admin, adminConfig2) {
    return setConfig3(
      { programs: this.programRepo },
      {
        admin,
        config: this.pda.config(),
        program: this.programId,
        eventAuthority: this.eventAuthority
      },
      {
        config: adminConfig2
      }
    ).items[0];
  }
  setDstConfig(admin, dstConfigs) {
    return this.setConfig(admin, {
      __kind: "DstConfigs",
      fields: [dstConfigs]
    });
  }
  setAdmins(admin, admins) {
    return this.setConfig(admin, {
      __kind: "Admins",
      fields: [admins]
    });
  }
  setDefaultMultiplierBps(admin, defaultMultiplierBps) {
    return this.setConfig(admin, {
      __kind: "DefaultMultiplierBps",
      fields: [defaultMultiplierBps]
    });
  }
  setPriceFeed(admin, priceFeedProgram) {
    const [priceFeedPda] = new PriceFeedPDA(priceFeedProgram).priceFeed();
    return this.setConfig(admin, {
      fields: [priceFeedPda],
      __kind: "PriceFeed"
    });
  }
  _getExpiration() {
    return BigInt((/* @__PURE__ */ new Date()).getTime() / 1e3 + 120);
  }
  withdrawFee(admin, param) {
    return withdrawFee(
      { programs: this.programRepo },
      {
        admin,
        config: this.pda.config(),
        receiver: param.receiver,
        program: this.programId,
        eventAuthority: this.eventAuthority,
        amount: param.amount
      }
    ).items[0];
  }
  closeExecute(admin, digestHash) {
    return closeExecute(
      { programs: this.programRepo },
      {
        admin,
        config: this.pda.config(),
        executeHash: this.pda.executeHash(digestHash),
        digestHash
      }
    ).items[0];
  }
  quoteDvn(param) {
    const [priceFeed] = new PriceFeedPDA(param.priceFeedProgram).priceFeed();
    return quoteDvn(
      { programs: this.programRepo },
      {
        dvnConfig: this.pda.config(),
        priceFeedConfig: priceFeed,
        ...param
      }
    ).items[0];
  }
  async verifiable(rpc, params) {
    const { packetBytes } = params;
    const packet = PacketV1Codec.fromBytes(packetBytes);
    const receiver = publicKey(Buffer.from(packet.receiver().slice(2), "hex"));
    const srcEid = packet.srcEid();
    const sender = addressToBytes32(packet.sender());
    const packetNonce = parseInt(packet.nonce());
    const headerHashBytes = Uint8Array.from(Buffer.from(packet.headerHash().slice(2), "hex"));
    const payloadHashBytes = Uint8Array.from(Buffer.from(packet.payloadHash().slice(2), "hex"));
    const endpointPDA = new EndpointPDA(params.endpointProgram ?? endpoint_exports2.ENDPOINT_PROGRAM_ID);
    const ulnSDK = new uln_exports2.Uln(params.msgLibProgram ?? uln_exports2.ULN_PROGRAM_ID);
    const finalReceiveConfig = await ulnSDK.getFinalReceiveConfigState(rpc, receiver, srcEid, "confirmed");
    const confirmations = [
      ...finalReceiveConfig.requiredDvns,
      ...finalReceiveConfig.optionalDvns
    ].map((dvn) => {
      return {
        pubkey: ulnSDK.pda.confirmations(headerHashBytes, payloadHashBytes, dvn)[0],
        isSigner: false,
        isWritable: false
      };
    });
    return verifiable(
      { programs: this.programRepo },
      {
        nonce: endpointPDA.nonce(receiver, srcEid, sender),
        payloadHash: endpointPDA.payloadHash(receiver, srcEid, sender, packetNonce),
        receiveConfig: ulnSDK.pda.receiveConfig(srcEid, receiver),
        defaultReceiveConfig: ulnSDK.pda.defaultReceiveConfig(srcEid)
      },
      {
        packetHeader: Array.from(Uint8Array.from(Buffer.from(packet.header().slice(2), "hex"))),
        payloadHash: payloadHashBytes
      }
    ).addRemainingAccounts(confirmations).items[0];
  }
  getQuoteIXAccountMetaForCPI(priceFeedConfig, priceFeedProgram, payment) {
    const txBuilder = quoteDvn(
      { programs: this.programRepo },
      {
        dvnConfig: this.pda.config(),
        priceFeedConfig,
        priceFeedProgram,
        // param
        msglib: defaultPublicKey(),
        dstEid: 0,
        sender: defaultPublicKey(),
        packetHeader: new Uint8Array(4),
        payloadHash: new Uint8Array(32),
        confirmations: 0,
        options: []
      }
    );
    const { keys } = txBuilder.getInstructions()[0];
    if (payment) {
      keys[0].isWritable = true;
    }
    return [
      {
        pubkey: this.programId,
        isWritable: false,
        isSigner: false
      }
    ].concat(keys);
  }
  async getConfigState(rpc, commitment = "confirmed") {
    const [config2] = this.pda.config();
    return safeFetchDvnConfig({ rpc }, config2, { commitment });
  }
};

// src/executor.ts
var executor_exports2 = {};
__export(executor_exports2, {
  EXECUTOR_PROGRAM_ID: () => EXECUTOR_PROGRAM_ID,
  Executor: () => Executor2,
  accounts: () => accounts_exports10,
  errors: () => errors_exports8,
  events: () => events_exports7,
  instructions: () => instructions_exports10,
  types: () => types_exports10
});

// src/generated/kinobi/executor/accounts/index.ts
var accounts_exports10 = {};
__export(accounts_exports10, {
  deserializeExecutionContextV1: () => deserializeExecutionContextV1,
  deserializeExecutorConfig: () => deserializeExecutorConfig,
  deserializeNonce: () => deserializeNonce2,
  fetchAllExecutionContextV1: () => fetchAllExecutionContextV1,
  fetchAllExecutorConfig: () => fetchAllExecutorConfig,
  fetchAllNonce: () => fetchAllNonce2,
  fetchExecutionContextV1: () => fetchExecutionContextV1,
  fetchExecutorConfig: () => fetchExecutorConfig,
  fetchNonce: () => fetchNonce2,
  getExecutionContextV1AccountDataSerializer: () => getExecutionContextV1AccountDataSerializer,
  getExecutionContextV1GpaBuilder: () => getExecutionContextV1GpaBuilder,
  getExecutionContextV1Size: () => getExecutionContextV1Size,
  getExecutorConfigAccountDataSerializer: () => getExecutorConfigAccountDataSerializer,
  getExecutorConfigGpaBuilder: () => getExecutorConfigGpaBuilder,
  getNonceAccountDataSerializer: () => getNonceAccountDataSerializer2,
  getNonceGpaBuilder: () => getNonceGpaBuilder2,
  getNonceSize: () => getNonceSize2,
  safeFetchAllExecutionContextV1: () => safeFetchAllExecutionContextV1,
  safeFetchAllExecutorConfig: () => safeFetchAllExecutorConfig,
  safeFetchAllNonce: () => safeFetchAllNonce2,
  safeFetchExecutionContextV1: () => safeFetchExecutionContextV1,
  safeFetchExecutorConfig: () => safeFetchExecutorConfig,
  safeFetchNonce: () => safeFetchNonce2
});
function getExecutionContextV1AccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["initialPayerBalance", u64$1()],
        ["feeLimit", u64$1()]
      ],
      { description: "ExecutionContextV1AccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([132, 92, 176, 59, 141, 186, 141, 137])
    })
  );
}
function deserializeExecutionContextV1(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getExecutionContextV1AccountDataSerializer()
  );
}
async function fetchExecutionContextV1(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "ExecutionContextV1");
  return deserializeExecutionContextV1(maybeAccount);
}
async function safeFetchExecutionContextV1(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeExecutionContextV1(maybeAccount) : null;
}
async function fetchAllExecutionContextV1(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "ExecutionContextV1");
    return deserializeExecutionContextV1(maybeAccount);
  });
}
async function safeFetchAllExecutionContextV1(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeExecutionContextV1(maybeAccount)
  );
}
function getExecutionContextV1GpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    initialPayerBalance: [8, u64$1()],
    feeLimit: [16, u64$1()]
  }).deserializeUsing(
    (account) => deserializeExecutionContextV1(account)
  ).whereField(
    "discriminator",
    new Uint8Array([132, 92, 176, 59, 141, 186, 141, 137])
  );
}
function getExecutionContextV1Size() {
  return 24;
}

// src/generated/kinobi/executor/types/index.ts
var types_exports10 = {};
__export(types_exports10, {
  ExecutionState: () => ExecutionState2,
  adminSetConfigParams: () => adminSetConfigParams,
  getAclSerializer: () => getAclSerializer2,
  getAdminSetConfigParamsSerializer: () => getAdminSetConfigParamsSerializer,
  getDstConfigSerializer: () => getDstConfigSerializer2,
  getExecutionStateSerializer: () => getExecutionStateSerializer,
  getLzComposeParamsSerializer: () => getLzComposeParamsSerializer,
  getLzOptionSerializer: () => getLzOptionSerializer2,
  getLzReceiveParamsSerializer: () => getLzReceiveParamsSerializer,
  getNativeDropRequestSerializer: () => getNativeDropRequestSerializer,
  getOwnerSetConfigParamsSerializer: () => getOwnerSetConfigParamsSerializer,
  getVersionSerializer: () => getVersionSerializer2,
  isAdminSetConfigParams: () => isAdminSetConfigParams,
  isOwnerSetConfigParams: () => isOwnerSetConfigParams,
  ownerSetConfigParams: () => ownerSetConfigParams
});
function getAclSerializer2() {
  return struct(
    [
      ["allowList", array$1(publicKey$1())],
      ["denyList", array$1(publicKey$1())]
    ],
    { description: "Acl" }
  );
}
function getAdminSetConfigParamsSerializer() {
  return dataEnum(
    [
      [
        "PriceFeed",
        struct([
          ["fields", tuple([publicKey$1()])]
        ])
      ],
      [
        "DefaultMultiplierBps",
        struct([["fields", tuple([u16$1()])]])
      ],
      [
        "DstConfigs",
        struct([
          ["fields", tuple([array$1(getDstConfigSerializer2())])]
        ])
      ]
    ],
    { description: "AdminSetConfigParams" }
  );
}
function adminSetConfigParams(kind, data) {
  return Array.isArray(data) ? { __kind: kind, fields: data } : { __kind: kind, ...data ?? {} };
}
function isAdminSetConfigParams(kind, value) {
  return value.__kind === kind;
}
function getDstConfigSerializer2() {
  return struct(
    [
      ["eid", u32$1()],
      ["lzReceiveBaseGas", u32$1()],
      ["lzComposeBaseGas", u32$1()],
      ["multiplierBps", option(u16$1())],
      ["floorMarginUsd", option(u128())],
      ["nativeDropCap", u128()]
    ],
    { description: "DstConfig" }
  );
}
var ExecutionState2 = /* @__PURE__ */ ((ExecutionState3) => {
  ExecutionState3[ExecutionState3["NotExecutable"] = 0] = "NotExecutable";
  ExecutionState3[ExecutionState3["VerifiedButNotExecutable"] = 1] = "VerifiedButNotExecutable";
  ExecutionState3[ExecutionState3["Executable"] = 2] = "Executable";
  ExecutionState3[ExecutionState3["Executed"] = 3] = "Executed";
  return ExecutionState3;
})(ExecutionState2 || {});
function getExecutionStateSerializer() {
  return scalarEnum(ExecutionState2, {
    description: "ExecutionState"
  });
}
function getLzComposeParamsSerializer() {
  return struct(
    [
      ["from", publicKey$1()],
      ["to", publicKey$1()],
      ["guid", bytes$1({ size: 32 })],
      ["index", u16$1()],
      ["message", bytes$1({ size: u32$1() })],
      ["extraData", bytes$1({ size: u32$1() })]
    ],
    { description: "LzComposeParams" }
  );
}
function getLzOptionSerializer2() {
  return struct(
    [
      ["optionType", u8$1()],
      ["params", bytes$1({ size: u32$1() })]
    ],
    { description: "LzOption" }
  );
}
function getLzReceiveParamsSerializer() {
  return struct(
    [
      ["srcEid", u32$1()],
      ["sender", bytes$1({ size: 32 })],
      ["nonce", u64$1()],
      ["guid", bytes$1({ size: 32 })],
      ["message", bytes$1({ size: u32$1() })],
      ["extraData", bytes$1({ size: u32$1() })]
    ],
    { description: "LzReceiveParams" }
  );
}
function getNativeDropRequestSerializer() {
  return struct(
    [
      ["receiver", publicKey$1()],
      ["amount", u64$1()]
    ],
    { description: "NativeDropRequest" }
  );
}
function getOwnerSetConfigParamsSerializer() {
  return dataEnum(
    [
      [
        "Admins",
        struct([
          ["fields", tuple([array$1(publicKey$1())])]
        ])
      ],
      [
        "Executors",
        struct([
          ["fields", tuple([array$1(publicKey$1())])]
        ])
      ],
      [
        "Msglibs",
        struct([
          ["fields", tuple([array$1(publicKey$1())])]
        ])
      ],
      [
        "Owner",
        struct([
          ["fields", tuple([publicKey$1()])]
        ])
      ],
      [
        "Paused",
        struct([
          ["fields", tuple([bool$1()])]
        ])
      ],
      [
        "Allowlist",
        struct([
          ["fields", tuple([array$1(publicKey$1())])]
        ])
      ],
      [
        "Denylist",
        struct([
          ["fields", tuple([array$1(publicKey$1())])]
        ])
      ]
    ],
    { description: "OwnerSetConfigParams" }
  );
}
function ownerSetConfigParams(kind, data) {
  return Array.isArray(data) ? { __kind: kind, fields: data } : { __kind: kind, ...data ?? {} };
}
function isOwnerSetConfigParams(kind, value) {
  return value.__kind === kind;
}
function getVersionSerializer2() {
  return struct(
    [
      ["major", u64$1()],
      ["minor", u8$1()]
    ],
    { description: "Version" }
  );
}

// src/generated/kinobi/executor/accounts/executorConfig.ts
function getExecutorConfigAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["bump", u8$1()],
        ["owner", publicKey$1()],
        ["acl", getAclSerializer2()],
        ["admins", array$1(publicKey$1())],
        ["executors", array$1(publicKey$1())],
        ["msglibs", array$1(publicKey$1())],
        ["paused", bool$1()],
        ["defaultMultiplierBps", u16$1()],
        ["priceFeed", publicKey$1()],
        ["dstConfigs", array$1(getDstConfigSerializer2())]
      ],
      { description: "ExecutorConfigAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([134, 17, 226, 24, 10, 173, 157, 78])
    })
  );
}
function deserializeExecutorConfig(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getExecutorConfigAccountDataSerializer()
  );
}
async function fetchExecutorConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "ExecutorConfig");
  return deserializeExecutorConfig(maybeAccount);
}
async function safeFetchExecutorConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeExecutorConfig(maybeAccount) : null;
}
async function fetchAllExecutorConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "ExecutorConfig");
    return deserializeExecutorConfig(maybeAccount);
  });
}
async function safeFetchAllExecutorConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeExecutorConfig(maybeAccount)
  );
}
function getExecutorConfigGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    bump: [8, u8$1()],
    owner: [9, publicKey$1()],
    acl: [41, getAclSerializer2()],
    admins: [null, array$1(publicKey$1())],
    executors: [null, array$1(publicKey$1())],
    msglibs: [null, array$1(publicKey$1())],
    paused: [null, bool$1()],
    defaultMultiplierBps: [null, u16$1()],
    priceFeed: [null, publicKey$1()],
    dstConfigs: [null, array$1(getDstConfigSerializer2())]
  }).deserializeUsing(
    (account) => deserializeExecutorConfig(account)
  ).whereField(
    "discriminator",
    new Uint8Array([134, 17, 226, 24, 10, 173, 157, 78])
  );
}
function getNonceAccountDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["bump", u8$1()],
        ["outboundNonce", u64$1()],
        ["inboundNonce", u64$1()]
      ],
      { description: "NonceAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([143, 197, 147, 95, 106, 165, 50, 43])
    })
  );
}
function deserializeNonce2(rawAccount) {
  return deserializeAccount(rawAccount, getNonceAccountDataSerializer2());
}
async function fetchNonce2(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "Nonce");
  return deserializeNonce2(maybeAccount);
}
async function safeFetchNonce2(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeNonce2(maybeAccount) : null;
}
async function fetchAllNonce2(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "Nonce");
    return deserializeNonce2(maybeAccount);
  });
}
async function safeFetchAllNonce2(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map((maybeAccount) => deserializeNonce2(maybeAccount));
}
function getNonceGpaBuilder2(context) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    bump: [8, u8$1()],
    outboundNonce: [9, u64$1()],
    inboundNonce: [17, u64$1()]
  }).deserializeUsing((account) => deserializeNonce2(account)).whereField(
    "discriminator",
    new Uint8Array([143, 197, 147, 95, 106, 165, 50, 43])
  );
}
function getNonceSize2() {
  return 25;
}

// src/generated/kinobi/executor/errors/index.ts
var errors_exports8 = {};
__export(errors_exports8, {
  ContextAccountAlreadyInitializedError: () => ContextAccountAlreadyInitializedError,
  EidNotSupportedError: () => EidNotSupportedError4,
  ExecutorIsAdminError: () => ExecutorIsAdminError2,
  InsufficientBalanceError: () => InsufficientBalanceError2,
  InvalidInstructionSequenceError: () => InvalidInstructionSequenceError,
  InvalidNativeDropReceiverError: () => InvalidNativeDropReceiverError2,
  InvalidNativeDropRequestsLengthError: () => InvalidNativeDropRequestsLengthError2,
  InvalidOwnerError: () => InvalidOwnerError2,
  InvalidSizeError: () => InvalidSizeError3,
  MsgLibNotAllowedError: () => MsgLibNotAllowedError4,
  NativeAmountExceedsCapError: () => NativeAmountExceedsCapError2,
  NotAdminError: () => NotAdminError4,
  NotExecutorError: () => NotExecutorError2,
  PausedError: () => PausedError4,
  TooManyAdminsError: () => TooManyAdminsError4,
  TooManyExecutorsError: () => TooManyExecutorsError2,
  TooManyOptionTypesError: () => TooManyOptionTypesError4,
  UnsupportedOptionTypeError: () => UnsupportedOptionTypeError2,
  ZeroLzComposeGasProvidedError: () => ZeroLzComposeGasProvidedError2,
  ZeroLzReceiveGasProvidedError: () => ZeroLzReceiveGasProvidedError2,
  getExecutorErrorFromCode: () => getExecutorErrorFromCode,
  getExecutorErrorFromName: () => getExecutorErrorFromName
});
var codeToErrorMap4 = /* @__PURE__ */ new Map();
var nameToErrorMap4 = /* @__PURE__ */ new Map();
var InvalidSizeError3 = class extends ProgramError {
  // 6000
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidSize";
    this.code = 6e3;
  }
};
codeToErrorMap4.set(6e3, InvalidSizeError3);
nameToErrorMap4.set("InvalidSize", InvalidSizeError3);
var PausedError4 = class extends ProgramError {
  // 6001
  constructor(program, cause) {
    super("", program, cause);
    this.name = "Paused";
    this.code = 6001;
  }
};
codeToErrorMap4.set(6001, PausedError4);
nameToErrorMap4.set("Paused", PausedError4);
var UnsupportedOptionTypeError2 = class extends ProgramError {
  // 6002
  constructor(program, cause) {
    super("", program, cause);
    this.name = "UnsupportedOptionType";
    this.code = 6002;
  }
};
codeToErrorMap4.set(6002, UnsupportedOptionTypeError2);
nameToErrorMap4.set("UnsupportedOptionType", UnsupportedOptionTypeError2);
var ZeroLzComposeGasProvidedError2 = class extends ProgramError {
  // 6003
  constructor(program, cause) {
    super("", program, cause);
    this.name = "ZeroLzComposeGasProvided";
    this.code = 6003;
  }
};
codeToErrorMap4.set(6003, ZeroLzComposeGasProvidedError2);
nameToErrorMap4.set("ZeroLzComposeGasProvided", ZeroLzComposeGasProvidedError2);
var ZeroLzReceiveGasProvidedError2 = class extends ProgramError {
  // 6004
  constructor(program, cause) {
    super("", program, cause);
    this.name = "ZeroLzReceiveGasProvided";
    this.code = 6004;
  }
};
codeToErrorMap4.set(6004, ZeroLzReceiveGasProvidedError2);
nameToErrorMap4.set("ZeroLzReceiveGasProvided", ZeroLzReceiveGasProvidedError2);
var NativeAmountExceedsCapError2 = class extends ProgramError {
  // 6005
  constructor(program, cause) {
    super("", program, cause);
    this.name = "NativeAmountExceedsCap";
    this.code = 6005;
  }
};
codeToErrorMap4.set(6005, NativeAmountExceedsCapError2);
nameToErrorMap4.set("NativeAmountExceedsCap", NativeAmountExceedsCapError2);
var NotAdminError4 = class extends ProgramError {
  // 6006
  constructor(program, cause) {
    super("", program, cause);
    this.name = "NotAdmin";
    this.code = 6006;
  }
};
codeToErrorMap4.set(6006, NotAdminError4);
nameToErrorMap4.set("NotAdmin", NotAdminError4);
var NotExecutorError2 = class extends ProgramError {
  // 6007
  constructor(program, cause) {
    super("", program, cause);
    this.name = "NotExecutor";
    this.code = 6007;
  }
};
codeToErrorMap4.set(6007, NotExecutorError2);
nameToErrorMap4.set("NotExecutor", NotExecutorError2);
var MsgLibNotAllowedError4 = class extends ProgramError {
  // 6008
  constructor(program, cause) {
    super("", program, cause);
    this.name = "MsgLibNotAllowed";
    this.code = 6008;
  }
};
codeToErrorMap4.set(6008, MsgLibNotAllowedError4);
nameToErrorMap4.set("MsgLibNotAllowed", MsgLibNotAllowedError4);
var TooManyAdminsError4 = class extends ProgramError {
  // 6009
  constructor(program, cause) {
    super("", program, cause);
    this.name = "TooManyAdmins";
    this.code = 6009;
  }
};
codeToErrorMap4.set(6009, TooManyAdminsError4);
nameToErrorMap4.set("TooManyAdmins", TooManyAdminsError4);
var TooManyExecutorsError2 = class extends ProgramError {
  // 6010
  constructor(program, cause) {
    super("", program, cause);
    this.name = "TooManyExecutors";
    this.code = 6010;
  }
};
codeToErrorMap4.set(6010, TooManyExecutorsError2);
nameToErrorMap4.set("TooManyExecutors", TooManyExecutorsError2);
var TooManyOptionTypesError4 = class extends ProgramError {
  // 6011
  constructor(program, cause) {
    super("", program, cause);
    this.name = "TooManyOptionTypes";
    this.code = 6011;
  }
};
codeToErrorMap4.set(6011, TooManyOptionTypesError4);
nameToErrorMap4.set("TooManyOptionTypes", TooManyOptionTypesError4);
var InvalidNativeDropRequestsLengthError2 = class extends ProgramError {
  // 6012
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidNativeDropRequestsLength";
    this.code = 6012;
  }
};
codeToErrorMap4.set(6012, InvalidNativeDropRequestsLengthError2);
nameToErrorMap4.set(
  "InvalidNativeDropRequestsLength",
  InvalidNativeDropRequestsLengthError2
);
var InvalidNativeDropReceiverError2 = class extends ProgramError {
  // 6013
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidNativeDropReceiver";
    this.code = 6013;
  }
};
codeToErrorMap4.set(6013, InvalidNativeDropReceiverError2);
nameToErrorMap4.set("InvalidNativeDropReceiver", InvalidNativeDropReceiverError2);
var InsufficientBalanceError2 = class extends ProgramError {
  // 6014
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InsufficientBalance";
    this.code = 6014;
  }
};
codeToErrorMap4.set(6014, InsufficientBalanceError2);
nameToErrorMap4.set("InsufficientBalance", InsufficientBalanceError2);
var EidNotSupportedError4 = class extends ProgramError {
  // 6015
  constructor(program, cause) {
    super("", program, cause);
    this.name = "EidNotSupported";
    this.code = 6015;
  }
};
codeToErrorMap4.set(6015, EidNotSupportedError4);
nameToErrorMap4.set("EidNotSupported", EidNotSupportedError4);
var ExecutorIsAdminError2 = class extends ProgramError {
  // 6016
  constructor(program, cause) {
    super("", program, cause);
    this.name = "ExecutorIsAdmin";
    this.code = 6016;
  }
};
codeToErrorMap4.set(6016, ExecutorIsAdminError2);
nameToErrorMap4.set("ExecutorIsAdmin", ExecutorIsAdminError2);
var InvalidOwnerError2 = class extends ProgramError {
  // 6017
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidOwner";
    this.code = 6017;
  }
};
codeToErrorMap4.set(6017, InvalidOwnerError2);
nameToErrorMap4.set("InvalidOwner", InvalidOwnerError2);
var InvalidInstructionSequenceError = class extends ProgramError {
  // 6018
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidInstructionSequence";
    this.code = 6018;
  }
};
codeToErrorMap4.set(6018, InvalidInstructionSequenceError);
nameToErrorMap4.set(
  "InvalidInstructionSequence",
  InvalidInstructionSequenceError
);
var ContextAccountAlreadyInitializedError = class extends ProgramError {
  // 6019
  constructor(program, cause) {
    super("", program, cause);
    this.name = "ContextAccountAlreadyInitialized";
    this.code = 6019;
  }
};
codeToErrorMap4.set(6019, ContextAccountAlreadyInitializedError);
nameToErrorMap4.set(
  "ContextAccountAlreadyInitialized",
  ContextAccountAlreadyInitializedError
);
function getExecutorErrorFromCode(code, program, cause) {
  const constructor = codeToErrorMap4.get(code);
  return constructor ? new constructor(program, cause) : null;
}
function getExecutorErrorFromName(name, program, cause) {
  const constructor = nameToErrorMap4.get(name);
  return constructor ? new constructor(program, cause) : null;
}

// src/generated/kinobi/executor/events/index.ts
var events_exports7 = {};
__export(events_exports7, {
  getNativeDropAppliedEventSerializer: () => getNativeDropAppliedEventSerializer
});
function getNativeDropAppliedEventSerializer() {
  return struct(
    [
      ["srcEid", u32$1()],
      ["sender", bytes$1({ size: 32 })],
      ["nonce", u64$1()],
      ["dstEid", u32$1()],
      ["oapp", publicKey$1()],
      ["nativeDropRequests", array$1(getNativeDropRequestSerializer())],
      ["successes", array$1(bool$1())]
    ],
    { description: "NativeDropAppliedEvent" }
  );
}

// src/generated/kinobi/executor/instructions/index.ts
var instructions_exports10 = {};
__export(instructions_exports10, {
  adminSetConfig: () => adminSetConfig,
  compose: () => compose,
  executable: () => executable,
  execute: () => execute,
  extendExecutorConfig: () => extendExecutorConfig,
  getAdminSetConfigInstructionDataSerializer: () => getAdminSetConfigInstructionDataSerializer,
  getComposeInstructionDataSerializer: () => getComposeInstructionDataSerializer,
  getExecutableInstructionDataSerializer: () => getExecutableInstructionDataSerializer,
  getExecuteInstructionDataSerializer: () => getExecuteInstructionDataSerializer,
  getExtendExecutorConfigInstructionDataSerializer: () => getExtendExecutorConfigInstructionDataSerializer,
  getInitExecutorInstructionDataSerializer: () => getInitExecutorInstructionDataSerializer,
  getNativeDropInstructionDataSerializer: () => getNativeDropInstructionDataSerializer,
  getOwnerSetConfigInstructionDataSerializer: () => getOwnerSetConfigInstructionDataSerializer,
  getPostExecuteInstructionDataSerializer: () => getPostExecuteInstructionDataSerializer,
  getPreExecuteInstructionDataSerializer: () => getPreExecuteInstructionDataSerializer,
  getQuoteExecutorInstructionDataSerializer: () => getQuoteExecutorInstructionDataSerializer,
  getVersionInstructionDataSerializer: () => getVersionInstructionDataSerializer2,
  initExecutor: () => initExecutor,
  nativeDrop: () => nativeDrop,
  ownerSetConfig: () => ownerSetConfig,
  postExecute: () => postExecute,
  preExecute: () => preExecute,
  quoteExecutor: () => quoteExecutor,
  version: () => version2
});
function getAccountMetasAndSigners4(accounts, optionalAccountStrategy, programId) {
  const keys = [];
  const signers = [];
  accounts.forEach((account) => {
    if (!account.value) {
      keys.push({ pubkey: programId, isSigner: false, isWritable: false });
      return;
    }
    if (isSigner(account.value)) {
      signers.push(account.value);
    }
    keys.push({
      pubkey: publicKey(account.value, false),
      isSigner: isSigner(account.value),
      isWritable: account.isWritable
    });
  });
  return [keys, signers];
}

// src/generated/kinobi/executor/instructions/adminSetConfig.ts
function getAdminSetConfigInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["params", getAdminSetConfigParamsSerializer()]
      ],
      { description: "AdminSetConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([35, 56, 160, 84, 132, 156, 27, 79])
    })
  );
}
function adminSetConfig(context, input) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    config: {
      index: 1,
      isWritable: true,
      value: input.config ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getAdminSetConfigInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getComposeInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["lzCompose", getLzComposeParamsSerializer()],
        ["computeUnits", u64$1()],
        ["value", u64$1()]
      ],
      { description: "ComposeInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([106, 64, 131, 142, 7, 159, 42, 15])
    })
  );
}
function compose(context, input) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {
    executor: {
      index: 0,
      isWritable: true,
      value: input.executor ?? null
    },
    config: {
      index: 1,
      isWritable: false,
      value: input.config ?? null
    },
    endpointProgram: {
      index: 2,
      isWritable: false,
      value: input.endpointProgram ?? null
    },
    endpointEventAuthority: {
      index: 3,
      isWritable: false,
      value: input.endpointEventAuthority ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getComposeInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getExecutableInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["receiver", publicKey$1()],
        ["srcEid", u32$1()],
        ["sender", bytes$1({ size: 32 })],
        ["nonce", u64$1()]
      ],
      { description: "ExecutableInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([57, 165, 171, 234, 157, 191, 156, 25])
    })
  );
}
function executable(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {
    nonce: {
      index: 0,
      isWritable: false,
      value: accounts.nonce ?? null
    },
    payloadHash: {
      index: 1,
      isWritable: false,
      value: accounts.payloadHash ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getExecutableInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getExecuteInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["receiver", publicKey$1()],
        ["lzReceive", getLzReceiveParamsSerializer()],
        ["value", u64$1()],
        ["computeUnits", u64$1()]
      ],
      { description: "ExecuteInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([130, 221, 242, 154, 13, 193, 189, 29])
    })
  );
}
function execute(context, input) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {
    executor: {
      index: 0,
      isWritable: true,
      value: input.executor ?? null
    },
    config: {
      index: 1,
      isWritable: false,
      value: input.config ?? null
    },
    endpointProgram: {
      index: 2,
      isWritable: false,
      value: input.endpointProgram ?? null
    },
    endpointEventAuthority: {
      index: 3,
      isWritable: false,
      value: input.endpointEventAuthority ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getExecuteInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getExtendExecutorConfigInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [["discriminator", bytes$1({ size: 8 })]],
      { description: "ExtendExecutorConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([238, 223, 196, 220, 61, 71, 72, 85])
    })
  );
}
function extendExecutorConfig(context, input) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true,
      value: input.admin ?? null
    },
    config: {
      index: 1,
      isWritable: true,
      value: input.config ?? null
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getExtendExecutorConfigInstructionDataSerializer().serialize({});
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitExecutorInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["owner", publicKey$1()],
        ["admins", array$1(publicKey$1())],
        ["executors", array$1(publicKey$1())],
        ["msglibs", array$1(publicKey$1())],
        ["priceFeed", publicKey$1()]
      ],
      { description: "InitExecutorInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([195, 68, 116, 173, 55, 173, 159, 31])
    })
  );
}
function initExecutor(context, input) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true,
      value: input.payer ?? null
    },
    config: {
      index: 1,
      isWritable: true,
      value: input.config ?? null
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitExecutorInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getNativeDropInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["srcEid", u32$1()],
        ["sender", bytes$1({ size: 32 })],
        ["nonce", u64$1()],
        ["dstEid", u32$1()],
        ["oapp", publicKey$1()],
        ["nativeDropRequests", array$1(getNativeDropRequestSerializer())]
      ],
      { description: "NativeDropInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([105, 30, 218, 121, 155, 234, 85, 12])
    })
  );
}
function nativeDrop(context, input) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {
    executor: {
      index: 0,
      isWritable: true,
      value: input.executor ?? null
    },
    config: {
      index: 1,
      isWritable: true,
      value: input.config ?? null
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    },
    eventAuthority: {
      index: 3,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 4,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getNativeDropInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getOwnerSetConfigInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["params", getOwnerSetConfigParamsSerializer()]
      ],
      { description: "OwnerSetConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([99, 197, 129, 19, 164, 164, 183, 123])
    })
  );
}
function ownerSetConfig(context, input) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {
    owner: {
      index: 0,
      isWritable: false,
      value: input.owner ?? null
    },
    config: {
      index: 1,
      isWritable: true,
      value: input.config ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getOwnerSetConfigInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getPostExecuteInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["contextVersion", u8$1()]
      ],
      { description: "PostExecuteInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([74, 168, 167, 167, 76, 220, 95, 56])
    })
  );
}
function postExecute(context, input) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {
    executor: {
      index: 0,
      isWritable: false,
      value: input.executor ?? null
    },
    context: {
      index: 1,
      isWritable: true,
      value: input.context ?? null
    },
    instructionSysvar: {
      index: 2,
      isWritable: false,
      value: input.instructionSysvar ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.instructionSysvar.value) {
    resolvedAccounts.instructionSysvar.value = publicKey(
      "Sysvar1nstructions1111111111111111111111111"
    );
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getPostExecuteInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getPreExecuteInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["contextVersion", u8$1()],
        ["feeLimit", u64$1()]
      ],
      { description: "PreExecuteInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([207, 125, 18, 148, 72, 192, 198, 83])
    })
  );
}
function preExecute(context, input) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {
    executor: {
      index: 0,
      isWritable: true,
      value: input.executor ?? null
    },
    context: {
      index: 1,
      isWritable: true,
      value: input.context ?? null
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    },
    instructionSysvar: {
      index: 3,
      isWritable: false,
      value: input.instructionSysvar ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  if (!resolvedAccounts.instructionSysvar.value) {
    resolvedAccounts.instructionSysvar.value = publicKey(
      "Sysvar1nstructions1111111111111111111111111"
    );
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getPreExecuteInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getQuoteExecutorInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["msglib", publicKey$1()],
        ["dstEid", u32$1()],
        ["sender", publicKey$1()],
        ["calldataSize", u64$1()],
        ["options", array$1(getLzOptionSerializer2())]
      ],
      { description: "QuoteExecutorInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([84, 246, 255, 191, 82, 65, 164, 92])
    })
  );
}
function quoteExecutor(context, input) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {
    executorConfig: {
      index: 0,
      isWritable: false,
      value: input.executorConfig ?? null
    },
    priceFeedProgram: {
      index: 1,
      isWritable: false,
      value: input.priceFeedProgram ?? null
    },
    priceFeedConfig: {
      index: 2,
      isWritable: false,
      value: input.priceFeedConfig ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getQuoteExecutorInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getVersionInstructionDataSerializer2() {
  return mapSerializer(
    struct([["discriminator", bytes$1({ size: 8 })]], {
      description: "VersionInstructionData"
    }),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([118, 65, 195, 198, 129, 216, 252, 192])
    })
  );
}
function version2(context) {
  const programId = context.programs.getPublicKey(
    "executor",
    "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn"
  );
  const resolvedAccounts = {};
  const orderedAccounts = Object.values(
    resolvedAccounts
  );
  const [keys, signers] = getAccountMetasAndSigners4(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getVersionInstructionDataSerializer2().serialize({});
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}

// src/generated/kinobi/executor/programs/executor.ts
var EXECUTOR_PROGRAM_ID = "6doghB248px58JSSwG4qejQ46kFMW4AMj7vzJnWZHNZn";
var MSG_TYPE_OFFSET2 = 0;
var MessageType2 = /* @__PURE__ */ ((MessageType3) => {
  MessageType3[MessageType3["VANILLA"] = 1] = "VANILLA";
  MessageType3[MessageType3["COMPOSED_TYPE"] = 2] = "COMPOSED_TYPE";
  MessageType3[MessageType3["ABA"] = 3] = "ABA";
  return MessageType3;
})(MessageType2 || {});
function getLzReceiveParamsSerializer2() {
  return struct(
    [
      ["srcEid", u32$1()],
      ["sender", bytes$1({ size: 32 })],
      ["nonce", u64$1()],
      ["guid", bytes$1({ size: 32 })],
      ["message", bytes$1({ size: u32$1() })],
      ["callerParams", bytes$1({ size: u32$1() })]
    ],
    { description: "LzReceiveParams" }
  );
}
function getLzComposeParamsSerializer2() {
  return struct(
    [
      ["from", publicKey$1()],
      ["to", publicKey$1()],
      ["guid", bytes$1({ size: 32 })],
      ["index", u16$1()],
      ["message", bytes$1({ size: u32$1() })],
      ["extraData", bytes$1({ size: u32$1() })]
    ],
    { description: "LzComposeParams" }
  );
}
function getLzReceiveAccountSerializer() {
  return struct(
    [
      ["pubkey", publicKey$1()],
      ["isSigner", bool$1()],
      ["isWritable", bool$1()]
    ],
    { description: "LzReceiveAccount" }
  );
}
var ExecutorOptionType2 = /* @__PURE__ */ ((ExecutorOptionType3) => {
  ExecutorOptionType3[ExecutorOptionType3["PlaceHolder"] = 0] = "PlaceHolder";
  ExecutorOptionType3[ExecutorOptionType3["LzReceive"] = 1] = "LzReceive";
  ExecutorOptionType3[ExecutorOptionType3["NativeDrop"] = 2] = "NativeDrop";
  ExecutorOptionType3[ExecutorOptionType3["LzCompose"] = 3] = "LzCompose";
  ExecutorOptionType3[ExecutorOptionType3["OrderExecution"] = 4] = "OrderExecution";
  return ExecutorOptionType3;
})(ExecutorOptionType2 || {});
var MaxExecutorOptionTypeLength2 = 10;
function getAddressLocatorSerializer() {
  return dataEnum(
    [
      [
        "Address",
        struct([["fields", tuple([publicKey$1()])]])
      ],
      ["AltIndex", struct([["fields", tuple([u8$1(), u8$1()])]])],
      ["Payer", unit()],
      ["Signer", struct([["fields", tuple([u8$1()])]])],
      ["Context", unit()]
    ],
    { description: "AddressLocator" }
  );
}
function getLzReceiveTypesV2AccountsSerializer() {
  return struct([["accounts", array$1(publicKey$1())]], {
    description: "LzReceiveTypesV2Result"
  });
}
function getLzComposeTypesV2AccountsSerializer() {
  return struct([["accounts", array$1(publicKey$1())]], {
    description: "LzComposeTypesV2Accounts"
  });
}
function getAccountMetaRefSerializer() {
  return struct(
    [
      ["pubkey", getAddressLocatorSerializer()],
      ["isWritable", bool$1()]
    ],
    { description: "AccountMetaRef" }
  );
}
function getInstructionForLzReceiveSerializer() {
  return dataEnum(
    [
      [
        "LzReceive",
        struct([
          ["accounts", array$1(getAccountMetaRefSerializer())]
        ])
      ],
      [
        "Standard",
        struct([
          ["programId", publicKey$1()],
          ["accounts", array$1(getAccountMetaRefSerializer())],
          ["data", bytes$1({ size: u32$1() })]
        ])
      ]
    ],
    { description: "Instruction" }
  );
}
function getInstructionForLzComposeSerializer() {
  return dataEnum(
    [
      [
        "LzCompose",
        struct([
          ["accounts", array$1(getAccountMetaRefSerializer())]
        ])
      ],
      [
        "Standard",
        struct([
          ["programId", publicKey$1()],
          ["accounts", array$1(getAccountMetaRefSerializer())],
          ["data", bytes$1({ size: u32$1() })]
        ])
      ]
    ],
    { description: "Instruction" }
  );
}
function getLzReceiveTypesV2ResultSerializer() {
  return struct(
    [
      ["contextVersion", u8$1()],
      ["alts", array$1(publicKey$1())],
      ["instructions", array$1(getInstructionForLzReceiveSerializer())]
    ],
    { description: "LzReceiveTypesV2Result" }
  );
}
function getLzComposeTypesV2ResultSerializer() {
  return struct(
    [
      ["contextVersion", u8$1()],
      ["alts", array$1(publicKey$1())],
      ["instructions", array$1(getInstructionForLzComposeSerializer())]
    ],
    { description: "LzComposeTypesV2Result" }
  );
}
function addressLocator(kind, data) {
  return Array.isArray(data) ? { __kind: kind, fields: data } : { __kind: kind, ...data ?? {} };
}
function isAddressLocator(kind, value) {
  return value.__kind === kind;
}

// src/generated/kinobi/uln/events/index.ts
var events_exports8 = {};
__export(events_exports8, {
  getAdminTransferredEventSerializer: () => getAdminTransferredEventSerializer2,
  getConfigSetEventSerializer: () => getConfigSetEventSerializer,
  getDefaultConfigSetEventSerializer: () => getDefaultConfigSetEventSerializer,
  getFeesPaidEventSerializer: () => getFeesPaidEventSerializer,
  getPayloadVerifiedEventSerializer: () => getPayloadVerifiedEventSerializer,
  getRentWithdrawnEventSerializer: () => getRentWithdrawnEventSerializer2,
  getTreasurySetEventSerializer: () => getTreasurySetEventSerializer
});
function getAdminTransferredEventSerializer2() {
  return struct([["newAdmin", publicKey$1()]], {
    description: "AdminTransferredEvent"
  });
}

// src/generated/kinobi/uln/types/index.ts
var types_exports11 = {};
__export(types_exports11, {
  config: () => config,
  getConfigSerializer: () => getConfigSerializer,
  getExecutorConfigSerializer: () => getExecutorConfigSerializer,
  getLzTokenTreasurySerializer: () => getLzTokenTreasurySerializer,
  getMessagingFeeSerializer: () => getMessagingFeeSerializer3,
  getPacketSerializer: () => getPacketSerializer2,
  getTreasuryFeeSerializer: () => getTreasuryFeeSerializer,
  getTreasurySerializer: () => getTreasurySerializer,
  getUlnConfigSerializer: () => getUlnConfigSerializer2,
  getVersionSerializer: () => getVersionSerializer3,
  getWorkerFeeSerializer: () => getWorkerFeeSerializer,
  isConfig: () => isConfig
});
function getConfigSerializer() {
  return dataEnum(
    [
      [
        "SendUln",
        struct([
          ["fields", tuple([getUlnConfigSerializer2()])]
        ])
      ],
      [
        "ReceiveUln",
        struct([
          ["fields", tuple([getUlnConfigSerializer2()])]
        ])
      ],
      [
        "Executor",
        struct([
          ["fields", tuple([getExecutorConfigSerializer()])]
        ])
      ]
    ],
    { description: "Config" }
  );
}
function config(kind, data) {
  return Array.isArray(data) ? { __kind: kind, fields: data } : { __kind: kind, ...data ?? {} };
}
function isConfig(kind, value) {
  return value.__kind === kind;
}
function getExecutorConfigSerializer() {
  return struct(
    [
      ["maxMessageSize", u32$1()],
      ["executor", publicKey$1()]
    ],
    { description: "ExecutorConfig" }
  );
}
function getLzTokenTreasurySerializer() {
  return struct(
    [
      ["receiver", publicKey$1()],
      ["fee", u64$1()]
    ],
    { description: "LzTokenTreasury" }
  );
}
function getMessagingFeeSerializer3() {
  return struct(
    [
      ["nativeFee", u64$1()],
      ["lzTokenFee", u64$1()]
    ],
    { description: "MessagingFee" }
  );
}
function getPacketSerializer2() {
  return struct(
    [
      ["nonce", u64$1()],
      ["srcEid", u32$1()],
      ["sender", publicKey$1()],
      ["dstEid", u32$1()],
      ["receiver", bytes$1({ size: 32 })],
      ["guid", bytes$1({ size: 32 })],
      ["message", bytes$1({ size: u32$1() })]
    ],
    { description: "Packet" }
  );
}
function getTreasurySerializer() {
  return struct(
    [
      ["admin", option(publicKey$1())],
      ["nativeReceiver", publicKey$1()],
      ["nativeFeeBps", u64$1()],
      ["lzToken", option(getLzTokenTreasurySerializer())]
    ],
    { description: "Treasury" }
  );
}
function getTreasuryFeeSerializer() {
  return struct(
    [
      ["treasury", publicKey$1()],
      ["fee", u64$1()],
      ["payInLzToken", bool$1()]
    ],
    { description: "TreasuryFee" }
  );
}
function getUlnConfigSerializer2() {
  return struct(
    [
      ["confirmations", u64$1()],
      ["requiredDvnCount", u8$1()],
      ["optionalDvnCount", u8$1()],
      ["optionalDvnThreshold", u8$1()],
      ["requiredDvns", array$1(publicKey$1())],
      ["optionalDvns", array$1(publicKey$1())]
    ],
    { description: "UlnConfig" }
  );
}
function getVersionSerializer3() {
  return struct(
    [
      ["major", u64$1()],
      ["minor", u8$1()],
      ["endpointVersion", u8$1()]
    ],
    { description: "Version" }
  );
}
function getWorkerFeeSerializer() {
  return struct(
    [
      ["worker", publicKey$1()],
      ["fee", u64$1()]
    ],
    { description: "WorkerFee" }
  );
}

// src/generated/kinobi/uln/events/configSetEvent.ts
function getConfigSetEventSerializer() {
  return struct(
    [
      ["oapp", publicKey$1()],
      ["eid", u32$1()],
      ["config", getConfigSerializer()]
    ],
    { description: "ConfigSetEvent" }
  );
}
function getDefaultConfigSetEventSerializer() {
  return struct(
    [
      ["eid", u32$1()],
      ["sendUlnConfig", option(getUlnConfigSerializer2())],
      ["receiveUlnConfig", option(getUlnConfigSerializer2())],
      ["executorConfig", option(getExecutorConfigSerializer())]
    ],
    { description: "DefaultConfigSetEvent" }
  );
}
function getFeesPaidEventSerializer() {
  return struct(
    [
      ["executor", getWorkerFeeSerializer()],
      ["dvns", array$1(getWorkerFeeSerializer())],
      ["treasury", option(getTreasuryFeeSerializer())]
    ],
    { description: "FeesPaidEvent" }
  );
}
function getPayloadVerifiedEventSerializer() {
  return struct(
    [
      ["dvn", publicKey$1()],
      ["header", array$1(u8$1(), { size: 81 })],
      ["confirmations", u64$1()],
      ["proofHash", bytes$1({ size: 32 })]
    ],
    { description: "PayloadVerifiedEvent" }
  );
}
function getRentWithdrawnEventSerializer2() {
  return struct(
    [
      ["receiver", publicKey$1()],
      ["amount", u64$1()]
    ],
    { description: "RentWithdrawnEvent" }
  );
}
function getTreasurySetEventSerializer() {
  return struct(
    [["treasury", option(getTreasurySerializer())]],
    { description: "TreasurySetEvent" }
  );
}

// src/utility.ts
function getEventDiscriminator2(event) {
  return Buffer.from(sha2_256(`event:${event}`)).subarray(0, 8).toString("hex");
}
async function extractPacketSentEventByTxHash(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature2(
    connection,
    program,
    signature,
    getPacketSentEventSerializer(),
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractVerifiedPacketEventByTxHash2(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature2(
    connection,
    program,
    signature,
    getPacketVerifiedEventSerializer(),
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractReceivedPacketEventByTxHash2(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature2(
    connection,
    program,
    signature,
    getPacketDeliveredEventSerializer(),
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractComposeSentEventByTxHash2(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature2(
    connection,
    program,
    signature,
    getComposeSentEventSerializer(),
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractComposeDeliveredEventByTxHash2(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature2(
    connection,
    program,
    signature,
    getComposeDeliveredEventSerializer(),
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractWorkerFeePaidEventByTxHash2(connection, program, signature, commitment, unsafeParseErr = false) {
  const events = await extractEventFromTransactionSignature2(
    connection,
    program,
    signature,
    getFeesPaidEventSerializer(),
    commitment,
    unsafeParseErr
  );
  if (!events) return null;
  return events;
}
async function extractEventFromTransactionSignature2(rpc, _program, signature, serializer, commitment, unsafeParseErr = false) {
  let program;
  if (typeof _program === "string" && isPublicKey(_program)) {
    program = toWeb3JsPublicKey(_program);
  } else {
    program = _program;
  }
  let tx;
  if (typeof signature === "string") {
    const connection = toWeb3Connection(rpc);
    tx = await connection.getParsedTransaction(signature, commitment);
  } else {
    tx = signature;
  }
  if (tx == null) return null;
  const events = new Array();
  if (tx.meta?.err !== null && !unsafeParseErr) {
    return null;
  }
  for (const instruction of tx.meta?.innerInstructions ?? []) {
    for (const innerInstruction of instruction.instructions) {
      const inst = innerInstruction;
      if (!inst.programId.equals(program)) {
        continue;
      }
      const decoded = base58$1.serialize(inst.data);
      const discriminator = Buffer.from(decoded.subarray(0, 8)).toString("hex");
      if (discriminator !== EventEmitDiscriminator2) {
        continue;
      }
      const eventDiscriminator = Buffer.from(decoded.subarray(8, 16)).toString("hex");
      if (eventDiscriminator !== getEventDiscriminator2(serializer.description)) {
        continue;
      }
      events.push(serializer.deserialize(decoded, 16)[0]);
    }
  }
  return events.length > 0 ? events : null;
}
async function generateAddressLookupTable2(connection, payer, authority, addresses) {
  const slot = await connection.getSlot("finalized");
  const [createInstruction, lookupTableAddress] = web3108.AddressLookupTableProgram.createLookupTable({
    payer,
    authority,
    recentSlot: slot
  });
  const extendInstruction = web3108.AddressLookupTableProgram.extendLookupTable({
    payer,
    authority,
    lookupTable: lookupTableAddress,
    addresses
  });
  return {
    instructions: [createInstruction, extendInstruction],
    address: lookupTableAddress
  };
}
function deactivateLookupTable2(authority, lookupTable) {
  return web3108.AddressLookupTableProgram.deactivateLookupTable({
    authority,
    lookupTable
  });
}
function closeLookupTable2(recipient, authority, lookupTable) {
  return web3108.AddressLookupTableProgram.closeLookupTable({
    authority,
    recipient,
    lookupTable
  });
}
async function txWithAddressLookupTable2(connection, payer, instructions, recentBlockHash, _tableAddrs) {
  recentBlockHash = recentBlockHash ?? (await connection.getLatestBlockhash()).blockhash;
  if (!_tableAddrs) {
    return new web3108.VersionedTransaction(
      new web3108.TransactionMessage({
        instructions,
        payerKey: payer,
        recentBlockhash: recentBlockHash
      }).compileToV0Message()
    );
  }
  const tableAddrs = Array.isArray(_tableAddrs) ? _tableAddrs : [_tableAddrs];
  const lookupTableAccountsResult = await connection.getMultipleAccountsInfo(tableAddrs);
  const lookupTableAccounts = lookupTableAccountsResult.map((result, index) => {
    if (result === null) return null;
    return new web3108.AddressLookupTableAccount({
      key: tableAddrs[index],
      state: web3108.AddressLookupTableAccount.deserialize(result.data)
    });
  }).filter((account) => account !== null);
  return new web3108.VersionedTransaction(
    new web3108.TransactionMessage({
      instructions,
      payerKey: payer,
      recentBlockhash: recentBlockHash
    }).compileToV0Message(lookupTableAccounts.length > 0 ? lookupTableAccounts : void 0)
  );
}
async function createNonceAccountTX2(connection, auth, lamportsForRent) {
  const nonceAccount = web3108.Keypair.generate();
  const lamports = lamportsForRent ?? await connection.getMinimumBalanceForRentExemption(web3108.NONCE_ACCOUNT_LENGTH);
  const tx = new web3108.Transaction();
  tx.add(
    // create nonce account
    web3108.SystemProgram.createAccount({
      fromPubkey: auth,
      newAccountPubkey: nonceAccount.publicKey,
      lamports,
      space: web3108.NONCE_ACCOUNT_LENGTH,
      programId: web3108.SystemProgram.programId
    }),
    // init nonce account
    web3108.SystemProgram.nonceInitialize({
      noncePubkey: nonceAccount.publicKey,
      // nonce account pubkey
      authorizedPubkey: auth
      // nonce account auth
    })
  );
  return { tx, nonceAccount };
}
async function txWithNonce2(connection, noncePubkey, instructions, nonceInfo) {
  const tx = new web3108.Transaction();
  if (!nonceInfo) {
    const accountInfo = await connection.getAccountInfo(noncePubkey);
    if (accountInfo) {
      nonceInfo = web3108.NonceAccount.fromAccountData(accountInfo.data);
    } else {
      return null;
    }
  }
  tx.add(
    // nonce advance must be the first insturction
    web3108.SystemProgram.nonceAdvance({
      noncePubkey,
      authorizedPubkey: nonceInfo.authorizedPubkey
    })
  );
  tx.add(...instructions);
  tx.recentBlockhash = nonceInfo.nonce;
  return tx;
}
async function isAccountInitialized2(connection, account, commitmentOrConfig) {
  return connection.getAccountInfo(account, commitmentOrConfig).then((res) => {
    return res !== null;
  });
}
async function buildMessageV02(connection, payerKey, instructions, commitmentOrConfig = "confirmed", blockhash) {
  return new web3108.TransactionMessage({
    payerKey,
    recentBlockhash: blockhash ?? (await connection.getLatestBlockhash(commitmentOrConfig)).blockhash,
    instructions
  }).compileToV0Message();
}
async function buildVersionedTransaction2(connection, payerKey, instructions, commitmentOrConfig = "confirmed", blockhash, lookupTableAddresses) {
  if (lookupTableAddresses) {
    return txWithAddressLookupTable2(connection, payerKey, instructions, blockhash, lookupTableAddresses);
  }
  return new web3108.VersionedTransaction(
    await buildMessageV02(connection, payerKey, instructions, commitmentOrConfig, blockhash)
  );
}
function instructionDiscriminator2(method) {
  return Buffer.from(sha2_256(Buffer.from(`global:${method}`))).subarray(0, 8);
}
async function simulateWeb3JsTransaction(_connection, _instructions, _programId, _payer, serializer, commitment = "confirmed", blockhash, _lookupTableAddresses) {
  let connection;
  if (typeof _connection === "string") {
    connection = new web3108.Connection(_connection, commitment);
  } else {
    connection = toWeb3Connection(_connection);
  }
  let instructions;
  if (_instructions.length === 0) {
    throw new Error("Instructions cannot be empty");
  }
  if (_instructions[0] instanceof web3108.TransactionInstruction) {
    instructions = _instructions;
  } else if ("bytesCreatedOnChain" in _instructions[0]) {
    instructions = _instructions.map((i) => toWeb3JsInstruction(i.instruction));
  } else {
    instructions = _instructions.map((i) => toWeb3JsInstruction(i));
  }
  let programId;
  if (_programId instanceof web3108.PublicKey) {
    programId = _programId;
  } else {
    programId = toWeb3JsPublicKey(_programId);
  }
  let payer;
  if (_payer instanceof web3108.PublicKey) {
    payer = _payer;
  } else {
    payer = toWeb3JsPublicKey(_payer);
  }
  const lookupTableAddresses = _lookupTableAddresses && (Array.isArray(_lookupTableAddresses) ? _lookupTableAddresses.map((addr) => addr instanceof web3108.PublicKey ? addr : toWeb3JsPublicKey(addr)) : _lookupTableAddresses instanceof web3108.PublicKey ? _lookupTableAddresses : toWeb3JsPublicKey(_lookupTableAddresses));
  const tx = await buildVersionedTransaction2(
    connection,
    payer,
    instructions,
    commitment,
    blockhash,
    lookupTableAddresses
  );
  const simulateResp = await connection.simulateTransaction(tx, { sigVerify: false, commitment });
  const returnPrefix = `Program return: ${programId.toBase58()} `;
  const returnLog = simulateResp.value.logs?.find((l) => l.startsWith(returnPrefix));
  if (returnLog === void 0 || simulateResp.value.returnData?.programId !== programId.toBase58()) {
    throw new Error(`Simulate Fail: ${JSON.stringify(simulateResp)}`);
  } else {
    const resp = Buffer.from(returnLog.slice(returnPrefix.length), "base64");
    return serializer.deserialize(resp, 0)[0];
  }
}
function toWeb3Connection(rpc) {
  if (rpc instanceof web3108.Connection) {
    return rpc;
  } else if ("connection" in rpc && rpc.connection instanceof web3108.Connection) {
    return rpc.connection;
  } else {
    throw new Error("Invalid connection");
  }
}

// src/compose-types-v1.ts
async function getLzComposeAccountsFromTypesV1(rpc, payer, to, composerProgram, params, commitment = "confirmed") {
  const oappPDA = new OmniAppPDA(composerProgram);
  const [lzComposeTypesAccountsPDA] = oappPDA.lzComposeTypesAccounts(to);
  const info = await rpc.getAccount(lzComposeTypesAccountsPDA, { commitment });
  const accounts = [];
  if (info.exists) {
    const buffer = Buffer.from(info.data);
    const len = buffer.length - 8;
    if (len % 32 !== 0) {
      throw new Error(
        `Invalid length of AccountInfo.data. The length must be a multiple of 32 plus 8.(n*32+8). Current length is ${buffer.length}`
      );
    }
    for (let i = 8; i < len; i += 32) {
      const [address] = publicKey$1().deserialize(buffer, i);
      accounts.push({
        pubkey: address,
        isSigner: false,
        isWritable: false
      });
    }
  }
  const data = getLzComposeParamsSerializer2().serialize(params);
  const lzComposeTypesIx = {
    programId: composerProgram,
    keys: accounts,
    data: Buffer.concat([instructionDiscriminator2("lz_compose_types"), data])
  };
  const keys = await simulateWeb3JsTransaction(
    rpc.getEndpoint(),
    [toWeb3JsInstruction(lzComposeTypesIx)],
    toWeb3JsPublicKey(composerProgram),
    toWeb3JsPublicKey(payer),
    array$1(getLzReceiveAccountSerializer()),
    commitment
  );
  return updateAccountFromSimulatedResp(keys, payer);
}
function updateAccountFromSimulatedResp(accounts, payer) {
  return accounts.map((r) => {
    if (r.pubkey == defaultPublicKey() && r.isSigner) {
      if (!payer) throw new Error("payer is required");
      return {
        pubkey: payer,
        isSigner: true,
        isWritable: r.isWritable
      };
    } else {
      return {
        pubkey: r.pubkey,
        isSigner: r.isSigner,
        isWritable: r.isWritable
      };
    }
  });
}
async function buildLzComposeExecutionPlan(rpc, executorProgram, payer, to, composerProgram, params, composeTypesAccounts, commitment = "confirmed") {
  composeTypesAccounts = composeTypesAccounts ?? await getLzComposeTypesInfo(rpc, payer, to, composerProgram, params, commitment);
  const data = getLzComposeParamsSerializer2().serialize(params);
  const lzComposeTypesIx = {
    programId: composerProgram,
    keys: composeTypesAccounts.accounts.map((pubkey) => {
      return {
        pubkey,
        isSigner: false,
        isWritable: false
      };
    }),
    data: Buffer.concat([instructionDiscriminator2("lz_compose_types_v2"), data])
  };
  const resp = await simulateWeb3JsTransaction(
    rpc.getEndpoint(),
    [toWeb3JsInstruction(lzComposeTypesIx)],
    toWeb3JsPublicKey(composerProgram),
    toWeb3JsPublicKey(payer),
    getLzComposeTypesV2ResultSerializer(),
    commitment
  );
  const tables = await fetchAllAddressLookupTable({ rpc }, resp.alts);
  const signers = {};
  const lzComposeDiscriminator = instructionDiscriminator2("lz_compose").toString("hex");
  const instructionTracker = {};
  for (const ix of resp.instructions) {
    let programId = composerProgram;
    let instructionType = "lz_compose";
    if (ix.__kind === "Standard") {
      const { programId: stdProgramId, data: data2 } = ix;
      programId = stdProgramId;
      const discriminator = Buffer.from(data2.slice(0, 8)).toString("hex");
      instructionType = discriminator === lzComposeDiscriminator ? "lz_compose" : discriminator;
    }
    const key = `${programId}-${instructionType}`;
    if (instructionTracker[key]) {
      throw new Error(`Duplicate instruction found: ${instructionType} for program ${programId}`);
    }
    instructionTracker[key] = true;
  }
  const { contextVersion } = resp;
  const instructions = resp.instructions.map((ix) => {
    if (ix.__kind === "LzCompose") {
      return {
        programId: composerProgram,
        keys: ix.accounts.map(
          (account) => accountMetaRefToAccountMeta(
            { eddsa: EDDSA },
            executorProgram,
            tables,
            signers,
            account,
            payer,
            contextVersion
          )
        ),
        data: Buffer.concat([instructionDiscriminator2("lz_compose"), data])
      };
    } else {
      return {
        programId: ix.programId,
        keys: ix.accounts.map(
          (account) => accountMetaRefToAccountMeta(
            { eddsa: EDDSA },
            executorProgram,
            tables,
            signers,
            account,
            payer,
            contextVersion
          )
        ),
        data: ix.data
      };
    }
  });
  return {
    contextVersion,
    signers: Object.values(signers).filter((signer) => signer !== void 0),
    instructions,
    addressLookupTables: tables
  };
}
async function getLzComposeTypesInfo(rpc, payer, to, composerProgram, params, commitment = "confirmed") {
  const oappPDA = new OmniAppPDA(composerProgram);
  const [lzComposeTypesAccountsPDA] = oappPDA.lzComposeTypesAccounts(to);
  const lzComposeTypesIx = {
    programId: composerProgram,
    keys: [
      {
        pubkey: to,
        isSigner: false,
        isWritable: false
      },
      {
        pubkey: lzComposeTypesAccountsPDA,
        isSigner: false,
        isWritable: false
      }
    ],
    data: Buffer.concat([
      instructionDiscriminator2("lz_compose_types_info"),
      getLzComposeParamsSerializer2().serialize(params)
    ])
  };
  const resp = await simulateWeb3JsTransaction(
    rpc.getEndpoint(),
    [toWeb3JsInstruction(lzComposeTypesIx)],
    toWeb3JsPublicKey(composerProgram),
    toWeb3JsPublicKey(payer),
    bytes$1(),
    commitment
  );
  const version5 = resp.at(0);
  if (version5 !== 2) {
    throw new Error(`Invalid version ${version5}. Expected version 2.`);
  }
  return getLzComposeTypesV2AccountsSerializer().deserialize(resp, 1)[0];
}
function accountMetaRefToAccountMeta(umi, executorProgram, tables, signers, accountMetaRef, payer, contextVersion) {
  const { isWritable, pubkey: locator } = accountMetaRef;
  const pubkey = addressLocatorToPublicKey(umi, executorProgram, tables, signers, locator, payer, contextVersion);
  return { pubkey, isSigner: locator.__kind === "Signer" || locator.__kind === "Payer", isWritable };
}
function addressLocatorToPublicKey(umi, executorProgram, tables, signers, locator, payer, contextVersion) {
  if (locator.__kind === "Payer") {
    return payer;
  } else if (locator.__kind === "Signer") {
    const index = locator.fields[0];
    let signer = signers[index];
    if (signer === void 0) {
      signer = generateSigner(umi);
      signers[index] = signer;
    }
    return signer.publicKey;
  } else if (locator.__kind === "Address") {
    return locator.fields[0];
  } else if (locator.__kind === "Context") {
    const pda = new ExecutorPDA(executorProgram);
    return pda.context(payer, contextVersion)[0];
  } else {
    const table = tables[locator.fields[0]];
    return table.addresses[locator.fields[1]];
  }
}
async function getLzReceiveAccountsFromTypesV1(rpc, payer, receiver, receiverProgram, params, commitment = "confirmed") {
  const oappPDA = new OmniAppPDA(receiverProgram);
  const [lzReceiveTypesAccountsPDA] = oappPDA.lzReceiveTypesAccounts(receiver);
  const info = await rpc.getAccount(lzReceiveTypesAccountsPDA, { commitment });
  const accounts = [];
  if (info.exists) {
    const buffer = Buffer.from(info.data);
    const len = buffer.length - 8;
    if (len % 32 !== 0) {
      throw new Error(
        `Invalid length of AccountInfo.data. The length must be a multiple of 32 plus 8.(n*32+8). Current length is ${buffer.length}`
      );
    }
    for (let i = 8; i < len; i += 32) {
      const [address] = publicKey$1().deserialize(buffer, i);
      accounts.push({
        pubkey: address,
        isSigner: false,
        isWritable: false
      });
    }
  }
  const data = getLzReceiveParamsSerializer2().serialize(params);
  const lzReceiveTypesIx = {
    programId: receiverProgram,
    keys: accounts,
    data: Buffer.concat([instructionDiscriminator2("lz_receive_types"), data])
  };
  const keys = await simulateWeb3JsTransaction(
    rpc.getEndpoint(),
    [toWeb3JsInstruction(lzReceiveTypesIx)],
    toWeb3JsPublicKey(receiverProgram),
    toWeb3JsPublicKey(payer),
    array$1(getLzReceiveAccountSerializer()),
    commitment
  );
  return updateAccountFromSimulatedResp2(keys, payer);
}
function updateAccountFromSimulatedResp2(accounts, payer) {
  return accounts.map((r) => {
    if (r.pubkey == defaultPublicKey() && r.isSigner) {
      if (!payer) throw new Error("payer is required");
      return {
        pubkey: payer,
        isSigner: true,
        isWritable: r.isWritable
      };
    } else {
      return {
        pubkey: r.pubkey,
        isSigner: r.isSigner,
        isWritable: r.isWritable
      };
    }
  });
}
async function buildLzReceiveExecutionPlan(rpc, executorProgram, payer, receiver, receiverProgram, params, receiveTypesAccounts, commitment = "confirmed") {
  receiveTypesAccounts = receiveTypesAccounts ?? await getLzReceiveTypesInfo(rpc, payer, receiver, receiverProgram, params, commitment);
  const data = getLzReceiveParamsSerializer2().serialize(params);
  const lzReceiveTypesIx = {
    programId: receiverProgram,
    keys: receiveTypesAccounts.accounts.map((pubkey) => {
      return {
        pubkey,
        isSigner: false,
        isWritable: false
      };
    }),
    data: Buffer.concat([instructionDiscriminator2("lz_receive_types_v2"), data])
  };
  const resp = await simulateWeb3JsTransaction(
    rpc.getEndpoint(),
    [toWeb3JsInstruction(lzReceiveTypesIx)],
    toWeb3JsPublicKey(receiverProgram),
    toWeb3JsPublicKey(payer),
    getLzReceiveTypesV2ResultSerializer(),
    commitment
  );
  const tables = await fetchAllAddressLookupTable({ rpc }, resp.alts);
  const signers = {};
  const lzReceiveDiscriminator = instructionDiscriminator2("lz_receive").toString("hex");
  const instructionTracker = {};
  for (const ix of resp.instructions) {
    let programId = receiverProgram;
    let instructionType = "lz_receive";
    if (ix.__kind === "Standard") {
      const { programId: stdProgramId, data: data2 } = ix;
      programId = stdProgramId;
      const discriminator = Buffer.from(data2.slice(0, 8)).toString("hex");
      instructionType = discriminator === lzReceiveDiscriminator ? "lz_receive" : discriminator;
    }
    const key = `${programId}-${instructionType}`;
    if (instructionTracker[key]) {
      throw new Error(`Duplicate instruction found: ${instructionType} for program ${programId}`);
    }
    instructionTracker[key] = true;
  }
  const { contextVersion } = resp;
  const instructions = resp.instructions.map((ix) => {
    if (ix.__kind === "LzReceive") {
      return {
        programId: receiverProgram,
        keys: ix.accounts.map(
          (account) => accountMetaRefToAccountMeta2(
            { eddsa: EDDSA },
            executorProgram,
            tables,
            signers,
            account,
            payer,
            contextVersion
          )
        ),
        data: Buffer.concat([instructionDiscriminator2("lz_receive"), data])
      };
    } else {
      return {
        programId: ix.programId,
        keys: ix.accounts.map(
          (account) => accountMetaRefToAccountMeta2(
            { eddsa: EDDSA },
            executorProgram,
            tables,
            signers,
            account,
            payer,
            contextVersion
          )
        ),
        data: ix.data
      };
    }
  });
  return {
    contextVersion,
    signers: Object.values(signers).filter((signer) => signer !== void 0),
    instructions,
    addressLookupTables: tables
  };
}
async function getLzReceiveTypesInfo(rpc, payer, receiver, receiverProgram, params, commitment = "confirmed") {
  const oappPDA = new OmniAppPDA(receiverProgram);
  const [lzReceiveTypesAccountsPDA] = oappPDA.lzReceiveTypesAccounts(receiver);
  const lzReceiveTypesIx = {
    programId: receiverProgram,
    keys: [
      {
        pubkey: receiver,
        isSigner: false,
        isWritable: false
      },
      {
        pubkey: lzReceiveTypesAccountsPDA,
        isSigner: false,
        isWritable: false
      }
    ],
    data: Buffer.concat([
      instructionDiscriminator2("lz_receive_types_info"),
      getLzReceiveParamsSerializer2().serialize(params)
    ])
  };
  const resp = await simulateWeb3JsTransaction(
    rpc.getEndpoint(),
    [toWeb3JsInstruction(lzReceiveTypesIx)],
    toWeb3JsPublicKey(receiverProgram),
    toWeb3JsPublicKey(payer),
    bytes$1(),
    commitment
  );
  const version5 = resp.at(0);
  if (version5 !== 2) {
    throw new Error(`Invalid version ${version5}. Expected version 2.`);
  }
  return getLzReceiveTypesV2AccountsSerializer().deserialize(resp, 1)[0];
}
function accountMetaRefToAccountMeta2(umi, executorProgram, tables, signers, accountMetaRef, payer, contextVersion) {
  const { isWritable, pubkey: locator } = accountMetaRef;
  const pubkey = addressLocatorToPublicKey2(umi, executorProgram, tables, signers, locator, payer, contextVersion);
  return { pubkey, isSigner: locator.__kind === "Signer" || locator.__kind === "Payer", isWritable };
}
function addressLocatorToPublicKey2(umi, executorProgram, tables, signers, locator, payer, contextVersion) {
  if (locator.__kind === "Payer") {
    return payer;
  } else if (locator.__kind === "Signer") {
    const index = locator.fields[0];
    let signer = signers[index];
    if (signer === void 0) {
      signer = generateSigner(umi);
      signers[index] = signer;
    }
    return signer.publicKey;
  } else if (locator.__kind === "Address") {
    return locator.fields[0];
  } else if (locator.__kind === "Context") {
    const pda = new ExecutorPDA(executorProgram);
    return pda.context(payer, contextVersion)[0];
  } else {
    const table = tables[locator.fields[0]];
    return table.addresses[locator.fields[1]];
  }
}

// src/receive.ts
async function lzReceive2(rpc, payer, packet, callerParams = Uint8Array.from([0, 0]), executorProgram = EXECUTOR_PROGRAM_ID, commitment = "confirmed") {
  const { message: message_, sender: sender_, srcEid, guid, receiver: receiver_, nonce } = packet;
  const sender = arrayify(sender_);
  const receiverBytes = addressToBytes32(receiver_);
  const message = arrayify(message_);
  const params = {
    srcEid,
    sender,
    guid: arrayify(guid),
    message,
    callerParams,
    nonce: BigInt(nonce)
  };
  const receiverPubkey = publicKey(receiverBytes);
  const receiverInfo = await rpc.getAccount(receiverPubkey, { commitment });
  invariant6(receiverInfo.exists, `Receiver account not found: ${receiverPubkey}`);
  const receiverProgram = publicKey(receiverInfo.owner);
  let version5 = 1;
  let receiveTypesAccounts;
  try {
    receiveTypesAccounts = await getLzReceiveTypesInfo(
      rpc,
      payer,
      receiverPubkey,
      receiverProgram,
      params,
      commitment
    );
    version5 = 2;
  } catch (e) {
    version5 = 1;
  }
  if (version5 === 2) {
    return buildLzReceiveExecutionPlan(
      rpc,
      executorProgram,
      payer,
      receiverPubkey,
      receiverProgram,
      params,
      receiveTypesAccounts,
      commitment
    );
  } else {
    const accounts = await getLzReceiveAccountsFromTypesV1(
      rpc,
      payer,
      receiverPubkey,
      receiverProgram,
      params,
      commitment
    );
    const data = getLzReceiveParamsSerializer2().serialize(params);
    return {
      instruction: {
        programId: receiverProgram,
        keys: accounts,
        data: Buffer.concat([instructionDiscriminator2("lz_receive"), data])
      },
      contextVersion: 1
    };
  }
}
async function lzCompose2(rpc, payer, event, extraData = Uint8Array.from([0, 0]), executorProgram = EXECUTOR_PROGRAM_ID, commitment = "confirmed") {
  const { to, from, guid, index, message } = event;
  const params = {
    from,
    to,
    guid,
    index,
    message,
    extraData
  };
  const accountInfo = await rpc.getAccount(to, { commitment });
  if (!accountInfo.exists) {
    throw new Error(`Account not found: ${to}`);
  }
  const composerProgram = accountInfo.owner;
  let version5 = 1;
  let composeTypesAccounts;
  try {
    composeTypesAccounts = await getLzComposeTypesInfo(rpc, payer, to, composerProgram, params, commitment);
    version5 = 2;
  } catch (e) {
    version5 = 1;
  }
  if (version5 === 2) {
    return buildLzComposeExecutionPlan(
      rpc,
      executorProgram,
      payer,
      to,
      composerProgram,
      params,
      composeTypesAccounts,
      commitment
    );
  } else {
    const accounts = await getLzComposeAccountsFromTypesV1(rpc, payer, to, composerProgram, params, commitment);
    const data = getLzComposeParamsSerializer2().serialize(params);
    return {
      instruction: {
        programId: composerProgram,
        keys: accounts,
        data: Buffer.concat([instructionDiscriminator2("lz_compose"), data])
      },
      contextVersion: 1
    };
  }
}

// src/executor.ts
var Executor2 = class {
  constructor(programId = EXECUTOR_PROGRAM_ID, rpc) {
    this.programId = programId;
    this.pda = new ExecutorPDA(this.programId);
    if (rpc === void 0) {
      rpc = createNullRpc();
      rpc.getCluster = () => "custom";
    }
    this.programRepo = createDefaultProgramRepository({ rpc }, [
      {
        name: "executor",
        publicKey: programId,
        getErrorFromCode(code, cause) {
          return getExecutorErrorFromCode(code, this, cause);
        },
        getErrorFromName(name, cause) {
          return getExecutorErrorFromName(name, this, cause);
        },
        isOnCluster() {
          return true;
        }
      }
    ]);
    this.eventAuthority = new EventPDA(programId).eventAuthority()[0];
  }
  getProgram(clusterFilter = "custom") {
    return this.programRepo.get("executor", clusterFilter);
  }
  // owner methods
  initExecutor(payer, param) {
    return initExecutor(
      { programs: this.programRepo, payer },
      {
        payer,
        config: this.pda.config(),
        ...param
      }
    ).items[0];
  }
  setOwner(owner, newOwner) {
    return ownerSetConfig(
      { programs: this.programRepo },
      {
        owner,
        config: this.pda.config(),
        params: {
          fields: [newOwner],
          __kind: "Owner"
        }
      }
    ).items[0];
  }
  setAdmins(owner, admins) {
    return ownerSetConfig(
      { programs: this.programRepo },
      {
        owner,
        config: this.pda.config(),
        params: {
          fields: [admins],
          __kind: "Admins"
        }
      }
    ).items[0];
  }
  setAllowList(owner, allowlist) {
    return ownerSetConfig(
      { programs: this.programRepo },
      {
        owner,
        config: this.pda.config(),
        params: {
          fields: [allowlist],
          __kind: "Allowlist"
        }
      }
    ).items[0];
  }
  setDenyList(owner, denylist) {
    return ownerSetConfig(
      { programs: this.programRepo },
      {
        owner,
        config: this.pda.config(),
        params: {
          fields: [denylist],
          __kind: "Denylist"
        }
      }
    ).items[0];
  }
  setPaused(owner, paused) {
    return ownerSetConfig(
      { programs: this.programRepo },
      {
        owner,
        config: this.pda.config(),
        params: {
          fields: [paused],
          __kind: "Paused"
        }
      }
    ).items[0];
  }
  setExecutors(owner, executors) {
    return ownerSetConfig(
      { programs: this.programRepo },
      {
        owner,
        config: this.pda.config(),
        params: {
          fields: [executors],
          __kind: "Executors"
        }
      }
    ).items[0];
  }
  setMsglibByPrograms(owner, msglibPrograms) {
    const msglibPdas = msglibPrograms.map((program) => {
      return new MessageLibPDA(program).messageLib()[0];
    });
    return this.setMsglibByPDAs(owner, msglibPdas);
  }
  setMsglibByPDAs(owner, msglibPDAs) {
    return ownerSetConfig(
      { programs: this.programRepo },
      {
        owner,
        config: this.pda.config(),
        params: {
          fields: [msglibPDAs],
          __kind: "Msglibs"
        }
      }
    ).items[0];
  }
  // admin methods
  setDefaultMultiplierBps(admin, defaultMultiplierBps) {
    return adminSetConfig(
      { programs: this.programRepo },
      {
        admin,
        config: this.pda.config(),
        params: {
          fields: [defaultMultiplierBps],
          __kind: "DefaultMultiplierBps"
        }
      }
    ).items[0];
  }
  setDstConfig(admin, dstConfigs) {
    return adminSetConfig(
      { programs: this.programRepo },
      {
        admin,
        config: this.pda.config(),
        params: {
          fields: [dstConfigs],
          __kind: "DstConfigs"
        }
      }
    ).items[0];
  }
  setPriceFeed(admin, priceFeedProgram) {
    const [priceFeed] = new PriceFeedPDA(priceFeedProgram).priceFeed();
    return adminSetConfig(
      { programs: this.programRepo },
      {
        admin,
        config: this.pda.config(),
        params: {
          fields: [priceFeed],
          __kind: "PriceFeed"
        }
      }
    ).items[0];
  }
  nativeDrop(executor, param) {
    const receivers = param.nativeDropRequests.map((request) => {
      return {
        pubkey: request.receiver,
        isSigner: false,
        isWritable: true
      };
    });
    return nativeDrop(
      { programs: this.programRepo },
      {
        executor,
        config: this.pda.config(),
        eventAuthority: new EventPDA(this.programId).eventAuthority()[0],
        program: this.programId,
        ...param
      }
    ).addRemainingAccounts(receivers).items[0];
  }
  quoteExecutor(param) {
    const [priceFeed] = new PriceFeedPDA(param.priceFeedProgram).priceFeed();
    return quoteExecutor(
      { programs: this.programRepo },
      {
        executorConfig: this.pda.config(),
        priceFeedConfig: priceFeed,
        ...param
      }
    ).items[0];
  }
  async getExecutorConfig(rpc, commitment = "confirmed") {
    const [configAccount] = this.pda.config();
    const config2 = await safeFetchExecutorConfig({ rpc }, configAccount, { commitment });
    return config2;
  }
  getQuoteIXAccountMetaForCPI(priceFeedConfig, priceFeedProgram, payment) {
    const txBuilder = quoteExecutor(
      { programs: this.programRepo },
      {
        executorConfig: this.pda.config(),
        priceFeedConfig,
        priceFeedProgram,
        // param
        msglib: defaultPublicKey(),
        dstEid: 0,
        sender: defaultPublicKey(),
        calldataSize: 0,
        options: []
      }
    );
    const ixAccounts = txBuilder.getInstructions()[0].keys;
    if (payment) {
      ixAccounts[0].isWritable = true;
    }
    return [
      {
        pubkey: this.programId,
        isWritable: false,
        isSigner: false
      }
    ].concat(ixAccounts);
  }
  async execute(rpc, executor, param, commitment = "confirmed") {
    const { packet, extraData, value = 0n } = param;
    const payer = executor.publicKey;
    const lzReceiveResult = await lzReceive2(rpc, payer, packet, extraData, this.programId, commitment);
    const { contextVersion } = lzReceiveResult;
    const instructions = [this.preExecute(executor, contextVersion, value).instruction];
    const signers = [];
    const addressLookupTables = [];
    if ("instruction" in lzReceiveResult) {
      instructions.push(lzReceiveResult.instruction);
    } else {
      instructions.push(...lzReceiveResult.instructions);
      signers.push(...lzReceiveResult.signers);
      addressLookupTables.push(...lzReceiveResult.addressLookupTables);
    }
    instructions.push(this.postExecute(executor, contextVersion).instruction);
    return { signers, instructions, addressLookupTables };
  }
  async compose(rpc, executor, param, commitment = "confirmed") {
    const { event, extraData, value = 0n } = param;
    const payer = executor.publicKey;
    const lzComposeResult = await lzCompose2(rpc, payer, event, extraData, this.programId, commitment);
    const { contextVersion } = lzComposeResult;
    const instructions = [this.preExecute(executor, contextVersion, value).instruction];
    const signers = [];
    const addressLookupTables = [];
    if ("instruction" in lzComposeResult) {
      instructions.push(lzComposeResult.instruction);
    } else {
      instructions.push(...lzComposeResult.instructions);
      signers.push(...lzComposeResult.signers);
      addressLookupTables.push(...lzComposeResult.addressLookupTables);
    }
    instructions.push(this.postExecute(executor, contextVersion).instruction);
    return { signers, instructions, addressLookupTables };
  }
  preExecute(executor, version5, feeLimit) {
    const context = this.pda.context(executor.publicKey, version5);
    return preExecute(
      { programs: this.programRepo },
      { executor, context, feeLimit, contextVersion: version5 }
    ).items[0];
  }
  postExecute(executor, version5) {
    const context = this.pda.context(executor.publicKey, version5);
    return postExecute({ programs: this.programRepo }, { context, executor, contextVersion: version5 }).items[0];
  }
};

// src/generated/kinobi/uln/accounts/index.ts
var accounts_exports11 = {};
__export(accounts_exports11, {
  deserializeConfirmations: () => deserializeConfirmations,
  deserializeReceiveConfig: () => deserializeReceiveConfig2,
  deserializeSendConfig: () => deserializeSendConfig,
  deserializeUlnSettings: () => deserializeUlnSettings,
  fetchAllConfirmations: () => fetchAllConfirmations,
  fetchAllReceiveConfig: () => fetchAllReceiveConfig2,
  fetchAllSendConfig: () => fetchAllSendConfig,
  fetchAllUlnSettings: () => fetchAllUlnSettings,
  fetchConfirmations: () => fetchConfirmations,
  fetchReceiveConfig: () => fetchReceiveConfig2,
  fetchSendConfig: () => fetchSendConfig,
  fetchUlnSettings: () => fetchUlnSettings,
  getConfirmationsAccountDataSerializer: () => getConfirmationsAccountDataSerializer,
  getConfirmationsGpaBuilder: () => getConfirmationsGpaBuilder,
  getReceiveConfigAccountDataSerializer: () => getReceiveConfigAccountDataSerializer2,
  getReceiveConfigGpaBuilder: () => getReceiveConfigGpaBuilder2,
  getSendConfigAccountDataSerializer: () => getSendConfigAccountDataSerializer,
  getSendConfigGpaBuilder: () => getSendConfigGpaBuilder,
  getUlnSettingsAccountDataSerializer: () => getUlnSettingsAccountDataSerializer,
  getUlnSettingsGpaBuilder: () => getUlnSettingsGpaBuilder,
  safeFetchAllConfirmations: () => safeFetchAllConfirmations,
  safeFetchAllReceiveConfig: () => safeFetchAllReceiveConfig2,
  safeFetchAllSendConfig: () => safeFetchAllSendConfig,
  safeFetchAllUlnSettings: () => safeFetchAllUlnSettings,
  safeFetchConfirmations: () => safeFetchConfirmations,
  safeFetchReceiveConfig: () => safeFetchReceiveConfig2,
  safeFetchSendConfig: () => safeFetchSendConfig,
  safeFetchUlnSettings: () => safeFetchUlnSettings
});
function getConfirmationsAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["value", option(u64$1())],
        ["bump", u8$1()]
      ],
      { description: "ConfirmationsAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([206, 57, 50, 8, 124, 133, 138, 112])
    })
  );
}
function deserializeConfirmations(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getConfirmationsAccountDataSerializer()
  );
}
async function fetchConfirmations(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "Confirmations");
  return deserializeConfirmations(maybeAccount);
}
async function safeFetchConfirmations(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeConfirmations(maybeAccount) : null;
}
async function fetchAllConfirmations(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "Confirmations");
    return deserializeConfirmations(maybeAccount);
  });
}
async function safeFetchAllConfirmations(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeConfirmations(maybeAccount)
  );
}
function getConfirmationsGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    value: [8, option(u64$1())],
    bump: [null, u8$1()]
  }).deserializeUsing(
    (account) => deserializeConfirmations(account)
  ).whereField(
    "discriminator",
    new Uint8Array([206, 57, 50, 8, 124, 133, 138, 112])
  );
}
function getReceiveConfigAccountDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["bump", u8$1()],
        ["uln", getUlnConfigSerializer2()]
      ],
      { description: "ReceiveConfigAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([162, 159, 153, 188, 56, 65, 245, 58])
    })
  );
}
function deserializeReceiveConfig2(rawAccount) {
  return deserializeAccount(
    rawAccount,
    getReceiveConfigAccountDataSerializer2()
  );
}
async function fetchReceiveConfig2(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "ReceiveConfig");
  return deserializeReceiveConfig2(maybeAccount);
}
async function safeFetchReceiveConfig2(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeReceiveConfig2(maybeAccount) : null;
}
async function fetchAllReceiveConfig2(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "ReceiveConfig");
    return deserializeReceiveConfig2(maybeAccount);
  });
}
async function safeFetchAllReceiveConfig2(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map(
    (maybeAccount) => deserializeReceiveConfig2(maybeAccount)
  );
}
function getReceiveConfigGpaBuilder2(context) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    bump: [8, u8$1()],
    uln: [9, getUlnConfigSerializer2()]
  }).deserializeUsing(
    (account) => deserializeReceiveConfig2(account)
  ).whereField(
    "discriminator",
    new Uint8Array([162, 159, 153, 188, 56, 65, 245, 58])
  );
}
function getSendConfigAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["bump", u8$1()],
        ["uln", getUlnConfigSerializer2()],
        ["executor", getExecutorConfigSerializer()]
      ],
      { description: "SendConfigAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([91, 221, 135, 137, 131, 199, 174, 25])
    })
  );
}
function deserializeSendConfig(rawAccount) {
  return deserializeAccount(rawAccount, getSendConfigAccountDataSerializer());
}
async function fetchSendConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "SendConfig");
  return deserializeSendConfig(maybeAccount);
}
async function safeFetchSendConfig(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeSendConfig(maybeAccount) : null;
}
async function fetchAllSendConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "SendConfig");
    return deserializeSendConfig(maybeAccount);
  });
}
async function safeFetchAllSendConfig(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map((maybeAccount) => deserializeSendConfig(maybeAccount));
}
function getSendConfigGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    bump: [8, u8$1()],
    uln: [9, getUlnConfigSerializer2()],
    executor: [null, getExecutorConfigSerializer()]
  }).deserializeUsing((account) => deserializeSendConfig(account)).whereField(
    "discriminator",
    new Uint8Array([91, 221, 135, 137, 131, 199, 174, 25])
  );
}
function getUlnSettingsAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["endpoint", publicKey$1()],
        ["endpointProgram", publicKey$1()],
        ["bump", u8$1()],
        ["admin", publicKey$1()],
        ["treasury", option(getTreasurySerializer())]
      ],
      { description: "UlnSettingsAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([226, 199, 100, 253, 38, 115, 167, 154])
    })
  );
}
function deserializeUlnSettings(rawAccount) {
  return deserializeAccount(rawAccount, getUlnSettingsAccountDataSerializer());
}
async function fetchUlnSettings(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "UlnSettings");
  return deserializeUlnSettings(maybeAccount);
}
async function safeFetchUlnSettings(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializeUlnSettings(maybeAccount) : null;
}
async function fetchAllUlnSettings(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "UlnSettings");
    return deserializeUlnSettings(maybeAccount);
  });
}
async function safeFetchAllUlnSettings(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map((maybeAccount) => deserializeUlnSettings(maybeAccount));
}
function getUlnSettingsGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    eid: [8, u32$1()],
    endpoint: [12, publicKey$1()],
    endpointProgram: [44, publicKey$1()],
    bump: [76, u8$1()],
    admin: [77, publicKey$1()],
    treasury: [109, option(getTreasurySerializer())]
  }).deserializeUsing((account) => deserializeUlnSettings(account)).whereField(
    "discriminator",
    new Uint8Array([226, 199, 100, 253, 38, 115, 167, 154])
  );
}

// src/generated/kinobi/uln/errors/index.ts
var errors_exports9 = {};
__export(errors_exports9, {
  AtLeastOneDVNError: () => AtLeastOneDVNError2,
  ExceededMaxMessageSizeError: () => ExceededMaxMessageSizeError2,
  ExceededU128Error: () => ExceededU128Error2,
  InsufficientFeeError: () => InsufficientFeeError4,
  InvalidAccountLengthError: () => InvalidAccountLengthError2,
  InvalidAmountError: () => InvalidAmountError8,
  InvalidBpsError: () => InvalidBpsError2,
  InvalidConfigTypeError: () => InvalidConfigTypeError4,
  InvalidConfirmationError: () => InvalidConfirmationError2,
  InvalidConfirmationsError: () => InvalidConfirmationsError2,
  InvalidDvnError: () => InvalidDvnError2,
  InvalidDvnProgramError: () => InvalidDvnProgramError2,
  InvalidEidError: () => InvalidEidError2,
  InvalidExecutorError: () => InvalidExecutorError2,
  InvalidExecutorProgramError: () => InvalidExecutorProgramError2,
  InvalidLzTokenMintError: () => InvalidLzTokenMintError4,
  InvalidOptionTypeError: () => InvalidOptionTypeError2,
  InvalidOptionalDVNCountError: () => InvalidOptionalDVNCountError2,
  InvalidOptionalDVNThresholdError: () => InvalidOptionalDVNThresholdError2,
  InvalidPacketVersionError: () => InvalidPacketVersionError2,
  InvalidPayerError: () => InvalidPayerError2,
  InvalidRequiredDVNCountError: () => InvalidRequiredDVNCountError2,
  InvalidTreasuryError: () => InvalidTreasuryError2,
  InvalidTreasuryFeeCapError: () => InvalidTreasuryFeeCapError2,
  InvalidType1SizeError: () => InvalidType1SizeError2,
  InvalidType2SizeError: () => InvalidType2SizeError2,
  InvalidWorkerIdError: () => InvalidWorkerIdError2,
  LzTokenUnavailableError: () => LzTokenUnavailableError6,
  NonSignerError: () => NonSignerError2,
  UnauthorizedError: () => UnauthorizedError4,
  UnsortedError: () => UnsortedError2,
  VerifyingError: () => VerifyingError2,
  ZeroMessageSizeError: () => ZeroMessageSizeError2,
  getUlnErrorFromCode: () => getUlnErrorFromCode,
  getUlnErrorFromName: () => getUlnErrorFromName
});
var codeToErrorMap5 = /* @__PURE__ */ new Map();
var nameToErrorMap5 = /* @__PURE__ */ new Map();
var UnauthorizedError4 = class extends ProgramError {
  // 6000
  constructor(program, cause) {
    super("", program, cause);
    this.name = "Unauthorized";
    this.code = 6e3;
  }
};
codeToErrorMap5.set(6e3, UnauthorizedError4);
nameToErrorMap5.set("Unauthorized", UnauthorizedError4);
var InvalidAmountError8 = class extends ProgramError {
  // 6001
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidAmount";
    this.code = 6001;
  }
};
codeToErrorMap5.set(6001, InvalidAmountError8);
nameToErrorMap5.set("InvalidAmount", InvalidAmountError8);
var InvalidExecutorError2 = class extends ProgramError {
  // 6002
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidExecutor";
    this.code = 6002;
  }
};
codeToErrorMap5.set(6002, InvalidExecutorError2);
nameToErrorMap5.set("InvalidExecutor", InvalidExecutorError2);
var ZeroMessageSizeError2 = class extends ProgramError {
  // 6003
  constructor(program, cause) {
    super("", program, cause);
    this.name = "ZeroMessageSize";
    this.code = 6003;
  }
};
codeToErrorMap5.set(6003, ZeroMessageSizeError2);
nameToErrorMap5.set("ZeroMessageSize", ZeroMessageSizeError2);
var InvalidRequiredDVNCountError2 = class extends ProgramError {
  // 6004
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidRequiredDVNCount";
    this.code = 6004;
  }
};
codeToErrorMap5.set(6004, InvalidRequiredDVNCountError2);
nameToErrorMap5.set("InvalidRequiredDVNCount", InvalidRequiredDVNCountError2);
var InvalidOptionalDVNCountError2 = class extends ProgramError {
  // 6005
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidOptionalDVNCount";
    this.code = 6005;
  }
};
codeToErrorMap5.set(6005, InvalidOptionalDVNCountError2);
nameToErrorMap5.set("InvalidOptionalDVNCount", InvalidOptionalDVNCountError2);
var InvalidOptionalDVNThresholdError2 = class extends ProgramError {
  // 6006
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidOptionalDVNThreshold";
    this.code = 6006;
  }
};
codeToErrorMap5.set(6006, InvalidOptionalDVNThresholdError2);
nameToErrorMap5.set(
  "InvalidOptionalDVNThreshold",
  InvalidOptionalDVNThresholdError2
);
var InvalidConfirmationsError2 = class extends ProgramError {
  // 6007
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidConfirmations";
    this.code = 6007;
  }
};
codeToErrorMap5.set(6007, InvalidConfirmationsError2);
nameToErrorMap5.set("InvalidConfirmations", InvalidConfirmationsError2);
var AtLeastOneDVNError2 = class extends ProgramError {
  // 6008
  constructor(program, cause) {
    super("", program, cause);
    this.name = "AtLeastOneDVN";
    this.code = 6008;
  }
};
codeToErrorMap5.set(6008, AtLeastOneDVNError2);
nameToErrorMap5.set("AtLeastOneDVN", AtLeastOneDVNError2);
var UnsortedError2 = class extends ProgramError {
  // 6009
  constructor(program, cause) {
    super("", program, cause);
    this.name = "Unsorted";
    this.code = 6009;
  }
};
codeToErrorMap5.set(6009, UnsortedError2);
nameToErrorMap5.set("Unsorted", UnsortedError2);
var InvalidTreasuryFeeCapError2 = class extends ProgramError {
  // 6010
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidTreasuryFeeCap";
    this.code = 6010;
  }
};
codeToErrorMap5.set(6010, InvalidTreasuryFeeCapError2);
nameToErrorMap5.set("InvalidTreasuryFeeCap", InvalidTreasuryFeeCapError2);
var InvalidPacketVersionError2 = class extends ProgramError {
  // 6011
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidPacketVersion";
    this.code = 6011;
  }
};
codeToErrorMap5.set(6011, InvalidPacketVersionError2);
nameToErrorMap5.set("InvalidPacketVersion", InvalidPacketVersionError2);
var InvalidEidError2 = class extends ProgramError {
  // 6012
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidEid";
    this.code = 6012;
  }
};
codeToErrorMap5.set(6012, InvalidEidError2);
nameToErrorMap5.set("InvalidEid", InvalidEidError2);
var VerifyingError2 = class extends ProgramError {
  // 6013
  constructor(program, cause) {
    super("", program, cause);
    this.name = "Verifying";
    this.code = 6013;
  }
};
codeToErrorMap5.set(6013, VerifyingError2);
nameToErrorMap5.set("Verifying", VerifyingError2);
var InvalidWorkerIdError2 = class extends ProgramError {
  // 6014
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidWorkerId";
    this.code = 6014;
  }
};
codeToErrorMap5.set(6014, InvalidWorkerIdError2);
nameToErrorMap5.set("InvalidWorkerId", InvalidWorkerIdError2);
var InvalidOptionTypeError2 = class extends ProgramError {
  // 6015
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidOptionType";
    this.code = 6015;
  }
};
codeToErrorMap5.set(6015, InvalidOptionTypeError2);
nameToErrorMap5.set("InvalidOptionType", InvalidOptionTypeError2);
var InvalidBpsError2 = class extends ProgramError {
  // 6016
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidBps";
    this.code = 6016;
  }
};
codeToErrorMap5.set(6016, InvalidBpsError2);
nameToErrorMap5.set("InvalidBps", InvalidBpsError2);
var ExceededMaxMessageSizeError2 = class extends ProgramError {
  // 6017
  constructor(program, cause) {
    super("", program, cause);
    this.name = "ExceededMaxMessageSize";
    this.code = 6017;
  }
};
codeToErrorMap5.set(6017, ExceededMaxMessageSizeError2);
nameToErrorMap5.set("ExceededMaxMessageSize", ExceededMaxMessageSizeError2);
var InvalidExecutorProgramError2 = class extends ProgramError {
  // 6018
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidExecutorProgram";
    this.code = 6018;
  }
};
codeToErrorMap5.set(6018, InvalidExecutorProgramError2);
nameToErrorMap5.set("InvalidExecutorProgram", InvalidExecutorProgramError2);
var InvalidAccountLengthError2 = class extends ProgramError {
  // 6019
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidAccountLength";
    this.code = 6019;
  }
};
codeToErrorMap5.set(6019, InvalidAccountLengthError2);
nameToErrorMap5.set("InvalidAccountLength", InvalidAccountLengthError2);
var InvalidDvnProgramError2 = class extends ProgramError {
  // 6020
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidDvnProgram";
    this.code = 6020;
  }
};
codeToErrorMap5.set(6020, InvalidDvnProgramError2);
nameToErrorMap5.set("InvalidDvnProgram", InvalidDvnProgramError2);
var InvalidDvnError2 = class extends ProgramError {
  // 6021
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidDvn";
    this.code = 6021;
  }
};
codeToErrorMap5.set(6021, InvalidDvnError2);
nameToErrorMap5.set("InvalidDvn", InvalidDvnError2);
var LzTokenUnavailableError6 = class extends ProgramError {
  // 6022
  constructor(program, cause) {
    super("", program, cause);
    this.name = "LzTokenUnavailable";
    this.code = 6022;
  }
};
codeToErrorMap5.set(6022, LzTokenUnavailableError6);
nameToErrorMap5.set("LzTokenUnavailable", LzTokenUnavailableError6);
var InsufficientFeeError4 = class extends ProgramError {
  // 6023
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InsufficientFee";
    this.code = 6023;
  }
};
codeToErrorMap5.set(6023, InsufficientFeeError4);
nameToErrorMap5.set("InsufficientFee", InsufficientFeeError4);
var InvalidTreasuryError2 = class extends ProgramError {
  // 6024
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidTreasury";
    this.code = 6024;
  }
};
codeToErrorMap5.set(6024, InvalidTreasuryError2);
nameToErrorMap5.set("InvalidTreasury", InvalidTreasuryError2);
var InvalidLzTokenMintError4 = class extends ProgramError {
  // 6025
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidLzTokenMint";
    this.code = 6025;
  }
};
codeToErrorMap5.set(6025, InvalidLzTokenMintError4);
nameToErrorMap5.set("InvalidLzTokenMint", InvalidLzTokenMintError4);
var InvalidConfigTypeError4 = class extends ProgramError {
  // 6026
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidConfigType";
    this.code = 6026;
  }
};
codeToErrorMap5.set(6026, InvalidConfigTypeError4);
nameToErrorMap5.set("InvalidConfigType", InvalidConfigTypeError4);
var InvalidConfirmationError2 = class extends ProgramError {
  // 6027
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidConfirmation";
    this.code = 6027;
  }
};
codeToErrorMap5.set(6027, InvalidConfirmationError2);
nameToErrorMap5.set("InvalidConfirmation", InvalidConfirmationError2);
var InvalidType1SizeError2 = class extends ProgramError {
  // 6028
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidType1Size";
    this.code = 6028;
  }
};
codeToErrorMap5.set(6028, InvalidType1SizeError2);
nameToErrorMap5.set("InvalidType1Size", InvalidType1SizeError2);
var InvalidType2SizeError2 = class extends ProgramError {
  // 6029
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidType2Size";
    this.code = 6029;
  }
};
codeToErrorMap5.set(6029, InvalidType2SizeError2);
nameToErrorMap5.set("InvalidType2Size", InvalidType2SizeError2);
var ExceededU128Error2 = class extends ProgramError {
  // 6030
  constructor(program, cause) {
    super("", program, cause);
    this.name = "ExceededU128";
    this.code = 6030;
  }
};
codeToErrorMap5.set(6030, ExceededU128Error2);
nameToErrorMap5.set("ExceededU128", ExceededU128Error2);
var NonSignerError2 = class extends ProgramError {
  // 6031
  constructor(program, cause) {
    super("", program, cause);
    this.name = "NonSigner";
    this.code = 6031;
  }
};
codeToErrorMap5.set(6031, NonSignerError2);
nameToErrorMap5.set("NonSigner", NonSignerError2);
var InvalidPayerError2 = class extends ProgramError {
  // 6032
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidPayer";
    this.code = 6032;
  }
};
codeToErrorMap5.set(6032, InvalidPayerError2);
nameToErrorMap5.set("InvalidPayer", InvalidPayerError2);
function getUlnErrorFromCode(code, program, cause) {
  const constructor = codeToErrorMap5.get(code);
  return constructor ? new constructor(program, cause) : null;
}
function getUlnErrorFromName(name, program, cause) {
  const constructor = nameToErrorMap5.get(name);
  return constructor ? new constructor(program, cause) : null;
}

// src/generated/kinobi/uln/instructions/index.ts
var instructions_exports11 = {};
__export(instructions_exports11, {
  closeVerify: () => closeVerify,
  commitVerification: () => commitVerification,
  getCloseVerifyInstructionDataSerializer: () => getCloseVerifyInstructionDataSerializer,
  getCommitVerificationInstructionDataSerializer: () => getCommitVerificationInstructionDataSerializer,
  getInitConfigInstructionDataSerializer: () => getInitConfigInstructionDataSerializer3,
  getInitDefaultConfigInstructionDataSerializer: () => getInitDefaultConfigInstructionDataSerializer2,
  getInitUlnInstructionDataSerializer: () => getInitUlnInstructionDataSerializer,
  getInitVerifyInstructionDataSerializer: () => getInitVerifyInstructionDataSerializer2,
  getQuoteInstructionDataSerializer: () => getQuoteInstructionDataSerializer3,
  getSendInstructionDataSerializer: () => getSendInstructionDataSerializer3,
  getSendWithLzTokenInstructionDataSerializer: () => getSendWithLzTokenInstructionDataSerializer2,
  getSetConfigInstructionDataSerializer: () => getSetConfigInstructionDataSerializer4,
  getSetDefaultConfigInstructionDataSerializer: () => getSetDefaultConfigInstructionDataSerializer2,
  getSetTreasuryInstructionDataSerializer: () => getSetTreasuryInstructionDataSerializer,
  getTransferAdminInstructionDataSerializer: () => getTransferAdminInstructionDataSerializer3,
  getVerifyInstructionDataSerializer: () => getVerifyInstructionDataSerializer2,
  getVersionInstructionDataSerializer: () => getVersionInstructionDataSerializer3,
  getWithdrawRentInstructionDataSerializer: () => getWithdrawRentInstructionDataSerializer2,
  initConfig: () => initConfig3,
  initDefaultConfig: () => initDefaultConfig2,
  initUln: () => initUln,
  initVerify: () => initVerify2,
  quote: () => quote3,
  send: () => send3,
  sendWithLzToken: () => sendWithLzToken2,
  setConfig: () => setConfig4,
  setDefaultConfig: () => setDefaultConfig2,
  setTreasury: () => setTreasury,
  transferAdmin: () => transferAdmin3,
  verify: () => verify2,
  version: () => version3,
  withdrawRent: () => withdrawRent2
});
function getAccountMetasAndSigners5(accounts, optionalAccountStrategy, programId) {
  const keys = [];
  const signers = [];
  accounts.forEach((account) => {
    if (!account.value) {
      keys.push({ pubkey: programId, isSigner: false, isWritable: false });
      return;
    }
    if (isSigner(account.value)) {
      signers.push(account.value);
    }
    keys.push({
      pubkey: publicKey(account.value, false),
      isSigner: isSigner(account.value),
      isWritable: account.isWritable
    });
  });
  return [keys, signers];
}

// src/generated/kinobi/uln/instructions/closeVerify.ts
function getCloseVerifyInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packetHeaderHash", bytes$1({ size: 32 })],
        ["payloadHash", bytes$1({ size: 32 })]
      ],
      { description: "CloseVerifyInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([44, 133, 228, 138, 169, 242, 53, 36])
    })
  );
}
function closeVerify(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    dvn: { index: 0, isWritable: false, value: input.dvn ?? null },
    receiver: {
      index: 1,
      isWritable: true,
      value: input.receiver ?? null
    },
    confirmations: {
      index: 2,
      isWritable: true,
      value: input.confirmations ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getCloseVerifyInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getCommitVerificationInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packetHeader", array$1(u8$1(), { size: 81 })],
        ["payloadHash", bytes$1({ size: 32 })]
      ],
      { description: "CommitVerificationInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([194, 120, 44, 252, 140, 215, 139, 103])
    })
  );
}
function commitVerification(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    receiveConfig: {
      index: 0,
      isWritable: false,
      value: input.receiveConfig ?? null
    },
    defaultReceiveConfig: {
      index: 1,
      isWritable: false,
      value: input.defaultReceiveConfig ?? null
    },
    uln: { index: 2, isWritable: false, value: input.uln ?? null }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getCommitVerificationInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitConfigInstructionDataSerializer3() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["oapp", publicKey$1()],
        ["eid", u32$1()]
      ],
      { description: "InitConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([23, 235, 115, 232, 168, 96, 1, 231])
    })
  );
}
function initConfig3(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    endpoint: {
      index: 0,
      isWritable: false,
      value: input.endpoint ?? null
    },
    payer: {
      index: 1,
      isWritable: true,
      value: input.payer ?? null
    },
    uln: { index: 2, isWritable: false, value: input.uln ?? null },
    sendConfig: {
      index: 3,
      isWritable: true,
      value: input.sendConfig ?? null
    },
    receiveConfig: {
      index: 4,
      isWritable: true,
      value: input.receiveConfig ?? null
    },
    systemProgram: {
      index: 5,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitConfigInstructionDataSerializer3().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitDefaultConfigInstructionDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["sendUlnConfig", getUlnConfigSerializer2()],
        ["receiveUlnConfig", getUlnConfigSerializer2()],
        ["executorConfig", getExecutorConfigSerializer()]
      ],
      { description: "InitDefaultConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([98, 218, 197, 194, 173, 179, 112, 21])
    })
  );
}
function initDefaultConfig2(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true,
      value: input.admin ?? null
    },
    uln: { index: 1, isWritable: false, value: input.uln ?? null },
    sendConfig: {
      index: 2,
      isWritable: true,
      value: input.sendConfig ?? null
    },
    receiveConfig: {
      index: 3,
      isWritable: true,
      value: input.receiveConfig ?? null
    },
    systemProgram: {
      index: 4,
      isWritable: false,
      value: input.systemProgram ?? null
    },
    eventAuthority: {
      index: 5,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 6,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitDefaultConfigInstructionDataSerializer2().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitUlnInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["endpoint", publicKey$1()],
        ["endpointProgram", publicKey$1()],
        ["admin", publicKey$1()]
      ],
      { description: "InitUlnInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([19, 215, 207, 92, 197, 112, 119, 240])
    })
  );
}
function initUln(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true,
      value: input.payer ?? null
    },
    uln: { index: 1, isWritable: true, value: input.uln ?? null },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitUlnInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitVerifyInstructionDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packetHeader", array$1(u8$1(), { size: 81 })],
        ["payloadHash", bytes$1({ size: 32 })],
        ["dvn", publicKey$1()]
      ],
      { description: "InitVerifyInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([76, 246, 244, 124, 115, 17, 235, 91])
    })
  );
}
function initVerify2(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true,
      value: input.payer ?? null
    },
    confirmations: {
      index: 1,
      isWritable: true,
      value: input.confirmations ?? null
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitVerifyInstructionDataSerializer2().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getQuoteInstructionDataSerializer3() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packet", getPacketSerializer2()],
        ["options", bytes$1({ size: u32$1() })],
        ["payInLzToken", bool$1()]
      ],
      { description: "QuoteInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([149, 42, 109, 247, 134, 146, 213, 123])
    })
  );
}
function quote3(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    endpoint: {
      index: 0,
      isWritable: false,
      value: input.endpoint ?? null
    },
    uln: { index: 1, isWritable: false, value: input.uln ?? null },
    sendConfig: {
      index: 2,
      isWritable: false,
      value: input.sendConfig ?? null
    },
    defaultSendConfig: {
      index: 3,
      isWritable: false,
      value: input.defaultSendConfig ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getQuoteInstructionDataSerializer3().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSendInstructionDataSerializer3() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packet", getPacketSerializer2()],
        ["options", bytes$1({ size: u32$1() })],
        ["nativeFee", u64$1()]
      ],
      { description: "SendInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([102, 251, 20, 187, 65, 75, 12, 69])
    })
  );
}
function send3(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    endpoint: {
      index: 0,
      isWritable: false,
      value: input.endpoint ?? null
    },
    uln: { index: 1, isWritable: false, value: input.uln ?? null },
    sendConfig: {
      index: 2,
      isWritable: false,
      value: input.sendConfig ?? null
    },
    defaultSendConfig: {
      index: 3,
      isWritable: false,
      value: input.defaultSendConfig ?? null
    },
    payer: {
      index: 4,
      isWritable: true,
      value: input.payer ?? null
    },
    treasury: {
      index: 5,
      isWritable: true,
      value: input.treasury ?? null
    },
    systemProgram: {
      index: 6,
      isWritable: false,
      value: input.systemProgram ?? null
    },
    eventAuthority: {
      index: 7,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 8,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSendInstructionDataSerializer3().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSendWithLzTokenInstructionDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packet", getPacketSerializer2()],
        ["options", bytes$1({ size: u32$1() })],
        ["nativeFee", u64$1()],
        ["lzTokenFee", u64$1()],
        ["lzTokenMint", publicKey$1()]
      ],
      { description: "SendWithLzTokenInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([165, 161, 84, 48, 129, 26, 193, 19])
    })
  );
}
function sendWithLzToken2(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    endpoint: {
      index: 0,
      isWritable: false,
      value: accounts.endpoint ?? null
    },
    uln: {
      index: 1,
      isWritable: false,
      value: accounts.uln ?? null
    },
    sendConfig: {
      index: 2,
      isWritable: false,
      value: accounts.sendConfig ?? null
    },
    defaultSendConfig: {
      index: 3,
      isWritable: false,
      value: accounts.defaultSendConfig ?? null
    },
    payer: {
      index: 4,
      isWritable: true,
      value: accounts.payer ?? null
    },
    systemProgram: {
      index: 5,
      isWritable: false,
      value: accounts.systemProgram ?? null
    },
    lzTokenSource: {
      index: 6,
      isWritable: true,
      value: accounts.lzTokenSource ?? null
    },
    lzTokenTreasury: {
      index: 7,
      isWritable: true,
      value: accounts.lzTokenTreasury ?? null
    },
    lzTokenMint: {
      index: 8,
      isWritable: false,
      value: accounts.lzTokenMint ?? null
    },
    tokenProgram: {
      index: 9,
      isWritable: false,
      value: accounts.tokenProgram ?? null
    },
    eventAuthority: {
      index: 10,
      isWritable: false,
      value: accounts.eventAuthority ?? null
    },
    program: {
      index: 11,
      isWritable: false,
      value: accounts.program ?? null
    }
  };
  const resolvedArgs = { ...args };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      "splToken",
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSendWithLzTokenInstructionDataSerializer2().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetConfigInstructionDataSerializer4() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["oapp", publicKey$1()],
        ["eid", u32$1()],
        ["configType", u32$1()],
        ["config", bytes$1({ size: u32$1() })]
      ],
      { description: "SetConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([108, 158, 154, 175, 212, 98, 52, 66])
    })
  );
}
function setConfig4(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    endpoint: {
      index: 0,
      isWritable: false,
      value: input.endpoint ?? null
    },
    uln: { index: 1, isWritable: false, value: input.uln ?? null },
    sendConfig: {
      index: 2,
      isWritable: true,
      value: input.sendConfig ?? null
    },
    receiveConfig: {
      index: 3,
      isWritable: true,
      value: input.receiveConfig ?? null
    },
    defaultSendConfig: {
      index: 4,
      isWritable: false,
      value: input.defaultSendConfig ?? null
    },
    defaultReceiveConfig: {
      index: 5,
      isWritable: false,
      value: input.defaultReceiveConfig ?? null
    },
    eventAuthority: {
      index: 6,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 7,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetConfigInstructionDataSerializer4().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetDefaultConfigInstructionDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["eid", u32$1()],
        ["sendUlnConfig", option(getUlnConfigSerializer2())],
        ["receiveUlnConfig", option(getUlnConfigSerializer2())],
        ["executorConfig", option(getExecutorConfigSerializer())]
      ],
      { description: "SetDefaultConfigInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([111, 228, 17, 75, 5, 76, 213, 169])
    })
  );
}
function setDefaultConfig2(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    uln: { index: 1, isWritable: false, value: input.uln ?? null },
    sendConfig: {
      index: 2,
      isWritable: true,
      value: input.sendConfig ?? null
    },
    receiveConfig: {
      index: 3,
      isWritable: true,
      value: input.receiveConfig ?? null
    },
    eventAuthority: {
      index: 4,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 5,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetDefaultConfigInstructionDataSerializer2().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetTreasuryInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["treasury", option(getTreasurySerializer())]
      ],
      { description: "SetTreasuryInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([57, 97, 196, 95, 195, 206, 106, 136])
    })
  );
}
function setTreasury(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    signer: {
      index: 0,
      isWritable: false,
      value: input.signer ?? null
    },
    uln: { index: 1, isWritable: true, value: input.uln ?? null },
    eventAuthority: {
      index: 2,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 3,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetTreasuryInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getTransferAdminInstructionDataSerializer3() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["admin", publicKey$1()]
      ],
      { description: "TransferAdminInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([42, 242, 66, 106, 228, 10, 111, 156])
    })
  );
}
function transferAdmin3(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: accounts.admin ?? null
    },
    uln: { index: 1, isWritable: true, value: accounts.uln ?? null },
    eventAuthority: {
      index: 2,
      isWritable: false,
      value: accounts.eventAuthority ?? null
    },
    program: {
      index: 3,
      isWritable: false,
      value: accounts.program ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getTransferAdminInstructionDataSerializer3().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getVerifyInstructionDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["packetHeader", array$1(u8$1(), { size: 81 })],
        ["payloadHash", bytes$1({ size: 32 })],
        ["confirmations", u64$1()]
      ],
      { description: "VerifyInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([133, 161, 141, 48, 120, 198, 88, 150])
    })
  );
}
function verify2(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    dvn: {
      index: 0,
      isWritable: false,
      value: accounts.dvn ?? null
    },
    confirmations: {
      index: 1,
      isWritable: true,
      value: accounts.confirmations ?? null
    },
    eventAuthority: {
      index: 2,
      isWritable: false,
      value: accounts.eventAuthority ?? null
    },
    program: {
      index: 3,
      isWritable: false,
      value: accounts.program ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getVerifyInstructionDataSerializer2().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getVersionInstructionDataSerializer3() {
  return mapSerializer(
    struct([["discriminator", bytes$1({ size: 8 })]], {
      description: "VersionInstructionData"
    }),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([118, 65, 195, 198, 129, 216, 252, 192])
    })
  );
}
function version3(context) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {};
  const orderedAccounts = Object.values(
    resolvedAccounts
  );
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getVersionInstructionDataSerializer3().serialize({});
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getWithdrawRentInstructionDataSerializer2() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["amount", u64$1()]
      ],
      { description: "WithdrawRentInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([226, 7, 41, 158, 173, 111, 192, 107])
    })
  );
}
function withdrawRent2(context, input) {
  const programId = context.programs.getPublicKey(
    "uln",
    "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: input.admin ?? null
    },
    uln: { index: 1, isWritable: true, value: input.uln ?? null },
    receiver: {
      index: 2,
      isWritable: true,
      value: input.receiver ?? null
    },
    eventAuthority: {
      index: 3,
      isWritable: false,
      value: input.eventAuthority ?? null
    },
    program: {
      index: 4,
      isWritable: false,
      value: input.program ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners5(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getWithdrawRentInstructionDataSerializer2().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}

// src/generated/kinobi/uln/programs/uln.ts
var ULN_PROGRAM_ID = "7a4WjyR8VZ7yZz5XJAKm39BUGn5iT9CKcv2pmG9tdXVH";

// src/uln.ts
var Uln2 = class {
  /**
   * Creates an instance of the Uln class.
   *
   * @param {PublicKey} programId - The program public key.
   */
  constructor(programId = ULN_PROGRAM_ID, rpc) {
    this.programId = programId;
    this.pda = new UlnPDA(programId);
    if (rpc === void 0) {
      rpc = createNullRpc();
      rpc.getCluster = () => "custom";
    }
    this.programRepo = createDefaultProgramRepository({ rpc }, [
      {
        name: "uln",
        publicKey: programId,
        getErrorFromCode(code, cause) {
          return getUlnErrorFromCode(code, this, cause);
        },
        getErrorFromName(name, cause) {
          return getUlnErrorFromName(name, this, cause);
        },
        isOnCluster() {
          return true;
        }
      }
    ]);
    this.eventAuthority = new EventPDA(programId).eventAuthority()[0];
  }
  getProgram(clusterFilter = "custom") {
    return this.programRepo.get("uln", clusterFilter);
  }
  initUln(payer, param) {
    const { admin, eid, endpointProgram } = param;
    const [msgLib] = this.pda.messageLib();
    const [endpointAuth] = new EndpointPDA(endpointProgram ?? ENDPOINT_PROGRAM_ID).messageLibraryInfo(msgLib);
    return initUln(
      { programs: this.programRepo, payer },
      {
        payer,
        uln: this.pda.setting(),
        // param
        eid,
        admin,
        endpoint: endpointAuth,
        endpointProgram: endpointProgram ?? ENDPOINT_PROGRAM_ID
      }
    ).items[0];
  }
  /**
   * Initializes or updates the default configuration.
   * before calling this function, you should call initUln to initialize the uln
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} admin - The admin public key.
   * @param {number} eid - The endpoint ID.
   * @param {types.UlnConfig} sendUlnConfig - The send ULN configuration.
   * @param {types.UlnConfig} receiveUlnConfig - The receive ULN configuration.
   * @param {types.ExecutorConfig} executorConfig - The executor configuration.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   */
  async initOrUpdateDefaultConfig(rpc, admin, param) {
    const { remote, sendUlnConfig, receiveUlnConfig, executorConfig } = param;
    const [setting] = this.pda.setting();
    const [sendConfig] = this.pda.defaultSendConfig(remote);
    const [receiveConfigPDA] = this.pda.defaultReceiveConfig(remote);
    const sendConfigExists = await rpc.accountExists(sendConfig, { commitment: "confirmed" });
    if (sendConfigExists) {
      return setDefaultConfig2(
        { programs: this.programRepo },
        {
          admin,
          uln: setting,
          sendConfig,
          receiveConfig: receiveConfigPDA,
          eventAuthority: this.eventAuthority,
          program: this.programId,
          // param
          eid: remote,
          sendUlnConfig,
          receiveUlnConfig,
          executorConfig
        }
      ).items[0];
    } else {
      invariant6(isSome(sendUlnConfig), "sendUlnConfig is required");
      invariant6(isSome(receiveUlnConfig), "receiveUlnConfig is required");
      invariant6(isSome(executorConfig), "executorConfig is required");
      return initDefaultConfig2(
        { programs: this.programRepo },
        {
          admin,
          uln: setting,
          sendConfig,
          receiveConfig: receiveConfigPDA,
          eventAuthority: this.eventAuthority,
          program: this.programId,
          // param
          eid: remote,
          sendUlnConfig: sendUlnConfig.value,
          receiveUlnConfig: receiveUlnConfig.value,
          executorConfig: executorConfig.value
        }
      ).items[0];
    }
  }
  // msg_lib -> msg_lib_program ->
  /**
   * Sets the treasury.
   * before calling this function, you should call initUln to initialize the uln
   *
   * @param {PublicKey} admin - The admin public key.
   * @param {object} treasury - The treasury object.
   * @param {PublicKey} treasury.admin - The treasury admin public key.
   * @param {PublicKey} treasury.nativeReceiver - The native receiver public key.
   * @param {number} treasury.nativeFeeBps - The native fee basis points.
   * @param {object} treasury.lzToken - The LZ token object.
   * @param {PublicKey} treasury.lzToken.receiver - The LZ token receiver public key.
   * @param {number} treasury.lzToken.fee - The LZ token fee.
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   */
  setTreasury(admin, treasury) {
    return setTreasury(
      { programs: this.programRepo },
      {
        signer: admin,
        uln: this.pda.setting(),
        eventAuthority: this.eventAuthority,
        program: this.programId,
        // param
        treasury
      }
    ).items[0];
  }
  transferAdmin(admin, newAdmin) {
    return transferAdmin3(
      { programs: this.programRepo },
      {
        admin,
        uln: this.pda.setting(),
        eventAuthority: this.eventAuthority,
        program: this.programId
      },
      { admin: newAdmin }
    ).items[0];
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the quote instruction.
   *
   * @param {RpcInterface} rpc - The connection to the Solana cluster.
   * @param {PublicKey} _payer - The payer public key.
   * @param {SolanaPacketPath} path - The packet path.
   * @param {Commitment} [commitment] - The commitment level.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  async getQuoteIXAccountMetaForCPI(rpc, _payer, path, commitment = "confirmed") {
    const { sender, dstEid } = path;
    const txBuilder = quote3(
      { programs: this.programRepo },
      {
        endpoint: createNoopSigner(defaultPublicKey()),
        uln: this.pda.setting(),
        sendConfig: this.pda.sendConfig(dstEid, sender),
        defaultSendConfig: this.pda.defaultSendConfig(dstEid),
        packet: {
          nonce: 0,
          srcEid: 0,
          sender: defaultPublicKey(),
          dstEid: 0,
          receiver: new Uint8Array(32),
          guid: new Uint8Array(32),
          message: new Uint8Array(4)
        },
        options: new Uint8Array(4),
        payInLzToken: false
      }
    );
    return txBuilder.addRemainingAccounts(await this.getSendIxRemainingAccounts(rpc, sender, dstEid, false, commitment)).getInstructions()[0].keys.slice(1);
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the send instruction.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {PacketPath} path - The packet path.
   * @param {Commitment} [commitment='confirmed'] - The commitment level.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  async getSendIXAccountMetaForCPI(rpc, payer, path, commitment = "confirmed") {
    const { sender, dstEid } = path;
    const ulnState = await this.getSetting(rpc, commitment);
    let treasury;
    if (ulnState !== null && isSome(ulnState.treasury)) {
      treasury = ulnState.treasury.value.nativeReceiver;
    }
    let txBuilder = send3(
      { payer: createNoopSigner(payer), programs: this.programRepo },
      {
        endpoint: createNoopSigner(defaultPublicKey()),
        payer: createNoopSigner(payer),
        uln: this.pda.setting(),
        sendConfig: this.pda.sendConfig(dstEid, sender),
        defaultSendConfig: this.pda.defaultSendConfig(dstEid),
        treasury,
        eventAuthority: this.eventAuthority,
        program: this.programId,
        // param
        packet: {
          nonce: 0,
          srcEid: 0,
          sender: defaultPublicKey(),
          dstEid: 0,
          receiver: new Uint8Array(32),
          guid: new Uint8Array(32),
          message: new Uint8Array(4)
        },
        options: new Uint8Array(4),
        nativeFee: 0
      }
    );
    txBuilder = txBuilder.addRemainingAccounts(
      await this.getSendIxRemainingAccounts(rpc, sender, dstEid, true, commitment)
    );
    const accounts = txBuilder.getInstructions()[0].keys.map((key) => {
      if (payer !== key.pubkey) {
        key.isSigner = false;
      }
      if (treasury !== void 0 && key.pubkey === treasury) {
        key.isWritable = true;
      }
      return key;
    });
    return accounts.slice(1);
  }
  /***
   * Get the account meta of the send instruction for CPI(Cross-Program Invocation ) with lzToken
   */
  async getSendWithLzTokenIXAccountMetaForCPI(rpc, accounts, path, programs, commitment = "confirmed") {
    const { payer, tokenMint, tokenSource } = accounts;
    const { sender, dstEid } = path;
    const ulnState = await this.getSetting(rpc, commitment);
    invariant6(ulnState !== null, "ULN not initialized");
    invariant6(isSome(ulnState.treasury), "treasury not set");
    invariant6(isSome(ulnState.treasury.value.lzToken), "lzToken treasury not set");
    const lzTokenTreasury = ulnState.treasury.value.lzToken.value.receiver;
    let txBuilder = sendWithLzToken2(
      { payer: createNoopSigner(payer), programs: this.programRepo },
      {
        endpoint: createNoopSigner(defaultPublicKey()),
        payer: createNoopSigner(payer),
        uln: this.pda.setting(),
        sendConfig: this.pda.sendConfig(dstEid, sender),
        defaultSendConfig: this.pda.defaultSendConfig(dstEid),
        lzTokenTreasury,
        eventAuthority: this.eventAuthority,
        program: this.programId,
        lzTokenSource: tokenSource,
        lzTokenMint: tokenMint,
        tokenProgram: programs.tokenProgram ?? fromWeb3JsPublicKey(TOKEN_PROGRAM_ID)
      },
      {
        packet: {
          nonce: 0,
          srcEid: 0,
          sender: defaultPublicKey(),
          dstEid: 0,
          receiver: new Uint8Array(32),
          guid: new Uint8Array(32),
          message: new Uint8Array(4)
        },
        options: new Uint8Array(4),
        nativeFee: 0,
        lzTokenFee: 0,
        lzTokenMint: tokenMint
      }
    );
    txBuilder = txBuilder.addRemainingAccounts(
      await this.getSendIxRemainingAccounts(rpc, sender, dstEid, true, commitment)
    );
    const keys = txBuilder.getInstructions()[0].keys.map((key) => {
      if (payer !== key.pubkey) {
        key.isSigner = false;
      }
      if (key.pubkey === lzTokenTreasury) {
        key.isWritable = true;
      }
      return key;
    });
    return keys.slice(1);
  }
  // 3 RPC requests
  /**
   * Gets the remaining accounts for the send instruction.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} sender - The sender public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {boolean} payment - Indicates if payment is required.
   * @param {Commitment | GetAccountInfoConfig} [commitment='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  async getSendIxRemainingAccounts(rpc, sender, dstEid, payment, commitment = "confirmed") {
    const { executor, dvns } = await this.getWorkers(rpc, sender, dstEid);
    const priceFeeds = new Array();
    [executor.config, ...dvns.map((dvn) => dvn.config)].forEach((config2) => {
      if (!priceFeeds.find((p) => p === config2.priceFeed)) {
        priceFeeds.push(config2.priceFeed);
      }
    });
    const priceFeedInfos = await rpc.getAccounts(priceFeeds, { commitment });
    const priceFeedDict = priceFeedInfos.reduce((acc, info, i) => {
      invariant6(info.exists, `priceFeed:${priceFeeds[i]} not initialized`);
      acc.set(priceFeeds[i], info.owner);
      return acc;
    }, /* @__PURE__ */ new Map());
    const executorAccounts = new Executor2(executor.owner).getQuoteIXAccountMetaForCPI(
      executor.config.priceFeed,
      // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
      priceFeedDict.get(executor.config.priceFeed),
      payment
    );
    const dvnAccounts = dvns.map((p, i) => {
      return new DVN2(p.owner).getQuoteIXAccountMetaForCPI(
        p.config.priceFeed,
        // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
        priceFeedDict.get(p.config.priceFeed),
        payment
      );
    });
    return executorAccounts.concat(dvnAccounts.flat());
  }
  /**
   * Gets all workers (executor and DVN).
   * 2 RPC requests
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} sender - The sender public key.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitment='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<{ executor: { config: ExecutorAccounts.ExecutorConfig; owner: PublicKey }; dvns: { config: DvnAccounts.DvnConfig; owner: PublicKey }[] }>} A promise that resolves to an object containing the executor and DVN configurations.
   */
  async getWorkers(rpc, sender, eid, commitment = "confirmed") {
    const [defaultSendConfig] = this.pda.defaultSendConfig(eid);
    const [sendConfig] = this.pda.sendConfig(eid, sender);
    const [defaultSendConfigBuf, sendConfigBuf] = await rpc.getAccounts([defaultSendConfig, sendConfig], {
      commitment
    });
    invariant6(defaultSendConfigBuf.exists, "defaultSendConfig not initialized");
    const defaultSendConfigState = deserializeSendConfig(defaultSendConfigBuf);
    const sendConfigState = sendConfigBuf.exists ? deserializeSendConfig(sendConfigBuf) : null;
    const finalConfigState = this.getUlnConfigState(defaultSendConfigState, sendConfigState);
    const finalExecutorConfigState = this.getExecutorConfigState(defaultSendConfigState, sendConfigState);
    const { requiredDvns, optionalDvns } = finalConfigState;
    const { executor } = finalExecutorConfigState;
    const dvns = requiredDvns.concat(optionalDvns);
    const [executorBuf, ...dvnBuf] = await rpc.getAccounts([executor, ...dvns], {
      commitment
    });
    invariant6(executorBuf.exists, `executor:${executor} not initialized`);
    return {
      executor: {
        config: accounts_exports10.deserializeExecutorConfig(executorBuf),
        owner: executorBuf.owner
      },
      dvns: dvnBuf.map((dvn, i) => {
        invariant6(dvn.exists, `dvn:${dvns[i]} not initialized`);
        return {
          config: accounts_exports9.deserializeDvnConfig(dvn),
          owner: dvn.owner
        };
      })
    };
  }
  /**
   * Initializes the verification process.
   *
   */
  initVerify(payer, param) {
    const { dvn, packetBytes } = param;
    const packet = PacketV1Codec.fromBytes(packetBytes);
    const headerHash = packet.headerHash();
    const headerHashBytes = Uint8Array.from(Buffer.from(headerHash.slice(2), "hex"));
    const payloadHash = packet.payloadHash();
    const payloadHashBytes = Uint8Array.from(Buffer.from(payloadHash.slice(2), "hex"));
    return initVerify2(
      { programs: this.programRepo, payer },
      {
        payer,
        confirmations: this.pda.confirmations(headerHashBytes, payloadHashBytes, dvn),
        // param
        packetHeader: Array.from(Uint8Array.from(Buffer.from(packet.header().slice(2), "hex"))),
        payloadHash: payloadHashBytes,
        dvn
      }
    ).items[0];
  }
  verify(dvn, param) {
    const { packetBytes, confirmations } = param;
    const packet = PacketV1Codec.fromBytes(packetBytes);
    const headerHash = packet.headerHash();
    const headerHashBytes = Uint8Array.from(Buffer.from(headerHash.slice(2), "hex"));
    const payloadHash = packet.payloadHash();
    const payloadHashBytes = Uint8Array.from(Buffer.from(payloadHash.slice(2), "hex"));
    return verify2(
      { programs: this.programRepo },
      {
        dvn,
        confirmations: this.pda.confirmations(headerHashBytes, payloadHashBytes, dvn.publicKey),
        eventAuthority: this.eventAuthority,
        program: this.programId
      },
      {
        packetHeader: Array.from(Uint8Array.from(Buffer.from(packet.header().slice(2), "hex"))),
        payloadHash: payloadHashBytes,
        confirmations
      }
    ).items[0];
  }
  closeVerify(dvn, param) {
    const { receiver, packetBytes } = param;
    const packet = PacketV1Codec.fromBytes(packetBytes);
    const headerHash = packet.headerHash();
    const headerHashBytes = Uint8Array.from(Buffer.from(headerHash.slice(2), "hex"));
    const payloadHash = packet.payloadHash();
    const payloadHashBytes = Uint8Array.from(Buffer.from(payloadHash.slice(2), "hex"));
    return closeVerify(
      { programs: this.programRepo },
      {
        dvn,
        receiver,
        confirmations: this.pda.confirmations(headerHashBytes, payloadHashBytes, dvn.publicKey),
        // param
        packetHeaderHash: headerHashBytes,
        payloadHash: payloadHashBytes
      }
    ).items[0];
  }
  /**
   * Checks if the DVN is verified.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {PublicKey} dvn - The DVN public key.
   * @param {Uint8Array} packetBytes - The packet bytes.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<boolean>} A promise that resolves to true if the DVN is verified, false otherwise.
   */
  async isDvnVerified(rpc, endpointProgram, dvn, packetBytes, commitment = "confirmed") {
    const packet = PacketV1Codec.fromBytes(packetBytes);
    const headerHash = packet.headerHash();
    const headerHashBytes = Uint8Array.from(Buffer.from(headerHash.slice(2), "hex"));
    const payloadHash = packet.payloadHash();
    const payloadHashBytes = Uint8Array.from(Buffer.from(payloadHash.slice(2), "hex"));
    const [confirmationsPDA] = this.pda.confirmations(headerHashBytes, payloadHashBytes, dvn);
    const endpoint = new endpoint_exports2.Endpoint(endpointProgram);
    const receiver = publicKey(arrayify(packet.receiver()));
    const sender = arrayify(packet.sender());
    const nonce = await endpoint.getNonce(rpc, receiver, packet.srcEid(), sender, commitment);
    const pendingNonce = await endpoint.getPendingInboundNonce(rpc, receiver, packet.srcEid(), sender, commitment);
    if (!nonce || !pendingNonce) {
      return false;
    }
    if (parseInt(nonce.inboundNonce.toString()) >= parseInt(packet.nonce())) {
      return true;
    }
    if (pendingNonce.nonces.find((n) => n.toString() === packet.nonce()) !== void 0) {
      return true;
    }
    const confirmation = await safeFetchConfirmations({ rpc }, confirmationsPDA, { commitment });
    if (confirmation?.value == null) {
      return false;
    } else {
      return true;
    }
  }
  /**
   * Commits the verification process.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {Uint8Array} packetBytes - The packet bytes.
   * @returns {Promise<TransactionInstruction>} A promise that resolves to the transaction instruction.
   */
  async commitVerification(rpc, packetBytes, endpointProgram = ENDPOINT_PROGRAM_ID) {
    const packet = PacketV1Codec.fromBytes(packetBytes);
    const payloadHash = packet.payloadHash();
    const srcEid = packet.srcEid();
    const receiver = publicKey(arrayify(packet.receiver()));
    const payloadHashBytes = Uint8Array.from(Buffer.from(payloadHash.slice(2), "hex"));
    const [msgLib] = this.pda.messageLib();
    const receiveConfigState = await this.getFinalReceiveConfigState(rpc, receiver, srcEid);
    const headerHash = packet.headerHash();
    const headerHashBytes = Uint8Array.from(Buffer.from(headerHash.slice(2), "hex"));
    const confirmations = receiveConfigState.requiredDvns.concat(receiveConfigState.optionalDvns).map((p) => {
      const [confirmationsPDA] = this.pda.confirmations(headerHashBytes, payloadHashBytes, p);
      return {
        pubkey: confirmationsPDA,
        isSigner: false,
        isWritable: true
      };
    });
    const endpoint = new endpoint_exports2.Endpoint(endpointProgram);
    const verifyAccounts = endpoint.getVerifyIXAccountMetaForCPI(packet, msgLib);
    const txBuilder = commitVerification(
      { programs: this.programRepo },
      {
        uln: this.pda.setting(),
        defaultReceiveConfig: this.pda.defaultReceiveConfig(srcEid),
        receiveConfig: this.pda.receiveConfig(srcEid, receiver),
        // param
        packetHeader: Array.from(arrayify(packet.header())),
        payloadHash: payloadHashBytes
      }
    );
    return txBuilder.addRemainingAccounts(confirmations.concat(verifyAccounts)).items[0];
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the init config instruction.
   *
   * @param {PublicKey} payer - The payer public key.
   * @param {PublicKey} oappID - The OApp ID public key.
   * @param {number} eid - The endpoint ID.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getInitConfigIXAccountMetaForCPI(payer, oappID, eid) {
    const txBuilder = initConfig3(
      { payer: createNoopSigner(payer), programs: this.programRepo },
      {
        endpoint: createNoopSigner(defaultPublicKey()),
        payer: createNoopSigner(payer),
        uln: this.pda.setting(),
        sendConfig: this.pda.sendConfig(eid, oappID),
        receiveConfig: this.pda.receiveConfig(eid, oappID),
        // params
        oapp: defaultPublicKey(),
        eid: 0
      }
    );
    const accounts = txBuilder.getInstructions()[0].keys.map((key) => {
      if (payer !== key.pubkey) {
        key.isSigner = false;
      }
      return key;
    });
    return accounts.slice(1);
  }
  /**
   * Constructs the set config data.
   *
   * @param {SetConfigType} configType - The configuration type.
   * @param {types.ExecutorConfig | types.UlnConfig} configData - The configuration data.
   * @returns {Uint8Array} The constructed set config data.
   * @throws {Error} If the configuration type is invalid.
   */
  static constructSetConfigData(configType, configData) {
    switch (configType) {
      case 1 /* EXECUTOR */: {
        return getExecutorConfigSerializer().serialize(configData);
      }
      case 2 /* SEND_ULN */:
      case 3 /* RECEIVE_ULN */: {
        return getUlnConfigSerializer2().serialize(configData);
      }
      default:
        throw new Error(`invalid config type: ${configType}`);
    }
  }
  /**
   * Gets the account metadata for the CPI (Cross-Program Invocation) of the set config instruction.
   *
   * @param {PublicKey} endpointProgram - The endpoint program public key.
   * @param {PublicKey} oappID - The OApp ID public key.
   * @param {number} eid - The endpoint ID.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  getSetConfigIXAccountMetaForCPI(oapp, eid, endpointProgram = ENDPOINT_PROGRAM_ID) {
    const [msgLib] = this.pda.messageLib();
    const [msgLibInfo] = new EndpointPDA(endpointProgram).messageLibraryInfo(msgLib);
    const txBuilder = setConfig4(
      { programs: this.programRepo },
      {
        endpoint: createNoopSigner(msgLibInfo),
        uln: this.pda.setting(),
        sendConfig: this.pda.sendConfig(eid, oapp),
        defaultSendConfig: this.pda.defaultSendConfig(eid),
        defaultReceiveConfig: this.pda.defaultReceiveConfig(eid),
        receiveConfig: this.pda.receiveConfig(eid, oapp),
        eventAuthority: this.eventAuthority,
        program: this.programId,
        // param
        oapp: defaultPublicKey(),
        eid: 0,
        configType: 0,
        config: new Uint8Array(32)
      }
    );
    return txBuilder.getInstructions()[0].keys.slice(1);
  }
  /**
   * Gets the ULN settings.
   *
   * @param {RpcInterface} rpc - The connection to the Solana cluster.
   * @param {Commitment} [commitment] - The commitment level or account info configuration.
   * @returns {Promise<accounts.UlnSettings | null>} A promise that resolves to the ULN settings or null if not found.
   */
  async getSetting(rpc, commitment = "confirmed") {
    const [setting] = this.pda.setting();
    return safeFetchUlnSettings({ rpc }, setting, { commitment });
  }
  /**
   * Gets the default send configuration state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.SendConfig | null>} A promise that resolves to the default send configuration state or null if not found.
   */
  async getDefaultSendConfigState(rpc, eid, commitment = "confirmed") {
    const [config2] = this.pda.defaultSendConfig(eid);
    return safeFetchSendConfig({ rpc }, config2, { commitment });
  }
  /**
   * Gets the send configuration state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} sender - The sender public key.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.SendConfig | null>} A promise that resolves to the send configuration state or null if not found.
   */
  async getSendConfigState(rpc, sender, eid, commitment = "confirmed") {
    const [config2] = this.pda.sendConfig(eid, sender);
    return safeFetchSendConfig({ rpc }, config2, { commitment });
  }
  /**
   * Gets the default receive configuration state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.ReceiveConfig | null>} A promise that resolves to the default receive configuration state or null if not found.
   */
  async getDefaultReceiveConfigState(rpc, eid, commitment = "confirmed") {
    const [config2] = this.pda.defaultReceiveConfig(eid);
    return safeFetchReceiveConfig2({ rpc }, config2, { commitment });
  }
  /**
   * Gets the receive configuration state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig] - The commitment level or account info configuration.
   * @returns {Promise<accounts.ReceiveConfig | null>} A promise that resolves to the receive configuration state or null if not found.
   */
  async getReceiveConfigState(rpc, receiver, eid, commitment = "confirmed") {
    const [config2] = this.pda.receiveConfig(eid, receiver);
    return safeFetchReceiveConfig2({ rpc }, config2, { commitment });
  }
  /**
   * Gets the final receive configuration state.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} receiver - The receiver public key.
   * @param {number} eid - The endpoint ID.
   * @param {Commitment | GetAccountInfoConfig} [commitmentOrConfig='confirmed'] - The commitment level or account info configuration.
   * @returns {Promise<accounts.ReceiveConfig>} A promise that resolves to the final receive configuration state.
   * @throws {Error} If no DVN is found.
   */
  async getFinalReceiveConfigState(rpc, receiver, eid, commitment = "confirmed") {
    const [defaultConfig] = this.pda.defaultReceiveConfig(eid);
    const [customConfig] = this.pda.receiveConfig(eid, receiver);
    const [defaultConfigInfo, customConfigInfo] = await rpc.getAccounts([defaultConfig, customConfig], {
      commitment
    });
    if (!defaultConfigInfo.exists) {
      throw new Error(`please init default receive config first. ${defaultConfig}`);
    }
    const defaultConfigState = deserializeReceiveConfig2(defaultConfigInfo);
    const customConfigState = customConfigInfo.exists ? deserializeReceiveConfig2(customConfigInfo) : null;
    return this.getUlnConfigState(defaultConfigState, customConfigState);
  }
  async getFinalSendConfigState(rpc, sender, eid, commitment = "confirmed") {
    const [defaultConfig] = this.pda.defaultSendConfig(eid);
    const [customConfig] = this.pda.sendConfig(eid, sender);
    const [defaultConfigInfo, customConfigInfo] = await rpc.getAccounts([defaultConfig, customConfig], {
      commitment
    });
    if (!defaultConfigInfo.exists) {
      throw new Error(`please init default send config first. ${defaultConfig}`);
    }
    const defaultConfigState = deserializeSendConfig(defaultConfigInfo);
    const customConfigState = customConfigInfo.exists ? deserializeSendConfig(customConfigInfo) : null;
    return this.getUlnConfigState(defaultConfigState, customConfigState);
  }
  getUlnConfigState(defaultConfigState, customConfigState) {
    const NIL_CONFIRMATIONS = "18446744073709551615";
    const NIL_DVN_COUNT = "255";
    const rtn_config = {
      confirmations: 0n,
      requiredDvnCount: 0,
      optionalDvnCount: 0,
      optionalDvnThreshold: 0,
      requiredDvns: [],
      optionalDvns: []
    };
    if (customConfigState == null || customConfigState.uln.confirmations === 0n) {
      rtn_config.confirmations = defaultConfigState.uln.confirmations;
    } else if (customConfigState.uln.confirmations.toString() !== NIL_CONFIRMATIONS) {
      rtn_config.confirmations = customConfigState.uln.confirmations;
    }
    if (customConfigState == null || customConfigState.uln.requiredDvnCount == 0) {
      if (defaultConfigState.uln.requiredDvnCount > 0) {
        rtn_config.requiredDvns = defaultConfigState.uln.requiredDvns;
        rtn_config.requiredDvnCount = defaultConfigState.uln.requiredDvnCount;
      }
    } else if (customConfigState.uln.requiredDvnCount.toString() !== NIL_DVN_COUNT) {
      rtn_config.requiredDvns = customConfigState.uln.requiredDvns;
      rtn_config.requiredDvnCount = customConfigState.uln.requiredDvnCount;
    }
    if (customConfigState == null || customConfigState.uln.optionalDvnCount == 0) {
      if (defaultConfigState.uln.optionalDvnCount > 0) {
        rtn_config.optionalDvns = defaultConfigState.uln.optionalDvns;
        rtn_config.optionalDvnCount = defaultConfigState.uln.optionalDvnCount;
        rtn_config.optionalDvnThreshold = defaultConfigState.uln.optionalDvnThreshold;
      }
    } else if (customConfigState.uln.optionalDvnCount.toString() !== NIL_DVN_COUNT) {
      rtn_config.optionalDvns = customConfigState.uln.optionalDvns;
      rtn_config.optionalDvnCount = customConfigState.uln.optionalDvnCount;
      rtn_config.optionalDvnThreshold = customConfigState.uln.optionalDvnThreshold;
    }
    if (rtn_config.requiredDvnCount === 0 && rtn_config.optionalDvnCount === 0) {
      throw new Error("no dvn");
    }
    return rtn_config;
  }
  getExecutorConfigState(defaultConfigState, customConfigState) {
    const rtn_config = {
      maxMessageSize: 0,
      executor: defaultPublicKey()
    };
    if (customConfigState == null || customConfigState.executor.executor === defaultPublicKey()) {
      rtn_config.executor = defaultConfigState.executor.executor;
    } else {
      rtn_config.executor = customConfigState.executor.executor;
    }
    if (customConfigState == null || customConfigState.executor.maxMessageSize === 0) {
      rtn_config.maxMessageSize = defaultConfigState.executor.maxMessageSize;
    } else {
      rtn_config.maxMessageSize = customConfigState.executor.maxMessageSize;
    }
    return rtn_config;
  }
};

// src/pricefeed.ts
var pricefeed_exports2 = {};
__export(pricefeed_exports2, {
  PRICEFEED_PROGRAM_ID: () => PRICEFEED_PROGRAM_ID,
  PriceFeed: () => PriceFeed3,
  accounts: () => accounts_exports12,
  errors: () => errors_exports10,
  instructions: () => instructions_exports12,
  types: () => types_exports12
});

// src/generated/kinobi/pricefeed/accounts/index.ts
var accounts_exports12 = {};
__export(accounts_exports12, {
  deserializePriceFeed: () => deserializePriceFeed,
  fetchAllPriceFeed: () => fetchAllPriceFeed,
  fetchPriceFeed: () => fetchPriceFeed,
  getPriceFeedAccountDataSerializer: () => getPriceFeedAccountDataSerializer,
  getPriceFeedGpaBuilder: () => getPriceFeedGpaBuilder,
  safeFetchAllPriceFeed: () => safeFetchAllPriceFeed,
  safeFetchPriceFeed: () => safeFetchPriceFeed
});

// src/generated/kinobi/pricefeed/types/index.ts
var types_exports12 = {};
__export(types_exports12, {
  getModelTypeSerializer: () => getModelTypeSerializer,
  getPriceParamsSerializer: () => getPriceParamsSerializer,
  getPriceSerializer: () => getPriceSerializer,
  isModelType: () => isModelType,
  modelType: () => modelType
});
function getModelTypeSerializer() {
  return dataEnum(
    [
      [
        "Arbitrum",
        struct([
          ["gasPerL2Tx", u64$1()],
          ["gasPerL1CalldataByte", u32$1()]
        ])
      ],
      [
        "Optimism",
        struct([
          ["l1Eid", u32$1()]
        ])
      ]
    ],
    { description: "ModelType" }
  );
}
function modelType(kind, data) {
  return Array.isArray(data) ? { __kind: kind, fields: data } : { __kind: kind, ...data ?? {} };
}
function isModelType(kind, value) {
  return value.__kind === kind;
}
function getPriceSerializer() {
  return struct(
    [
      ["eid", u32$1()],
      ["priceRatio", u128()],
      ["gasPriceInUnit", u64$1()],
      ["gasPerByte", u32$1()],
      ["modelType", option(getModelTypeSerializer())]
    ],
    { description: "Price" }
  );
}
function getPriceParamsSerializer() {
  return struct(
    [
      ["priceRatio", u128()],
      ["gasPriceInUnit", u64$1()],
      ["gasPerByte", u32$1()],
      ["modelType", option(getModelTypeSerializer())]
    ],
    { description: "PriceParams" }
  );
}

// src/generated/kinobi/pricefeed/accounts/priceFeed.ts
function getPriceFeedAccountDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["admin", publicKey$1()],
        ["updaters", array$1(publicKey$1())],
        ["priceRatioDenominator", u128()],
        ["arbitrumCompressionPercent", u128()],
        ["nativeTokenPriceUsd", option(u128())],
        ["prices", array$1(getPriceSerializer())],
        ["bump", u8$1()]
      ],
      { description: "PriceFeedAccountData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([189, 103, 252, 23, 152, 35, 243, 156])
    })
  );
}
function deserializePriceFeed(rawAccount) {
  return deserializeAccount(rawAccount, getPriceFeedAccountDataSerializer());
}
async function fetchPriceFeed(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  assertAccountExists(maybeAccount, "PriceFeed");
  return deserializePriceFeed(maybeAccount);
}
async function safeFetchPriceFeed(context, publicKey124, options) {
  const maybeAccount = await context.rpc.getAccount(
    publicKey(publicKey124, false),
    options
  );
  return maybeAccount.exists ? deserializePriceFeed(maybeAccount) : null;
}
async function fetchAllPriceFeed(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, "PriceFeed");
    return deserializePriceFeed(maybeAccount);
  });
}
async function safeFetchAllPriceFeed(context, publicKeys, options) {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => publicKey(key, false)),
    options
  );
  return maybeAccounts.filter((maybeAccount) => maybeAccount.exists).map((maybeAccount) => deserializePriceFeed(maybeAccount));
}
function getPriceFeedGpaBuilder(context) {
  const programId = context.programs.getPublicKey(
    "pricefeed",
    "8ahPGPjEbpgGaZx2NV1iG5Shj7TDwvsjkEDcGWjt94TP"
  );
  return gpaBuilder(context, programId).registerFields({
    discriminator: [0, bytes$1({ size: 8 })],
    admin: [8, publicKey$1()],
    updaters: [40, array$1(publicKey$1())],
    priceRatioDenominator: [null, u128()],
    arbitrumCompressionPercent: [null, u128()],
    nativeTokenPriceUsd: [null, option(u128())],
    prices: [null, array$1(getPriceSerializer())],
    bump: [null, u8$1()]
  }).deserializeUsing((account) => deserializePriceFeed(account)).whereField(
    "discriminator",
    new Uint8Array([189, 103, 252, 23, 152, 35, 243, 156])
  );
}

// src/generated/kinobi/pricefeed/errors/index.ts
var errors_exports10 = {};
__export(errors_exports10, {
  InvalidSizeError: () => InvalidSizeError4,
  InvalidUpdaterError: () => InvalidUpdaterError2,
  NotFoundError: () => NotFoundError2,
  TooManyUpdatersError: () => TooManyUpdatersError2,
  getPricefeedErrorFromCode: () => getPricefeedErrorFromCode,
  getPricefeedErrorFromName: () => getPricefeedErrorFromName
});
var codeToErrorMap6 = /* @__PURE__ */ new Map();
var nameToErrorMap6 = /* @__PURE__ */ new Map();
var TooManyUpdatersError2 = class extends ProgramError {
  // 6000
  constructor(program, cause) {
    super("", program, cause);
    this.name = "TooManyUpdaters";
    this.code = 6e3;
  }
};
codeToErrorMap6.set(6e3, TooManyUpdatersError2);
nameToErrorMap6.set("TooManyUpdaters", TooManyUpdatersError2);
var InvalidUpdaterError2 = class extends ProgramError {
  // 6001
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidUpdater";
    this.code = 6001;
  }
};
codeToErrorMap6.set(6001, InvalidUpdaterError2);
nameToErrorMap6.set("InvalidUpdater", InvalidUpdaterError2);
var NotFoundError2 = class extends ProgramError {
  // 6002
  constructor(program, cause) {
    super("", program, cause);
    this.name = "NotFound";
    this.code = 6002;
  }
};
codeToErrorMap6.set(6002, NotFoundError2);
nameToErrorMap6.set("NotFound", NotFoundError2);
var InvalidSizeError4 = class extends ProgramError {
  // 6003
  constructor(program, cause) {
    super("", program, cause);
    this.name = "InvalidSize";
    this.code = 6003;
  }
};
codeToErrorMap6.set(6003, InvalidSizeError4);
nameToErrorMap6.set("InvalidSize", InvalidSizeError4);
function getPricefeedErrorFromCode(code, program, cause) {
  const constructor = codeToErrorMap6.get(code);
  return constructor ? new constructor(program, cause) : null;
}
function getPricefeedErrorFromName(name, program, cause) {
  const constructor = nameToErrorMap6.get(name);
  return constructor ? new constructor(program, cause) : null;
}

// src/generated/kinobi/pricefeed/instructions/index.ts
var instructions_exports12 = {};
__export(instructions_exports12, {
  extendPriceFeed: () => extendPriceFeed,
  getExtendPriceFeedInstructionDataSerializer: () => getExtendPriceFeedInstructionDataSerializer,
  getFee: () => getFee,
  getGetFeeInstructionDataSerializer: () => getGetFeeInstructionDataSerializer,
  getInitPriceFeedInstructionDataSerializer: () => getInitPriceFeedInstructionDataSerializer,
  getSetPriceFeedInstructionDataSerializer: () => getSetPriceFeedInstructionDataSerializer,
  getSetPriceInstructionDataSerializer: () => getSetPriceInstructionDataSerializer,
  getSetSolPriceInstructionDataSerializer: () => getSetSolPriceInstructionDataSerializer,
  getTransferAdminInstructionDataSerializer: () => getTransferAdminInstructionDataSerializer4,
  initPriceFeed: () => initPriceFeed,
  setPrice: () => setPrice,
  setPriceFeed: () => setPriceFeed,
  setSolPrice: () => setSolPrice,
  transferAdmin: () => transferAdmin4
});
function getAccountMetasAndSigners6(accounts, optionalAccountStrategy, programId) {
  const keys = [];
  const signers = [];
  accounts.forEach((account) => {
    if (!account.value) {
      keys.push({ pubkey: programId, isSigner: false, isWritable: false });
      return;
    }
    if (isSigner(account.value)) {
      signers.push(account.value);
    }
    keys.push({
      pubkey: publicKey(account.value, false),
      isSigner: isSigner(account.value),
      isWritable: account.isWritable
    });
  });
  return [keys, signers];
}

// src/generated/kinobi/pricefeed/instructions/extendPriceFeed.ts
function getExtendPriceFeedInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [["discriminator", bytes$1({ size: 8 })]],
      { description: "ExtendPriceFeedInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([16, 95, 1, 5, 220, 54, 15, 80])
    })
  );
}
function extendPriceFeed(context, input) {
  const programId = context.programs.getPublicKey(
    "pricefeed",
    "8ahPGPjEbpgGaZx2NV1iG5Shj7TDwvsjkEDcGWjt94TP"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true,
      value: input.admin ?? null
    },
    priceFeed: {
      index: 1,
      isWritable: true,
      value: input.priceFeed ?? null
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners6(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getExtendPriceFeedInstructionDataSerializer().serialize({});
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getGetFeeInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["dstEid", u32$1()],
        ["calldataSize", u64$1()],
        ["totalGas", u128()]
      ],
      { description: "GetFeeInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([115, 195, 235, 161, 25, 219, 60, 29])
    })
  );
}
function getFee(context, input) {
  const programId = context.programs.getPublicKey(
    "pricefeed",
    "8ahPGPjEbpgGaZx2NV1iG5Shj7TDwvsjkEDcGWjt94TP"
  );
  const resolvedAccounts = {
    priceFeed: {
      index: 0,
      isWritable: false,
      value: input.priceFeed ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners6(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getGetFeeInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getInitPriceFeedInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["admin", publicKey$1()],
        ["updaters", array$1(publicKey$1())]
      ],
      { description: "InitPriceFeedInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([27, 209, 184, 5, 152, 116, 136, 16])
    })
  );
}
function initPriceFeed(context, input) {
  const programId = context.programs.getPublicKey(
    "pricefeed",
    "8ahPGPjEbpgGaZx2NV1iG5Shj7TDwvsjkEDcGWjt94TP"
  );
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true,
      value: input.payer ?? null
    },
    priceFeed: {
      index: 1,
      isWritable: true,
      value: input.priceFeed ?? null
    },
    systemProgram: {
      index: 2,
      isWritable: false,
      value: input.systemProgram ?? null
    }
  };
  const resolvedArgs = { ...input };
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111"
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners6(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getInitPriceFeedInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetPriceInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["dstEid", u32$1()],
        ["params", option(getPriceParamsSerializer())]
      ],
      { description: "SetPriceInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([16, 19, 182, 8, 149, 83, 72, 181])
    })
  );
}
function setPrice(context, input) {
  const programId = context.programs.getPublicKey(
    "pricefeed",
    "8ahPGPjEbpgGaZx2NV1iG5Shj7TDwvsjkEDcGWjt94TP"
  );
  const resolvedAccounts = {
    updater: {
      index: 0,
      isWritable: false,
      value: input.updater ?? null
    },
    priceFeed: {
      index: 1,
      isWritable: true,
      value: input.priceFeed ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners6(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetPriceInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetPriceFeedInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["updaters", array$1(publicKey$1())],
        ["priceRatioDenominator", u128()],
        ["arbitrumCompressionPercent", u128()]
      ],
      { description: "SetPriceFeedInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([13, 15, 231, 129, 61, 7, 28, 122])
    })
  );
}
function setPriceFeed(context, input) {
  const programId = context.programs.getPublicKey(
    "pricefeed",
    "8ahPGPjEbpgGaZx2NV1iG5Shj7TDwvsjkEDcGWjt94TP"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true,
      value: input.admin ?? null
    },
    priceFeed: {
      index: 1,
      isWritable: true,
      value: input.priceFeed ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners6(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetPriceFeedInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getSetSolPriceInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["nativeTokenPriceUsd", option(u128())]
      ],
      { description: "SetSolPriceInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([151, 237, 70, 191, 232, 81, 138, 214])
    })
  );
}
function setSolPrice(context, input) {
  const programId = context.programs.getPublicKey(
    "pricefeed",
    "8ahPGPjEbpgGaZx2NV1iG5Shj7TDwvsjkEDcGWjt94TP"
  );
  const resolvedAccounts = {
    updater: {
      index: 0,
      isWritable: false,
      value: input.updater ?? null
    },
    priceFeed: {
      index: 1,
      isWritable: true,
      value: input.priceFeed ?? null
    }
  };
  const resolvedArgs = { ...input };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners6(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getSetSolPriceInstructionDataSerializer().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}
function getTransferAdminInstructionDataSerializer4() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["admin", publicKey$1()]
      ],
      { description: "TransferAdminInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([42, 242, 66, 106, 228, 10, 111, 156])
    })
  );
}
function transferAdmin4(context, accounts, args) {
  const programId = context.programs.getPublicKey(
    "pricefeed",
    "8ahPGPjEbpgGaZx2NV1iG5Shj7TDwvsjkEDcGWjt94TP"
  );
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false,
      value: accounts.admin ?? null
    },
    priceFeed: {
      index: 1,
      isWritable: true,
      value: accounts.priceFeed ?? null
    }
  };
  const resolvedArgs = { ...args };
  const orderedAccounts = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);
  const [keys, signers] = getAccountMetasAndSigners6(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getTransferAdminInstructionDataSerializer4().serialize(
    resolvedArgs
  );
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}

// src/generated/kinobi/pricefeed/programs/pricefeed.ts
var PRICEFEED_PROGRAM_ID = "8ahPGPjEbpgGaZx2NV1iG5Shj7TDwvsjkEDcGWjt94TP";

// src/pricefeed.ts
var PriceFeed3 = class {
  constructor(programId = PRICEFEED_PROGRAM_ID, rpc) {
    this.programId = programId;
    this.pda = new PriceFeedPDA(this.programId);
    if (rpc === void 0) {
      rpc = createNullRpc();
      rpc.getCluster = () => "custom";
    }
    this.programRepo = createDefaultProgramRepository({ rpc }, [
      {
        name: "pricefeed",
        publicKey: programId,
        getErrorFromCode(code, cause) {
          return getPricefeedErrorFromCode(code, this, cause);
        },
        getErrorFromName(name, cause) {
          return getPricefeedErrorFromName(name, this, cause);
        },
        isOnCluster() {
          return true;
        }
      }
    ]);
  }
  getProgram(clusterFilter = "custom") {
    return this.programRepo.get("pricefeed", clusterFilter);
  }
  initPriceFeed(payer, param) {
    return initPriceFeed(
      { programs: this.programRepo, payer },
      {
        payer,
        priceFeed: this.pda.priceFeed(),
        ...param
      }
    ).items[0];
  }
  setPriceFeed(admin, param) {
    return setPriceFeed(
      { programs: this.programRepo },
      {
        admin,
        priceFeed: this.pda.priceFeed(),
        ...param
      }
    ).items[0];
  }
  setPrice(updater, param) {
    const EID_MODULUS = 3e4;
    const dstEid = param.dstEid % EID_MODULUS;
    const isParamsNull = !("priceRatio" in param);
    return setPrice(
      { programs: this.programRepo },
      {
        updater,
        priceFeed: this.pda.priceFeed(),
        dstEid,
        params: isParamsNull ? null : {
          priceRatio: param.priceRatio,
          gasPriceInUnit: param.gasPriceInUnit,
          gasPerByte: param.gasPerByte,
          modelType: param.modelType
        }
      }
    ).items[0];
  }
  setSolPrice(updater, nativeTokenPriceUsd) {
    return setSolPrice(
      { programs: this.programRepo },
      {
        updater,
        priceFeed: this.pda.priceFeed(),
        nativeTokenPriceUsd
      }
    ).items[0];
  }
  transferAdmin(admin, newAdmin) {
    return transferAdmin4(
      { programs: this.programRepo },
      {
        admin,
        priceFeed: this.pda.priceFeed()
      },
      {
        admin: newAdmin
      }
    ).items[0];
  }
  getFee(param) {
    return getFee(
      { programs: this.programRepo },
      {
        priceFeed: this.pda.priceFeed(),
        ...param
      }
    ).items[0];
  }
  /**
   * Retrieves the price feed account.
   *
   * @param {RpcInterface} rpc - The Solana connection object.
   * @param {Commitment} [commitment] - Optional commitment or configuration.
   *
   * @returns {Promise<accounts.PriceFeed | null>} The price feed account or null if not found.
   */
  async getPriceFeed(rpc, commitment = "confirmed") {
    const [priceFeed] = this.pda.priceFeed();
    return safeFetchPriceFeed({ rpc }, priceFeed, { commitment });
  }
};
var idlTypes2 = [
  "MessageLibQuoteParams",
  "MessageLibSendParams",
  "MessagingFee",
  "MessagingReceipt",
  "PacketForQuote",
  "Packet",
  "InitConfigParams",
  "MessageLibSetConfigParams",
  "MessageLibSnapshotConfigParams",
  "MessageLibResetConfigParams"
];
var messageLibs2 = ["uln", "simple_messagelib"];
var FAUCET_URL2 = {
  [Environment.MAINNET]: "",
  [Environment.TESTNET]: "",
  [Environment.DEVNET]: "",
  [Environment.LOCAL]: "http://127.0.0.1:9900"
};
function getProgramKeypair2(network, program) {
  network = network === "default" ? "solana-sandbox-local" : network;
  const deployment = __require(`@layerzerolabs/lz-solana-sdk-v2/deployments/${network}/${program}.json`);
  return publicKey(deployment.address);
}
function getEndpointProgramId2(network) {
  return getProgramKeypair2(network, "endpoint");
}
function getSimpleMessageLibProgramId2(network) {
  return getProgramKeypair2(network, "simple_messagelib");
}
function getULNProgramId2(network) {
  return getProgramKeypair2(network, "uln");
}
function getDVNProgramId2(network) {
  return getProgramKeypair2(network, "dvn");
}
function getBlockedMessageLibProgramId2(network) {
  return getProgramKeypair2(network, "blocked_messagelib");
}
function getExecutorProgramId2(network) {
  return getProgramKeypair2(network, "executor");
}
function getPricefeedProgramId2(network) {
  return getProgramKeypair2(network, "pricefeed");
}
function getNextNonceInstructionDataSerializer() {
  return mapSerializer(
    struct(
      [
        ["discriminator", bytes$1({ size: 8 })],
        ["srcEid", u32$1()],
        ["sender", bytes$1({ size: 32 })],
        ["receiver", publicKey$1()]
      ],
      { description: "NextNonceInstructionData" }
    ),
    (value) => ({
      ...value,
      discriminator: instructionDiscriminator2("next_nonce")
    })
  );
}
async function nextNonce(rpc, payer, packet, commitment = "confirmed") {
  const { sender: sender_, srcEid, receiver: receiver_ } = packet;
  const receiver = new web3108.PublicKey(addressToBytes32(receiver_));
  const connection = toWeb3Connection(rpc);
  const receiverInfo = await connection.getAccountInfo(receiver);
  if (receiverInfo === null) {
    throw new Error(`Receiver account not found: ${receiver.toBase58()}`);
  }
  const params = {
    srcEid,
    sender: arrayify(sender_),
    receiver: fromWeb3JsPublicKey(receiver)
  };
  const receiverProgram = receiverInfo.owner;
  const [nonceAccount] = EDDSA.findPda(fromWeb3JsPublicKey(receiverProgram), [
    Buffer.from("Nonce", "utf8"),
    publicKeyBytes(params.receiver),
    u32$1({ endian: Endian.Big }).serialize(srcEid),
    params.sender
  ]);
  const accounts = [
    {
      pubkey: receiver,
      isSigner: false,
      isWritable: false
    },
    {
      pubkey: toWeb3JsPublicKey(nonceAccount),
      isSigner: false,
      isWritable: true
    }
  ];
  const nextNonceIx = {
    programId: receiverProgram,
    keys: accounts,
    data: Buffer.from(getNextNonceInstructionDataSerializer().serialize(params))
  };
  return simulateWeb3JsTransaction(connection, [nextNonceIx], receiverProgram, payer, u64$1(), commitment);
}

// src/generated/kinobi/blocked_messagelib/index.ts
var blocked_messagelib_exports2 = {};
__export(blocked_messagelib_exports2, {
  BLOCKED_MESSAGELIB_PROGRAM_ID: () => BLOCKED_MESSAGELIB_PROGRAM_ID,
  createBlockedMessagelibProgram: () => createBlockedMessagelibProgram,
  expectPda: () => expectPda2,
  expectPublicKey: () => expectPublicKey2,
  expectSome: () => expectSome2,
  getAccountMetasAndSigners: () => getAccountMetasAndSigners7,
  getBlockedMessagelibErrorFromCode: () => getBlockedMessagelibErrorFromCode,
  getBlockedMessagelibErrorFromName: () => getBlockedMessagelibErrorFromName,
  getBlockedMessagelibProgram: () => getBlockedMessagelibProgram,
  getBlockedMessagelibProgramId: () => getBlockedMessagelibProgramId,
  getVersionInstructionDataSerializer: () => getVersionInstructionDataSerializer4,
  version: () => version4
});

// src/generated/kinobi/blocked_messagelib/errors/blockedMessagelib.ts
var codeToErrorMap7 = /* @__PURE__ */ new Map();
var nameToErrorMap7 = /* @__PURE__ */ new Map();
function getBlockedMessagelibErrorFromCode(code, program, cause) {
  const constructor = codeToErrorMap7.get(code);
  return constructor ? new constructor(program, cause) : null;
}
function getBlockedMessagelibErrorFromName(name, program, cause) {
  const constructor = nameToErrorMap7.get(name);
  return constructor ? new constructor(program, cause) : null;
}
function expectSome2(value) {
  if (value == null) {
    throw new Error("Expected a value but received null or undefined.");
  }
  return value;
}
function expectPublicKey2(value) {
  if (!value) {
    throw new Error("Expected a PublicKey.");
  }
  return publicKey(value, false);
}
function expectPda2(value) {
  if (!value || !Array.isArray(value) || !isPda(value)) {
    throw new Error("Expected a PDA.");
  }
  return value;
}
function getAccountMetasAndSigners7(accounts, optionalAccountStrategy, programId) {
  const keys = [];
  const signers = [];
  accounts.forEach((account) => {
    if (!account.value) {
      if (optionalAccountStrategy === "omitted") return;
      keys.push({ pubkey: programId, isSigner: false, isWritable: false });
      return;
    }
    if (isSigner(account.value)) {
      signers.push(account.value);
    }
    keys.push({
      pubkey: publicKey(account.value, false),
      isSigner: isSigner(account.value),
      isWritable: account.isWritable
    });
  });
  return [keys, signers];
}

// src/generated/kinobi/blocked_messagelib/instructions/version.ts
function getVersionInstructionDataSerializer4() {
  return mapSerializer(
    struct([["discriminator", bytes$1({ size: 8 })]], {
      description: "VersionInstructionData"
    }),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([118, 65, 195, 198, 129, 216, 252, 192])
    })
  );
}
function version4(context) {
  const programId = context.programs.getPublicKey(
    "blockedMessagelib",
    "2XrYqmhBMPJgDsb4SVbjV1PnJBprurd5bzRCkHwiFCJB"
  );
  const resolvedAccounts = {};
  const orderedAccounts = Object.values(
    resolvedAccounts
  );
  const [keys, signers] = getAccountMetasAndSigners7(
    orderedAccounts,
    "programId",
    programId
  );
  const data = getVersionInstructionDataSerializer4().serialize({});
  const bytesCreatedOnChain = 0;
  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain }
  ]);
}

// src/generated/kinobi/blocked_messagelib/programs/blockedMessagelib.ts
var BLOCKED_MESSAGELIB_PROGRAM_ID = "2XrYqmhBMPJgDsb4SVbjV1PnJBprurd5bzRCkHwiFCJB";
function createBlockedMessagelibProgram() {
  return {
    name: "blockedMessagelib",
    publicKey: BLOCKED_MESSAGELIB_PROGRAM_ID,
    getErrorFromCode(code, cause) {
      return getBlockedMessagelibErrorFromCode(code, this, cause);
    },
    getErrorFromName(name, cause) {
      return getBlockedMessagelibErrorFromName(name, this, cause);
    },
    isOnCluster() {
      return true;
    }
  };
}
function getBlockedMessagelibProgram(context, clusterFilter) {
  return context.programs.get("blockedMessagelib", clusterFilter);
}
function getBlockedMessagelibProgramId(context, clusterFilter) {
  return context.programs.getPublicKey(
    "blockedMessagelib",
    BLOCKED_MESSAGELIB_PROGRAM_ID,
    clusterFilter
  );
}
var SendHelper2 = class {
  /**
   * Creates an instance of the SendHelper class.
   *
   * @param {PublicKey} [endpointProgram=EndpointProgram.PROGRAM_ID] - The endpoint program public key.
   * @param {PublicKey} [ulnProgram=UlnProgram.PROGRAM_ID] - The ULN program public key.
   * @param {PublicKey} [simpleMsgLibProgram=SimpleMessageLibProgram.PROGRAM_ID] - The simple message library program public key.
   */
  constructor(endpointProgram = endpoint_exports2.ENDPOINT_PROGRAM_ID, ulnProgram = uln_exports2.ULN_PROGRAM_ID, simpleMsgLibProgram = simple_message_lib_exports2.SIMPLE_MESSAGELIB_PROGRAM_ID) {
    this.endpointProgram = endpointProgram;
    this.ulnProgram = ulnProgram;
    this.simpleMsgLibProgram = simpleMsgLibProgram;
    this.accounts = /* @__PURE__ */ new Map();
    this.endpoint = new endpoint_exports2.Endpoint(endpointProgram);
    this.uln = new uln_exports2.Uln(ulnProgram);
    this.simpleMsgLib = new simple_message_lib_exports2.SimpleMessageLib(simpleMsgLibProgram);
  }
  /**
   * Fetches multiple account information.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey[]} keys - The public keys of the accounts.
   * @param {Commitment | GetAccountInfoConfig} [commitment] - The commitment level or account info configuration.
   * @returns {Promise<(AccountInfo<Buffer> | null)[]>} A promise that resolves to an array of account information.
   */
  async getMultipleAccountsInfo(rpc, keys, commitment = "confirmed") {
    const missingKeys = keys.filter((key) => !this.accounts.has(key));
    if (missingKeys.length > 0) {
      const infos = await rpc.getAccounts(missingKeys, { commitment });
      infos.forEach((info, i) => {
        this.accounts.set(missingKeys[i], info);
      });
    }
    return keys.map((key) => this.accounts.get(key));
  }
  async getQuoteAccounts(rpc, params, commitment = "confirmed") {
    const { payer: _payer, sender: _sender, dstEid, receiver } = params;
    const payer = _payer instanceof web3108.PublicKey ? fromWeb3JsPublicKey(_payer) : _payer;
    const sender = _sender instanceof web3108.PublicKey ? fromWeb3JsPublicKey(_sender) : _sender;
    const [sendLibConfig] = this.endpoint.pda.sendLibraryConfig(sender, dstEid);
    const [defaultSendLibConfig] = this.endpoint.pda.defaultSendLibraryConfig(dstEid);
    const [simpleMsgLib] = this.simpleMsgLib.pda.messageLib();
    const [uln] = this.uln.pda.messageLib();
    const [ulnDefaultSendConfig] = this.uln.pda.defaultSendConfig(dstEid);
    const [ulnSendConfig] = this.uln.pda.sendConfig(dstEid, sender);
    const [
      sendLibConfigBuf,
      defaultSendLibConfigBuf,
      _simpleMsgLibBuf,
      ulnBuf,
      ulnDefaultSendConfigBuf,
      ulnSendConfigBuf
    ] = await this.getMultipleAccountsInfo(
      rpc,
      [sendLibConfig, defaultSendLibConfig, simpleMsgLib, uln, ulnDefaultSendConfig, ulnSendConfig],
      commitment
    );
    invariant6(defaultSendLibConfigBuf.exists && sendLibConfigBuf.exists, "endpoint send library not initialized");
    const sendLibConfigInfo = endpoint_exports2.accounts.deserializeSendLibraryConfig(sendLibConfigBuf);
    const defaultSendLibConfigInfo = endpoint_exports2.accounts.deserializeSendLibraryConfig(defaultSendLibConfigBuf);
    const msgLib = sendLibConfigInfo.messageLib.toString() === DefaultMessageLib2.toString() ? defaultSendLibConfigInfo.messageLib : sendLibConfigInfo.messageLib;
    let msgLibProgram = this.ulnProgram;
    if (msgLib === this.simpleMsgLib.pda.messageLib()[0]) {
      msgLibProgram = this.simpleMsgLibProgram;
      return this.getEndpointAccounts(msgLibProgram, msgLib, sender, dstEid, receiver, 0 /* Quote */).concat(
        this.getSimpleMsgLibAccounts(payer, 0 /* Quote */)
      );
    }
    if (msgLib !== this.uln.pda.messageLib()[0]) {
      throw new Error("Invalid message library");
    }
    return this.getEndpointAccounts(msgLibProgram, msgLib, sender, dstEid, receiver, 0 /* Quote */).concat(
      await this.getUlnAccounts(
        rpc,
        payer,
        {
          accountId: uln,
          accountInfo: ulnBuf
        },
        {
          accountId: ulnDefaultSendConfig,
          accountInfo: ulnDefaultSendConfigBuf
        },
        {
          accountId: ulnSendConfig,
          accountInfo: ulnSendConfigBuf
        },
        0 /* Quote */,
        commitment
      )
    );
  }
  /**
   * @param connection
   * @param payer
   * @param sender the PDA of the oApp
   * @param dstEid the destination endpoint id
   * @param receiver the remote peer's address
   * @param commitmentOrConfig
   * 1 or 3(1+2) RPC calls
   * */
  async getSendAccounts(rpc, params, commitment = "confirmed") {
    const { payer: _payer, sender: _sender, dstEid, receiver } = params;
    const payer = _payer instanceof web3108.PublicKey ? fromWeb3JsPublicKey(_payer) : _payer;
    const sender = _sender instanceof web3108.PublicKey ? fromWeb3JsPublicKey(_sender) : _sender;
    const [sendLibConfig] = this.endpoint.pda.sendLibraryConfig(sender, dstEid);
    const [defaultSendLibConfig] = this.endpoint.pda.defaultSendLibraryConfig(dstEid);
    const [simpleMsgLib] = this.simpleMsgLib.pda.messageLib();
    const [uln] = this.uln.pda.messageLib();
    const [ulnDefaultSendConfig] = this.uln.pda.defaultSendConfig(dstEid);
    const [ulnSendConfig] = this.uln.pda.sendConfig(dstEid, sender);
    const [
      sendLibConfigBuf,
      defaultSendLibConfigBuf,
      _simpleMsgLibBuf,
      ulnBuf,
      ulnDefaultSendConfigBuf,
      ulnSendConfigBuf
    ] = await this.getMultipleAccountsInfo(
      rpc,
      [sendLibConfig, defaultSendLibConfig, simpleMsgLib, uln, ulnDefaultSendConfig, ulnSendConfig],
      commitment
    );
    invariant6(defaultSendLibConfigBuf.exists && sendLibConfigBuf.exists, "endpoint send library not initialized");
    const sendLibConfigInfo = endpoint_exports2.accounts.deserializeSendLibraryConfig(sendLibConfigBuf);
    const defaultSendLibConfigInfo = endpoint_exports2.accounts.deserializeSendLibraryConfig(defaultSendLibConfigBuf);
    const msgLib = sendLibConfigInfo.messageLib.toString() === DefaultMessageLib2.toString() ? defaultSendLibConfigInfo.messageLib : sendLibConfigInfo.messageLib;
    let msgLibProgram = this.ulnProgram;
    if (msgLib === this.simpleMsgLib.pda.messageLib()[0]) {
      msgLibProgram = this.simpleMsgLibProgram;
      return this.getEndpointAccounts(msgLibProgram, msgLib, sender, dstEid, receiver, 1 /* Send */).concat(
        this.getSimpleMsgLibAccounts(payer, 1 /* Send */)
      );
    }
    if (msgLib !== this.uln.pda.messageLib()[0]) {
      throw new Error("Invalid message library");
    }
    return this.getEndpointAccounts(msgLibProgram, msgLib, sender, dstEid, receiver, 1 /* Send */).concat(
      await this.getUlnAccounts(
        rpc,
        payer,
        {
          accountId: uln,
          accountInfo: ulnBuf
        },
        {
          accountId: ulnDefaultSendConfig,
          accountInfo: ulnDefaultSendConfigBuf
        },
        {
          accountId: ulnSendConfig,
          accountInfo: ulnSendConfigBuf
        },
        1 /* Send */,
        commitment
      )
    );
  }
  /**
   * Gets the account metadata for the endpoint.
   *
   * @param {PublicKey} msgLibProgram - The message library program public key.
   * @param {PublicKey} msgLib - The message library public key.
   * @param {PublicKey} sender - The sender public key.
   * @param {number} dstEid - The destination endpoint ID.
   * @param {string} receiver - The receiver address.
   * @param {QuoteOrSend} quoteOrSend - The quote or send action.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getEndpointAccounts(msgLibProgram, msgLib, sender, dstEid, _receiver, quoteOrSend) {
    const receiver = typeof _receiver === "string" ? addressToBytes32(_receiver) : _receiver;
    const [sendLibraryConfig] = this.endpoint.pda.sendLibraryConfig(sender, dstEid);
    const [defaultSendLibraryConfig] = this.endpoint.pda.defaultSendLibraryConfig(dstEid);
    const [sendLibraryInfo] = this.endpoint.pda.messageLibraryInfo(msgLib);
    const [nonce] = this.endpoint.pda.nonce(sender, dstEid, receiver);
    let accounts;
    const endpoint = new endpoint_exports2.Endpoint(this.endpointProgram);
    if (quoteOrSend === 0 /* Quote */) {
      accounts = endpoint_exports2.instructions.quote(
        { programs: endpoint.programRepo },
        {
          sendLibraryProgram: msgLibProgram,
          sendLibraryConfig,
          defaultSendLibraryConfig,
          sendLibraryInfo,
          endpoint: this.endpoint.pda.setting()[0],
          nonce,
          // param
          sender: defaultPublicKey(),
          dstEid: 0,
          receiver: new Uint8Array(32),
          message: new Uint8Array(32),
          options: new Uint8Array(32),
          payInLzToken: false
        }
      ).getInstructions()[0].keys;
    } else {
      accounts = endpoint_exports2.instructions.send(
        { programs: endpoint.programRepo },
        {
          sender: createNoopSigner(sender),
          //signer
          sendLibraryProgram: msgLibProgram,
          sendLibraryConfig,
          defaultSendLibraryConfig,
          sendLibraryInfo,
          endpoint: this.endpoint.pda.setting()[0],
          nonce,
          program: this.endpointProgram,
          eventAuthority: this.endpoint.eventAuthority,
          // param
          dstEid: 0,
          receiver: new Uint8Array(32),
          message: new Uint8Array(32),
          options: new Uint8Array(32),
          nativeFee: 0,
          lzTokenFee: 0
        }
      ).getInstructions()[0].keys;
    }
    accounts.forEach((item) => {
      item.isSigner = false;
    });
    return [
      {
        pubkey: this.endpoint.programId,
        isSigner: false,
        isWritable: false
      },
      ...accounts
    ];
  }
  /**
   * Gets the account metadata for the simple message library.
   *
   * @param {PublicKey} payer - The payer public key.
   * @param {QuoteOrSend} quoteOrSend - The quote or send action.
   * @returns {AccountMeta[]} An array of account metadata.
   */
  getSimpleMsgLibAccounts(payer, quoteOrSend) {
    const [msgLib] = this.simpleMsgLib.pda.messageLib();
    let accounts;
    const simpleMsgLib = new simple_message_lib_exports2.SimpleMessageLib(this.simpleMsgLibProgram);
    if (quoteOrSend === 0 /* Quote */) {
      accounts = simple_message_lib_exports2.instructions.quote(
        { programs: simpleMsgLib.programRepo },
        {
          endpoint: createNoopSigner(defaultPublicKey()),
          messageLib: msgLib,
          // param
          packet: {
            nonce: 0,
            srcEid: 0,
            sender: defaultPublicKey(),
            dstEid: 0,
            receiver: new Uint8Array(32),
            guid: new Uint8Array(32),
            message: new Uint8Array(4)
          },
          options: new Uint8Array(4),
          payInLzToken: false
        }
      ).getInstructions()[0].keys;
    } else {
      accounts = simple_message_lib_exports2.instructions.send(
        { programs: simpleMsgLib.programRepo, payer: createNoopSigner(payer) },
        {
          endpoint: createNoopSigner(defaultPublicKey()),
          // useless
          messageLib: msgLib,
          payer: createNoopSigner(payer),
          // param
          packet: {
            nonce: 0,
            srcEid: 0,
            sender: defaultPublicKey(),
            dstEid: 0,
            receiver: new Uint8Array(32),
            guid: new Uint8Array(32),
            message: new Uint8Array(4)
          },
          options: new Uint8Array(4),
          nativeFee: 0
        }
      ).getInstructions()[0].keys;
    }
    accounts.forEach((key) => {
      if (payer !== key.pubkey) {
        key.isSigner = false;
      }
    });
    return accounts.slice(1);
  }
  /**
   * Gets the account metadata for the ULN.
   *
   * @param {Connection} connection - The connection to the Solana cluster.
   * @param {PublicKey} payer - The payer public key.
   * @param {KeyedAccountInfo} ulnInfo - The ULN account information.
   * @param {KeyedAccountInfo} ulnDefaultSendConfigInfo - The ULN default send configuration account information.
   * @param {KeyedAccountInfo} ulnSendConfigInfo - The ULN send configuration account information.
   * @param {QuoteOrSend} quoteOrSend - The quote or send action.
   * @param {Commitment | GetAccountInfoConfig} [commitment] - The commitment level or account info configuration.
   * @returns {Promise<AccountMeta[]>} A promise that resolves to an array of account metadata.
   */
  async getUlnAccounts(rpc, payer, ulnInfo, ulnDefaultSendConfigInfo, ulnSendConfigInfo, quoteOrSend, commitment = "confirmed") {
    invariant6(
      ulnInfo.accountInfo.exists && ulnDefaultSendConfigInfo.accountInfo.exists,
      "uln send library not initialized"
    );
    const ulnState = uln_exports2.accounts.deserializeUlnSettings(ulnInfo.accountInfo);
    const defaultSendConfigState = uln_exports2.accounts.deserializeSendConfig(ulnDefaultSendConfigInfo.accountInfo);
    const sendConfigState = ulnSendConfigInfo.accountInfo.exists ? uln_exports2.accounts.deserializeSendConfig(ulnSendConfigInfo.accountInfo) : null;
    let {
      executor,
      uln: { requiredDvns, optionalDvns }
    } = defaultSendConfigState;
    if (sendConfigState?.executor && sendConfigState.executor.executor !== defaultPublicKey()) {
      ({ executor } = sendConfigState);
    }
    if (sendConfigState && sendConfigState.uln.requiredDvns.length > 0) {
      requiredDvns = sendConfigState.uln.requiredDvns.filter((p) => {
        return p !== defaultPublicKey();
      });
    }
    if (sendConfigState && sendConfigState.uln.optionalDvns.length > 0) {
      optionalDvns = sendConfigState.uln.optionalDvns.filter((p) => {
        return p !== defaultPublicKey();
      });
    }
    const dvnsKey = requiredDvns.concat(optionalDvns);
    const [executorBuf, ...dvnBuf] = await this.getMultipleAccountsInfo(
      rpc,
      [executor.executor, ...dvnsKey],
      commitment
    );
    invariant6(executorBuf.exists, `executor:${executor.executor} not initialized`);
    let executorAccounts, dvnAccounts;
    {
      const executor2 = {
        config: accounts_exports10.deserializeExecutorConfig(executorBuf),
        owner: executorBuf.owner
      };
      const dvns = dvnBuf.map((dvn, i) => {
        invariant6(dvn.exists, `dvn:${dvnsKey[i]} not initialized`);
        return {
          config: accounts_exports9.deserializeDvnConfig(dvn),
          owner: dvn.owner
        };
      });
      const priceFeeds = new Array();
      [executor2.config, ...dvns.map((dvn) => dvn.config)].forEach((config2) => {
        priceFeeds.push(config2.priceFeed);
      });
      const priceFeedInfos = await this.getMultipleAccountsInfo(rpc, priceFeeds, commitment);
      priceFeedInfos.forEach((info, i) => {
        invariant6(info.exists, `priceFeed:${priceFeeds[i]} not initialized`);
      });
      const executorPriceFeed = await this.getMultipleAccountsInfo(rpc, [executor2.config.priceFeed], commitment);
      invariant6(executorPriceFeed[0].exists, `executor priceFeed:${executor2.config.priceFeed} not initialized`);
      executorAccounts = new Executor2(executor2.owner).getQuoteIXAccountMetaForCPI(
        executor2.config.priceFeed,
        executorPriceFeed[0].owner,
        quoteOrSend === 1 /* Send */
      );
      dvnAccounts = (await Promise.all(
        dvns.map(async (p, i) => {
          const dvnPriceFeed = await this.getMultipleAccountsInfo(rpc, [p.config.priceFeed], commitment);
          invariant6(dvnPriceFeed[0].exists, `dvn priceFeed:${p.config.priceFeed} not initialized`);
          return new DVN2(p.owner).getQuoteIXAccountMetaForCPI(
            p.config.priceFeed,
            dvnPriceFeed[0].owner,
            quoteOrSend === 1 /* Send */
          );
        })
      )).flat();
    }
    const treasury = isSome(ulnState.treasury) ? ulnState.treasury.value.nativeReceiver : void 0;
    let accounts;
    const uln = new uln_exports2.Uln(this.ulnProgram);
    if (quoteOrSend === 0 /* Quote */) {
      const txBuilder = uln_exports2.instructions.quote(
        { programs: uln.programRepo },
        {
          endpoint: createNoopSigner(defaultPublicKey()),
          uln: this.uln.pda.setting()[0],
          sendConfig: ulnSendConfigInfo.accountId,
          defaultSendConfig: ulnDefaultSendConfigInfo.accountId,
          // param
          packet: {
            nonce: 0,
            srcEid: 0,
            sender: defaultPublicKey(),
            dstEid: 0,
            receiver: new Uint8Array(32),
            guid: new Uint8Array(32),
            message: new Uint8Array(4)
          },
          options: new Uint8Array(4),
          payInLzToken: false
        }
      ).addRemainingAccounts(executorAccounts.concat(dvnAccounts));
      accounts = txBuilder.getInstructions()[0].keys;
    } else {
      const txBuilder = uln_exports2.instructions.send(
        { programs: uln.programRepo, payer: createNoopSigner(payer) },
        {
          endpoint: createNoopSigner(defaultPublicKey()),
          uln: this.uln.pda.setting()[0],
          sendConfig: ulnSendConfigInfo.accountId,
          defaultSendConfig: ulnDefaultSendConfigInfo.accountId,
          payer: createNoopSigner(payer),
          treasury,
          eventAuthority: this.uln.eventAuthority,
          program: this.uln.programId,
          packet: {
            nonce: 0,
            srcEid: 0,
            sender: defaultPublicKey(),
            dstEid: 0,
            receiver: new Uint8Array(32),
            guid: new Uint8Array(32),
            message: new Uint8Array(4)
          },
          options: new Uint8Array(4),
          nativeFee: 0
        }
      ).addRemainingAccounts(executorAccounts.concat(dvnAccounts));
      accounts = txBuilder.getInstructions()[0].keys;
      if (treasury) {
        const treasuryIndex = accounts.findIndex((k) => k.pubkey == treasury);
        accounts[treasuryIndex].isWritable = true;
      }
    }
    accounts.forEach((key) => {
      if (payer !== key.pubkey) {
        key.isSigner = false;
      }
    });
    return accounts.slice(1);
  }
};

// src/index.ts
var SetConfigType4 = /* @__PURE__ */ ((SetConfigType5) => {
  SetConfigType5[SetConfigType5["EXECUTOR"] = 1] = "EXECUTOR";
  SetConfigType5[SetConfigType5["SEND_ULN"] = 2] = "SEND_ULN";
  SetConfigType5[SetConfigType5["RECEIVE_ULN"] = 3] = "RECEIVE_ULN";
  return SetConfigType5;
})(SetConfigType4 || {});

// src/solita/index.ts
var SetConfigType2 = /* @__PURE__ */ ((SetConfigType5) => {
  SetConfigType5[SetConfigType5["EXECUTOR"] = 1] = "EXECUTOR";
  SetConfigType5[SetConfigType5["SEND_ULN"] = 2] = "SEND_ULN";
  SetConfigType5[SetConfigType5["RECEIVE_ULN"] = 3] = "RECEIVE_ULN";
  return SetConfigType5;
})(SetConfigType2 || {});

export { AddressType, BaseOApp, blocked_messagelib_exports as BlockedMessageLibProgram, COMPOSED_MESSAGE_HASH_SEED, CONFIRMATIONS_SEED, COUNT_SEED, DVNDeriver, dvn_exports2 as DVNProgram, DVN_CONFIG_SEED, dvn_exports as DvnProgram, ENDPOINT_SEED, ENFORCED_OPTIONS_SEED, EVENT_SEED, EXECUTOR_CONFIG_SEED, EndpointPDADeriver, endpoint_exports as EndpointProgram, EventPDADeriver, ExecutorOptionType, ExecutorPDADeriver, executor_exports as ExecutorProgram, FAUCET_URL, LZ_COMPOSE_TYPES_SEED, LZ_RECEIVE_TYPES_SEED, LzComposeParamsBeet, LzReceiveAccountBeet, LzReceiveParamsBeet, MESSAGE_LIB_SEED, MINT_SEED, MSG_TYPE_OFFSET, MaxExecutorOptionTypeLength, MessageLibPDADeriver, MessageType, NONCE_SEED, OAPP_SEED, OAppBasePDADeriver, OPTIONS_SEED, PAYLOAD_HASH_SEED, PEER_SEED, PENDING_NONCE_SEED, PRICE_FEED_SEED, PriceFeedPDADeriver, pricefeed_exports as PriceFeedProgram, RECEIVE_CONFIG_SEED, RECEIVE_LIBRARY_CONFIG_SEED, REMOTE_SEED, SEND_CONFIG_SEED, SEND_LIBRARY_CONFIG_SEED, SendHelper, SetConfigType2 as SetConfigType, simple_message_lib_exports as SimpleMessageLibProgram, ULN_CONFIG_SEED, ULN_SEED, src_exports as UMI, UlnPDADeriver, uln_exports as UlnProgram, WORKER_SEED, buildMessageV0, buildVersionedTransaction, closeLookupTable, createNonceAccountTX, deactivateLookupTable, deriveLzComposeTypesAccountsPDA, deriveLzReceiveTypesAccountsPDA, extractComposeDeliveredEventByTxHash, extractComposeSentEventByTxHash, extractEventFromTransactionSignature, extractReceivedPacketEventByTxHash, extractSentPacketEventByTxHash, extractVerifiedPacketEventByTxHash, extractWorkerFeePaidEventByTxHash, generateAddressLookupTable, getBlockedMessageLibProgramId, getDVNProgramId, getEndpointProgramId, getExecutorProgramId, getLzComposeAccountMeta, getLzReceiveAccounts, getPricefeedProgramId, getProgramKeypair, getSimpleMessageLibProgramId, getULNProgramId, idlTypes, instructionDiscriminator, isAccountInitialized, lzCompose, lzReceive, messageLibs, oappIDPDA, simulateTransaction, txWithAddressLookupTable, txWithNonce };
//# sourceMappingURL=index.mjs.map
//# sourceMappingURL=index.mjs.map