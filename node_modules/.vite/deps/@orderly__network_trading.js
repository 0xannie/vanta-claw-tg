import __buffer_polyfill from 'vite-plugin-node-polyfills/shims/buffer'
globalThis.Buffer = globalThis.Buffer || __buffer_polyfill
import __global_polyfill from 'vite-plugin-node-polyfills/shims/global'
globalThis.global = globalThis.global || __global_polyfill
import __process_polyfill from 'vite-plugin-node-polyfills/shims/process'
globalThis.process = globalThis.process || __process_polyfill

import {
  CloseAllPositionsWidget,
  DesktopOrderListWidget,
  LiquidationWidget,
  MobileLiquidationWidget,
  MobileOrderListWidget,
  MobilePositionHistoryWidget,
  MobilePositionsWidget,
  PositionHistoryWidget,
  PositionsWidget,
  SymbolLeverageDialogId,
  SymbolLeverageSheetId,
  TPSLAdvancedWidget,
  TPSLPositionTypeWidget,
  TabType,
  assets_exports2,
  useReversePositionEnabled
} from "./chunk-7YBV33XM.js";
import {
  DepositAndWithdrawWithDialogId,
  DepositAndWithdrawWithSheetId,
  DepositStatusWidget,
  TransferDialogId,
  TransferSheetId
} from "./chunk-4S5JRTC4.js";
import {
  HorizontalMarketsWidget,
  MarketsSheetWidget,
  SideMarketsWidget,
  SymbolInfoBarFullWidget,
  SymbolInfoBarWidget
} from "./chunk-XI3KAWIM.js";
import {
  _extends,
  _objectWithoutPropertiesLoose
} from "./chunk-2HWDSNVI.js";
import "./chunk-XKDBZXIP.js";
import {
  ScanQRCodeWidget
} from "./chunk-J6DTPFJE.js";
import {
  AuthGuard,
  ChainSelectorSheetId
} from "./chunk-24DGMYBJ.js";
import {
  useAppContext,
  useCanTrade,
  useDataTap,
  useOrderEntryFormErrorMsg
} from "./chunk-FQFTCGNL.js";
import {
  ERROR_MSG_CODES,
  TWType,
  account_exports,
  c,
  useAccount,
  useAccountInstance,
  useAppStore,
  useAssetsHistory,
  useBoolean,
  useChains,
  useCollateral,
  useComputedLTV,
  useConfig,
  useCurEpochEstimate,
  useEpochInfo,
  useEventEmitter,
  useFeeState,
  useFundingDetails,
  useFundingRate,
  useFundingRateBySymbol,
  useGetEstLiqPrice,
  useGetRwaSymbolCloseTimeInterval,
  useGetRwaSymbolInfo,
  useGetRwaSymbolOpenStatus,
  useHoldingStream,
  useIndexPricesStream,
  useLeverage,
  useLocalStorage,
  useMarginRatio,
  useMarketTradeStream,
  useMediaQuery,
  useMemoizedFn,
  useMutation,
  useOrderEntry,
  useOrderEntry2,
  useOrderStream,
  useOrderbookStream,
  useOrderlyContext,
  usePositionStream,
  useQuery,
  useReferralInfo,
  useRwaSymbolsInfoStore,
  useSymbolLeverage,
  useSymbolsInfo,
  useTickerStream,
  useTpslPriceChecker,
  useTrack,
  useWS,
  useWalletConnector,
  utils_exports2 as utils_exports
} from "./chunk-LKGTCLMW.js";
import "./chunk-IRK6YIG5.js";
import "./chunk-XOXN5XQJ.js";
import "./chunk-XPCMXA3P.js";
import "./chunk-DVJCRTEZ.js";
import "./chunk-E4THU5TJ.js";
import "./chunk-MV2QDQSH.js";
import "./chunk-6FN6GMXX.js";
import "./chunk-AFMN6TAI.js";
import "./chunk-5NXOGS6W.js";
import "./chunk-US3ISA6J.js";
import "./chunk-56IKAB2G.js";
import "./chunk-SR5T5QTJ.js";
import "./chunk-VTIMQKLI.js";
import "./chunk-XIZJ7GIP.js";
import "./chunk-25DLSVN2.js";
import "./chunk-ZBGMNZ4G.js";
import "./chunk-QEPTV7FP.js";
import "./chunk-UQ76QKZV.js";
import "./chunk-L2YYEYH6.js";
import "./chunk-KVMB3J3O.js";
import "./chunk-WSIE5WWB.js";
import "./chunk-VSFRA4CO.js";
import "./chunk-Y3BGTO6U.js";
import "./chunk-MTN4OBPL.js";
import "./chunk-P2NSRJ6F.js";
import "./chunk-DYEGVO7E.js";
import "./chunk-FDCKJXHS.js";
import "./chunk-UI66QAET.js";
import "./chunk-TFQH4EAC.js";
import "./chunk-4E5CUZQE.js";
import "./chunk-YOPLTIGC.js";
import {
  AddCircleIcon,
  ArrowDownShortIcon,
  ArrowLeftRightIcon,
  ArrowRightShortIcon,
  ArrowUpShortIcon,
  Badge,
  Box,
  Button,
  CaretDownIcon,
  CaretUpIcon,
  ChainIcon,
  Checkbox,
  ChevronDownIcon,
  CloseIcon,
  DataTable,
  Divider,
  DotStatus,
  DropdownMenuContent,
  DropdownMenuPortal,
  DropdownMenuRoot,
  DropdownMenuTrigger,
  EditIcon,
  ExclamationFillIcon,
  ExtensionPositionEnum,
  ExtensionSlot,
  EyeCloseIcon,
  EyeIcon,
  Flex,
  Grid2,
  InfoCircleIcon,
  Input2,
  ListView,
  M,
  NewsFillIcon,
  Picker,
  Popover,
  PopoverContent,
  PopoverRoot,
  PopoverTrigger,
  RefreshIcon,
  Select2,
  SettingFillIcon,
  Sheet,
  SheetContent,
  SheetTrigger,
  SimpleDialog,
  SimpleDropdownMenu,
  SimpleSheet,
  Slider,
  Spinner,
  Statistic,
  Switch,
  TabPanel,
  Tabs,
  Text2,
  ThrottledButton,
  TokenIcon,
  Tooltip,
  TooltipArrow,
  TooltipContent,
  TooltipRoot,
  TooltipTrigger,
  Vt,
  formatAddress,
  formatter_exports,
  gradientTextVariants,
  installExtension,
  modal,
  parseNumber,
  registerSimpleDialog,
  registerSimpleSheet,
  startOfDay,
  startOfHour,
  startOfMinute,
  startOfMonth,
  startOfSecond,
  startOfWeek,
  startOfYear,
  textVariants,
  useModal,
  useScreen
} from "./chunk-PFNLW6VD.js";
import {
  AccountStatusEnum,
  AlgoOrderRootType,
  AssetHistorySideEnum,
  AssetHistoryStatusEnum,
  BBOOrderType,
  ChainNamespace,
  DistributionType,
  EMPTY_LIST,
  MEDIA_TABLET,
  OrderEntrySortKeys,
  OrderLevel,
  OrderSide,
  OrderStatus,
  OrderType,
  PositionType,
  TrackerEventName,
  TradingviewFullscreenKey,
  TrailingCallbackType,
  commify,
  commifyOptional,
  decimal_default2 as decimal_default,
  formatSymbol,
  getBBOType,
  getPrecisionByNumber,
  getTrailingStopPrice,
  isTestnet,
  optimizeSymbolDisplay,
  removeTrailingZeros,
  todpIfNeed,
  zero
} from "./chunk-665UVHU6.js";
import "./chunk-TQ2QBXKN.js";
import "./chunk-SKTD7REL.js";
import {
  require_react_dom
} from "./chunk-5Q5JT3TT.js";
import {
  Trans2 as Trans,
  i18n_default,
  useLocaleCode,
  useTranslation
} from "./chunk-HNOAOT7S.js";
import {
  require_jsx_runtime
} from "./chunk-VNIJ3NDK.js";
import {
  require_react
} from "./chunk-K46N5KBP.js";
import {
  __toESM,
  require_dist,
  require_dist2,
  require_dist3
} from "./chunk-XJ3ZXRPO.js";

// node_modules/@orderly.network/trading/dist/index.mjs
var import_dist25 = __toESM(require_dist(), 1);
var import_dist26 = __toESM(require_dist2(), 1);
var import_dist27 = __toESM(require_dist3(), 1);
var import_react8 = __toESM(require_react(), 1);
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);

// node_modules/@orderly.network/ui-order-entry/dist/index.mjs
var import_dist = __toESM(require_dist(), 1);
var import_dist2 = __toESM(require_dist2(), 1);
var import_dist3 = __toESM(require_dist3(), 1);
var import_react = __toESM(require_react(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var AdditionalInfo = (props) => {
  const { pinned, orderTypeExtra } = props;
  const { t } = useTranslation();
  const onTypeToggle = (type) => (checked) => {
    if (props.onValueChange) {
      props.onValueChange(
        "order_type_ext",
        checked ? type : ""
        // orderTypeExtra === type ? "" : type
      );
    }
  };
  (0, import_react.useEffect)(() => {
    props.onValueChange?.("visible_quantity", props.hidden ? 0 : 1);
  }, [props.hidden]);
  return (0, import_jsx_runtime.jsxs)("div", { className: "oui-text-base-contrast-54", children: [
    (0, import_jsx_runtime.jsxs)(
      Flex,
      {
        justify: pinned ? "start" : "between",
        mb: 3,
        width: pinned ? "unset" : "100%",
        className: "oui-gap-x-2 md:oui-gap-x-3",
        wrap: "wrap",
        gapY: 1,
        children: [
          (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", children: [
            (0, import_jsx_runtime.jsx)(
              Checkbox,
              {
                "data-testid": "oui-testid-orderEntry-postOnly-checkBox",
                id: "toggle_order_post_only",
                className: "oui-peer",
                color: "white",
                variant: "radio",
                disabled: !props.showExtra,
                checked: orderTypeExtra === OrderType.POST_ONLY,
                onCheckedChange: onTypeToggle(OrderType.POST_ONLY)
              }
            ),
            (0, import_jsx_runtime.jsx)(
              Tooltip,
              {
                content: t("orderEntry.orderType.postOnly.tooltip"),
                className: "oui-max-w-[280px] oui-text-2xs oui-text-base-contrast-80",
                children: (0, import_jsx_runtime.jsx)(
                  "label",
                  {
                    htmlFor: "toggle_order_post_only",
                    className: M(
                      "oui-ml-1 oui-text-2xs peer-data-[disabled]:oui-text-base-contrast-20",
                      "oui-cursor-pointer oui-whitespace-nowrap oui-break-normal oui-border-b oui-border-dashed oui-border-line-12"
                    ),
                    children: t("orderEntry.orderType.postOnly")
                  }
                )
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", children: [
            (0, import_jsx_runtime.jsx)(
              Checkbox,
              {
                "data-testid": "oui-testid-orderEntry-ioc-checkBox",
                id: "toggle_order_iov",
                color: "white",
                className: "oui-peer",
                variant: "radio",
                checked: orderTypeExtra === OrderType.IOC,
                onCheckedChange: onTypeToggle(OrderType.IOC),
                disabled: !props.showExtra
              }
            ),
            (0, import_jsx_runtime.jsx)(
              Tooltip,
              {
                content: t("orderEntry.orderType.ioc.tooltip"),
                className: "oui-max-w-[280px] oui-text-2xs oui-text-base-contrast-80",
                children: (0, import_jsx_runtime.jsx)(
                  "label",
                  {
                    htmlFor: "toggle_order_iov",
                    className: M(
                      "oui-ml-1 oui-text-2xs peer-data-[disabled]:oui-text-base-contrast-20",
                      "oui-cursor-pointer oui-whitespace-nowrap oui-break-normal oui-border-b oui-border-dashed oui-border-line-12"
                    ),
                    children: t("orderEntry.orderType.ioc")
                  }
                )
              }
            )
          ] }),
          (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", children: [
            (0, import_jsx_runtime.jsx)(
              Checkbox,
              {
                "data-testid": "oui-testid-orderEntry-fox-checkBox",
                id: "toggle_order_fok",
                color: "white",
                variant: "radio",
                className: "oui-peer",
                checked: orderTypeExtra === OrderType.FOK,
                onCheckedChange: onTypeToggle(OrderType.FOK),
                disabled: !props.showExtra
              }
            ),
            (0, import_jsx_runtime.jsx)(
              Tooltip,
              {
                content: t("orderEntry.orderType.fok.tooltip"),
                className: "oui-max-w-[280px] oui-text-2xs oui-text-base-contrast-80",
                children: (0, import_jsx_runtime.jsx)(
                  "label",
                  {
                    htmlFor: "toggle_order_fok",
                    className: M(
                      "oui-ml-1 oui-text-2xs peer-data-[disabled]:oui-text-base-contrast-20",
                      "oui-cursor-pointer oui-whitespace-nowrap oui-break-normal oui-border-b oui-border-dashed oui-border-line-12"
                    ),
                    children: t("orderEntry.orderType.fok")
                  }
                )
              }
            )
          ] })
        ]
      }
    ),
    (0, import_jsx_runtime.jsxs)(Flex, { gapX: 6, children: [
      (0, import_jsx_runtime.jsxs)(Flex, { children: [
        (0, import_jsx_runtime.jsx)(
          Checkbox,
          {
            "data-testid": "oui-testid-orderEntry-orderConfirm-checkBox",
            id: "toggle_order_confirm",
            color: "white",
            checked: props.needConfirm,
            onCheckedChange: (checked) => {
              props.setNeedConfirm(!!checked);
            }
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "label",
          {
            htmlFor: "toggle_order_confirm",
            className: "oui-ml-1 oui-text-2xs",
            children: t("orderEntry.orderConfirm")
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)(Flex, { children: [
        (0, import_jsx_runtime.jsx)(
          Checkbox,
          {
            "data-testid": "oui-testid-orderEntry-hidden-checkBox",
            id: "toggle_order_hidden",
            color: "white",
            checked: props.hidden,
            onCheckedChange: (checked) => {
              props.setHidden(checked);
            }
          }
        ),
        (0, import_jsx_runtime.jsx)(
          Tooltip,
          {
            content: t("orderEntry.hidden.tooltip"),
            className: "oui-max-w-[280px] oui-text-2xs oui-text-base-contrast-80",
            children: (0, import_jsx_runtime.jsx)(
              "label",
              {
                htmlFor: "toggle_order_hidden",
                className: "oui-ml-1 oui-cursor-pointer oui-border-b oui-border-dashed oui-border-line-12 oui-text-2xs",
                children: t("orderEntry.hidden")
              }
            )
          }
        )
      ] })
    ] }),
    !pinned && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsx)(Divider, { className: "oui-my-3" }),
      (0, import_jsx_runtime.jsxs)(Flex, { children: [
        (0, import_jsx_runtime.jsx)(
          Switch,
          {
            "data-testid": "oui-testid-orderEntry-additional-keepVisible-switch",
            id: "toggle_order_keep_visible",
            onCheckedChange: (checked) => {
              props.setPinned(checked);
            }
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "label",
          {
            htmlFor: "toggle_order_keep_visible",
            className: "oui-ml-1 oui-text-2xs",
            children: t("orderEntry.keepVisible")
          }
        )
      ] })
    ] })
  ] });
};
function AdditionalConfigButton(props) {
  const [open2, setOpen] = (0, import_react.useState)(false);
  return (0, import_jsx_runtime.jsxs)(PopoverRoot, { open: open2, onOpenChange: setOpen, children: [
    (0, import_jsx_runtime.jsx)(PopoverTrigger, { asChild: true, children: (0, import_jsx_runtime.jsx)(
      "button",
      {
        "data-testid": "oui-testid-orderEntry-additional-button",
        onClick: () => {
          setOpen(true);
        },
        children: (0, import_jsx_runtime.jsx)(
          "svg",
          {
            width: "16",
            height: "16",
            viewBox: "0 0 16 16",
            fill: "currentColor",
            xmlns: "http://www.w3.org/2000/svg",
            className: "oui-fill-white/[.36] hover:oui-fill-white/80",
            children: (0, import_jsx_runtime.jsx)(
              "path",
              {
                d: "M3.332 2.665a.667.667 0 0 0-.667.667v1.333c0 .368.299.667.667.667h1.333a.667.667 0 0 0 .667-.667V3.332a.667.667 0 0 0-.667-.667zm4 0a.667.667 0 0 0-.667.667v1.333c0 .368.299.667.667.667h1.333a.667.667 0 0 0 .667-.667V3.332a.667.667 0 0 0-.667-.667zm4 0a.667.667 0 0 0-.667.667v1.333c0 .368.299.667.667.667h1.333a.667.667 0 0 0 .667-.667V3.332a.667.667 0 0 0-.667-.667zm-8 4a.667.667 0 0 0-.667.667v1.333c0 .368.299.667.667.667h1.333a.667.667 0 0 0 .667-.667V7.332a.667.667 0 0 0-.667-.667zm4 0a.667.667 0 0 0-.667.667v1.333c0 .368.299.667.667.667h1.333a.667.667 0 0 0 .667-.667V7.332a.667.667 0 0 0-.667-.667zm4 0a.667.667 0 0 0-.667.667v1.333c0 .368.299.667.667.667h1.333a.667.667 0 0 0 .667-.667V7.332a.667.667 0 0 0-.667-.667zm-8 4a.667.667 0 0 0-.667.667v1.333c0 .368.299.667.667.667h1.333a.667.667 0 0 0 .667-.667v-1.333a.667.667 0 0 0-.667-.667zm4 0a.667.667 0 0 0-.667.667v1.333c0 .368.299.667.667.667h1.333a.667.667 0 0 0 .667-.667v-1.333a.667.667 0 0 0-.667-.667zm4 0a.667.667 0 0 0-.667.667v1.333c0 .368.299.667.667.667h1.333a.667.667 0 0 0 .667-.667v-1.333a.667.667 0 0 0-.667-.667z"
              }
            )
          }
        )
      }
    ) }),
    (0, import_jsx_runtime.jsx)(PopoverContent, { side: "top", align: "end", className: "oui-w-[230px]", children: (0, import_jsx_runtime.jsx)(AdditionalInfo, { ...props }) })
  ] });
}
var defaultPath = "M10.007 1.302a.74.74 0 0 0-.486.214c-1.033.989-1.349 1.815-.972 2.948-.88.675-1.437.84-2.536.84-1.503 0-2.484.182-3.152.85v.02a1.583 1.583 0 0 0 0 2.248l1.867 1.882-3.181 3.18c-.26.26-.28.696-.02.956.261.26.699.26.959 0l3.193-3.194 1.87 1.861a1.585 1.585 0 0 0 2.25 0h.02c.668-.667.854-1.523.854-3.144 0-1.03.212-1.758.852-2.523 1.233.361 1.95.015 2.961-.995a.68.68 0 0 0 .188-.48c0-.234-.06-.593-.209-1.04a5.34 5.34 0 0 0-1.312-2.103 5.35 5.35 0 0 0-2.104-1.312c-.448-.15-.808-.208-1.042-.208";
var PinButton = (props) => {
  const [path, setPath] = (0, import_react.useState)(defaultPath);
  return (0, import_jsx_runtime.jsx)("button", { ...props, children: (0, import_jsx_runtime.jsx)(
    "svg",
    {
      width: 16,
      height: 16,
      viewBox: "0 0 16 16",
      fill: "currentColor",
      xmlns: "http://www.w3.org/2000/svg",
      focusable: false,
      onMouseEnter: () => {
        setPath(
          'M10.008 1.302a.74.74 0 0 0-.486.214c-1.033.989-1.349 1.815-.972 2.948-.88.675-1.437.84-2.536.84-1.503 0-2.484.182-3.152.85v.02a1.583 1.583 0 0 0 0 2.248l1.867 1.882-3.181 3.18c-.26.26-.28.696-.02.956.261.26.699.26.959 0l3.193-3.194 1.87 1.861a1.585 1.585 0 0 0 2.25 0h.02c.668-.667.854-1.523.854-3.144 0-1.03.212-1.758.853-2.523 1.232.361 1.95.015 2.96-.995a.68.68 0 0 0 .188-.48c0-.234-.06-.593-.209-1.04a5.34 5.34 0 0 0-1.312-2.103A5.35 5.35 0 0 0 11.05 1.51c-.448-.15-.808-.208-1.042-.208m.258 1.37c.708.131 1.421.6 1.93 1.107.507.508.94 1.13 1.119 1.945-.636.61-1.026.658-1.662.323a.67.67 0 0 0-.779.117c-1.214 1.213-1.533 2.314-1.533 3.8 0 1.292-.076 1.773-.48 2.206-.113.123-.27.104-.374 0L3.799 7.486a.24.24 0 0 1-.017-.34c.239-.29.769-.515 2.226-.514 1.742.001 2.668-.448 3.812-1.52a.67.67 0 0 0 .125-.77c-.343-.686-.29-1.047.321-1.67"'
        );
      },
      onMouseLeave: () => {
        setPath(defaultPath);
      },
      className: "oui-text-primary-darken",
      children: (0, import_jsx_runtime.jsx)("path", { d: path })
    }
  ) });
};
function AdvancedTPSLResult(props) {
  const { order: formattedOrder, symbolInfo, onEdit, onDelete, errors } = props;
  const { getErrorMsg } = useOrderEntryFormErrorMsg(errors);
  const { t } = useTranslation();
  const renderTp = () => {
    const error = getErrorMsg("tp_trigger_price");
    if (formattedOrder.tp_trigger_price || formattedOrder.tp_order_price) {
      return (0, import_jsx_runtime.jsxs)(
        Flex,
        {
          direction: "column",
          itemAlign: "start",
          className: "oui-w-full",
          gap: 4,
          children: [
            (0, import_jsx_runtime.jsxs)(
              Flex,
              {
                direction: "column",
                itemAlign: "start",
                justify: "between",
                gapY: 1,
                className: "oui-w-full",
                children: [
                  (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", className: "oui-w-full", children: [
                    (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.tpTriggerPrice") }),
                    (0, import_jsx_runtime.jsx)(
                      Text2.numeral,
                      {
                        suffix: (0, import_jsx_runtime.jsx)(Text2, { className: "oui-ml-1 oui-text-base-contrast-36", children: symbolInfo.quote }),
                        className: "oui-text-base-contrast",
                        dp: symbolInfo.quote_dp,
                        children: formattedOrder.tp_trigger_price ?? ""
                      }
                    )
                  ] }),
                  (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", className: "oui-w-full", children: [
                    (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.tpOrderPrice") }),
                    formattedOrder.tp_order_type === OrderType.LIMIT ? (0, import_jsx_runtime.jsx)(
                      Text2.numeral,
                      {
                        suffix: (0, import_jsx_runtime.jsx)(Text2, { className: "oui-ml-1 oui-text-base-contrast-36", children: symbolInfo.quote }),
                        className: "oui-text-base-contrast",
                        dp: symbolInfo.quote_dp,
                        children: formattedOrder.tp_order_price ?? ""
                      }
                    ) : (0, import_jsx_runtime.jsx)(Text2, { className: "oui-text-base-contrast", children: "Market" })
                  ] }),
                  (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", className: "oui-w-full", children: [
                    (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.totalEstTpPnl") }),
                    (0, import_jsx_runtime.jsx)(
                      Text2.numeral,
                      {
                        suffix: (0, import_jsx_runtime.jsx)(Text2, { className: "oui-ml-1 oui-text-base-contrast-36", children: symbolInfo.quote }),
                        coloring: true,
                        dp: 2,
                        children: Number(formattedOrder.tp_pnl)
                      }
                    )
                  ] })
                ]
              }
            ),
            error && (0, import_jsx_runtime.jsxs)(
              Flex,
              {
                justify: "start",
                itemAlign: "start",
                gap: 2,
                className: "oui-w-full",
                children: [
                  (0, import_jsx_runtime.jsx)("div", { className: "oui-relative oui-top-[7px] oui-size-1 oui-rounded-full oui-bg-danger" }),
                  (0, import_jsx_runtime.jsx)(Text2, { className: "oui-text-danger", children: error })
                ]
              }
            )
          ]
        }
      );
    }
    return null;
  };
  const renderSl = () => {
    if (formattedOrder.sl_trigger_price || formattedOrder.sl_order_price) {
      const error = getErrorMsg("sl_trigger_price");
      return (0, import_jsx_runtime.jsx)(
        Flex,
        {
          direction: "column",
          itemAlign: "start",
          className: "oui-w-full",
          gap: 4,
          children: (0, import_jsx_runtime.jsxs)(
            Flex,
            {
              direction: "column",
              itemAlign: "start",
              justify: "between",
              gapY: 1,
              className: "oui-w-full",
              children: [
                (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", className: "oui-w-full", children: [
                  (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.slTriggerPrice") }),
                  (0, import_jsx_runtime.jsx)(
                    Text2.numeral,
                    {
                      suffix: (0, import_jsx_runtime.jsx)(Text2, { className: "oui-ml-1 oui-text-base-contrast-36", children: symbolInfo.quote }),
                      className: "oui-text-base-contrast",
                      dp: symbolInfo.quote_dp,
                      children: formattedOrder.sl_trigger_price ?? ""
                    }
                  )
                ] }),
                (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", className: "oui-w-full", children: [
                  (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.slOrderPrice") }),
                  formattedOrder.sl_order_type === OrderType.LIMIT ? (0, import_jsx_runtime.jsx)(
                    Text2.numeral,
                    {
                      suffix: (0, import_jsx_runtime.jsx)(Text2, { className: "oui-ml-1 oui-text-base-contrast-36", children: symbolInfo.quote }),
                      className: "oui-text-base-contrast",
                      dp: symbolInfo.quote_dp,
                      children: formattedOrder.sl_order_price ?? ""
                    }
                  ) : (0, import_jsx_runtime.jsx)(Text2, { className: "oui-text-base-contrast", children: "Market" })
                ] }),
                (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", className: "oui-w-full", children: [
                  (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.totalEstSlPnl") }),
                  (0, import_jsx_runtime.jsx)(
                    Text2.numeral,
                    {
                      coloring: true,
                      suffix: (0, import_jsx_runtime.jsx)(Text2, { className: "oui-ml-1 oui-text-base-contrast-36", children: symbolInfo.quote }),
                      dp: 2,
                      children: Number(formattedOrder.sl_pnl)
                    }
                  )
                ] }),
                error && (0, import_jsx_runtime.jsxs)(
                  Flex,
                  {
                    justify: "start",
                    itemAlign: "start",
                    gap: 2,
                    className: "oui-w-full",
                    children: [
                      (0, import_jsx_runtime.jsx)("div", { className: "oui-relative oui-top-[7px] oui-size-1 oui-rounded-full oui-bg-danger" }),
                      (0, import_jsx_runtime.jsx)(Text2, { className: "oui-text-danger", children: error })
                    ]
                  }
                )
              ]
            }
          )
        }
      );
    }
    return null;
  };
  return (0, import_jsx_runtime.jsxs)(
    Flex,
    {
      direction: "column",
      itemAlign: "start",
      className: "oui-w-full oui-text-2xs",
      gap: 4,
      children: [
        (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", itemAlign: "start", className: "oui-w-full", children: [
          (0, import_jsx_runtime.jsx)(Text2, { children: t("common.tpsl") }),
          (0, import_jsx_runtime.jsxs)(Flex, { gap: 2, children: [
            (0, import_jsx_runtime.jsx)(
              DeleteIcon,
              {
                size: 12,
                className: "oui-cursor-pointer oui-text-base-contrast-54 hover:oui-text-base-contrast",
                opacity: 1,
                onClick: onDelete
              }
            ),
            (0, import_jsx_runtime.jsx)(
              EditIcon2,
              {
                size: 12,
                className: "oui-cursor-pointer oui-text-base-contrast-54 hover:oui-text-base-contrast",
                onClick: onEdit
              }
            )
          ] })
        ] }),
        (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", itemAlign: "start", className: "oui-w-full", children: [
          (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.mode") }),
          (0, import_jsx_runtime.jsx)(Text2, { className: "oui-text-base-contrast", children: formattedOrder.position_type === PositionType.FULL ? t("tpsl.fullPosition") : t("tpsl.partialPosition") })
        ] }),
        renderTp(),
        renderSl(),
        (0, import_jsx_runtime.jsx)(Divider, { className: "oui-mb-2 oui-w-full" })
      ]
    }
  );
}
var DeleteIcon = (props) => {
  return (0, import_jsx_runtime.jsx)(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      width: props.size,
      height: props.size,
      viewBox: "0 0 12 12",
      fill: "currentColor",
      ...props,
      children: (0, import_jsx_runtime.jsx)("path", { d: "M5.99903 0.976562C5.44653 0.976562 4.99903 1.42406 4.99903 1.97656H2.49902C2.22302 1.97656 1.99902 2.20056 1.99902 2.47656C1.99902 2.75256 2.22302 2.97656 2.49902 2.97656H9.49903C9.77503 2.97656 9.99903 2.75256 9.99903 2.47656C9.99903 2.20056 9.77503 1.97656 9.49903 1.97656H6.99903C6.99903 1.42406 6.55153 0.976562 5.99903 0.976562ZM2.49902 3.97655V8.97654C2.49902 10.0715 3.40152 10.961 4.49903 10.961L7.51453 10.9765C8.61203 10.9765 9.49903 10.074 9.49903 8.97654V3.97655H2.49902ZM4.99903 5.47655C5.27503 5.47655 5.49903 5.70055 5.49903 5.97655V8.97654C5.49903 9.25254 5.27503 9.47654 4.99903 9.47654C4.72303 9.47654 4.49903 9.25254 4.49903 8.97654V5.97655C4.49903 5.70055 4.72303 5.47655 4.99903 5.47655ZM6.99903 5.47655C7.27503 5.47655 7.49903 5.70055 7.49903 5.97655V8.97654C7.49903 9.25254 7.27503 9.47654 6.99903 9.47654C6.72303 9.47654 6.49903 9.25254 6.49903 8.97654V5.97655C6.49903 5.70055 6.72303 5.47655 6.99903 5.47655Z" })
    }
  );
};
var EditIcon2 = (props) => {
  return (0, import_jsx_runtime.jsx)(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      viewBox: "0 0 12 12",
      width: props.size,
      height: props.size,
      fill: "currentColor",
      ...props,
      children: (0, import_jsx_runtime.jsx)("path", { d: "M8.49779 0.976562C8.36529 0.976562 8.23229 1.02357 8.13829 1.11707C7.86029 1.39507 6.85979 2.39558 6.63779 2.61808L2.13529 7.12059L1.63479 7.62059C1.56529 7.69059 1.52929 7.78958 1.50979 7.88658L1.00979 10.3881C0.939788 10.7381 1.23779 11.0361 1.58779 10.9666C1.90079 10.9036 3.77679 10.5286 4.08929 10.4661C4.18629 10.4466 4.28529 10.4106 4.35529 10.3411L4.85529 9.84059L9.35779 5.33808C9.58029 5.11608 10.5808 4.11506 10.8588 3.83756C10.9523 3.74356 10.9993 3.61056 10.9993 3.47806C10.9993 2.65956 10.7908 2.07456 10.3583 1.63306C9.92179 1.18756 9.33879 0.976562 8.49779 0.976562ZM8.69479 1.98606C9.14629 2.01256 9.43879 2.11608 9.63929 2.32108C9.84429 2.53008 9.97379 2.82008 10.0018 3.26258C9.72779 3.53608 9.32679 3.93106 8.99829 4.25956C8.60179 3.86306 8.11279 3.37407 7.71629 2.97757C8.04529 2.64907 8.42129 2.25956 8.69479 1.98606ZM6.99729 3.69657L8.27929 4.97858L4.49579 8.76207L3.21379 7.48009L6.99729 3.69657ZM2.49479 8.19908L3.77679 9.48107L3.72979 9.52809C3.39979 9.59409 2.73329 9.73359 2.11929 9.85659L2.44779 8.24608L2.49479 8.19908Z" })
    }
  );
};
var RegularFeesUI = (props) => {
  const { t } = useTranslation();
  const { taker, maker } = props;
  const originalTrailingFees = (0, import_jsx_runtime.jsx)(Flex, { itemAlign: "center", justify: "between", width: "100%", gap: 1, children: (0, import_jsx_runtime.jsxs)(Flex, { width: "100%", itemAlign: "center", justify: "between", children: [
    (0, import_jsx_runtime.jsx)(Text2, { className: "oui-truncate", size: "2xs", children: t("common.fees") }),
    (0, import_jsx_runtime.jsx)(
      AuthGuard,
      {
        fallback: () => (0, import_jsx_runtime.jsxs)(Text2, { className: "oui-truncate", size: "2xs", children: [
          t("dmm.taker"),
          ": --% / ",
          t("dmm.maker"),
          ": --%"
        ] }),
        children: (0, import_jsx_runtime.jsxs)(Flex, { gap: 1, children: [
          (0, import_jsx_runtime.jsxs)(Text2, { className: "oui-truncate", size: "2xs", children: [
            t("dmm.taker"),
            ":"
          ] }),
          (0, import_jsx_runtime.jsx)(Text2, { size: "2xs", className: "oui-text-base-contrast-80", children: taker }),
          (0, import_jsx_runtime.jsx)(Text2, { size: "2xs", children: "/" }),
          (0, import_jsx_runtime.jsxs)(Text2, { className: "oui-truncate", size: "2xs", children: [
            t("dmm.maker"),
            ":"
          ] }),
          (0, import_jsx_runtime.jsx)(Text2, { size: "2xs", className: "oui-text-base-contrast-80", children: maker })
        ] })
      }
    )
  ] }) });
  return originalTrailingFees;
};
var RegularFeesWidget = (props) => {
  return (0, import_jsx_runtime.jsx)(RegularFeesUI, { ...props });
};
var FeesWidget = ({ symbol }) => {
  const { takerFee, makerFee, rwaTakerFee, rwaMakerFee } = useFeeState();
  const info = useRwaSymbolsInfoStore();
  const isRwa = info?.[symbol] !== void 0;
  return (0, import_jsx_runtime.jsx)(
    RegularFeesWidget,
    {
      taker: isRwa ? rwaTakerFee : takerFee,
      maker: isRwa ? rwaMakerFee : makerFee
    }
  );
};
var options = [0.01, 0.05, 0.1];
var SlippageEditor = (0, import_react.forwardRef)((props, ref) => {
  const { t } = useTranslation();
  const [value, setValue] = (0, import_react.useState)();
  const [customValue, setCustomValue] = (0, import_react.useState)("");
  const [error, setError] = (0, import_react.useState)(void 0);
  (0, import_react.useImperativeHandle)(ref, () => ({
    getValue: () => customValue ? new decimal_default(customValue)?.toNumber() : value
  }));
  (0, import_react.useEffect)(() => {
    if (props.initialValue && !options.includes(props.initialValue)) {
      setCustomValue(props.initialValue.toString());
    } else {
      setValue(props.initialValue);
    }
  }, [props.initialValue, open]);
  const onClick = (val) => {
    setValue(val);
    setCustomValue("");
    setError(void 0);
  };
  const onValueChange = (val) => {
    if (!val) {
      setCustomValue(val);
      return;
    }
    const d = new decimal_default(val);
    setValue(void 0);
    if (d.gt(3)) {
      setCustomValue("3");
      setError(t("orderEntry.slippage.error.exceed"));
    } else {
      setCustomValue(val);
      setError(void 0);
    }
  };
  const toolTipButton = props.isMobile ? (0, import_jsx_runtime.jsx)(
    "button",
    {
      onClick: () => {
        modal.alert({
          title: t("common.tips"),
          message: (0, import_jsx_runtime.jsx)(Text2, { size: "2xs", children: t("orderEntry.slippage.tips") })
        });
      },
      children: (0, import_jsx_runtime.jsx)(ExclamationFillIcon, { className: "oui-text-base-contrast-54", size: 16 })
    }
  ) : (0, import_jsx_runtime.jsx)(
    Tooltip,
    {
      content: (0, import_jsx_runtime.jsx)(Text2, { intensity: 80, size: "2xs", children: t("orderEntry.slippage.tips") }),
      className: "oui-w-[260px] oui-bg-base-6",
      arrow: { className: "oui-fill-base-6" },
      children: (0, import_jsx_runtime.jsx)(TooltipTrigger, { children: (0, import_jsx_runtime.jsx)(ExclamationFillIcon, { className: "oui-text-base-contrast-54", size: 16 }) })
    }
  );
  return (0, import_jsx_runtime.jsxs)("div", { className: "oui-text-2xs", children: [
    (0, import_jsx_runtime.jsxs)(Flex, { mb: 2, gapX: 1, children: [
      (0, import_jsx_runtime.jsx)(Text2, { size: "xs", children: t("orderEntry.slippage") }),
      toolTipButton
    ] }),
    (0, import_jsx_runtime.jsxs)(Flex, { gapX: 2, children: [
      options.map((item) => {
        const isActive = value === item;
        return (0, import_jsx_runtime.jsx)(
          SlippageItem,
          {
            value: item,
            isActive,
            onClick: () => {
              onClick(item);
            }
          },
          item
        );
      }),
      (0, import_jsx_runtime.jsx)(
        Input2,
        {
          suffix: "%",
          formatters: [
            formatter_exports.numberFormatter,
            formatter_exports.dpFormatter(2)
          ],
          value: customValue,
          onValueChange,
          classNames: {
            root: M(
              "oui-rounded-md oui-bg-base-6",
              "oui-h-[40px] oui-w-[74px]"
            ),
            input: "oui-text-base-contrast",
            additional: "oui-pl-1"
          }
        }
      )
    ] }),
    !!error && (0, import_jsx_runtime.jsx)(Box, { mt: 5, className: "-oui-mb-5", children: (0, import_jsx_runtime.jsx)(Text2, { size: "2xs", color: "danger", children: error }) })
  ] });
});
var SlippageItem = ({ value, isActive, onClick }) => {
  return (0, import_jsx_runtime.jsx)(
    Flex,
    {
      intensity: 600,
      justify: "center",
      itemAlign: "center",
      r: "md",
      width: 74,
      height: 40,
      className: M(
        "oui-cursor-pointer oui-select-none",
        isActive ? "oui-bg-primary-light oui-text-primary-contrast/80" : "oui-text-base-contrast-80"
      ),
      onClick,
      children: (0, import_jsx_runtime.jsxs)(Text2, { size: "sm", children: [
        value,
        "%"
      ] })
    }
  );
};
var SlippageCell = (props) => {
  const { t } = useTranslation();
  const [open2, { setTrue: setOpen, setFalse: setClose, toggle }] = useBoolean(false);
  const { isMobile } = useScreen();
  const slippageRef = (0, import_react.useRef)(null);
  const onConfirm = () => {
    const val = slippageRef.current?.getValue();
    props.setSlippage(!val ? "1" : val.toString());
    setClose();
    return Promise.resolve(true);
  };
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      SimpleDialog,
      {
        open: open2,
        onOpenChange: toggle,
        title: t("common.settings"),
        contentProps: { size: isMobile ? "xs" : "sm" },
        actions: {
          primary: {
            disabled: false,
            label: t("common.save"),
            onClick: onConfirm
          },
          secondary: {
            label: t("common.cancel"),
            onClick: () => setClose()
          }
        },
        children: (0, import_jsx_runtime.jsx)(
          SlippageEditor,
          {
            ref: slippageRef,
            isMobile,
            initialValue: props.slippage ? Number(props.slippage) : void 0
          }
        )
      }
    ),
    (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", children: [
      (0, import_jsx_runtime.jsx)(Text2, { size: "2xs", children: t("orderEntry.slippage") }),
      (0, import_jsx_runtime.jsx)(
        AuthGuard,
        {
          fallback: () => (0, import_jsx_runtime.jsxs)(Text2, { size: "2xs", children: [
            t("orderEntry.slippage.est"),
            ": -% / ",
            t("common.max"),
            ": --%"
          ] }),
          children: (0, import_jsx_runtime.jsxs)(Flex, { gap: 1, children: [
            (0, import_jsx_runtime.jsx)(
              Text2.numeral,
              {
                size: "2xs",
                rule: "percentages",
                prefix: `${t("orderEntry.slippage.est")}: `,
                suffix: ` / ${t("common.max")}: `,
                children: props.estSlippage ?? 0
              }
            ),
            (0, import_jsx_runtime.jsx)("button", { className: "oui-text-2xs", onClick: () => setOpen(), children: (0, import_jsx_runtime.jsxs)(Flex, { className: "oui-gap-0.5", as: "span", children: [
              (0, import_jsx_runtime.jsx)(Text2, { size: "2xs", className: "oui-text-primary", children: `${props.slippage || "-"}%` }),
              (0, import_jsx_runtime.jsx)(
                EditIcon,
                {
                  className: "oui-text-primary oui-hidden md:oui-block",
                  size: 12,
                  opacity: 1
                }
              )
            ] }) })
          ] })
        }
      )
    ] })
  ] });
};
var SlippageUI = (props) => {
  return (0, import_jsx_runtime.jsx)(SlippageCell, { ...props });
};
function AssetInfo(props) {
  const { canTrade, disableFeatures, orderType, symbol } = props;
  const { t } = useTranslation();
  const { isMobile } = useScreen();
  const displayEstLiqPrice = useGetEstLiqPrice({
    estLiqPrice: props.estLiqPrice,
    symbol,
    side: props.side
  });
  return (0, import_jsx_runtime.jsxs)("div", { className: "oui-space-y-[2px] xl:oui-space-y-1", children: [
    (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", children: [
      isMobile ? (0, import_jsx_runtime.jsx)(Text2, { size: "2xs", children: t("orderEntry.estLiqPrice") }) : (0, import_jsx_runtime.jsx)(
        Tooltip,
        {
          content: (0, import_jsx_runtime.jsxs)("div", { className: "oui-min-w-[204px] oui-max-w-[280px] oui-text-2xs oui-leading-normal oui-text-base-contrast-80", children: [
            (0, import_jsx_runtime.jsx)("div", { className: "oui-text-pretty", children: t("common.liquidationPrice.tooltip") }),
            (0, import_jsx_runtime.jsx)("div", { children: (0, import_jsx_runtime.jsx)(
              "a",
              {
                href: "https://orderly.network/docs/introduction/trade-on-orderly/perpetual-futures/liquidations",
                target: "_blank",
                rel: "noopener noreferrer",
                className: "oui-text-primary oui-underline",
                children: t("common.liquidationPrice.tooltip.learnMore")
              }
            ) })
          ] }),
          children: (0, import_jsx_runtime.jsx)(
            Text2,
            {
              size: "2xs",
              className: "oui-cursor-pointer oui-border-b oui-border-dashed oui-border-line-12",
              children: t("orderEntry.estLiqPrice")
            }
          )
        }
      ),
      (0, import_jsx_runtime.jsx)(
        Text2.numeral,
        {
          unit: props.quote,
          size: "2xs",
          dp: props.dp,
          className: "oui-text-base-contrast-80",
          unitClassName: "oui-ml-1 oui-text-base-contrast-36",
          children: canTrade ? displayEstLiqPrice ?? "--" : "--"
        }
      )
    ] }),
    orderType === OrderType.MARKET && !disableFeatures?.includes("slippageSetting") && (0, import_jsx_runtime.jsx)(
      SlippageUI,
      {
        slippage: props.slippage,
        setSlippage: props.setSlippage,
        estSlippage: props.estSlippage
      }
    ),
    !disableFeatures?.includes("feesInfo") && (0, import_jsx_runtime.jsx)(FeesWidget, { symbol: props.symbol })
  ] });
}
var calculateLTVColor = (val) => {
  if (val >= 0 && val < 50) {
    return "oui-text-success";
  } else if (val >= 50 && val < 80) {
    return "oui-text-warning";
  } else if (val >= 80) {
    return "oui-text-danger";
  } else {
    return "";
  }
};
var LTVRiskTooltipUI = (props) => {
  const { t } = useTranslation();
  const {
    ltv_threshold,
    negative_usdc_threshold,
    isThresholdLoading,
    holdingData = [],
    currentLtv,
    onConvert
  } = props;
  return (0, import_jsx_runtime.jsxs)(Flex, { gap: 1, className: "oui-w-72 oui-max-w-72", direction: "column", children: [
    (0, import_jsx_runtime.jsxs)(Flex, { width: "100%", justify: "between", itemAlign: "center", children: [
      (0, import_jsx_runtime.jsx)(Text2, { intensity: 36, size: "xs", children: t("common.assets") }),
      (0, import_jsx_runtime.jsx)(Text2, { intensity: 36, size: "xs", children: t("transfer.deposit.collateralContribution") })
    ] }),
    holdingData.map((asset, index) => {
      return (0, import_jsx_runtime.jsxs)(
        Flex,
        {
          width: "100%",
          justify: "between",
          itemAlign: "center",
          children: [
            (0, import_jsx_runtime.jsx)(Text2, { intensity: 80, size: "xs", children: asset.token }),
            (0, import_jsx_runtime.jsx)(
              Text2,
              {
                size: "xs",
                intensity: 80,
                className: M(
                  Number(asset.collateralContribution) < 0 && "oui-text-warning"
                ),
                children: removeTrailingZeros(asset.collateralContribution)
              }
            )
          ]
        },
        `item-${index}`
      );
    }),
    (0, import_jsx_runtime.jsx)(Divider, { className: "oui-w-full" }),
    (0, import_jsx_runtime.jsxs)(Flex, { width: "100%", justify: "between", itemAlign: "center", children: [
      (0, import_jsx_runtime.jsx)(Text2, { intensity: 36, size: "xs", children: t("transfer.LTV.currentLTV") }),
      (0, import_jsx_runtime.jsxs)(
        Text2,
        {
          size: "xs",
          intensity: 36,
          className: M("oui-select-none", calculateLTVColor(currentLtv)),
          children: [
            currentLtv,
            "%"
          ]
        }
      )
    ] }),
    (0, import_jsx_runtime.jsx)(Text2, { className: "oui-py-2", intensity: 54, size: "2xs", children: t("transfer.LTV.tooltip", {
      threshold: isThresholdLoading ? "-" : ltv_threshold,
      usdcThreshold: isThresholdLoading ? "-" : negative_usdc_threshold
    }) }),
    (0, import_jsx_runtime.jsx)(
      Button,
      {
        fullWidth: true,
        size: "md",
        variant: "outlined",
        color: "secondary",
        onClick: onConvert,
        children: t("transfer.convert.convertAssets")
      }
    )
  ] });
};
var useConvertThreshold = () => {
  const { data, error, isLoading } = useQuery(
    "/v1/public/auto_convert_threshold",
    { errorRetryCount: 3 }
  );
  return {
    ltv_threshold: new decimal_default(data?.ltv_threshold ?? 0).mul(100).toNumber(),
    negative_usdc_threshold: data?.negative_usdc_threshold,
    isLoading,
    error
  };
};
var useLTVTooltipScript = () => {
  const { data: holdingList = [], isLoading: isHoldingLoading } = useHoldingStream();
  const {
    ltv_threshold,
    negative_usdc_threshold,
    isLoading: isThresholdLoading
  } = useConvertThreshold();
  const tokensInfo = useAppStore((state) => state.tokensInfo);
  const { getIndexPrice } = useIndexPricesStream();
  const holdingData = holdingList.map((item) => {
    const tokenInfo = tokensInfo?.find(({ token }) => token === item.token);
    const indexPrice = getIndexPrice(item.token);
    const collateralRatio = tokenInfo ? account_exports.collateralRatio({
      baseWeight: tokenInfo.base_weight ?? 0,
      discountFactor: tokenInfo.discount_factor ?? 0,
      collateralQty: item.holding,
      collateralCap: tokenInfo?.user_max_qty ?? item.holding,
      indexPrice
    }) : zero;
    const collateralContribution = account_exports.collateralContribution({
      collateralQty: item.holding,
      collateralCap: tokenInfo?.user_max_qty ?? item.holding,
      collateralRatio: collateralRatio.toNumber(),
      indexPrice
    });
    return {
      ...item,
      collateralContribution
    };
  });
  const currentLtv = useComputedLTV();
  const onConvert = (0, import_react.useCallback)(async () => {
    return modal.show("ConvertDialogId");
  }, []);
  return {
    holdingData,
    isHoldingLoading,
    ltv_threshold,
    negative_usdc_threshold,
    isThresholdLoading,
    currentLtv,
    onConvert
  };
};
var LTVRiskTooltipWidget = () => {
  const state = useLTVTooltipScript();
  return (0, import_jsx_runtime.jsx)(LTVRiskTooltipUI, { ...state });
};
var Available = (props) => {
  const { canTrade, currentLtv, quote, freeCollateral } = props;
  const { t } = useTranslation();
  const { isMobile } = useScreen();
  const showLTV = (0, import_react.useMemo)(() => {
    return typeof currentLtv === "number" && !Number.isNaN(currentLtv) && currentLtv > 0;
  }, [currentLtv]);
  return (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", justify: "between", children: [
    (0, import_jsx_runtime.jsx)(Text2, { size: "2xs", children: t("common.available") }),
    (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", justify: "center", gap: 1, children: [
      showLTV && (0, import_jsx_runtime.jsx)(
        Tooltip,
        {
          className: "oui-bg-base-6 oui-p-2",
          content: (0, import_jsx_runtime.jsx)(LTVRiskTooltipWidget, {}),
          children: (0, import_jsx_runtime.jsx)(
            InfoCircleIcon,
            {
              className: "oui-cursor-pointer oui-text-warning oui-opacity-80"
            }
          )
        }
      ),
      (0, import_jsx_runtime.jsx)(
        Text2.numeral,
        {
          unit: quote,
          size: "2xs",
          className: "oui-text-base-contrast-80",
          unitClassName: "oui-ml-1 oui-text-base-contrast-54",
          dp: 2,
          padding: false,
          children: canTrade ? freeCollateral : 0
        }
      ),
      (0, import_jsx_runtime.jsx)(
        Button,
        {
          variant: "text",
          size: "xs",
          color: "secondary",
          className: "oui-p-0 hover:oui-text-base-contrast-80",
          onClick: () => {
            const handleDomId = isMobile ? "DepositAndWithdrawWithSheetId" : "DepositAndWithdrawWithDialogId";
            modal.show(handleDomId, {
              activeTab: "deposit"
            });
          },
          children: (0, import_jsx_runtime.jsx)(AddCircleIcon, { opacity: 1 })
        }
      )
    ] })
  ] });
};
function isBBOOrder(options2) {
  const { order_type, order_type_ext } = options2;
  const isBBO = [OrderType.ASK, OrderType.BID].includes(order_type_ext);
  if (order_type) {
    return order_type === OrderType.LIMIT && isBBO;
  }
  return isBBO;
}
function getOrderTypeByBBO(value, size) {
  if ([BBOOrderType.COUNTERPARTY1, BBOOrderType.COUNTERPARTY5].includes(value)) {
    return size === OrderSide.BUY ? OrderType.ASK : OrderType.BID;
  }
  if ([BBOOrderType.QUEUE1, BBOOrderType.QUEUE5].includes(value)) {
    return size === OrderSide.BUY ? OrderType.BID : OrderType.ASK;
  }
}
function getOrderLevelByBBO(value) {
  if ([BBOOrderType.COUNTERPARTY1, BBOOrderType.QUEUE1].includes(value)) {
    return OrderLevel.ONE;
  }
  if ([BBOOrderType.COUNTERPARTY5, BBOOrderType.QUEUE5].includes(value)) {
    return OrderLevel.FIVE;
  }
}
function getScaledPlaceOrderMessage(result) {
  const rows = result?.data?.rows || [];
  if (rows.length > 0) {
    const totalCount = rows.length;
    const successCount = rows.filter((row) => row.success).length;
    if (successCount === totalCount) {
      return i18n_default.t("orderEntry.scaledOrder.fullySuccessful", {
        total: totalCount
      });
    }
    if (successCount === 0) {
      return i18n_default.t("orderEntry.scaledOrder.allFailed");
    }
    return i18n_default.t("orderEntry.scaledOrder.partiallySuccessful", {
      successCount,
      total: totalCount
    });
  }
}
var safeNumber = (val) => {
  return Number.isNaN(Number(val)) ? 0 : Number(val);
};
var OrderConfirmDialog = (props) => {
  const { symbolInfo, order, onConfirm, onCancel } = props;
  const { quote, quote_dp, base_dp } = symbolInfo;
  const { side, order_type, order_type_ext, level, symbol } = order;
  const { t } = useTranslation();
  const [{ rows: positions }] = usePositionStream(symbol);
  const position = positions?.[0];
  const positionQty = position?.position_qty;
  const [_, setNeedConfirm] = useLocalStorage("orderly_order_confirm", true);
  const renderPositionType = () => {
    if (order.position_type === PositionType.FULL) {
      return (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.positionType.full") });
    }
    return (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.positionType.partial") });
  };
  const renderPrice = () => {
    if (order_type === OrderType.MARKET || order_type === OrderType.STOP_MARKET) {
      return (0, import_jsx_runtime.jsx)(Text2, { intensity: 80, children: t("common.marketPrice") });
    }
    if (isBBOOrder({ order_type, order_type_ext })) {
      const bboType = getBBOType({
        type: order_type_ext,
        side,
        level
      });
      const label = {
        [BBOOrderType.COUNTERPARTY1]: t("orderEntry.bbo.counterparty1"),
        [BBOOrderType.COUNTERPARTY5]: t("orderEntry.bbo.counterparty5"),
        [BBOOrderType.QUEUE1]: t("orderEntry.bbo.queue1"),
        [BBOOrderType.QUEUE5]: t("orderEntry.bbo.queue5")
      }[bboType];
      return (0, import_jsx_runtime.jsx)(Text2, { intensity: 80, children: label });
    }
    return (0, import_jsx_runtime.jsx)(
      Text2.numeral,
      {
        unit: quote,
        rule: "price",
        className: "oui-text-base-contrast",
        unitClassName: "oui-text-base-contrast-36 oui-ml-1",
        dp: quote_dp,
        padding: false,
        children: order.order_price
      }
    );
  };
  const renderTPSLPrice = ({
    price,
    isOrderPrice,
    isEnable,
    colorType
  }) => {
    if (!isEnable) {
      return (0, import_jsx_runtime.jsx)(Text2, { className: "oui-text-base-contrast-36", children: "-- USDC" });
    }
    if (!price) {
      if (isOrderPrice) {
        return (0, import_jsx_runtime.jsx)(Text2, { className: "oui-text-base-contrast-36", children: t("common.marketPrice") });
      }
    }
    return (0, import_jsx_runtime.jsx)(
      Text2.numeral,
      {
        unit: "USDC",
        rule: "price",
        className: M(
          "oui-text-base-contrast",
          colorType === "TP" ? "oui-text-trade-profit" : "oui-text-trade-loss"
        ),
        unitClassName: "oui-text-base-contrast-36 oui-ml-1",
        dp: quote_dp,
        padding: false,
        children: price
      }
    );
  };
  const renderTPSLQty = () => {
    if (!positionQty || !order.order_quantity) {
      return null;
    }
    let qty = new decimal_default(order.order_quantity);
    if (order.position_type === PositionType.FULL) {
      qty = qty.plus(new decimal_default(positionQty ?? 0));
    }
    return (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", children: [
      (0, import_jsx_runtime.jsx)(Text2, { children: order.position_type === PositionType.FULL ? t("common.positionQty") : t("common.orderQty") }),
      (0, import_jsx_runtime.jsx)(
        Text2.numeral,
        {
          rule: "price",
          dp: base_dp,
          padding: false,
          className: "oui-text-base-contrast",
          children: qty.toNumber()
        }
      )
    ] });
  };
  const renderPriceAndTotal = () => {
    if (order_type === OrderType.TRAILING_STOP) {
      const { activated_price, callback_value, callback_rate } = order;
      const callbackView = callback_rate ? (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", children: [
        (0, import_jsx_runtime.jsx)(Text2, { children: t("orderEntry.trailingRate") }),
        (0, import_jsx_runtime.jsxs)(Text2, { className: "oui-text-base-contrast", children: [
          callback_rate,
          "%"
        ] })
      ] }) : (0, import_jsx_runtime.jsx)(
        OrderItem,
        {
          title: t("orderEntry.trailingValue"),
          value: callback_value,
          unit: quote,
          dp: quote_dp
        }
      );
      return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        activated_price && (0, import_jsx_runtime.jsx)(
          OrderItem,
          {
            title: t("common.triggerPrice"),
            value: activated_price,
            unit: quote,
            dp: quote_dp
          }
        ),
        callbackView
      ] });
    }
    return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
      (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", children: [
        (0, import_jsx_runtime.jsx)(Text2, { children: t("common.price") }),
        renderPrice()
      ] }),
      (0, import_jsx_runtime.jsx)(
        OrderItem,
        {
          title: t("common.estTotal"),
          value: order.total,
          unit: quote,
          dp: quote_dp
        }
      )
    ] });
  };
  const header = (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", children: [
    (0, import_jsx_runtime.jsx)(Text2.formatted, { rule: "symbol", showIcon: true, children: order.symbol }),
    (0, import_jsx_runtime.jsxs)(Flex, { justify: "end", gapX: 1, children: [
      (0, import_jsx_runtime.jsx)(OrderTypeTag, { type: order_type }),
      side === OrderSide.BUY ? (0, import_jsx_runtime.jsx)(Badge, { color: "buy", size: "sm", children: t("common.buy") }) : (0, import_jsx_runtime.jsx)(Badge, { color: "sell", size: "sm", children: t("common.sell") })
    ] })
  ] });
  const quantityItem = (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", children: [
    (0, import_jsx_runtime.jsx)(Text2, { children: t("common.orderQty") }),
    (0, import_jsx_runtime.jsx)(
      Text2.numeral,
      {
        rule: "price",
        dp: base_dp,
        padding: false,
        className: "oui-text-base-contrast",
        children: order.order_quantity
      }
    )
  ] });
  const triggerPriceItem = (order_type === OrderType.STOP_LIMIT || order_type === OrderType.STOP_MARKET && order.trigger_price) && (0, import_jsx_runtime.jsx)(
    OrderItem,
    {
      title: t("common.trigger"),
      value: order.trigger_price,
      unit: quote,
      dp: quote_dp
    }
  );
  const tpslTriggerPrice = (order.tp_trigger_price || order.sl_trigger_price) && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(Divider, { className: "oui-my-4" }),
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className: textVariants({
          size: "sm",
          intensity: 54,
          className: "oui-space-y-1 oui-w-full oui-flex oui-flex-col oui-gap-3"
        }),
        children: [
          (0, import_jsx_runtime.jsx)(Text2, { className: "oui-text-base-contrast", children: renderPositionType() }),
          renderTPSLQty(),
          (0, import_jsx_runtime.jsxs)(
            Flex,
            {
              direction: "column",
              justify: "between",
              itemAlign: "start",
              gap: 1,
              className: "oui-w-full",
              children: [
                (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", className: "oui-w-full", children: [
                  (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.tpTriggerPrice") }),
                  renderTPSLPrice({
                    price: order.tp_trigger_price ?? "",
                    isOrderPrice: false,
                    isEnable: !!order.tp_trigger_price,
                    colorType: "TP"
                  })
                ] }),
                (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", className: "oui-w-full", children: [
                  (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.tpOrderPrice") }),
                  renderTPSLPrice({
                    price: order.tp_order_price ?? "",
                    isOrderPrice: true,
                    isEnable: !!order.tp_trigger_price,
                    colorType: "TP"
                  })
                ] })
              ]
            }
          ),
          (0, import_jsx_runtime.jsxs)(
            Flex,
            {
              direction: "column",
              justify: "between",
              itemAlign: "start",
              gap: 1,
              className: "oui-w-full",
              children: [
                (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", className: "oui-w-full", children: [
                  (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.slTriggerPrice") }),
                  renderTPSLPrice({
                    price: order.sl_trigger_price ?? "",
                    isOrderPrice: false,
                    isEnable: !!order.sl_trigger_price,
                    colorType: "SL"
                  })
                ] }),
                (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", className: "oui-w-full", children: [
                  (0, import_jsx_runtime.jsx)(Text2, { children: t("tpsl.slOrderPrice") }),
                  renderTPSLPrice({
                    price: order.sl_order_price ?? "",
                    isOrderPrice: true,
                    isEnable: !!order.sl_trigger_price,
                    colorType: "SL"
                  })
                ] })
              ]
            }
          )
        ]
      }
    )
  ] });
  const orderConfirmCheckbox = (0, import_jsx_runtime.jsxs)(Flex, { gapX: 1, pt: 4, pb: 5, children: [
    (0, import_jsx_runtime.jsx)(
      Checkbox,
      {
        id: "orderConfirm",
        color: "white",
        onCheckedChange: (checked) => {
          setNeedConfirm(!!!checked);
        }
      }
    ),
    (0, import_jsx_runtime.jsx)(
      "label",
      {
        htmlFor: "orderConfirm",
        className: textVariants({
          size: "xs",
          intensity: 54
        }),
        children: t("orderEntry.disableOrderConfirm")
      }
    )
  ] });
  const buttons = (0, import_jsx_runtime.jsxs)(Grid2, { cols: 2, gapX: 3, children: [
    (0, import_jsx_runtime.jsx)(Button, { color: "secondary", size: "md", onClick: () => onCancel(), children: t("common.cancel") }),
    (0, import_jsx_runtime.jsx)(Button, { size: "md", onClick: () => onConfirm(), children: t("common.confirm") })
  ] });
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    header,
    (0, import_jsx_runtime.jsx)(Divider, { className: "oui-my-4" }),
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className: textVariants({
          size: "sm",
          intensity: 54,
          className: "oui-space-y-1"
        }),
        children: [
          quantityItem,
          triggerPriceItem,
          renderPriceAndTotal()
        ]
      }
    ),
    tpslTriggerPrice,
    orderConfirmCheckbox,
    buttons
  ] });
};
var OrderItem = (props) => {
  const { title, value, unit, dp } = props;
  return (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", children: [
    (0, import_jsx_runtime.jsx)(Text2, { children: title }),
    (0, import_jsx_runtime.jsx)(
      Text2.numeral,
      {
        unit,
        rule: "price",
        dp,
        padding: false,
        className: "oui-text-base-contrast",
        unitClassName: "oui-text-base-contrast-36 oui-ml-1",
        children: value
      }
    )
  ] });
};
OrderConfirmDialog.displayName = "OrderConfirmDialog";
var OrderTypeTag = (props) => {
  const { t } = useTranslation();
  const typeStr = (0, import_react.useMemo)(() => {
    switch (props.type) {
      case OrderType.LIMIT:
        return t("orderEntry.orderType.limit");
      case OrderType.MARKET:
        return t("common.marketPrice");
      case OrderType.STOP_LIMIT:
        return t("orderEntry.orderType.stopLimit");
      case OrderType.STOP_MARKET:
        return t("orderEntry.orderType.stopMarket");
      case OrderType.TRAILING_STOP:
        return t("orderEntry.orderType.trailingStop");
      default:
        return "";
    }
  }, [props.type]);
  return (0, import_jsx_runtime.jsx)(Badge, { color: "neutral", size: "sm", children: typeStr });
};
var Dialog = (props) => {
  const { close, resolve, reject, ...rest } = props;
  return (0, import_jsx_runtime.jsx)(
    OrderConfirmDialog,
    {
      ...rest,
      onCancel: () => {
        reject();
        close();
      },
      onConfirm: () => {
        resolve();
        close();
      }
    }
  );
};
var orderConfirmDialogId = "orderConfirm";
registerSimpleDialog(orderConfirmDialogId, Dialog, {
  size: "sm",
  title: () => i18n_default.t("orderEntry.orderConfirm")
});
var MaxQtyConfirm = (0, import_react.memo)((props) => {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsx)(
    SimpleDialog,
    {
      open: props.open,
      title: t("orderEntry.orderConfirm"),
      closable: true,
      onOpenChange: props.onOpenChange,
      size: "sm",
      actions: {
        primary: {
          label: t("orderEntry.placeOrderNow"),
          className: "oui-text-sm oui-font-semibold oui-w-[100%] oui-h-8",
          onClick: () => {
            props.onConfirm();
            return Promise.resolve();
          }
        },
        secondary: {
          label: t("common.cancel"),
          className: "oui-text-sm oui-font-semibold oui-w-[100%] oui-h-8",
          onClick: () => {
            props.onOpenChange(false);
            return Promise.resolve();
          }
        }
      },
      children: (0, import_jsx_runtime.jsx)("div", { className: "oui-text-2xs lg:oui-text-sm", children: t("orderEntry.maxQty.reminder.content", {
        maxQty: `${props.maxQty} ${props.base}`
      }) })
    }
  );
});
var ScaledOrderConfirm = (props) => {
  const { order, symbolInfo, dataSource, national, askAndBid, totalQuantity } = props;
  const { base, quote, base_dp, quote_dp } = symbolInfo;
  const { t } = useTranslation();
  const onCancel = () => {
    props.reject();
    props.close?.();
  };
  const onConfirm = () => {
    props.resolve();
    props.close?.();
  };
  const columns = (0, import_react.useMemo)(() => {
    return [
      {
        title: t("common.symbol"),
        dataIndex: "symbol",
        width: 125,
        render: (value, record) => {
          return (0, import_jsx_runtime.jsxs)(Flex, { gap: 2, children: [
            (0, import_jsx_runtime.jsx)(
              "div",
              {
                className: M(
                  "oui-h-[38px] oui-w-1 oui-shrink-0 oui-rounded-[1px]",
                  record.side === OrderSide.BUY ? "oui-bg-trade-profit" : "oui-bg-trade-loss"
                )
              }
            ),
            (0, import_jsx_runtime.jsxs)(Flex, { direction: "column", itemAlign: "start", children: [
              (0, import_jsx_runtime.jsxs)(Flex, { gapX: 1, children: [
                (0, import_jsx_runtime.jsx)(TokenIcon, { symbol: value, className: "oui-size-3" }),
                (0, import_jsx_runtime.jsx)(
                  Text2.formatted,
                  {
                    rule: "symbol",
                    size: "xs",
                    formatString: "base-type",
                    children: value
                  }
                )
              ] }),
              (0, import_jsx_runtime.jsx)(Badge, { color: "neutral", size: "xs", children: t("orderEntry.orderType.limit") })
            ] })
          ] });
        }
      },
      {
        title: t("common.quantity"),
        dataIndex: "order_quantity",
        width: 100,
        render: (value, record) => {
          return (0, import_jsx_runtime.jsx)(
            Text2.numeral,
            {
              rule: "price",
              dp: base_dp,
              padding: false,
              color: record.side === OrderSide.BUY ? "buy" : "sell",
              children: value
            }
          );
        }
      },
      {
        title: t("common.orderPrice"),
        dataIndex: "order_price",
        width: 100,
        render: (value, record) => {
          const showWarning = !!(record.side === OrderSide.BUY ? askAndBid?.[0] && Number(value) >= askAndBid?.[0] : askAndBid?.[1] && Number(value) <= askAndBid?.[1]);
          return (0, import_jsx_runtime.jsxs)(Flex, { gapX: 1, children: [
            (0, import_jsx_runtime.jsx)(Text2.numeral, { rule: "price", dp: quote_dp, children: value }),
            showWarning && (0, import_jsx_runtime.jsx)(
              Tooltip,
              {
                content: t(
                  "orderEntry.confirmScaledOrder.orderPrice.warning"
                ),
                className: "oui-w-[240px] oui-text-2xs oui-font-semibold oui-text-base-contrast-80",
                children: (0, import_jsx_runtime.jsx)(TooltipIcon, { className: "oui-text-warning-darken" })
              }
            )
          ] });
        }
      }
    ];
  }, [t, symbolInfo, askAndBid, base_dp, quote_dp]);
  return (0, import_jsx_runtime.jsxs)("div", { className: "oui-font-semibold", children: [
    (0, import_jsx_runtime.jsx)(
      DataTable,
      {
        classNames: {
          root: M(
            "oui-bg-base-7",
            "oui-rounded-lg",
            // need to set overflow hidden because table header will avoid the border radius
            "oui-overflow-hidden",
            // "oui-text-2xs lg:oui-text-xs",
            // if orders is greater than 6, set the height to 320px to show scroll bar
            order.orders?.length >= 6 && "oui-h-[320px]"
          ),
          // set the min height of the table to show 2 rows
          scroll: "!oui-min-h-[130px]"
        },
        dataSource,
        columns,
        bordered: true,
        onRow: () => {
          return {
            className: M("oui-h-[50px]")
          };
        }
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: "oui-mb-5 oui-mt-4 oui-text-2xs", children: [
      (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", children: [
        (0, import_jsx_runtime.jsx)(Text2, { children: t("orderEntry.totalOrders") }),
        (0, import_jsx_runtime.jsx)(Text2, { intensity: 80, children: order.orders?.length })
      ] }),
      (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", mt: 2, children: [
        (0, import_jsx_runtime.jsx)(Text2, { children: t("orderEntry.totalQuantity") }),
        (0, import_jsx_runtime.jsx)(
          Text2.numeral,
          {
            rule: "price",
            unit: base,
            dp: base_dp,
            padding: false,
            intensity: 80,
            unitClassName: "oui-text-base-contrast-36 oui-ml-1",
            children: totalQuantity
          }
        )
      ] }),
      (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", mt: 2, children: [
        (0, import_jsx_runtime.jsx)(Text2, { children: t("common.notional") }),
        (0, import_jsx_runtime.jsx)(
          Text2.numeral,
          {
            rule: "price",
            unit: quote,
            dp: quote_dp,
            padding: false,
            intensity: 80,
            unitClassName: "oui-text-base-contrast-36 oui-ml-1",
            children: national
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsxs)(Grid2, { cols: 2, gapX: 3, children: [
      (0, import_jsx_runtime.jsx)(Button, { color: "secondary", size: "md", onClick: onCancel, children: t("common.cancel") }),
      (0, import_jsx_runtime.jsx)(Button, { size: "md", onClick: onConfirm, children: t("common.confirm") })
    ] })
  ] });
};
var TooltipIcon = (0, import_react.forwardRef)(
  (props, ref) => {
    return (0, import_jsx_runtime.jsx)(
      "svg",
      {
        width: "12",
        height: "13",
        viewBox: "0 0 12 13",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ref,
        ...props,
        children: (0, import_jsx_runtime.jsx)("path", { d: "M5.99951 1.50708C3.23811 1.50708 0.999512 3.74558 0.999512 6.50708C0.999512 9.26858 3.23811 11.5071 5.99951 11.5071C8.76091 11.5071 10.9995 9.26858 10.9995 6.50708C10.9995 3.74558 8.76091 1.50708 5.99951 1.50708ZM5.99951 4.00708C6.27566 4.00708 6.49951 4.23108 6.49951 4.50708C6.49951 4.78308 6.27566 5.00708 5.99951 5.00708C5.72336 5.00708 5.49951 4.78308 5.49951 4.50708C5.49951 4.23108 5.72336 4.00708 5.99951 4.00708ZM5.99951 5.50708C6.27566 5.50708 6.49951 5.73108 6.49951 6.00708V8.50708C6.49951 8.78308 6.27566 9.00708 5.99951 9.00708C5.72336 9.00708 5.49951 8.78308 5.49951 8.50708V6.00708C5.49951 5.73108 5.72336 5.50708 5.99951 5.50708Z" })
      }
    );
  }
);
function useAskAndBid() {
  const ee = useEventEmitter();
  const [askAndBid, setAskAndBid] = (0, import_react.useState)([0, 0]);
  const onOrderBookUpdate = c((data) => {
    const ask0 = data.asks?.[data.asks.length - 1]?.[0];
    const bid0 = data.bids?.[0]?.[0];
    setAskAndBid([ask0, bid0]);
  }, 200);
  (0, import_react.useEffect)(() => {
    ee.on("orderbook:update", onOrderBookUpdate);
    return () => {
      ee.off("orderbook:update", onOrderBookUpdate);
      onOrderBookUpdate.cancel();
    };
  }, [onOrderBookUpdate]);
  return askAndBid;
}
function useScaledOrderConfirmScript(options2) {
  const { order, symbolInfo } = options2;
  const orders = order.orders;
  const askAndBid = useAskAndBid();
  const national = (0, import_react.useMemo)(() => {
    const national2 = orders.reduce((acc, order2) => {
      return acc.add(new decimal_default(order2.order_price).mul(order2.order_quantity));
    }, zero);
    return national2.toNumber();
  }, [orders]);
  const totalQuantity = (0, import_react.useMemo)(() => {
    const totalQuantity2 = orders.reduce((acc, order2) => {
      return acc.add(new decimal_default(order2.order_quantity));
    }, zero);
    return totalQuantity2.toString();
  }, [orders, symbolInfo.base_dp]);
  return { dataSource: orders, national, askAndBid, totalQuantity };
}
var ScaledOrderConfirmWidget = (props) => {
  const state = useScaledOrderConfirmScript(props);
  return (0, import_jsx_runtime.jsx)(ScaledOrderConfirm, { ...props, ...state });
};
var scaledOrderConfirmDialogId = "scaledOrderConfirm";
registerSimpleDialog(scaledOrderConfirmDialogId, ScaledOrderConfirmWidget, {
  size: "md",
  title: () => i18n_default.t("orderEntry.confirmScaledOrder"),
  contentProps: {
    // className: "oui-p-0",
  }
});
var OrderTypeSelect = (props) => {
  const { t } = useTranslation();
  const options2 = (0, import_react.useMemo)(() => {
    return [
      { label: t("orderEntry.orderType.limitOrder"), value: OrderType.LIMIT },
      { label: t("orderEntry.orderType.marketOrder"), value: OrderType.MARKET },
      {
        label: t("orderEntry.orderType.stopLimit"),
        value: OrderType.STOP_LIMIT
      },
      {
        label: t("orderEntry.orderType.stopMarket"),
        value: OrderType.STOP_MARKET
      },
      {
        label: t("orderEntry.orderType.scaledOrder"),
        value: OrderType.SCALED
      },
      {
        label: t("orderEntry.orderType.trailingStop"),
        value: OrderType.TRAILING_STOP
      }
    ];
  }, [t]);
  const displayLabelMap = (0, import_react.useMemo)(() => {
    return {
      [OrderType.LIMIT]: t("orderEntry.orderType.limit"),
      [OrderType.MARKET]: t("common.marketPrice"),
      [OrderType.STOP_LIMIT]: t("orderEntry.orderType.stopLimit"),
      [OrderType.STOP_MARKET]: t("orderEntry.orderType.stopMarket"),
      [OrderType.SCALED]: t("orderEntry.orderType.scaledOrder"),
      [OrderType.TRAILING_STOP]: t("orderEntry.orderType.trailingStop")
    };
  }, [t]);
  return (0, import_jsx_runtime.jsx)(
    Select2.options,
    {
      testid: "oui-testid-orderEntry-orderType-button",
      currentValue: props.type,
      value: props.type,
      options: options2,
      onValueChange: props.onChange,
      contentProps: {
        className: "oui-bg-base-8"
      },
      classNames: {
        trigger: "oui-bg-base-6 oui-border-line"
      },
      valueFormatter: (value, option) => {
        const item = options2.find((o) => o.value === value);
        if (!item) {
          return (0, import_jsx_runtime.jsx)(Text2, { size: "xs", children: option.placeholder });
        }
        const label = displayLabelMap[value];
        return (0, import_jsx_runtime.jsx)(
          Text2,
          {
            size: "xs",
            color: props.canTrade ? props.side === OrderSide.BUY ? "buy" : "sell" : void 0,
            children: label
          }
        );
      },
      size: "md"
    }
  );
};
var LeverageBadge = (props) => {
  const { symbol, side, symbolLeverage } = props;
  const { isMobile } = useScreen();
  const { maxLeverage } = useSymbolLeverage(symbol);
  const curLeverage = symbolLeverage || maxLeverage;
  const showModal = () => {
    const modalId = isMobile ? SymbolLeverageSheetId : SymbolLeverageDialogId;
    modal.show(modalId, {
      symbol,
      side,
      curLeverage
    });
  };
  return (0, import_jsx_runtime.jsxs)(
    Flex,
    {
      justify: "center",
      itemAlign: "center",
      gapX: 1,
      className: M(
        "oui-h-8",
        "oui-rounded oui-border oui-border-line oui-bg-base-6",
        "oui-cursor-pointer oui-select-none oui-text-xs oui-font-semibold oui-text-base-contrast-54"
      ),
      onClick: showModal,
      children: [
        (0, import_jsx_runtime.jsx)(Text2, { children: "Cross" }),
        (0, import_jsx_runtime.jsx)(Text2.numeral, { dp: 0, rm: decimal_default.ROUND_DOWN, unit: "X", children: curLeverage })
      ]
    }
  );
};
function OrderEntryHeader(props) {
  const { canTrade, side, order_type, setOrderValue } = props;
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className: M(
          "oui-grid oui-w-full oui-flex-1 oui-gap-x-2 lg:oui-flex lg:oui-gap-x-[6px]",
          "oui-grid-cols-2"
        ),
        children: [
          (0, import_jsx_runtime.jsx)(
            Button,
            {
              onClick: () => {
                props.setOrderValue("side", OrderSide.BUY);
              },
              size: "md",
              fullWidth: true,
              "data-type": OrderSide.BUY,
              className: M(
                side === OrderSide.BUY && canTrade ? "oui-bg-success-darken hover:oui-bg-success-darken/80 active:oui-bg-success-darken/80" : "oui-bg-base-7 oui-text-base-contrast-36 hover:oui-bg-base-6 active:oui-bg-base-6"
              ),
              "data-testid": "oui-testid-orderEntry-side-buy-button",
              children: t("common.buy")
            }
          ),
          (0, import_jsx_runtime.jsx)(
            Button,
            {
              onClick: () => {
                props.setOrderValue("side", OrderSide.SELL);
              },
              "data-type": OrderSide.SELL,
              fullWidth: true,
              size: "md",
              className: M(
                side === OrderSide.SELL && props.canTrade ? "oui-bg-danger-darken hover:oui-bg-danger-darken/80 active:oui-bg-danger-darken/80" : "oui-bg-base-7 oui-text-base-contrast-36 hover:oui-bg-base-6 active:oui-bg-base-6"
              ),
              "data-testid": "oui-testid-orderEntry-side-sell-button",
              children: t("common.sell")
            }
          )
        ]
      }
    ),
    (0, import_jsx_runtime.jsxs)(
      "div",
      {
        className: M(
          "oui-grid oui-gap-x-2 lg:oui-flex lg:oui-gap-x-[6px]",
          "oui-grid-cols-2"
        ),
        children: [
          (0, import_jsx_runtime.jsx)("div", { className: "oui-w-full", children: (0, import_jsx_runtime.jsx)(
            OrderTypeSelect,
            {
              type: order_type,
              side,
              canTrade,
              onChange: (type) => {
                setOrderValue("order_type", type);
              }
            }
          ) }),
          (0, import_jsx_runtime.jsx)("div", { className: "oui-w-full", children: (0, import_jsx_runtime.jsx)(
            LeverageBadge,
            {
              symbol: props.symbol,
              side: props.side,
              symbolLeverage: props.symbolLeverage
            }
          ) })
        ]
      }
    )
  ] });
}
var OrderEntryContext = (0, import_react.createContext)(
  {}
);
var useOrderEntryContext = () => {
  return (0, import_react.useContext)(OrderEntryContext);
};
var OrderEntryProvider = (props) => {
  const {
    errorMsgVisible,
    symbolInfo,
    onFocus,
    onBlur,
    getErrorMsg,
    setOrderValue,
    setOrderValues,
    currentFocusInput,
    errors,
    priceInputRef,
    priceInputContainerRef,
    triggerPriceInputRef,
    activatedPriceInputRef,
    lastQuantityInputType,
    leverage
  } = props;
  const memoizedValue = (0, import_react.useMemo)(() => {
    return {
      errorMsgVisible,
      symbolInfo,
      onFocus,
      onBlur,
      getErrorMsg,
      setOrderValue,
      setOrderValues,
      currentFocusInput,
      errors,
      // refs
      priceInputRef,
      priceInputContainerRef,
      triggerPriceInputRef,
      activatedPriceInputRef,
      lastQuantityInputType,
      leverage
    };
  }, [
    errorMsgVisible,
    symbolInfo,
    onFocus,
    onBlur,
    getErrorMsg,
    setOrderValue,
    setOrderValues,
    currentFocusInput,
    errors,
    priceInputRef,
    priceInputContainerRef,
    triggerPriceInputRef,
    activatedPriceInputRef,
    lastQuantityInputType,
    leverage
  ]);
  return (0, import_jsx_runtime.jsx)(OrderEntryContext.Provider, { value: memoizedValue, children: props.children });
};
var CustomInput = (0, import_react.forwardRef)(
  (props, ref) => {
    const { placeholder = "0" } = props;
    const { errorMsgVisible } = useOrderEntryContext();
    return (0, import_jsx_runtime.jsx)(
      Input2.tooltip,
      {
        ref,
        tooltip: errorMsgVisible ? props.error : void 0,
        autoComplete: "off",
        autoFocus: props.autoFocus,
        size: "lg",
        placeholder: props.readonly ? "" : placeholder,
        id: props.id,
        name: props.name,
        color: props.error ? "danger" : void 0,
        prefix: props.prefix || (0, import_jsx_runtime.jsx)(InputLabel, { id: props.id, className: props.classNames?.prefix, children: props.label }),
        suffix: props.suffix,
        value: props.readonly ? "" : props.value || "",
        onValueChange: props.onChange,
        onFocus: (event) => {
          props.onFocus?.(event);
        },
        onBlur: (event) => {
          props.onBlur?.(event);
        },
        formatters: props.overrideFormatters || [
          ...props.formatters ?? EMPTY_LIST,
          formatter_exports.numberFormatter,
          formatter_exports.currencyFormatter,
          formatter_exports.decimalPointFormatter
        ],
        classNames: {
          root: M(
            "orderly-order-entry oui-relative oui-h-[54px] oui-rounded oui-border oui-border-solid oui-border-line oui-px-2 oui-py-1 group-first:oui-rounded-t-xl group-last:oui-rounded-b-xl",
            props.className,
            props.classNames?.root
          ),
          input: M("oui-mb-1 oui-mt-5 oui-h-5", props?.classNames?.input),
          // prefix: cn(props.classNames?.prefix),
          suffix: M(
            "oui-absolute oui-right-0 oui-top-0 oui-justify-start oui-py-2 oui-text-2xs oui-text-base-contrast-36",
            props.classNames?.suffix
          )
        },
        readOnly: props.readonly
      }
    );
  }
);
CustomInput.displayName = "CustomInput";
var InputLabel = (props) => {
  return (0, import_jsx_runtime.jsx)(
    "label",
    {
      htmlFor: props.id,
      className: M(
        "oui-absolute oui-left-2 oui-top-[7px] oui-text-2xs oui-text-base-contrast-36",
        props.className
      ),
      children: props.children
    }
  );
};
var BBOOrderTypeSelect = (props) => {
  const { t } = useTranslation();
  const options2 = (0, import_react.useMemo)(
    () => [
      {
        label: t("orderEntry.bbo.counterparty1"),
        value: BBOOrderType.COUNTERPARTY1
      },
      {
        label: t("orderEntry.bbo.counterparty5"),
        value: BBOOrderType.COUNTERPARTY5
      },
      {
        label: t("orderEntry.bbo.queue1"),
        value: BBOOrderType.QUEUE1
      },
      {
        label: t("orderEntry.bbo.queue5"),
        value: BBOOrderType.QUEUE5
      }
    ],
    []
  );
  return (0, import_jsx_runtime.jsx)(
    Select2.options,
    {
      testid: "oui-testid-orderEntry-bbo-orderType-button",
      currentValue: props.value,
      value: props.value,
      options: options2,
      onValueChange: props.onChange,
      contentProps: {
        className: "oui-bg-base-8 oui-w-full",
        style: props.contentStyle
      },
      size: "sm",
      classNames: {
        trigger: "oui-border-none oui-bg-transparent"
      },
      valueFormatter: (value, option) => {
        const item = options2.find((item2) => item2.value === value);
        return (0, import_jsx_runtime.jsx)(Box, { children: (0, import_jsx_runtime.jsx)(Text2, { size: "sm", children: item?.label }) });
      }
    }
  );
};
var LimitPriceSuffix = (props) => {
  const { quote, bbo, fillMiddleValue } = props;
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(
    Flex,
    {
      direction: "column",
      itemAlign: "end",
      className: M("oui-order-entry-limit-price-input-suffix", "oui-text-2xs"),
      children: [
        quote,
        (0, import_jsx_runtime.jsxs)(Flex, { justify: "end", itemAlign: "center", gap: 2, children: [
          (0, import_jsx_runtime.jsx)(
            Flex,
            {
              px: 3,
              height: 20,
              justify: "center",
              itemAlign: "center",
              r: "base",
              className: M(
                "oui-mt-[2px] oui-cursor-pointer oui-select-none oui-border",
                bbo.bboStatus === "on" ? "oui-border-primary" : "oui-border-line-12",
                bbo.bboStatus === "disabled" && "oui-cursor-not-allowed"
              ),
              onClick: () => {
                if (bbo.bboStatus === "disabled") {
                  modal.dialog({
                    title: t("common.tips"),
                    size: "xs",
                    content: (0, import_jsx_runtime.jsx)(Text2, { intensity: 54, children: t("orderEntry.bbo.disabled.tips") })
                  });
                } else {
                  bbo.toggleBBO();
                }
              },
              children: (0, import_jsx_runtime.jsx)(
                Text2,
                {
                  className: M(
                    bbo.bboStatus === "on" && "oui-text-primary",
                    bbo.bboStatus === "off" && "oui-text-base-contrast-54",
                    bbo.bboStatus === "disabled" && "oui-text-base-contrast-20"
                  ),
                  children: t("orderEntry.bbo")
                }
              )
            }
          ),
          (0, import_jsx_runtime.jsx)(
            Text2,
            {
              className: M(
                "oui-select-none",
                "oui-cursor-pointer oui-text-primary"
              ),
              onClick: fillMiddleValue,
              children: "Mid"
            }
          )
        ] })
      ]
    }
  );
};
var PriceInput = (props) => {
  const { bbo } = props;
  const { t } = useTranslation();
  const {
    symbolInfo,
    onFocus,
    onBlur,
    getErrorMsg,
    setOrderValue,
    priceInputRef,
    priceInputContainerRef
  } = useOrderEntryContext();
  const { quote, quote_dp } = symbolInfo;
  const readOnly = bbo.bboStatus === "on";
  const suffix = props.order_type === OrderType.LIMIT ? (0, import_jsx_runtime.jsx)(
    LimitPriceSuffix,
    {
      quote,
      bbo,
      fillMiddleValue: props.fillMiddleValue
    }
  ) : quote;
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      ref: priceInputContainerRef,
      className: "oui-group oui-relative oui-w-full",
      children: [
        (0, import_jsx_runtime.jsx)(
          CustomInput,
          {
            id: "order_price_input",
            name: "order_price_input",
            label: t("common.price"),
            suffix,
            value: props.order_price,
            onChange: (e) => {
              setOrderValue("order_price", e);
            },
            error: getErrorMsg("order_price"),
            formatters: [formatter_exports.dpFormatter(quote_dp)],
            onFocus: onFocus(
              1
              /* PRICE */
            ),
            onBlur: onBlur(
              1
              /* PRICE */
            ),
            readonly: readOnly,
            ref: priceInputRef,
            classNames: {
              root: M(readOnly && "focus-within:oui-outline-transparent "),
              input: M(readOnly && "oui-cursor-auto")
            }
          }
        ),
        bbo.bboStatus === "on" && (0, import_jsx_runtime.jsx)("div", { className: M("oui-absolute oui-bottom-1 oui-left-0"), children: (0, import_jsx_runtime.jsx)(
          BBOOrderTypeSelect,
          {
            value: bbo.bboType,
            onChange: bbo.onBBOChange,
            contentStyle: {
              width: props.priceInputContainerWidth
            }
          }
        ) })
      ]
    }
  );
};
var QuantityInput = (0, import_react.memo)((props) => {
  const { t } = useTranslation();
  const { symbolInfo, onFocus, onBlur, getErrorMsg, setOrderValue } = useOrderEntryContext();
  const { base, base_dp } = symbolInfo;
  return (0, import_jsx_runtime.jsx)(
    CustomInput,
    {
      id: "order_quantity_input",
      name: "order_quantity_input",
      label: t("common.qty"),
      suffix: base,
      error: getErrorMsg("order_quantity"),
      value: props.order_quantity,
      onChange: (e) => {
        setOrderValue("order_quantity", e);
      },
      formatters: [formatter_exports.dpFormatter(base_dp)],
      onFocus: onFocus(
        3
        /* QUANTITY */
      ),
      onBlur: onBlur(
        3
        /* QUANTITY */
      ),
      className: "!oui-rounded-r",
      classNames: {
        suffix: "oui-justify-end"
      }
    }
  );
});
QuantityInput.displayName = "QuantityInput";
var TotalTypeSelect = (0, import_react.memo)((props) => {
  const { t } = useTranslation();
  const options2 = (0, import_react.useMemo)(() => {
    return [
      {
        label: t("orderEntry.orderSize"),
        value: "orderSize"
        /* OrderSize */
      },
      {
        label: t("orderEntry.initialMargin"),
        value: "initialMargin"
        /* InitialMargin */
      }
    ];
  }, [t]);
  return (0, import_jsx_runtime.jsx)(
    Select2.options,
    {
      size: "xs",
      value: props.value,
      valueFormatter: (value, option) => {
        const item = options2.find((o) => o.value === value);
        return item?.label + "≈";
      },
      classNames: {
        trigger: M(
          "oui-w-auto oui-border-none oui-bg-transparent",
          "oui-absolute oui-left-0 oui-top-[5px] oui-text-2xs oui-text-base-contrast-36"
        )
      },
      onValueChange: props.onChange,
      options: options2
    }
  );
});
TotalTypeSelect.displayName = "TotalTypeSelect";
var TotalInput = (0, import_react.memo)((props) => {
  const { t } = useTranslation();
  const { total } = props;
  const [margin, setMargin] = (0, import_react.useState)("");
  const {
    symbolInfo,
    onFocus,
    onBlur,
    getErrorMsg,
    setOrderValue,
    leverage = 1,
    currentFocusInput
  } = useOrderEntryContext();
  const { quote } = symbolInfo;
  const [totalType, setTotalType] = useLocalStorage(
    "orderly_order_total_type",
    "orderSize"
    /* OrderSize */
  );
  (0, import_react.useEffect)(() => {
    if (total) {
      if (currentFocusInput !== 6) {
        const margin2 = new decimal_default(total).div(leverage).todp(2).toString();
        setMargin(margin2);
      }
    } else {
      setMargin("");
    }
  }, [total, leverage, currentFocusInput]);
  const onMarginChange = (val) => {
    const total2 = val ? new decimal_default(val).mul(leverage).toString() : "";
    setOrderValue("total", total2);
    setMargin(val);
  };
  const prefix = (0, import_jsx_runtime.jsx)(TotalTypeSelect, { value: totalType, onChange: setTotalType });
  if (totalType === "initialMargin") {
    return (0, import_jsx_runtime.jsx)(
      CustomInput,
      {
        id: "order_margin_input",
        name: "order_margin_input",
        label: `${t("orderEntry.initialMargin")}≈`,
        prefix,
        suffix: quote,
        error: getErrorMsg("total") ? "initial margin error" : "",
        value: margin,
        onChange: onMarginChange,
        formatters: [formatter_exports.dpFormatter(2)],
        onFocus: onFocus(
          6
          /* MARGIN */
        ),
        onBlur: onBlur(
          6
          /* MARGIN */
        ),
        className: "!oui-rounded-l",
        classNames: {
          suffix: "oui-justify-end"
        }
      }
    );
  }
  return (0, import_jsx_runtime.jsx)(
    CustomInput,
    {
      id: "order_total_input",
      name: "order_total_input",
      label: `${t("orderEntry.orderSize")}≈`,
      prefix,
      suffix: quote,
      error: getErrorMsg("total"),
      value: props.total,
      onChange: (val) => {
        setOrderValue("total", val);
      },
      className: "!oui-rounded-l",
      classNames: {
        suffix: "oui-justify-end"
      },
      formatters: [formatter_exports.dpFormatter(2)],
      onFocus: onFocus(
        5
        /* TOTAL */
      ),
      onBlur: onBlur(
        5
        /* TOTAL */
      )
    }
  );
});
TotalInput.displayName = "TotalInput";
var QtyAndTotalInput = (0, import_react.memo)((props) => {
  return (0, import_jsx_runtime.jsxs)(Grid2, { cols: 2, className: "oui-group oui-space-x-1", children: [
    (0, import_jsx_runtime.jsx)(QuantityInput, { order_quantity: props.order_quantity }),
    (0, import_jsx_runtime.jsx)(TotalInput, { total: props.total })
  ] });
});
QtyAndTotalInput.displayName = "QtyAndTotalInput";
var QuantityDistributionInput = (0, import_react.memo)((props) => {
  const { t } = useTranslation();
  const { setOrderValue } = useOrderEntryContext();
  const showHint = () => {
    modal.dialog({
      title: t("common.tips"),
      size: "sm",
      content: (0, import_jsx_runtime.jsx)(QuantityDistributionHint, { value: props.distribution_type })
      // classNames: {
      //   content: "oui-bg-base-6",
      // },
    });
  };
  return (0, import_jsx_runtime.jsxs)(
    Flex,
    {
      direction: "column",
      itemAlign: "start",
      justify: "center",
      p: 2,
      r: "base",
      width: "100%",
      intensity: 600,
      className: M(
        "oui-t-rounded oui-text-base-contrast-36",
        "oui-border oui-border-solid oui-border-line",
        props.className
      ),
      children: [
        (0, import_jsx_runtime.jsx)(
          Text2,
          {
            size: "2xs",
            className: "oui-cursor-pointer oui-border-b oui-border-dashed oui-border-line-12",
            onClick: showHint,
            children: t("orderEntry.quantityDistribution")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          QuantityDistribution,
          {
            value: props.distribution_type,
            onValueChange: (value) => {
              setOrderValue("distribution_type", value);
            }
          }
        )
      ]
    }
  );
});
var QuantityDistributionHint = (props) => {
  const { t } = useTranslation();
  const [type, setType] = (0, import_react.useState)(DistributionType.FLAT);
  (0, import_react.useEffect)(() => {
    setType(
      [
        DistributionType.FLAT,
        DistributionType.ASCENDING,
        DistributionType.DESCENDING
      ].includes(props.value) ? props.value : DistributionType.FLAT
    );
  }, [props.value]);
  const content = (0, import_react.useMemo)(() => {
    return [
      {
        type: DistributionType.FLAT,
        title: t("orderEntry.distributionType.flat"),
        description: t("orderEntry.distributionType.flat.description"),
        formula: `${t("orderEntry.skew")} = 1`,
        quantity: (0, import_jsx_runtime.jsx)(FlatQuantity, {})
      },
      {
        type: DistributionType.ASCENDING,
        title: t("orderEntry.distributionType.ascending"),
        description: t("orderEntry.distributionType.ascending.description"),
        formula: `${t("orderEntry.skew")} > 1`,
        quantity: (0, import_jsx_runtime.jsx)(AscendingQuantity, {})
      },
      {
        type: DistributionType.DESCENDING,
        title: t("orderEntry.distributionType.descending"),
        description: t("orderEntry.distributionType.descending.description"),
        formula: `0 < ${t("orderEntry.skew")} < 1`,
        quantity: (0, import_jsx_runtime.jsx)(DescendingQuantity, {})
      }
    ];
  }, []);
  const currentContent = (0, import_react.useMemo)(() => {
    return content.find((item) => item.type === type);
  }, [content, type]);
  return (0, import_jsx_runtime.jsxs)("div", { className: "oui-text-2xs oui-font-semibold oui-text-base-contrast-54", children: [
    (0, import_jsx_runtime.jsxs)(Flex, { direction: "column", itemAlign: "start", gapY: 1, children: [
      (0, import_jsx_runtime.jsx)(Text2, { intensity: 80, children: t("orderEntry.quantityDistribution.description") }),
      (0, import_jsx_runtime.jsx)(Text2, { children: t("orderEntry.quantityDistribution.formula") })
    ] }),
    (0, import_jsx_runtime.jsxs)(Flex, { intensity: 600, p: 1, r: "base", mt: 3, itemAlign: "start", children: [
      (0, import_jsx_runtime.jsx)(Flex, { direction: "column", itemAlign: "start", children: content.map((item) => (0, import_jsx_runtime.jsx)(
        Box,
        {
          intensity: type === item.type ? 500 : 600,
          width: 78,
          p: 2,
          className: "oui-cursor-pointer oui-rounded-l",
          onClick: () => {
            setType(item.type);
          },
          children: (0, import_jsx_runtime.jsx)(Text2, { children: item.title })
        },
        item.title
      )) }),
      (0, import_jsx_runtime.jsxs)(
        Flex,
        {
          direction: "column",
          itemAlign: "start",
          gapY: 1,
          p: 2,
          intensity: 500,
          width: "100%",
          className: M(
            "oui-rounded-r",
            type === DistributionType.FLAT && "oui-rounded-bl",
            type === DistributionType.ASCENDING && "oui-rounded-l",
            type === DistributionType.DESCENDING && "oui-rounded-tl"
          ),
          children: [
            (0, import_jsx_runtime.jsx)("div", { children: currentContent?.formula }),
            (0, import_jsx_runtime.jsx)(Text2, { children: currentContent?.description }),
            (0, import_jsx_runtime.jsx)(Flex, { width: "100%", justify: "center", children: currentContent?.quantity }),
            (0, import_jsx_runtime.jsxs)(Flex, { width: "100%", gapX: 1, justify: "center", children: [
              (0, import_jsx_runtime.jsx)(Text2, { className: "oui-text-base-1", children: t("common.price") }),
              (0, import_jsx_runtime.jsx)(PriceChart, {})
            ] })
          ]
        }
      )
    ] })
  ] });
};
var QuantityDistribution = (props) => {
  const { value, onValueChange } = props;
  const { t } = useTranslation();
  const onChange = (value2) => (checked) => {
    onValueChange(value2);
  };
  const distributionTypeMap = (0, import_react.useMemo)(() => {
    return {
      [DistributionType.FLAT]: t("orderEntry.distributionType.flat"),
      [DistributionType.ASCENDING]: t(
        "orderEntry.distributionType.ascending.abbr"
      ),
      [DistributionType.DESCENDING]: t(
        "orderEntry.distributionType.descending.abbr"
      ),
      [DistributionType.CUSTOM]: t("orderEntry.distributionType.custom")
    };
  }, [t]);
  return (0, import_jsx_runtime.jsx)(Flex, { className: M("oui-gap-x-[6px] lg:oui-gap-x-2"), wrap: "wrap", children: Object.values(DistributionType).map((type) => {
    return (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", children: [
      (0, import_jsx_runtime.jsx)(
        Checkbox,
        {
          id: `distribution-type-${type}`,
          color: "white",
          variant: "radio",
          checked: value === type,
          onCheckedChange: onChange(type)
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "label",
        {
          htmlFor: `distribution-type-${type}`,
          className: M(
            "oui-text-2xs",
            "oui-ml-1",
            "oui-whitespace-nowrap oui-break-normal"
          ),
          children: distributionTypeMap[type]
        }
      )
    ] }, type);
  }) });
};
var FlatQuantity = () => {
  return (0, import_jsx_runtime.jsxs)(
    "svg",
    {
      width: "218",
      height: "40",
      viewBox: "0 0 218 40",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      children: [
        (0, import_jsx_runtime.jsx)(
          "rect",
          {
            x: "18",
            y: "18",
            width: "181",
            height: "4",
            fill: "rgb(var(--oui-color-base-2))"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "15",
            cy: "20",
            r: "6",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "2"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "62",
            cy: "20",
            r: "6",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "2"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "109",
            cy: "20",
            r: "6",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "2"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "156",
            cy: "20",
            r: "6",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "2"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "203",
            cy: "20",
            r: "6",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "2"
          }
        )
      ]
    }
  );
};
var AscendingQuantity = () => {
  return (0, import_jsx_runtime.jsxs)(
    "svg",
    {
      width: "218",
      height: "40",
      viewBox: "0 0 218 40",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      children: [
        (0, import_jsx_runtime.jsx)(
          "rect",
          {
            x: "18",
            y: "18",
            width: "181",
            height: "4",
            fill: "rgb(var(--oui-color-base-2))"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "15",
            cy: "20",
            r: "4.28571",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "1.42857"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "62",
            cy: "20",
            r: "5.14286",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "1.71429"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "109",
            cy: "20",
            r: "6",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "2"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "156",
            cy: "20",
            r: "6.85714",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "2.28571"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "203",
            cy: "20",
            r: "7.71429",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "2.57143"
          }
        )
      ]
    }
  );
};
var DescendingQuantity = () => {
  return (0, import_jsx_runtime.jsxs)(
    "svg",
    {
      width: "218",
      height: "40",
      viewBox: "0 0 218 40",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      children: [
        (0, import_jsx_runtime.jsx)(
          "rect",
          {
            width: "181",
            height: "4",
            transform: "matrix(-1 0 0 1 200 18)",
            fill: "rgb(var(--oui-color-base-2))"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "5",
            cy: "5",
            r: "4.28571",
            transform: "matrix(-1 0 0 1 208 15)",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "1.42857"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "6",
            cy: "6",
            r: "5.14286",
            transform: "matrix(-1 0 0 1 162 14)",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "1.71429"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "7",
            cy: "7",
            r: "6",
            transform: "matrix(-1 0 0 1 116 13)",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "2"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "8",
            cy: "8",
            r: "6.85714",
            transform: "matrix(-1 0 0 1 70 12)",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "2.28571"
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "circle",
          {
            cx: "9",
            cy: "9",
            r: "7.71429",
            transform: "matrix(-1 0 0 1 24 11)",
            fill: "rgb(var(--oui-color-base-2))",
            stroke: "rgb(var(--oui-color-base-5))",
            strokeWidth: "2.57143"
          }
        )
      ]
    }
  );
};
var PriceChart = () => {
  return (0, import_jsx_runtime.jsx)(
    "svg",
    {
      width: "176",
      height: "8",
      viewBox: "0 0 176 8",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      children: (0, import_jsx_runtime.jsx)(
        "path",
        {
          d: "M175.354 4.35355C175.549 4.15829 175.549 3.84171 175.354 3.64645L172.172 0.464466C171.976 0.269204 171.66 0.269204 171.464 0.464466C171.269 0.659728 171.269 0.976311 171.464 1.17157L174.293 4L171.464 6.82843C171.269 7.02369 171.269 7.34027 171.464 7.53553C171.66 7.7308 171.976 7.7308 172.172 7.53553L175.354 4.35355ZM0 4V4.5H175V4V3.5H0V4Z",
          fill: "rgb(var(--oui-color-base-1))"
        }
      )
    }
  );
};
var ScaledPriceInput = (0, import_react.memo)((props) => {
  const { t } = useTranslation();
  const { symbolInfo, onFocus, onBlur, getErrorMsg, setOrderValue } = useOrderEntryContext();
  const { quote, quote_dp } = symbolInfo;
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)(
      CustomInput,
      {
        label: t("orderEntry.startPrice"),
        suffix: quote,
        id: "order_start_price_input",
        value: props.start_price,
        error: getErrorMsg("start_price"),
        onChange: (e) => {
          setOrderValue("start_price", e);
        },
        formatters: [formatter_exports.dpFormatter(quote_dp)],
        onFocus: onFocus(
          7
          /* START_PRICE */
        ),
        onBlur: onBlur(
          7
          /* START_PRICE */
        ),
        classNames: {
          root: "oui-rounded-t-xl"
        }
      }
    ),
    (0, import_jsx_runtime.jsx)(
      CustomInput,
      {
        label: t("orderEntry.endPrice"),
        suffix: quote,
        id: "order_end_price_input",
        value: props.end_price,
        error: getErrorMsg("end_price"),
        onChange: (val) => {
          setOrderValue("end_price", val);
        },
        formatters: [formatter_exports.dpFormatter(quote_dp)],
        onFocus: onFocus(
          8
          /* END_PRICE */
        ),
        onBlur: onBlur(
          8
          /* END_PRICE */
        )
      }
    )
  ] });
});
ScaledPriceInput.displayName = "ScaledPriceInput";
var valueRenderer = (value) => {
  return (0, import_jsx_runtime.jsx)(Text2, { size: "2xs", intensity: 36, children: value });
};
var ScaledQuantityUnit = (props) => {
  const { base, quote } = props;
  const options2 = (0, import_react.useMemo)(() => {
    return [{ name: quote }, { name: base }];
  }, [base, quote]);
  return (0, import_jsx_runtime.jsx)(
    Select2.tokens,
    {
      variant: "text",
      size: "xs",
      iconSize: "2xs",
      tokens: options2,
      classNames: {
        trigger: M(
          "oui-absolute oui-right-0 oui-top-1",
          "oui-w-full oui-justify-end"
        )
      },
      value: props.value,
      onValueChange: props.onValueChange,
      valueFormatter: valueRenderer,
      contentProps: {
        align: "end",
        alignOffset: -1,
        sideOffset: -4,
        className: "oui-border oui-border-line-6"
      }
    }
  );
};
var ScaledQuantityInput = (0, import_react.memo)((props) => {
  const { t } = useTranslation();
  const { errors, symbolInfo, onFocus, onBlur, getErrorMsg, setOrderValue } = useOrderEntryContext();
  const [quantityUnit, setQuantityUnit] = useLocalStorage(
    "orderly_order_quantity_unit",
    "quote"
  );
  const { base, quote, base_dp, quote_dp } = symbolInfo;
  const isBase = quantityUnit === "base";
  const unit = isBase ? base : quote;
  const suffix = (0, import_jsx_runtime.jsx)(
    ScaledQuantityUnit,
    {
      base,
      quote,
      value: unit,
      onValueChange: (value) => {
        setQuantityUnit(value === base ? "base" : "quote");
      }
    }
  );
  if (isBase) {
    return (0, import_jsx_runtime.jsx)(
      CustomInput,
      {
        label: t("common.qty"),
        suffix,
        id: "order_quantity_input",
        name: "order_quantity_input",
        className: "!oui-rounded-r",
        value: props.order_quantity,
        error: getErrorMsg(
          "order_quantity",
          `${errors?.order_quantity?.value} ${base}`
        ),
        onChange: (val) => {
          setOrderValue("order_quantity", val);
        },
        formatters: [formatter_exports.dpFormatter(base_dp)],
        onFocus: onFocus(
          3
          /* QUANTITY */
        ),
        onBlur: onBlur(
          3
          /* QUANTITY */
        )
      }
    );
  }
  return (0, import_jsx_runtime.jsx)(
    CustomInput,
    {
      label: t("common.qty"),
      suffix,
      id: "order_total_input",
      name: "order_total_input",
      className: "!oui-rounded-r",
      value: props.total,
      error: getErrorMsg("order_quantity", `${errors?.total?.value} ${quote}`),
      onChange: (val) => {
        setOrderValue("total", val);
      },
      formatters: [formatter_exports.dpFormatter(quote_dp)],
      onFocus: onFocus(
        5
        /* TOTAL */
      ),
      onBlur: onBlur(
        5
        /* TOTAL */
      )
    }
  );
});
var SkewInput = (0, import_react.memo)((props) => {
  const { t } = useTranslation();
  const { onFocus, onBlur, getErrorMsg, setOrderValue } = useOrderEntryContext();
  return (0, import_jsx_runtime.jsx)(
    CustomInput,
    {
      id: "order_skew_input",
      label: t("orderEntry.skew"),
      value: props.skew,
      error: getErrorMsg("skew"),
      onChange: (val) => {
        setOrderValue("skew", val);
      },
      onFocus: onFocus(
        10
        /* SKEW */
      ),
      onBlur: onBlur(
        10
        /* SKEW */
      ),
      overrideFormatters: [
        formatter_exports.rangeFormatter({ min: 0, max: 100, dp: 2 }),
        formatter_exports.dpFormatter(2)
      ],
      classNames: {
        root: "oui-rounded-b-xl"
      }
    }
  );
});
var TotalOrdersInput = (0, import_react.memo)((props) => {
  const { t } = useTranslation();
  const { onFocus, onBlur, getErrorMsg, setOrderValue } = useOrderEntryContext();
  return (0, import_jsx_runtime.jsx)(
    CustomInput,
    {
      label: t("orderEntry.totalOrders"),
      placeholder: "2-20",
      id: "order_total_orders_input",
      className: "!oui-rounded-l",
      value: props.total_orders,
      error: getErrorMsg("total_orders"),
      onChange: (val) => {
        setOrderValue("total_orders", val);
      },
      overrideFormatters: [
        // inputFormatter.rangeFormatter({ min: 2, max: 20 }),
        formatter_exports.numberFormatter,
        formatter_exports.dpFormatter(0)
      ],
      onFocus: onFocus(
        9
        /* TOTAL_ORDERS */
      ),
      onBlur: onBlur(
        9
        /* TOTAL_ORDERS */
      )
    }
  );
});
var ScaledOrderInput = (props) => {
  const { values } = props;
  const showSkewInput = values.distribution_type === DistributionType.CUSTOM;
  return (0, import_jsx_runtime.jsxs)("div", { className: "oui-space-y-1", children: [
    (0, import_jsx_runtime.jsx)(
      ScaledPriceInput,
      {
        start_price: values.start_price,
        end_price: values.end_price
      }
    ),
    (0, import_jsx_runtime.jsxs)(Grid2, { cols: 2, className: "oui-group oui-space-x-1", children: [
      (0, import_jsx_runtime.jsx)(
        ScaledQuantityInput,
        {
          order_quantity: values.order_quantity,
          total: values.total
        }
      ),
      (0, import_jsx_runtime.jsx)(TotalOrdersInput, { total_orders: values.total_orders })
    ] }),
    (0, import_jsx_runtime.jsx)(
      QuantityDistributionInput,
      {
        distribution_type: values.distribution_type,
        className: M(!showSkewInput && "oui-rounded-b-xl")
      }
    ),
    showSkewInput && (0, import_jsx_runtime.jsx)(SkewInput, { skew: values.skew })
  ] });
};
var TriggerPriceInput = (0, import_react.memo)((props) => {
  const { t } = useTranslation();
  const {
    symbolInfo,
    onFocus,
    onBlur,
    getErrorMsg,
    setOrderValue,
    triggerPriceInputRef
  } = useOrderEntryContext();
  return (0, import_jsx_runtime.jsx)("div", { className: "oui-group", children: (0, import_jsx_runtime.jsx)(
    CustomInput,
    {
      ref: triggerPriceInputRef,
      id: "order_trigger_price_input",
      name: "order_trigger_price_input",
      label: t("common.trigger"),
      suffix: symbolInfo.quote,
      value: props.trigger_price,
      onChange: (e) => {
        setOrderValue("trigger_price", e);
      },
      error: getErrorMsg("trigger_price"),
      formatters: [formatter_exports.dpFormatter(symbolInfo.quote_dp)],
      onFocus: onFocus(
        2
        /* TRIGGER_PRICE */
      ),
      onBlur: onBlur(
        2
        /* TRIGGER_PRICE */
      )
    }
  ) });
});
TriggerPriceInput.displayName = "TriggerPriceInput";
var ActivePriceInput = (0, import_react.memo)((props) => {
  const { t } = useTranslation();
  const {
    symbolInfo,
    onFocus,
    onBlur,
    getErrorMsg,
    setOrderValue,
    activatedPriceInputRef
  } = useOrderEntryContext();
  return (0, import_jsx_runtime.jsx)("div", { className: "oui-group", children: (0, import_jsx_runtime.jsx)(
    CustomInput,
    {
      ref: activatedPriceInputRef,
      id: "order_activated_price_input",
      name: "order_activated_price_input",
      label: t("common.triggerPrice"),
      placeholder: `0 (${t("common.optional")})`,
      suffix: symbolInfo.quote,
      error: getErrorMsg("activated_price"),
      value: props.activated_price,
      onChange: (val) => {
        setOrderValue("activated_price", val);
      },
      formatters: [formatter_exports.dpFormatter(symbolInfo.quote_dp)],
      onFocus: onFocus(
        11
        /* ACTIVATED_PRICE */
      ),
      onBlur: onBlur(
        11
        /* ACTIVATED_PRICE */
      )
    }
  ) });
});
ActivePriceInput.displayName = "ActivePriceInput";
var percentages = [1, 2, 3, 5];
var CallbackRatePercentages = (0, import_react.memo)(
  (props) => {
    const { setOrderValue } = useOrderEntryContext();
    return (0, import_jsx_runtime.jsx)(Flex, { gapX: 2, className: props.className, children: percentages.map((item) => {
      const value = item.toString();
      const isActive = props.callback_rate === value;
      return (0, import_jsx_runtime.jsx)(
        Flex,
        {
          justify: "center",
          itemAlign: "center",
          width: 46,
          height: 18,
          className: M(
            "oui-cursor-pointer oui-select-none",
            "oui-rounded oui-border",
            isActive ? "oui-border-base-contrast-36" : "oui-border-line-12"
          ),
          onClick: () => {
            setOrderValue("callback_rate", value);
            props.onClick?.(value);
          },
          children: (0, import_jsx_runtime.jsxs)(Text2, { size: "2xs", intensity: isActive ? 80 : 36, weight: "semibold", children: [
            item,
            "%"
          ] })
        },
        item
      );
    }) });
  }
);
CallbackRatePercentages.displayName = "CallbackRatePercentages";
var TrailingCallbackSelect = (0, import_react.memo)(
  (props) => {
    const { quote } = props;
    const options2 = (0, import_react.useMemo)(() => {
      return [
        { label: quote, value: TrailingCallbackType.VALUE },
        { label: "%", value: TrailingCallbackType.RATE }
      ];
    }, [quote]);
    return (0, import_jsx_runtime.jsx)(
      Select2.options,
      {
        variant: "text",
        size: "xs",
        options: options2,
        classNames: {
          trigger: M(
            "oui-absolute oui-right-0 oui-top-1",
            "oui-w-[124px] oui-justify-end",
            "oui-text-base-contrast-36"
          )
        },
        value: props.value,
        onValueChange: props.onValueChange,
        contentProps: {
          align: "end",
          alignOffset: -1,
          sideOffset: -4,
          className: "oui-border oui-border-line-6"
        }
      }
    );
  }
);
TrailingCallbackSelect.displayName = "trailingCallbackSelect";
var TrailingCallbackInput = (0, import_react.memo)(
  (props) => {
    const { callback_value, callback_rate } = props;
    const { t } = useTranslation();
    const {
      symbolInfo,
      onFocus,
      onBlur,
      getErrorMsg,
      setOrderValue,
      setOrderValues
    } = useOrderEntryContext();
    const { quote, quote_dp } = symbolInfo;
    const lastCallbackValueRef = (0, import_react.useRef)();
    const lastCallbackRateRef = (0, import_react.useRef)();
    const [callbackType, setCallbackType] = useLocalStorage(
      "orderly_order_trailing_callback_type",
      TrailingCallbackType.VALUE
    );
    const onCallbackTypeChange = (0, import_react.useCallback)(
      (type) => {
        setCallbackType(type);
        if (type === TrailingCallbackType.RATE) {
          lastCallbackValueRef.current = callback_value;
          setOrderValues({
            callback_value: "",
            callback_rate: lastCallbackRateRef.current
          });
        } else {
          lastCallbackRateRef.current = callback_rate;
          setOrderValues({
            callback_value: lastCallbackValueRef.current,
            callback_rate: ""
          });
        }
      },
      [callback_value, callback_rate]
    );
    const suffix = (0, import_jsx_runtime.jsx)(
      TrailingCallbackSelect,
      {
        quote,
        value: callbackType,
        onValueChange: onCallbackTypeChange
      }
    );
    if (callbackType === TrailingCallbackType.RATE) {
      return (
        // <Tooltip
        //   content={tooltipContent}
        //   open={open && !!tooltipContent}
        //   sideOffset={-25}
        //   className="!oui-p-3"
        // >
        (0, import_jsx_runtime.jsxs)("div", { className: "oui-relative", children: [
          (0, import_jsx_runtime.jsx)(
            CustomInput,
            {
              id: "order_callback_rate_input",
              name: "order_callback_rate_input",
              label: t("orderEntry.trailingRate"),
              suffix,
              error: getErrorMsg("callback_rate"),
              value: callback_rate,
              onChange: (val) => {
                setOrderValue("callback_rate", val);
              },
              formatters: [formatter_exports.dpFormatter(1)],
              onFocus: (e) => {
                onFocus(
                  13
                  /* CALLBACK_RATE */
                )(e);
              },
              onBlur: (e) => {
                onBlur(
                  13
                  /* CALLBACK_RATE */
                )(e);
              },
              classNames: {
                root: "oui-h-[68px]",
                input: "oui-mb-5",
                prefix: "!oui-top-1"
              }
            }
          ),
          (0, import_jsx_runtime.jsx)(
            CallbackRatePercentages,
            {
              className: "oui-absolute oui-bottom-1 oui-left-2",
              callback_rate
            }
          )
        ] })
      );
    }
    return (0, import_jsx_runtime.jsx)(
      CustomInput,
      {
        id: "order_callback_value_input",
        name: "order_callback_value_input",
        label: t("orderEntry.trailingValue"),
        suffix,
        error: getErrorMsg("callback_value"),
        value: callback_value,
        onChange: (val) => {
          setOrderValue("callback_value", val);
        },
        formatters: [formatter_exports.dpFormatter(quote_dp)],
        onFocus: onFocus(
          12
          /* CALLBACK_VALUE */
        ),
        onBlur: onBlur(
          12
          /* CALLBACK_VALUE */
        ),
        classNames: {
          input: "!oui-mb-[6px]",
          prefix: "!oui-top-1"
        }
      }
    );
  }
);
TrailingCallbackInput.displayName = "TrailingCallbackInput";
var TrailingStopInput = (props) => {
  const { values } = props;
  return (0, import_jsx_runtime.jsxs)("div", { className: "oui-space-y-1", children: [
    (0, import_jsx_runtime.jsx)(ActivePriceInput, { activated_price: values.activated_price }),
    (0, import_jsx_runtime.jsx)(
      TrailingCallbackInput,
      {
        callback_value: values.callback_value,
        callback_rate: values.callback_rate
      }
    ),
    (0, import_jsx_runtime.jsx)(
      QtyAndTotalInput,
      {
        order_quantity: values.order_quantity,
        total: values.total
      }
    )
  ] });
};
function OrderInput(props) {
  const { values } = props;
  const type = values.order_type;
  if (type === OrderType.SCALED) {
    return (0, import_jsx_runtime.jsx)(ScaledOrderInput, { values });
  }
  if (type === OrderType.TRAILING_STOP) {
    return (0, import_jsx_runtime.jsx)(TrailingStopInput, { values });
  }
  const showTriggerPrice = type === OrderType.STOP_LIMIT || type === OrderType.STOP_MARKET;
  const showPrice = type === OrderType.LIMIT || type === OrderType.STOP_LIMIT;
  const triggerPriceInput = showTriggerPrice && (0, import_jsx_runtime.jsx)(TriggerPriceInput, { trigger_price: values.trigger_price });
  const priceInput = showPrice && (0, import_jsx_runtime.jsx)(
    PriceInput,
    {
      order_type: values.order_type,
      order_price: values.order_price,
      bbo: props.bbo,
      fillMiddleValue: props.fillMiddleValue,
      priceInputContainerWidth: props.priceInputContainerWidth
    }
  );
  return (0, import_jsx_runtime.jsxs)("div", { className: "oui-space-y-1", children: [
    triggerPriceInput,
    priceInput,
    (0, import_jsx_runtime.jsx)(
      QtyAndTotalInput,
      {
        order_quantity: values.order_quantity,
        total: values.total
      }
    )
  ] });
}
var SLIDER_MIN = 0;
var SLIDER_MAX = 100;
var QuantitySlider = (0, import_react.memo)((props) => {
  const { canTrade, side, order_quantity, maxQty } = props;
  const [sliderValue, setSliderValue] = (0, import_react.useState)(0);
  const { setOrderValue, symbolInfo, lastQuantityInputType } = useOrderEntryContext();
  const { base_dp, base_tick } = symbolInfo;
  const { t } = useTranslation();
  const color = (0, import_react.useMemo)(
    () => canTrade ? side === OrderSide.BUY ? "buy" : "sell" : void 0,
    [side, canTrade]
  );
  const maxLabel = (0, import_react.useMemo)(() => {
    return side === OrderSide.BUY ? t("orderEntry.maxBuy") : t("orderEntry.maxSell");
  }, [side, t]);
  const onSliderValueChange = (value) => {
    lastQuantityInputType.current = 4;
    setSliderValue(value);
  };
  const sliderToQuantity = (value) => {
    const newQty = new decimal_default(value).div(SLIDER_MAX).mul(maxQty).toFixed(base_dp, decimal_default.ROUND_DOWN);
    setOrderValue("order_quantity", utils_exports.formatNumber(newQty, base_tick));
  };
  const onMax = () => {
    onSliderValueChange(SLIDER_MAX);
    if (sliderValue === SLIDER_MAX) {
      sliderToQuantity(SLIDER_MAX);
    }
  };
  (0, import_react.useEffect)(() => {
    if (lastQuantityInputType.current === 4) {
      sliderToQuantity(sliderValue);
    }
  }, [sliderValue, maxQty]);
  (0, import_react.useEffect)(() => {
    const quantityToSlider = () => {
      if (order_quantity && Number(order_quantity) !== 0 && maxQty !== 0) {
        return new decimal_default(Math.min(Number(order_quantity), maxQty)).div(maxQty).mul(SLIDER_MAX).todp(2, decimal_default.ROUND_DOWN).toNumber();
      }
      return 0;
    };
    if (lastQuantityInputType.current !== 4) {
      setSliderValue(quantityToSlider());
    }
  }, [order_quantity, maxQty]);
  return (0, import_jsx_runtime.jsxs)("div", { children: [
    (0, import_jsx_runtime.jsx)(
      Slider,
      {
        disabled: maxQty === 0 || !canTrade,
        value: [sliderValue],
        color,
        markCount: 4,
        showTip: true,
        onValueChange: (e) => {
          onSliderValueChange(e[0]);
        },
        min: SLIDER_MIN,
        max: SLIDER_MAX
      }
    ),
    (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", className: "oui-pt-1 xl:oui-pt-2", children: [
      (0, import_jsx_runtime.jsx)(
        Text2.numeral,
        {
          size: "2xs",
          color,
          dp: 2,
          padding: false,
          suffix: "%",
          children: canTrade ? sliderValue : 0
        }
      ),
      (0, import_jsx_runtime.jsxs)(Flex, { children: [
        (0, import_jsx_runtime.jsx)(
          "button",
          {
            className: textVariants({
              size: "2xs",
              className: "oui-mr-1"
            }),
            onClick: onMax,
            "data-testid": "oui-testid-orderEntry-maxQty-value-button",
            children: maxLabel
          }
        ),
        (0, import_jsx_runtime.jsx)(
          Text2.numeral,
          {
            size: "2xs",
            color,
            dp: base_dp,
            padding: false,
            "data-testid": "oui-testid-orderEntry-maxQty-value",
            children: canTrade ? maxQty : 0
          }
        )
      ] })
    ] })
  ] });
});
QuantitySlider.displayName = "QuantitySlider";
var ReduceOnlySwitch = ({
  checked,
  onCheckedChange,
  className,
  testId = "oui-testid-orderEntry-reduceOnly-switch"
}) => {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", gapX: 1, className, children: [
    (0, import_jsx_runtime.jsx)(
      Switch,
      {
        "data-testid": testId,
        className: "oui-h-[14px]",
        id: "reduceOnly",
        checked,
        onCheckedChange
      }
    ),
    (0, import_jsx_runtime.jsx)("label", { htmlFor: "reduceOnly", className: "oui-text-xs", children: t("orderEntry.reduceOnly") })
  ] });
};
var PnlInputContext = (0, import_react.createContext)(
  {}
);
var usePnlInputContext = () => {
  return (0, import_react.useContext)(PnlInputContext);
};
var usePNLInputBuilder = (props) => {
  const { type, values, quote_dp } = props;
  const { t } = useTranslation();
  const [focus, setFocus] = (0, import_react.useState)(true);
  const { mode, setMode, tipsEle } = usePnlInputContext();
  const [tipVisible, setTipVisible] = (0, import_react.useState)(false);
  const [isFocused, setIsFocused] = (0, import_react.useState)(false);
  const key2 = (0, import_react.useMemo)(() => {
    switch (mode) {
      case "Offset":
        return `${type.toLowerCase()}_offset`;
      case "Offset%":
        return `${type.toLowerCase()}_offset_percentage`;
      default:
        return `${type.toLowerCase()}_pnl`;
    }
  }, [mode]);
  const [innerValue, setInnerValue] = (0, import_react.useState)(
    values[mode]
  );
  (0, import_react.useEffect)(() => {
    if (isFocused) {
      return;
    }
    setInnerValue(values[mode]);
  }, [values, mode, isFocused]);
  const modes = (0, import_react.useMemo)(() => {
    return [
      {
        label: t("tpsl.pnl"),
        value: "PnL",
        testId: `${"PnL"}_menu_item`
      },
      {
        label: t("tpsl.offset"),
        value: "Offset",
        testId: `${"Offset"}_mneu_item`
      },
      {
        label: `${t("tpsl.offset")}%`,
        value: "Offset%",
        testId: `${"Offset%"}_menu_item`
      }
    ];
  }, [t]);
  const modeLabelMap = (0, import_react.useMemo)(() => {
    return {
      [
        "PnL"
        /* PnL */
      ]: t("tpsl.pnl"),
      [
        "Offset"
        /* OFFSET */
      ]: t("tpsl.offset"),
      [
        "Offset%"
        /* PERCENTAGE */
      ]: `${t("tpsl.offset")}%`
    };
  }, [t]);
  const percentageSuffix = (0, import_react.useRef)("");
  const onValueChange = (value) => {
    setInnerValue(value);
    props.onChange(key2, value);
  };
  const onFocus = () => {
    setTipVisible(true);
    setIsFocused(true);
  };
  const onBlur = () => {
    setTipVisible(false);
    setIsFocused(false);
    props.onChange(key2, innerValue);
  };
  const formatter = (options2) => {
    const { dp = 2 } = options2;
    return {
      onRenderBefore: (value, options3) => {
        value = `${value}`;
        if (focus) {
          if (type === "SL" && mode === "PnL") {
            value = value.startsWith("-") ? value : "-" + value;
          }
        }
        if (value === "" || value === "-") return "";
        if (mode === "Offset%") {
          return `${new decimal_default(
            value.replace(
              new RegExp(percentageSuffix.current.replace(".", "\\.") + "$"),
              ""
            )
          ).mul(100).todp(2, 4).toString()}${percentageSuffix.current}`;
        } else if (mode === "Offset") {
          value = todpIfNeed(value, dp);
        } else ;
        return `${value}`;
      },
      onSendBefore: (value) => {
        if (/^\-?0{2,}$/.test(value)) {
          return "0";
        }
        if (mode === "Offset%") {
          if (value !== "") {
            value = todpIfNeed(value, 2);
            const endStr = value.match(/\.0{0,2}$/);
            if (!!endStr) {
              percentageSuffix.current = endStr[0];
            } else {
              percentageSuffix.current = "";
            }
            value = new decimal_default(value).div(100).toString();
            value = `${value}${percentageSuffix.current}`;
          }
        } else if (mode === "PnL" && type === "SL" && focus) {
          value = value.startsWith("-") ? value : "-" + value;
        } else {
          value = todpIfNeed(value, dp);
        }
        if (value === "" || value === "-") return "";
        return value;
      }
    };
  };
  return {
    mode,
    modes,
    modeLabelMap,
    formatter,
    onModeChange: (mode2) => {
      setMode(mode2);
    },
    onFocus,
    onBlur,
    value: innerValue,
    onValueChange,
    quote_dp,
    tips: tipVisible ? tipsEle : void 0,
    setFocus
  };
};
var PNLInput = (props) => {
  const {
    mode,
    modes,
    modeLabelMap,
    onModeChange,
    onValueChange,
    quote,
    quote_dp,
    value,
    type,
    tips,
    onFocus,
    onBlur,
    setFocus
  } = props;
  const [prefix, setPrefix] = (0, import_react.useState)(mode);
  const [placeholder, setPlaceholder] = (0, import_react.useState)(
    mode === "Offset%" ? "%" : quote
  );
  (0, import_react.useEffect)(() => {
    setPrefix(mode);
    setPlaceholder(mode === "Offset%" ? "%" : quote);
  }, [mode]);
  (0, import_react.useEffect)(() => {
    setPrefix(!!value ? "" : mode);
  }, [value]);
  const id = (0, import_react.useMemo)(() => `${type.toLowerCase()}_${mode.toLowerCase()}`, []);
  return (0, import_jsx_runtime.jsx)(
    Input2.tooltip,
    {
      prefix: modeLabelMap[prefix] || prefix,
      size: "md",
      placeholder,
      id,
      align: "right",
      value,
      tooltip: tips,
      tooltipProps: {
        content: {
          side: props.type === "TP" ? "top" : "bottom"
        }
      },
      "data-testid": props.testIds?.input,
      autoComplete: "off",
      onValueChange,
      formatters: [
        props.formatter({ dp: quote_dp, mode, type }),
        formatter_exports.currencyFormatter
        // inputFormatter.identifierFormatter(),
      ],
      classNames: {
        root: type === "TP" ? "oui-text-trade-profit" : "oui-text-trade-loss",
        additional: "oui-text-base-contrast-54",
        input: "oui-text-inherit"
      },
      onFocus: () => {
        setPrefix("");
        setPlaceholder("");
        setFocus(true);
        onFocus();
      },
      onBlur: () => {
        setPrefix(!!value ? "" : mode);
        setPlaceholder(mode === "Offset%" ? "%" : quote);
        onBlur();
      },
      suffix: (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
        mode === "Offset%" && !!value && (0, import_jsx_runtime.jsx)(Text2, { size: "2xs", color: "inherit", className: "oui-ml-[2px]", children: "%" }),
        (0, import_jsx_runtime.jsx)(
          PNLMenus,
          {
            mode,
            modes,
            onModeChange: (item) => onModeChange(item.value),
            testId: props.testIds?.dropDown
          }
        )
      ] })
    }
  );
};
var PNLMenus = (props) => {
  return (0, import_jsx_runtime.jsx)(
    SimpleDropdownMenu,
    {
      currentValue: props.mode,
      menu: props.modes,
      align: "end",
      size: "xs",
      className: "oui-min-w-[80px]",
      onCloseAutoFocus: (event) => event.preventDefault(),
      onSelect: (item) => props.onModeChange(item),
      children: (0, import_jsx_runtime.jsx)("button", { className: "oui-p-2", "data-testid": props.testId, children: (0, import_jsx_runtime.jsx)(CaretDownIcon, { size: 12, color: "white" }) })
    }
  );
};
var PnlInputWidget = (props) => {
  const { testIds, quote, ...rest } = props;
  const state = usePNLInputBuilder(rest);
  return (0, import_jsx_runtime.jsx)(PNLInput, { ...state, testIds, quote, type: props.type });
};
var PnlInputProvider = (props) => {
  const { type, values, children } = props;
  const [mode, setMode] = useLocalStorage(
    "TP/SL_Mode",
    "Offset%"
    /* PERCENTAGE */
  );
  const { t } = useTranslation();
  const tipsEle = (0, import_react.useMemo)(() => {
    if (!values.PnL || !values.trigger_price) {
      return null;
    }
    return (0, import_jsx_runtime.jsxs)(Flex, { children: [
      (0, import_jsx_runtime.jsx)("span", { className: "oui-text-xs oui-text-base-contrast-54", children: mode === "PnL" ? t("orderEntry.estRoi") : t("orderEntry.estPnL") }),
      mode === "PnL" ? (0, import_jsx_runtime.jsx)(
        Text2.numeral,
        {
          rule: "percentages",
          className: M(
            "oui-ml-1 oui-text-xs",
            type === "TP" ? "oui-text-trade-profit" : "oui-text-trade-loss"
          ),
          children: values.ROI
        }
      ) : (0, import_jsx_runtime.jsx)(
        Text2.numeral,
        {
          rule: "price",
          className: M(
            "oui-ml-1 oui-text-xs",
            type === "TP" ? "oui-text-trade-profit" : "oui-text-trade-loss"
          ),
          children: values.PnL
        }
      )
    ] });
  }, [mode, values.ROI, values.PnL, values.trigger_price]);
  const memoizedValue = (0, import_react.useMemo)(() => {
    return { mode, setMode, tipsEle };
  }, [mode, setMode, tipsEle]);
  return (0, import_jsx_runtime.jsx)(PnlInputContext.Provider, { value: memoizedValue, children });
};
var OrderTPSL = (props) => {
  const tpslFormRef = import_react.default.useRef(null);
  const { t } = useTranslation();
  const { isMobile } = useScreen();
  (0, import_react.useEffect)(() => {
    if (props.orderType !== OrderType.LIMIT && props.orderType !== OrderType.MARKET) {
      props.onSwitchChanged(false);
    }
  }, [props.orderType]);
  if (props.orderType !== OrderType.LIMIT && props.orderType !== OrderType.MARKET) {
    return null;
  }
  const isSlPriceWarning = props.errors?.["sl_trigger_price"]?.["type"] === ERROR_MSG_CODES.SL_PRICE_WARNING;
  return (0, import_jsx_runtime.jsxs)("div", { children: [
    (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", justify: "between", children: [
      (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", gapX: 1, children: [
        (0, import_jsx_runtime.jsx)(
          Switch,
          {
            id: "order_entry_tpsl",
            className: "oui-h-[14px]",
            checked: props.switchState,
            disabled: props.orderType !== OrderType.LIMIT && props.orderType !== OrderType.MARKET,
            onCheckedChange: (checked) => {
              props.onSwitchChanged(checked);
            }
          }
        ),
        (0, import_jsx_runtime.jsx)("label", { htmlFor: "order_entry_tpsl", className: "oui-text-xs", children: t("common.tpsl") })
      ] }),
      (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", gapX: 2, children: [
        isMobile && props.onReduceOnlyChange && (0, import_jsx_runtime.jsx)(
          ReduceOnlySwitch,
          {
            checked: props.reduceOnlyChecked ?? false,
            onCheckedChange: props.onReduceOnlyChange
          }
        ),
        !isMobile && (0, import_jsx_runtime.jsx)(
          TPSLAdvancedButton,
          {
            className: M(
              "oui-group oui-invisible",
              props.switchState && "oui-visible"
            ),
            showTPSLAdvanced: props.showTPSLAdvanced
          }
        )
      ] })
    ] }),
    (0, import_jsx_runtime.jsx)(
      "div",
      {
        className: M(
          "oui-max-h-0 oui-overflow-hidden oui-transition-all",
          props.switchState && "oui-max-h-[120px]"
        ),
        onTransitionEnd: () => {
          tpslFormRef.current?.style.setProperty(
            "opacity",
            props.switchState ? "1" : "0"
          );
        },
        children: (0, import_jsx_runtime.jsx)(
          TPSLInputForm,
          {
            ref: tpslFormRef,
            setOrderValue: props.setOrderValue,
            onChange: props.onChange,
            values: props.values,
            errors: props.errors,
            quote_dp: props.quote_dp,
            showTPSLAdvanced: props.showTPSLAdvanced,
            isMobile,
            isSlPriceWarning
          }
        )
      }
    ),
    isSlPriceWarning && (0, import_jsx_runtime.jsx)(TPSLPriceWarning, { errors: props.errors })
  ] });
};
var TPSLPriceWarning = (props) => {
  const { getErrorMsg } = useOrderEntryFormErrorMsg(props.errors);
  return (0, import_jsx_runtime.jsx)(
    DotStatus,
    {
      color: "warning",
      size: "xs",
      label: getErrorMsg("sl_trigger_price")
    }
  );
};
var TPSLInputForm = import_react.default.forwardRef((props, ref) => {
  const { getErrorMsg } = useOrderEntryFormErrorMsg(props.errors);
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(
    "div",
    {
      ref,
      className: "oui-space-y-1 oui-px-px oui-py-2 oui-transition-all",
      children: [
        (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", justify: "between", gapX: 2, children: [
          (0, import_jsx_runtime.jsx)(
            TPSLPositionTypeWidget,
            {
              value: props.values.position_type,
              onChange: props.onChange
            }
          ),
          props.isMobile && (0, import_jsx_runtime.jsx)(
            TPSLAdvancedButton,
            {
              showTPSLAdvanced: props.showTPSLAdvanced,
              isMobile: props.isMobile
            }
          )
        ] }),
        (0, import_jsx_runtime.jsx)(PnlInputProvider, { values: props.values.tp, type: "TP", children: (0, import_jsx_runtime.jsx)(
          TPSLInputRow,
          {
            type: "TP",
            error: getErrorMsg("tp_trigger_price"),
            onChange: props.onChange,
            values: props.values.tp,
            quote_dp: props.quote_dp,
            testIds: {
              first: "oui-testid-orderEntry-tpsl-tpPrice-input",
              second: "oui-testid-orderEntry-tpsl-tpPnl-input",
              dropDown: "oui-testid-orderEntry-tpsl-tp-dropDown-trigger-button"
            }
          }
        ) }),
        (0, import_jsx_runtime.jsx)(PnlInputProvider, { values: props.values.sl, type: "SL", children: (0, import_jsx_runtime.jsx)(
          TPSLInputRow,
          {
            type: "SL",
            error: props.isSlPriceWarning ? void 0 : getErrorMsg("sl_trigger_price"),
            displayErrorMessage: getErrorMsg("sl_trigger_price") !== void 0,
            onChange: props.onChange,
            values: props.values.sl,
            quote_dp: props.quote_dp,
            testIds: {
              first: "oui-testid-orderEntry-tpsl-slPrice-input",
              second: "oui-testid-orderEntry-tpsl-slPnl-input",
              dropDown: "oui-testid-orderEntry-tpsl-sl-dropDown-trigger-button"
            },
            classNames: {
              root: props.isSlPriceWarning ? "oui-outline-warning-darken focus-within:oui-outline-warning-darken" : void 0
            }
          }
        ) })
      ]
    }
  );
});
TPSLInputForm.displayName = "TPSLInputForm";
var TPSLAdvancedButton = (props) => {
  const { t } = useTranslation();
  return (0, import_jsx_runtime.jsxs)(
    Flex,
    {
      itemAlign: "center",
      gapX: 1,
      onClick: props.showTPSLAdvanced,
      className: M("oui-group oui-cursor-pointer", props.className),
      children: [
        (0, import_jsx_runtime.jsx)(
          Text2,
          {
            className: M(
              "oui-cursor-pointer group-hover:oui-text-base-contrast",
              props.isMobile ? "oui-text-2xs" : "oui-text-sm"
            ),
            children: t("tpsl.advanced")
          }
        ),
        (0, import_jsx_runtime.jsx)(
          SettingFillIcon,
          {
            size: 12,
            className: "oui-text-base-contrast-54 group-hover:oui-text-base-contrast oui-cursor-pointer",
            opacity: 1,
            onClick: props.showTPSLAdvanced
          }
        )
      ]
    }
  );
};
var TPSLTriggerPriceInput = (props) => {
  const { t } = useTranslation();
  const { errorMsgVisible } = useOrderEntryContext();
  const { tipsEle } = usePnlInputContext();
  const [prefix, setPrefix] = (0, import_react.useState)(`${props.type} Price`);
  const [placeholder, setPlaceholder] = (0, import_react.useState)("USDC");
  const [tipVisible, setTipVisible] = (0, import_react.useState)(false);
  const [isFocused, setIsFocused] = (0, import_react.useState)(false);
  const [innerValue, setInnerValue] = (0, import_react.useState)(
    props.values.trigger_price ?? ""
  );
  (0, import_react.useEffect)(() => {
    if (isFocused) {
      return;
    }
    setInnerValue(props.values.trigger_price ?? "");
  }, [props.values.trigger_price, isFocused]);
  const triggerPriceToolTipEle = (0, import_react.useMemo)(() => {
    if (props.error && (errorMsgVisible || props.displayErrorMessage))
      return props.error;
    if (tipVisible) return tipsEle;
    return null;
  }, [
    props.error,
    errorMsgVisible,
    tipVisible,
    tipsEle,
    props.displayErrorMessage
  ]);
  const getPrefixLabel = (trigger_price) => {
    let _prefix = props.type === "TP" ? t("tpsl.tpPrice") : t("tpsl.slPrice");
    if (trigger_price) {
      _prefix = props.type === "TP" ? t("tpsl.tp") : t("tpsl.sl");
    }
    return _prefix;
  };
  const onValueChange = (value) => {
    setInnerValue(value);
    props.onChange(value);
  };
  (0, import_react.useEffect)(() => {
    setPrefix(getPrefixLabel(props.values.trigger_price));
    if (!isFocused) {
      setInnerValue(props.values.trigger_price ?? "");
    }
  }, [props.type, props.values.trigger_price]);
  const onFocus = () => {
    setPrefix(props.type === "TP" ? t("tpsl.tp") : t("tpsl.sl"));
    setPlaceholder("");
    setTipVisible(true);
    setIsFocused(true);
  };
  const onBlur = () => {
    setPrefix(getPrefixLabel(props.values.trigger_price));
    setPlaceholder("USDC");
    setTipVisible(false);
    setIsFocused(false);
    props.onChange(innerValue);
  };
  return (0, import_jsx_runtime.jsx)(
    Input2.tooltip,
    {
      "data-testid": props.testId,
      prefix,
      size: "md",
      placeholder,
      align: "right",
      onFocus,
      onBlur,
      tooltip: triggerPriceToolTipEle,
      tooltipProps: {
        content: {
          side: props.type === "TP" ? "top" : "bottom"
        }
      },
      color: props.error ? "danger" : void 0,
      autoComplete: "off",
      value: innerValue,
      classNames: {
        additional: "oui-text-base-contrast-54",
        root: M("oui-pr-2 md:oui-pr-3", props.classNames?.root),
        prefix: M("oui-pr-1 md:oui-pr-2", props.classNames?.prefix),
        input: M(
          "oui-text-2xs placeholder:oui-text-2xs",
          props.classNames?.input
        )
      },
      onValueChange,
      formatters: [
        formatter_exports.numberFormatter,
        formatter_exports.dpFormatter(props.quote_dp ?? 2),
        formatter_exports.currencyFormatter
      ]
    }
  );
};
var TPSLInputRow = (props) => {
  return (0, import_jsx_runtime.jsxs)(Grid2, { cols: 2, gapX: 1, children: [
    (0, import_jsx_runtime.jsx)(
      TPSLTriggerPriceInput,
      {
        testId: props.testIds?.first,
        type: props.type,
        error: props.error,
        displayErrorMessage: props.displayErrorMessage,
        values: props.values ?? "",
        classNames: props.classNames,
        onChange: (event) => {
          props.onChange(
            props.type === "SL" ? "sl_trigger_price" : "tp_trigger_price",
            event
          );
        },
        quote_dp: props.quote_dp
      }
    ),
    (0, import_jsx_runtime.jsx)(
      PnlInputWidget,
      {
        testIds: {
          input: props.testIds?.second,
          dropDown: props.testIds?.dropDown
        },
        onChange: props.onChange,
        quote: "USDC",
        quote_dp: 2,
        type: props.type,
        values: {
          PnL: props.values.PnL,
          Offset: props.values.Offset,
          "Offset%": props.values["Offset%"],
          ROI: props.values.ROI
        }
      }
    )
  ] });
};
var OrderEntry = (props) => {
  const {
    side,
    formattedOrder,
    setOrderValue,
    setOrderValues,
    symbolInfo,
    maxQty,
    freeCollateral,
    helper,
    submit,
    metaState,
    bboStatus,
    bboType,
    onBBOChange,
    toggleBBO,
    disableFeatures,
    currentLtv,
    fillMiddleValue,
    soundAlert,
    setSoundAlert,
    currentFocusInput
  } = props;
  const [maxQtyConfirmOpen, setMaxQtyConfirmOpen] = (0, import_react.useState)(false);
  const { t } = useTranslation();
  const { isMobile } = useScreen();
  const [hasAdvancedTPSLResult, setHasAdvancedTPSLResult] = (0, import_react.useState)(false);
  const { errors, validated } = metaState;
  const [errorMsgVisible, setErrorMsgVisible] = (0, import_react.useState)(false);
  const [needConfirm, setNeedConfirm] = useLocalStorage(
    "orderly_order_confirm",
    true
  );
  const [pinned, setPinned] = useLocalStorage(
    "orderly-order-additional-pinned",
    true
  );
  const [showTPSLAdvanced, setShowTPSLAdvanced] = (0, import_react.useState)(false);
  const [hidden, setHidden] = useLocalStorage("orderly-order-hidden", false);
  const [slippage, setSlippage] = useLocalStorage("orderly-slippage", "1", {
    parseJSON: (value) => {
      return !value || value === '""' ? "1" : JSON.parse(value);
    }
  });
  const { notification } = useOrderlyContext();
  const soundAlertId = (0, import_react.useId)();
  const { getErrorMsg } = useOrderEntryFormErrorMsg(validated ? errors : null);
  const buttonLabel = (0, import_react.useMemo)(() => {
    return side === OrderSide.BUY ? t("orderEntry.buyLong") : t("orderEntry.sellShort");
  }, [side, t, isMobile]);
  (0, import_react.useEffect)(() => {
    if (validated) {
      setErrorMsgVisible(true);
    }
  }, [validated]);
  (0, import_react.useEffect)(() => {
    if (disableFeatures?.includes("slippageSetting")) {
      return;
    }
    if (slippage) {
      setOrderValue("slippage", Number(slippage));
    } else {
      setOrderValue("slippage", void 0);
    }
  }, [slippage, disableFeatures]);
  (0, import_react.useEffect)(() => {
    const clickHandler = (event) => {
      const target = event.target;
      if (target.closest("#order-entry-submit-button")) {
        return;
      }
      setErrorMsgVisible((visible) => {
        if (visible) {
          return false;
        }
        return visible;
      });
    };
    if (errorMsgVisible) {
      document.addEventListener("click", clickHandler);
    } else {
      document.removeEventListener("click", clickHandler);
    }
    return () => {
      document.removeEventListener("click", clickHandler);
    };
  }, [errorMsgVisible]);
  const onSubmit = useMemoizedFn(async () => {
    const isScaledOrder = formattedOrder.order_type === OrderType.SCALED;
    const isSlPriceError = props.slPriceError?.sl_trigger_price?.type === ERROR_MSG_CODES.SL_PRICE_ERROR;
    helper.validate(isSlPriceError ? props.slPriceError : void 0).then(
      // validate success, it return the order
      // TODO: get order from other function
      (order) => {
        if (isScaledOrder) {
          return modal.show(scaledOrderConfirmDialogId, {
            order,
            symbolInfo,
            size: isMobile ? "sm" : "md"
          });
        }
        if (needConfirm) {
          return modal.show(orderConfirmDialogId, {
            order: formattedOrder,
            symbolInfo
          });
        }
        return true;
      },
      // should catch validate error first, then submit
      (errors2) => {
        if (errors2.slippage) {
          Vt.error(t("orderEntry.slippage.error.max"));
        }
        setErrorMsgVisible(true);
        return Promise.reject();
      }
    ).then(() => {
      return submit({ resetOnSuccess: false }).then((result) => {
        if (!result.success && result.message) {
          Vt.error(result.message);
        } else if (result.success && isScaledOrder) {
          const message = getScaledPlaceOrderMessage(result);
          if (message) {
            Vt.success(message);
          }
        }
      });
    }).catch((error) => {
      if (error?.message) {
        Vt.error(error.message);
      }
    });
  });
  const formattedMaxQty = (0, import_react.useMemo)(() => {
    return new decimal_default(maxQty).todp(symbolInfo.base_dp, decimal_default.ROUND_DOWN).toString();
  }, [maxQty, symbolInfo.base_dp]);
  const onMaxQtyConfirm = (0, import_react.useCallback)(() => {
    setOrderValue("order_quantity", formattedMaxQty);
    requestAnimationFrame(() => {
      onSubmit();
    });
    setMaxQtyConfirmOpen(false);
  }, [setOrderValue, formattedMaxQty]);
  const validateSubmit = async () => {
    if (formattedOrder.reduce_only && maxQty === 0) {
      return modal.confirm({
        title: t("orderEntry.reduceOnly.reminder"),
        content: t("orderEntry.reduceOnly.reminder.content"),
        okLabel: t("orderEntry.placeOrderNow"),
        onOk: async () => {
          setOrderValue("reduce_only", false);
          requestAnimationFrame(() => {
            props.resetMetaState();
            onSubmit();
          });
          return Promise.resolve(true);
        },
        onCancel: async () => {
          return Promise.resolve(false);
        }
      });
    } else if (maxQty > 0 && Number(formattedOrder.order_quantity) > maxQty) {
      setMaxQtyConfirmOpen(true);
    } else {
      onSubmit();
    }
  };
  const onShowTPSLAdvanced = () => {
    helper.validate().then(
      () => {
        setShowTPSLAdvanced(true);
      },
      (errors2) => {
        const tpslKey = /* @__PURE__ */ new Set(["tp_trigger_price", "sl_trigger_price"]);
        if (Object.keys(errors2).every((key2) => tpslKey.has(key2))) {
          setShowTPSLAdvanced(true);
        }
      }
    );
  };
  const onSubmitAdvancedTPSL = (order) => {
    if (order.side !== formattedOrder.side) {
      setOrderValue("side", order.side);
    }
    setOrderValues({
      position_type: order.position_type,
      tp_order_type: order.tp_order_type,
      tp_pnl: order.tp_pnl,
      tp_offset: order.tp_offset,
      tp_offset_percentage: order.tp_offset_percentage,
      tp_ROI: order.tp_ROI,
      tp_trigger_price: order.tp_trigger_price,
      tp_order_price: order.tp_order_price,
      sl_order_type: order.sl_order_type,
      sl_trigger_price: order.sl_trigger_price,
      sl_order_price: order.sl_order_price,
      sl_pnl: order.sl_pnl,
      sl_offset: order.sl_offset,
      sl_offset_percentage: order.sl_offset_percentage,
      sl_ROI: order.sl_ROI
    });
    setShowTPSLAdvanced(false);
    setHasAdvancedTPSLResult(true);
  };
  const onDeleteAdvancedTPSL = () => {
    setHasAdvancedTPSLResult(false);
    setOrderValues({
      tp_trigger_price: void 0,
      tp_order_price: void 0,
      tp_order_type: OrderType.MARKET,
      sl_trigger_price: void 0,
      sl_order_price: void 0,
      sl_order_type: OrderType.MARKET,
      tp_pnl: void 0,
      sl_pnl: void 0,
      position_type: PositionType.FULL
    });
  };
  (0, import_react.useEffect)(() => {
    setHasAdvancedTPSLResult(false);
  }, [props.symbol]);
  const showReduceOnlySection = isMobile && formattedOrder.order_type !== OrderType.LIMIT && formattedOrder.order_type !== OrderType.MARKET || !isMobile;
  const showSoundSection = Boolean(notification?.orderFilled?.media) && (notification?.orderFilled?.displayInOrderEntry ?? true);
  const additionalInfoProps = {
    pinned,
    setPinned,
    needConfirm,
    setNeedConfirm,
    hidden,
    setHidden,
    onValueChange: setOrderValue,
    orderTypeExtra: formattedOrder["order_type_ext"],
    showExtra: formattedOrder["order_type"] === OrderType.LIMIT && !props.tpslSwitch
  };
  const extraButton = !pinned && (0, import_jsx_runtime.jsx)(AdditionalConfigButton, { ...additionalInfoProps });
  return (0, import_jsx_runtime.jsxs)(
    OrderEntryProvider,
    {
      errors,
      errorMsgVisible,
      symbolInfo,
      onFocus: props.onFocus,
      onBlur: props.onBlur,
      getErrorMsg,
      setOrderValue,
      setOrderValues,
      currentFocusInput: currentFocusInput.current,
      priceInputRef: props.priceInputRef,
      priceInputContainerRef: props.priceInputContainerRef,
      triggerPriceInputRef: props.triggerPriceInputRef,
      activatedPriceInputRef: props.activatedPriceInputRef,
      lastQuantityInputType: props.lastQuantityInputType,
      leverage: props.symbolLeverage,
      children: [
        (0, import_jsx_runtime.jsx)(
          MaxQtyConfirm,
          {
            open: maxQtyConfirmOpen,
            onOpenChange: setMaxQtyConfirmOpen,
            maxQty: formattedMaxQty,
            onConfirm: onMaxQtyConfirm,
            base: symbolInfo.base
          }
        ),
        (0, import_jsx_runtime.jsxs)(
          "div",
          {
            className: "oui-space-y-2 oui-text-base-contrast-54 xl:oui-space-y-3",
            ref: props.containerRef,
            children: [
              (0, import_jsx_runtime.jsx)(
                OrderEntryHeader,
                {
                  symbol: props.symbol,
                  canTrade: props.canTrade,
                  side,
                  order_type: formattedOrder.order_type,
                  setOrderValue,
                  symbolLeverage: props.symbolLeverage
                }
              ),
              (0, import_jsx_runtime.jsx)(
                Available,
                {
                  currentLtv,
                  canTrade: props.canTrade,
                  quote: symbolInfo?.quote,
                  freeCollateral
                }
              ),
              (0, import_jsx_runtime.jsx)(
                OrderInput,
                {
                  values: formattedOrder,
                  priceInputContainerWidth: props.priceInputContainerWidth,
                  fillMiddleValue,
                  bbo: {
                    bboStatus,
                    bboType,
                    onBBOChange,
                    toggleBBO
                  }
                }
              ),
              (0, import_jsx_runtime.jsx)(
                QuantitySlider,
                {
                  canTrade: props.canTrade,
                  side: props.side,
                  order_quantity: formattedOrder.order_quantity,
                  maxQty
                }
              ),
              (0, import_jsx_runtime.jsx)(
                ThrottledButton,
                {
                  fullWidth: true,
                  id: "order-entry-submit-button",
                  "data-type": OrderSide.BUY,
                  className: M(
                    side === OrderSide.BUY ? "orderly-order-entry-submit-button-buy oui-bg-success-darken hover:oui-bg-success-darken/80 active:oui-bg-success-darken/80" : "orderly-order-entry-submit-button-sell oui-bg-danger-darken hover:oui-bg-danger-darken/80 active:oui-bg-danger-darken/80"
                  ),
                  onClick: validateSubmit,
                  loading: props.isMutating,
                  disabled: !props.canTrade,
                  children: buttonLabel
                }
              ),
              (0, import_jsx_runtime.jsx)(
                AssetInfo,
                {
                  canTrade: props.canTrade,
                  quote: symbolInfo.quote,
                  estLiqPrice: props.estLiqPrice,
                  estLiqPriceDistance: props.estLiqPriceDistance,
                  estLeverage: props.estLeverage,
                  currentLeverage: props.currentLeverage,
                  slippage,
                  dp: symbolInfo.quote_dp,
                  setSlippage,
                  estSlippage: props.estSlippage,
                  orderType: formattedOrder.order_type,
                  disableFeatures,
                  symbol: props.symbol,
                  side
                }
              ),
              (0, import_jsx_runtime.jsx)(Divider, { className: "oui-w-full" }),
              hasAdvancedTPSLResult ? (0, import_jsx_runtime.jsx)(
                AdvancedTPSLResult,
                {
                  order: formattedOrder,
                  symbolInfo: props.symbolInfo,
                  errors: validated ? errors : null,
                  onEdit: () => {
                    setShowTPSLAdvanced(true);
                  },
                  onDelete: () => {
                    onDeleteAdvancedTPSL();
                  }
                }
              ) : (0, import_jsx_runtime.jsx)(
                OrderTPSL,
                {
                  quote_dp: props.symbolInfo.quote_dp,
                  switchState: props.tpslSwitch,
                  onSwitchChanged: props.setTpslSwitch,
                  orderType: formattedOrder.order_type,
                  errors: validated || props.slPriceError !== void 0 ? { ...errors, ...props.slPriceError } : null,
                  setOrderValue,
                  reduceOnlyChecked: formattedOrder.reduce_only ?? false,
                  onReduceOnlyChange: (checked) => {
                    setOrderValue("reduce_only", checked);
                  },
                  values: {
                    position_type: formattedOrder.position_type ?? PositionType.PARTIAL,
                    tp: {
                      trigger_price: formattedOrder.tp_trigger_price ?? "",
                      PnL: formattedOrder.tp_pnl ?? "",
                      Offset: formattedOrder.tp_offset ?? "",
                      "Offset%": formattedOrder.tp_offset_percentage ?? "",
                      ROI: formattedOrder.tp_ROI ?? ""
                    },
                    sl: {
                      trigger_price: formattedOrder.sl_trigger_price ?? "",
                      PnL: formattedOrder.sl_pnl ?? "",
                      Offset: formattedOrder.sl_offset ?? "",
                      "Offset%": formattedOrder.sl_offset_percentage ?? "",
                      ROI: formattedOrder.sl_ROI ?? ""
                    }
                  },
                  showTPSLAdvanced: onShowTPSLAdvanced,
                  onChange: (key2, value) => {
                    setOrderValue(key2, value);
                  }
                }
              ),
              showReduceOnlySection && (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", itemAlign: "center", className: "oui-mt-2", children: [
                (0, import_jsx_runtime.jsx)(
                  ReduceOnlySwitch,
                  {
                    checked: formattedOrder.reduce_only ?? false,
                    onCheckedChange: (checked) => {
                      setOrderValue("reduce_only", checked);
                    }
                  }
                ),
                !showSoundSection && extraButton
              ] }),
              showSoundSection && (0, import_jsx_runtime.jsxs)(Flex, { justify: "between", itemAlign: "center", children: [
                (0, import_jsx_runtime.jsxs)(Flex, { itemAlign: "center", gapX: 1, children: [
                  (0, import_jsx_runtime.jsx)(
                    Switch,
                    {
                      className: "oui-h-[14px]",
                      id: soundAlertId,
                      checked: soundAlert,
                      onCheckedChange: (checked) => setSoundAlert(checked)
                    }
                  ),
                  (0, import_jsx_runtime.jsx)("label", { htmlFor: soundAlertId, className: "oui-text-xs", children: t("portfolio.setting.soundAlerts") })
                ] }),
                extraButton
              ] }),
              !showSoundSection && isMobile && (formattedOrder.order_type == OrderType.LIMIT || formattedOrder.order_type == OrderType.MARKET) && !formattedOrder.reduce_only && !pinned && (0, import_jsx_runtime.jsx)(Flex, { className: "oui-w-full", justify: "end", children: extraButton }),
              pinned && (0, import_jsx_runtime.jsxs)(Box, { p: 2, r: "md", intensity: 700, position: "relative", children: [
                (0, import_jsx_runtime.jsx)(AdditionalInfo, { ...additionalInfoProps }),
                (0, import_jsx_runtime.jsx)(
                  PinButton,
                  {
                    onClick: () => {
                      setPinned(false);
                    },
                    className: "oui-group oui-absolute oui-right-2 oui-top-2",
                    "data-testid": "oui-testid-orderEntry-pinned-button"
                  }
                )
              ] })
            ]
          }
        ),
        (0, import_jsx_runtime.jsx)(
          SimpleSheet,
          {
            open: showTPSLAdvanced,
            onOpenChange: setShowTPSLAdvanced,
            classNames: {
              body: "oui-h-full oui-pb-0 oui-border-none",
              overlay: "!oui-bg-base-10/60",
              content: M(
                "oui-rounded-[16px] oui-border-none !oui-p-0",
                isMobile ? "oui-inset-y-0 oui-right-0 oui-w-[280px]" : "!oui-bottom-[40px] oui-right-3 oui-top-[44px] !oui-h-auto oui-w-[360px]"
              )
            },
            contentProps: { side: "right", closeable: false },
            children: (0, import_jsx_runtime.jsx)(
              TPSLAdvancedWidget,
              {
                setOrderValue,
                order: formattedOrder,
                onSubmit: onSubmitAdvancedTPSL,
                onClose: () => {
                  setShowTPSLAdvanced(false);
                },
                symbolLeverage: props.symbolLeverage
              }
            )
          }
        )
      ]
    }
  );
};
function useBBOState({
  tpslSwitch,
  order_type,
  order_type_ext,
  side,
  setOrderValues
}) {
  const [localBBOType, setLocalBBOType] = useLocalStorage("orderly_order_bbo_type", void 0);
  const lastBBOType = (0, import_react.useRef)(localBBOType);
  const { track } = useTrack();
  const bboStatus = (0, import_react.useMemo)(() => {
    if (tpslSwitch || [OrderType.POST_ONLY, OrderType.IOC, OrderType.FOK].includes(
      order_type_ext
    )) {
      return "disabled";
    }
    return localBBOType && order_type === OrderType.LIMIT ? "on" : "off";
  }, [tpslSwitch, order_type_ext, order_type, localBBOType]);
  const toggleBBO = () => {
    track(TrackerEventName.clickBBOButton);
    if (localBBOType) {
      setLocalBBOType(void 0);
      setOrderValues({
        order_type_ext: void 0,
        level: void 0
      });
    } else {
      setLocalBBOType(lastBBOType.current || BBOOrderType.COUNTERPARTY1);
    }
  };
  const onBBOChange = (value) => {
    setLocalBBOType(value);
    lastBBOType.current = value;
  };
  (0, import_react.useEffect)(() => {
    if (bboStatus === "disabled") {
      setOrderValues({
        // if order_type_ext is not bbo(ask, bid), keep previous value
        order_type_ext: isBBOOrder({ order_type_ext }) ? void 0 : order_type_ext,
        level: void 0
      });
    }
  }, [bboStatus, order_type_ext]);
  (0, import_react.useEffect)(() => {
    if (bboStatus === "on") {
      const orderType = getOrderTypeByBBO(localBBOType, side);
      const orderLevel = getOrderLevelByBBO(localBBOType);
      setOrderValues({
        order_type_ext: orderType,
        level: orderLevel
      });
    }
  }, [localBBOType, bboStatus, side]);
  return {
    bboStatus,
    bboType: localBBOType,
    setBBOType: setLocalBBOType,
    onBBOChange,
    toggleBBO
  };
}
function useFocusAndBlur(props) {
  const { base_tick, order_type, order_quantity, setValue } = props;
  const currentFocusInput = (0, import_react.useRef)(
    0
    /* NONE */
  );
  const lastScaledOrderPriceInput = (0, import_react.useRef)(
    8
    /* END_PRICE */
  );
  const lastQuantityInputType = (0, import_react.useRef)(
    0
    /* NONE */
  );
  const formatQty = () => {
    if (base_tick < 1 || // scaled order should not format quantity, because it is total quantity
    order_type === OrderType.SCALED || !order_quantity) {
      return;
    }
    const quantity = utils_exports.formatNumber(
      order_quantity,
      new decimal_default(base_tick || "0").toNumber()
    );
    setValue("order_quantity", quantity, {
      shouldUpdateLastChangedField: false
    });
  };
  const onFocus = (type) => (_) => {
    currentFocusInput.current = type;
    if ([
      7,
      8
      /* END_PRICE */
    ].includes(
      currentFocusInput.current
    )) {
      lastScaledOrderPriceInput.current = type;
    }
    if ([
      3,
      5,
      6
      /* MARGIN */
    ].includes(type)) {
      lastQuantityInputType.current = type;
    }
  };
  const onBlur = (type) => (_) => {
    setTimeout(() => {
      if (currentFocusInput.current !== type) {
        return;
      }
      currentFocusInput.current = 0;
    }, 300);
    if (type === 3) {
      formatQty();
    }
  };
  return {
    currentFocusInput,
    lastScaledOrderPriceInput,
    lastQuantityInputType,
    onFocus: useMemoizedFn(onFocus),
    onBlur: useMemoizedFn(onBlur)
  };
}
function usePriceInputContainer({
  order_type_ext
}) {
  const [priceInputContainerWidth, setPriceInputContainerWidth] = (0, import_react.useState)(0);
  const priceInputContainerRef = (0, import_react.useRef)(null);
  (0, import_react.useEffect)(() => {
    const element = priceInputContainerRef.current;
    if (!element) {
      return;
    }
    const resizeObserver = new ResizeObserver((entries) => {
      for (const entry of entries) {
        const width = entry.contentRect.width;
        if (width) {
          setPriceInputContainerWidth(width);
        }
      }
    });
    resizeObserver.observe(element);
    return () => {
      resizeObserver.unobserve(element);
    };
  }, [priceInputContainerRef, order_type_ext]);
  return { priceInputContainerRef, priceInputContainerWidth };
}
var ORDERLY_ORDER_SOUND_ALERT_KEY = "orderly_order_sound_alert";
var useOrderEntryScript = (inputs) => {
  const { symbol } = inputs;
  const [localOrderType, setLocalOrderType] = useLocalStorage(
    "orderly-order-entry-order-type",
    OrderType.LIMIT
  );
  const [localOrderSide, setLocalOrderSide] = useLocalStorage(
    "orderly-order-entry-order-side",
    OrderSide.BUY
  );
  const { notification } = useOrderlyContext();
  const [soundAlert, setSoundAlert] = useLocalStorage(
    ORDERLY_ORDER_SOUND_ALERT_KEY,
    notification?.orderFilled?.defaultOpen ?? false
  );
  const canTrade = useCanTrade();
  const {
    formattedOrder,
    setValue,
    setValues: setOrderValues,
    symbolInfo,
    ...state
  } = useOrderEntry2(symbol, {
    initialOrder: {
      symbol,
      order_type: localOrderType,
      position_type: PositionType.PARTIAL,
      side: localOrderSide
    }
  });
  const [tpslSwitch, setTpslSwitch] = useLocalStorage(
    "orderly-order-entry-tp_sl-switch",
    false
  );
  const { currentLeverage } = useMarginRatio();
  const ee = useEventEmitter();
  const triggerPriceInputRef = (0, import_react.useRef)(null);
  const priceInputRef = (0, import_react.useRef)(null);
  const activatedPriceInputRef = (0, import_react.useRef)(null);
  const { bboStatus, bboType, setBBOType, onBBOChange, toggleBBO } = useBBOState({
    tpslSwitch,
    order_type: formattedOrder.order_type,
    order_type_ext: formattedOrder.order_type_ext,
    side: formattedOrder.side,
    setOrderValues
  });
  const {
    currentFocusInput,
    lastScaledOrderPriceInput,
    lastQuantityInputType,
    onFocus,
    onBlur
  } = useFocusAndBlur({
    base_tick: symbolInfo?.base_tick,
    order_type: formattedOrder.order_type,
    order_quantity: formattedOrder.order_quantity,
    setValue
  });
  const cancelTP_SL = () => {
    setOrderValues({
      tp_trigger_price: "",
      sl_trigger_price: "",
      position_type: PositionType.FULL
    });
  };
  const enableTP_SL = () => {
    setOrderValues({
      order_type_ext: void 0,
      position_type: PositionType.FULL
    });
  };
  const setOrderValue = useMemoizedFn(
    (key2, value, options2) => {
      if (key2 === "order_type") {
        setLocalOrderType(value);
      }
      if (key2 === "side") {
        setLocalOrderSide(value);
      }
      if (key2 === "reduce_only" && value || key2 === "order_type" && (value === OrderType.STOP_LIMIT || value === OrderType.STOP_MARKET)) {
        const data = {
          tp_trigger_price: "",
          sl_trigger_price: "",
          [key2]: value
        };
        if (key2 === "order_type") {
          data["order_type_ext"] = "";
        }
        setOrderValues(data);
        return;
      }
      if (key2 === "order_type" && value !== OrderType.LIMIT) {
        const data = {
          level: void 0,
          order_type_ext: void 0,
          [key2]: value
        };
        setOrderValues(data);
        return;
      }
      if (key2 === "order_type" && value === OrderType.SCALED) {
        setOrderValues({
          distribution_type: DistributionType.FLAT,
          [key2]: value
        });
        return;
      }
      setValue(key2, value, options2);
    }
  );
  const onTPSLSwitchChanged = (state2) => {
    setTpslSwitch(state2);
    if (state2) {
      enableTP_SL();
    } else {
      cancelTP_SL();
    }
  };
  (0, import_react.useEffect)(() => {
    const updateOrderPrice = (price) => {
      setValue("order_price", price);
    };
    ee.on("update:orderPrice", updateOrderPrice);
    return () => {
      ee.off("update:orderPrice", updateOrderPrice);
    };
  }, []);
  (0, import_react.useEffect)(() => {
    const focusInputElement = (target) => {
      requestAnimationFrame(() => {
        target?.focus();
      });
    };
    const orderBookItemClickHandler = (item) => {
      const price = removeTrailingZeros(item[0]);
      const { order_type, order_type_ext } = formattedOrder;
      if (currentFocusInput.current === 2 && (order_type === OrderType.STOP_LIMIT || order_type === OrderType.STOP_MARKET)) {
        setValue("trigger_price", price);
        focusInputElement(triggerPriceInputRef.current);
        return;
      }
      if (isBBOOrder({ order_type, order_type_ext })) {
        setBBOType(void 0);
        setOrderValues({
          order_type_ext: void 0,
          level: void 0
        });
        requestAnimationFrame(() => {
          ee.emit("update:orderPrice", price);
        });
        focusInputElement(priceInputRef.current);
        return;
      }
      if (order_type === OrderType.STOP_LIMIT || order_type === OrderType.LIMIT) {
        setValue("order_price", price);
        focusInputElement(priceInputRef.current);
        return;
      }
      if (order_type === OrderType.STOP_MARKET) {
        setValue("trigger_price", price);
        focusInputElement(triggerPriceInputRef.current);
        return;
      }
      if (order_type === OrderType.MARKET) {
        setBBOType(void 0);
        setOrderValues({
          order_type: OrderType.LIMIT,
          order_price: price
        });
        focusInputElement(priceInputRef.current);
        return;
      }
      if (order_type === OrderType.SCALED && lastScaledOrderPriceInput.current) {
        const field = lastScaledOrderPriceInput.current === 7 ? "start_price" : "end_price";
        setValue(field, price);
        focusInputElement(priceInputRef.current);
        return;
      }
      if (order_type === OrderType.TRAILING_STOP) {
        setValue("activated_price", price);
        focusInputElement(activatedPriceInputRef.current);
        return;
      }
      setValue("order_price", price);
      focusInputElement(priceInputRef.current);
    };
    ee.on("orderbook:item:click", orderBookItemClickHandler);
    return () => {
      ee.off("orderbook:item:click", orderBookItemClickHandler);
    };
  }, [formattedOrder, symbolInfo]);
  (0, import_react.useEffect)(() => {
    state.reset();
    state.resetMetaState();
    lastQuantityInputType.current = 0;
  }, [symbol]);
  (0, import_react.useEffect)(() => {
    if (formattedOrder.order_type === OrderType.SCALED && !formattedOrder.distribution_type) {
      setValue("distribution_type", DistributionType.FLAT);
    }
  }, [formattedOrder.order_type, formattedOrder.distribution_type]);
  const currentLtv = useComputedLTV();
  const askAndBid = useAskAndBid();
  const fillMiddleValue = () => {
    if (bboStatus === "on") {
      toggleBBO();
    }
    if (formattedOrder.order_type === OrderType.LIMIT) {
      const [bestAsk = 0, bestBid = 0] = askAndBid;
      const midPrice = new decimal_default(safeNumber(bestAsk)).add(safeNumber(bestBid)).div(2).toNumber();
      requestAnimationFrame(() => {
        ee.emit("update:orderPrice", midPrice);
      });
    }
  };
  const { priceInputContainerRef, priceInputContainerWidth } = usePriceInputContainer({
    order_type_ext: formattedOrder.order_type_ext
  });
  const slPriceError = useTpslPriceChecker({
    slPrice: formattedOrder.sl_trigger_price,
    liqPrice: state.estLiqPrice,
    side: formattedOrder.side,
    currentPosition: state.currentPosition,
    orderQuantity: Number(formattedOrder.order_quantity)
  });
  (0, import_react.useEffect)(() => {
    if (formattedOrder.reduce_only) {
      setTpslSwitch(false);
    }
  }, [formattedOrder.reduce_only]);
  (0, import_react.useEffect)(() => {
    if (tpslSwitch) {
      setOrderValue("reduce_only", false);
    }
  }, [tpslSwitch]);
  return {
    ...state,
    slPriceError: slPriceError ?? void 0,
    side: formattedOrder.side,
    type: formattedOrder.order_type,
    level: formattedOrder.level,
    formattedOrder,
    setOrderValue,
    setOrderValues,
    currentLeverage,
    // cancelTP_SL,
    // enableTP_SL,
    tpslSwitch,
    setTpslSwitch: onTPSLSwitchChanged,
    symbolInfo,
    onFocus,
    onBlur,
    priceInputRef,
    priceInputContainerRef,
    priceInputContainerWidth,
    triggerPriceInputRef,
    activatedPriceInputRef,
    lastQuantityInputType,
    canTrade,
    bboStatus,
    bboType,
    onBBOChange,
    toggleBBO,
    currentLtv,
    fillMiddleValue,
    symbol,
    soundAlert,
    setSoundAlert,
    currentFocusInput
  };
};
var OrderEntryWidget = (props) => {
  const state = useOrderEntryScript(props);
  return (0, import_jsx_runtime.jsx)(
    OrderEntry,
    {
      ...state,
      containerRef: props.containerRef,
      disableFeatures: props.disableFeatures
    }
  );
};

// node_modules/@orderly.network/ui-tradingview/dist/index.mjs
var import_dist4 = __toESM(require_dist(), 1);
var import_dist5 = __toESM(require_dist2(), 1);
var import_dist6 = __toESM(require_dist3(), 1);
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_react2 = __toESM(require_react(), 1);
var __defProp = Object.defineProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var CaretIcon;
var DisplaySettingIcon;
var IndicatorsIcon;
var SettingIcon;
var BarIcon;
var CandlesIcon;
var HollowCandlesIcon;
var LineIcon;
var AreaIcon;
var BaseLineIcon;
var UnSelectIcon;
var SelectedIcon;
var init_icons = __esm({
  "src/icons/index.tsx"() {
    CaretIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 12 12",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsx)("g", { id: "Caret-down", children: (0, import_jsx_runtime2.jsx)(
          "path",
          {
            id: "Vector",
            d: "M3.00349 3.9978C2.59149 3.9978 2.36564 4.4653 2.61289 4.7948C2.98789 5.2948 5.23784 8.29479 5.61289 8.79479C5.81289 9.06128 6.20974 9.06128 6.40974 8.79479L9.40974 4.7948C9.65694 4.4653 9.41554 3.9978 9.00349 3.9978H3.00349Z"
          }
        ) })
      }
    );
    DisplaySettingIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 18 18",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsx)("g", { id: "Display settings", children: (0, import_jsx_runtime2.jsx)(
          "path",
          {
            id: "Union",
            fillRule: "evenodd",
            clipRule: "evenodd",
            d: "M15.0571 10.9856L11.6295 5.06523L8.71377 10.8968C8.38933 11.5456 7.54464 11.7223 6.98733 11.2579L5.75311 10.2294L2.83709 14.7654L1.57532 13.9543L4.49134 9.41824C4.97892 8.65978 6.0207 8.49981 6.71338 9.07705L7.58387 9.80246L10.5991 3.77192C10.9997 2.97082 12.1302 2.93624 12.579 3.71137L14.6517 7.29147C14.7714 7.03643 15.0305 6.85983 15.3309 6.85983C15.7451 6.85983 16.0809 7.19562 16.0809 7.60983C16.0809 8.02405 15.7451 8.35983 15.3309 8.35983C15.31 8.35983 15.2892 8.35897 15.2687 8.35729L16.3553 10.2341L15.0571 10.9856ZM2.58093 6.85983C2.16672 6.85983 1.83093 7.19562 1.83093 7.60983C1.83093 8.02405 2.16672 8.35983 2.58093 8.35983C2.99515 8.35983 3.33093 8.02405 3.33093 7.60983C3.33093 7.19562 2.99515 6.85983 2.58093 6.85983ZM4.83093 6.85983C4.41672 6.85983 4.08093 7.19562 4.08093 7.60983C4.08093 8.02405 4.41672 8.35983 4.83093 8.35983C5.24515 8.35983 5.58093 8.02405 5.58093 7.60983C5.58093 7.19562 5.24515 6.85983 4.83093 6.85983ZM6.33093 7.60983C6.33093 7.19562 6.66672 6.85983 7.08093 6.85983C7.49515 6.85983 7.83093 7.19562 7.83093 7.60983C7.83093 8.02405 7.49515 8.35983 7.08093 8.35983C6.66672 8.35983 6.33093 8.02405 6.33093 7.60983ZM11.5809 6.85983C11.1667 6.85983 10.8309 7.19562 10.8309 7.60983C10.8309 8.02405 11.1667 8.35983 11.5809 8.35983C11.9951 8.35983 12.3309 8.02405 12.3309 7.60983C12.3309 7.19562 11.9951 6.85983 11.5809 6.85983Z"
          }
        ) })
      }
    );
    IndicatorsIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 18 18",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsx)("g", { id: "Indicators", children: (0, import_jsx_runtime2.jsx)(
          "path",
          {
            id: "Union",
            fillRule: "evenodd",
            clipRule: "evenodd",
            d: "M9.74483 2.25C8.96145 2.25 8.26885 2.76459 8.04549 3.51818L7.46202 5.48254H5.06262V7.08339H6.98652L5.00539 13.7532L4.31554 13.1112L3.22498 14.2831L4.4777 15.4489C5.08739 16.015 6.08317 15.7469 6.32134 14.945L8.65649 7.08339H11.6032V5.48254H9.13198L9.58033 3.97311C9.60167 3.90112 9.66833 3.85084 9.74483 3.85084H11.69V2.25H9.74483ZM10.6234 11.2354L10.2638 11.5172C10.0153 11.712 9.65591 11.6684 9.46115 11.4199C9.26639 11.1713 9.30998 10.812 9.55852 10.6172L10.1451 10.1575C10.5693 9.82514 11.1868 9.94011 11.4642 10.4005L11.4682 10.407L12.1304 11.5752L13.4594 10.3111C13.6882 10.0935 14.05 10.1025 14.2677 10.3313C14.4853 10.5601 14.4763 10.922 14.2475 11.1396L12.7116 12.6006L13.3897 13.797L13.6482 13.4503C13.8369 13.1972 14.1951 13.145 14.4483 13.3337C14.7014 13.5225 14.7536 13.8807 14.5649 14.1338L13.9917 14.9025L13.94 14.9457C13.5178 15.2981 12.8837 15.1886 12.6003 14.7226L12.5958 14.7151L11.8576 13.4129L10.3563 14.841C10.1275 15.0586 9.76561 15.0495 9.54798 14.8208C9.33036 14.592 9.3394 14.2301 9.56819 14.0125L11.2764 12.3875L10.6234 11.2354Z"
          }
        ) })
      }
    );
    SettingIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 18 18",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsx)("g", { id: "Setting", children: (0, import_jsx_runtime2.jsx)(
          "path",
          {
            id: "Vector",
            d: "M6.72589 1.84154C5.66239 2.17904 4.74515 2.72954 3.93665 3.48254C3.6869 3.71504 3.6104 4.09604 3.7724 4.39604C4.37315 5.50679 3.74464 6.69554 2.38939 6.76379C2.05789 6.78029 1.76465 7.0263 1.68665 7.34955C1.5524 7.9083 1.49915 8.37629 1.49915 8.98979C1.49915 9.50504 1.55464 10.0885 1.66339 10.6075C1.73089 10.9315 2.0129 11.1648 2.3429 11.1933C3.7064 11.311 4.3814 12.3513 3.7724 13.6773C3.6374 13.972 3.6989 14.3245 3.93665 14.545C4.73315 15.2815 5.64814 15.8013 6.72589 16.1388C7.03339 16.2348 7.38064 16.1185 7.56964 15.8575C8.40364 14.7033 9.6134 14.6995 10.4054 15.8575C10.5921 16.1298 10.9341 16.261 11.2491 16.162C12.2894 15.8343 13.2584 15.277 14.0616 14.545C14.3091 14.3193 14.3744 13.954 14.2259 13.654C13.6019 12.3948 14.3197 11.239 15.6082 11.2165C15.9502 11.2105 16.2547 10.9863 16.3349 10.654C16.4647 10.1163 16.4991 9.64829 16.4991 8.98979C16.4991 8.42429 16.4324 7.86704 16.3116 7.32629C16.2351 6.98354 15.9359 6.74054 15.5849 6.73979C14.3167 6.73754 13.6056 5.49104 14.2259 4.39604C14.3984 4.09154 14.3444 3.71804 14.0849 3.48254C13.2674 2.74004 12.2706 2.15954 11.2259 1.84154C10.9049 1.74404 10.5636 1.86404 10.3821 2.14604C9.65764 3.27179 8.30465 3.29129 7.5929 2.17004C7.41065 1.88204 7.04989 1.73879 6.72589 1.84154ZM11.2844 3.43455C11.7966 3.64905 12.2009 3.8733 12.6651 4.22505C12.1206 5.9478 13.0439 7.73729 14.9564 8.17304C15.0036 8.48279 14.9991 8.67029 14.9991 8.98979C14.9991 9.37154 15.0044 9.50579 14.9609 9.78179C13.0566 10.1763 12.1146 11.9155 12.6449 13.7538C12.1889 14.0845 11.8626 14.314 11.2904 14.5353C9.94639 13.1673 8.0804 13.1073 6.7079 14.5443C6.1724 14.3088 5.76066 14.0995 5.34441 13.7478C5.86041 11.881 4.98815 10.2693 3.05165 9.7803C2.9654 9.4383 2.9999 8.4723 3.0494 8.17905C5.0519 7.69905 5.8409 5.92679 5.34365 4.21979C5.78315 3.88904 6.1784 3.6483 6.69215 3.4398C7.9859 4.75605 9.92839 4.8873 11.2844 3.43455ZM8.99915 5.98979C7.3424 5.98979 5.99915 7.33304 5.99915 8.98979C5.99915 10.6473 7.3424 11.9898 8.99915 11.9898C10.6559 11.9898 11.9991 10.6473 11.9991 8.98979C11.9991 7.33304 10.6559 5.98979 8.99915 5.98979ZM8.99915 7.48979C9.8279 7.48979 10.4991 8.16179 10.4991 8.98979C10.4991 9.81855 9.8279 10.4898 8.99915 10.4898C8.1704 10.4898 7.49915 9.81855 7.49915 8.98979C7.49915 8.16179 8.1704 7.48979 8.99915 7.48979Z"
          }
        ) })
      }
    );
    BarIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 20 20",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsx)("g", { id: "Bars", children: (0, import_jsx_runtime2.jsx)(
          "path",
          {
            id: "Union",
            fillRule: "evenodd",
            clipRule: "evenodd",
            d: "M12.5 15.5C12.5 15.7761 12.7239 16 13 16H14C14.2761 16 14.5 15.7761 14.5 15.5V14H16C16.2761 14 16.5 13.7761 16.5 13.5V12.5C16.5 12.2239 16.2761 12 16 12H14.5V2.5C14.5 2.22386 14.2761 2 14 2H13C12.7239 2 12.5 2.22386 12.5 2.5V4H11C10.7239 4 10.5 4.22386 10.5 4.5V5.5C10.5 5.77614 10.7239 6 11 6H12.5V15.5ZM5.5 17.5C5.5 17.7761 5.72386 18 6 18H7C7.27614 18 7.5 17.7761 7.5 17.5V10H9C9.27614 10 9.5 9.77614 9.5 9.5V8.5C9.5 8.22386 9.27614 8 9 8H7.5V4.5C7.5 4.22386 7.27614 4 7 4H6C5.72386 4 5.5 4.22386 5.5 4.5V14H4C3.72386 14 3.5 14.2239 3.5 14.5V15.5C3.5 15.7761 3.72386 16 4 16H5.5V17.5Z"
          }
        ) })
      }
    );
    CandlesIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 20 20",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsx)("g", { id: "Candles", children: (0, import_jsx_runtime2.jsx)(
          "path",
          {
            id: "Union",
            fillRule: "evenodd",
            clipRule: "evenodd",
            d: "M7.25 2.5V4H8.5C8.77614 4 9 4.22386 9 4.5V15.5C9 15.7761 8.77614 16 8.5 16H7.25V17.5C7.25 17.7761 7.02614 18 6.75 18H6.25C5.97386 18 5.75 17.7761 5.75 17.5V16H4.5C4.22386 16 4 15.7761 4 15.5V4.5C4 4.22386 4.22386 4 4.5 4H5.75V2.5C5.75 2.22386 5.97386 2 6.25 2H6.75C7.02614 2 7.25 2.22386 7.25 2.5ZM5.5 5.5V14.5H7.5V5.5H5.5ZM14.25 4.5V7H15.5C15.7761 7 16 7.22386 16 7.5V15.5C16 15.7761 15.7761 16 15.5 16H14.25V17.5C14.25 17.7761 14.0261 18 13.75 18H13.25C12.9739 18 12.75 17.7761 12.75 17.5V16H11.5C11.2239 16 11 15.7761 11 15.5V7.5C11 7.22386 11.2239 7 11.5 7H12.75V4.5C12.75 4.22386 12.9739 4 13.25 4H13.75C14.0261 4 14.25 4.22386 14.25 4.5ZM12.5 8.5V14.5H14.5V8.5H12.5Z"
          }
        ) })
      }
    );
    HollowCandlesIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 20 20",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsxs)("g", { id: "Hollow Candles", children: [
          (0, import_jsx_runtime2.jsx)(
            "path",
            {
              id: "Subtract",
              fillRule: "evenodd",
              clipRule: "evenodd",
              d: "M5.5 14.5V5.5H7.5V14.5H5.5Z",
              fill: "white",
              fillOpacity: "0.12"
            }
          ),
          (0, import_jsx_runtime2.jsx)(
            "path",
            {
              id: "Union",
              fillRule: "evenodd",
              clipRule: "evenodd",
              d: "M7.25 2.5V4H8.5C8.77614 4 9 4.22386 9 4.5V15.5C9 15.7761 8.77614 16 8.5 16H7.25V17.5C7.25 17.7761 7.02614 18 6.75 18H6.25C5.97386 18 5.75 17.7761 5.75 17.5V16H4.5C4.22386 16 4 15.7761 4 15.5V4.5C4 4.22386 4.22386 4 4.5 4H5.75V2.5C5.75 2.22386 5.97386 2 6.25 2H6.75C7.02614 2 7.25 2.22386 7.25 2.5ZM5.5 5.5V14.5H7.5V5.5H5.5ZM14.25 4.5V7H15.5C15.7761 7 16 7.22386 16 7.5V15.5C16 15.7761 15.7761 16 15.5 16H14.25V17.5C14.25 17.7761 14.0261 18 13.75 18H13.25C12.9739 18 12.75 17.7761 12.75 17.5V16H11.5C11.2239 16 11 15.7761 11 15.5V7.5C11 7.22386 11.2239 7 11.5 7H12.75V4.5C12.75 4.22386 12.9739 4 13.25 4H13.75C14.0261 4 14.25 4.22386 14.25 4.5ZM12.5 8.5V14.5H14.5V8.5H12.5Z"
            }
          )
        ] })
      }
    );
    LineIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 20 20",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsx)("g", { id: "Line", children: (0, import_jsx_runtime2.jsx)(
          "path",
          {
            id: "Vector 16 (Stroke)",
            fillRule: "evenodd",
            clipRule: "evenodd",
            d: "M18.3 2.72288C18.542 2.85596 18.6303 3.15999 18.4972 3.40195L13.495 12.4968C13.0776 13.2557 12.1061 13.5044 11.3754 13.0394L7.19878 10.3816L2.97738 17.558C2.83737 17.796 2.53092 17.8754 2.2929 17.7354L1.43097 17.2284C1.19295 17.0884 1.1135 16.7819 1.25351 16.5439L5.73746 8.92122C6.16792 8.18943 7.1194 7.96045 7.83567 8.41626L11.9995 11.0659L16.7448 2.43811C16.8778 2.19615 17.1819 2.10788 17.4238 2.24096L18.3 2.72288Z"
          }
        ) })
      }
    );
    AreaIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 20 20",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsxs)("g", { id: "Area", children: [
          (0, import_jsx_runtime2.jsx)(
            "path",
            {
              id: "Vector 16 (Stroke)",
              fillRule: "evenodd",
              clipRule: "evenodd",
              d: "M18.3 2.72288C18.542 2.85596 18.6303 3.15999 18.4972 3.40195L13.495 12.4968C13.0776 13.2557 12.1061 13.5044 11.3754 13.0394L7.19878 10.3816L2.97738 17.558C2.83737 17.796 2.53092 17.8754 2.2929 17.7354L1.43097 17.2284C1.19295 17.0884 1.1135 16.7819 1.25351 16.5439L5.73746 8.92122C6.16792 8.18943 7.1194 7.96045 7.83567 8.41626L11.9995 11.0659L16.7448 2.43811C16.8778 2.19615 17.1819 2.10788 17.4238 2.24096L18.3 2.72288Z"
            }
          ),
          (0, import_jsx_runtime2.jsx)(
            "path",
            {
              id: "Subtract",
              fillRule: "evenodd",
              clipRule: "evenodd",
              d: "M2.7608 17.7562C2.84602 17.899 3.00106 18 3.19016 18H16.362C17.4665 18 18.362 17.1046 18.362 16V3.94663C18.362 3.86618 18.3453 3.79397 18.3162 3.73108L13.495 12.4968C13.0776 13.2557 12.1061 13.5044 11.3754 13.0394L7.19881 10.3816L2.9774 17.558C2.92465 17.6477 2.84826 17.7148 2.7608 17.7562Z",
              fill: "white",
              fillOpacity: "0.12"
            }
          )
        ] })
      }
    );
    BaseLineIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 20 20",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsx)("g", { id: "Baseline", children: (0, import_jsx_runtime2.jsx)(
          "path",
          {
            id: "Union",
            fillRule: "evenodd",
            clipRule: "evenodd",
            d: "M6.05938 2.93544C6.55642 1.71198 8.27766 1.68139 8.81785 2.88642L11.4361 8.72723H9.24438L7.4717 4.77279L5.86521 8.72723H3.70647L6.05938 2.93544ZM13.9282 14.2865L13.0051 12.2272H10.8133L12.5586 16.1204C13.088 17.3014 14.7641 17.3026 15.2952 16.1224L17.048 12.2272H14.8549L13.9282 14.2865ZM2.28459 12.2272H4.44333L1.85293 18.6036L0 17.8509L2.28459 12.2272ZM16.4299 8.72723H18.623L19.3384 7.13759L17.5145 6.31686L16.4299 8.72723ZM1.42645 10.2272C1.42645 9.95109 1.65031 9.72723 1.92645 9.72723H2.92645C3.20259 9.72723 3.42645 9.95109 3.42645 10.2272V10.7272C3.42645 11.0034 3.20259 11.2272 2.92645 11.2272H1.92645C1.65031 11.2272 1.42645 11.0034 1.42645 10.7272V10.2272ZM4.42645 10.2272C4.42645 9.95109 4.65031 9.72723 4.92645 9.72723H5.92645C6.20259 9.72723 6.42645 9.95109 6.42645 10.2272V10.7272C6.42645 11.0034 6.20259 11.2272 5.92645 11.2272H4.92645C4.65031 11.2272 4.42645 11.0034 4.42645 10.7272V10.2272ZM7.92645 9.72723C7.65031 9.72723 7.42645 9.95109 7.42645 10.2272V10.7272C7.42645 11.0034 7.65031 11.2272 7.92645 11.2272H8.92645C9.20259 11.2272 9.42645 11.0034 9.42645 10.7272V10.2272C9.42645 9.95109 9.20259 9.72723 8.92645 9.72723H7.92645ZM10.4265 10.2272C10.4265 9.95109 10.6503 9.72723 10.9265 9.72723H11.9265C12.2026 9.72723 12.4265 9.95109 12.4265 10.2272V10.7272C12.4265 11.0034 12.2026 11.2272 11.9265 11.2272H10.9265C10.6503 11.2272 10.4265 11.0034 10.4265 10.7272V10.2272ZM13.9265 9.72723C13.6503 9.72723 13.4265 9.95109 13.4265 10.2272V10.7272C13.4265 11.0034 13.6503 11.2272 13.9265 11.2272H14.9265C15.2026 11.2272 15.4265 11.0034 15.4265 10.7272V10.2272C15.4265 9.95109 15.2026 9.72723 14.9265 9.72723H13.9265ZM16.4265 10.2272C16.4265 9.95109 16.6503 9.72723 16.9265 9.72723H17.9265C18.2026 9.72723 18.4265 9.95109 18.4265 10.2272V10.7272C18.4265 11.0034 18.2026 11.2272 17.9265 11.2272H16.9265C16.6503 11.2272 16.4265 11.0034 16.4265 10.7272V10.2272Z"
          }
        ) })
      }
    );
    UnSelectIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 12 12",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsx)("g", { id: "Unselected", children: (0, import_jsx_runtime2.jsx)(
          "path",
          {
            id: "Vector",
            d: "M6.00684 0.999023C3.24544 0.999023 1.00684 3.23752 1.00684 5.99902C1.00684 8.76051 3.24544 10.999 6.00684 10.999C8.76834 10.999 11.0068 8.76051 11.0068 5.99902C11.0068 3.23752 8.76834 0.999023 6.00684 0.999023ZM6.00684 1.99902C8.21584 1.99902 10.0068 3.79002 10.0068 5.99902C10.0068 8.20801 8.21584 9.99901 6.00684 9.99901C3.79769 9.99901 2.00684 8.20801 2.00684 5.99902C2.00684 3.79002 3.79769 1.99902 6.00684 1.99902Z",
            fill: "white",
            fillOpacity: "0.2"
          }
        ) })
      }
    );
    SelectedIcon = (props) => (0, import_jsx_runtime2.jsx)(
      "svg",
      {
        viewBox: "0 0 12 12",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime2.jsx)("g", { id: "Selected-Multiple Choices-fill", children: (0, import_jsx_runtime2.jsx)(
          "path",
          {
            id: "Subtract",
            fillRule: "evenodd",
            clipRule: "evenodd",
            d: "M1.00684 5.99902C1.00684 3.23752 3.24544 0.999023 6.00684 0.999023C8.76834 0.999023 11.0068 3.23752 11.0068 5.99902C11.0068 8.76051 8.76834 10.999 6.00684 10.999C3.24544 10.999 1.00684 8.76051 1.00684 5.99902ZM8.49243 3.65563C8.60602 3.54726 8.76156 3.49957 8.91068 3.49957C9.05981 3.49957 9.21534 3.54726 9.32893 3.65563C9.5567 3.87231 9.5567 4.23649 9.32893 4.45317L5.25362 8.33706C5.02644 8.55374 4.6443 8.55374 4.41712 8.33706L2.67014 6.67253C2.44296 6.45585 2.44296 6.09161 2.67014 5.87499C2.89791 5.65831 3.28004 5.65831 3.50722 5.87499L4.83537 7.1407L8.49243 3.65563Z",
            fill: "#608CFF"
          }
        ) })
      }
    );
  }
});
var lineType_exports = {};
__export(lineType_exports, {
  default: () => lineType_default
});
var LineType;
var lineType_default;
var init_lineType = __esm({
  "src/components/lineType/index.tsx"() {
    init_icons();
    LineType = (props) => {
      const [open2, setOpen] = (0, import_react2.useState)(false);
      const { t } = useTranslation();
      const lineTypeList = (0, import_react2.useMemo)(() => {
        return [
          {
            icon: (0, import_jsx_runtime2.jsx)(BarIcon, { fill: "currentColor", className: "oui-w-5 oui-h-5" }),
            label: t("tradingView.lineType.bars"),
            value: "0"
          },
          {
            icon: (0, import_jsx_runtime2.jsx)(CandlesIcon, { fill: "currentColor", className: "oui-w-5 oui-h-5" }),
            label: t("tradingView.lineType.candles"),
            value: "1"
          },
          {
            icon: (0, import_jsx_runtime2.jsx)(HollowCandlesIcon, { fill: "currentColor", className: "oui-w-5 oui-h-5" }),
            label: t("tradingView.lineType.hollowCandles"),
            value: "9"
          },
          {
            icon: (0, import_jsx_runtime2.jsx)(LineIcon, { fill: "currentColor", className: "oui-w-5 oui-h-5" }),
            label: t("tradingView.lineType.line"),
            value: "2"
          },
          {
            icon: (0, import_jsx_runtime2.jsx)(AreaIcon, { fill: "currentColor", className: "oui-w-5 oui-h-5" }),
            label: t("tradingView.lineType.area"),
            value: "3"
          },
          {
            icon: (0, import_jsx_runtime2.jsx)(BaseLineIcon, { fill: "currentColor", className: "oui-w-5 oui-h-5" }),
            label: t("tradingView.lineType.baseline"),
            value: "10"
          }
        ];
      }, [t]);
      const currentLineTypeIcon = (0, import_react2.useMemo)(() => {
        const data = lineTypeList.find((item) => item.value === props.lineType);
        if (data) {
          return data.icon;
        }
        return lineTypeList[1].icon;
      }, [props.lineType]);
      return (0, import_jsx_runtime2.jsxs)(DropdownMenuRoot, { open: open2, onOpenChange: setOpen, children: [
        (0, import_jsx_runtime2.jsx)(DropdownMenuTrigger, { asChild: true, children: (0, import_jsx_runtime2.jsx)(
          Box,
          {
            className: M(
              "oui-w-[18px] oui-h-[18px] oui-cursor-pointer oui-text-base-contrast-36 hover:oui-text-base-contrast-80",
              open2 && "oui-text-base-contrast-80"
            ),
            children: currentLineTypeIcon
          }
        ) }),
        (0, import_jsx_runtime2.jsx)(DropdownMenuPortal, { children: (0, import_jsx_runtime2.jsx)(
          DropdownMenuContent,
          {
            onCloseAutoFocus: (e) => e.preventDefault(),
            onClick: (e) => e.stopPropagation(),
            align: "start",
            sideOffset: 20,
            className: "oui-bg-base-8",
            children: (0, import_jsx_runtime2.jsx)(
              Flex,
              {
                direction: "column",
                gap: 4,
                px: 5,
                py: 5,
                width: 240,
                justify: "start",
                itemAlign: "start",
                children: lineTypeList.map((item) => (0, import_jsx_runtime2.jsxs)(
                  Flex,
                  {
                    justify: "start",
                    itemAlign: "center",
                    gap: 2,
                    className: M(
                      "oui-text-base-contrast-36 oui-cursor-pointer oui-w-full hover:oui-text-base-contrast",
                      props.lineType === item.value && "oui-text-base-contrast"
                    ),
                    onClick: () => props.changeLineType(item.value),
                    children: [
                      item.icon,
                      (0, import_jsx_runtime2.jsx)(Text2, { className: "oui-text-sm", children: item.label })
                    ]
                  },
                  item.value
                ))
              }
            )
          }
        ) })
      ] });
    };
    lineType_default = LineType;
  }
});
var timeInterval_exports = {};
__export(timeInterval_exports, {
  MobileTimeInterval: () => MobileTimeInterval,
  TimeInterval: () => TimeInterval
});
var useMobileTimeIntervalMoreMap;
var TimeInterval;
var DesktopTimeInterval;
var MobileTimeInterval;
var DropDownTimeInterval;
var init_timeInterval = __esm({
  "src/components/timeInterval/index.tsx"() {
    init_icons();
    useMobileTimeIntervalMoreMap = () => {
      const { t } = useTranslation();
      const mobileTimeIntervalMoreMap = (0, import_react2.useMemo)(() => {
        return [
          [
            {
              value: "3",
              label: t("tradingView.timeInterval.3m")
            },
            {
              value: "5",
              label: t("tradingView.timeInterval.5m")
            },
            {
              value: "30",
              label: t("tradingView.timeInterval.30m")
            },
            {
              value: "120",
              label: t("tradingView.timeInterval.2h")
            }
          ],
          [
            {
              value: "360",
              label: t("tradingView.timeInterval.6h")
            },
            {
              value: "720",
              label: t("tradingView.timeInterval.12h")
            },
            {
              value: "3d",
              label: t("tradingView.timeInterval.3d")
            },
            {
              value: "1M",
              label: t("tradingView.timeInterval.1M")
            }
          ]
        ];
      }, [t]);
      return { mobileTimeIntervalMoreMap };
    };
    TimeInterval = (props) => {
      const isMobile = useMediaQuery(MEDIA_TABLET);
      if (isMobile) {
        return (0, import_jsx_runtime2.jsx)(MobileTimeInterval, { ...props });
      }
      return (0, import_jsx_runtime2.jsx)(DesktopTimeInterval, { ...props });
    };
    DesktopTimeInterval = (props) => {
      const { t } = useTranslation();
      const timeIntervalMap = (0, import_react2.useMemo)(() => {
        return [
          {
            value: "1",
            label: t("tradingView.timeInterval.1m")
          },
          {
            value: "3",
            label: t("tradingView.timeInterval.3m")
          },
          {
            value: "5",
            label: t("tradingView.timeInterval.5m")
          },
          {
            value: "15",
            label: t("tradingView.timeInterval.15m")
          },
          {
            value: "30",
            label: t("tradingView.timeInterval.30m")
          },
          {
            value: "60",
            label: t("tradingView.timeInterval.1h")
          },
          {
            value: "240",
            label: t("tradingView.timeInterval.4h")
          },
          {
            value: "720",
            label: t("tradingView.timeInterval.12h")
          },
          {
            value: "1D",
            label: t("tradingView.timeInterval.1d")
          },
          {
            value: "1W",
            label: t("tradingView.timeInterval.1w")
          },
          {
            value: "1M",
            label: t("tradingView.timeInterval.1M")
          }
        ];
      }, [t]);
      return (0, import_jsx_runtime2.jsx)(
        "div",
        {
          className: M(
            "oui-text-2xs oui-text-base-contrast-36 oui-flex oui-gap-[2px] oui-items-center oui-mr-3 oui-font-semibold",
            "oui-overflow-hidden"
          ),
          children: timeIntervalMap.map((item) => (0, import_jsx_runtime2.jsx)(
            "div",
            {
              className: M(
                "oui-cursor-pointer oui-px-2",
                "hover:oui-text-base-contrast-80",
                "oui-break-normal oui-whitespace-nowrap",
                props.interval === item.value && "oui-text-base-contrast-80 oui-bg-white/[.06] oui-rounded"
              ),
              id: item.value,
              onClick: () => props.changeInterval(item.value),
              children: item.label
            },
            item.value
          ))
        }
      );
    };
    MobileTimeInterval = (props) => {
      const { t } = useTranslation();
      const mobileTimeIntervalDefaultMap = (0, import_react2.useMemo)(() => {
        return [
          {
            value: "1",
            label: t("tradingView.timeInterval.1m")
          },
          {
            value: "15",
            label: t("tradingView.timeInterval.15m")
          },
          {
            value: "60",
            label: t("tradingView.timeInterval.1h")
          },
          {
            value: "240",
            label: t("tradingView.timeInterval.4h")
          },
          {
            value: "1D",
            label: t("tradingView.timeInterval.1d")
          },
          {
            value: "1W",
            label: t("tradingView.timeInterval.1w")
          }
        ];
      }, [t]);
      const { mobileTimeIntervalMoreMap } = useMobileTimeIntervalMoreMap();
      const currentIntervalIsInExpand = (0, import_react2.useMemo)(() => {
        for (const row of mobileTimeIntervalMoreMap) {
          for (const item of row) {
            if (item.value === props.interval) {
              return item.label;
            }
          }
        }
        return null;
      }, [props.interval, mobileTimeIntervalMoreMap]);
      return (0, import_jsx_runtime2.jsxs)(
        Flex,
        {
          justify: "start",
          itemAlign: "center",
          gap: 3,
          className: M(
            "oui-text-2xs oui-text-base-contrast-36",
            "oui-overflow-hidden"
          ),
          children: [
            (0, import_jsx_runtime2.jsx)("div", { className: " oui-flex oui-gap-1 oui-items-center oui-mr-3 oui-font-semibold", children: mobileTimeIntervalDefaultMap.map((item) => (0, import_jsx_runtime2.jsx)(
              "div",
              {
                className: M(
                  "oui-px-2",
                  "oui-break-normal oui-whitespace-nowrap",
                  props.interval === item.value && "oui-text-base-contrast-80 oui-bg-white/[.06] oui-rounded"
                ),
                onClick: () => props.changeInterval(item.value),
                children: item.label
              },
              item.value
            )) }),
            (0, import_jsx_runtime2.jsx)(DropDownTimeInterval, { ...props, children: currentIntervalIsInExpand ? (0, import_jsx_runtime2.jsx)("div", { className: "oui-text-base-contrast-80", children: currentIntervalIsInExpand }) : (0, import_jsx_runtime2.jsx)(Text2, { className: "oui-break-normal oui-whitespace-nowrap", children: t("tradingView.timeInterval.more") }) })
          ]
        }
      );
    };
    DropDownTimeInterval = (props) => {
      const [open2, setOpen] = import_react2.default.useState(false);
      const { mobileTimeIntervalMoreMap } = useMobileTimeIntervalMoreMap();
      return (0, import_jsx_runtime2.jsxs)(DropdownMenuRoot, { open: open2, onOpenChange: setOpen, children: [
        (0, import_jsx_runtime2.jsx)(DropdownMenuTrigger, { asChild: true, children: (0, import_jsx_runtime2.jsxs)("div", { className: "oui-flex oui-justify-start oui-items-center oui-gap-0.5", children: [
          props.children,
          (0, import_jsx_runtime2.jsx)(
            CaretIcon,
            {
              className: M(
                "oui-w-3 oui-h-3",
                open2 && "oui-text-base-contrast-80 oui-rotate-180"
              )
            }
          )
        ] }) }),
        (0, import_jsx_runtime2.jsx)(DropdownMenuPortal, { children: (0, import_jsx_runtime2.jsx)(
          DropdownMenuContent,
          {
            onCloseAutoFocus: (e) => e.preventDefault(),
            onClick: (e) => e.stopPropagation(),
            align: "start",
            alignOffset: 0,
            sideOffset: 0,
            className: M(
              "oui-markets-dropdown-menu-content oui-bg-base-9 oui-w-screen oui-flex oui-flex-col oui-gap-2 oui-p-3"
            ),
            children: mobileTimeIntervalMoreMap.map((row, id) => (0, import_jsx_runtime2.jsx)("div", { className: "oui-flex oui-gap-2", children: row.map((item) => (0, import_jsx_runtime2.jsx)(
              "div",
              {
                className: M(
                  "oui-w-full  oui-text-2xs oui-flex oui-items-center oui-justify-center oui-h-6  oui-rounded",
                  item.value === props.interval ? "oui-text-base-contrast oui-bg-primary-darken" : "oui-text-base-contrast-36 oui-bg-base-5"
                ),
                onClick: () => {
                  props.changeInterval(item.value);
                },
                children: (0, import_jsx_runtime2.jsx)("div", { children: item.label })
              },
              item.value
            )) }, id))
          }
        ) })
      ] });
    };
  }
});
var DesktopDisplayControl;
var init_displayControl_desktop = __esm({
  "src/components/displayControl/displayControl.desktop.tsx"() {
    init_icons();
    DesktopDisplayControl = (props) => {
      const { displayControlState, changeDisplayControlState } = props;
      const [open2, setOpen] = (0, import_react2.useState)(false);
      const { t } = useTranslation();
      const displayControlMap = (0, import_react2.useMemo)(() => {
        return [
          {
            label: t("common.position"),
            id: "position"
          },
          {
            label: t("tradingView.displayControl.buySell"),
            id: "buySell"
          },
          {
            label: t("tradingView.displayControl.limitOrders"),
            id: "limitOrders"
          },
          {
            label: t("tradingView.displayControl.stopOrders"),
            id: "stopOrders"
          },
          {
            label: t("common.tpsl"),
            id: "tpsl"
          },
          {
            label: t("tpsl.positionTpsl"),
            id: "positionTpsl"
          },
          {
            label: t("orderEntry.orderType.trailingStop"),
            id: "trailingStop"
          }
        ];
      }, [t]);
      return (0, import_jsx_runtime2.jsx)(import_jsx_runtime2.Fragment, { children: (0, import_jsx_runtime2.jsxs)(DropdownMenuRoot, { open: open2, onOpenChange: setOpen, children: [
        (0, import_jsx_runtime2.jsx)(DropdownMenuTrigger, { asChild: true, children: (0, import_jsx_runtime2.jsxs)(
          Flex,
          {
            justify: "start",
            itemAlign: "center",
            className: "oui-gap-[2px] oui-cursor-pointer oui-text-base-contrast-36  hover:oui-text-base-contrast-80",
            children: [
              (0, import_jsx_runtime2.jsx)(
                DisplaySettingIcon,
                {
                  className: M(
                    "oui-w-[18px] oui-h-[18px] ",
                    open2 && "oui-text-base-contrast-80"
                  )
                }
              ),
              (0, import_jsx_runtime2.jsx)(
                CaretIcon,
                {
                  className: M(
                    "oui-w-3 oui-h-3",
                    open2 && "oui-text-base-contrast-80 oui-rotate-180"
                  )
                }
              )
            ]
          }
        ) }),
        (0, import_jsx_runtime2.jsx)(DropdownMenuPortal, { children: (0, import_jsx_runtime2.jsx)(
          DropdownMenuContent,
          {
            onCloseAutoFocus: (e) => e.preventDefault(),
            onClick: (e) => e.stopPropagation(),
            align: "start",
            className: "oui-bg-base-8",
            children: (0, import_jsx_runtime2.jsx)(
              Flex,
              {
                direction: "column",
                gap: 4,
                px: 5,
                py: 5,
                width: 240,
                justify: "start",
                itemAlign: "start",
                children: displayControlMap.map((item) => (0, import_jsx_runtime2.jsxs)(
                  Flex,
                  {
                    justify: "between",
                    itemAlign: "center",
                    className: "oui-w-full",
                    children: [
                      (0, import_jsx_runtime2.jsx)(
                        Text2,
                        {
                          className: M(
                            "oui-text-sm oui-text-base-contrast-80",
                            !displayControlState[item.id] && "oui-text-base-contrast-36"
                          ),
                          children: item.label
                        }
                      ),
                      (0, import_jsx_runtime2.jsx)(
                        Switch,
                        {
                          className: "oui-h-4 oui-w-8",
                          checked: displayControlState[item.id],
                          onCheckedChange: (checked) => {
                            changeDisplayControlState({
                              ...displayControlState,
                              [item.id]: checked
                            });
                          }
                        }
                      )
                    ]
                  },
                  item.id
                ))
              }
            )
          }
        ) })
      ] }) });
    };
  }
});
var MobileDisplayControl;
var init_displayControl_mobile = __esm({
  "src/components/displayControl/displayControl.mobile.tsx"() {
    init_icons();
    MobileDisplayControl = (props) => {
      const [open2, setOpen] = (0, import_react2.useState)(false);
      const { t } = useTranslation();
      const mobileDisplayControlMap = (0, import_react2.useMemo)(() => {
        return [
          [
            {
              label: t("common.position"),
              id: "position"
            },
            {
              label: t("tradingView.displayControl.limitOrders"),
              id: "limitOrders"
            }
          ],
          [
            {
              label: t("tradingView.displayControl.stopOrders"),
              id: "stopOrders"
            },
            {
              label: t("common.tpsl"),
              id: "tpsl"
            }
          ],
          [
            {
              label: t("tpsl.positionTpsl"),
              id: "positionTpsl"
            },
            {
              label: t("tradingView.displayControl.buySell"),
              id: "buySell"
            }
          ],
          [
            {
              label: t("orderEntry.orderType.trailingStop"),
              id: "trailingStop"
            },
            // placeholder
            {}
          ]
        ];
      }, [t]);
      return (0, import_jsx_runtime2.jsxs)(DropdownMenuRoot, { open: open2, onOpenChange: setOpen, children: [
        (0, import_jsx_runtime2.jsx)(DropdownMenuTrigger, { asChild: true, children: (0, import_jsx_runtime2.jsxs)(
          "div",
          {
            className: M(
              "oui-flex oui-items-center oui-justify-center oui-gap-0.5 oui-text-base-contrast-36",
              open2 && "oui-text-base-contrast-8"
            ),
            children: [
              (0, import_jsx_runtime2.jsx)(
                DisplaySettingIcon,
                {
                  className: M(
                    "oui-size-[18px] ",
                    open2 && "oui-text-base-contrast-80"
                  )
                }
              ),
              (0, import_jsx_runtime2.jsx)(
                CaretIcon,
                {
                  className: M(
                    "oui-size-3",
                    open2 && "oui-rotate-180 oui-text-base-contrast-80"
                  )
                }
              )
            ]
          }
        ) }),
        (0, import_jsx_runtime2.jsx)(DropdownMenuPortal, { children: (0, import_jsx_runtime2.jsx)(
          DropdownMenuContent,
          {
            onCloseAutoFocus: (e) => e.preventDefault(),
            onClick: (e) => e.stopPropagation(),
            align: "start",
            alignOffset: 0,
            sideOffset: 0,
            className: M(
              "oui-tradingview-display-control-dropdown-menu-content oui-flex oui-w-screen oui-flex-col oui-gap-2 oui-bg-base-9 oui-p-3"
            ),
            children: mobileDisplayControlMap.map((row, id) => (0, import_jsx_runtime2.jsx)("div", { className: "oui-flex oui-gap-2", children: row.map((item, index) => {
              return (0, import_jsx_runtime2.jsx)(
                "div",
                {
                  className: M(
                    "oui-flex oui-h-6 oui-w-full oui-items-center oui-justify-between",
                    "oui-rounded oui-px-2 oui-text-2xs",
                    item.id && "oui-bg-base-5",
                    props.displayControlState[item.id] ? "oui-text-base-contrast" : "oui-text-base-contrast-36"
                  ),
                  onClick: () => {
                    if (!item.id) {
                      return;
                    }
                    props.changeDisplayControlState({
                      ...props.displayControlState,
                      [item.id]: !props.displayControlState[item.id]
                    });
                  },
                  children: item.id && (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
                    (0, import_jsx_runtime2.jsx)("div", { children: item.label }),
                    props.displayControlState[item.id] ? (0, import_jsx_runtime2.jsx)(SelectedIcon, { className: "oui-size-3" }) : (0, import_jsx_runtime2.jsx)(UnSelectIcon, { className: "oui-size-3" })
                  ] })
                },
                item.id || index
              );
            }) }, id))
          }
        ) })
      ] });
    };
  }
});
var displayControl_exports = {};
__export(displayControl_exports, {
  DesktopDisplayControl: () => DesktopDisplayControl,
  MobileDisplayControl: () => MobileDisplayControl
});
var init_displayControl = __esm({
  "src/components/displayControl/index.tsx"() {
    init_displayControl_desktop();
    init_displayControl_mobile();
  }
});
var upColor = "#008676";
var downColor = "#D92D6B";
var chartBG = "#131519";
var pnlUpColor = "#00B49E";
var pnlDownColor = "#FF447C";
var pnlZoreColor = "#333948";
var textColor = "#FFFFFF";
var qtyTextColor = "#F4F7F9";
var font = "regular 11px Manrope";
var defaultColorConfig = {
  upColor,
  downColor,
  chartBG,
  pnlUpColor,
  pnlDownColor,
  pnlZoreColor,
  textColor,
  qtyTextColor,
  font,
  volumeUpColor: "#0C3E3A",
  volumeDownColor: "#5A1E36",
  closeIcon: "rgba(255, 255, 255, 0.8)"
};
var getOveriides = (colorConfig, isMobile) => {
  const overrides = {
    "paneProperties.background": colorConfig.chartBG,
    // "paneProperties.background": "#ffff00",
    // "mainSeriesProperties.style": 1,
    "paneProperties.backgroundType": "solid",
    // "paneProperties.background": "#151822",
    "mainSeriesProperties.candleStyle.upColor": colorConfig.upColor,
    "mainSeriesProperties.candleStyle.downColor": colorConfig.downColor,
    "mainSeriesProperties.candleStyle.borderColor": colorConfig.upColor,
    "mainSeriesProperties.candleStyle.borderUpColor": colorConfig.upColor,
    "mainSeriesProperties.candleStyle.borderDownColor": colorConfig.downColor,
    "mainSeriesProperties.candleStyle.wickUpColor": colorConfig.upColor,
    "mainSeriesProperties.candleStyle.wickDownColor": colorConfig.downColor,
    "paneProperties.separatorColor": "#2B2833",
    "paneProperties.vertGridProperties.color": "#26232F",
    "paneProperties.horzGridProperties.color": "#26232F",
    "scalesProperties.fontSize": isMobile ? 8 : 12,
    "scalesProperties.textColor": "#97969B",
    "paneProperties.legendProperties.showSeriesTitle": isMobile ? false : true,
    "mainSeriesProperties.statusViewStyle.symbolTextSource": "ticker"
  };
  const studiesOverrides = {
    "volume.volume.color.0": colorConfig.volumeDownColor,
    "volume.volume.color.1": colorConfig.volumeUpColor
  };
  return {
    overrides,
    studiesOverrides
  };
};
var EXCHANGE = "Orderly";
var withoutExchangePrefix = (symbol) => symbol.includes(":") ? symbol.split(":")[1] : symbol;
var withExchangePrefix = (symbol) => symbol.startsWith(`${EXCHANGE}:`) ? symbol : `${EXCHANGE}:${symbol}`;
var getBrokerAdapter = (host, broker) => {
  let _symbolInfo;
  const getOrderCombinationType = (orderType) => {
    return [
      null,
      "limit",
      "MARKET",
      "STOP_MARKET",
      "STOP_LIMIT"
      /* STOP_LIMIT */
    ][orderType];
  };
  return {
    symbolInfo: async (symbol2) => {
      _symbolInfo = broker.getSymbolInfo(withoutExchangePrefix(symbol2));
      return {
        qty: {
          min: _symbolInfo?.baseMin ?? 0,
          max: _symbolInfo?.baseMax ?? 0,
          step: _symbolInfo?.baseTick ?? 0
        },
        pipValue: 0,
        pipSize: _symbolInfo?.quoteTick ?? 0,
        minTick: _symbolInfo?.quoteTick ?? 0,
        description: ""
      };
    },
    placeOrder: async (order) => {
      const side = [
        "SELL",
        "BUY"
        /* BUY */
      ][+(order.side > 0)];
      const quantity = order.qty.toString();
      (order.limitPrice ?? 0).toString();
      (order.stopPrice ?? 0).toString();
      const symbol2 = withoutExchangePrefix(order.symbol);
      const orderCombinationType = getOrderCombinationType(order.type);
      if (orderCombinationType === "MARKET") {
        broker.sendMarketOrder({ side, order_quantity: quantity, symbol: symbol2, order_type: OrderType.MARKET });
      }
    },
    orders: () => [],
    positions: () => [],
    executions: () => [],
    connectionStatus: () => 1,
    chartContextMenuActions: (context) => host.defaultContextMenuActions(context),
    isTradable: async () => true,
    accountManagerInfo: () => ({}),
    currentAccount: () => "1",
    accountsMetainfo: async () => [{ id: "1" }],
    remove: () => host?.silentOrdersPlacement().unsubscribe()
  };
};
var getBrokerAdapter_default = getBrokerAdapter;
function logMessage(message) {
}
function getErrorMessage(error) {
  if (error === void 0) {
    return "";
  }
  if (typeof error === "string") {
    return error;
  }
  return error.message;
}
var HISTORY_PATH = "tv/history";
var KLINE_HISTORY_PATH = "v1/tv/kline_history";
var _HistoryProvider = class _HistoryProvider2 {
  constructor(datafeedUrl, requester, limitedServerResponse) {
    this._klinePreference = /* @__PURE__ */ new Map();
    this._datafeedUrl = datafeedUrl;
    this._requester = requester;
    this._limitedServerResponse = limitedServerResponse;
  }
  /**
   * Build request parameters for history API calls
   * @param symbolInfo - Symbol information
   * @param resolution - Resolution string
   * @param periodParams - Period parameters with optional countback
   * @returns Request parameters object
   */
  _buildRequestParams(symbolInfo, resolution, periodParams) {
    const requestParams = {
      symbol: symbolInfo.ticker || "",
      resolution,
      from: periodParams.from,
      to: periodParams.to
    };
    const countBack = Math.min(periodParams.countBack ?? 0, 1e3);
    if (periodParams.countBack !== void 0) {
      requestParams.countback = countBack;
    }
    if (symbolInfo.currency_code !== void 0) {
      requestParams.currencyCode = symbolInfo.currency_code;
    }
    if (symbolInfo.unit_id !== void 0) {
      requestParams.unitId = symbolInfo.unit_id;
    }
    return requestParams;
  }
  getBars(symbolInfo, resolution, periodParams) {
    const requestParams = this._buildRequestParams(
      symbolInfo,
      resolution,
      periodParams
    );
    const preferenceKey = this._getPreferenceKey(symbolInfo, resolution);
    const prefersKline = this._klinePreference.get(preferenceKey) === true;
    const countBack = Math.min(periodParams.countBack ?? 0, 1e3);
    return new Promise(
      async (resolve, reject) => {
        try {
          let result;
          let usedHistoryResult = false;
          if (prefersKline) {
            result = await this._requestKlineHistory(
              this._buildKlineParams(requestParams, countBack)
            );
          } else {
            const initialResponse = await this._requestHistory(requestParams);
            result = this._processHistoryResponse(initialResponse);
            usedHistoryResult = true;
            const needsFallback = this._shouldFallbackToKline(
              initialResponse,
              countBack
            );
            if (needsFallback) {
              const earliestTime = this._getEarliestTime(result.bars);
              if (earliestTime !== null) {
                const remainingCountBack = Math.max(
                  0,
                  countBack - result.bars.length
                );
                const mergedResult = await this._tryKlineFallbackWithMerge(
                  requestParams,
                  remainingCountBack,
                  earliestTime,
                  result
                );
                if (mergedResult !== null) {
                  result = mergedResult;
                  usedHistoryResult = false;
                  this._klinePreference.set(preferenceKey, true);
                } else {
                  this._klinePreference.set(preferenceKey, false);
                }
              } else {
                const klineResult = await this._tryKlineFallback(
                  requestParams,
                  countBack
                );
                if (klineResult !== null) {
                  result = klineResult;
                  usedHistoryResult = false;
                  this._klinePreference.set(preferenceKey, true);
                } else {
                  this._klinePreference.set(preferenceKey, false);
                }
              }
            } else {
              this._klinePreference.set(preferenceKey, false);
            }
          }
          if (usedHistoryResult && this._limitedServerResponse) {
            await this._processTruncatedResponse(result, { ...requestParams });
          }
          resolve(result);
        } catch (e) {
          const error = e instanceof Error ? e : typeof e === "string" ? e : void 0;
          const reasonString = getErrorMessage(error);
          reject(reasonString || "Error");
        }
      }
    );
  }
  /**
   * Request kline history using KLINE_HISTORY_PATH endpoint
   * Handles rate limiting (429 errors) by waiting and retrying
   * @param requestParams - Request parameters
   * @param retryCount - Current retry attempt (internal use)
   * @returns Processed history response
   */
  async _requestKlineHistory(requestParams, retryCount = 0) {
    const maxRetries = 5;
    const baseRetryDelay = 2500;
    const maxRetryDelay = 1e4;
    const params = {
      ...requestParams,
      resolution: this._mapToKlineHistoryResolution(
        requestParams.resolution
      )
    };
    const urlPath = this._buildUrlWithParams(KLINE_HISTORY_PATH, params);
    const options2 = { credentials: "same-origin" };
    const requesterWithHeaders = this._requester;
    if (requesterWithHeaders._headers !== void 0) {
      options2.headers = requesterWithHeaders._headers;
    }
    let response;
    try {
      response = await fetch(`${this._datafeedUrl}/${urlPath}`, options2);
    } catch (error) {
      throw error;
    }
    if (response.status === 429) {
      if (retryCount >= maxRetries) {
        throw new Error(
          `Rate limit exceeded: Maximum retry attempts (${maxRetries}) reached`
        );
      }
      const retryDelay = Math.min(
        baseRetryDelay * Math.pow(2, retryCount),
        maxRetryDelay
      );
      const retryAfter = response.headers.get("Retry-After");
      const delay = retryAfter ? parseInt(retryAfter, 10) * 1e3 : retryDelay;
      await new Promise((resolve) => setTimeout(resolve, delay));
      return this._requestKlineHistory(requestParams, retryCount + 1);
    }
    if (!response.ok) {
      const errorData = await response.json().catch(() => ({
        message: response.statusText
      }));
      throw new Error(
        errorData.message || errorData.errmsg || response.statusText
      );
    }
    let data;
    try {
      data = await response.json();
    } catch {
      throw new Error("Failed to parse response JSON");
    }
    return this._processHistoryResponse(data);
  }
  /**
   * Process truncated response by making follow-up requests if needed
   * @param result - Current result with bars
   * @param requestParams - Request parameters
   */
  async _processTruncatedResponse(result, requestParams) {
    let lastResultLength = result.bars.length;
    try {
      while (this._limitedServerResponse && this._limitedServerResponse.maxResponseLength > 0 && this._limitedServerResponse.maxResponseLength === lastResultLength && requestParams.from < requestParams.to) {
        if (requestParams.countback) {
          requestParams.countback = requestParams.countback - lastResultLength;
        }
        if (this._limitedServerResponse.expectedOrder === "earliestFirst") {
          requestParams.from = Math.round(
            result.bars[result.bars.length - 1].time / 1e3
          );
        } else {
          requestParams.to = Math.round(result.bars[0].time / 1e3);
        }
        const followupResponse = await this._requester.sendRequest(
          this._datafeedUrl,
          HISTORY_PATH,
          requestParams
        );
        const followupResult = this._processHistoryResponse(followupResponse);
        lastResultLength = followupResult.bars.length;
        if (this._limitedServerResponse.expectedOrder === "earliestFirst") {
          if (followupResult.bars[0].time === result.bars[result.bars.length - 1].time) {
            followupResult.bars.shift();
          }
          result.bars.push(...followupResult.bars);
        } else {
          if (followupResult.bars[followupResult.bars.length - 1].time === result.bars[0].time) {
            followupResult.bars.pop();
          }
          result.bars.unshift(...followupResult.bars);
        }
      }
    } catch (e) {
      if (e instanceof Error || typeof e === "string") {
        getErrorMessage(e);
      }
    }
  }
  _processHistoryResponse(response) {
    if (response.s !== "ok" && response.s !== "no_data") {
      throw new Error(response.errmsg);
    }
    const bars = [];
    const meta = {
      noData: false
    };
    if (response.s === "no_data") {
      meta.noData = true;
      meta.nextTime = response.nextTime;
    } else {
      const volumePresent = response.v !== void 0;
      const ohlPresent = response.o !== void 0;
      for (let i = 0; i < response.t.length; ++i) {
        const barValue = {
          time: response.t[i] * 1e3,
          close: parseFloat(response.c[i]),
          open: parseFloat(response.c[i]),
          high: parseFloat(response.c[i]),
          low: parseFloat(response.c[i])
        };
        if (ohlPresent) {
          barValue.open = parseFloat(
            response.o[i]
          );
          barValue.high = parseFloat(
            response.h[i]
          );
          barValue.low = parseFloat(response.l[i]);
        }
        if (volumePresent) {
          const volume = parseFloat(response.v[i]);
          if (volume > 0) {
            barValue.volume = volume;
          } else {
            barValue.volume = 1e-10;
          }
        }
        bars.push(barValue);
      }
    }
    return {
      bars,
      meta
    };
  }
  /**
   * Maps TradingView resolution format to Kline History API resolution format
   * @param resolution - TradingView resolution string (e.g., "1", "60", "1D")
   * @returns Kline History API resolution string (e.g., "1m", "1h", "1d")
   */
  _mapToKlineHistoryResolution(resolution) {
    return _HistoryProvider2._RESOLUTION_MAP.get(resolution) ?? resolution;
  }
  _buildUrlWithParams(path, params) {
    if (!params || Object.keys(params).length === 0) {
      return path;
    }
    const searchParams = new URLSearchParams();
    Object.keys(params).forEach((key2) => {
      const value = params[key2];
      if (Array.isArray(value)) {
        value.forEach((item) => searchParams.append(key2, item));
      } else {
        searchParams.append(key2, value.toString());
      }
    });
    const queryString = searchParams.toString();
    return queryString ? `${path}?${queryString}` : path;
  }
  _getPreferenceKey(symbolInfo, resolution) {
    return `${symbolInfo.ticker ?? ""}|${resolution}`;
  }
  _requestHistory(requestParams) {
    return this._requester.sendRequest(
      this._datafeedUrl,
      HISTORY_PATH,
      requestParams
    );
  }
  _buildKlineParams(requestParams, countBack) {
    const params = {
      ...requestParams
    };
    delete params.countback;
    if (countBack > 0) {
      params.limit = countBack;
    } else {
      delete params.limit;
    }
    return params;
  }
  /**
   * Get the earliest time from bars array
   * @param bars - Array of bars
   * @returns Earliest timestamp in seconds, or null if bars array is empty
   */
  _getEarliestTime(bars) {
    if (bars.length === 0) {
      return null;
    }
    return bars[0].time / 1e3;
  }
  /**
   * Try kline fallback with merge logic
   * Requests kline data from the earliest time in history result to the original end time,
   * then merges the kline data with history data
   * @param requestParams - Original request parameters
   * @param countBack - Count back value
   * @param earliestTime - Earliest time from history result (in seconds)
   * @param historyResult - History result to merge with
   * @returns Merged result or null if kline request fails
   */
  async _tryKlineFallbackWithMerge(requestParams, countBack, earliestTime, historyResult) {
    try {
      const klineParams = {
        ...requestParams,
        from: requestParams.from,
        to: earliestTime
      };
      const klineResult = await this._requestKlineHistory(
        this._buildKlineParams(klineParams, countBack)
      );
      if (klineResult.bars.length === 0) {
        return null;
      }
      return this._mergeBars(historyResult, klineResult);
    } catch {
      return null;
    }
  }
  /**
   * Merge history bars with kline bars
   * Optimized for the case where klineBars are earlier data than historyBars
   * Uses direct concatenation when no overlap, otherwise uses two-pointer merge
   * Assumes both arrays are sorted by time (ascending, earliest first)
   * Kline data takes precedence when timestamps match
   * @param historyResult - History result (later time range)
   * @param klineResult - Kline result (earlier time range)
   * @returns Merged result with sorted bars
   */
  _mergeBars(historyResult, klineResult) {
    const historyBars = historyResult.bars;
    const klineBars = klineResult.bars;
    if (historyBars.length === 0) {
      return klineResult;
    }
    if (klineBars.length === 0) {
      return historyResult;
    }
    const latestKlineTime = klineBars[klineBars.length - 1].time;
    const earliestHistoryTime = historyBars[0].time;
    let mergedBars;
    if (latestKlineTime < earliestHistoryTime) {
      mergedBars = [...klineBars, ...historyBars];
    } else if (latestKlineTime === earliestHistoryTime) {
      mergedBars = [...klineBars, ...historyBars.slice(1)];
    } else {
      mergedBars = [];
      let historyIdx = 0;
      let klineIdx = 0;
      while (historyIdx < historyBars.length && klineIdx < klineBars.length) {
        const historyBar = historyBars[historyIdx];
        const klineBar = klineBars[klineIdx];
        if (historyBar.time < klineBar.time) {
          mergedBars.push(historyBar);
          historyIdx++;
        } else if (historyBar.time > klineBar.time) {
          mergedBars.push(klineBar);
          klineIdx++;
        } else {
          mergedBars.push(klineBar);
          historyIdx++;
          klineIdx++;
        }
      }
      while (historyIdx < historyBars.length) {
        mergedBars.push(historyBars[historyIdx++]);
      }
      while (klineIdx < klineBars.length) {
        mergedBars.push(klineBars[klineIdx++]);
      }
    }
    const meta = {
      noData: klineResult.meta.noData && historyResult.meta.noData,
      nextTime: klineResult.meta.nextTime ?? historyResult.meta.nextTime
    };
    return {
      bars: mergedBars,
      meta
    };
  }
  async _tryKlineFallback(requestParams, countBack) {
    try {
      const result = await this._requestKlineHistory(
        this._buildKlineParams(requestParams, countBack)
      );
      return result.bars.length > 0 ? result : null;
    } catch {
      return null;
    }
  }
  _shouldFallbackToKline(response, expectedCount) {
    if (response.s !== "ok") {
      return false;
    }
    const barsCount = response.t.length;
    if (expectedCount > 0 && barsCount < expectedCount) {
      return true;
    }
    if (this._limitedServerResponse && this._limitedServerResponse.maxResponseLength > 0 && barsCount >= this._limitedServerResponse.maxResponseLength) {
      return true;
    }
    return false;
  }
};
_HistoryProvider._RESOLUTION_MAP = /* @__PURE__ */ new Map([
  ["1", "1m"],
  // 1 minute
  ["3", "3m"],
  // 3 minutes
  ["5", "5m"],
  // 5 minutes
  ["15", "15m"],
  // 15 minutes
  ["30", "30m"],
  // 30 minutes
  ["60", "1h"],
  // 1 hour
  ["240", "4h"],
  // 4 hours
  ["720", "12h"],
  // 12 hours
  ["1D", "1d"],
  // 1 day
  ["1W", "1w"],
  // 1 week
  ["1M", "1mon"]
  // 1 month (mapped to 1m)
]);
var HistoryProvider = _HistoryProvider;
var Requester = class {
  constructor(headers) {
    if (headers) {
      this._headers = headers;
    }
  }
  sendRequest(datafeedUrl, urlPath, params) {
    if (params !== void 0) {
      const paramKeys = Object.keys(params);
      if (paramKeys.length !== 0) {
        urlPath += "?";
      }
      urlPath += paramKeys.map((key2) => {
        return `${encodeURIComponent(key2)}=${encodeURIComponent(
          params[key2].toString()
        )}`;
      }).join("&");
    }
    const options2 = { credentials: "same-origin" };
    if (this._headers !== void 0) {
      options2.headers = this._headers;
    }
    return fetch(`${datafeedUrl}/${urlPath}`, options2).then((response) => response.json()).then((data) => {
      if (typeof data.success === "undefined") {
        return data;
      }
      if (!data.success) {
        throw new Error(data.message);
      }
      return data;
    });
  }
};
function extractField(data, field, arrayIndex, valueIsArray) {
  const value = data[field];
  if (Array.isArray(value) && (!valueIsArray || Array.isArray(value[0]))) {
    return value[arrayIndex];
  }
  return value;
}
function symbolKey(symbol, currency, unit) {
  return symbol + (currency !== void 0 ? "_%|#|%_" + currency : "") + (unit !== void 0 ? "_%|#|%_" + unit : "");
}
var SymbolsStorage = class {
  constructor(datafeedUrl, datafeedSupportedResolutions, requester) {
    this._exchangesList = ["Orderly"];
    this._symbolsInfo = {};
    this._symbolsList = [];
    this._datafeedUrl = datafeedUrl;
    this._datafeedSupportedResolutions = datafeedSupportedResolutions;
    this._requester = requester;
    this._readyPromise = this._init();
    this._readyPromise.catch((error) => {
    });
  }
  // BEWARE: this function does not consider symbol's exchange
  resolveSymbol(symbolName, currencyCode, unitId) {
    return this._readyPromise.then(() => {
      const symbolInfo = this._symbolsInfo[symbolKey(symbolName, currencyCode, unitId)];
      if (symbolInfo === void 0) {
        return Promise.reject("invalid symbol");
      }
      return Promise.resolve(symbolInfo);
    });
  }
  searchSymbols(searchString, exchange, symbolType, maxSearchResults) {
    return this._readyPromise.then(() => {
      const weightedResult = [];
      const queryIsEmpty = searchString.length === 0;
      searchString = searchString.toUpperCase();
      for (const symbolName of this._symbolsList) {
        const symbolInfo = this._symbolsInfo[symbolName];
        if (symbolInfo === void 0) {
          continue;
        }
        if (symbolType.length > 0 && symbolInfo.type !== symbolType) {
          continue;
        }
        if (exchange && exchange.length > 0 && symbolInfo.exchange !== exchange) {
          continue;
        }
        const positionInName = symbolInfo.name.toUpperCase().indexOf(searchString);
        const positionInDescription = symbolInfo.description.toUpperCase().indexOf(searchString);
        if (queryIsEmpty || positionInName >= 0 || positionInDescription >= 0) {
          const alreadyExists = weightedResult.some(
            (item) => item.symbolInfo === symbolInfo
          );
          if (!alreadyExists) {
            const weight = positionInName >= 0 ? positionInName : 8e3 + positionInDescription;
            weightedResult.push({ symbolInfo, weight });
          }
        }
      }
      const result = weightedResult.sort(
        (item1, item2) => item1.weight - item2.weight
      ).slice(0, maxSearchResults).map((item) => {
        const symbolInfo = item.symbolInfo;
        return {
          symbol: symbolInfo.name,
          full_name: `${symbolInfo.exchange}:${symbolInfo.name}`,
          description: symbolInfo.description,
          exchange: symbolInfo.exchange,
          params: [],
          type: symbolInfo.type,
          ticker: symbolInfo.name
        };
      });
      return Promise.resolve(result);
    });
  }
  _init() {
    const promises = [];
    const alreadyRequestedExchanges = {};
    for (const exchange of this._exchangesList) {
      if (alreadyRequestedExchanges[exchange]) {
        continue;
      }
      alreadyRequestedExchanges[exchange] = true;
      promises.push(this._requestExchangeData(exchange));
    }
    return Promise.all(promises).then(() => {
      this._symbolsList.sort();
    });
  }
  _requestExchangeData(exchange) {
    return new Promise(
      (resolve, reject) => {
        this._requester.sendRequest(
          this._datafeedUrl,
          "tv/symbol_info",
          {
            group: exchange
          }
        ).then((response) => {
          try {
            this._onExchangeDataReceived(exchange, response);
          } catch (error) {
            reject(
              error instanceof Error ? error : new Error(`SymbolsStorage: Unexpected exception ${error}`)
            );
            return;
          }
          resolve();
        }).catch((reason) => {
          logMessage(
            `SymbolsStorage: Request data for exchange '${exchange}' failed, reason=${getErrorMessage(
              reason
            )}`
          );
          resolve();
        });
      }
    );
  }
  _onExchangeDataReceived(exchange, data) {
    let symbolIndex = 0;
    try {
      const symbolsCount = data.symbol.length;
      const tickerPresent = data.ticker !== void 0;
      for (; symbolIndex < symbolsCount; ++symbolIndex) {
        const symbolName = data.symbol[symbolIndex];
        const listedExchange = extractField(
          data,
          "exchange-listed",
          symbolIndex
        );
        const tradedExchange = extractField(
          data,
          "exchange-traded",
          symbolIndex
        );
        const fullName = tradedExchange + ":" + symbolName;
        const currencyCode = extractField(data, "currency-code", symbolIndex);
        const unitId = extractField(data, "unit-id", symbolIndex);
        const ticker = tickerPresent ? extractField(data, "ticker", symbolIndex) : symbolName;
        const symbolInfo = {
          ticker,
          name: symbolName,
          base_name: [listedExchange + ":" + symbolName],
          listed_exchange: listedExchange,
          exchange: tradedExchange,
          currency_code: currencyCode,
          original_currency_code: extractField(
            data,
            "original-currency-code",
            symbolIndex
          ),
          unit_id: unitId,
          original_unit_id: extractField(data, "original-unit-id", symbolIndex),
          unit_conversion_types: extractField(
            data,
            "unit-conversion-types",
            symbolIndex,
            true
          ),
          description: extractField(data, "description", symbolIndex),
          has_intraday: definedValueOrDefault(
            extractField(data, "has-intraday", symbolIndex),
            false
          ),
          // show volume at the bottom by default: https://github.com/tradingview/charting_library/issues/8306
          visible_plots_set: definedValueOrDefault(
            extractField(data, "visible-plots-set", symbolIndex),
            "ohlcv"
          ),
          minmov: extractField(data, "minmovement", symbolIndex) || extractField(data, "minmov", symbolIndex) || 0,
          minmove2: extractField(data, "minmove2", symbolIndex) || extractField(data, "minmov2", symbolIndex),
          fractional: extractField(data, "fractional", symbolIndex),
          pricescale: extractField(data, "pricescale", symbolIndex),
          type: extractField(data, "type", symbolIndex),
          session: extractField(data, "session-regular", symbolIndex),
          session_holidays: extractField(data, "session-holidays", symbolIndex),
          corrections: extractField(data, "corrections", symbolIndex),
          timezone: extractField(data, "timezone", symbolIndex),
          supported_resolutions: definedValueOrDefault(
            extractField(data, "supported-resolutions", symbolIndex, true),
            this._datafeedSupportedResolutions
          ),
          has_daily: definedValueOrDefault(
            extractField(data, "has-daily", symbolIndex),
            true
          ),
          intraday_multipliers: definedValueOrDefault(
            extractField(data, "intraday-multipliers", symbolIndex, true),
            ["1", "5", "15", "30", "60"]
          ),
          // daily_multipliers:["1"],
          // has_weekly_and_monthly: extractField(
          //   data,
          //   "has-weekly-and-monthly",
          //   symbolIndex,
          // ),
          has_weekly_and_monthly: true,
          has_empty_bars: extractField(data, "has-empty-bars", symbolIndex),
          volume_precision: definedValueOrDefault(
            extractField(data, "volume-precision", symbolIndex),
            0
          ),
          format: "price"
        };
        this._symbolsInfo[ticker] = symbolInfo;
        this._symbolsInfo[symbolName] = symbolInfo;
        this._symbolsInfo[fullName] = symbolInfo;
        if (currencyCode !== void 0 || unitId !== void 0) {
          this._symbolsInfo[symbolKey(ticker, currencyCode, unitId)] = symbolInfo;
          this._symbolsInfo[symbolKey(symbolName, currencyCode, unitId)] = symbolInfo;
          this._symbolsInfo[symbolKey(fullName, currencyCode, unitId)] = symbolInfo;
        }
        this._symbolsList.push(symbolName);
      }
    } catch (error) {
      throw new Error(
        `SymbolsStorage: API error when processing exchange ${exchange} symbol #${symbolIndex} (${data.symbol[symbolIndex]}): ${Object(error).message}`
      );
    }
  }
};
function definedValueOrDefault(value, defaultValue2) {
  return value !== void 0 ? value : defaultValue2;
}
var AbstractDatafeed = class {
  constructor(datafeedURL) {
    this._configuration = defaultConfiguration();
    this._symbolsStorage = null;
    this._historyCursor = null;
    this._datafeedURL = datafeedURL;
    this._requester = new Requester();
    this._historyProvider = new HistoryProvider(datafeedURL, this._requester);
    this._configurationReadyPromise = this._requestConfiguration().then(
      (configuration) => {
        if (configuration === null) {
          configuration = defaultConfiguration();
        }
        this._setupWithConfiguration(configuration);
      }
    );
  }
  getBars(symbolInfo, resolution, periodParams, onResult, onError) {
    const { to, firstDataRequest } = periodParams;
    if (firstDataRequest || this._historyCursor === null) {
      this._historyCursor = to;
    }
    const resolutionInSeconds = this._resolutionToSeconds(resolution);
    const cursor = this._historyCursor ?? to;
    const maxBarsPerRequest = 1e3;
    const shouldUseOriginalRange = resolution.toLowerCase?.() === "1m" && periodParams.from !== void 0;
    const barsToLoad = shouldUseOriginalRange ? periodParams.countBack ?? maxBarsPerRequest : maxBarsPerRequest;
    const requestTo = shouldUseOriginalRange ? periodParams.to ?? to : cursor;
    const requestFrom = shouldUseOriginalRange ? periodParams.from : Math.max(requestTo - resolutionInSeconds * barsToLoad, 0);
    this._historyProvider.getBars(symbolInfo, resolution, {
      ...periodParams,
      countBack: barsToLoad,
      from: requestFrom,
      to: requestTo
    }).then(
      (result) => {
        if (result.bars.length > 0) {
          this._historyCursor = Math.floor(result.bars[0].time / 1e3) - 1;
        }
        onResult(result.bars, result.meta);
      },
      (error) => {
        onError(error);
      }
    ).catch((error) => {
      onError(error);
    });
  }
  _resolutionToSeconds(resolution) {
    switch (resolution) {
      case "1D":
        return 86400;
      case "3D":
        return 86400 * 3;
      case "1W":
        return 86400 * 7;
      case "1M":
        return 86400 * 30;
    }
    const res = parseInt(resolution);
    if (!isNaN(res)) {
      return res * 60;
    }
    return 60;
  }
  onReady(callback) {
    this._configurationReadyPromise.then(() => {
      callback(this._configuration);
    });
  }
  searchSymbols(userInput, exchange, symbolType, onResult) {
    if (this._symbolsStorage === null) {
      throw new Error("Datafeed: inconsistent configuration (symbols storage)");
    }
    this._symbolsStorage.searchSymbols(
      userInput,
      exchange,
      symbolType,
      30
      /* SearchItemsLimit */
    ).then(onResult).catch(onResult.bind(null, []));
  }
  resolveSymbol(symbolName, onResolve, onError, extension) {
    const currencyCode = extension && extension.currencyCode;
    const unitId = extension && extension.unitId;
    function onResultReady(symbolInfo) {
      onResolve(symbolInfo);
    }
    if (this._symbolsStorage === null) {
      throw new Error("Datafeed: inconsistent configuration (symbols storage)");
    }
    this._symbolsStorage.resolveSymbol(symbolName, currencyCode, unitId).then(onResultReady).catch(onError);
  }
  getMarks() {
  }
  getTimescaleMarks() {
  }
  getServerTime() {
  }
  _requestConfiguration() {
    return this._send("tv/config").catch(
      (reason) => {
        logMessage(
          `Datafeed: Cannot get datafeed configuration - use default, error=${getErrorMessage(
            reason
          )}`
        );
        return null;
      }
    );
  }
  _send(urlPath, params) {
    return this._requester.sendRequest(this._datafeedURL, urlPath, params);
  }
  _setupWithConfiguration(configurationData) {
    this._configuration = configurationData;
    if (configurationData.exchanges === void 0) {
      configurationData.exchanges = [];
    }
    if (!configurationData.supports_search && !configurationData.supports_group_request) {
      throw new Error(
        "Unsupported datafeed configuration. Must either support search, or support group request"
      );
    }
    if (configurationData.supports_group_request || !configurationData.supports_search) {
      this._symbolsStorage = new SymbolsStorage(
        this._datafeedURL,
        configurationData.supported_resolutions || [],
        this._requester
      );
    }
    logMessage(
      `Datafeed: Initialized with ${JSON.stringify(configurationData)}`
    );
  }
};
function defaultConfiguration() {
  return {
    supports_search: false,
    supports_group_request: true,
    supported_resolutions: [
      "1",
      "3",
      "5",
      "15",
      "30",
      "60",
      "120",
      "240",
      "480",
      "720",
      "1D",
      "3D",
      "1W",
      "1M"
    ],
    supports_marks: false,
    supports_timescale_marks: false
  };
}
var MultiBroadcastEventBus = class {
  constructor() {
    this.subscribers = /* @__PURE__ */ new Map();
  }
  subscribe(eventName, callback) {
    if (!this.subscribers.has(eventName)) {
      this.subscribers.set(eventName, []);
    }
    const callbacks = this.subscribers.get(eventName);
    callbacks.push(callback);
    return () => {
      this.unsubscribe(eventName, callback);
    };
  }
  unsubscribe(eventName, callback) {
    if (this.subscribers.has(eventName)) {
      const callbacks = this.subscribers.get(eventName);
      this.subscribers.set(
        eventName,
        callbacks.filter((cb) => cb !== callback)
      );
      if (this.subscribers.get(eventName).length === 0) {
        this.subscribers.delete(eventName);
      }
    }
  }
  publish(eventName, data) {
    if (this.subscribers.has(eventName)) {
      const callbacks = this.subscribers.get(eventName);
      callbacks.forEach((callback) => {
        callback(data);
      });
    }
  }
};
var mapResolution = (resolution) => {
  let time = "1d";
  switch (resolution) {
    case "1":
      time = "1m";
      break;
    case "3":
      time = "3m";
      break;
    case "5":
      time = "5m";
      break;
    case "15":
      time = "15m";
      break;
    case "30":
      time = "30m";
      break;
    case "60":
      time = "1h";
      break;
    case "120":
      time = "2h";
      break;
    case "240":
      time = "4h";
      break;
    case "480":
      time = "8h";
      break;
    case "720":
      time = "12h";
      break;
    case "D":
    case "1D":
      time = "1d";
      break;
    case "3D":
      time = "3d";
      break;
    case "1W":
      time = "1w";
      break;
    case "1M":
      time = "1M ";
      break;
  }
  return time;
};
var TradingViewSDKLocalstorageKey = {
  interval: "TradingviewSDK.lastUsedTimeBasedResolution",
  lineType: "TradingviewSDK.lastUsedStyle",
  displayControlSetting: "TradingviewSDK.displaySetting"
};
var getKlineKey = (symbol, type) => `${symbol}kline_${type}`;
var getSymbolTopics = (symbol) => {
  const symbolTopics = ["trade"];
  return symbolTopics.map((topic) => `${symbol}@${topic}`);
};
var _WebsocketService = class _WebsocketService2 {
  constructor(ws) {
    this.klineSubscribeIdMap = /* @__PURE__ */ new Map();
    this.klineOnTickCallback = /* @__PURE__ */ new Map();
    this.subscribeCachedTopics = /* @__PURE__ */ new Map();
    this.wsInstance = null;
    this.klineData = /* @__PURE__ */ new Map();
    if (!_WebsocketService2._created) {
      this.wsInstance = ws;
      _WebsocketService2._instance = this;
      _WebsocketService2._created = true;
    }
    return _WebsocketService2._instance;
  }
  subscribeKline(subscribeId, symbol, resolution, onTickCallback) {
    const time = mapResolution(resolution);
    this.klineSubscribeIdMap.set(subscribeId, { symbol, resolution });
    const klineKey = getKlineKey(symbol, time);
    if (this.klineOnTickCallback.has(klineKey)) {
      const value = this.klineOnTickCallback.get(klineKey);
      value[subscribeId] = onTickCallback;
    } else {
      this.klineOnTickCallback.set(klineKey, {
        [subscribeId]: onTickCallback
      });
      const unsub = this.wsInstance?.subscribe(
        {
          event: "subscribe",
          topic: `${symbol}@kline_${time}`,
          id: `${symbol}@kline_${time}`,
          ts: (/* @__PURE__ */ new Date()).getTime()
        },
        {
          onMessage: (data) => {
            const { open: open2, close, high, low, volume, startTime } = data;
            const key2 = getKlineKey(data.symbol, data.type);
            this.updateKline(key2, {
              time: startTime,
              close,
              open: open2,
              high,
              low,
              volume
            });
          }
        }
      );
      this.subscribeCachedTopics.set(`${symbol}@kline_${time}`, unsub);
    }
  }
  unsubscribeKline(subscribeId) {
    if (!this.klineSubscribeIdMap.has(subscribeId)) {
      return;
    }
    const { symbol, resolution } = this.klineSubscribeIdMap.get(subscribeId);
    const time = mapResolution(resolution);
    const klineKey = getKlineKey(symbol, time);
    if (this.klineOnTickCallback.has(klineKey)) {
      const value = this.klineOnTickCallback.get(klineKey);
      delete value[subscribeId];
      if (Object.keys(value).length === 0) {
        this.klineOnTickCallback.delete(klineKey);
        const unsub = this.subscribeCachedTopics.get(`${symbol}@kline_${time}`);
        unsub();
      }
    }
    delete this.klineSubscribeIdMap[subscribeId];
  }
  subscribeSymbol(symbol) {
    const symbolTopics = getSymbolTopics(symbol);
    symbolTopics.forEach((topic) => {
      if (!this.subscribeCachedTopics.has(topic)) {
        const unsub = this.wsInstance?.subscribe(
          {
            event: "subscribe",
            topic,
            id: topic,
            ts: (/* @__PURE__ */ new Date()).getTime()
          },
          {
            onMessage: (data) => {
              this.updateKlineByLastPrice(data.symbol, data.price);
            }
          }
        );
        this.subscribeCachedTopics.set(topic, unsub);
      }
    });
  }
  updateKlineByLastPrice(symbol, lastPrice) {
    this.klineOnTickCallback.forEach((_, key2) => {
      if (key2.startsWith(symbol)) {
        const klineData = this.klineData.get(key2);
        if (klineData) {
          this.updateKline(key2, { ...klineData, close: lastPrice });
        }
      }
    });
  }
  updateKline(key2, cbParams) {
    const onTickCbs = this.klineOnTickCallback.get(key2);
    if (onTickCbs && cbParams) {
      this.klineData.set(key2, cbParams);
      Object.keys(onTickCbs).forEach((key22) => {
        const onTickCb = onTickCbs[key22];
        if (onTickCb && typeof onTickCb === "function") {
          onTickCb(cbParams);
        }
      });
    }
  }
};
_WebsocketService._created = false;
_WebsocketService._instance = null;
var WebsocketService = _WebsocketService;
var getAutoIncrementId = /* @__PURE__ */ (() => {
  let id = 0;
  return () => id++;
})();
var Datafeed = class extends AbstractDatafeed {
  constructor(apiUrl, ws) {
    const datafeedURL = `${apiUrl}`;
    super(datafeedURL);
    this.bbosMap = /* @__PURE__ */ new Map();
    this.tickersMap = /* @__PURE__ */ new Map();
    this.eventBus = new MultiBroadcastEventBus();
    this._subscribeQuoteMap = /* @__PURE__ */ new Map();
    this._prefixId = getAutoIncrementId();
    this._publicWs = new WebsocketService(ws);
    this.bbosMap = /* @__PURE__ */ new Map();
    ws.on("tickers", (message) => {
      for (const ticker of message.data) {
        ticker.change = ticker.close - ticker.open;
        ticker.perChange = ticker.open ? +(100 * ticker.change / ticker.open).toFixed(2) : 0;
        this.tickersMap.set(ticker.symbol, ticker);
      }
      this.eventBus.publish("tickerUpdate", { message: "ticker" });
    });
    ws.subscribe(
      {
        event: "subscribe",
        topic: `bbos`
      },
      {
        formatter: (message) => message,
        onMessage: (message) => {
          for (const bbo of message.data) {
            this.bbosMap.set(bbo.symbol, {
              ask: bbo.ask,
              bid: bbo.bid,
              askSize: bbo.askSize,
              bidSize: bbo.bidSize
            });
          }
          this.eventBus.publish("tickerUpdate", { message: "bbos" });
        }
      }
    );
  }
  remove() {
    Array.from(this._subscribeQuoteMap.values()).forEach((s) => s?.());
  }
  getSubscriptionId(listenerGuid) {
    return `${this._prefixId}${listenerGuid}`;
  }
  subscribeBars(symbolInfo, resolution, onTick, listenerGuid, onResetCacheNeededCallback) {
    window.onResetCacheNeededCallback = onResetCacheNeededCallback;
    this._publicWs.subscribeKline(
      `${this._prefixId}${listenerGuid}`,
      symbolInfo.ticker,
      resolution,
      onTick
    );
  }
  unsubscribeBars(listenerGuid) {
    this._publicWs.unsubscribeKline(`${this._prefixId}${listenerGuid}`);
  }
  getQuotes(symbols, onDataCallback) {
    const subscriptionId = this.getSubscriptionId("getQuotes");
    this.unsubscribeQuotes("getQuotes");
    const unsub = this.eventBus.subscribe("tickerUpdate", (msg) => {
      const dataMap = /* @__PURE__ */ new Map();
      symbols.forEach((symbol) => {
        const bbo = this.bbosMap.get(withoutExchangePrefix(symbol));
        const ticker = this.tickersMap.get(withoutExchangePrefix(symbol));
        if (!bbo || !ticker) {
          return;
        }
        const data = {
          ...ticker,
          ask: bbo.ask,
          bid: bbo.bid
        };
        dataMap.set(withoutExchangePrefix(symbol), data);
      });
      if (!dataMap.size) {
        return;
      }
      onDataCallback(
        Array.from(dataMap.values()).map(
          (symbolData) => this._toUDFTicker(symbolData)
        )
      );
    });
    this._subscribeQuoteMap.set(subscriptionId, unsub);
  }
  subscribeQuotes(symbols, fastSymbols, onRealtimeCallback, listenerGuid) {
    const subscriptionId = `${this._prefixId}${listenerGuid}`;
    if (symbols.length > 0) {
      this.unsubscribeQuotes(subscriptionId);
      const unsub = this.eventBus.subscribe("tickerUpdate", (msg) => {
        const dataMap = /* @__PURE__ */ new Map();
        symbols.forEach((symbol) => {
          const bbo = this.bbosMap.get(withoutExchangePrefix(symbol));
          const ticker = this.tickersMap.get(withoutExchangePrefix(symbol));
          if (!bbo || !ticker) {
            return;
          }
          const data = {
            ...ticker,
            ask: bbo.ask,
            bid: bbo.bid
          };
          dataMap.set(withoutExchangePrefix(symbol), data);
        });
        if (!dataMap.size) {
          return;
        }
        onRealtimeCallback(
          Array.from(dataMap.values()).map(
            (symbolData) => this._toUDFTicker(symbolData)
          )
        );
      });
      this._subscribeQuoteMap.set(subscriptionId, unsub);
    }
  }
  unsubscribeQuotes(listenerGuid) {
    const subscriptionId = this.getSubscriptionId(listenerGuid);
    const unsub = this._subscribeQuoteMap.get(subscriptionId);
    if (unsub) {
      unsub();
      this._subscribeQuoteMap.delete(subscriptionId);
    }
  }
  _toUDFTicker(t) {
    return {
      n: withExchangePrefix(t.symbol),
      s: "ok",
      v: {
        ask: t.ask,
        bid: t.bid,
        ch: t.change,
        chp: t.perChange / 100,
        description: "",
        exchange: EXCHANGE,
        hight_price: t.high,
        low_price: t.low,
        lp: t.close,
        open_price: t.open,
        prev_close_price: 0,
        volume: t.volume
      }
    };
  }
};
var EST_TPSL_PNL_DECIMAL = 2;
var textDash = "--";
var BracketAlgoType = [
  "BRACKET",
  "STOP_BRACKET"
  /* STOP_BRACKET */
];
var TpslAlgoType = [
  "POSITIONAL_TP_SL",
  "TP_SL"
  /* TP_SL */
];
var isTpslOrder = (order) => order.root_algo_order_id !== order.algo_order_id && (order.algo_type === "TAKE_PROFIT" || order.algo_type === "STOP_LOSS");
var isBracketAlgoType = (algoType) => !!algoType && BracketAlgoType.includes(algoType);
var isPositionTpsl = (order) => order.type === "CLOSE_POSITION";
var isActivatedPositionTpsl = (order) => isPositionTpsl(order) && order.is_activated;
var isActivatedTpslOrder = (order) => isTpslOrder(order) && order.is_activated;
var isActivatedQuantityTpsl = (order) => order.root_algo_order_algo_type === "TP_SL" || isBracketAlgoType(order.root_algo_order_algo_type) && order.is_activated;
var getTpslTag = (order, quantityTpslNoMap) => {
  const algoType = order.algo_type;
  const suffix = {
    [
      "TAKE_PROFIT"
      /* TAKE_PROFIT */
    ]: i18n_default.t("tpsl.takeProfit"),
    [
      "STOP_LOSS"
      /* STOP_LOSS */
    ]: i18n_default.t("tpsl.stopLoss")
  }[algoType];
  if (!suffix) {
    return null;
  }
  return suffix;
};
var buildQuantityTpslNoMap = (orders) => {
  const quantityTpslNoMap = /* @__PURE__ */ new Map();
  let idx = 1;
  [...orders].reverse().filter(isActivatedQuantityTpsl).forEach((order) => {
    if (order.root_algo_order_id && !quantityTpslNoMap.has(order.root_algo_order_id)) {
      quantityTpslNoMap.set(order.root_algo_order_id, idx++);
    }
  });
  return quantityTpslNoMap;
};
var getTpslEstPnl = (tpslOrder, position) => {
  const quantity = Math.abs(
    tpslOrder.type === "CLOSE_POSITION" ? position.balance : tpslOrder.quantity
  );
  const sideFlag = tpslOrder.side === "SELL" ? 1 : -1;
  const openPrice = position.open.toString();
  const estPnl = new decimal_default(tpslOrder.trigger_price).minus(openPrice ?? 0).times(quantity).times(sideFlag).toString();
  return { estPnl, quantity, openPrice };
};
var formatPnl = (pnl) => {
  return pnl !== void 0 && pnl !== "" ? new decimal_default(pnl).todp(EST_TPSL_PNL_DECIMAL, decimal_default.ROUND_FLOOR) : textDash;
};
function useCancelOrder() {
  const [
    pendingOrders,
    { cancelOrder, cancelAlgoOrder, cancelTPSLChildOrder }
  ] = useOrderStream({
    status: OrderStatus.INCOMPLETE
  });
  return (0, import_react2.useCallback)(
    (order) => {
      if (order.algo_order_id) {
        if (TpslAlgoType.includes(order.root_algo_order_algo_type)) {
          const rootOrder = pendingOrders?.find(
            (item) => item.algo_order_id === order.root_algo_order_id
          );
          const isEditActivated = rootOrder.child_orders.every(
            (item) => !!item.trigger_price
          );
          if (isEditActivated) {
            return cancelTPSLChildOrder(
              order.algo_order_id,
              order.root_algo_order_id
            );
          }
          return cancelAlgoOrder(rootOrder.algo_order_id, order.symbol).then();
        }
        return cancelAlgoOrder(order.algo_order_id, order.symbol).then();
      }
      return cancelOrder(order.order_id, order.symbol).then();
    },
    [cancelOrder, pendingOrders]
  );
}
function useEditOrder(onToast) {
  useEventEmitter();
  const [, { updateOrder, cancelAlgoOrder, updateAlgoOrder, updateTPSLOrder }] = useOrderStream({
    status: OrderStatus.INCOMPLETE
  });
  return (0, import_react2.useCallback)(
    (order, lineValue) => {
      if (order.algo_order_id) {
        if (TpslAlgoType.includes(order.root_algo_order_algo_type)) {
          const algoParams = [
            {
              order_id: order.algo_order_id,
              trigger_price: new decimal_default(lineValue.value).toString()
            }
          ];
          return updateTPSLOrder(order.root_algo_order_id, algoParams).then((res) => {
          }).catch((e) => {
            if (onToast) {
              onToast.error(e.message);
            }
          });
        } else if (BracketAlgoType.includes(order.algo_type)) {
          return updateAlgoOrder(order.algo_order_id, {
            order_price: new decimal_default(lineValue.value).toString()
          }).then((res) => {
          }).catch((e) => {
            if (onToast) {
              onToast.error(e.message);
            }
          });
        } else {
          const values2 = {
            quantity: order.quantity,
            trigger_price: order.trigger_price,
            symbol: order.symbol,
            price: order.price,
            // order_type: order.type,
            // side: order.side,
            // reduce_only: Boolean(order.reduce_only),
            algo_order_id: order.algo_order_id
          };
          if (order.order_tag) {
            values2.order_tag = order.order_tag;
          }
          if (order.client_order_id) {
            values2.client_order_id = order.client_order_id;
          }
          if (lineValue.type === "price") {
            values2.price = new decimal_default(lineValue.value).toString();
          }
          if (lineValue.type === "trigger_price") {
            values2.trigger_price = new decimal_default(lineValue.value).toString();
          }
          return updateAlgoOrder(order.algo_order_id, values2).then((res) => {
          }).catch((e) => {
            if (onToast) {
              onToast.error(e.message);
            }
          });
        }
      }
      const values = {
        order_price: order.price?.toString(),
        order_quantity: order.quantity.toString(),
        symbol: order.symbol,
        order_type: order.type,
        side: order.side,
        visible_quantity: 0,
        reduce_only: order.reduce_only
      };
      if (new decimal_default(order.visible_quantity ?? order.visible ?? 0).eq(
        order.quantity
      )) {
        delete values.visible_quantity;
      }
      if (!Object.keys(order).includes("reduce_only")) {
        delete values.reduce_only;
      }
      if (order.order_tag) {
        values.order_tag = order.order_tag;
      }
      if (order.client_order_id) {
        values.client_order_id = order.client_order_id;
      }
      if (lineValue.type === "price") {
        values.order_price = new decimal_default(lineValue.value).toString();
      }
      return updateOrder(order.order_id, values).then((res) => {
      }).catch((e) => {
        onToast.error(e.message);
      });
    },
    [updateOrder]
  );
}
function useSendOrder(symbol) {
  const { onSubmit: _sendMarketOrder } = useOrderEntry(
    {
      symbol,
      side: OrderSide.BUY,
      order_type: OrderType.MARKET
    },
    {
      watchOrderbook: true
    }
  );
  const sendLimitOrder = () => {
  };
  const sendMarketOrder = (data) => {
    data.reduce_only = false;
    return _sendMarketOrder(data).catch((e) => {
      Vt.error(e);
    });
  };
  return {
    sendLimitOrder,
    sendMarketOrder
  };
}
var createBrokerMethod = (method) => {
  return (params) => method(params);
};
var useBroker = ({
  closeConfirm,
  colorConfig,
  onToast,
  mode,
  symbol
}) => {
  const cancelOrder = useCancelOrder();
  const editOrder = useEditOrder(onToast);
  const symbolData = useSymbolsInfo();
  const closePosition = (0, import_react2.useCallback)(
    (position) => closeConfirm && closeConfirm(position),
    [closeConfirm]
  );
  const { sendMarketOrder, sendLimitOrder } = useSendOrder(symbol);
  const getSymbolInfo = (0, import_react2.useCallback)(
    (symbol2) => {
      if (!symbolData) {
        return;
      }
      return {
        baseMin: symbolData[symbol2]("base_min"),
        baseMax: symbolData[symbol2]("base_max"),
        baseTick: symbolData[symbol2]("base_tick"),
        quoteTick: symbolData[symbol2]("quote_tick")
      };
    },
    [symbolData]
  );
  const broker = (0, import_react2.useRef)({
    cancelOrder,
    closePosition,
    editOrder,
    colorConfig,
    sendLimitOrder,
    getSymbolInfo,
    sendMarketOrder: createBrokerMethod(sendMarketOrder),
    mode
  });
  (0, import_react2.useEffect)(() => {
    broker.current.getSymbolInfo = getSymbolInfo;
  }, [symbolData]);
  (0, import_react2.useEffect)(() => {
    broker.current.sendLimitOrder = sendLimitOrder;
    broker.current.sendMarketOrder = sendMarketOrder;
  }, [sendLimitOrder, sendMarketOrder]);
  (0, import_react2.useEffect)(() => {
    broker.current.closePosition = closePosition;
  }, [closeConfirm]);
  (0, import_react2.useEffect)(() => {
    broker.current.cancelOrder = cancelOrder;
  }, [cancelOrder]);
  return broker.current;
};
var useBroker_default = useBroker;
var IntervalMap = {
  1: { startOf: "minute", period: 0 },
  3: { startOf: "hour", period: 3 * 60 * 1e3 },
  5: { startOf: "hour", period: 5 * 60 * 1e3 },
  15: { startOf: "hour", period: 15 * 60 * 1e3 },
  30: { startOf: "hour", period: 30 * 60 * 1e3 },
  60: { startOf: "hour", period: 0 },
  120: { startOf: "day", period: 2 * 60 * 60 * 1e3 },
  240: { startOf: "day", period: 4 * 60 * 60 * 1e3 },
  480: { startOf: "day", period: 8 * 60 * 60 * 1e3 },
  720: { startOf: "day", period: 12 * 60 * 60 * 1e3 },
  D: { startOf: "day", period: 0 },
  // default day interval shown as D instead of 1D
  "1D": { startOf: "day", period: 0 },
  "3D": { startOf: "year", period: 3 * 24 * 60 * 60 * 1e3 },
  "5D": { startOf: "year", period: 5 * 24 * 60 * 60 * 1e3 },
  "1W": { startOf: "week", period: 0 },
  "1M": { startOf: "month", period: 0 }
};
function collectionOrders(order, collection, interval, orderList) {
  const { startOf, period } = IntervalMap[interval];
  const time = new Date(order.updated_time).getTime();
  let base = startOfSecond(time).getTime();
  if (startOf === "minute") {
    base = startOfMinute(time).getTime();
  } else if (startOf === "hour") {
    base = startOfHour(time).getTime();
  } else if (startOf === "day") {
    base = startOfDay(time).getTime();
  } else if (startOf === "month") {
    base = startOfMonth(time).getTime();
  } else if (startOf === "year") {
    base = startOfYear(time).getTime();
  } else if (startOf === "week") {
    base = startOfWeek(time).getTime();
  } else if (startOf === "month") {
    base = startOfMonth(time).getTime();
  }
  const group = period === 0 ? base : Math.floor((time - base) / period) * period + base;
  if (!collection[group]) {
    collection[group] = { [
      "BUY"
      /* BUY */
    ]: [], [
      "SELL"
      /* SELL */
    ]: [] };
  }
  if (collection[group][order.side].length < 5) {
    collection[group][order.side].push(order);
    orderList.push(order);
  }
}
var limitOrdersByInterval = (orders, interval) => {
  const res = [];
  const collection = {};
  if (!IntervalMap[interval]) {
    return [];
  }
  orders.forEach((order) => {
    if (order.child_orders) {
      for (const child of order.child_orders) {
        if (child.is_activated && child.algo_status === OrderStatus.FILLED) {
          collectionOrders(child, collection, interval, res);
        }
      }
    } else {
      collectionOrders(order, collection, interval, res);
    }
  });
  return res;
};
var ExecutionService = class _ExecutionService {
  constructor(instance, broker) {
    this.interval = "1D";
    this.changeInterval = (interval) => {
      const rerenderExecutions = () => {
        this.renderExecutions(this.filledOrders, this.basePriceDecimal);
        this.instance.activeChart().onDataLoaded().unsubscribe(null, rerenderExecutions);
      };
      this.interval = interval;
      this.instance.activeChart().onDataLoaded().subscribe(null, rerenderExecutions);
    };
    this.instance = instance;
    this.executions = [];
    this.filledOrders = [];
    this.basePriceDecimal = 0;
    this.broker = broker;
    this.subscribeIntervalChange();
  }
  async subscribeIntervalChange() {
    this.interval = this.instance.symbolInterval().interval;
    const changeInterval = this.changeInterval;
    this.instance.activeChart().onIntervalChanged().subscribe(null, changeInterval);
  }
  renderExecutions(filledOrders, basePriceDecimal) {
    this.filledOrders = filledOrders;
    this.basePriceDecimal = basePriceDecimal;
    if (!this.interval) {
      return;
    }
    this.removeAll();
    limitOrdersByInterval(filledOrders, this.interval).forEach((order) => {
      this.executions.push(this.drawExecution(order, basePriceDecimal));
    });
  }
  removeAll() {
    this.executions.forEach((execution) => execution.remove());
    this.executions = [];
  }
  static getExecutionInfo(order, basePriceDecimal) {
    const side = order.side;
    const avgExecPrice = order.average_executed_price || order.child_orders?.find((child) => !!child.average_executed_price)?.average_executed_price || 0;
    const excutedNumber = new decimal_default(avgExecPrice).todp(basePriceDecimal, decimal_default.ROUND_FLOOR).toString();
    return `${side === "BUY" ? i18n_default.t("common.buy") : i18n_default.t("common.sell")} ${order.total_executed_quantity} @${commify(excutedNumber)}`;
  }
  drawExecution(order, basePriceDecimal) {
    const side = order.side;
    const avgExecPrice = order.average_executed_price || order.child_orders?.find((child) => !!child.average_executed_price)?.average_executed_price || 0;
    const timestamp = new Date(order.updated_time).getTime() / 1e3;
    const colorConfig = this.broker.colorConfig;
    return this.instance.activeChart().createExecutionShape().setArrowHeight(9).setTooltip(_ExecutionService.getExecutionInfo(order, basePriceDecimal)).setTime(timestamp).setPrice(avgExecPrice).setArrowColor(
      side === "BUY" ? colorConfig.upColor : colorConfig.downColor
    ).setDirection(side === "BUY" ? "buy" : "sell");
  }
  unsubscribeIntervalChange() {
    const changeInterval = this.changeInterval;
    try {
      this.instance.activeChart().onIntervalChanged().unsubscribe(null, changeInterval);
    } catch (e) {
      const errorString = e?.toString() || String(e);
      if (errorString.includes("tradingViewApi") || errorString.includes("Cannot read properties of null") || errorString.includes("Cannot read property") || e instanceof TypeError && errorString.includes("null")) ;
    }
  }
  destroy() {
    this.removeAll();
    this.unsubscribeIntervalChange();
  }
};
var getOrderId = (order) => {
  if (order === null || order === void 0) {
    return void 0;
  }
  return order.algo_order_id || order.order_id;
};
var TpslCalService = class {
  constructor() {
    this.quantityTpslNoMap = /* @__PURE__ */ new Map();
    this.tpslPnlMap = /* @__PURE__ */ new Map();
    this.positions = null;
  }
  getQuantityTpslNoMap() {
    return this.quantityTpslNoMap;
  }
  getTpslPnlMap() {
    return this.tpslPnlMap;
  }
  getFormattedEstPnl(tpslOrder) {
    if (this.positions === null) {
      return "";
    }
    const position = this.positions[0];
    if (!position) {
      return "";
    }
    const { estPnl } = getTpslEstPnl(tpslOrder, position);
    return formatPnl(estPnl);
  }
  prepareTpslPnlMap(newPendingOrders) {
    const changed = [];
    newPendingOrders.forEach((order) => {
      const orderId = getOrderId(order);
      if (orderId && isActivatedTpslOrder(order)) {
        const prevPnl = this.tpslPnlMap.get(orderId);
        const newPnl = this.getFormattedEstPnl(order);
        if (prevPnl !== newPnl) {
          changed.push(orderId);
          this.tpslPnlMap.set(orderId, newPnl);
        }
      }
    });
    return changed;
  }
  prepareQuantityTpslNoMap(newPendingOrders) {
    this.quantityTpslNoMap = buildQuantityTpslNoMap(newPendingOrders);
  }
  recalculatePnl(positions, pendingOrders) {
    this.positions = positions;
    return this.prepareTpslPnlMap(pendingOrders);
  }
  clear() {
    this.positions = null;
    this.quantityTpslNoMap.clear();
    this.tpslPnlMap.clear();
  }
};
var _OrderLineService = class _OrderLineService2 {
  constructor(instance, broker) {
    this.instance = instance;
    this.pendingOrderLineMap = /* @__PURE__ */ new Map();
    this.pendingOrders = [];
    this.broker = broker;
    this.tpslCalService = new TpslCalService();
  }
  renderPendingOrders(newPendingOrders) {
    if (newPendingOrders) {
      this.pendingOrders = newPendingOrders;
    }
    this.cleanOldPendingOrders(this.pendingOrders);
    this.tpslCalService.prepareTpslPnlMap(this.pendingOrders);
    this.tpslCalService.prepareQuantityTpslNoMap(this.pendingOrders);
    this.pendingOrders.forEach((order) => this.renderPendingOrder(order));
  }
  updatePositions(positions) {
    const changed = this.tpslCalService.recalculatePnl(
      positions,
      this.pendingOrders
    );
    this.pendingOrders.filter((order) => changed.includes(getOrderId(order))).forEach((order) => this.renderPendingOrder(order));
  }
  renderPendingOrder(order) {
    const orderId = _OrderLineService2.getOrderId(order);
    if (!orderId) {
      return;
    }
    const orderLine = this.drawOrderLine(orderId, order);
    if (orderLine) {
      this.pendingOrderLineMap.set(orderId, orderLine);
    }
  }
  cleanOldPendingOrders(newPendingOrders) {
    const newOrderIdSet = new Set(
      newPendingOrders.map((order) => _OrderLineService2.getOrderId(order))
    );
    this.pendingOrderLineMap.forEach(
      (_, orderId) => !newOrderIdSet.has(orderId) && this.removePendingOrder(orderId)
    );
  }
  removePendingOrder(orderId) {
    if (orderId === void 0) {
      return;
    }
    const orderLine = this.pendingOrderLineMap.get(orderId);
    if (orderLine) {
      this.pendingOrderLineMap.delete(orderId);
      orderLine.remove();
    }
  }
  /**
   * Creates a base order line with default styling.
   * Returns null if the chart is not ready (e.g., during initialization, hot reload, or chart switching).
   *
   * @returns IOrderLineAdapter instance or null if chart is not available
   */
  getBaseOrderLine() {
    try {
      const activeChart = this.instance.activeChart();
      if (!activeChart) {
        return null;
      }
      const colorConfig = this.broker.colorConfig;
      const orderLine = activeChart.createOrderLine();
      if (!orderLine) {
        return null;
      }
      return orderLine.setCancelTooltip(i18n_default.t("orders.cancelOrder")).setQuantityTextColor(colorConfig.qtyTextColor).setQuantityBackgroundColor(colorConfig.chartBG).setBodyBackgroundColor(colorConfig.chartBG).setCancelButtonBackgroundColor(colorConfig.chartBG).setLineStyle(1).setBodyFont(colorConfig.font).setQuantityFont(colorConfig.font);
    } catch (e) {
      const errorString = e?.toString() || String(e);
      if (errorString.includes("Value is null") || errorString.includes("tradingViewApi") || errorString.includes("Cannot read properties of null") || errorString.includes("Cannot read property") || e instanceof TypeError && errorString.includes("null")) {
        return null;
      }
      throw e;
    }
  }
  static getCombinationType(order) {
    const { algo_type: algoType, type } = order;
    if ((algoType === "STOP_LOSS" || algoType === "TAKE_PROFIT" || algoType === "STOP") && type === "LIMIT") {
      return "STOP_LIMIT";
    }
    if ((algoType === "STOP_LOSS" || algoType === "TAKE_PROFIT" || algoType === "STOP") && type === "MARKET") {
      return "STOP_MARKET";
    }
    if (algoType === "BRACKET") {
      if (type === "LIMIT") {
        return "BRACKET_LIMIT";
      }
      if (type === "MARKET") {
        return "BRACKET_MARKET";
      }
    }
    if (algoType === "TRAILING_STOP") {
      return "TRAILING_STOP";
    }
    return "LIMIT";
  }
  static getText(pendingOrder) {
    const orderCombinationType = _OrderLineService2.getCombinationType(pendingOrder);
    if (orderCombinationType === "STOP_LIMIT" || orderCombinationType === "STOP_MARKET" || orderCombinationType === "STOP_BRACKET_LIMIT" || orderCombinationType === "STOP_BRACKET_MARKET") {
      if (pendingOrder.type === "LIMIT") {
        return `${i18n_default.t("orderEntry.orderType.stopLimit")} ${commify(
          pendingOrder.price
        )}`;
      }
      return i18n_default.t("orderEntry.orderType.stopMarket");
    }
    if (orderCombinationType === "TRAILING_STOP") {
      return i18n_default.t("orderEntry.trailing");
    }
    return i18n_default.t("orderEntry.orderType.limit");
  }
  static getOrderPrice(pendingOrder) {
    if (pendingOrder.algo_type === "TRAILING_STOP") {
      return getTrailingStopPrice(pendingOrder);
    }
    return pendingOrder.trigger_price || pendingOrder.price;
  }
  getTPSLTextWithTpsl(text, pendingOrder) {
    const orderId = getOrderId(pendingOrder);
    if (!orderId) {
      return text;
    }
    return text;
  }
  getTPSLText(pendingOrder) {
    const tpslTypeText = getTpslTag(
      pendingOrder,
      this.tpslCalService.getQuantityTpslNoMap()
    );
    if (tpslTypeText) {
      return this.getTPSLTextWithTpsl(tpslTypeText, pendingOrder);
    }
    return null;
  }
  getOrderQuantity(pendingOrder) {
    if (pendingOrder.algo_order_id) {
      if (isActivatedPositionTpsl(pendingOrder) || isPositionTpsl(pendingOrder)) {
        return "100%";
      }
      if (isActivatedQuantityTpsl(pendingOrder)) {
        const qty = new decimal_default(pendingOrder.quantity).minus(
          pendingOrder.executed ?? 0
        );
        const per = qty.div(new decimal_default(pendingOrder.position_qty)).mul(100).todp(2).toNumber();
        return `${Math.min(Math.abs(per), 100).toString()}%`;
      }
    }
    return commify(new decimal_default(pendingOrder.quantity).toString());
  }
  drawOrderLine(orderId, pendingOrder) {
    const text = isTpslOrder(pendingOrder) ? this.getTPSLText(pendingOrder) : _OrderLineService2.getText(pendingOrder);
    if (text === null) {
      return null;
    }
    const colorConfig = this.broker.colorConfig;
    const existingOrderLine = this.pendingOrderLineMap.get(orderId);
    const orderLine = existingOrderLine ?? this.getBaseOrderLine();
    if (!orderLine) {
      return null;
    }
    const color = pendingOrder.side === "BUY" ? colorConfig.upColor : colorConfig.downColor;
    pendingOrder.side === "BUY" ? colorConfig.pnlUpColor : colorConfig.pnlDownColor;
    const price = _OrderLineService2.getOrderPrice(pendingOrder);
    const lineLength = 100;
    const quantity = this.getOrderQuantity(pendingOrder);
    const textColor2 = colorConfig.textColor;
    orderLine.setText(text).setCancelButtonIconColor(colorConfig.closeIcon).setCancelButtonBorderColor(color).setBodyTextColor(textColor2).setBodyBorderColor(color).setQuantityBorderColor(color).setQuantityTextColor(color).setLineColor(color).setLineLength(lineLength).setQuantity(quantity ?? "").setPrice(price);
    if (this.broker.mode !== 3) {
      orderLine.onCancel(null, () => this.broker.cancelOrder(pendingOrder));
      this.applyEditOnMove(orderLine, pendingOrder);
    } else {
      orderLine.setEditable(false).setCancellable(false);
    }
    return orderLine;
  }
  static getOrderEditKey(pendingOrder) {
    const orderCombinationType = this.getCombinationType(pendingOrder);
    if ([
      "LIMIT",
      "BRACKET_LIMIT"
      /* BRACKET_LIMIT */
    ].includes(
      orderCombinationType
    )) {
      return "price";
    }
    if (orderCombinationType === "STOP_LIMIT" || orderCombinationType === "STOP_MARKET" || pendingOrder.rootAlgoOrderAlgoType === "POSITIONAL_TP_SL" || pendingOrder.rootAlgoOrderAlgoType === "TP_SL" || pendingOrder.rootAlgoOrderAlgoType === "BRACKET" || pendingOrder.rootAlgoOrderAlgoType === "STOP_BRACKET") {
      return "trigger_price";
    }
  }
  applyEditOnMove(orderLine, pendingOrder) {
    const editKey = _OrderLineService2.getOrderEditKey(pendingOrder);
    if (!editKey) {
      return;
    }
    orderLine.onMove(() => {
      this.broker.editOrder(pendingOrder, {
        type: editKey,
        value: `${orderLine.getPrice()}`
      }).then((res) => {
        if (!res.success) {
          this.renderPendingOrder(pendingOrder);
        }
      }).catch(() => this.renderPendingOrder(pendingOrder));
    });
  }
  removeAll() {
    this.pendingOrderLineMap.forEach((orderLine) => orderLine.remove());
    this.pendingOrderLineMap.clear();
    this.pendingOrders = [];
    this.tpslCalService.clear();
  }
};
_OrderLineService.getOrderId = (order) => {
  if (order === null || order === void 0) {
    return void 0;
  }
  return order.algo_order_id || order.order_id;
};
var OrderLineService = _OrderLineService;
var PositionLineService = class _PositionLineService {
  constructor(instance, broker) {
    this.instance = instance;
    this.currentSymbol = "";
    this.broker = broker;
    this.positionLines = {};
    this.lastPositions = null;
  }
  renderPositions(positions) {
    if (positions === null || positions.length === 0) {
      this.removePositions();
      return;
    }
    if (this.lastPositions?.length !== positions.length) {
      this.removePositions();
    }
    if (positions[0].symbol !== this.currentSymbol) {
      this.removePositions();
      this.currentSymbol = positions[0].symbol;
    }
    positions.forEach((position, idx) => this.drawPositionLine(position, idx));
    this.lastPositions = positions;
  }
  getBasePositionLine() {
    return this.instance.activeChart().createPositionLine().setTooltip(i18n_default.t("positions.closePosition")).setQuantityBackgroundColor(this.broker.colorConfig.chartBG).setCloseButtonBackgroundColor(this.broker.colorConfig.chartBG).setBodyTextColor(this.broker.colorConfig.textColor).setQuantityTextColor(this.broker.colorConfig.qtyTextColor).setBodyFont(this.broker.colorConfig.font).setQuantityFont(this.broker.colorConfig.font).setLineLength(100).setLineStyle(1);
  }
  static getPositionQuantity(balance) {
    return commify(new decimal_default(balance).todp(4, decimal_default.ROUND_DOWN).toString());
  }
  static getPositionPnL(unrealPnl, decimal) {
    const text = i18n_default.t("tpsl.pnl");
    const pnl = new decimal_default(unrealPnl).toFixed(decimal, decimal_default.ROUND_DOWN);
    if (new decimal_default(unrealPnl).eq(0)) {
      return `${text} 0`;
    }
    if (new decimal_default(unrealPnl).greaterThan(0)) {
      return `${text} +${commify(pnl)}`;
    }
    return `${text} ${commify(pnl)}`;
  }
  removePositions() {
    Object.keys(this.positionLines).forEach((lineId) => {
      this.positionLines[Number(lineId)].remove();
      delete this.positionLines[Number(lineId)];
    });
  }
  drawPositionLine(position, idx) {
    const colorConfig = this.broker.colorConfig;
    const isPositiveUnrealPnl = position.unrealPnl >= 0;
    const isPositiveBalance = position.balance >= 0;
    let pnlColor = colorConfig.pnlZoreColor;
    const pnlDecimal = new decimal_default(position.unrealPnl);
    if (pnlDecimal.greaterThan(0)) {
      pnlColor = colorConfig.upColor;
    } else if (pnlDecimal.lessThan(0)) {
      pnlColor = colorConfig.downColor;
    }
    isPositiveUnrealPnl ? colorConfig.pnlUpColor : colorConfig.pnlDownColor;
    const sideColor = isPositiveBalance ? colorConfig.upColor : colorConfig.downColor;
    const price = new decimal_default(position.open).toNumber();
    this.positionLines[idx] = this.positionLines[idx] ?? this.getBasePositionLine();
    this.positionLines[idx].setQuantity(_PositionLineService.getPositionQuantity(position.balance)).setPrice(price).setCloseButtonIconColor(colorConfig.closeIcon).setCloseButtonBorderColor(sideColor).setBodyBackgroundColor(pnlColor).setQuantityTextColor(sideColor).setBodyBorderColor(pnlColor).setLineColor(sideColor).setQuantityBorderColor(sideColor).setText(
      _PositionLineService.getPositionPnL(
        position.unrealPnl,
        position.unrealPnlDecimal
      )
    );
    if (this.broker.mode !== 3) {
      this.positionLines[idx].onClose(null, () => {
        this.broker.closePosition(position);
      });
    }
  }
};
var DEFAULT_THRESHOLD = 10;
var TPSLService = class {
  constructor(instance, broker) {
    this.interactiveMode = 0;
    this.tpslElRemoveTimer = null;
    this.currentPosition = null;
    this.tpslOrderLine = null;
    this.tpslPnLVerticalLineEntityId = null;
    this.tpslVerticalLineTime = null;
    this.tpslStartCircleEntityId = null;
    this.tpslEndCircleEntityId = null;
    this.threshold = 10;
    this.lastArgs = null;
    this.instance = instance;
    this.broker = broker;
    this.lastPositions = null;
    this.tpslElRemoveTimer = null;
    this.currentPosition = null;
    this.bindEvent();
  }
  bindEvent() {
    this.chart.crossHairMoved().subscribe(null, (args) => {
      this.lastArgs = args;
      if (this.interactiveMode === 2) {
        this.clearTpslElRemoveTimer();
        return;
      }
      const position = this.getIntersectantPosition(args);
      if (this.currentPosition && position && this.currentPosition.symbol === position.symbol) {
        return;
      }
      if (!position) {
        this.clearTPSLElements();
        return;
      }
      if (position) {
        this.clearTpslElRemoveTimer();
        this.currentPosition = position;
        this.createTPSLTriggerButton(args);
      }
    });
  }
  clearTpslElRemoveTimer() {
    if (this.tpslElRemoveTimer) {
      clearTimeout(this.tpslElRemoveTimer);
      this.tpslElRemoveTimer = null;
    }
  }
  showTPSLDialog(params) {
    const pnl = new decimal_default(params.price).minus(this.currentPosition.open).mul(this.currentPosition?.balance ?? 0);
    modal.show("TPSLSimpleDialogId", {
      title: pnl.gt(0) ? i18n_default.t("tpsl.TPOrderConfirm") : i18n_default.t("tpsl.SLOrderConfirm"),
      triggerPrice: params.price,
      type: pnl.gt(0) ? "tp" : "sl",
      symbol: this.currentPosition.symbol,
      onComplete: () => {
        this.clearTPSLElements();
        this.chart.setScrollEnabled(true);
        this.chart.setZoomEnabled(true);
        this.interactiveMode = 0;
      },
      showAdvancedTPSLDialog: (options2) => {
        this.showAdvancedTPSLDialog({
          type: pnl.gt(0) ? "tp" : "sl",
          triggerPrice: params.price,
          qty: options2.qty
        });
      }
    }).then(
      () => {
      },
      (err) => {
      }
    ).finally(() => {
      this.clearTPSLElements();
      this.chart.setScrollEnabled(true);
      this.chart.setZoomEnabled(true);
      this.interactiveMode = 0;
    });
  }
  showAdvancedTPSLDialog({
    type,
    triggerPrice,
    qty
  }) {
    modal.show("TPSLDialogId", {
      withTriggerPrice: true,
      type,
      triggerPrice,
      symbol: this.currentPosition?.symbol,
      qty,
      onComplete: () => {
        this.clearTPSLElements();
        this.chart.setScrollEnabled(true);
        this.chart.setZoomEnabled(true);
        this.interactiveMode = 0;
      }
    }).then(
      () => {
      },
      (err) => {
      }
    ).finally(() => {
      this.clearTPSLElements();
      this.chart.setScrollEnabled(true);
      this.chart.setZoomEnabled(true);
      this.interactiveMode = 0;
    });
  }
  updatePositions(positions) {
    this.lastPositions = positions;
    this.threshold = this.generateThreshold();
  }
  /**
   * Generates a threshold value for position intersection detection.
   * The threshold is calculated as 2% of the visible price range but is capped
   * to ensure it never exceeds the actual price range (highest - lowest price).
   *
   * @returns The calculated threshold value, or DEFAULT_THRESHOLD if calculation fails
   */
  generateThreshold() {
    const priceScale = this.chart.getPanes()[0]?.getRightPriceScales()[0];
    if (!priceScale) {
      return DEFAULT_THRESHOLD;
    }
    try {
      const priceRange = priceScale.getVisiblePriceRange();
      if (!priceRange) {
        return DEFAULT_THRESHOLD;
      }
      const priceWidth = priceRange.to - priceRange.from;
      const calculatedThreshold = priceWidth * 0.02;
      const threshold = Math.min(calculatedThreshold, priceWidth);
      return threshold;
    } catch (error) {
      return DEFAULT_THRESHOLD;
    }
  }
  drawTPSL(params) {
    const { price } = params;
    const pnl = new decimal_default(price).minus(this.currentPosition.open).mul(this.currentPosition?.balance ?? 0);
    const { tpslOrderLine, verticalLine } = this.ensureTPSLElements({
      price,
      pnl
    });
    const direction = pnl.gt(0) ? i18n_default.t("tpsl.tp") : i18n_default.t("tpsl.sl");
    const color = pnl.gt(0) ? this.broker.colorConfig.upColor : this.broker.colorConfig.downColor;
    tpslOrderLine?.setText(`${direction} ${pnl.toDecimalPlaces(2).toNumber()}`).setBodyTextColor(color).setBodyBorderColor(color).setLineColor(color);
    if (this.tpslVerticalLineTime) {
      verticalLine?.setPoints([
        { price: this.currentPosition?.open, time: this.tpslVerticalLineTime },
        { time: this.tpslVerticalLineTime, price }
      ]);
    }
  }
  ensureTPSLElements(params) {
    const tpslOrderLine = this.tpslOrderLine;
    let verticalLine;
    if (!tpslOrderLine) {
      this.tpslOrderLine = this.createTPSLOrderLine();
    }
    if (this.tpslPnLVerticalLineEntityId) {
      verticalLine = this.chart.getShapeById(this.tpslPnLVerticalLineEntityId);
    }
    if (!verticalLine) {
      if (!this.currentPosition || !this.tpslVerticalLineTime) {
        return {};
      }
      this.tpslPnLVerticalLineEntityId = this.chart.createMultipointShape(
        [
          {
            price: this.currentPosition.open,
            time: this.tpslVerticalLineTime
          },
          { time: this.tpslVerticalLineTime, price: params.price }
        ],
        {
          shape: "trend_line",
          // shape: "parallel_channel",
          lock: true,
          disableSave: true,
          disableSelection: true,
          disableUndo: true,
          zOrder: "top",
          overrides: {
            linecolor: "rgba(255,255,255, 0.2)",
            linewidth: 1,
            rightEnd: 1,
            leftEnd: 1
          }
        }
      );
    }
    verticalLine?.setProperties({
      linecolor: params.pnl.gt(0) ? this.broker.colorConfig.upColor : this.broker.colorConfig.downColor,
      linewidth: 1
    });
    return {
      tpslOrderLine,
      verticalLine
    };
  }
  createTPSLTriggerButton(params) {
    if (!this.tpslOrderLine) {
      this.tpslOrderLine = this.createTPSLOrderLine();
    }
    this.tpslOrderLine.onMove(() => {
      const price = this.tpslOrderLine?.getPrice();
      this.showTPSLDialog({ price: price ?? 0 });
    });
    this.tpslOrderLine.onMoving(() => {
      this.interactiveMode = 2;
      const price = this.tpslOrderLine?.getPrice();
      this.verticalLineTime();
      this.drawTPSL({ price: price ?? 0 });
    });
  }
  createTPSLOrderLine() {
    return this.chart.createOrderLine().setCancellable(false).setExtendLeft(true).setTooltip(i18n_default.t("tpsl.dragToSet")).setPrice(this.currentPosition.open).setLineLength(-200, "pixel").setText(i18n_default.t("common.tpsl")).setQuantity("").setBodyTextColor(this.broker.colorConfig.textColor).setBodyBackgroundColor(this.broker.colorConfig.chartBG).setBodyBorderColor(this.broker.colorConfig.pnlZoreColor).setQuantityBackgroundColor(this.broker.colorConfig.chartBG).setQuantityBorderColor(this.broker.colorConfig.pnlZoreColor).setQuantityTextColor(this.broker.colorConfig.qtyTextColor).setBodyFont(this.broker.colorConfig.font).setQuantityFont(this.broker.colorConfig.font).setLineStyle(3);
  }
  verticalLineTime() {
    const range = this.chart.getVisibleRange();
    this.tpslVerticalLineTime = this.getTimeAtPercentage(
      range.from,
      range.to,
      90
    );
  }
  clearTPSLElements() {
    if (this.tpslElRemoveTimer) {
      return;
    }
    this.tpslElRemoveTimer = setTimeout(() => {
      this.currentPosition = null;
      if (this.tpslOrderLine) {
        this.tpslOrderLine.remove();
        this.tpslOrderLine = null;
      }
      if (this.tpslPnLVerticalLineEntityId) {
        this.chart.removeEntity(this.tpslPnLVerticalLineEntityId);
        this.tpslPnLVerticalLineEntityId = null;
      }
      this.tpslElRemoveTimer = null;
    }, 100);
  }
  getIntersectantPosition(params) {
    if (!Array.isArray(this.lastPositions) || this.lastPositions.length === 0) {
      return null;
    }
    const { price, time } = params;
    let intersectantPosition = null;
    for (const position of this.lastPositions) {
      if (position) {
        if (Math.abs(position.open - price) < this.threshold) {
          intersectantPosition = position;
          break;
        }
      }
    }
    return intersectantPosition;
  }
  get chart() {
    return this.instance.activeChart();
  }
  getTimeAtPercentage(startTime, endTime, percentage) {
    if (typeof startTime !== "number" || typeof endTime !== "number" || typeof percentage !== "number") {
      return null;
    }
    if (percentage < 0 || percentage > 100) {
      return null;
    }
    if (startTime > endTime) {
      return null;
    }
    const duration = endTime - startTime;
    const factor = percentage / 100;
    const offset = duration * factor;
    const targetTime = startTime + offset;
    return Math.round(targetTime);
  }
};
var Renderer = class {
  constructor(instance, host, broker) {
    this.instance = instance;
    this.positionLineService = new PositionLineService(instance, broker);
    this.orderLineService = new OrderLineService(instance, broker);
    this.executionService = new ExecutionService(instance, broker);
    this.tpslService = new TPSLService(instance, broker);
  }
  async renderPositions(positions) {
    await this.chartReady();
    await this.onDataLoaded();
    this.positionLineService.renderPositions(positions);
    this.orderLineService.updatePositions(positions);
    this.tpslService.updatePositions(positions);
  }
  async renderPendingOrders(pendingOrders) {
    await this.chartReady();
    this.orderLineService.renderPendingOrders(pendingOrders);
  }
  async renderFilledOrders(filledOrders, basePriceDecimal) {
    await this.chartReady();
    await this.onDataLoaded();
    this.executionService.renderExecutions(filledOrders, basePriceDecimal);
  }
  remove() {
    this.orderLineService.removeAll();
    this.positionLineService.removePositions();
    this.executionService.destroy();
  }
  onDataLoaded() {
    if (this.instance.activeChart().symbolExt()) {
      return Promise.resolve();
    }
    return new Promise(
      (resolve) => this.instance.activeChart().onDataLoaded().subscribe(
        null,
        () => {
          resolve();
        },
        true
      )
    );
  }
  chartReady() {
    return new Promise(
      (resolve) => this.instance.onChartReady(() => {
        try {
          this.instance.activeChart().dataReady(() => resolve());
        } catch (err) {
          if (err.toString().includes("tradingViewApi")) ;
        }
      })
    );
  }
};
function useCreateRenderer(symbol, displayControlSetting) {
  const [renderer, setRenderer] = (0, import_react2.useState)();
  const rendererRef = (0, import_react2.useRef)();
  const { state } = useAccount();
  const [unPnlPriceBasis] = useLocalStorage("unPnlPriceBasis", "markPrice");
  const [{ rows: positions }, positionsInfo] = usePositionStream(symbol, {
    calcMode: unPnlPriceBasis
  });
  const [pendingOrders] = useOrderStream({
    status: OrderStatus.INCOMPLETE,
    symbol
  });
  const config = useSymbolsInfo();
  const symbolInfo = config?.[symbol];
  const quote_dp = symbolInfo("quote_dp");
  const [fillOrders] = useOrderStream({
    symbol,
    status: OrderStatus.FILLED,
    size: 500
  });
  const createRenderer = (0, import_react2.useRef)(
    (instance, host, broker, container) => {
      if (rendererRef.current) {
        rendererRef.current.remove();
      }
      rendererRef.current = new Renderer(instance, host, broker);
      setRenderer(rendererRef.current);
    }
  );
  const removeRenderer = (0, import_react2.useRef)(() => {
    rendererRef.current?.remove();
    rendererRef.current = void 0;
  });
  (0, import_react2.useEffect)(() => {
    if (state.status < AccountStatusEnum.EnableTrading && state.status !== AccountStatusEnum.EnableTradingWithoutConnected) {
      renderer?.renderPositions([]);
      return;
    }
    if (!displayControlSetting || !displayControlSetting.position) {
      renderer?.renderPositions([]);
      return;
    }
    const positionList = (positions ?? []).filter((_) => _.symbol === symbol).map((item) => {
      return {
        symbol: item.symbol,
        open: item.average_open_price,
        balance: item.position_qty,
        closablePosition: 9999,
        // @ts-ignore
        unrealPnl: item.unrealized_pnl ?? 0,
        interest: 0,
        unrealPnlDecimal: 2,
        basePriceDecimal: 4
      };
    });
    renderer?.renderPositions(positionList);
  }, [renderer, positions, symbol, displayControlSetting, state]);
  (0, import_react2.useEffect)(() => {
    if (!displayControlSetting || !displayControlSetting.buySell) {
      renderer?.renderFilledOrders([], 6);
      return;
    }
    const currentSymbolFillOrders = fillOrders?.filter(
      (item) => item.symbol === symbol
    );
    renderer?.renderFilledOrders(currentSymbolFillOrders ?? [], quote_dp ?? 6);
  }, [renderer, fillOrders, symbol, quote_dp, displayControlSetting]);
  (0, import_react2.useEffect)(() => {
    let tpslOrder = [];
    let positionTpsl = [];
    let limitOrder = [];
    let stopOrder = [];
    let bracketOrder = [];
    let trailingStopOrder = [];
    if (state.status < AccountStatusEnum.EnableTrading && state.status !== AccountStatusEnum.EnableTradingWithoutConnected) {
      renderer?.renderPendingOrders([]);
      return;
    }
    const symbolPosition = (positions ?? []).find(
      (item) => item.symbol === symbol
    );
    pendingOrders?.forEach((order) => {
      if (symbol !== order.symbol) {
        return;
      }
      if (!order.algo_order_id) {
        limitOrder.push(order);
      } else if (order.algo_order_id) {
        if (order.algo_type === "POSITIONAL_TP_SL") {
          for (const child_order of order.child_orders) {
            child_order.root_algo_order_algo_type = order.algo_type;
            if (child_order.trigger_price && child_order.status !== OrderStatus.FILLED) {
              positionTpsl.push(child_order);
            }
          }
        } else if (order.algo_type === "TP_SL") {
          if (symbolPosition) {
            for (const child_order of order.child_orders) {
              child_order.root_algo_order_algo_type = order.algo_type;
              child_order.position_qty = symbolPosition.position_qty;
              if (child_order.trigger_price && child_order.status !== OrderStatus.FILLED) {
                tpslOrder.push(child_order);
              }
            }
          }
        } else if (order.algo_type === "STOP_LOSS" || order.algo_type === "TAKE_PROFIT") {
          stopOrder.push(order);
        } else if (order.algo_type === "BRACKET") {
          bracketOrder.push(order);
        } else if (order.algo_type === "TRAILING_STOP") {
          if (order.is_activated && order.extreme_price) {
            trailingStopOrder.push(order);
          }
        }
      }
    });
    if (displayControlSetting) {
      if (!displayControlSetting.positionTpsl) {
        positionTpsl = [];
      }
      if (!displayControlSetting.tpsl) {
        tpslOrder = [];
      }
      if (!displayControlSetting.limitOrders) {
        limitOrder = [];
        bracketOrder = [];
      }
      if (!displayControlSetting.stopOrders) {
        stopOrder = [];
      }
      if (!displayControlSetting.trailingStop) {
        trailingStopOrder = [];
      }
    }
    renderer?.renderPendingOrders(
      tpslOrder.concat(positionTpsl).concat(limitOrder).concat(stopOrder).concat(bracketOrder).concat(trailingStopOrder)
    );
  }, [
    renderer,
    pendingOrders,
    symbol,
    displayControlSetting,
    positions,
    state.status
  ]);
  return [createRenderer.current, removeRenderer.current];
}
function preventDefaultRenderHack(host) {
  host.setBrokerConnectionAdapter = function(adapter) {
    const delegate = {
      subscribe: () => {
      },
      unsubscribe: () => {
      },
      unsubscribeAll: () => {
      }
    };
    Object.defineProperty(adapter, "_ordersCache", {
      get: function() {
        return {
          start: () => {
          },
          stop: () => {
          },
          update: () => {
          },
          partialUpdate: () => {
          },
          fullUpdate: () => {
          },
          getObjects: async () => [],
          updateDelegate: delegate,
          partialUpdateDelegate: delegate
        };
      },
      set: () => {
      }
    });
    adapter._waitForOrderModification = async () => true;
    this._adapter = adapter;
  };
}
function forceSilentOrdersPlacement(instance, host) {
  instance.onChartReady(() => {
    host.silentOrdersPlacement().subscribe((val) => {
      if (!val) {
        host.silentOrdersPlacement().setValue(true);
        if (instance) {
          instance._iFrame.contentDocument.querySelector(
            ".wrapper-3X2QgaDd"
          ).className = "wrapper-3X2QgaDd highButtons-3X2QgaDd";
        }
        host.sellBuyButtonsVisibility()?.setValue(false);
      }
    });
  });
}
function brokerHostHandler(instance, host) {
  preventDefaultRenderHack(host);
  forceSilentOrdersPlacement(instance, host);
}
var waitForElm = (iframeDocument, selector) => {
  return new Promise((resolve) => {
    const initialIframe = iframeDocument.querySelector(selector);
    if (initialIframe) {
      resolve(initialIframe);
    }
    const observer = new MutationObserver(() => {
      const iframe = iframeDocument.querySelector(selector);
      if (iframe) {
        resolve(iframe);
        observer.disconnect();
      }
    });
    observer.observe(iframeDocument, {
      childList: true,
      subtree: true
    });
  });
};
var ChartHack = class {
  constructor({ iframeDocument }) {
    this.iframeDocument = iframeDocument;
  }
  defaultHack() {
    this.showFavoriteStarByDefault();
  }
  showFavoriteStarByDefault() {
    waitForElm(this.iframeDocument, ".dropdown-2R6OKuTS").then(() => {
      const items = this.iframeDocument.querySelectorAll(
        ".toolbox-2IihgTnv.showOnHover-2IihgTnv"
      );
      items.forEach((item) => {
        item.style.opacity = "1";
      });
    });
  }
};
var getDisabledFeatures = (mode) => {
  let disabledFeatures = [
    "header_symbol_search",
    "volume_force_overlay",
    "trading_account_manager",
    "drawing_templates",
    "open_account_manager",
    "right_toolbar",
    "support_multicharts",
    "header_layouttoggle",
    "order_panel",
    "order_info",
    "trading_notifications",
    "display_market_status",
    "broker_button",
    "add_to_watchlist",
    "chart_crosshair_menu",
    "header_fullscreen_button",
    "header_widget"
  ];
  if (mode === 3) {
    disabledFeatures = [
      ...disabledFeatures,
      "left_toolbar",
      "timeframes_toolbar",
      "go_to_date",
      "timezone_menu",
      // add volume back
      "create_volume_indicator_by_default",
      "buy_sell_buttons"
    ];
  }
  if (mode === 0) {
    disabledFeatures = [
      ...disabledFeatures,
      "header_widget",
      "left_toolbar",
      "timeframes_toolbar",
      "buy_sell_buttons"
    ];
  } else if (mode === 1) {
    disabledFeatures = [
      ...disabledFeatures,
      "left_toolbar",
      "timeframes_toolbar",
      "buy_sell_buttons"
    ];
  }
  return disabledFeatures;
};
function getOptions(options2, mode, externalEnabledFeatures, externalDisabledFeatures) {
  const defaultEnabledFeatures = [
    "hide_left_toolbar_by_default",
    "order_panel_close_button",
    "iframe_loading_compatibility_mode"
  ];
  const mergedEnabledFeatures = Array.from(
    /* @__PURE__ */ new Set([...defaultEnabledFeatures, ...externalEnabledFeatures || []])
  );
  const defaultDisabledFeatures = getDisabledFeatures(mode);
  const mergedDisabledFeatures = Array.from(
    /* @__PURE__ */ new Set([...defaultDisabledFeatures, ...externalDisabledFeatures || []])
  );
  return {
    ...options2,
    disabled_features: mergedDisabledFeatures,
    enabled_features: mergedEnabledFeatures,
    auto_save_delay: 0.1,
    broker_config: {
      configFlags: {
        supportStopLimitOrders: true,
        supportReversePosition: false
      }
    }
  };
}
var getChartSettingAdapterKey = (chartKey) => `${chartKey}_adapter`;
var defaultSettings = {
  "trading.chart.proterty": JSON.stringify({
    showSellBuyButtons: 0,
    noConfirmEnabled: 1,
    qweqrq: 0,
    showPricesWithZeroVolume: 1,
    showSpread: 1,
    orderExecutedSoundParams: '{"enabled":0,"name":"alert/alarm_clock"}'
  }),
  "hint.startFocusedZoom": "true"
};
var chartCacheMap = /* @__PURE__ */ new Map();
var parseAndCacheChartData = (chartKey, savedDataString, adapterSettingString) => {
  const settingAdapterKey = getChartSettingAdapterKey(chartKey);
  try {
    const transformedSavedDataString = savedDataString;
    const savedData = transformedSavedDataString ? JSON.parse(transformedSavedDataString) : void 0;
    const adapterSetting = adapterSettingString ? JSON.parse(adapterSettingString) : defaultSettings;
    if (transformedSavedDataString) {
      chartCacheMap.set(chartKey, transformedSavedDataString);
    }
    if (adapterSettingString) {
      chartCacheMap.set(settingAdapterKey, adapterSettingString);
    }
    return { savedData, adapterSetting };
  } catch (e) {
  }
  return { savedData: void 0, adapterSetting: defaultSettings };
};
var getChartData = async (chartKey, isLoggedIn) => {
  const settingAdapterKey = getChartSettingAdapterKey(chartKey);
  const localStorageSavedData = localStorage.getItem(chartKey) || "";
  const localStorageAdapterSetting = localStorage.getItem(settingAdapterKey) || "";
  try {
    if (chartCacheMap.has(chartKey) && chartCacheMap.has(settingAdapterKey)) {
      return {
        savedData: JSON.parse(chartCacheMap.get(chartKey)),
        adapterSetting: JSON.parse(chartCacheMap.get(settingAdapterKey))
      };
    }
  } catch (e) {
  }
  return parseAndCacheChartData(
    chartKey,
    localStorageSavedData,
    localStorageAdapterSetting
  );
};
var saveChartData = async (chartKey, setting, isLoggedIn) => {
  if (setting) {
    localStorage.setItem(chartKey, setting);
    chartCacheMap.set(chartKey, setting);
  }
};
var saveChartAdapterSetting = async (chartKey, setting, isLoggedIn) => {
  const settingAdapterKey = getChartSettingAdapterKey(chartKey);
  if (setting) {
    localStorage.setItem(settingAdapterKey, setting);
    chartCacheMap.set(settingAdapterKey, setting);
  }
};
var debounce = (func, delay) => {
  let timer = null;
  const debounced = (...args) => {
    timer && window.clearTimeout(timer);
    timer = setTimeout(() => {
      func(...args);
    }, delay);
  };
  debounced.cancel = () => {
    timer && window.clearTimeout(timer);
    timer = null;
  };
  return debounced;
};
var DEFAULT_SETTINGS_KEY = "chartProp_default";
var DEBOUNCE_SAVE_TIME = 300;
var Widget = class {
  constructor(props) {
    this._instance = null;
    this._onClick = null;
    this._datafeed = null;
    this._chartKey = DEFAULT_SETTINGS_KEY;
    this._adapterSetting = defaultSettings;
    this._savedData = null;
    this._isLoggedIn = false;
    this.debounceSaveChart = debounce(() => {
      try {
        this._instance?.save((chartProps) => {
          if (!Object.is(this._savedData, chartProps)) {
            this._savedData = chartProps;
            saveChartData(
              this._chartKey,
              JSON.stringify(chartProps),
              this._isLoggedIn
            );
          }
        });
      } catch (e) {
      }
    }, DEBOUNCE_SAVE_TIME * 2);
    this.debounceSaveChartAdapterSetting = debounce(() => {
      saveChartAdapterSetting(
        this._chartKey,
        JSON.stringify(this._adapterSetting),
        this._isLoggedIn
      );
    }, DEBOUNCE_SAVE_TIME);
    this._create(props);
  }
  remove() {
    this.unsubscribeClick();
    this._datafeed?.remove();
    this._broker?.remove();
    this._instance?.remove();
    this.debounceSaveChart.cancel();
    this.debounceSaveChartAdapterSetting.cancel();
  }
  updateOverrides(overrides) {
    if (!this.instance) {
      return;
    }
    this.instance.applyOverrides(overrides);
  }
  setSymbol(symbol, interval, callback) {
    try {
      this._instance?.onChartReady(() => {
        let currentInterval = interval ?? this._instance?.symbolInterval()?.interval;
        if (!currentInterval) {
          currentInterval = 1;
        }
        this._instance?.setSymbol(symbol, currentInterval, callback);
      });
    } catch (e) {
    }
  }
  setResolution(resolution) {
    if (this._instance) {
      this._instance.activeChart().setResolution(resolution);
    }
  }
  executeActionById(actionId) {
    try {
      this._instance?.onChartReady(() => {
        this._instance?.activeChart().executeActionById(actionId);
      });
    } catch (e) {
    }
  }
  changeLineType(lineType) {
    try {
      this._instance?.onChartReady(() => {
        this._instance?.activeChart().setChartType(lineType);
      });
    } catch (e) {
    }
  }
  subscribeClick(onClick) {
    this._onClick = onClick;
    this._instance?.onChartReady(() => {
      this._instance?._iFrame.contentDocument?.addEventListener(
        "click",
        this._onClick
      );
    });
  }
  unsubscribeClick() {
    this._instance?._iFrame.contentDocument?.removeEventListener(
      "click",
      this._onClick
    );
  }
  get instance() {
    return this._instance;
  }
  chartHack() {
    this._instance?.onChartReady(() => {
      const iframeDocument = this._instance._iFrame.contentWindow.document;
      new ChartHack({ iframeDocument }).defaultHack();
    });
  }
  subscribeAutoSave() {
    this._instance?.onChartReady(() => {
      this._instance?.subscribe("onAutoSaveNeeded", () => {
        this.debounceSaveChart();
      });
      this._instance?.activeChart().onVisibleRangeChanged().subscribe(null, () => {
        this.debounceSaveChart();
      });
    });
  }
  async _create({
    options: options2,
    chartKey,
    mode,
    onClick,
    enabled_features,
    disabled_features
  }) {
    const getBroker = options2.getBroker;
    const widgetOptions = {
      fullscreen: options2.fullscreen ?? true,
      autosize: options2.autosize ?? false,
      timezone: options2.timezone,
      symbol: options2.symbol,
      library_path: options2.libraryPath,
      interval: options2.interval ?? "1",
      custom_css_url: options2.customCssUrl,
      custom_font_family: options2.customFontFamily,
      datafeed: options2.datafeed,
      studies_overrides: options2.studiesOverrides,
      locale: options2.locale,
      theme: options2.theme,
      loading_screen: options2.loadingScreen,
      overrides: options2.overrides,
      container: options2.container,
      favorites: {
        intervals: [
          "1",
          "3",
          "5",
          "15",
          "30",
          "60",
          "240",
          "1d",
          "1W",
          "1M"
        ],
        chartTypes: ["Area", "Line"]
      },
      broker_factory: getBroker ? (host) => {
        if (this._broker) {
          this._broker.remove();
        }
        this._broker = getBroker(
          this._instance,
          host
        );
        return this._broker;
      } : void 0,
      custom_indicators_getter: options2.customIndicatorsGetter
    };
    this._datafeed = options2.datafeed;
    if (chartKey) {
      this._chartKey = chartKey;
    }
    const { savedData, adapterSetting } = await getChartData(
      this._chartKey,
      this._isLoggedIn
    );
    this._adapterSetting = adapterSetting;
    this._savedData = savedData;
    const mergedOptions = getOptions(
      widgetOptions,
      mode,
      enabled_features,
      disabled_features
    );
    this._instance = new TradingView.widget({
      ...mergedOptions,
      ...options2.customIndicatorsGetter != null && {
        custom_indicators_getter: options2.customIndicatorsGetter
      },
      interval: adapterSetting["chart.lastUsedTimeBasedResolution"] ?? widgetOptions.interval,
      saved_data: savedData,
      settings_adapter: {
        initialSettings: adapterSetting,
        setValue: (key2, value) => {
          this._adapterSetting = { ...this._adapterSetting, [key2]: value };
          this.debounceSaveChartAdapterSetting();
        },
        removeValue: () => {
        }
      }
    });
    this._instance.onChartReady(() => {
      if (options2.symbol && this._instance?.activeChart().symbol() !== withoutExchangePrefix(options2.symbol)) {
        this.setSymbol(options2.symbol);
      }
    });
    this.subscribeAutoSave();
    this.subscribeClick(onClick);
    this.chartHack();
  }
};
var CHART_KEY = "SDK_Tradingview";
var MOBILE_CHART_KEY = "SDK_Moblie_Tradingview";
var getChartKey = (isMobile) => {
  return isMobile ? MOBILE_CHART_KEY : CHART_KEY;
};
var defaultLocale = (localeCode) => {
  return localeCode === "id" ? "id_ID" : localeCode === "tc" ? "zh_TW" : localeCode;
};
function useTradingviewScript(props) {
  const {
    scriptSRC: tradingViewScriptSrc,
    libraryPath,
    customCssUrl: tradingViewCustomCssUrl,
    overrides: customerOverrides,
    studiesOverrides: customerStudiesOverrides,
    symbol,
    theme,
    loadingScreen: customerLoadingScreen,
    mode,
    colorConfig: customerColorConfig,
    locale = defaultLocale,
    classNames,
    enabled_features,
    disabled_features,
    customIndicatorsGetter
  } = props;
  const localeCode = useLocaleCode();
  const chart = (0, import_react2.useRef)(null);
  const apiBaseUrl = useConfig("apiBaseUrl");
  const { state: accountState } = useAccount();
  const [side, setSide] = (0, import_react2.useState)(OrderSide.SELL);
  const symbolsInfo = useSymbolsInfo();
  const [fullscreen, setFullscreen] = useLocalStorage(
    TradingviewFullscreenKey,
    false
  );
  const { onSubmit, submitting } = useOrderEntry(
    {
      symbol: symbol ?? "",
      side,
      order_type: OrderType.MARKET
    },
    {
      watchOrderbook: true
    }
  );
  const [displayControlState, setDisplayControlState] = (0, import_react2.useState)(() => {
    const displaySettingInfo = localStorage.getItem(
      TradingViewSDKLocalstorageKey.displayControlSetting
    );
    if (displaySettingInfo) {
      return JSON.parse(displaySettingInfo);
    }
    return {
      position: true,
      buySell: true,
      limitOrders: true,
      stopOrders: true,
      tpsl: true,
      positionTpsl: true,
      trailingStop: true
    };
  });
  const [interval, setInterval2] = (0, import_react2.useState)(() => {
    const lastUsedInterval = localStorage.getItem(
      TradingViewSDKLocalstorageKey.interval
    );
    if (!lastUsedInterval) {
      return "15";
    }
    return lastUsedInterval;
  });
  const [lineType, setLineType] = (0, import_react2.useState)(() => {
    const lastUsedLineType = localStorage.getItem(
      TradingViewSDKLocalstorageKey.lineType
    );
    if (!lastUsedLineType) {
      return "1";
    }
    return lastUsedLineType;
  });
  const isMobile = useMediaQuery(MEDIA_TABLET);
  const colorConfig = (0, import_react2.useMemo)(
    () => Object.assign({}, defaultColorConfig, customerColorConfig ?? {}),
    [customerColorConfig]
  );
  const loadingScreen = (0, import_react2.useMemo)(() => {
    if (typeof customerLoadingScreen === "object") {
      return customerLoadingScreen;
    }
    return {
      backgroundColor: chartBG
    };
  }, [customerLoadingScreen]);
  const ws = useWS();
  const [chartingLibrarySciprtReady, setChartingLibrarySciprtReady] = (0, import_react2.useState)(false);
  const closePositionConfirmCallback = (data) => {
    const symbolInfo = symbolsInfo[symbol];
    if (!symbolInfo) {
      return;
    }
    const side2 = new decimal_default(data.balance).greaterThan(0) ? OrderSide.SELL : OrderSide.BUY;
    const order = {
      //   order_price: undefined,
      order_quantity: new decimal_default(data.balance).abs().toNumber(),
      symbol,
      order_type: OrderType.MARKET,
      side: side2,
      reduce_only: true
    };
    setSide(side2);
    modal.show("MarketCloseConfirmID", {
      base: symbolInfo("base"),
      quantity: data.balance,
      onConfirm: async () => {
        return onSubmit(order).catch((error) => {
          if (typeof error === "string") {
            Vt.error(error);
          } else {
            Vt.error(error.message);
          }
        });
      },
      submitting
    });
  };
  const chartRef = (0, import_react2.useRef)(null);
  const isLoggedIn = (0, import_react2.useMemo)(() => {
    if (accountState.status < AccountStatusEnum.EnableTrading && accountState.status !== AccountStatusEnum.EnableTradingWithoutConnected) {
      return false;
    }
    return true;
  }, [accountState]);
  const broker = useBroker_default({
    closeConfirm: closePositionConfirmCallback,
    colorConfig,
    onToast: Vt,
    symbol: symbol ?? "",
    mode
  });
  const [createRenderer, removeRenderer] = useCreateRenderer(
    symbol,
    displayControlState
  );
  const onFullScreenChange = () => {
    if (fullscreen) {
      setFullscreen(false);
    } else {
      setFullscreen(true);
    }
    props.onFullScreenChange?.(!fullscreen);
  };
  const changeInterval = (newInterval) => {
    if (!chart.current) {
      return;
    }
    localStorage.setItem(TradingViewSDKLocalstorageKey.interval, newInterval);
    setInterval2(newInterval);
    chart.current?.setResolution(newInterval);
  };
  const changeLineType = (newLineType) => {
    if (!chart.current) {
      return;
    }
    localStorage.setItem(TradingViewSDKLocalstorageKey.lineType, newLineType);
    setLineType(newLineType);
    chart.current?.changeLineType(Number(newLineType));
  };
  const changeDisplaySetting = (newSetting) => {
    localStorage.setItem(
      TradingViewSDKLocalstorageKey.displayControlSetting,
      JSON.stringify(newSetting)
    );
    setDisplayControlState(newSetting);
  };
  const openChartSetting = () => {
    if (!chart.current) {
      return;
    }
    chart.current.executeActionById("chartProperties");
  };
  const openChartIndicators = () => {
    if (!chart.current) {
      return;
    }
    chart.current.executeActionById("insertIndicator");
  };
  (0, import_react2.useEffect)(() => {
    if (!tradingViewScriptSrc) {
      return;
    }
    if (!chartRef.current) {
      return;
    }
    if (!chartingLibrarySciprtReady) {
      const script = document.createElement("script");
      script.setAttribute("data-nscript", "afterInteractive");
      script.src = tradingViewScriptSrc;
      script.async = true;
      script.type = "text/javascript";
      script.onload = () => {
        setChartingLibrarySciprtReady(true);
      };
      script.onerror = () => {
      };
      chartRef.current.appendChild(script);
    }
  }, [chartRef, chartingLibrarySciprtReady, tradingViewScriptSrc]);
  (0, import_react2.useEffect)(() => {
    if (!symbol) {
      return;
    }
    if (!chartingLibrarySciprtReady || !tradingViewScriptSrc) {
      return;
    }
    const defaultOverrides = getOveriides(colorConfig, isMobile);
    const overrides = customerOverrides ? Object.assign({}, defaultOverrides.overrides, customerOverrides) : defaultOverrides.overrides;
    const studiesOverrides = customerStudiesOverrides ? Object.assign(
      {},
      defaultOverrides.studiesOverrides,
      customerStudiesOverrides
    ) : defaultOverrides.studiesOverrides;
    if (chartRef.current) {
      const options2 = {
        // fullscreen: fullscreen ?? false,
        fullscreen: false,
        autosize: true,
        symbol: withExchangePrefix(symbol),
        locale: typeof locale === "function" ? locale(localeCode) : locale,
        timezone: Intl.DateTimeFormat().resolvedOptions().timeZone,
        container: chartRef.current,
        libraryPath,
        customCssUrl: tradingViewCustomCssUrl,
        interval: interval ?? "1",
        theme: theme ?? "dark",
        loadingScreen: loadingScreen ?? {},
        overrides,
        studiesOverrides,
        datafeed: new Datafeed(apiBaseUrl, ws),
        contextMenu: {
          items_processor: async (defaultItems) => {
            return defaultItems;
          }
        },
        // todo broker effect sell/buy
        getBroker: (instance, host) => {
          brokerHostHandler(instance, host);
          return getBrokerAdapter_default(host, broker);
        },
        customIndicatorsGetter
      };
      const chartProps = {
        options: options2,
        chartKey: getChartKey(isMobile),
        mode,
        onClick: () => {
        },
        enabled_features,
        disabled_features
      };
      chart.current = new Widget(chartProps);
    }
    return () => {
      chart.current?.remove();
    };
  }, [
    chartingLibrarySciprtReady,
    isMobile,
    mode,
    chart,
    chartRef,
    chartingLibrarySciprtReady,
    tradingViewScriptSrc,
    colorConfig,
    locale,
    localeCode,
    customIndicatorsGetter
  ]);
  (0, import_react2.useEffect)(() => {
    ws.on(
      "status:change",
      (message) => {
        if (!message.isPrivate && message.isReconnect) {
          if (typeof window.onResetCacheNeededCallback === "function") {
            window.onResetCacheNeededCallback();
            if (chart.current?.instance) {
              chart.current?.instance.activeChart()?.resetData();
            }
          }
        }
      },
      "tradingview"
    );
  }, [ws]);
  (0, import_react2.useEffect)(() => {
    if (chart.current && chart.current?.instance) {
      chart.current?.instance?.onChartReady(() => {
        if (isLoggedIn && chart.current?.instance) {
          createRenderer(
            chart.current.instance,
            void 0,
            broker,
            chartRef.current
          );
        }
      });
    }
    return () => {
      removeRenderer();
    };
  }, [chart.current, isLoggedIn]);
  (0, import_react2.useEffect)(() => {
    if (!symbol || !chart.current) {
      return;
    }
    chart.current?.setSymbol(symbol);
    const service = new WebsocketService(ws);
    service.subscribeSymbol(symbol);
    return () => {
      service.unsubscribeKline(symbol);
    };
  }, [symbol]);
  return {
    tradingViewScriptSrc,
    chartRef,
    changeDisplaySetting,
    displayControlState,
    interval,
    changeInterval,
    lineType,
    changeLineType,
    openChartSetting,
    openChartIndicators,
    symbol,
    onFullScreenChange,
    classNames,
    fullscreen
  };
}
init_icons();
var Link = (props) => {
  const { url, children } = props;
  return (0, import_jsx_runtime2.jsx)(
    "span",
    {
      onClick: () => window.open(url),
      className: "oui-cursor-pointer oui-px-0.5 oui-text-primary-light oui-underline",
      children
    }
  );
};
var NoTradingview = () => {
  const { t } = useTranslation();
  return (0, import_jsx_runtime2.jsx)("div", { className: "oui-absolute oui-inset-0 oui-z-0 oui-flex oui-flex-col oui-items-center oui-justify-start oui-p-2 oui-text-base-contrast-80 md:oui-justify-center md:oui-p-10", children: (0, import_jsx_runtime2.jsxs)("div", { children: [
    (0, import_jsx_runtime2.jsx)("p", { className: "oui-mb-6 oui-text-xs", children: t("tradingView.noScriptSrc") }),
    (0, import_jsx_runtime2.jsx)("p", { className: "oui-mb-3 oui-pl-0 oui-text-2xs oui-text-base-contrast-54 md:oui-pl-2 md:oui-text-base md:oui-text-base-contrast-80", children: (0, import_jsx_runtime2.jsx)(
      Trans,
      {
        i18nKey: "tradingView.noScriptSrc.1",
        components: [
          (0, import_jsx_runtime2.jsx)(
            Link,
            {
              url: "https://www.tradingview.com/advanced-charts"
            },
            "tradingview-advanced-charts"
          )
        ]
      }
    ) }),
    (0, import_jsx_runtime2.jsx)("p", { className: "oui-pl-0 oui-text-2xs oui-text-base-contrast-54 md:oui-pl-2 md:oui-text-base md:oui-text-base-contrast-80", children: (0, import_jsx_runtime2.jsx)(
      Trans,
      {
        i18nKey: "tradingView.noScriptSrc.2",
        components: [
          (0, import_jsx_runtime2.jsx)(
            Link,
            {
              url: "https://orderly.network/docs/sdks/react/components/trading#tradingviewconfig"
            },
            "tradingview-config"
          )
        ]
      }
    ) })
  ] }) });
};
var TopBar = (props) => {
  return (0, import_jsx_runtime2.jsx)("div", { className: "top-toolbar oui-flex oui-h-[44px] oui-justify-between md:oui-justify-start oui-items-center oui-p-2 md:oui-px-3 md:oui-pt-3 md:oui-pb-[14px]", children: props.children });
};
var topBar_default = TopBar;
var LazyLineType = import_react2.default.lazy(() => Promise.resolve().then(() => (init_lineType(), lineType_exports)));
var LazyTimeInterval = import_react2.default.lazy(
  () => Promise.resolve().then(() => (init_timeInterval(), timeInterval_exports)).then((mod) => ({ default: mod.TimeInterval }))
);
var LazyMobileDisplayControl = import_react2.default.lazy(
  () => Promise.resolve().then(() => (init_displayControl(), displayControl_exports)).then((mod) => ({
    default: mod.MobileDisplayControl
  }))
);
var LazyDesktopDisplayControl = import_react2.default.lazy(
  () => Promise.resolve().then(() => (init_displayControl(), displayControl_exports)).then((mod) => ({
    default: mod.DesktopDisplayControl
  }))
);
var OperateButton = ({ children, onClick }) => {
  return (0, import_jsx_runtime2.jsx)(
    Box,
    {
      onClick,
      className: "oui-cursor-pointer oui-w-[18px] oui-h-[18px] oui-text-base-contrast-36  hover:oui-text-base-contrast-80",
      children
    }
  );
};
var ZoomOutIcon = (props) => {
  return (0, import_jsx_runtime2.jsx)(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      width: "18",
      height: "18",
      viewBox: "0 0 18 18",
      fill: "currentColor",
      ...props,
      children: (0, import_jsx_runtime2.jsx)("path", { d: "M15.0008 2.24304C14.8088 2.24304 14.6085 2.30755 14.4615 2.4538L11.2508 5.66455V3.74304H9.75079V7.49304C9.75079 7.90704 10.0868 8.24304 10.5008 8.24304H14.2508V6.74304H12.3285L15.54 3.53229C15.8325 3.23904 15.8325 2.74705 15.54 2.4538C15.393 2.30755 15.1928 2.24304 15.0008 2.24304ZM3.7508 9.74303V11.243H5.67231L2.46156 14.4538C2.16906 14.747 2.16906 15.239 2.46156 15.5323C2.75481 15.8248 3.2468 15.8248 3.54005 15.5323L6.7508 12.3215V14.243H8.25079V10.493C8.25079 10.079 7.9148 9.74303 7.5008 9.74303H3.7508Z" })
    }
  );
};
var ZoomInIcon = (props) => {
  return (0, import_jsx_runtime2.jsx)(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      width: "18",
      height: "18",
      viewBox: "0 0 18 18",
      fill: "currentColor",
      ...props,
      children: (0, import_jsx_runtime2.jsx)("path", { d: "M7.49219 9.74304C7.30026 9.74304 7.09964 9.80755 6.95309 9.9538L3.74219 13.1646V11.243H2.24219V14.993C2.24219 15.407 2.57796 15.743 2.99219 15.743H6.74219V14.243H4.82031L8.03121 11.0323C8.32416 10.739 8.32416 10.247 8.03121 9.9538C7.88481 9.80755 7.68404 9.74304 7.49219 9.74304ZM11.2509 2.24304V3.74304H13.1728L9.96186 6.9538C9.66899 7.24705 9.66899 7.73904 9.96186 8.03229C10.2547 8.32479 10.7471 8.32479 11.04 8.03229L14.2509 4.82153V6.74304H15.7509V2.99304C15.7509 2.57904 15.4151 2.24304 15.0009 2.24304H11.2509Z" })
    }
  );
};
var TradingviewUI = (0, import_react2.forwardRef)((props, ref) => {
  const {
    chartRef,
    interval,
    changeDisplaySetting,
    displayControlState,
    tradingViewScriptSrc,
    changeInterval,
    lineType,
    changeLineType,
    openChartSetting,
    openChartIndicators,
    onFullScreenChange
  } = props;
  const isMobile = useMediaQuery(MEDIA_TABLET);
  return (0, import_jsx_runtime2.jsx)(
    "div",
    {
      ref,
      className: M("oui-relative oui-size-full", props.classNames?.root),
      children: !tradingViewScriptSrc ? (0, import_jsx_runtime2.jsx)(NoTradingview, {}) : (0, import_jsx_runtime2.jsxs)(
        "div",
        {
          className: M(
            "oui-absolute oui-inset-0 oui-z-[1] oui-flex oui-flex-col",
            props.classNames?.content
          ),
          children: [
            (0, import_jsx_runtime2.jsx)(topBar_default, { children: isMobile ? (0, import_jsx_runtime2.jsxs)(
              Flex,
              {
                gapX: 2,
                width: "100%",
                justify: "between",
                className: "oui-hide-scrollbar oui-overflow-x-scroll",
                children: [
                  (0, import_jsx_runtime2.jsx)(import_react2.default.Suspense, { fallback: null, children: (0, import_jsx_runtime2.jsx)(
                    LazyTimeInterval,
                    {
                      interval: interval ?? "15",
                      changeInterval
                    }
                  ) }),
                  (0, import_jsx_runtime2.jsx)(OperateButton, { onClick: openChartIndicators, children: (0, import_jsx_runtime2.jsx)(IndicatorsIcon, {}) }),
                  (0, import_jsx_runtime2.jsx)(import_react2.default.Suspense, { fallback: null, children: (0, import_jsx_runtime2.jsx)(
                    LazyMobileDisplayControl,
                    {
                      displayControlState,
                      changeDisplayControlState: changeDisplaySetting
                    }
                  ) })
                ]
              }
            ) : (0, import_jsx_runtime2.jsxs)(Flex, { justify: "between", itemAlign: "center", width: "100%", children: [
              (0, import_jsx_runtime2.jsxs)(Flex, { children: [
                (0, import_jsx_runtime2.jsx)(import_react2.default.Suspense, { fallback: null, children: (0, import_jsx_runtime2.jsx)(
                  LazyTimeInterval,
                  {
                    interval: interval ?? "1",
                    changeInterval
                  }
                ) }),
                (0, import_jsx_runtime2.jsx)(
                  Divider,
                  {
                    direction: "vertical",
                    className: "oui-h-4",
                    mx: 2,
                    intensity: 8
                  }
                ),
                (0, import_jsx_runtime2.jsxs)(Flex, { justify: "start", itemAlign: "center", gap: 2, children: [
                  (0, import_jsx_runtime2.jsx)(import_react2.default.Suspense, { fallback: null, children: (0, import_jsx_runtime2.jsx)(
                    LazyDesktopDisplayControl,
                    {
                      displayControlState,
                      changeDisplayControlState: changeDisplaySetting
                    }
                  ) }),
                  (0, import_jsx_runtime2.jsx)(OperateButton, { onClick: openChartIndicators, children: (0, import_jsx_runtime2.jsx)(IndicatorsIcon, {}) }),
                  (0, import_jsx_runtime2.jsx)(import_react2.default.Suspense, { fallback: null, children: (0, import_jsx_runtime2.jsx)(
                    LazyLineType,
                    {
                      lineType,
                      changeLineType
                    }
                  ) }),
                  (0, import_jsx_runtime2.jsx)(OperateButton, { onClick: openChartSetting, children: (0, import_jsx_runtime2.jsx)(SettingIcon, {}) })
                ] })
              ] }),
              (0, import_jsx_runtime2.jsx)(Flex, { children: props.fullscreen ? (0, import_jsx_runtime2.jsx)(
                ZoomOutIcon,
                {
                  className: "oui-text-base-contrast-54 hover:oui-text-base-contrast oui-cursor-pointer",
                  onClick: onFullScreenChange
                }
              ) : (0, import_jsx_runtime2.jsx)(
                ZoomInIcon,
                {
                  className: "oui-text-base-contrast-54 hover:oui-text-base-contrast oui-cursor-pointer",
                  onClick: onFullScreenChange
                }
              ) })
            ] }) }),
            (0, import_jsx_runtime2.jsx)("div", { ref: chartRef, className: "oui-size-full oui-overflow-hidden" })
          ]
        }
      )
    }
  );
});
var TradingviewWidget = (0, import_react2.forwardRef)((props, ref) => {
  const state = useTradingviewScript(props);
  return (0, import_jsx_runtime2.jsx)(TradingviewUI, { ...state, ref });
});

// node_modules/@uiw/react-split/esm/index.js
var import_dist7 = __toESM(require_dist());
var import_dist8 = __toESM(require_dist2());
var import_dist9 = __toESM(require_dist3());
var import_react3 = __toESM(require_react());
var import_jsx_runtime3 = __toESM(require_jsx_runtime());
var import_jsx_runtime4 = __toESM(require_jsx_runtime());
import "/root/.openclaw/workspace/vanta-dex/node_modules/@uiw/react-split/esm/style/index.css";
var _excluded = ["prefixCls", "className", "children", "mode", "visiable", "visible", "renderBar", "lineBar", "disable", "onDragEnd", "onDragging"];
var Split = class extends import_react3.default.Component {
  constructor(props) {
    super(props);
    this.state = {
      dragging: false
    };
    this.warpper = void 0;
    this.paneNumber = void 0;
    this.startX = void 0;
    this.startY = void 0;
    this.move = void 0;
    this.target = void 0;
    this.boxWidth = void 0;
    this.boxHeight = void 0;
    this.preWidth = void 0;
    this.nextWidth = void 0;
    this.preHeight = void 0;
    this.nextHeight = void 0;
    this.preSize = void 0;
    this.nextSize = void 0;
    this.onDragEnd = this.onDragEnd.bind(this);
    this.onDragging = this.onDragging.bind(this);
  }
  componentWillUnmount() {
    this.removeEvent();
  }
  removeEvent() {
    window.removeEventListener("mousemove", this.onDragging, false);
    window.removeEventListener("mouseup", this.onDragEnd, false);
  }
  onMouseDown(paneNumber, env) {
    if (!env.target || !this.warpper) {
      return;
    }
    this.paneNumber = paneNumber;
    this.startX = env.clientX;
    this.startY = env.clientY;
    this.move = true;
    this.target = env.target.parentNode;
    var prevTarget = this.target.previousElementSibling;
    var nextTarget = this.target.nextElementSibling;
    this.boxWidth = this.warpper.clientWidth;
    this.boxHeight = this.warpper.clientHeight;
    if (prevTarget) {
      this.preWidth = prevTarget.clientWidth;
      this.preHeight = prevTarget.clientHeight;
    }
    if (nextTarget) {
      this.nextWidth = nextTarget.clientWidth;
      this.nextHeight = nextTarget.clientHeight;
    }
    window.addEventListener("mousemove", this.onDragging);
    window.addEventListener("mouseup", this.onDragEnd, false);
    this.setState({
      dragging: true
    });
  }
  onDragging(env) {
    if (!this.move) {
      return;
    }
    if (!this.state.dragging) {
      this.setState({
        dragging: true
      });
    }
    var {
      mode,
      onDragging
    } = this.props;
    var nextTarget = this.target.nextElementSibling;
    var prevTarget = this.target.previousElementSibling;
    var x = env.clientX - this.startX;
    var y = env.clientY - this.startY;
    this.preSize = 0;
    this.nextSize = 0;
    if (mode === "horizontal") {
      this.preSize = this.preWidth + x > -1 ? this.preWidth + x : 0;
      this.nextSize = this.nextWidth - x > -1 ? this.nextWidth - x : 0;
      if (this.preSize === 0 || this.nextSize === 0) {
        return;
      }
      this.preSize = (this.preSize / this.boxWidth >= 1 ? 1 : this.preSize / this.boxWidth) * 100;
      this.nextSize = (this.nextSize / this.boxWidth >= 1 ? 1 : this.nextSize / this.boxWidth) * 100;
      if (prevTarget && nextTarget) {
        prevTarget.style.width = this.preSize + "%";
        nextTarget.style.width = this.nextSize + "%";
      }
    }
    if (mode === "vertical" && this.preHeight + y > -1 && this.nextHeight - y > -1) {
      this.preSize = this.preHeight + y > -1 ? this.preHeight + y : 0;
      this.nextSize = this.nextHeight - y > -1 ? this.nextHeight - y : 0;
      this.preSize = (this.preSize / this.boxHeight >= 1 ? 1 : this.preSize / this.boxHeight) * 100;
      this.nextSize = (this.nextSize / this.boxHeight >= 1 ? 1 : this.nextSize / this.boxHeight) * 100;
      if (this.preSize === 0 || this.nextSize === 0) {
        return;
      }
      if (prevTarget && nextTarget) {
        prevTarget.style.height = this.preSize + "%";
        nextTarget.style.height = this.nextSize + "%";
      }
    }
    onDragging && onDragging(this.preSize, this.nextSize, this.paneNumber);
  }
  onDragEnd() {
    var {
      onDragEnd
    } = this.props;
    this.move = false;
    onDragEnd && onDragEnd(this.preSize, this.nextSize, this.paneNumber);
    this.removeEvent();
    this.setState({
      dragging: false
    });
  }
  render() {
    var _this$props$visible;
    var _this$props = this.props, {
      prefixCls,
      className,
      children,
      mode,
      visible = (_this$props$visible = this.props.visible) != null ? _this$props$visible : this.props.visiable,
      renderBar,
      lineBar,
      disable
    } = _this$props, other = _objectWithoutPropertiesLoose(_this$props, _excluded);
    var {
      dragging
    } = this.state;
    var cls = [prefixCls, className, prefixCls + "-" + mode, dragging ? "dragging" : null].filter(Boolean).join(" ").trim();
    var child = import_react3.default.Children.toArray(children);
    return (0, import_jsx_runtime3.jsx)("div", _extends({
      className: cls
    }, other, {
      ref: (node) => this.warpper = node,
      children: import_react3.default.Children.map(child, (element, idx) => {
        var props = Object.assign({}, element.props, {
          className: [prefixCls + "-pane", element.props.className].filter(Boolean).join(" ").trim(),
          style: _extends({}, element.props.style)
        });
        var visibleBar = visible === true || visible && visible.includes(idx + 1) || false;
        var barProps = {
          className: [prefixCls + "-bar", lineBar ? prefixCls + "-line-bar" : null, !lineBar ? prefixCls + "-large-bar" : null].filter(Boolean).join(" ").trim()
        };
        if (disable === true || disable && disable.includes(idx + 1)) {
          barProps.className = [barProps.className, disable ? "disable" : null].filter(Boolean).join(" ").trim();
        }
        var BarCom = null;
        if (idx !== 0 && visibleBar && renderBar) {
          BarCom = renderBar(_extends({}, barProps, {
            onMouseDown: this.onMouseDown.bind(this, idx + 1)
          }));
        } else if (idx !== 0 && visibleBar) {
          BarCom = import_react3.default.createElement("div", _extends({}, barProps), (0, import_jsx_runtime3.jsx)("div", {
            onMouseDown: this.onMouseDown.bind(this, idx + 1)
          }));
        }
        return (0, import_jsx_runtime4.jsxs)(import_react3.default.Fragment, {
          children: [BarCom, import_react3.default.cloneElement(element, _extends({}, props))]
        }, idx);
      })
    }));
  }
};
Split.defaultProps = {
  prefixCls: "w-split",
  visiable: true,
  mode: "horizontal"
};

// node_modules/@dnd-kit/core/dist/core.esm.js
var import_dist16 = __toESM(require_dist());
var import_dist17 = __toESM(require_dist2());
var import_dist18 = __toESM(require_dist3());
var import_react6 = __toESM(require_react());
var import_react_dom = __toESM(require_react_dom());

// node_modules/@dnd-kit/utilities/dist/utilities.esm.js
var import_dist10 = __toESM(require_dist());
var import_dist11 = __toESM(require_dist2());
var import_dist12 = __toESM(require_dist3());
var import_react4 = __toESM(require_react());
function useCombinedRefs() {
  for (var _len = arguments.length, refs = new Array(_len), _key = 0; _key < _len; _key++) {
    refs[_key] = arguments[_key];
  }
  return (0, import_react4.useMemo)(
    () => (node) => {
      refs.forEach((ref) => ref(node));
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    refs
  );
}
var canUseDOM = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
function isWindow(element) {
  const elementString = Object.prototype.toString.call(element);
  return elementString === "[object Window]" || // In Electron context the Window object serializes to [object global]
  elementString === "[object global]";
}
function isNode(node) {
  return "nodeType" in node;
}
function getWindow(target) {
  var _target$ownerDocument, _target$ownerDocument2;
  if (!target) {
    return window;
  }
  if (isWindow(target)) {
    return target;
  }
  if (!isNode(target)) {
    return window;
  }
  return (_target$ownerDocument = (_target$ownerDocument2 = target.ownerDocument) == null ? void 0 : _target$ownerDocument2.defaultView) != null ? _target$ownerDocument : window;
}
function isDocument(node) {
  const {
    Document
  } = getWindow(node);
  return node instanceof Document;
}
function isHTMLElement(node) {
  if (isWindow(node)) {
    return false;
  }
  return node instanceof getWindow(node).HTMLElement;
}
function isSVGElement(node) {
  return node instanceof getWindow(node).SVGElement;
}
function getOwnerDocument(target) {
  if (!target) {
    return document;
  }
  if (isWindow(target)) {
    return target.document;
  }
  if (!isNode(target)) {
    return document;
  }
  if (isDocument(target)) {
    return target;
  }
  if (isHTMLElement(target) || isSVGElement(target)) {
    return target.ownerDocument;
  }
  return document;
}
var useIsomorphicLayoutEffect = canUseDOM ? import_react4.useLayoutEffect : import_react4.useEffect;
function useEvent(handler) {
  const handlerRef = (0, import_react4.useRef)(handler);
  useIsomorphicLayoutEffect(() => {
    handlerRef.current = handler;
  });
  return (0, import_react4.useCallback)(function() {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    return handlerRef.current == null ? void 0 : handlerRef.current(...args);
  }, []);
}
function useInterval() {
  const intervalRef = (0, import_react4.useRef)(null);
  const set = (0, import_react4.useCallback)((listener, duration) => {
    intervalRef.current = setInterval(listener, duration);
  }, []);
  const clear = (0, import_react4.useCallback)(() => {
    if (intervalRef.current !== null) {
      clearInterval(intervalRef.current);
      intervalRef.current = null;
    }
  }, []);
  return [set, clear];
}
function useLatestValue(value, dependencies) {
  if (dependencies === void 0) {
    dependencies = [value];
  }
  const valueRef = (0, import_react4.useRef)(value);
  useIsomorphicLayoutEffect(() => {
    if (valueRef.current !== value) {
      valueRef.current = value;
    }
  }, dependencies);
  return valueRef;
}
function useLazyMemo(callback, dependencies) {
  const valueRef = (0, import_react4.useRef)();
  return (0, import_react4.useMemo)(
    () => {
      const newValue = callback(valueRef.current);
      valueRef.current = newValue;
      return newValue;
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [...dependencies]
  );
}
function useNodeRef(onChange) {
  const onChangeHandler = useEvent(onChange);
  const node = (0, import_react4.useRef)(null);
  const setNodeRef = (0, import_react4.useCallback)(
    (element) => {
      if (element !== node.current) {
        onChangeHandler == null ? void 0 : onChangeHandler(element, node.current);
      }
      node.current = element;
    },
    //eslint-disable-next-line
    []
  );
  return [node, setNodeRef];
}
function usePrevious(value) {
  const ref = (0, import_react4.useRef)();
  (0, import_react4.useEffect)(() => {
    ref.current = value;
  }, [value]);
  return ref.current;
}
var ids = {};
function useUniqueId(prefix, value) {
  return (0, import_react4.useMemo)(() => {
    if (value) {
      return value;
    }
    const id = ids[prefix] == null ? 0 : ids[prefix] + 1;
    ids[prefix] = id;
    return prefix + "-" + id;
  }, [prefix, value]);
}
function createAdjustmentFn(modifier) {
  return function(object) {
    for (var _len = arguments.length, adjustments = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      adjustments[_key - 1] = arguments[_key];
    }
    return adjustments.reduce((accumulator, adjustment) => {
      const entries = Object.entries(adjustment);
      for (const [key2, valueAdjustment] of entries) {
        const value = accumulator[key2];
        if (value != null) {
          accumulator[key2] = value + modifier * valueAdjustment;
        }
      }
      return accumulator;
    }, {
      ...object
    });
  };
}
var add = createAdjustmentFn(1);
var subtract = createAdjustmentFn(-1);
function hasViewportRelativeCoordinates(event) {
  return "clientX" in event && "clientY" in event;
}
function isKeyboardEvent(event) {
  if (!event) {
    return false;
  }
  const {
    KeyboardEvent
  } = getWindow(event.target);
  return KeyboardEvent && event instanceof KeyboardEvent;
}
function isTouchEvent(event) {
  if (!event) {
    return false;
  }
  const {
    TouchEvent
  } = getWindow(event.target);
  return TouchEvent && event instanceof TouchEvent;
}
function getEventCoordinates(event) {
  if (isTouchEvent(event)) {
    if (event.touches && event.touches.length) {
      const {
        clientX: x,
        clientY: y
      } = event.touches[0];
      return {
        x,
        y
      };
    } else if (event.changedTouches && event.changedTouches.length) {
      const {
        clientX: x,
        clientY: y
      } = event.changedTouches[0];
      return {
        x,
        y
      };
    }
  }
  if (hasViewportRelativeCoordinates(event)) {
    return {
      x: event.clientX,
      y: event.clientY
    };
  }
  return null;
}
var CSS = Object.freeze({
  Translate: {
    toString(transform) {
      if (!transform) {
        return;
      }
      const {
        x,
        y
      } = transform;
      return "translate3d(" + (x ? Math.round(x) : 0) + "px, " + (y ? Math.round(y) : 0) + "px, 0)";
    }
  },
  Scale: {
    toString(transform) {
      if (!transform) {
        return;
      }
      const {
        scaleX,
        scaleY
      } = transform;
      return "scaleX(" + scaleX + ") scaleY(" + scaleY + ")";
    }
  },
  Transform: {
    toString(transform) {
      if (!transform) {
        return;
      }
      return [CSS.Translate.toString(transform), CSS.Scale.toString(transform)].join(" ");
    }
  },
  Transition: {
    toString(_ref) {
      let {
        property,
        duration,
        easing
      } = _ref;
      return property + " " + duration + "ms " + easing;
    }
  }
});
var SELECTOR = "a,frame,iframe,input:not([type=hidden]):not(:disabled),select:not(:disabled),textarea:not(:disabled),button:not(:disabled),*[tabindex]";
function findFirstFocusableNode(element) {
  if (element.matches(SELECTOR)) {
    return element;
  }
  return element.querySelector(SELECTOR);
}

// node_modules/@dnd-kit/accessibility/dist/accessibility.esm.js
var import_dist13 = __toESM(require_dist());
var import_dist14 = __toESM(require_dist2());
var import_dist15 = __toESM(require_dist3());
var import_react5 = __toESM(require_react());
var hiddenStyles = {
  display: "none"
};
function HiddenText(_ref) {
  let {
    id,
    value
  } = _ref;
  return import_react5.default.createElement("div", {
    id,
    style: hiddenStyles
  }, value);
}
function LiveRegion(_ref) {
  let {
    id,
    announcement,
    ariaLiveType = "assertive"
  } = _ref;
  const visuallyHidden = {
    position: "fixed",
    top: 0,
    left: 0,
    width: 1,
    height: 1,
    margin: -1,
    border: 0,
    padding: 0,
    overflow: "hidden",
    clip: "rect(0 0 0 0)",
    clipPath: "inset(100%)",
    whiteSpace: "nowrap"
  };
  return import_react5.default.createElement("div", {
    id,
    style: visuallyHidden,
    role: "status",
    "aria-live": ariaLiveType,
    "aria-atomic": true
  }, announcement);
}
function useAnnouncement() {
  const [announcement, setAnnouncement] = (0, import_react5.useState)("");
  const announce = (0, import_react5.useCallback)((value) => {
    if (value != null) {
      setAnnouncement(value);
    }
  }, []);
  return {
    announce,
    announcement
  };
}

// node_modules/@dnd-kit/core/dist/core.esm.js
var DndMonitorContext = (0, import_react6.createContext)(null);
function useDndMonitor(listener) {
  const registerListener = (0, import_react6.useContext)(DndMonitorContext);
  (0, import_react6.useEffect)(() => {
    if (!registerListener) {
      throw new Error("useDndMonitor must be used within a children of <DndContext>");
    }
    const unsubscribe = registerListener(listener);
    return unsubscribe;
  }, [listener, registerListener]);
}
function useDndMonitorProvider() {
  const [listeners] = (0, import_react6.useState)(() => /* @__PURE__ */ new Set());
  const registerListener = (0, import_react6.useCallback)((listener) => {
    listeners.add(listener);
    return () => listeners.delete(listener);
  }, [listeners]);
  const dispatch = (0, import_react6.useCallback)((_ref) => {
    let {
      type,
      event
    } = _ref;
    listeners.forEach((listener) => {
      var _listener$type;
      return (_listener$type = listener[type]) == null ? void 0 : _listener$type.call(listener, event);
    });
  }, [listeners]);
  return [dispatch, registerListener];
}
var defaultScreenReaderInstructions = {
  draggable: "\n    To pick up a draggable item, press the space bar.\n    While dragging, use the arrow keys to move the item.\n    Press space again to drop the item in its new position, or press escape to cancel.\n  "
};
var defaultAnnouncements = {
  onDragStart(_ref) {
    let {
      active
    } = _ref;
    return "Picked up draggable item " + active.id + ".";
  },
  onDragOver(_ref2) {
    let {
      active,
      over
    } = _ref2;
    if (over) {
      return "Draggable item " + active.id + " was moved over droppable area " + over.id + ".";
    }
    return "Draggable item " + active.id + " is no longer over a droppable area.";
  },
  onDragEnd(_ref3) {
    let {
      active,
      over
    } = _ref3;
    if (over) {
      return "Draggable item " + active.id + " was dropped over droppable area " + over.id;
    }
    return "Draggable item " + active.id + " was dropped.";
  },
  onDragCancel(_ref4) {
    let {
      active
    } = _ref4;
    return "Dragging was cancelled. Draggable item " + active.id + " was dropped.";
  }
};
function Accessibility(_ref) {
  let {
    announcements = defaultAnnouncements,
    container,
    hiddenTextDescribedById,
    screenReaderInstructions = defaultScreenReaderInstructions
  } = _ref;
  const {
    announce,
    announcement
  } = useAnnouncement();
  const liveRegionId = useUniqueId("DndLiveRegion");
  const [mounted, setMounted] = (0, import_react6.useState)(false);
  (0, import_react6.useEffect)(() => {
    setMounted(true);
  }, []);
  useDndMonitor((0, import_react6.useMemo)(() => ({
    onDragStart(_ref2) {
      let {
        active
      } = _ref2;
      announce(announcements.onDragStart({
        active
      }));
    },
    onDragMove(_ref3) {
      let {
        active,
        over
      } = _ref3;
      if (announcements.onDragMove) {
        announce(announcements.onDragMove({
          active,
          over
        }));
      }
    },
    onDragOver(_ref4) {
      let {
        active,
        over
      } = _ref4;
      announce(announcements.onDragOver({
        active,
        over
      }));
    },
    onDragEnd(_ref5) {
      let {
        active,
        over
      } = _ref5;
      announce(announcements.onDragEnd({
        active,
        over
      }));
    },
    onDragCancel(_ref6) {
      let {
        active,
        over
      } = _ref6;
      announce(announcements.onDragCancel({
        active,
        over
      }));
    }
  }), [announce, announcements]));
  if (!mounted) {
    return null;
  }
  const markup = import_react6.default.createElement(import_react6.default.Fragment, null, import_react6.default.createElement(HiddenText, {
    id: hiddenTextDescribedById,
    value: screenReaderInstructions.draggable
  }), import_react6.default.createElement(LiveRegion, {
    id: liveRegionId,
    announcement
  }));
  return container ? (0, import_react_dom.createPortal)(markup, container) : markup;
}
var Action;
(function(Action2) {
  Action2["DragStart"] = "dragStart";
  Action2["DragMove"] = "dragMove";
  Action2["DragEnd"] = "dragEnd";
  Action2["DragCancel"] = "dragCancel";
  Action2["DragOver"] = "dragOver";
  Action2["RegisterDroppable"] = "registerDroppable";
  Action2["SetDroppableDisabled"] = "setDroppableDisabled";
  Action2["UnregisterDroppable"] = "unregisterDroppable";
})(Action || (Action = {}));
function noop() {
}
function useSensor(sensor, options2) {
  return (0, import_react6.useMemo)(
    () => ({
      sensor,
      options: options2 != null ? options2 : {}
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [sensor, options2]
  );
}
function useSensors() {
  for (var _len = arguments.length, sensors = new Array(_len), _key = 0; _key < _len; _key++) {
    sensors[_key] = arguments[_key];
  }
  return (0, import_react6.useMemo)(
    () => [...sensors].filter((sensor) => sensor != null),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [...sensors]
  );
}
var defaultCoordinates = Object.freeze({
  x: 0,
  y: 0
});
function distanceBetween(p1, p2) {
  return Math.sqrt(Math.pow(p1.x - p2.x, 2) + Math.pow(p1.y - p2.y, 2));
}
function getRelativeTransformOrigin(event, rect) {
  const eventCoordinates = getEventCoordinates(event);
  if (!eventCoordinates) {
    return "0 0";
  }
  const transformOrigin = {
    x: (eventCoordinates.x - rect.left) / rect.width * 100,
    y: (eventCoordinates.y - rect.top) / rect.height * 100
  };
  return transformOrigin.x + "% " + transformOrigin.y + "%";
}
function sortCollisionsAsc(_ref, _ref2) {
  let {
    data: {
      value: a
    }
  } = _ref;
  let {
    data: {
      value: b
    }
  } = _ref2;
  return a - b;
}
function sortCollisionsDesc(_ref3, _ref4) {
  let {
    data: {
      value: a
    }
  } = _ref3;
  let {
    data: {
      value: b
    }
  } = _ref4;
  return b - a;
}
function cornersOfRectangle(_ref5) {
  let {
    left,
    top,
    height,
    width
  } = _ref5;
  return [{
    x: left,
    y: top
  }, {
    x: left + width,
    y: top
  }, {
    x: left,
    y: top + height
  }, {
    x: left + width,
    y: top + height
  }];
}
function getFirstCollision(collisions, property) {
  if (!collisions || collisions.length === 0) {
    return null;
  }
  const [firstCollision] = collisions;
  return property ? firstCollision[property] : firstCollision;
}
function centerOfRectangle(rect, left, top) {
  if (left === void 0) {
    left = rect.left;
  }
  if (top === void 0) {
    top = rect.top;
  }
  return {
    x: left + rect.width * 0.5,
    y: top + rect.height * 0.5
  };
}
var closestCenter = (_ref) => {
  let {
    collisionRect,
    droppableRects,
    droppableContainers
  } = _ref;
  const centerRect = centerOfRectangle(collisionRect, collisionRect.left, collisionRect.top);
  const collisions = [];
  for (const droppableContainer of droppableContainers) {
    const {
      id
    } = droppableContainer;
    const rect = droppableRects.get(id);
    if (rect) {
      const distBetween = distanceBetween(centerOfRectangle(rect), centerRect);
      collisions.push({
        id,
        data: {
          droppableContainer,
          value: distBetween
        }
      });
    }
  }
  return collisions.sort(sortCollisionsAsc);
};
var closestCorners = (_ref) => {
  let {
    collisionRect,
    droppableRects,
    droppableContainers
  } = _ref;
  const corners = cornersOfRectangle(collisionRect);
  const collisions = [];
  for (const droppableContainer of droppableContainers) {
    const {
      id
    } = droppableContainer;
    const rect = droppableRects.get(id);
    if (rect) {
      const rectCorners = cornersOfRectangle(rect);
      const distances = corners.reduce((accumulator, corner, index) => {
        return accumulator + distanceBetween(rectCorners[index], corner);
      }, 0);
      const effectiveDistance = Number((distances / 4).toFixed(4));
      collisions.push({
        id,
        data: {
          droppableContainer,
          value: effectiveDistance
        }
      });
    }
  }
  return collisions.sort(sortCollisionsAsc);
};
function getIntersectionRatio(entry, target) {
  const top = Math.max(target.top, entry.top);
  const left = Math.max(target.left, entry.left);
  const right = Math.min(target.left + target.width, entry.left + entry.width);
  const bottom = Math.min(target.top + target.height, entry.top + entry.height);
  const width = right - left;
  const height = bottom - top;
  if (left < right && top < bottom) {
    const targetArea = target.width * target.height;
    const entryArea = entry.width * entry.height;
    const intersectionArea = width * height;
    const intersectionRatio = intersectionArea / (targetArea + entryArea - intersectionArea);
    return Number(intersectionRatio.toFixed(4));
  }
  return 0;
}
var rectIntersection = (_ref) => {
  let {
    collisionRect,
    droppableRects,
    droppableContainers
  } = _ref;
  const collisions = [];
  for (const droppableContainer of droppableContainers) {
    const {
      id
    } = droppableContainer;
    const rect = droppableRects.get(id);
    if (rect) {
      const intersectionRatio = getIntersectionRatio(rect, collisionRect);
      if (intersectionRatio > 0) {
        collisions.push({
          id,
          data: {
            droppableContainer,
            value: intersectionRatio
          }
        });
      }
    }
  }
  return collisions.sort(sortCollisionsDesc);
};
function adjustScale(transform, rect1, rect2) {
  return {
    ...transform,
    scaleX: rect1 && rect2 ? rect1.width / rect2.width : 1,
    scaleY: rect1 && rect2 ? rect1.height / rect2.height : 1
  };
}
function getRectDelta(rect1, rect2) {
  return rect1 && rect2 ? {
    x: rect1.left - rect2.left,
    y: rect1.top - rect2.top
  } : defaultCoordinates;
}
function createRectAdjustmentFn(modifier) {
  return function adjustClientRect(rect) {
    for (var _len = arguments.length, adjustments = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      adjustments[_key - 1] = arguments[_key];
    }
    return adjustments.reduce((acc, adjustment) => ({
      ...acc,
      top: acc.top + modifier * adjustment.y,
      bottom: acc.bottom + modifier * adjustment.y,
      left: acc.left + modifier * adjustment.x,
      right: acc.right + modifier * adjustment.x
    }), {
      ...rect
    });
  };
}
var getAdjustedRect = createRectAdjustmentFn(1);
function parseTransform(transform) {
  if (transform.startsWith("matrix3d(")) {
    const transformArray = transform.slice(9, -1).split(/, /);
    return {
      x: +transformArray[12],
      y: +transformArray[13],
      scaleX: +transformArray[0],
      scaleY: +transformArray[5]
    };
  } else if (transform.startsWith("matrix(")) {
    const transformArray = transform.slice(7, -1).split(/, /);
    return {
      x: +transformArray[4],
      y: +transformArray[5],
      scaleX: +transformArray[0],
      scaleY: +transformArray[3]
    };
  }
  return null;
}
function inverseTransform(rect, transform, transformOrigin) {
  const parsedTransform = parseTransform(transform);
  if (!parsedTransform) {
    return rect;
  }
  const {
    scaleX,
    scaleY,
    x: translateX,
    y: translateY
  } = parsedTransform;
  const x = rect.left - translateX - (1 - scaleX) * parseFloat(transformOrigin);
  const y = rect.top - translateY - (1 - scaleY) * parseFloat(transformOrigin.slice(transformOrigin.indexOf(" ") + 1));
  const w = scaleX ? rect.width / scaleX : rect.width;
  const h = scaleY ? rect.height / scaleY : rect.height;
  return {
    width: w,
    height: h,
    top: y,
    right: x + w,
    bottom: y + h,
    left: x
  };
}
var defaultOptions = {
  ignoreTransform: false
};
function getClientRect(element, options2) {
  if (options2 === void 0) {
    options2 = defaultOptions;
  }
  let rect = element.getBoundingClientRect();
  if (options2.ignoreTransform) {
    const {
      transform,
      transformOrigin
    } = getWindow(element).getComputedStyle(element);
    if (transform) {
      rect = inverseTransform(rect, transform, transformOrigin);
    }
  }
  const {
    top,
    left,
    width,
    height,
    bottom,
    right
  } = rect;
  return {
    top,
    left,
    width,
    height,
    bottom,
    right
  };
}
function getTransformAgnosticClientRect(element) {
  return getClientRect(element, {
    ignoreTransform: true
  });
}
function getWindowClientRect(element) {
  const width = element.innerWidth;
  const height = element.innerHeight;
  return {
    top: 0,
    left: 0,
    right: width,
    bottom: height,
    width,
    height
  };
}
function isFixed(node, computedStyle) {
  if (computedStyle === void 0) {
    computedStyle = getWindow(node).getComputedStyle(node);
  }
  return computedStyle.position === "fixed";
}
function isScrollable(element, computedStyle) {
  if (computedStyle === void 0) {
    computedStyle = getWindow(element).getComputedStyle(element);
  }
  const overflowRegex = /(auto|scroll|overlay)/;
  const properties2 = ["overflow", "overflowX", "overflowY"];
  return properties2.some((property) => {
    const value = computedStyle[property];
    return typeof value === "string" ? overflowRegex.test(value) : false;
  });
}
function getScrollableAncestors(element, limit) {
  const scrollParents = [];
  function findScrollableAncestors(node) {
    if (limit != null && scrollParents.length >= limit) {
      return scrollParents;
    }
    if (!node) {
      return scrollParents;
    }
    if (isDocument(node) && node.scrollingElement != null && !scrollParents.includes(node.scrollingElement)) {
      scrollParents.push(node.scrollingElement);
      return scrollParents;
    }
    if (!isHTMLElement(node) || isSVGElement(node)) {
      return scrollParents;
    }
    if (scrollParents.includes(node)) {
      return scrollParents;
    }
    const computedStyle = getWindow(element).getComputedStyle(node);
    if (node !== element) {
      if (isScrollable(node, computedStyle)) {
        scrollParents.push(node);
      }
    }
    if (isFixed(node, computedStyle)) {
      return scrollParents;
    }
    return findScrollableAncestors(node.parentNode);
  }
  if (!element) {
    return scrollParents;
  }
  return findScrollableAncestors(element);
}
function getFirstScrollableAncestor(node) {
  const [firstScrollableAncestor] = getScrollableAncestors(node, 1);
  return firstScrollableAncestor != null ? firstScrollableAncestor : null;
}
function getScrollableElement(element) {
  if (!canUseDOM || !element) {
    return null;
  }
  if (isWindow(element)) {
    return element;
  }
  if (!isNode(element)) {
    return null;
  }
  if (isDocument(element) || element === getOwnerDocument(element).scrollingElement) {
    return window;
  }
  if (isHTMLElement(element)) {
    return element;
  }
  return null;
}
function getScrollXCoordinate(element) {
  if (isWindow(element)) {
    return element.scrollX;
  }
  return element.scrollLeft;
}
function getScrollYCoordinate(element) {
  if (isWindow(element)) {
    return element.scrollY;
  }
  return element.scrollTop;
}
function getScrollCoordinates(element) {
  return {
    x: getScrollXCoordinate(element),
    y: getScrollYCoordinate(element)
  };
}
var Direction;
(function(Direction2) {
  Direction2[Direction2["Forward"] = 1] = "Forward";
  Direction2[Direction2["Backward"] = -1] = "Backward";
})(Direction || (Direction = {}));
function isDocumentScrollingElement(element) {
  if (!canUseDOM || !element) {
    return false;
  }
  return element === document.scrollingElement;
}
function getScrollPosition(scrollingContainer) {
  const minScroll = {
    x: 0,
    y: 0
  };
  const dimensions = isDocumentScrollingElement(scrollingContainer) ? {
    height: window.innerHeight,
    width: window.innerWidth
  } : {
    height: scrollingContainer.clientHeight,
    width: scrollingContainer.clientWidth
  };
  const maxScroll = {
    x: scrollingContainer.scrollWidth - dimensions.width,
    y: scrollingContainer.scrollHeight - dimensions.height
  };
  const isTop = scrollingContainer.scrollTop <= minScroll.y;
  const isLeft = scrollingContainer.scrollLeft <= minScroll.x;
  const isBottom = scrollingContainer.scrollTop >= maxScroll.y;
  const isRight = scrollingContainer.scrollLeft >= maxScroll.x;
  return {
    isTop,
    isLeft,
    isBottom,
    isRight,
    maxScroll,
    minScroll
  };
}
var defaultThreshold = {
  x: 0.2,
  y: 0.2
};
function getScrollDirectionAndSpeed(scrollContainer, scrollContainerRect, _ref, acceleration, thresholdPercentage) {
  let {
    top,
    left,
    right,
    bottom
  } = _ref;
  if (acceleration === void 0) {
    acceleration = 10;
  }
  if (thresholdPercentage === void 0) {
    thresholdPercentage = defaultThreshold;
  }
  const {
    isTop,
    isBottom,
    isLeft,
    isRight
  } = getScrollPosition(scrollContainer);
  const direction = {
    x: 0,
    y: 0
  };
  const speed = {
    x: 0,
    y: 0
  };
  const threshold = {
    height: scrollContainerRect.height * thresholdPercentage.y,
    width: scrollContainerRect.width * thresholdPercentage.x
  };
  if (!isTop && top <= scrollContainerRect.top + threshold.height) {
    direction.y = Direction.Backward;
    speed.y = acceleration * Math.abs((scrollContainerRect.top + threshold.height - top) / threshold.height);
  } else if (!isBottom && bottom >= scrollContainerRect.bottom - threshold.height) {
    direction.y = Direction.Forward;
    speed.y = acceleration * Math.abs((scrollContainerRect.bottom - threshold.height - bottom) / threshold.height);
  }
  if (!isRight && right >= scrollContainerRect.right - threshold.width) {
    direction.x = Direction.Forward;
    speed.x = acceleration * Math.abs((scrollContainerRect.right - threshold.width - right) / threshold.width);
  } else if (!isLeft && left <= scrollContainerRect.left + threshold.width) {
    direction.x = Direction.Backward;
    speed.x = acceleration * Math.abs((scrollContainerRect.left + threshold.width - left) / threshold.width);
  }
  return {
    direction,
    speed
  };
}
function getScrollElementRect(element) {
  if (element === document.scrollingElement) {
    const {
      innerWidth,
      innerHeight
    } = window;
    return {
      top: 0,
      left: 0,
      right: innerWidth,
      bottom: innerHeight,
      width: innerWidth,
      height: innerHeight
    };
  }
  const {
    top,
    left,
    right,
    bottom
  } = element.getBoundingClientRect();
  return {
    top,
    left,
    right,
    bottom,
    width: element.clientWidth,
    height: element.clientHeight
  };
}
function getScrollOffsets(scrollableAncestors) {
  return scrollableAncestors.reduce((acc, node) => {
    return add(acc, getScrollCoordinates(node));
  }, defaultCoordinates);
}
function getScrollXOffset(scrollableAncestors) {
  return scrollableAncestors.reduce((acc, node) => {
    return acc + getScrollXCoordinate(node);
  }, 0);
}
function getScrollYOffset(scrollableAncestors) {
  return scrollableAncestors.reduce((acc, node) => {
    return acc + getScrollYCoordinate(node);
  }, 0);
}
function scrollIntoViewIfNeeded(element, measure) {
  if (measure === void 0) {
    measure = getClientRect;
  }
  if (!element) {
    return;
  }
  const {
    top,
    left,
    bottom,
    right
  } = measure(element);
  const firstScrollableAncestor = getFirstScrollableAncestor(element);
  if (!firstScrollableAncestor) {
    return;
  }
  if (bottom <= 0 || right <= 0 || top >= window.innerHeight || left >= window.innerWidth) {
    element.scrollIntoView({
      block: "center",
      inline: "center"
    });
  }
}
var properties = [["x", ["left", "right"], getScrollXOffset], ["y", ["top", "bottom"], getScrollYOffset]];
var Rect = class {
  constructor(rect, element) {
    this.rect = void 0;
    this.width = void 0;
    this.height = void 0;
    this.top = void 0;
    this.bottom = void 0;
    this.right = void 0;
    this.left = void 0;
    const scrollableAncestors = getScrollableAncestors(element);
    const scrollOffsets = getScrollOffsets(scrollableAncestors);
    this.rect = {
      ...rect
    };
    this.width = rect.width;
    this.height = rect.height;
    for (const [axis, keys, getScrollOffset] of properties) {
      for (const key2 of keys) {
        Object.defineProperty(this, key2, {
          get: () => {
            const currentOffsets = getScrollOffset(scrollableAncestors);
            const scrollOffsetsDeltla = scrollOffsets[axis] - currentOffsets;
            return this.rect[key2] + scrollOffsetsDeltla;
          },
          enumerable: true
        });
      }
    }
    Object.defineProperty(this, "rect", {
      enumerable: false
    });
  }
};
var Listeners = class {
  constructor(target) {
    this.target = void 0;
    this.listeners = [];
    this.removeAll = () => {
      this.listeners.forEach((listener) => {
        var _this$target;
        return (_this$target = this.target) == null ? void 0 : _this$target.removeEventListener(...listener);
      });
    };
    this.target = target;
  }
  add(eventName, handler, options2) {
    var _this$target2;
    (_this$target2 = this.target) == null ? void 0 : _this$target2.addEventListener(eventName, handler, options2);
    this.listeners.push([eventName, handler, options2]);
  }
};
function getEventListenerTarget(target) {
  const {
    EventTarget
  } = getWindow(target);
  return target instanceof EventTarget ? target : getOwnerDocument(target);
}
function hasExceededDistance(delta, measurement) {
  const dx = Math.abs(delta.x);
  const dy = Math.abs(delta.y);
  if (typeof measurement === "number") {
    return Math.sqrt(dx ** 2 + dy ** 2) > measurement;
  }
  if ("x" in measurement && "y" in measurement) {
    return dx > measurement.x && dy > measurement.y;
  }
  if ("x" in measurement) {
    return dx > measurement.x;
  }
  if ("y" in measurement) {
    return dy > measurement.y;
  }
  return false;
}
var EventName;
(function(EventName2) {
  EventName2["Click"] = "click";
  EventName2["DragStart"] = "dragstart";
  EventName2["Keydown"] = "keydown";
  EventName2["ContextMenu"] = "contextmenu";
  EventName2["Resize"] = "resize";
  EventName2["SelectionChange"] = "selectionchange";
  EventName2["VisibilityChange"] = "visibilitychange";
})(EventName || (EventName = {}));
function preventDefault(event) {
  event.preventDefault();
}
function stopPropagation(event) {
  event.stopPropagation();
}
var KeyboardCode;
(function(KeyboardCode2) {
  KeyboardCode2["Space"] = "Space";
  KeyboardCode2["Down"] = "ArrowDown";
  KeyboardCode2["Right"] = "ArrowRight";
  KeyboardCode2["Left"] = "ArrowLeft";
  KeyboardCode2["Up"] = "ArrowUp";
  KeyboardCode2["Esc"] = "Escape";
  KeyboardCode2["Enter"] = "Enter";
  KeyboardCode2["Tab"] = "Tab";
})(KeyboardCode || (KeyboardCode = {}));
var defaultKeyboardCodes = {
  start: [KeyboardCode.Space, KeyboardCode.Enter],
  cancel: [KeyboardCode.Esc],
  end: [KeyboardCode.Space, KeyboardCode.Enter, KeyboardCode.Tab]
};
var defaultKeyboardCoordinateGetter = (event, _ref) => {
  let {
    currentCoordinates
  } = _ref;
  switch (event.code) {
    case KeyboardCode.Right:
      return {
        ...currentCoordinates,
        x: currentCoordinates.x + 25
      };
    case KeyboardCode.Left:
      return {
        ...currentCoordinates,
        x: currentCoordinates.x - 25
      };
    case KeyboardCode.Down:
      return {
        ...currentCoordinates,
        y: currentCoordinates.y + 25
      };
    case KeyboardCode.Up:
      return {
        ...currentCoordinates,
        y: currentCoordinates.y - 25
      };
  }
  return void 0;
};
var KeyboardSensor = class {
  constructor(props) {
    this.props = void 0;
    this.autoScrollEnabled = false;
    this.referenceCoordinates = void 0;
    this.listeners = void 0;
    this.windowListeners = void 0;
    this.props = props;
    const {
      event: {
        target
      }
    } = props;
    this.props = props;
    this.listeners = new Listeners(getOwnerDocument(target));
    this.windowListeners = new Listeners(getWindow(target));
    this.handleKeyDown = this.handleKeyDown.bind(this);
    this.handleCancel = this.handleCancel.bind(this);
    this.attach();
  }
  attach() {
    this.handleStart();
    this.windowListeners.add(EventName.Resize, this.handleCancel);
    this.windowListeners.add(EventName.VisibilityChange, this.handleCancel);
    setTimeout(() => this.listeners.add(EventName.Keydown, this.handleKeyDown));
  }
  handleStart() {
    const {
      activeNode,
      onStart
    } = this.props;
    const node = activeNode.node.current;
    if (node) {
      scrollIntoViewIfNeeded(node);
    }
    onStart(defaultCoordinates);
  }
  handleKeyDown(event) {
    if (isKeyboardEvent(event)) {
      const {
        active,
        context,
        options: options2
      } = this.props;
      const {
        keyboardCodes = defaultKeyboardCodes,
        coordinateGetter = defaultKeyboardCoordinateGetter,
        scrollBehavior = "smooth"
      } = options2;
      const {
        code
      } = event;
      if (keyboardCodes.end.includes(code)) {
        this.handleEnd(event);
        return;
      }
      if (keyboardCodes.cancel.includes(code)) {
        this.handleCancel(event);
        return;
      }
      const {
        collisionRect
      } = context.current;
      const currentCoordinates = collisionRect ? {
        x: collisionRect.left,
        y: collisionRect.top
      } : defaultCoordinates;
      if (!this.referenceCoordinates) {
        this.referenceCoordinates = currentCoordinates;
      }
      const newCoordinates = coordinateGetter(event, {
        active,
        context: context.current,
        currentCoordinates
      });
      if (newCoordinates) {
        const coordinatesDelta = subtract(newCoordinates, currentCoordinates);
        const scrollDelta = {
          x: 0,
          y: 0
        };
        const {
          scrollableAncestors
        } = context.current;
        for (const scrollContainer of scrollableAncestors) {
          const direction = event.code;
          const {
            isTop,
            isRight,
            isLeft,
            isBottom,
            maxScroll,
            minScroll
          } = getScrollPosition(scrollContainer);
          const scrollElementRect = getScrollElementRect(scrollContainer);
          const clampedCoordinates = {
            x: Math.min(direction === KeyboardCode.Right ? scrollElementRect.right - scrollElementRect.width / 2 : scrollElementRect.right, Math.max(direction === KeyboardCode.Right ? scrollElementRect.left : scrollElementRect.left + scrollElementRect.width / 2, newCoordinates.x)),
            y: Math.min(direction === KeyboardCode.Down ? scrollElementRect.bottom - scrollElementRect.height / 2 : scrollElementRect.bottom, Math.max(direction === KeyboardCode.Down ? scrollElementRect.top : scrollElementRect.top + scrollElementRect.height / 2, newCoordinates.y))
          };
          const canScrollX = direction === KeyboardCode.Right && !isRight || direction === KeyboardCode.Left && !isLeft;
          const canScrollY = direction === KeyboardCode.Down && !isBottom || direction === KeyboardCode.Up && !isTop;
          if (canScrollX && clampedCoordinates.x !== newCoordinates.x) {
            const newScrollCoordinates = scrollContainer.scrollLeft + coordinatesDelta.x;
            const canScrollToNewCoordinates = direction === KeyboardCode.Right && newScrollCoordinates <= maxScroll.x || direction === KeyboardCode.Left && newScrollCoordinates >= minScroll.x;
            if (canScrollToNewCoordinates && !coordinatesDelta.y) {
              scrollContainer.scrollTo({
                left: newScrollCoordinates,
                behavior: scrollBehavior
              });
              return;
            }
            if (canScrollToNewCoordinates) {
              scrollDelta.x = scrollContainer.scrollLeft - newScrollCoordinates;
            } else {
              scrollDelta.x = direction === KeyboardCode.Right ? scrollContainer.scrollLeft - maxScroll.x : scrollContainer.scrollLeft - minScroll.x;
            }
            if (scrollDelta.x) {
              scrollContainer.scrollBy({
                left: -scrollDelta.x,
                behavior: scrollBehavior
              });
            }
            break;
          } else if (canScrollY && clampedCoordinates.y !== newCoordinates.y) {
            const newScrollCoordinates = scrollContainer.scrollTop + coordinatesDelta.y;
            const canScrollToNewCoordinates = direction === KeyboardCode.Down && newScrollCoordinates <= maxScroll.y || direction === KeyboardCode.Up && newScrollCoordinates >= minScroll.y;
            if (canScrollToNewCoordinates && !coordinatesDelta.x) {
              scrollContainer.scrollTo({
                top: newScrollCoordinates,
                behavior: scrollBehavior
              });
              return;
            }
            if (canScrollToNewCoordinates) {
              scrollDelta.y = scrollContainer.scrollTop - newScrollCoordinates;
            } else {
              scrollDelta.y = direction === KeyboardCode.Down ? scrollContainer.scrollTop - maxScroll.y : scrollContainer.scrollTop - minScroll.y;
            }
            if (scrollDelta.y) {
              scrollContainer.scrollBy({
                top: -scrollDelta.y,
                behavior: scrollBehavior
              });
            }
            break;
          }
        }
        this.handleMove(event, add(subtract(newCoordinates, this.referenceCoordinates), scrollDelta));
      }
    }
  }
  handleMove(event, coordinates) {
    const {
      onMove
    } = this.props;
    event.preventDefault();
    onMove(coordinates);
  }
  handleEnd(event) {
    const {
      onEnd
    } = this.props;
    event.preventDefault();
    this.detach();
    onEnd();
  }
  handleCancel(event) {
    const {
      onCancel
    } = this.props;
    event.preventDefault();
    this.detach();
    onCancel();
  }
  detach() {
    this.listeners.removeAll();
    this.windowListeners.removeAll();
  }
};
KeyboardSensor.activators = [{
  eventName: "onKeyDown",
  handler: (event, _ref, _ref2) => {
    let {
      keyboardCodes = defaultKeyboardCodes,
      onActivation
    } = _ref;
    let {
      active
    } = _ref2;
    const {
      code
    } = event.nativeEvent;
    if (keyboardCodes.start.includes(code)) {
      const activator = active.activatorNode.current;
      if (activator && event.target !== activator) {
        return false;
      }
      event.preventDefault();
      onActivation == null ? void 0 : onActivation({
        event: event.nativeEvent
      });
      return true;
    }
    return false;
  }
}];
function isDistanceConstraint(constraint) {
  return Boolean(constraint && "distance" in constraint);
}
function isDelayConstraint(constraint) {
  return Boolean(constraint && "delay" in constraint);
}
var AbstractPointerSensor = class {
  constructor(props, events2, listenerTarget) {
    var _getEventCoordinates;
    if (listenerTarget === void 0) {
      listenerTarget = getEventListenerTarget(props.event.target);
    }
    this.props = void 0;
    this.events = void 0;
    this.autoScrollEnabled = true;
    this.document = void 0;
    this.activated = false;
    this.initialCoordinates = void 0;
    this.timeoutId = null;
    this.listeners = void 0;
    this.documentListeners = void 0;
    this.windowListeners = void 0;
    this.props = props;
    this.events = events2;
    const {
      event
    } = props;
    const {
      target
    } = event;
    this.props = props;
    this.events = events2;
    this.document = getOwnerDocument(target);
    this.documentListeners = new Listeners(this.document);
    this.listeners = new Listeners(listenerTarget);
    this.windowListeners = new Listeners(getWindow(target));
    this.initialCoordinates = (_getEventCoordinates = getEventCoordinates(event)) != null ? _getEventCoordinates : defaultCoordinates;
    this.handleStart = this.handleStart.bind(this);
    this.handleMove = this.handleMove.bind(this);
    this.handleEnd = this.handleEnd.bind(this);
    this.handleCancel = this.handleCancel.bind(this);
    this.handleKeydown = this.handleKeydown.bind(this);
    this.removeTextSelection = this.removeTextSelection.bind(this);
    this.attach();
  }
  attach() {
    const {
      events: events2,
      props: {
        options: {
          activationConstraint,
          bypassActivationConstraint
        }
      }
    } = this;
    this.listeners.add(events2.move.name, this.handleMove, {
      passive: false
    });
    this.listeners.add(events2.end.name, this.handleEnd);
    if (events2.cancel) {
      this.listeners.add(events2.cancel.name, this.handleCancel);
    }
    this.windowListeners.add(EventName.Resize, this.handleCancel);
    this.windowListeners.add(EventName.DragStart, preventDefault);
    this.windowListeners.add(EventName.VisibilityChange, this.handleCancel);
    this.windowListeners.add(EventName.ContextMenu, preventDefault);
    this.documentListeners.add(EventName.Keydown, this.handleKeydown);
    if (activationConstraint) {
      if (bypassActivationConstraint != null && bypassActivationConstraint({
        event: this.props.event,
        activeNode: this.props.activeNode,
        options: this.props.options
      })) {
        return this.handleStart();
      }
      if (isDelayConstraint(activationConstraint)) {
        this.timeoutId = setTimeout(this.handleStart, activationConstraint.delay);
        this.handlePending(activationConstraint);
        return;
      }
      if (isDistanceConstraint(activationConstraint)) {
        this.handlePending(activationConstraint);
        return;
      }
    }
    this.handleStart();
  }
  detach() {
    this.listeners.removeAll();
    this.windowListeners.removeAll();
    setTimeout(this.documentListeners.removeAll, 50);
    if (this.timeoutId !== null) {
      clearTimeout(this.timeoutId);
      this.timeoutId = null;
    }
  }
  handlePending(constraint, offset) {
    const {
      active,
      onPending
    } = this.props;
    onPending(active, constraint, this.initialCoordinates, offset);
  }
  handleStart() {
    const {
      initialCoordinates
    } = this;
    const {
      onStart
    } = this.props;
    if (initialCoordinates) {
      this.activated = true;
      this.documentListeners.add(EventName.Click, stopPropagation, {
        capture: true
      });
      this.removeTextSelection();
      this.documentListeners.add(EventName.SelectionChange, this.removeTextSelection);
      onStart(initialCoordinates);
    }
  }
  handleMove(event) {
    var _getEventCoordinates2;
    const {
      activated,
      initialCoordinates,
      props
    } = this;
    const {
      onMove,
      options: {
        activationConstraint
      }
    } = props;
    if (!initialCoordinates) {
      return;
    }
    const coordinates = (_getEventCoordinates2 = getEventCoordinates(event)) != null ? _getEventCoordinates2 : defaultCoordinates;
    const delta = subtract(initialCoordinates, coordinates);
    if (!activated && activationConstraint) {
      if (isDistanceConstraint(activationConstraint)) {
        if (activationConstraint.tolerance != null && hasExceededDistance(delta, activationConstraint.tolerance)) {
          return this.handleCancel();
        }
        if (hasExceededDistance(delta, activationConstraint.distance)) {
          return this.handleStart();
        }
      }
      if (isDelayConstraint(activationConstraint)) {
        if (hasExceededDistance(delta, activationConstraint.tolerance)) {
          return this.handleCancel();
        }
      }
      this.handlePending(activationConstraint, delta);
      return;
    }
    if (event.cancelable) {
      event.preventDefault();
    }
    onMove(coordinates);
  }
  handleEnd() {
    const {
      onAbort,
      onEnd
    } = this.props;
    this.detach();
    if (!this.activated) {
      onAbort(this.props.active);
    }
    onEnd();
  }
  handleCancel() {
    const {
      onAbort,
      onCancel
    } = this.props;
    this.detach();
    if (!this.activated) {
      onAbort(this.props.active);
    }
    onCancel();
  }
  handleKeydown(event) {
    if (event.code === KeyboardCode.Esc) {
      this.handleCancel();
    }
  }
  removeTextSelection() {
    var _this$document$getSel;
    (_this$document$getSel = this.document.getSelection()) == null ? void 0 : _this$document$getSel.removeAllRanges();
  }
};
var events = {
  cancel: {
    name: "pointercancel"
  },
  move: {
    name: "pointermove"
  },
  end: {
    name: "pointerup"
  }
};
var PointerSensor = class extends AbstractPointerSensor {
  constructor(props) {
    const {
      event
    } = props;
    const listenerTarget = getOwnerDocument(event.target);
    super(props, events, listenerTarget);
  }
};
PointerSensor.activators = [{
  eventName: "onPointerDown",
  handler: (_ref, _ref2) => {
    let {
      nativeEvent: event
    } = _ref;
    let {
      onActivation
    } = _ref2;
    if (!event.isPrimary || event.button !== 0) {
      return false;
    }
    onActivation == null ? void 0 : onActivation({
      event
    });
    return true;
  }
}];
var events$1 = {
  move: {
    name: "mousemove"
  },
  end: {
    name: "mouseup"
  }
};
var MouseButton;
(function(MouseButton2) {
  MouseButton2[MouseButton2["RightClick"] = 2] = "RightClick";
})(MouseButton || (MouseButton = {}));
var MouseSensor = class extends AbstractPointerSensor {
  constructor(props) {
    super(props, events$1, getOwnerDocument(props.event.target));
  }
};
MouseSensor.activators = [{
  eventName: "onMouseDown",
  handler: (_ref, _ref2) => {
    let {
      nativeEvent: event
    } = _ref;
    let {
      onActivation
    } = _ref2;
    if (event.button === MouseButton.RightClick) {
      return false;
    }
    onActivation == null ? void 0 : onActivation({
      event
    });
    return true;
  }
}];
var events$2 = {
  cancel: {
    name: "touchcancel"
  },
  move: {
    name: "touchmove"
  },
  end: {
    name: "touchend"
  }
};
var TouchSensor = class extends AbstractPointerSensor {
  constructor(props) {
    super(props, events$2);
  }
  static setup() {
    window.addEventListener(events$2.move.name, noop2, {
      capture: false,
      passive: false
    });
    return function teardown() {
      window.removeEventListener(events$2.move.name, noop2);
    };
    function noop2() {
    }
  }
};
TouchSensor.activators = [{
  eventName: "onTouchStart",
  handler: (_ref, _ref2) => {
    let {
      nativeEvent: event
    } = _ref;
    let {
      onActivation
    } = _ref2;
    const {
      touches
    } = event;
    if (touches.length > 1) {
      return false;
    }
    onActivation == null ? void 0 : onActivation({
      event
    });
    return true;
  }
}];
var AutoScrollActivator;
(function(AutoScrollActivator2) {
  AutoScrollActivator2[AutoScrollActivator2["Pointer"] = 0] = "Pointer";
  AutoScrollActivator2[AutoScrollActivator2["DraggableRect"] = 1] = "DraggableRect";
})(AutoScrollActivator || (AutoScrollActivator = {}));
var TraversalOrder;
(function(TraversalOrder2) {
  TraversalOrder2[TraversalOrder2["TreeOrder"] = 0] = "TreeOrder";
  TraversalOrder2[TraversalOrder2["ReversedTreeOrder"] = 1] = "ReversedTreeOrder";
})(TraversalOrder || (TraversalOrder = {}));
function useAutoScroller(_ref) {
  let {
    acceleration,
    activator = AutoScrollActivator.Pointer,
    canScroll,
    draggingRect,
    enabled,
    interval = 5,
    order = TraversalOrder.TreeOrder,
    pointerCoordinates,
    scrollableAncestors,
    scrollableAncestorRects,
    delta,
    threshold
  } = _ref;
  const scrollIntent = useScrollIntent({
    delta,
    disabled: !enabled
  });
  const [setAutoScrollInterval, clearAutoScrollInterval] = useInterval();
  const scrollSpeed = (0, import_react6.useRef)({
    x: 0,
    y: 0
  });
  const scrollDirection = (0, import_react6.useRef)({
    x: 0,
    y: 0
  });
  const rect = (0, import_react6.useMemo)(() => {
    switch (activator) {
      case AutoScrollActivator.Pointer:
        return pointerCoordinates ? {
          top: pointerCoordinates.y,
          bottom: pointerCoordinates.y,
          left: pointerCoordinates.x,
          right: pointerCoordinates.x
        } : null;
      case AutoScrollActivator.DraggableRect:
        return draggingRect;
    }
  }, [activator, draggingRect, pointerCoordinates]);
  const scrollContainerRef = (0, import_react6.useRef)(null);
  const autoScroll = (0, import_react6.useCallback)(() => {
    const scrollContainer = scrollContainerRef.current;
    if (!scrollContainer) {
      return;
    }
    const scrollLeft = scrollSpeed.current.x * scrollDirection.current.x;
    const scrollTop = scrollSpeed.current.y * scrollDirection.current.y;
    scrollContainer.scrollBy(scrollLeft, scrollTop);
  }, []);
  const sortedScrollableAncestors = (0, import_react6.useMemo)(() => order === TraversalOrder.TreeOrder ? [...scrollableAncestors].reverse() : scrollableAncestors, [order, scrollableAncestors]);
  (0, import_react6.useEffect)(
    () => {
      if (!enabled || !scrollableAncestors.length || !rect) {
        clearAutoScrollInterval();
        return;
      }
      for (const scrollContainer of sortedScrollableAncestors) {
        if ((canScroll == null ? void 0 : canScroll(scrollContainer)) === false) {
          continue;
        }
        const index = scrollableAncestors.indexOf(scrollContainer);
        const scrollContainerRect = scrollableAncestorRects[index];
        if (!scrollContainerRect) {
          continue;
        }
        const {
          direction,
          speed
        } = getScrollDirectionAndSpeed(scrollContainer, scrollContainerRect, rect, acceleration, threshold);
        for (const axis of ["x", "y"]) {
          if (!scrollIntent[axis][direction[axis]]) {
            speed[axis] = 0;
            direction[axis] = 0;
          }
        }
        if (speed.x > 0 || speed.y > 0) {
          clearAutoScrollInterval();
          scrollContainerRef.current = scrollContainer;
          setAutoScrollInterval(autoScroll, interval);
          scrollSpeed.current = speed;
          scrollDirection.current = direction;
          return;
        }
      }
      scrollSpeed.current = {
        x: 0,
        y: 0
      };
      scrollDirection.current = {
        x: 0,
        y: 0
      };
      clearAutoScrollInterval();
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      acceleration,
      autoScroll,
      canScroll,
      clearAutoScrollInterval,
      enabled,
      interval,
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(rect),
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(scrollIntent),
      setAutoScrollInterval,
      scrollableAncestors,
      sortedScrollableAncestors,
      scrollableAncestorRects,
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(threshold)
    ]
  );
}
var defaultScrollIntent = {
  x: {
    [Direction.Backward]: false,
    [Direction.Forward]: false
  },
  y: {
    [Direction.Backward]: false,
    [Direction.Forward]: false
  }
};
function useScrollIntent(_ref2) {
  let {
    delta,
    disabled
  } = _ref2;
  const previousDelta = usePrevious(delta);
  return useLazyMemo((previousIntent) => {
    if (disabled || !previousDelta || !previousIntent) {
      return defaultScrollIntent;
    }
    const direction = {
      x: Math.sign(delta.x - previousDelta.x),
      y: Math.sign(delta.y - previousDelta.y)
    };
    return {
      x: {
        [Direction.Backward]: previousIntent.x[Direction.Backward] || direction.x === -1,
        [Direction.Forward]: previousIntent.x[Direction.Forward] || direction.x === 1
      },
      y: {
        [Direction.Backward]: previousIntent.y[Direction.Backward] || direction.y === -1,
        [Direction.Forward]: previousIntent.y[Direction.Forward] || direction.y === 1
      }
    };
  }, [disabled, delta, previousDelta]);
}
function useCachedNode(draggableNodes, id) {
  const draggableNode = id != null ? draggableNodes.get(id) : void 0;
  const node = draggableNode ? draggableNode.node.current : null;
  return useLazyMemo((cachedNode) => {
    var _ref;
    if (id == null) {
      return null;
    }
    return (_ref = node != null ? node : cachedNode) != null ? _ref : null;
  }, [node, id]);
}
function useCombineActivators(sensors, getSyntheticHandler) {
  return (0, import_react6.useMemo)(() => sensors.reduce((accumulator, sensor) => {
    const {
      sensor: Sensor
    } = sensor;
    const sensorActivators = Sensor.activators.map((activator) => ({
      eventName: activator.eventName,
      handler: getSyntheticHandler(activator.handler, sensor)
    }));
    return [...accumulator, ...sensorActivators];
  }, []), [sensors, getSyntheticHandler]);
}
var MeasuringStrategy;
(function(MeasuringStrategy2) {
  MeasuringStrategy2[MeasuringStrategy2["Always"] = 0] = "Always";
  MeasuringStrategy2[MeasuringStrategy2["BeforeDragging"] = 1] = "BeforeDragging";
  MeasuringStrategy2[MeasuringStrategy2["WhileDragging"] = 2] = "WhileDragging";
})(MeasuringStrategy || (MeasuringStrategy = {}));
var MeasuringFrequency;
(function(MeasuringFrequency2) {
  MeasuringFrequency2["Optimized"] = "optimized";
})(MeasuringFrequency || (MeasuringFrequency = {}));
var defaultValue = /* @__PURE__ */ new Map();
function useDroppableMeasuring(containers, _ref) {
  let {
    dragging,
    dependencies,
    config
  } = _ref;
  const [queue, setQueue] = (0, import_react6.useState)(null);
  const {
    frequency,
    measure,
    strategy
  } = config;
  const containersRef = (0, import_react6.useRef)(containers);
  const disabled = isDisabled();
  const disabledRef = useLatestValue(disabled);
  const measureDroppableContainers = (0, import_react6.useCallback)(function(ids2) {
    if (ids2 === void 0) {
      ids2 = [];
    }
    if (disabledRef.current) {
      return;
    }
    setQueue((value) => {
      if (value === null) {
        return ids2;
      }
      return value.concat(ids2.filter((id) => !value.includes(id)));
    });
  }, [disabledRef]);
  const timeoutId = (0, import_react6.useRef)(null);
  const droppableRects = useLazyMemo((previousValue) => {
    if (disabled && !dragging) {
      return defaultValue;
    }
    if (!previousValue || previousValue === defaultValue || containersRef.current !== containers || queue != null) {
      const map = /* @__PURE__ */ new Map();
      for (let container of containers) {
        if (!container) {
          continue;
        }
        if (queue && queue.length > 0 && !queue.includes(container.id) && container.rect.current) {
          map.set(container.id, container.rect.current);
          continue;
        }
        const node = container.node.current;
        const rect = node ? new Rect(measure(node), node) : null;
        container.rect.current = rect;
        if (rect) {
          map.set(container.id, rect);
        }
      }
      return map;
    }
    return previousValue;
  }, [containers, queue, dragging, disabled, measure]);
  (0, import_react6.useEffect)(() => {
    containersRef.current = containers;
  }, [containers]);
  (0, import_react6.useEffect)(
    () => {
      if (disabled) {
        return;
      }
      measureDroppableContainers();
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [dragging, disabled]
  );
  (0, import_react6.useEffect)(
    () => {
      if (queue && queue.length > 0) {
        setQueue(null);
      }
    },
    //eslint-disable-next-line react-hooks/exhaustive-deps
    [JSON.stringify(queue)]
  );
  (0, import_react6.useEffect)(
    () => {
      if (disabled || typeof frequency !== "number" || timeoutId.current !== null) {
        return;
      }
      timeoutId.current = setTimeout(() => {
        measureDroppableContainers();
        timeoutId.current = null;
      }, frequency);
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [frequency, disabled, measureDroppableContainers, ...dependencies]
  );
  return {
    droppableRects,
    measureDroppableContainers,
    measuringScheduled: queue != null
  };
  function isDisabled() {
    switch (strategy) {
      case MeasuringStrategy.Always:
        return false;
      case MeasuringStrategy.BeforeDragging:
        return dragging;
      default:
        return !dragging;
    }
  }
}
function useInitialValue(value, computeFn) {
  return useLazyMemo((previousValue) => {
    if (!value) {
      return null;
    }
    if (previousValue) {
      return previousValue;
    }
    return typeof computeFn === "function" ? computeFn(value) : value;
  }, [computeFn, value]);
}
function useInitialRect(node, measure) {
  return useInitialValue(node, measure);
}
function useMutationObserver(_ref) {
  let {
    callback,
    disabled
  } = _ref;
  const handleMutations = useEvent(callback);
  const mutationObserver = (0, import_react6.useMemo)(() => {
    if (disabled || typeof window === "undefined" || typeof window.MutationObserver === "undefined") {
      return void 0;
    }
    const {
      MutationObserver: MutationObserver2
    } = window;
    return new MutationObserver2(handleMutations);
  }, [handleMutations, disabled]);
  (0, import_react6.useEffect)(() => {
    return () => mutationObserver == null ? void 0 : mutationObserver.disconnect();
  }, [mutationObserver]);
  return mutationObserver;
}
function useResizeObserver(_ref) {
  let {
    callback,
    disabled
  } = _ref;
  const handleResize = useEvent(callback);
  const resizeObserver = (0, import_react6.useMemo)(
    () => {
      if (disabled || typeof window === "undefined" || typeof window.ResizeObserver === "undefined") {
        return void 0;
      }
      const {
        ResizeObserver: ResizeObserver2
      } = window;
      return new ResizeObserver2(handleResize);
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [disabled]
  );
  (0, import_react6.useEffect)(() => {
    return () => resizeObserver == null ? void 0 : resizeObserver.disconnect();
  }, [resizeObserver]);
  return resizeObserver;
}
function defaultMeasure(element) {
  return new Rect(getClientRect(element), element);
}
function useRect(element, measure, fallbackRect) {
  if (measure === void 0) {
    measure = defaultMeasure;
  }
  const [rect, setRect] = (0, import_react6.useState)(null);
  function measureRect() {
    setRect((currentRect) => {
      if (!element) {
        return null;
      }
      if (element.isConnected === false) {
        var _ref;
        return (_ref = currentRect != null ? currentRect : fallbackRect) != null ? _ref : null;
      }
      const newRect = measure(element);
      if (JSON.stringify(currentRect) === JSON.stringify(newRect)) {
        return currentRect;
      }
      return newRect;
    });
  }
  const mutationObserver = useMutationObserver({
    callback(records) {
      if (!element) {
        return;
      }
      for (const record of records) {
        const {
          type,
          target
        } = record;
        if (type === "childList" && target instanceof HTMLElement && target.contains(element)) {
          measureRect();
          break;
        }
      }
    }
  });
  const resizeObserver = useResizeObserver({
    callback: measureRect
  });
  useIsomorphicLayoutEffect(() => {
    measureRect();
    if (element) {
      resizeObserver == null ? void 0 : resizeObserver.observe(element);
      mutationObserver == null ? void 0 : mutationObserver.observe(document.body, {
        childList: true,
        subtree: true
      });
    } else {
      resizeObserver == null ? void 0 : resizeObserver.disconnect();
      mutationObserver == null ? void 0 : mutationObserver.disconnect();
    }
  }, [element]);
  return rect;
}
function useRectDelta(rect) {
  const initialRect = useInitialValue(rect);
  return getRectDelta(rect, initialRect);
}
var defaultValue$1 = [];
function useScrollableAncestors(node) {
  const previousNode = (0, import_react6.useRef)(node);
  const ancestors = useLazyMemo((previousValue) => {
    if (!node) {
      return defaultValue$1;
    }
    if (previousValue && previousValue !== defaultValue$1 && node && previousNode.current && node.parentNode === previousNode.current.parentNode) {
      return previousValue;
    }
    return getScrollableAncestors(node);
  }, [node]);
  (0, import_react6.useEffect)(() => {
    previousNode.current = node;
  }, [node]);
  return ancestors;
}
function useScrollOffsets(elements) {
  const [scrollCoordinates, setScrollCoordinates] = (0, import_react6.useState)(null);
  const prevElements = (0, import_react6.useRef)(elements);
  const handleScroll = (0, import_react6.useCallback)((event) => {
    const scrollingElement = getScrollableElement(event.target);
    if (!scrollingElement) {
      return;
    }
    setScrollCoordinates((scrollCoordinates2) => {
      if (!scrollCoordinates2) {
        return null;
      }
      scrollCoordinates2.set(scrollingElement, getScrollCoordinates(scrollingElement));
      return new Map(scrollCoordinates2);
    });
  }, []);
  (0, import_react6.useEffect)(() => {
    const previousElements = prevElements.current;
    if (elements !== previousElements) {
      cleanup(previousElements);
      const entries = elements.map((element) => {
        const scrollableElement = getScrollableElement(element);
        if (scrollableElement) {
          scrollableElement.addEventListener("scroll", handleScroll, {
            passive: true
          });
          return [scrollableElement, getScrollCoordinates(scrollableElement)];
        }
        return null;
      }).filter((entry) => entry != null);
      setScrollCoordinates(entries.length ? new Map(entries) : null);
      prevElements.current = elements;
    }
    return () => {
      cleanup(elements);
      cleanup(previousElements);
    };
    function cleanup(elements2) {
      elements2.forEach((element) => {
        const scrollableElement = getScrollableElement(element);
        scrollableElement == null ? void 0 : scrollableElement.removeEventListener("scroll", handleScroll);
      });
    }
  }, [handleScroll, elements]);
  return (0, import_react6.useMemo)(() => {
    if (elements.length) {
      return scrollCoordinates ? Array.from(scrollCoordinates.values()).reduce((acc, coordinates) => add(acc, coordinates), defaultCoordinates) : getScrollOffsets(elements);
    }
    return defaultCoordinates;
  }, [elements, scrollCoordinates]);
}
function useScrollOffsetsDelta(scrollOffsets, dependencies) {
  if (dependencies === void 0) {
    dependencies = [];
  }
  const initialScrollOffsets = (0, import_react6.useRef)(null);
  (0, import_react6.useEffect)(
    () => {
      initialScrollOffsets.current = null;
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    dependencies
  );
  (0, import_react6.useEffect)(() => {
    const hasScrollOffsets = scrollOffsets !== defaultCoordinates;
    if (hasScrollOffsets && !initialScrollOffsets.current) {
      initialScrollOffsets.current = scrollOffsets;
    }
    if (!hasScrollOffsets && initialScrollOffsets.current) {
      initialScrollOffsets.current = null;
    }
  }, [scrollOffsets]);
  return initialScrollOffsets.current ? subtract(scrollOffsets, initialScrollOffsets.current) : defaultCoordinates;
}
function useSensorSetup(sensors) {
  (0, import_react6.useEffect)(
    () => {
      if (!canUseDOM) {
        return;
      }
      const teardownFns = sensors.map((_ref) => {
        let {
          sensor
        } = _ref;
        return sensor.setup == null ? void 0 : sensor.setup();
      });
      return () => {
        for (const teardown of teardownFns) {
          teardown == null ? void 0 : teardown();
        }
      };
    },
    // TO-DO: Sensors length could theoretically change which would not be a valid dependency
    // eslint-disable-next-line react-hooks/exhaustive-deps
    sensors.map((_ref2) => {
      let {
        sensor
      } = _ref2;
      return sensor;
    })
  );
}
function useSyntheticListeners(listeners, id) {
  return (0, import_react6.useMemo)(() => {
    return listeners.reduce((acc, _ref) => {
      let {
        eventName,
        handler
      } = _ref;
      acc[eventName] = (event) => {
        handler(event, id);
      };
      return acc;
    }, {});
  }, [listeners, id]);
}
function useWindowRect(element) {
  return (0, import_react6.useMemo)(() => element ? getWindowClientRect(element) : null, [element]);
}
var defaultValue$2 = [];
function useRects(elements, measure) {
  if (measure === void 0) {
    measure = getClientRect;
  }
  const [firstElement] = elements;
  const windowRect = useWindowRect(firstElement ? getWindow(firstElement) : null);
  const [rects, setRects] = (0, import_react6.useState)(defaultValue$2);
  function measureRects() {
    setRects(() => {
      if (!elements.length) {
        return defaultValue$2;
      }
      return elements.map((element) => isDocumentScrollingElement(element) ? windowRect : new Rect(measure(element), element));
    });
  }
  const resizeObserver = useResizeObserver({
    callback: measureRects
  });
  useIsomorphicLayoutEffect(() => {
    resizeObserver == null ? void 0 : resizeObserver.disconnect();
    measureRects();
    elements.forEach((element) => resizeObserver == null ? void 0 : resizeObserver.observe(element));
  }, [elements]);
  return rects;
}
function getMeasurableNode(node) {
  if (!node) {
    return null;
  }
  if (node.children.length > 1) {
    return node;
  }
  const firstChild = node.children[0];
  return isHTMLElement(firstChild) ? firstChild : node;
}
function useDragOverlayMeasuring(_ref) {
  let {
    measure
  } = _ref;
  const [rect, setRect] = (0, import_react6.useState)(null);
  const handleResize = (0, import_react6.useCallback)((entries) => {
    for (const {
      target
    } of entries) {
      if (isHTMLElement(target)) {
        setRect((rect2) => {
          const newRect = measure(target);
          return rect2 ? {
            ...rect2,
            width: newRect.width,
            height: newRect.height
          } : newRect;
        });
        break;
      }
    }
  }, [measure]);
  const resizeObserver = useResizeObserver({
    callback: handleResize
  });
  const handleNodeChange = (0, import_react6.useCallback)((element) => {
    const node = getMeasurableNode(element);
    resizeObserver == null ? void 0 : resizeObserver.disconnect();
    if (node) {
      resizeObserver == null ? void 0 : resizeObserver.observe(node);
    }
    setRect(node ? measure(node) : null);
  }, [measure, resizeObserver]);
  const [nodeRef, setRef] = useNodeRef(handleNodeChange);
  return (0, import_react6.useMemo)(() => ({
    nodeRef,
    rect,
    setRef
  }), [rect, nodeRef, setRef]);
}
var defaultSensors = [{
  sensor: PointerSensor,
  options: {}
}, {
  sensor: KeyboardSensor,
  options: {}
}];
var defaultData = {
  current: {}
};
var defaultMeasuringConfiguration = {
  draggable: {
    measure: getTransformAgnosticClientRect
  },
  droppable: {
    measure: getTransformAgnosticClientRect,
    strategy: MeasuringStrategy.WhileDragging,
    frequency: MeasuringFrequency.Optimized
  },
  dragOverlay: {
    measure: getClientRect
  }
};
var DroppableContainersMap = class extends Map {
  get(id) {
    var _super$get;
    return id != null ? (_super$get = super.get(id)) != null ? _super$get : void 0 : void 0;
  }
  toArray() {
    return Array.from(this.values());
  }
  getEnabled() {
    return this.toArray().filter((_ref) => {
      let {
        disabled
      } = _ref;
      return !disabled;
    });
  }
  getNodeFor(id) {
    var _this$get$node$curren, _this$get;
    return (_this$get$node$curren = (_this$get = this.get(id)) == null ? void 0 : _this$get.node.current) != null ? _this$get$node$curren : void 0;
  }
};
var defaultPublicContext = {
  activatorEvent: null,
  active: null,
  activeNode: null,
  activeNodeRect: null,
  collisions: null,
  containerNodeRect: null,
  draggableNodes: /* @__PURE__ */ new Map(),
  droppableRects: /* @__PURE__ */ new Map(),
  droppableContainers: new DroppableContainersMap(),
  over: null,
  dragOverlay: {
    nodeRef: {
      current: null
    },
    rect: null,
    setRef: noop
  },
  scrollableAncestors: [],
  scrollableAncestorRects: [],
  measuringConfiguration: defaultMeasuringConfiguration,
  measureDroppableContainers: noop,
  windowRect: null,
  measuringScheduled: false
};
var defaultInternalContext = {
  activatorEvent: null,
  activators: [],
  active: null,
  activeNodeRect: null,
  ariaDescribedById: {
    draggable: ""
  },
  dispatch: noop,
  draggableNodes: /* @__PURE__ */ new Map(),
  over: null,
  measureDroppableContainers: noop
};
var InternalContext = (0, import_react6.createContext)(defaultInternalContext);
var PublicContext = (0, import_react6.createContext)(defaultPublicContext);
function getInitialState() {
  return {
    draggable: {
      active: null,
      initialCoordinates: {
        x: 0,
        y: 0
      },
      nodes: /* @__PURE__ */ new Map(),
      translate: {
        x: 0,
        y: 0
      }
    },
    droppable: {
      containers: new DroppableContainersMap()
    }
  };
}
function reducer(state, action) {
  switch (action.type) {
    case Action.DragStart:
      return {
        ...state,
        draggable: {
          ...state.draggable,
          initialCoordinates: action.initialCoordinates,
          active: action.active
        }
      };
    case Action.DragMove:
      if (state.draggable.active == null) {
        return state;
      }
      return {
        ...state,
        draggable: {
          ...state.draggable,
          translate: {
            x: action.coordinates.x - state.draggable.initialCoordinates.x,
            y: action.coordinates.y - state.draggable.initialCoordinates.y
          }
        }
      };
    case Action.DragEnd:
    case Action.DragCancel:
      return {
        ...state,
        draggable: {
          ...state.draggable,
          active: null,
          initialCoordinates: {
            x: 0,
            y: 0
          },
          translate: {
            x: 0,
            y: 0
          }
        }
      };
    case Action.RegisterDroppable: {
      const {
        element
      } = action;
      const {
        id
      } = element;
      const containers = new DroppableContainersMap(state.droppable.containers);
      containers.set(id, element);
      return {
        ...state,
        droppable: {
          ...state.droppable,
          containers
        }
      };
    }
    case Action.SetDroppableDisabled: {
      const {
        id,
        key: key2,
        disabled
      } = action;
      const element = state.droppable.containers.get(id);
      if (!element || key2 !== element.key) {
        return state;
      }
      const containers = new DroppableContainersMap(state.droppable.containers);
      containers.set(id, {
        ...element,
        disabled
      });
      return {
        ...state,
        droppable: {
          ...state.droppable,
          containers
        }
      };
    }
    case Action.UnregisterDroppable: {
      const {
        id,
        key: key2
      } = action;
      const element = state.droppable.containers.get(id);
      if (!element || key2 !== element.key) {
        return state;
      }
      const containers = new DroppableContainersMap(state.droppable.containers);
      containers.delete(id);
      return {
        ...state,
        droppable: {
          ...state.droppable,
          containers
        }
      };
    }
    default: {
      return state;
    }
  }
}
function RestoreFocus(_ref) {
  let {
    disabled
  } = _ref;
  const {
    active,
    activatorEvent,
    draggableNodes
  } = (0, import_react6.useContext)(InternalContext);
  const previousActivatorEvent = usePrevious(activatorEvent);
  const previousActiveId = usePrevious(active == null ? void 0 : active.id);
  (0, import_react6.useEffect)(() => {
    if (disabled) {
      return;
    }
    if (!activatorEvent && previousActivatorEvent && previousActiveId != null) {
      if (!isKeyboardEvent(previousActivatorEvent)) {
        return;
      }
      if (document.activeElement === previousActivatorEvent.target) {
        return;
      }
      const draggableNode = draggableNodes.get(previousActiveId);
      if (!draggableNode) {
        return;
      }
      const {
        activatorNode,
        node
      } = draggableNode;
      if (!activatorNode.current && !node.current) {
        return;
      }
      requestAnimationFrame(() => {
        for (const element of [activatorNode.current, node.current]) {
          if (!element) {
            continue;
          }
          const focusableNode = findFirstFocusableNode(element);
          if (focusableNode) {
            focusableNode.focus();
            break;
          }
        }
      });
    }
  }, [activatorEvent, disabled, draggableNodes, previousActiveId, previousActivatorEvent]);
  return null;
}
function applyModifiers(modifiers, _ref) {
  let {
    transform,
    ...args
  } = _ref;
  return modifiers != null && modifiers.length ? modifiers.reduce((accumulator, modifier) => {
    return modifier({
      transform: accumulator,
      ...args
    });
  }, transform) : transform;
}
function useMeasuringConfiguration(config) {
  return (0, import_react6.useMemo)(
    () => ({
      draggable: {
        ...defaultMeasuringConfiguration.draggable,
        ...config == null ? void 0 : config.draggable
      },
      droppable: {
        ...defaultMeasuringConfiguration.droppable,
        ...config == null ? void 0 : config.droppable
      },
      dragOverlay: {
        ...defaultMeasuringConfiguration.dragOverlay,
        ...config == null ? void 0 : config.dragOverlay
      }
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [config == null ? void 0 : config.draggable, config == null ? void 0 : config.droppable, config == null ? void 0 : config.dragOverlay]
  );
}
function useLayoutShiftScrollCompensation(_ref) {
  let {
    activeNode,
    measure,
    initialRect,
    config = true
  } = _ref;
  const initialized = (0, import_react6.useRef)(false);
  const {
    x,
    y
  } = typeof config === "boolean" ? {
    x: config,
    y: config
  } : config;
  useIsomorphicLayoutEffect(() => {
    const disabled = !x && !y;
    if (disabled || !activeNode) {
      initialized.current = false;
      return;
    }
    if (initialized.current || !initialRect) {
      return;
    }
    const node = activeNode == null ? void 0 : activeNode.node.current;
    if (!node || node.isConnected === false) {
      return;
    }
    const rect = measure(node);
    const rectDelta = getRectDelta(rect, initialRect);
    if (!x) {
      rectDelta.x = 0;
    }
    if (!y) {
      rectDelta.y = 0;
    }
    initialized.current = true;
    if (Math.abs(rectDelta.x) > 0 || Math.abs(rectDelta.y) > 0) {
      const firstScrollableAncestor = getFirstScrollableAncestor(node);
      if (firstScrollableAncestor) {
        firstScrollableAncestor.scrollBy({
          top: rectDelta.y,
          left: rectDelta.x
        });
      }
    }
  }, [activeNode, x, y, initialRect, measure]);
}
var ActiveDraggableContext = (0, import_react6.createContext)({
  ...defaultCoordinates,
  scaleX: 1,
  scaleY: 1
});
var Status;
(function(Status2) {
  Status2[Status2["Uninitialized"] = 0] = "Uninitialized";
  Status2[Status2["Initializing"] = 1] = "Initializing";
  Status2[Status2["Initialized"] = 2] = "Initialized";
})(Status || (Status = {}));
var DndContext = (0, import_react6.memo)(function DndContext2(_ref) {
  var _sensorContext$curren, _dragOverlay$nodeRef$, _dragOverlay$rect, _over$rect;
  let {
    id,
    accessibility,
    autoScroll = true,
    children,
    sensors = defaultSensors,
    collisionDetection = rectIntersection,
    measuring,
    modifiers,
    ...props
  } = _ref;
  const store = (0, import_react6.useReducer)(reducer, void 0, getInitialState);
  const [state, dispatch] = store;
  const [dispatchMonitorEvent, registerMonitorListener] = useDndMonitorProvider();
  const [status, setStatus] = (0, import_react6.useState)(Status.Uninitialized);
  const isInitialized = status === Status.Initialized;
  const {
    draggable: {
      active: activeId,
      nodes: draggableNodes,
      translate
    },
    droppable: {
      containers: droppableContainers
    }
  } = state;
  const node = activeId != null ? draggableNodes.get(activeId) : null;
  const activeRects = (0, import_react6.useRef)({
    initial: null,
    translated: null
  });
  const active = (0, import_react6.useMemo)(() => {
    var _node$data;
    return activeId != null ? {
      id: activeId,
      // It's possible for the active node to unmount while dragging
      data: (_node$data = node == null ? void 0 : node.data) != null ? _node$data : defaultData,
      rect: activeRects
    } : null;
  }, [activeId, node]);
  const activeRef = (0, import_react6.useRef)(null);
  const [activeSensor, setActiveSensor] = (0, import_react6.useState)(null);
  const [activatorEvent, setActivatorEvent] = (0, import_react6.useState)(null);
  const latestProps = useLatestValue(props, Object.values(props));
  const draggableDescribedById = useUniqueId("DndDescribedBy", id);
  const enabledDroppableContainers = (0, import_react6.useMemo)(() => droppableContainers.getEnabled(), [droppableContainers]);
  const measuringConfiguration = useMeasuringConfiguration(measuring);
  const {
    droppableRects,
    measureDroppableContainers,
    measuringScheduled
  } = useDroppableMeasuring(enabledDroppableContainers, {
    dragging: isInitialized,
    dependencies: [translate.x, translate.y],
    config: measuringConfiguration.droppable
  });
  const activeNode = useCachedNode(draggableNodes, activeId);
  const activationCoordinates = (0, import_react6.useMemo)(() => activatorEvent ? getEventCoordinates(activatorEvent) : null, [activatorEvent]);
  const autoScrollOptions = getAutoScrollerOptions();
  const initialActiveNodeRect = useInitialRect(activeNode, measuringConfiguration.draggable.measure);
  useLayoutShiftScrollCompensation({
    activeNode: activeId != null ? draggableNodes.get(activeId) : null,
    config: autoScrollOptions.layoutShiftCompensation,
    initialRect: initialActiveNodeRect,
    measure: measuringConfiguration.draggable.measure
  });
  const activeNodeRect = useRect(activeNode, measuringConfiguration.draggable.measure, initialActiveNodeRect);
  const containerNodeRect = useRect(activeNode ? activeNode.parentElement : null);
  const sensorContext = (0, import_react6.useRef)({
    activatorEvent: null,
    active: null,
    activeNode,
    collisionRect: null,
    collisions: null,
    droppableRects,
    draggableNodes,
    draggingNode: null,
    draggingNodeRect: null,
    droppableContainers,
    over: null,
    scrollableAncestors: [],
    scrollAdjustedTranslate: null
  });
  const overNode = droppableContainers.getNodeFor((_sensorContext$curren = sensorContext.current.over) == null ? void 0 : _sensorContext$curren.id);
  const dragOverlay = useDragOverlayMeasuring({
    measure: measuringConfiguration.dragOverlay.measure
  });
  const draggingNode = (_dragOverlay$nodeRef$ = dragOverlay.nodeRef.current) != null ? _dragOverlay$nodeRef$ : activeNode;
  const draggingNodeRect = isInitialized ? (_dragOverlay$rect = dragOverlay.rect) != null ? _dragOverlay$rect : activeNodeRect : null;
  const usesDragOverlay = Boolean(dragOverlay.nodeRef.current && dragOverlay.rect);
  const nodeRectDelta = useRectDelta(usesDragOverlay ? null : activeNodeRect);
  const windowRect = useWindowRect(draggingNode ? getWindow(draggingNode) : null);
  const scrollableAncestors = useScrollableAncestors(isInitialized ? overNode != null ? overNode : activeNode : null);
  const scrollableAncestorRects = useRects(scrollableAncestors);
  const modifiedTranslate = applyModifiers(modifiers, {
    transform: {
      x: translate.x - nodeRectDelta.x,
      y: translate.y - nodeRectDelta.y,
      scaleX: 1,
      scaleY: 1
    },
    activatorEvent,
    active,
    activeNodeRect,
    containerNodeRect,
    draggingNodeRect,
    over: sensorContext.current.over,
    overlayNodeRect: dragOverlay.rect,
    scrollableAncestors,
    scrollableAncestorRects,
    windowRect
  });
  const pointerCoordinates = activationCoordinates ? add(activationCoordinates, translate) : null;
  const scrollOffsets = useScrollOffsets(scrollableAncestors);
  const scrollAdjustment = useScrollOffsetsDelta(scrollOffsets);
  const activeNodeScrollDelta = useScrollOffsetsDelta(scrollOffsets, [activeNodeRect]);
  const scrollAdjustedTranslate = add(modifiedTranslate, scrollAdjustment);
  const collisionRect = draggingNodeRect ? getAdjustedRect(draggingNodeRect, modifiedTranslate) : null;
  const collisions = active && collisionRect ? collisionDetection({
    active,
    collisionRect,
    droppableRects,
    droppableContainers: enabledDroppableContainers,
    pointerCoordinates
  }) : null;
  const overId = getFirstCollision(collisions, "id");
  const [over, setOver] = (0, import_react6.useState)(null);
  const appliedTranslate = usesDragOverlay ? modifiedTranslate : add(modifiedTranslate, activeNodeScrollDelta);
  const transform = adjustScale(appliedTranslate, (_over$rect = over == null ? void 0 : over.rect) != null ? _over$rect : null, activeNodeRect);
  const activeSensorRef = (0, import_react6.useRef)(null);
  const instantiateSensor = (0, import_react6.useCallback)(
    (event, _ref2) => {
      let {
        sensor: Sensor,
        options: options2
      } = _ref2;
      if (activeRef.current == null) {
        return;
      }
      const activeNode2 = draggableNodes.get(activeRef.current);
      if (!activeNode2) {
        return;
      }
      const activatorEvent2 = event.nativeEvent;
      const sensorInstance = new Sensor({
        active: activeRef.current,
        activeNode: activeNode2,
        event: activatorEvent2,
        options: options2,
        // Sensors need to be instantiated with refs for arguments that change over time
        // otherwise they are frozen in time with the stale arguments
        context: sensorContext,
        onAbort(id2) {
          const draggableNode = draggableNodes.get(id2);
          if (!draggableNode) {
            return;
          }
          const {
            onDragAbort
          } = latestProps.current;
          const event2 = {
            id: id2
          };
          onDragAbort == null ? void 0 : onDragAbort(event2);
          dispatchMonitorEvent({
            type: "onDragAbort",
            event: event2
          });
        },
        onPending(id2, constraint, initialCoordinates, offset) {
          const draggableNode = draggableNodes.get(id2);
          if (!draggableNode) {
            return;
          }
          const {
            onDragPending
          } = latestProps.current;
          const event2 = {
            id: id2,
            constraint,
            initialCoordinates,
            offset
          };
          onDragPending == null ? void 0 : onDragPending(event2);
          dispatchMonitorEvent({
            type: "onDragPending",
            event: event2
          });
        },
        onStart(initialCoordinates) {
          const id2 = activeRef.current;
          if (id2 == null) {
            return;
          }
          const draggableNode = draggableNodes.get(id2);
          if (!draggableNode) {
            return;
          }
          const {
            onDragStart
          } = latestProps.current;
          const event2 = {
            activatorEvent: activatorEvent2,
            active: {
              id: id2,
              data: draggableNode.data,
              rect: activeRects
            }
          };
          (0, import_react_dom.unstable_batchedUpdates)(() => {
            onDragStart == null ? void 0 : onDragStart(event2);
            setStatus(Status.Initializing);
            dispatch({
              type: Action.DragStart,
              initialCoordinates,
              active: id2
            });
            dispatchMonitorEvent({
              type: "onDragStart",
              event: event2
            });
            setActiveSensor(activeSensorRef.current);
            setActivatorEvent(activatorEvent2);
          });
        },
        onMove(coordinates) {
          dispatch({
            type: Action.DragMove,
            coordinates
          });
        },
        onEnd: createHandler(Action.DragEnd),
        onCancel: createHandler(Action.DragCancel)
      });
      activeSensorRef.current = sensorInstance;
      function createHandler(type) {
        return async function handler() {
          const {
            active: active2,
            collisions: collisions2,
            over: over2,
            scrollAdjustedTranslate: scrollAdjustedTranslate2
          } = sensorContext.current;
          let event2 = null;
          if (active2 && scrollAdjustedTranslate2) {
            const {
              cancelDrop
            } = latestProps.current;
            event2 = {
              activatorEvent: activatorEvent2,
              active: active2,
              collisions: collisions2,
              delta: scrollAdjustedTranslate2,
              over: over2
            };
            if (type === Action.DragEnd && typeof cancelDrop === "function") {
              const shouldCancel = await Promise.resolve(cancelDrop(event2));
              if (shouldCancel) {
                type = Action.DragCancel;
              }
            }
          }
          activeRef.current = null;
          (0, import_react_dom.unstable_batchedUpdates)(() => {
            dispatch({
              type
            });
            setStatus(Status.Uninitialized);
            setOver(null);
            setActiveSensor(null);
            setActivatorEvent(null);
            activeSensorRef.current = null;
            const eventName = type === Action.DragEnd ? "onDragEnd" : "onDragCancel";
            if (event2) {
              const handler2 = latestProps.current[eventName];
              handler2 == null ? void 0 : handler2(event2);
              dispatchMonitorEvent({
                type: eventName,
                event: event2
              });
            }
          });
        };
      }
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [draggableNodes]
  );
  const bindActivatorToSensorInstantiator = (0, import_react6.useCallback)((handler, sensor) => {
    return (event, active2) => {
      const nativeEvent = event.nativeEvent;
      const activeDraggableNode = draggableNodes.get(active2);
      if (
        // Another sensor is already instantiating
        activeRef.current !== null || // No active draggable
        !activeDraggableNode || // Event has already been captured
        nativeEvent.dndKit || nativeEvent.defaultPrevented
      ) {
        return;
      }
      const activationContext = {
        active: activeDraggableNode
      };
      const shouldActivate = handler(event, sensor.options, activationContext);
      if (shouldActivate === true) {
        nativeEvent.dndKit = {
          capturedBy: sensor.sensor
        };
        activeRef.current = active2;
        instantiateSensor(event, sensor);
      }
    };
  }, [draggableNodes, instantiateSensor]);
  const activators = useCombineActivators(sensors, bindActivatorToSensorInstantiator);
  useSensorSetup(sensors);
  useIsomorphicLayoutEffect(() => {
    if (activeNodeRect && status === Status.Initializing) {
      setStatus(Status.Initialized);
    }
  }, [activeNodeRect, status]);
  (0, import_react6.useEffect)(
    () => {
      const {
        onDragMove
      } = latestProps.current;
      const {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        over: over2
      } = sensorContext.current;
      if (!active2 || !activatorEvent2) {
        return;
      }
      const event = {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        delta: {
          x: scrollAdjustedTranslate.x,
          y: scrollAdjustedTranslate.y
        },
        over: over2
      };
      (0, import_react_dom.unstable_batchedUpdates)(() => {
        onDragMove == null ? void 0 : onDragMove(event);
        dispatchMonitorEvent({
          type: "onDragMove",
          event
        });
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [scrollAdjustedTranslate.x, scrollAdjustedTranslate.y]
  );
  (0, import_react6.useEffect)(
    () => {
      const {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        droppableContainers: droppableContainers2,
        scrollAdjustedTranslate: scrollAdjustedTranslate2
      } = sensorContext.current;
      if (!active2 || activeRef.current == null || !activatorEvent2 || !scrollAdjustedTranslate2) {
        return;
      }
      const {
        onDragOver
      } = latestProps.current;
      const overContainer = droppableContainers2.get(overId);
      const over2 = overContainer && overContainer.rect.current ? {
        id: overContainer.id,
        rect: overContainer.rect.current,
        data: overContainer.data,
        disabled: overContainer.disabled
      } : null;
      const event = {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        delta: {
          x: scrollAdjustedTranslate2.x,
          y: scrollAdjustedTranslate2.y
        },
        over: over2
      };
      (0, import_react_dom.unstable_batchedUpdates)(() => {
        setOver(over2);
        onDragOver == null ? void 0 : onDragOver(event);
        dispatchMonitorEvent({
          type: "onDragOver",
          event
        });
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [overId]
  );
  useIsomorphicLayoutEffect(() => {
    sensorContext.current = {
      activatorEvent,
      active,
      activeNode,
      collisionRect,
      collisions,
      droppableRects,
      draggableNodes,
      draggingNode,
      draggingNodeRect,
      droppableContainers,
      over,
      scrollableAncestors,
      scrollAdjustedTranslate
    };
    activeRects.current = {
      initial: draggingNodeRect,
      translated: collisionRect
    };
  }, [active, activeNode, collisions, collisionRect, draggableNodes, draggingNode, draggingNodeRect, droppableRects, droppableContainers, over, scrollableAncestors, scrollAdjustedTranslate]);
  useAutoScroller({
    ...autoScrollOptions,
    delta: translate,
    draggingRect: collisionRect,
    pointerCoordinates,
    scrollableAncestors,
    scrollableAncestorRects
  });
  const publicContext = (0, import_react6.useMemo)(() => {
    const context = {
      active,
      activeNode,
      activeNodeRect,
      activatorEvent,
      collisions,
      containerNodeRect,
      dragOverlay,
      draggableNodes,
      droppableContainers,
      droppableRects,
      over,
      measureDroppableContainers,
      scrollableAncestors,
      scrollableAncestorRects,
      measuringConfiguration,
      measuringScheduled,
      windowRect
    };
    return context;
  }, [active, activeNode, activeNodeRect, activatorEvent, collisions, containerNodeRect, dragOverlay, draggableNodes, droppableContainers, droppableRects, over, measureDroppableContainers, scrollableAncestors, scrollableAncestorRects, measuringConfiguration, measuringScheduled, windowRect]);
  const internalContext = (0, import_react6.useMemo)(() => {
    const context = {
      activatorEvent,
      activators,
      active,
      activeNodeRect,
      ariaDescribedById: {
        draggable: draggableDescribedById
      },
      dispatch,
      draggableNodes,
      over,
      measureDroppableContainers
    };
    return context;
  }, [activatorEvent, activators, active, activeNodeRect, dispatch, draggableDescribedById, draggableNodes, over, measureDroppableContainers]);
  return import_react6.default.createElement(DndMonitorContext.Provider, {
    value: registerMonitorListener
  }, import_react6.default.createElement(InternalContext.Provider, {
    value: internalContext
  }, import_react6.default.createElement(PublicContext.Provider, {
    value: publicContext
  }, import_react6.default.createElement(ActiveDraggableContext.Provider, {
    value: transform
  }, children)), import_react6.default.createElement(RestoreFocus, {
    disabled: (accessibility == null ? void 0 : accessibility.restoreFocus) === false
  })), import_react6.default.createElement(Accessibility, {
    ...accessibility,
    hiddenTextDescribedById: draggableDescribedById
  }));
  function getAutoScrollerOptions() {
    const activeSensorDisablesAutoscroll = (activeSensor == null ? void 0 : activeSensor.autoScrollEnabled) === false;
    const autoScrollGloballyDisabled = typeof autoScroll === "object" ? autoScroll.enabled === false : autoScroll === false;
    const enabled = isInitialized && !activeSensorDisablesAutoscroll && !autoScrollGloballyDisabled;
    if (typeof autoScroll === "object") {
      return {
        ...autoScroll,
        enabled
      };
    }
    return {
      enabled
    };
  }
});
var NullContext = (0, import_react6.createContext)(null);
var defaultRole = "button";
var ID_PREFIX = "Draggable";
function useDraggable(_ref) {
  let {
    id,
    data,
    disabled = false,
    attributes
  } = _ref;
  const key2 = useUniqueId(ID_PREFIX);
  const {
    activators,
    activatorEvent,
    active,
    activeNodeRect,
    ariaDescribedById,
    draggableNodes,
    over
  } = (0, import_react6.useContext)(InternalContext);
  const {
    role = defaultRole,
    roleDescription = "draggable",
    tabIndex = 0
  } = attributes != null ? attributes : {};
  const isDragging = (active == null ? void 0 : active.id) === id;
  const transform = (0, import_react6.useContext)(isDragging ? ActiveDraggableContext : NullContext);
  const [node, setNodeRef] = useNodeRef();
  const [activatorNode, setActivatorNodeRef] = useNodeRef();
  const listeners = useSyntheticListeners(activators, id);
  const dataRef = useLatestValue(data);
  useIsomorphicLayoutEffect(
    () => {
      draggableNodes.set(id, {
        id,
        key: key2,
        node,
        activatorNode,
        data: dataRef
      });
      return () => {
        const node2 = draggableNodes.get(id);
        if (node2 && node2.key === key2) {
          draggableNodes.delete(id);
        }
      };
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [draggableNodes, id]
  );
  const memoizedAttributes = (0, import_react6.useMemo)(() => ({
    role,
    tabIndex,
    "aria-disabled": disabled,
    "aria-pressed": isDragging && role === defaultRole ? true : void 0,
    "aria-roledescription": roleDescription,
    "aria-describedby": ariaDescribedById.draggable
  }), [disabled, role, tabIndex, isDragging, roleDescription, ariaDescribedById.draggable]);
  return {
    active,
    activatorEvent,
    activeNodeRect,
    attributes: memoizedAttributes,
    isDragging,
    listeners: disabled ? void 0 : listeners,
    node,
    over,
    setNodeRef,
    setActivatorNodeRef,
    transform
  };
}
function useDndContext() {
  return (0, import_react6.useContext)(PublicContext);
}
var ID_PREFIX$1 = "Droppable";
var defaultResizeObserverConfig = {
  timeout: 25
};
function useDroppable(_ref) {
  let {
    data,
    disabled = false,
    id,
    resizeObserverConfig
  } = _ref;
  const key2 = useUniqueId(ID_PREFIX$1);
  const {
    active,
    dispatch,
    over,
    measureDroppableContainers
  } = (0, import_react6.useContext)(InternalContext);
  const previous = (0, import_react6.useRef)({
    disabled
  });
  const resizeObserverConnected = (0, import_react6.useRef)(false);
  const rect = (0, import_react6.useRef)(null);
  const callbackId = (0, import_react6.useRef)(null);
  const {
    disabled: resizeObserverDisabled,
    updateMeasurementsFor,
    timeout: resizeObserverTimeout
  } = {
    ...defaultResizeObserverConfig,
    ...resizeObserverConfig
  };
  const ids2 = useLatestValue(updateMeasurementsFor != null ? updateMeasurementsFor : id);
  const handleResize = (0, import_react6.useCallback)(
    () => {
      if (!resizeObserverConnected.current) {
        resizeObserverConnected.current = true;
        return;
      }
      if (callbackId.current != null) {
        clearTimeout(callbackId.current);
      }
      callbackId.current = setTimeout(() => {
        measureDroppableContainers(Array.isArray(ids2.current) ? ids2.current : [ids2.current]);
        callbackId.current = null;
      }, resizeObserverTimeout);
    },
    //eslint-disable-next-line react-hooks/exhaustive-deps
    [resizeObserverTimeout]
  );
  const resizeObserver = useResizeObserver({
    callback: handleResize,
    disabled: resizeObserverDisabled || !active
  });
  const handleNodeChange = (0, import_react6.useCallback)((newElement, previousElement) => {
    if (!resizeObserver) {
      return;
    }
    if (previousElement) {
      resizeObserver.unobserve(previousElement);
      resizeObserverConnected.current = false;
    }
    if (newElement) {
      resizeObserver.observe(newElement);
    }
  }, [resizeObserver]);
  const [nodeRef, setNodeRef] = useNodeRef(handleNodeChange);
  const dataRef = useLatestValue(data);
  (0, import_react6.useEffect)(() => {
    if (!resizeObserver || !nodeRef.current) {
      return;
    }
    resizeObserver.disconnect();
    resizeObserverConnected.current = false;
    resizeObserver.observe(nodeRef.current);
  }, [nodeRef, resizeObserver]);
  (0, import_react6.useEffect)(
    () => {
      dispatch({
        type: Action.RegisterDroppable,
        element: {
          id,
          key: key2,
          disabled,
          node: nodeRef,
          rect,
          data: dataRef
        }
      });
      return () => dispatch({
        type: Action.UnregisterDroppable,
        key: key2,
        id
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [id]
  );
  (0, import_react6.useEffect)(() => {
    if (disabled !== previous.current.disabled) {
      dispatch({
        type: Action.SetDroppableDisabled,
        id,
        key: key2,
        disabled
      });
      previous.current.disabled = disabled;
    }
  }, [id, key2, disabled, dispatch]);
  return {
    active,
    rect,
    isOver: (over == null ? void 0 : over.id) === id,
    node: nodeRef,
    over,
    setNodeRef
  };
}
function AnimationManager(_ref) {
  let {
    animation,
    children
  } = _ref;
  const [clonedChildren, setClonedChildren] = (0, import_react6.useState)(null);
  const [element, setElement] = (0, import_react6.useState)(null);
  const previousChildren = usePrevious(children);
  if (!children && !clonedChildren && previousChildren) {
    setClonedChildren(previousChildren);
  }
  useIsomorphicLayoutEffect(() => {
    if (!element) {
      return;
    }
    const key2 = clonedChildren == null ? void 0 : clonedChildren.key;
    const id = clonedChildren == null ? void 0 : clonedChildren.props.id;
    if (key2 == null || id == null) {
      setClonedChildren(null);
      return;
    }
    Promise.resolve(animation(id, element)).then(() => {
      setClonedChildren(null);
    });
  }, [animation, clonedChildren, element]);
  return import_react6.default.createElement(import_react6.default.Fragment, null, children, clonedChildren ? (0, import_react6.cloneElement)(clonedChildren, {
    ref: setElement
  }) : null);
}
var defaultTransform = {
  x: 0,
  y: 0,
  scaleX: 1,
  scaleY: 1
};
function NullifiedContextProvider(_ref) {
  let {
    children
  } = _ref;
  return import_react6.default.createElement(InternalContext.Provider, {
    value: defaultInternalContext
  }, import_react6.default.createElement(ActiveDraggableContext.Provider, {
    value: defaultTransform
  }, children));
}
var baseStyles = {
  position: "fixed",
  touchAction: "none"
};
var defaultTransition = (activatorEvent) => {
  const isKeyboardActivator = isKeyboardEvent(activatorEvent);
  return isKeyboardActivator ? "transform 250ms ease" : void 0;
};
var PositionedOverlay = (0, import_react6.forwardRef)((_ref, ref) => {
  let {
    as,
    activatorEvent,
    adjustScale: adjustScale2,
    children,
    className,
    rect,
    style,
    transform,
    transition = defaultTransition
  } = _ref;
  if (!rect) {
    return null;
  }
  const scaleAdjustedTransform = adjustScale2 ? transform : {
    ...transform,
    scaleX: 1,
    scaleY: 1
  };
  const styles = {
    ...baseStyles,
    width: rect.width,
    height: rect.height,
    top: rect.top,
    left: rect.left,
    transform: CSS.Transform.toString(scaleAdjustedTransform),
    transformOrigin: adjustScale2 && activatorEvent ? getRelativeTransformOrigin(activatorEvent, rect) : void 0,
    transition: typeof transition === "function" ? transition(activatorEvent) : transition,
    ...style
  };
  return import_react6.default.createElement(as, {
    className,
    style: styles,
    ref
  }, children);
});
var defaultDropAnimationSideEffects = (options2) => (_ref) => {
  let {
    active,
    dragOverlay
  } = _ref;
  const originalStyles = {};
  const {
    styles,
    className
  } = options2;
  if (styles != null && styles.active) {
    for (const [key2, value] of Object.entries(styles.active)) {
      if (value === void 0) {
        continue;
      }
      originalStyles[key2] = active.node.style.getPropertyValue(key2);
      active.node.style.setProperty(key2, value);
    }
  }
  if (styles != null && styles.dragOverlay) {
    for (const [key2, value] of Object.entries(styles.dragOverlay)) {
      if (value === void 0) {
        continue;
      }
      dragOverlay.node.style.setProperty(key2, value);
    }
  }
  if (className != null && className.active) {
    active.node.classList.add(className.active);
  }
  if (className != null && className.dragOverlay) {
    dragOverlay.node.classList.add(className.dragOverlay);
  }
  return function cleanup() {
    for (const [key2, value] of Object.entries(originalStyles)) {
      active.node.style.setProperty(key2, value);
    }
    if (className != null && className.active) {
      active.node.classList.remove(className.active);
    }
  };
};
var defaultKeyframeResolver = (_ref2) => {
  let {
    transform: {
      initial,
      final
    }
  } = _ref2;
  return [{
    transform: CSS.Transform.toString(initial)
  }, {
    transform: CSS.Transform.toString(final)
  }];
};
var defaultDropAnimationConfiguration = {
  duration: 250,
  easing: "ease",
  keyframes: defaultKeyframeResolver,
  sideEffects: defaultDropAnimationSideEffects({
    styles: {
      active: {
        opacity: "0"
      }
    }
  })
};
function useDropAnimation(_ref3) {
  let {
    config,
    draggableNodes,
    droppableContainers,
    measuringConfiguration
  } = _ref3;
  return useEvent((id, node) => {
    if (config === null) {
      return;
    }
    const activeDraggable = draggableNodes.get(id);
    if (!activeDraggable) {
      return;
    }
    const activeNode = activeDraggable.node.current;
    if (!activeNode) {
      return;
    }
    const measurableNode = getMeasurableNode(node);
    if (!measurableNode) {
      return;
    }
    const {
      transform
    } = getWindow(node).getComputedStyle(node);
    const parsedTransform = parseTransform(transform);
    if (!parsedTransform) {
      return;
    }
    const animation = typeof config === "function" ? config : createDefaultDropAnimation(config);
    scrollIntoViewIfNeeded(activeNode, measuringConfiguration.draggable.measure);
    return animation({
      active: {
        id,
        data: activeDraggable.data,
        node: activeNode,
        rect: measuringConfiguration.draggable.measure(activeNode)
      },
      draggableNodes,
      dragOverlay: {
        node,
        rect: measuringConfiguration.dragOverlay.measure(measurableNode)
      },
      droppableContainers,
      measuringConfiguration,
      transform: parsedTransform
    });
  });
}
function createDefaultDropAnimation(options2) {
  const {
    duration,
    easing,
    sideEffects,
    keyframes
  } = {
    ...defaultDropAnimationConfiguration,
    ...options2
  };
  return (_ref4) => {
    let {
      active,
      dragOverlay,
      transform,
      ...rest
    } = _ref4;
    if (!duration) {
      return;
    }
    const delta = {
      x: dragOverlay.rect.left - active.rect.left,
      y: dragOverlay.rect.top - active.rect.top
    };
    const scale = {
      scaleX: transform.scaleX !== 1 ? active.rect.width * transform.scaleX / dragOverlay.rect.width : 1,
      scaleY: transform.scaleY !== 1 ? active.rect.height * transform.scaleY / dragOverlay.rect.height : 1
    };
    const finalTransform = {
      x: transform.x - delta.x,
      y: transform.y - delta.y,
      ...scale
    };
    const animationKeyframes = keyframes({
      ...rest,
      active,
      dragOverlay,
      transform: {
        initial: transform,
        final: finalTransform
      }
    });
    const [firstKeyframe] = animationKeyframes;
    const lastKeyframe = animationKeyframes[animationKeyframes.length - 1];
    if (JSON.stringify(firstKeyframe) === JSON.stringify(lastKeyframe)) {
      return;
    }
    const cleanup = sideEffects == null ? void 0 : sideEffects({
      active,
      dragOverlay,
      ...rest
    });
    const animation = dragOverlay.node.animate(animationKeyframes, {
      duration,
      easing,
      fill: "forwards"
    });
    return new Promise((resolve) => {
      animation.onfinish = () => {
        cleanup == null ? void 0 : cleanup();
        resolve();
      };
    });
  };
}
var key = 0;
function useKey(id) {
  return (0, import_react6.useMemo)(() => {
    if (id == null) {
      return;
    }
    key++;
    return key;
  }, [id]);
}
var DragOverlay = import_react6.default.memo((_ref) => {
  let {
    adjustScale: adjustScale2 = false,
    children,
    dropAnimation: dropAnimationConfig,
    style,
    transition,
    modifiers,
    wrapperElement = "div",
    className,
    zIndex = 999
  } = _ref;
  const {
    activatorEvent,
    active,
    activeNodeRect,
    containerNodeRect,
    draggableNodes,
    droppableContainers,
    dragOverlay,
    over,
    measuringConfiguration,
    scrollableAncestors,
    scrollableAncestorRects,
    windowRect
  } = useDndContext();
  const transform = (0, import_react6.useContext)(ActiveDraggableContext);
  const key2 = useKey(active == null ? void 0 : active.id);
  const modifiedTransform = applyModifiers(modifiers, {
    activatorEvent,
    active,
    activeNodeRect,
    containerNodeRect,
    draggingNodeRect: dragOverlay.rect,
    over,
    overlayNodeRect: dragOverlay.rect,
    scrollableAncestors,
    scrollableAncestorRects,
    transform,
    windowRect
  });
  const initialRect = useInitialValue(activeNodeRect);
  const dropAnimation = useDropAnimation({
    config: dropAnimationConfig,
    draggableNodes,
    droppableContainers,
    measuringConfiguration
  });
  const ref = initialRect ? dragOverlay.setRef : void 0;
  return import_react6.default.createElement(NullifiedContextProvider, null, import_react6.default.createElement(AnimationManager, {
    animation: dropAnimation
  }, active && key2 ? import_react6.default.createElement(PositionedOverlay, {
    key: key2,
    id: active.id,
    ref,
    as: wrapperElement,
    activatorEvent,
    adjustScale: adjustScale2,
    className,
    transition,
    rect: initialRect,
    style: {
      zIndex,
      ...style
    },
    transform: modifiedTransform
  }, children) : null));
});

// node_modules/@dnd-kit/modifiers/dist/modifiers.esm.js
var import_dist19 = __toESM(require_dist());
var import_dist20 = __toESM(require_dist2());
var import_dist21 = __toESM(require_dist3());
var restrictToVerticalAxis = (_ref) => {
  let {
    transform
  } = _ref;
  return {
    ...transform,
    x: 0
  };
};

// node_modules/@dnd-kit/sortable/dist/sortable.esm.js
var import_dist22 = __toESM(require_dist());
var import_dist23 = __toESM(require_dist2());
var import_dist24 = __toESM(require_dist3());
var import_react7 = __toESM(require_react());
function arrayMove(array, from, to) {
  const newArray = array.slice();
  newArray.splice(to < 0 ? newArray.length + to : to, 0, newArray.splice(from, 1)[0]);
  return newArray;
}
function getSortedRects(items, rects) {
  return items.reduce((accumulator, id, index) => {
    const rect = rects.get(id);
    if (rect) {
      accumulator[index] = rect;
    }
    return accumulator;
  }, Array(items.length));
}
function isValidIndex(index) {
  return index !== null && index >= 0;
}
function itemsEqual(a, b) {
  if (a === b) {
    return true;
  }
  if (a.length !== b.length) {
    return false;
  }
  for (let i = 0; i < a.length; i++) {
    if (a[i] !== b[i]) {
      return false;
    }
  }
  return true;
}
function normalizeDisabled(disabled) {
  if (typeof disabled === "boolean") {
    return {
      draggable: disabled,
      droppable: disabled
    };
  }
  return disabled;
}
var rectSortingStrategy = (_ref) => {
  let {
    rects,
    activeIndex,
    overIndex,
    index
  } = _ref;
  const newRects = arrayMove(rects, overIndex, activeIndex);
  const oldRect = rects[index];
  const newRect = newRects[index];
  if (!newRect || !oldRect) {
    return null;
  }
  return {
    x: newRect.left - oldRect.left,
    y: newRect.top - oldRect.top,
    scaleX: newRect.width / oldRect.width,
    scaleY: newRect.height / oldRect.height
  };
};
var defaultScale$1 = {
  scaleX: 1,
  scaleY: 1
};
var verticalListSortingStrategy = (_ref) => {
  var _rects$activeIndex;
  let {
    activeIndex,
    activeNodeRect: fallbackActiveRect,
    index,
    rects,
    overIndex
  } = _ref;
  const activeNodeRect = (_rects$activeIndex = rects[activeIndex]) != null ? _rects$activeIndex : fallbackActiveRect;
  if (!activeNodeRect) {
    return null;
  }
  if (index === activeIndex) {
    const overIndexRect = rects[overIndex];
    if (!overIndexRect) {
      return null;
    }
    return {
      x: 0,
      y: activeIndex < overIndex ? overIndexRect.top + overIndexRect.height - (activeNodeRect.top + activeNodeRect.height) : overIndexRect.top - activeNodeRect.top,
      ...defaultScale$1
    };
  }
  const itemGap = getItemGap$1(rects, index, activeIndex);
  if (index > activeIndex && index <= overIndex) {
    return {
      x: 0,
      y: -activeNodeRect.height - itemGap,
      ...defaultScale$1
    };
  }
  if (index < activeIndex && index >= overIndex) {
    return {
      x: 0,
      y: activeNodeRect.height + itemGap,
      ...defaultScale$1
    };
  }
  return {
    x: 0,
    y: 0,
    ...defaultScale$1
  };
};
function getItemGap$1(clientRects, index, activeIndex) {
  const currentRect = clientRects[index];
  const previousRect = clientRects[index - 1];
  const nextRect = clientRects[index + 1];
  if (!currentRect) {
    return 0;
  }
  if (activeIndex < index) {
    return previousRect ? currentRect.top - (previousRect.top + previousRect.height) : nextRect ? nextRect.top - (currentRect.top + currentRect.height) : 0;
  }
  return nextRect ? nextRect.top - (currentRect.top + currentRect.height) : previousRect ? currentRect.top - (previousRect.top + previousRect.height) : 0;
}
var ID_PREFIX2 = "Sortable";
var Context = import_react7.default.createContext({
  activeIndex: -1,
  containerId: ID_PREFIX2,
  disableTransforms: false,
  items: [],
  overIndex: -1,
  useDragOverlay: false,
  sortedRects: [],
  strategy: rectSortingStrategy,
  disabled: {
    draggable: false,
    droppable: false
  }
});
function SortableContext(_ref) {
  let {
    children,
    id,
    items: userDefinedItems,
    strategy = rectSortingStrategy,
    disabled: disabledProp = false
  } = _ref;
  const {
    active,
    dragOverlay,
    droppableRects,
    over,
    measureDroppableContainers
  } = useDndContext();
  const containerId = useUniqueId(ID_PREFIX2, id);
  const useDragOverlay = Boolean(dragOverlay.rect !== null);
  const items = (0, import_react7.useMemo)(() => userDefinedItems.map((item) => typeof item === "object" && "id" in item ? item.id : item), [userDefinedItems]);
  const isDragging = active != null;
  const activeIndex = active ? items.indexOf(active.id) : -1;
  const overIndex = over ? items.indexOf(over.id) : -1;
  const previousItemsRef = (0, import_react7.useRef)(items);
  const itemsHaveChanged = !itemsEqual(items, previousItemsRef.current);
  const disableTransforms = overIndex !== -1 && activeIndex === -1 || itemsHaveChanged;
  const disabled = normalizeDisabled(disabledProp);
  useIsomorphicLayoutEffect(() => {
    if (itemsHaveChanged && isDragging) {
      measureDroppableContainers(items);
    }
  }, [itemsHaveChanged, items, isDragging, measureDroppableContainers]);
  (0, import_react7.useEffect)(() => {
    previousItemsRef.current = items;
  }, [items]);
  const contextValue = (0, import_react7.useMemo)(
    () => ({
      activeIndex,
      containerId,
      disabled,
      disableTransforms,
      items,
      overIndex,
      useDragOverlay,
      sortedRects: getSortedRects(items, droppableRects),
      strategy
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [activeIndex, containerId, disabled.draggable, disabled.droppable, disableTransforms, items, overIndex, droppableRects, useDragOverlay, strategy]
  );
  return import_react7.default.createElement(Context.Provider, {
    value: contextValue
  }, children);
}
var defaultNewIndexGetter = (_ref) => {
  let {
    id,
    items,
    activeIndex,
    overIndex
  } = _ref;
  return arrayMove(items, activeIndex, overIndex).indexOf(id);
};
var defaultAnimateLayoutChanges = (_ref2) => {
  let {
    containerId,
    isSorting,
    wasDragging,
    index,
    items,
    newIndex,
    previousItems,
    previousContainerId,
    transition
  } = _ref2;
  if (!transition || !wasDragging) {
    return false;
  }
  if (previousItems !== items && index === newIndex) {
    return false;
  }
  if (isSorting) {
    return true;
  }
  return newIndex !== index && containerId === previousContainerId;
};
var defaultTransition2 = {
  duration: 200,
  easing: "ease"
};
var transitionProperty = "transform";
var disabledTransition = CSS.Transition.toString({
  property: transitionProperty,
  duration: 0,
  easing: "linear"
});
var defaultAttributes = {
  roleDescription: "sortable"
};
function useDerivedTransform(_ref) {
  let {
    disabled,
    index,
    node,
    rect
  } = _ref;
  const [derivedTransform, setDerivedtransform] = (0, import_react7.useState)(null);
  const previousIndex = (0, import_react7.useRef)(index);
  useIsomorphicLayoutEffect(() => {
    if (!disabled && index !== previousIndex.current && node.current) {
      const initial = rect.current;
      if (initial) {
        const current = getClientRect(node.current, {
          ignoreTransform: true
        });
        const delta = {
          x: initial.left - current.left,
          y: initial.top - current.top,
          scaleX: initial.width / current.width,
          scaleY: initial.height / current.height
        };
        if (delta.x || delta.y) {
          setDerivedtransform(delta);
        }
      }
    }
    if (index !== previousIndex.current) {
      previousIndex.current = index;
    }
  }, [disabled, index, node, rect]);
  (0, import_react7.useEffect)(() => {
    if (derivedTransform) {
      setDerivedtransform(null);
    }
  }, [derivedTransform]);
  return derivedTransform;
}
function useSortable(_ref) {
  let {
    animateLayoutChanges = defaultAnimateLayoutChanges,
    attributes: userDefinedAttributes,
    disabled: localDisabled,
    data: customData,
    getNewIndex = defaultNewIndexGetter,
    id,
    strategy: localStrategy,
    resizeObserverConfig,
    transition = defaultTransition2
  } = _ref;
  const {
    items,
    containerId,
    activeIndex,
    disabled: globalDisabled,
    disableTransforms,
    sortedRects,
    overIndex,
    useDragOverlay,
    strategy: globalStrategy
  } = (0, import_react7.useContext)(Context);
  const disabled = normalizeLocalDisabled(localDisabled, globalDisabled);
  const index = items.indexOf(id);
  const data = (0, import_react7.useMemo)(() => ({
    sortable: {
      containerId,
      index,
      items
    },
    ...customData
  }), [containerId, customData, index, items]);
  const itemsAfterCurrentSortable = (0, import_react7.useMemo)(() => items.slice(items.indexOf(id)), [items, id]);
  const {
    rect,
    node,
    isOver,
    setNodeRef: setDroppableNodeRef
  } = useDroppable({
    id,
    data,
    disabled: disabled.droppable,
    resizeObserverConfig: {
      updateMeasurementsFor: itemsAfterCurrentSortable,
      ...resizeObserverConfig
    }
  });
  const {
    active,
    activatorEvent,
    activeNodeRect,
    attributes,
    setNodeRef: setDraggableNodeRef,
    listeners,
    isDragging,
    over,
    setActivatorNodeRef,
    transform
  } = useDraggable({
    id,
    data,
    attributes: {
      ...defaultAttributes,
      ...userDefinedAttributes
    },
    disabled: disabled.draggable
  });
  const setNodeRef = useCombinedRefs(setDroppableNodeRef, setDraggableNodeRef);
  const isSorting = Boolean(active);
  const displaceItem = isSorting && !disableTransforms && isValidIndex(activeIndex) && isValidIndex(overIndex);
  const shouldDisplaceDragSource = !useDragOverlay && isDragging;
  const dragSourceDisplacement = shouldDisplaceDragSource && displaceItem ? transform : null;
  const strategy = localStrategy != null ? localStrategy : globalStrategy;
  const finalTransform = displaceItem ? dragSourceDisplacement != null ? dragSourceDisplacement : strategy({
    rects: sortedRects,
    activeNodeRect,
    activeIndex,
    overIndex,
    index
  }) : null;
  const newIndex = isValidIndex(activeIndex) && isValidIndex(overIndex) ? getNewIndex({
    id,
    items,
    activeIndex,
    overIndex
  }) : index;
  const activeId = active == null ? void 0 : active.id;
  const previous = (0, import_react7.useRef)({
    activeId,
    items,
    newIndex,
    containerId
  });
  const itemsHaveChanged = items !== previous.current.items;
  const shouldAnimateLayoutChanges = animateLayoutChanges({
    active,
    containerId,
    isDragging,
    isSorting,
    id,
    index,
    items,
    newIndex: previous.current.newIndex,
    previousItems: previous.current.items,
    previousContainerId: previous.current.containerId,
    transition,
    wasDragging: previous.current.activeId != null
  });
  const derivedTransform = useDerivedTransform({
    disabled: !shouldAnimateLayoutChanges,
    index,
    node,
    rect
  });
  (0, import_react7.useEffect)(() => {
    if (isSorting && previous.current.newIndex !== newIndex) {
      previous.current.newIndex = newIndex;
    }
    if (containerId !== previous.current.containerId) {
      previous.current.containerId = containerId;
    }
    if (items !== previous.current.items) {
      previous.current.items = items;
    }
  }, [isSorting, newIndex, containerId, items]);
  (0, import_react7.useEffect)(() => {
    if (activeId === previous.current.activeId) {
      return;
    }
    if (activeId != null && previous.current.activeId == null) {
      previous.current.activeId = activeId;
      return;
    }
    const timeoutId = setTimeout(() => {
      previous.current.activeId = activeId;
    }, 50);
    return () => clearTimeout(timeoutId);
  }, [activeId]);
  return {
    active,
    activeIndex,
    attributes,
    data,
    rect,
    index,
    newIndex,
    items,
    isOver,
    isSorting,
    isDragging,
    listeners,
    node,
    overIndex,
    over,
    setNodeRef,
    setActivatorNodeRef,
    setDroppableNodeRef,
    setDraggableNodeRef,
    transform: derivedTransform != null ? derivedTransform : finalTransform,
    transition: getTransition()
  };
  function getTransition() {
    if (
      // Temporarily disable transitions for a single frame to set up derived transforms
      derivedTransform || // Or to prevent items jumping to back to their "new" position when items change
      itemsHaveChanged && previous.current.newIndex === index
    ) {
      return disabledTransition;
    }
    if (shouldDisplaceDragSource && !isKeyboardEvent(activatorEvent) || !transition) {
      return void 0;
    }
    if (isSorting || shouldAnimateLayoutChanges) {
      return CSS.Transition.toString({
        ...transition,
        property: transitionProperty
      });
    }
    return void 0;
  }
}
function normalizeLocalDisabled(localDisabled, globalDisabled) {
  var _localDisabled$dragga, _localDisabled$droppa;
  if (typeof localDisabled === "boolean") {
    return {
      draggable: localDisabled,
      // Backwards compatibility
      droppable: false
    };
  }
  return {
    draggable: (_localDisabled$dragga = localDisabled == null ? void 0 : localDisabled.draggable) != null ? _localDisabled$dragga : globalDisabled.draggable,
    droppable: (_localDisabled$droppa = localDisabled == null ? void 0 : localDisabled.droppable) != null ? _localDisabled$droppa : globalDisabled.droppable
  };
}
function hasSortableData(entry) {
  if (!entry) {
    return false;
  }
  const data = entry.data.current;
  if (data && "sortable" in data && typeof data.sortable === "object" && "containerId" in data.sortable && "items" in data.sortable && "index" in data.sortable) {
    return true;
  }
  return false;
}
var directions = [KeyboardCode.Down, KeyboardCode.Right, KeyboardCode.Up, KeyboardCode.Left];
var sortableKeyboardCoordinates = (event, _ref) => {
  let {
    context: {
      active,
      collisionRect,
      droppableRects,
      droppableContainers,
      over,
      scrollableAncestors
    }
  } = _ref;
  if (directions.includes(event.code)) {
    event.preventDefault();
    if (!active || !collisionRect) {
      return;
    }
    const filteredContainers = [];
    droppableContainers.getEnabled().forEach((entry) => {
      if (!entry || entry != null && entry.disabled) {
        return;
      }
      const rect = droppableRects.get(entry.id);
      if (!rect) {
        return;
      }
      switch (event.code) {
        case KeyboardCode.Down:
          if (collisionRect.top < rect.top) {
            filteredContainers.push(entry);
          }
          break;
        case KeyboardCode.Up:
          if (collisionRect.top > rect.top) {
            filteredContainers.push(entry);
          }
          break;
        case KeyboardCode.Left:
          if (collisionRect.left > rect.left) {
            filteredContainers.push(entry);
          }
          break;
        case KeyboardCode.Right:
          if (collisionRect.left < rect.left) {
            filteredContainers.push(entry);
          }
          break;
      }
    });
    const collisions = closestCorners({
      active,
      collisionRect,
      droppableRects,
      droppableContainers: filteredContainers,
      pointerCoordinates: null
    });
    let closestId = getFirstCollision(collisions, "id");
    if (closestId === (over == null ? void 0 : over.id) && collisions.length > 1) {
      closestId = collisions[1].id;
    }
    if (closestId != null) {
      const activeDroppable = droppableContainers.get(active.id);
      const newDroppable = droppableContainers.get(closestId);
      const newRect = newDroppable ? droppableRects.get(newDroppable.id) : null;
      const newNode = newDroppable == null ? void 0 : newDroppable.node.current;
      if (newNode && newRect && activeDroppable && newDroppable) {
        const newScrollAncestors = getScrollableAncestors(newNode);
        const hasDifferentScrollAncestors = newScrollAncestors.some((element, index) => scrollableAncestors[index] !== element);
        const hasSameContainer = isSameContainer(activeDroppable, newDroppable);
        const isAfterActive = isAfter(activeDroppable, newDroppable);
        const offset = hasDifferentScrollAncestors || !hasSameContainer ? {
          x: 0,
          y: 0
        } : {
          x: isAfterActive ? collisionRect.width - newRect.width : 0,
          y: isAfterActive ? collisionRect.height - newRect.height : 0
        };
        const rectCoordinates = {
          x: newRect.left,
          y: newRect.top
        };
        const newCoordinates = offset.x && offset.y ? rectCoordinates : subtract(rectCoordinates, offset);
        return newCoordinates;
      }
    }
  }
  return void 0;
};
function isSameContainer(a, b) {
  if (!hasSortableData(a) || !hasSortableData(b)) {
    return false;
  }
  return a.data.current.sortable.containerId === b.data.current.sortable.containerId;
}
function isAfter(a, b) {
  if (!hasSortableData(a) || !hasSortableData(b)) {
    return false;
  }
  if (!isSameContainer(a, b)) {
    return false;
  }
  return a.data.current.sortable.index < b.data.current.sortable.index;
}

// node_modules/@orderly.network/trading/dist/index.mjs
var __defProp2 = Object.defineProperty;
var __getOwnPropNames2 = Object.getOwnPropertyNames;
var __esm2 = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames2(fn)[0]])(fn = 0)), res;
};
var __export2 = (target, all) => {
  for (var name in all)
    __defProp2(target, name, { get: all[name], enumerable: true });
};
var useTradingLocalStorage;
var init_useTradingLocalStorage = __esm2({
  "src/hooks/useTradingLocalStorage.ts"() {
    useTradingLocalStorage = (props) => {
      const [unPnlPriceBasis, setUnPnlPriceBasic] = useLocalStorage(
        "unPnlPriceBasis",
        "markPrice"
      );
      const [pnlNotionalDecimalPrecision, setPnlNotionalDecimalPrecision] = useLocalStorage(
        "pnlNotionalDecimalPrecision",
        props?.pnlNotionalDecimalPrecision ?? 2
      );
      const [showAllSymbol, setShowAllSymbol] = useLocalStorage(
        "showAllSymbol",
        true
      );
      const [hideAssets, setHideAssets] = useLocalStorage("hideAssets", false);
      return {
        unPnlPriceBasis,
        setUnPnlPriceBasic,
        pnlNotionalDecimalPrecision,
        setPnlNotionalDecimalPrecision,
        showAllSymbol,
        setShowAllSymbol,
        hideAssets,
        setHideAssets
      };
    };
  }
});
var usePositionsCount;
var init_usePositionsCount = __esm2({
  "src/hooks/usePositionsCount.ts"() {
    init_useTradingLocalStorage();
    usePositionsCount = (symbol) => {
      const { showAllSymbol } = useTradingLocalStorage();
      const [data] = usePositionStream(showAllSymbol ? void 0 : symbol);
      const count = (0, import_react8.useMemo)(() => {
        return data.rows?.length;
      }, [data.rows?.length]);
      const positionCount = useDataTap(count) ?? 0;
      return {
        positionCount
      };
    };
  }
});
var usePendingOrderCount;
var init_usePendingOrderCount = __esm2({
  "src/hooks/usePendingOrderCount.ts"() {
    init_useTradingLocalStorage();
    usePendingOrderCount = (symbol) => {
      const { showAllSymbol } = useTradingLocalStorage();
      const [pendingOrders] = useOrderStream(
        {
          symbol: showAllSymbol ? void 0 : symbol,
          status: OrderStatus.INCOMPLETE,
          excludes: [AlgoOrderRootType.POSITIONAL_TP_SL, AlgoOrderRootType.TP_SL],
          // size: pendingOrderPageSize,
          size: 500
          // sourceTypeAll: true,
        },
        {
          keeplive: true
        }
      );
      const [tpslOrders] = useOrderStream(
        {
          symbol: showAllSymbol ? void 0 : symbol,
          status: OrderStatus.INCOMPLETE,
          includes: [AlgoOrderRootType.POSITIONAL_TP_SL, AlgoOrderRootType.TP_SL],
          // size: tpslOrderPageSize,
          size: 500
          // sourceTypeAll: true,
        },
        {
          keeplive: true
        }
      );
      const pendingCount = (0, import_react8.useMemo)(() => {
        if (showAllSymbol) {
          return pendingOrders?.length ?? 0;
        }
        return pendingOrders?.filter((item) => item.symbol === symbol)?.length ?? 0;
      }, [pendingOrders, showAllSymbol, symbol]);
      const tpslCount = (0, import_react8.useMemo)(() => {
        if (showAllSymbol) {
          return tpslOrders?.length ?? 0;
        }
        return tpslOrders?.filter((item) => item.symbol === symbol)?.length ?? 0;
      }, [tpslOrders, showAllSymbol, symbol]);
      const pendingOrderCount = useDataTap(pendingCount) ?? 0;
      const tpSlOrderCount = useDataTap(tpslCount) ?? 0;
      return {
        pendingOrderCount,
        tpSlOrderCount
      };
    };
  }
});
var init_hooks = __esm2({
  "src/hooks/index.ts"() {
    init_useTradingLocalStorage();
    init_usePositionsCount();
    init_usePendingOrderCount();
  }
});
var TradingPageContext;
var useTradingPageContext;
var init_tradingPageContext = __esm2({
  "src/provider/tradingPageContext.tsx"() {
    TradingPageContext = (0, import_react8.createContext)({});
    useTradingPageContext = () => {
      return (0, import_react8.useContext)(TradingPageContext);
    };
  }
});
var useDataListScript;
var init_dataList_script = __esm2({
  "src/components/desktop/dataList/dataList.script.tsx"() {
    init_hooks();
    init_tradingPageContext();
    useDataListScript = (inputs) => {
      const {
        current,
        pnlNotionalDecimalPrecision,
        sharePnLConfig,
        symbol,
        includedPendingOrder
      } = inputs;
      const localStorage2 = useTradingLocalStorage({ pnlNotionalDecimalPrecision });
      const { onSymbolChange } = useTradingPageContext();
      const { positionCount } = usePositionsCount(symbol);
      const { pendingOrderCount, tpSlOrderCount } = usePendingOrderCount(symbol);
      return {
        current,
        sharePnLConfig,
        symbol,
        calcMode: localStorage2.unPnlPriceBasis,
        includedPendingOrder,
        ...localStorage2,
        positionCount,
        pendingOrderCount,
        tpSlOrderCount,
        onSymbolChange
      };
    };
  }
});
var Setting;
var UnPnlPriceBasisCheckBox;
var DecimalPrecisionCheckbox;
var RadioButton;
var SelIcon;
var UnselIcon;
var init_setting_ui = __esm2({
  "src/components/desktop/dataList/setting/setting.ui.tsx"() {
    Setting = (props) => {
      const [open2, setOpen] = (0, import_react8.useState)(false);
      const { t } = useTranslation();
      const { isMobile } = useScreen();
      const SettingsContent = (0, import_react8.useMemo)(() => {
        return () => (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
          (0, import_jsx_runtime5.jsxs)("div", { className: "oui-flex oui-flex-col oui-text-sm", children: [
            (0, import_jsx_runtime5.jsx)(
              Flex,
              {
                itemAlign: "center",
                justify: isMobile ? "center" : "start",
                className: "oui-w-full",
                children: (0, import_jsx_runtime5.jsx)(Text2, { className: "oui-text-base oui-pb-3", children: t("trading.portfolioSettings") })
              }
            ),
            (0, import_jsx_runtime5.jsx)(Divider, {}),
            (0, import_jsx_runtime5.jsx)(Text2, { className: "oui-pb-3 oui-text-base-contrast-54 oui-mt-2", children: t("trading.portfolioSettings.decimalPrecision") }),
            (0, import_jsx_runtime5.jsx)(
              DecimalPrecisionCheckbox,
              {
                value: props.pnlNotionalDecimalPrecision,
                onValueChange: (e) => {
                  props.setPnlNotionalDecimalPrecision(e);
                  setOpen(false);
                }
              }
            ),
            (0, import_jsx_runtime5.jsx)(Divider, { className: "oui-my-3" }),
            (0, import_jsx_runtime5.jsx)(Text2, { className: "oui-pb-3 oui-text-base-contrast-54 oui-mt-2", children: t("trading.portfolioSettings.unrealPnlPriceBasis") }),
            (0, import_jsx_runtime5.jsx)(
              UnPnlPriceBasisCheckBox,
              {
                value: props.unPnlPriceBasis,
                onValueChange: (e) => {
                  props.setUnPnlPriceBasic(e);
                  setOpen(false);
                }
              }
            )
          ] }),
          (0, import_jsx_runtime5.jsx)(Divider, { className: "oui-my-3" }),
          (0, import_jsx_runtime5.jsxs)(Flex, { itemAlign: "center", gap: 1, justify: "between", children: [
            (0, import_jsx_runtime5.jsxs)(Flex, { gap: 1, itemAlign: "center", children: [
              (0, import_jsx_runtime5.jsx)(Text2, { size: "sm", intensity: 54, children: t("trading.portfolioSettings.reversePosition") }),
              isMobile ? (0, import_jsx_runtime5.jsx)(
                ExclamationFillIcon,
                {
                  size: 14,
                  className: "oui-text-base-contrast-54 hover:oui-text-base-contrast-80 oui-cursor-pointer",
                  onClick: () => {
                    modal.alert({
                      title: t("common.tips"),
                      message: t(
                        "trading.portfolioSettings.reversePosition.tooltip"
                      )
                    });
                  }
                }
              ) : (0, import_jsx_runtime5.jsx)(
                Tooltip,
                {
                  content: t("trading.portfolioSettings.reversePosition.tooltip"),
                  className: "oui-max-w-[300px]",
                  children: (0, import_jsx_runtime5.jsx)(
                    ExclamationFillIcon,
                    {
                      size: 14,
                      className: "oui-text-base-contrast-54 hover:oui-text-base-contrast-80 oui-cursor-pointer"
                    }
                  )
                }
              )
            ] }),
            (0, import_jsx_runtime5.jsx)(
              Switch,
              {
                checked: props.reversePosition,
                onCheckedChange: (checked) => {
                  props.setReversePosition(checked);
                }
              }
            )
          ] })
        ] });
      }, [
        t,
        isMobile,
        props.pnlNotionalDecimalPrecision,
        props.unPnlPriceBasis,
        props.reversePosition
      ]);
      const triggerButton = (0, import_jsx_runtime5.jsx)(
        Button,
        {
          size: "xs",
          type: "button",
          variant: "contained",
          className: "oui-bg-transparent hover:oui-bg-transparent",
          children: (0, import_jsx_runtime5.jsx)(
            SettingFillIcon,
            {
              size: 16,
              color: "white",
              opacity: 1,
              className: "oui-text-white/[.36] hover:oui-text-white/80"
            }
          )
        }
      );
      return (0, import_jsx_runtime5.jsxs)(Flex, { gap: 0, children: [
        (0, import_jsx_runtime5.jsxs)(Flex, { gap: 1, children: [
          (0, import_jsx_runtime5.jsx)(
            Checkbox,
            {
              id: "oui-checkbox-hideOtherSymbols",
              color: "white",
              checked: props.hideOtherSymbols,
              onCheckedChange: (checked) => {
                props.setHideOtherSymbols(checked);
              }
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            "label",
            {
              className: "oui-text-xs oui-text-base-contrast-54 oui-cursor-pointer",
              htmlFor: "oui-checkbox-hideOtherSymbols",
              children: t("trading.hideOtherSymbols")
            }
          )
        ] }),
        isMobile ? (0, import_jsx_runtime5.jsxs)(Sheet, { open: open2, onOpenChange: setOpen, children: [
          (0, import_jsx_runtime5.jsx)(SheetTrigger, { asChild: true, children: triggerButton }),
          (0, import_jsx_runtime5.jsx)(SheetContent, { side: "bottom", className: "oui-px-5 oui-pt-3", children: (0, import_jsx_runtime5.jsx)(
            "div",
            {
              style: {
                paddingBottom: `max(32px, calc(12px + env(safe-area-inset-bottom)))`
              },
              children: (0, import_jsx_runtime5.jsx)(SettingsContent, {})
            }
          ) })
        ] }) : (0, import_jsx_runtime5.jsxs)(DropdownMenuRoot, { open: open2, onOpenChange: setOpen, children: [
          (0, import_jsx_runtime5.jsx)(DropdownMenuTrigger, { asChild: true, children: triggerButton }),
          (0, import_jsx_runtime5.jsx)(
            DropdownMenuContent,
            {
              className: "oui-px-5 oui-py-3 oui-w-[360px]",
              alignOffset: 2,
              align: "end",
              children: (0, import_jsx_runtime5.jsx)(SettingsContent, {})
            }
          )
        ] })
      ] });
    };
    UnPnlPriceBasisCheckBox = (props) => {
      const { value, onValueChange } = props;
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsxs)(Flex, { gap: 2, children: [
        (0, import_jsx_runtime5.jsx)(
          RadioButton,
          {
            sel: value === "markPrice",
            label: t("common.markPrice"),
            value: "markPrice",
            onCheckChange: onValueChange
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          RadioButton,
          {
            sel: value === "lastPrice",
            label: t("common.lastPrice"),
            value: "lastPrice",
            onCheckChange: onValueChange
          }
        )
      ] });
    };
    DecimalPrecisionCheckbox = (props) => {
      const { value, onValueChange } = props;
      return (0, import_jsx_runtime5.jsxs)(Flex, { gap: 2, children: [
        (0, import_jsx_runtime5.jsx)(
          RadioButton,
          {
            sel: value === 0,
            label: 1,
            value: 0,
            onCheckChange: onValueChange
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          RadioButton,
          {
            sel: value === 1,
            label: 0.1,
            value: 1,
            onCheckChange: onValueChange
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          RadioButton,
          {
            sel: value === 2,
            label: 0.01,
            value: 2,
            onCheckChange: onValueChange
          }
        )
      ] });
    };
    RadioButton = (props) => {
      const { sel, label, value, onCheckChange } = props;
      return (0, import_jsx_runtime5.jsxs)(
        Flex,
        {
          onClick: (e) => {
            onCheckChange(value);
            e.stopPropagation();
          },
          gap: 1,
          className: "oui-cursor-pointer",
          children: [
            sel ? (0, import_jsx_runtime5.jsx)(SelIcon, {}) : (0, import_jsx_runtime5.jsx)(UnselIcon, {}),
            (0, import_jsx_runtime5.jsx)(Text2, { size: "2xs", intensity: sel ? 98 : 54, children: label })
          ]
        }
      );
    };
    SelIcon = () => {
      return (0, import_jsx_runtime5.jsxs)(
        "svg",
        {
          width: "16",
          height: "16",
          viewBox: "0 0 16 16",
          fill: "currentColor",
          xmlns: "http://www.w3.org/2000/svg",
          className: "oui-fill-white",
          children: [
            (0, import_jsx_runtime5.jsx)(
              "path",
              {
                d: "M8.01 1.333a6.667 6.667 0 1 0 0 13.333 6.667 6.667 0 0 0 0-13.333m0 1.333a5.334 5.334 0 1 1-.001 10.667 5.334 5.334 0 0 1 0-10.667",
                fill: "#fff",
                fillOpacity: ".36"
              }
            ),
            (0, import_jsx_runtime5.jsx)("circle", { cx: "8", cy: "8", r: "3.333" })
          ]
        }
      );
    };
    UnselIcon = () => {
      return (0, import_jsx_runtime5.jsx)(
        "svg",
        {
          width: "16",
          height: "16",
          viewBox: "0 0 16 16",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg",
          children: (0, import_jsx_runtime5.jsx)(
            "path",
            {
              d: "M8.01 1.333a6.667 6.667 0 1 0 0 13.333 6.667 6.667 0 0 0 0-13.333m0 1.333a5.334 5.334 0 1 1-.001 10.667 5.334 5.334 0 0 1 0-10.667",
              fill: "#fff",
              fillOpacity: ".54"
            }
          )
        }
      );
    };
  }
});
var useSettingScript;
var init_setting_script = __esm2({
  "src/components/desktop/dataList/setting/setting.script.tsx"() {
    useSettingScript = (props) => {
      const { isEnabled, setEnabled } = useReversePositionEnabled();
      return {
        ...props,
        reversePosition: isEnabled,
        setReversePosition: setEnabled
      };
    };
  }
});
var SettingWidget;
var init_setting_widget = __esm2({
  "src/components/desktop/dataList/setting/setting.widget.tsx"() {
    init_setting_script();
    init_setting_ui();
    SettingWidget = (props) => {
      const state = useSettingScript(props);
      return (0, import_jsx_runtime5.jsx)(Setting, { ...state });
    };
  }
});
var setting_exports = {};
__export2(setting_exports, {
  Setting: () => Setting,
  SettingWidget: () => SettingWidget
});
var init_setting = __esm2({
  "src/components/desktop/dataList/setting/index.ts"() {
    init_setting_ui();
    init_setting_widget();
  }
});
var PositionHeader;
var MobileLayout;
var DesktopLayout;
var UnrealPnL;
var Notional;
var init_positionHeader_ui = __esm2({
  "src/components/base/positionHeader/positionHeader.ui.tsx"() {
    init_setting();
    PositionHeader = (props) => {
      const { isMobile } = useScreen();
      return isMobile ? (0, import_jsx_runtime5.jsx)(MobileLayout, { ...props }) : (0, import_jsx_runtime5.jsx)(DesktopLayout, { ...props });
    };
    MobileLayout = (props) => {
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsxs)(
        Flex,
        {
          direction: "column",
          gap: 2,
          width: "100%",
          itemAlign: "start",
          p: 2,
          className: "oui-rounded-b-xl oui-bg-base-9",
          children: [
            (0, import_jsx_runtime5.jsxs)(Flex, { width: "100%", justify: "between", gap: 2, children: [
              (0, import_jsx_runtime5.jsx)(
                UnrealPnL,
                {
                  classNames: {
                    label: "oui-text-2xs oui-text-base-contrast-54",
                    root: "oui-text-sm"
                  },
                  ...props
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                Notional,
                {
                  classNames: {
                    label: "oui-text-2xs oui-text-base-contrast-54",
                    root: "oui-text-sm"
                  },
                  ...props
                }
              )
            ] }),
            (0, import_jsx_runtime5.jsx)(Divider, { className: "oui-w-full" }),
            (0, import_jsx_runtime5.jsxs)(
              Flex,
              {
                className: "oui-cursor-pointer oui-gap-[2px]",
                justify: "between",
                width: "100%",
                children: [
                  (0, import_jsx_runtime5.jsx)(Flex, { children: (0, import_jsx_runtime5.jsx)(
                    SettingWidget,
                    {
                      pnlNotionalDecimalPrecision: props.pnlNotionalDecimalPrecision,
                      setPnlNotionalDecimalPrecision: props.setPnlNotionalDecimalPrecision,
                      unPnlPriceBasis: props.unPnlPriceBasis,
                      setUnPnlPriceBasic: props.setUnPnlPriceBasic,
                      hideOtherSymbols: !props.showAllSymbol,
                      setHideOtherSymbols: (value) => {
                        props.setShowAllSymbol(!value);
                      }
                    }
                  ) }),
                  (0, import_jsx_runtime5.jsx)(CloseAllPositionsWidget, { symbol: props.symbol })
                ]
              }
            )
          ]
        }
      );
    };
    DesktopLayout = (props) => {
      return (0, import_jsx_runtime5.jsxs)(Flex, { py: 2, px: 3, gap: 6, width: "100%", justify: "between", children: [
        (0, import_jsx_runtime5.jsxs)(Flex, { gap: 5, children: [
          (0, import_jsx_runtime5.jsx)(
            UnrealPnL,
            {
              ...props,
              classNames: { label: "oui-text-base-contrast-54" }
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            Notional,
            {
              ...props,
              classNames: { label: "oui-text-base-contrast-54" }
            }
          )
        ] }),
        (0, import_jsx_runtime5.jsx)(CloseAllPositionsWidget, { symbol: props.symbol })
      ] });
    };
    UnrealPnL = (props) => {
      const { t } = useTranslation();
      const unrealPnLClsName = typeof props.unrealPnL === "number" ? props.unrealPnL >= 0 ? "oui-text-trade-profit" : "oui-text-trade-loss" : "oui-text-base-contrast-80";
      const unrealPnLROIClsName = typeof props.unrealPnL === "number" && props.unrealPnlROI ? props.unrealPnlROI >= 0 ? "oui-text-success-darken" : "oui-text-danger-darken" : "oui-text-base-contrast-80";
      return (0, import_jsx_runtime5.jsx)(Statistic, { label: t("common.unrealizedPnl"), classNames: props.classNames, children: (0, import_jsx_runtime5.jsxs)(Flex, { gap: 1, children: [
        (0, import_jsx_runtime5.jsx)(
          Text2.pnl,
          {
            dp: props.pnlNotionalDecimalPrecision,
            intensity: 80,
            className: unrealPnLClsName,
            children: props.unrealPnL ?? "--"
          }
        ),
        typeof props.unrealPnlROI !== "undefined" && (0, import_jsx_runtime5.jsx)(
          Text2.roi,
          {
            prefix: "(",
            suffix: ")",
            rule: "percentages",
            size: "2xs",
            dp: props.pnlNotionalDecimalPrecision,
            className: unrealPnLROIClsName,
            children: props.unrealPnlROI
          }
        )
      ] }) });
    };
    Notional = (props) => {
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsx)(Statistic, { label: t("common.notional"), classNames: props.classNames, children: (0, import_jsx_runtime5.jsx)(
        Text2.numeral,
        {
          dp: props.pnlNotionalDecimalPrecision,
          rm: decimal_default.ROUND_DOWN,
          intensity: 80,
          children: props.notional ?? "--"
        }
      ) });
    };
  }
});
var usePositionHeaderScript;
var init_positionHeader_script = __esm2({
  "src/components/base/positionHeader/positionHeader.script.tsx"() {
    init_hooks();
    usePositionHeaderScript = (inputs) => {
      const {
        pnlNotionalDecimalPrecision,
        setPnlNotionalDecimalPrecision,
        unPnlPriceBasis,
        setUnPnlPriceBasic,
        symbol
      } = inputs;
      const calcMode = unPnlPriceBasis;
      const [data] = usePositionStream(symbol, {
        calcMode
      });
      const aggregated = useDataTap(data.aggregated);
      const unrealPnL = aggregated?.total_unreal_pnl;
      const unrealPnlROI = aggregated?.unrealPnlROI;
      const notional = aggregated?.notional;
      const { showAllSymbol, setShowAllSymbol } = useTradingLocalStorage();
      return {
        pnlNotionalDecimalPrecision,
        unrealPnL,
        unrealPnlROI,
        notional,
        showAllSymbol,
        setShowAllSymbol,
        symbol,
        setPnlNotionalDecimalPrecision,
        unPnlPriceBasis,
        setUnPnlPriceBasic
      };
    };
  }
});
var PositionHeaderWidget;
var init_positionHeader_widget = __esm2({
  "src/components/base/positionHeader/positionHeader.widget.tsx"() {
    init_positionHeader_script();
    init_positionHeader_ui();
    PositionHeaderWidget = (props) => {
      const state = usePositionHeaderScript(props);
      return (0, import_jsx_runtime5.jsx)(PositionHeader, { ...state });
    };
  }
});
var positionHeader_exports = {};
__export2(positionHeader_exports, {
  PositionHeader: () => PositionHeader,
  PositionHeaderWidget: () => PositionHeaderWidget
});
var init_positionHeader = __esm2({
  "src/components/base/positionHeader/index.ts"() {
    init_positionHeader_ui();
    init_positionHeader_widget();
  }
});
var LazySettingWidget;
var LazyPositionHeaderWidget;
var PositionsView;
var LiquidationTab;
var DataList;
var init_dataList_ui = __esm2({
  "src/components/desktop/dataList/dataList.ui.tsx"() {
    init_dataList_script();
    LazySettingWidget = import_react8.default.lazy(
      () => Promise.resolve().then(() => (init_setting(), setting_exports)).then((mod) => {
        return { default: mod.SettingWidget };
      })
    );
    LazyPositionHeaderWidget = import_react8.default.lazy(
      () => Promise.resolve().then(() => (init_positionHeader(), positionHeader_exports)).then((mod) => {
        return { default: mod.PositionHeaderWidget };
      })
    );
    PositionsView = (props) => {
      return (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", width: "100%", height: "100%", children: [
        (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
          LazyPositionHeaderWidget,
          {
            setPnlNotionalDecimalPrecision: props.setPnlNotionalDecimalPrecision,
            pnlNotionalDecimalPrecision: props.pnlNotionalDecimalPrecision,
            symbol: !!props.showAllSymbol ? void 0 : props.symbol,
            setUnPnlPriceBasic: props.setUnPnlPriceBasic,
            unPnlPriceBasis: props.unPnlPriceBasis
          }
        ) }),
        (0, import_jsx_runtime5.jsx)(Divider, { className: "oui-w-full" }),
        (0, import_jsx_runtime5.jsx)(Box, { className: "oui-h-[calc(100%_-_60px)]", width: "100%", children: (0, import_jsx_runtime5.jsx)(
          PositionsWidget,
          {
            symbol: !!props.showAllSymbol ? void 0 : props.symbol,
            pnlNotionalDecimalPrecision: props.pnlNotionalDecimalPrecision,
            sharePnLConfig: props.sharePnLConfig,
            calcMode: props.calcMode,
            includedPendingOrder: props.includedPendingOrder,
            onSymbolChange: props.onSymbolChange
          }
        ) })
      ] });
    };
    LiquidationTab = () => {
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsxs)("div", { className: "oui-flex oui-space-x-1", children: [
        (0, import_jsx_runtime5.jsx)("span", { children: t("positions.liquidation") }),
        (0, import_jsx_runtime5.jsx)(
          Tooltip,
          {
            className: "oui-max-w-[275px] oui-bg-base-6",
            content: (0, import_jsx_runtime5.jsxs)("div", { children: [
              (0, import_jsx_runtime5.jsx)("div", { className: "oui-text-pretty", children: t("positions.Liquidation.tooltip.liquidation") }),
              (0, import_jsx_runtime5.jsx)("div", { children: (0, import_jsx_runtime5.jsx)(
                "a",
                {
                  href: "https://orderly.network/docs/introduction/trade-on-orderly/perpetual-futures/liquidations",
                  target: "_blank",
                  rel: "noopener noreferrer",
                  className: "oui-text-primary",
                  children: t("positions.Liquidation.tooltip.viewMore")
                }
              ) })
            ] }),
            arrow: { className: "oui-fill-base-6" },
            children: (0, import_jsx_runtime5.jsx)("span", { className: "oui-hidden group-data-[state=active]:oui-block oui-cursor-pointer", children: (0, import_jsx_runtime5.jsx)(InfoCircleIcon, {}) })
          }
        )
      ] });
    };
    DataList = (props) => {
      const { t } = useTranslation();
      const {
        positionCount = 0,
        pendingOrderCount = 0,
        tpSlOrderCount = 0,
        showAllSymbol,
        symbol,
        onSymbolChange,
        pnlNotionalDecimalPrecision,
        sharePnLConfig,
        setShowAllSymbol,
        current,
        unPnlPriceBasis,
        setUnPnlPriceBasic,
        setPnlNotionalDecimalPrecision
      } = props;
      const tabPanelItems = [
        {
          value: "Positions",
          title: `${t("common.positions")} ${positionCount > 0 ? `(${positionCount})` : ""}`,
          content: (0, import_jsx_runtime5.jsx)(PositionsView, { ...props })
        },
        {
          value: "Pending",
          title: `${t("orders.status.pending")} ${pendingOrderCount > 0 ? `(${pendingOrderCount})` : ""}`,
          content: (0, import_jsx_runtime5.jsx)(
            DesktopOrderListWidget,
            {
              type: TabType.pending,
              ordersStatus: OrderStatus.INCOMPLETE,
              symbol: showAllSymbol ? void 0 : symbol,
              onSymbolChange,
              testIds: { tableBody: "oui-testid-dataList-pending-table-body" }
            }
          )
        },
        {
          value: "TP/SL",
          title: `${t("common.tpsl")} ${tpSlOrderCount > 0 ? `(${tpSlOrderCount})` : ""}`,
          content: (0, import_jsx_runtime5.jsx)(
            DesktopOrderListWidget,
            {
              type: TabType.tp_sl,
              ordersStatus: OrderStatus.INCOMPLETE,
              symbol: showAllSymbol ? void 0 : symbol,
              onSymbolChange,
              testIds: { tableBody: "oui-testid-dataList-tpsl-table-body" }
            }
          )
        },
        {
          value: "Filled",
          title: t("orders.status.filled"),
          content: (0, import_jsx_runtime5.jsx)(
            DesktopOrderListWidget,
            {
              type: TabType.filled,
              symbol: showAllSymbol ? void 0 : symbol,
              pnlNotionalDecimalPrecision,
              ordersStatus: OrderStatus.FILLED,
              onSymbolChange,
              testIds: { tableBody: "oui-testid-dataList-filled-table-body" },
              sharePnLConfig
            }
          )
        },
        {
          value: "Position history",
          title: t("positions.positionHistory"),
          content: (0, import_jsx_runtime5.jsx)(
            PositionHistoryWidget,
            {
              pnlNotionalDecimalPrecision,
              symbol: showAllSymbol ? void 0 : symbol,
              onSymbolChange,
              sharePnLConfig
            }
          )
        },
        {
          value: "Order history",
          title: t("orders.orderHistory"),
          content: (0, import_jsx_runtime5.jsx)(
            DesktopOrderListWidget,
            {
              type: TabType.orderHistory,
              pnlNotionalDecimalPrecision,
              symbol: showAllSymbol ? void 0 : symbol,
              onSymbolChange,
              testIds: { tableBody: "oui-testid-dataList-orderHistory-table-body" },
              sharePnLConfig
            }
          )
        },
        {
          value: "Liquidation",
          title: (0, import_jsx_runtime5.jsx)(LiquidationTab, {}),
          content: (0, import_jsx_runtime5.jsx)(LiquidationWidget, { symbol: showAllSymbol ? void 0 : symbol })
        },
        {
          value: "Assets",
          title: t("common.assets"),
          content: (0, import_jsx_runtime5.jsx)(
            Flex,
            {
              width: "100%",
              height: "100%",
              className: "oui-overflow-y-auto oui-hide-scrollbar oui-pt-3",
              children: (0, import_jsx_runtime5.jsx)(
                assets_exports2.AssetsDataTableWidget,
                {
                  classNames: {
                    scrollRoot: "oui-h-full oui-mb-6 oui-mt-0 oui-p-0 oui-pt-3 oui-h-[calc(100%_-_40px)]",
                    root: "oui-gap-4 oui-h-full",
                    desc: "oui-ml-1"
                  },
                  dataTableClassNames: {
                    header: "oui-bg-base-9",
                    root: "oui-h-[calc(100%_-_28px)]"
                  }
                }
              )
            }
          )
        }
      ];
      return (0, import_jsx_runtime5.jsx)(
        Tabs,
        {
          defaultValue: current || "Positions",
          variant: "contained",
          trailing: (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
            LazySettingWidget,
            {
              pnlNotionalDecimalPrecision,
              setPnlNotionalDecimalPrecision,
              unPnlPriceBasis,
              setUnPnlPriceBasic,
              hideOtherSymbols: !showAllSymbol,
              setHideOtherSymbols: (value) => setShowAllSymbol(!value)
            }
          ) }),
          size: "lg",
          className: "oui-h-full",
          classNames: {
            trigger: "oui-group",
            tabsContent: "oui-h-[calc(100%_-_32px)]"
          },
          children: tabPanelItems.map((item) => {
            const { content, ...rest } = item;
            return (0, import_react8.createElement)(TabPanel, { ...rest, key: `item-${rest.value}` }, content);
          })
        }
      );
    };
  }
});
var DataListWidget;
var init_dataList_widget = __esm2({
  "src/components/desktop/dataList/dataList.widget.tsx"() {
    init_dataList_script();
    init_dataList_ui();
    DataListWidget = (props) => {
      const state = useDataListScript(props);
      return (0, import_jsx_runtime5.jsx)(DataList, { ...state });
    };
  }
});
var dataList_exports = {};
__export2(dataList_exports, {
  DataList: () => DataList,
  DataListWidget: () => DataListWidget
});
var init_dataList = __esm2({
  "src/components/desktop/dataList/index.ts"() {
    init_dataList_ui();
    init_dataList_widget();
  }
});
var LastTrades;
var Row;
var Header;
var List;
var init_lastTrades_ui = __esm2({
  "src/components/base/lastTrades/lastTrades.ui.tsx"() {
    LastTrades = (props) => {
      return (0, import_jsx_runtime5.jsxs)(
        Box,
        {
          className: M(
            "oui-grid oui-grid-rows=[auto,1fr] oui-h-full oui-w-full",
            props.classNames?.root
          ),
          style: props.style,
          children: [
            (0, import_jsx_runtime5.jsx)(Box, { className: "oui-pr-1", children: (0, import_jsx_runtime5.jsx)(
              Header,
              {
                base: props.base,
                quote: props.quote,
                className: props.classNames?.listHeader
              }
            ) }),
            (0, import_jsx_runtime5.jsx)(
              List,
              {
                data: props.data,
                isLoading: props.isLoading,
                baseDp: props.baseDp,
                quoteDp: props.quoteDp,
                classNames: props.classNames?.listItem,
                className: props.classNames?.list
              }
            )
          ]
        }
      );
    };
    Row = (props) => {
      const { left, mid, right, classNames } = props;
      return (
        // <Flex
        //   key={key}
        //   height={20}
        //   gap={2}
        //   width={"100%"}
        //   className={cn("oui-text-xs oui-tabular-nums", classNames?.root)}
        // >
        //   <Box className={cn("oui-flex-1", classNames?.left)}>{left}</Box>
        //   <Box className={cn("oui-flex-1", classNames?.mid)}>{mid}</Box>
        //   <Box className={cn("oui-flex-1 oui-text-right", classNames?.right)}>
        //     {right}
        //   </Box>
        // </Flex>
        (0, import_jsx_runtime5.jsxs)(
          Grid2,
          {
            cols: 3,
            gapX: 2,
            width: "100%",
            className: M("oui-text-xs oui-tabular-nums", classNames?.root),
            children: [
              (0, import_jsx_runtime5.jsx)("div", { className: M("oui-flex-1", classNames?.left), children: left }),
              (0, import_jsx_runtime5.jsx)("div", { className: M("oui-flex-1", classNames?.mid), children: mid }),
              (0, import_jsx_runtime5.jsx)("div", { className: M("oui-flex-1 oui-text-right", classNames?.right), children: right })
            ]
          }
        )
      );
    };
    Header = (props) => {
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsx)(
        Row,
        {
          left: t("common.time"),
          mid: `${t("common.price")}(${props.quote})`,
          right: `${t("common.qty")}(${props.base})`,
          classNames: {
            root: M(
              "oui-text-base-contrast-54 oui-h-[32px] oui-sticky",
              props.className
            )
          }
        }
      );
    };
    List = (props) => {
      return (0, import_jsx_runtime5.jsx)(
        ListView,
        {
          dataSource: props.data,
          className: M(
            "oui-last-trades-list",
            "oui-w-full oui-h-full",
            props.className,
            "oui-overflow-auto"
          ),
          contentClassName: "!oui-space-y-0 oui-pr-[-4px]",
          renderItem: (item, index) => {
            return (0, import_jsx_runtime5.jsx)(
              Row,
              {
                left: (0, import_jsx_runtime5.jsx)(Text2.formatted, { rule: "date", formatString: "HH:mm:ss", children: item?.ts }),
                mid: commifyOptional(item?.price, { fix: props.quoteDp }),
                right: commifyOptional(item?.size, { fix: props.baseDp }),
                classNames: {
                  left: M("oui-text-base-contrast-80", props.classNames?.left),
                  right: M(
                    item.side === OrderSide.BUY ? "oui-text-trade-profit" : "oui-text-trade-loss",
                    props.classNames?.mid
                  ),
                  mid: M(
                    item.side === OrderSide.BUY ? "oui-text-trade-profit" : "oui-text-trade-loss",
                    props.classNames?.right
                  )
                }
              },
              index
            );
          }
        }
      );
    };
  }
});
var useLastTradesScript;
var init_lastTrades_script = __esm2({
  "src/components/base/lastTrades/lastTrades.script.tsx"() {
    useLastTradesScript = (symbol) => {
      const { data, isLoading } = useMarketTradeStream(symbol);
      const config = useSymbolsInfo()?.[symbol];
      const base = config?.("base");
      const quote = config?.("quote");
      const baseDp = config?.("base_dp");
      const quoteDp = config?.("quote_dp");
      return {
        base,
        quote,
        data,
        isLoading,
        baseDp,
        quoteDp
      };
    };
  }
});
var LastTradesWidget;
var init_lastTrades_widget = __esm2({
  "src/components/base/lastTrades/lastTrades.widget.tsx"() {
    init_lastTrades_script();
    init_lastTrades_ui();
    LastTradesWidget = (props) => {
      const state = useLastTradesScript(props.symbol);
      return (0, import_jsx_runtime5.jsx)(LastTrades, { ...state, classNames: props.classNames, style: props.style });
    };
  }
});
var lastTrades_exports = {};
__export2(lastTrades_exports, {
  LastTrades: () => LastTrades,
  LastTradesWidget: () => LastTradesWidget
});
var init_lastTrades = __esm2({
  "src/components/base/lastTrades/index.ts"() {
    init_lastTrades_ui();
    init_lastTrades_widget();
  }
});
function FaucetUi(props) {
  const { t } = useTranslation();
  if (!props.showFaucet) {
    return null;
  }
  return (0, import_jsx_runtime5.jsx)(
    Button,
    {
      variant: "outlined",
      fullWidth: true,
      size: "md",
      onClick: props.getFaucet,
      loading: props.loading,
      className: "oui-text-primary-light oui-border-primary-light oui-rounded",
      "data-testid": "oui-testid-assetView-getFaucet-button",
      children: t("trading.faucet.getTestUSDC")
    }
  );
}
var init_faucet_ui = __esm2({
  "src/components/desktop/assetView/faucet/faucet.ui.tsx"() {
  }
});
function useFaucetScript() {
  const { t } = useTranslation();
  const { connectedChain, namespace } = useWalletConnector();
  const { state, account } = useAccount();
  const config = useConfig();
  const operatorUrl = config.get("operatorUrl");
  const [getTestUSDC, { isMutating }] = useMutation(
    `${operatorUrl}/v1/faucet/usdc`
  );
  const [loading, setLoading] = (0, import_react8.useState)(false);
  const showFaucet = (0, import_react8.useMemo)(() => {
    if (!connectedChain || !connectedChain.id) {
      return false;
    }
    return (state.status === AccountStatusEnum.EnableTrading || state.status === AccountStatusEnum.EnableTradingWithoutConnected) && isTestnet(parseInt(connectedChain.id));
  }, [state, connectedChain]);
  const getFaucet = () => {
    if (loading) {
      return;
    }
    setLoading(true);
    const message = t("trading.faucet.getTestUSDC.success", {
      quantity: namespace === ChainNamespace.solana ? "100" : "1,000"
    });
    return getTestUSDC({
      chain_id: account.walletAdapter?.chainId.toString(),
      user_address: state.address,
      broker_id: config.get("brokerId")
    }).then(
      (res) => {
        setLoading(false);
        if (res.success) {
          return modal.alert({
            title: t("trading.faucet.getTestUSDC"),
            message,
            onOk: () => {
              return new Promise((resolve) => resolve(true));
            }
          });
        }
        res.message && Vt.error(res.message);
      },
      (error) => {
        Vt.error(error.message);
      }
    );
  };
  return { getFaucet, showFaucet, loading };
}
var init_faucet_script = __esm2({
  "src/components/desktop/assetView/faucet/faucet.script.tsx"() {
  }
});
function FaucetWidget() {
  const state = useFaucetScript();
  return (0, import_jsx_runtime5.jsx)(FaucetUi, { ...state });
}
var init_faucet_widget = __esm2({
  "src/components/desktop/assetView/faucet/faucet.widget.tsx"() {
    init_faucet_ui();
    init_faucet_script();
  }
});
var calculateTextColor;
var useCurrentStatusText;
var TooltipContent2;
var TotalValue;
var AssetDetail;
var LTVDetail;
var AssetValueList;
var AssetView;
var init_assetView_ui = __esm2({
  "src/components/desktop/assetView/assetView.ui.tsx"() {
    init_faucet_widget();
    calculateTextColor = (val) => {
      if (val >= 0 && val < 50) {
        return "oui-text-success";
      } else if (val >= 50 && val < 80) {
        return "oui-text-warning";
      } else if (val >= 80) {
        return "oui-text-danger";
      } else {
        return "";
      }
    };
    useCurrentStatusText = () => {
      const { state } = useAccount();
      const { wrongNetwork, disabledConnect } = useAppContext();
      const { t } = useTranslation();
      return (0, import_react8.useMemo)(() => {
        const statusText = {
          wrongNetwork: {
            title: t("connector.wrongNetwork"),
            description: t("connector.wrongNetwork.tooltip"),
            titleColor: "warning"
          },
          connectWallet: {
            title: t("connector.connectWallet"),
            description: t("connector.trade.connectWallet.tooltip"),
            titleClsName: "oui-text-transparent oui-bg-clip-text oui-gradient-brand"
          },
          createAccount: {
            title: t("connector.createAccount"),
            description: t("connector.trade.createAccount.tooltip"),
            titleColor: "primary"
          },
          enableTrading: {
            title: t("connector.enableTrading"),
            description: t("connector.trade.enableTrading.tooltip"),
            titleColor: "primary"
          },
          empty: {
            title: "",
            description: ""
          }
        };
        if (disabledConnect) {
          return statusText.connectWallet;
        }
        if (wrongNetwork) {
          return statusText.wrongNetwork;
        }
        if (state.status === AccountStatusEnum.EnableTradingWithoutConnected) {
          return statusText.empty;
        }
        if (state.status <= AccountStatusEnum.NotConnected) {
          return statusText.connectWallet;
        }
        if (state.status <= AccountStatusEnum.NotSignedIn) {
          return statusText.createAccount;
        }
        if (state.status < AccountStatusEnum.EnableTrading) {
          return statusText.enableTrading;
        }
        return statusText.empty;
      }, [t, state.status, wrongNetwork, disabledConnect]);
    };
    TooltipContent2 = (props) => {
      const { description, formula } = props;
      return (0, import_jsx_runtime5.jsxs)("div", { className: "oui-min-w-[204px] oui-max-w-[240px] oui-text-2xs oui-leading-normal oui-text-base-contrast-80", children: [
        typeof description !== "undefined" && description !== null && (0, import_jsx_runtime5.jsx)("span", { children: description }),
        (0, import_jsx_runtime5.jsx)(Divider, { className: "oui-border-white/10", my: 2 }),
        typeof formula !== "undefined" && formula !== null && (0, import_jsx_runtime5.jsx)("span", { children: formula })
      ] });
    };
    TotalValue = (props) => {
      const { t } = useTranslation();
      const { totalValue, visible = true, onToggleVisibility } = props;
      return (0, import_jsx_runtime5.jsxs)(
        Flex,
        {
          direction: "column",
          gap: 1,
          className: "oui-text-2xs",
          itemAlign: "center",
          children: [
            (0, import_jsx_runtime5.jsx)(
              Text2.numeral,
              {
                visible,
                weight: "bold",
                size: "2xl",
                className: gradientTextVariants({ color: "brand" }),
                as: "div",
                padding: false,
                dp: 2,
                children: totalValue ?? "--"
              }
            ),
            (0, import_jsx_runtime5.jsxs)(Flex, { gap: 1, itemAlign: "center", children: [
              (0, import_jsx_runtime5.jsx)(Text2, { size: "2xs", color: "neutral", weight: "semibold", children: `${t("trading.asset.myAssets")} (USDC)` }),
              (0, import_jsx_runtime5.jsx)("button", { onClick: onToggleVisibility, children: visible ? (0, import_jsx_runtime5.jsx)(EyeIcon, { size: 18, className: "oui-text-base-contrast-54" }) : (0, import_jsx_runtime5.jsx)(EyeCloseIcon, { size: 18, className: "oui-text-base-contrast-54" }) })
            ] })
          ]
        }
      );
    };
    AssetDetail = (props) => {
      const {
        label,
        description,
        formula,
        visible,
        value,
        unit,
        rule,
        placeholder
      } = props;
      return (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", children: [
        (0, import_jsx_runtime5.jsx)(
          Tooltip,
          {
            className: "",
            content: (0, import_jsx_runtime5.jsx)(TooltipContent2, { description, formula }),
            children: (0, import_jsx_runtime5.jsx)(
              Text2,
              {
                size: "2xs",
                color: "neutral",
                weight: "semibold",
                className: "oui-cursor-pointer oui-border-b oui-border-dashed oui-border-line-12",
                children: label
              }
            )
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          Text2.numeral,
          {
            visible,
            size: "2xs",
            unit,
            unitClassName: "oui-text-base-contrast-36 oui-ml-0.5",
            as: "div",
            rule,
            padding: false,
            dp: 2,
            placeholder,
            children: value || "--"
          }
        )
      ] });
    };
    LTVDetail = (props) => {
      const { visible, value } = props;
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", children: [
        (0, import_jsx_runtime5.jsx)(
          Tooltip,
          {
            className: M("oui-bg-base-6 oui-p-2"),
            content: (0, import_jsx_runtime5.jsx)(LTVRiskTooltipWidget, {}),
            children: (0, import_jsx_runtime5.jsx)(
              Text2,
              {
                size: "2xs",
                color: "neutral",
                weight: "semibold",
                className: "oui-cursor-pointer oui-border-b oui-border-dashed oui-border-line-12",
                children: t("transfer.LTV")
              }
            )
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          Text2,
          {
            size: "2xs",
            className: M(
              "select-none",
              visible && calculateTextColor(Number(value))
            ),
            children: visible ? `${value}%` : "*****"
          }
        )
      ] });
    };
    AssetValueList = (props) => {
      const {
        visible = true,
        freeCollateral,
        marginRatioVal,
        renderMMR,
        maintenanceMargin,
        currentLeverage,
        isConnected,
        currentLtv
      } = props;
      const [optionsOpen, setOptionsOpen] = useLocalStorage(
        "orderly_entry_asset_list_open",
        false
      );
      const [open2, setOpen] = (0, import_react8.useState)(optionsOpen);
      const { t } = useTranslation();
      const toggleOpen = (0, import_react8.useCallback)(() => {
        setOpen((prevOpen) => !prevOpen);
        setTimeout(() => {
          setOptionsOpen(!open2);
        }, 0);
      }, []);
      const showLTV = typeof currentLtv === "number" && !Number.isNaN(currentLtv) && currentLtv > 0;
      return (0, import_jsx_runtime5.jsxs)(Box, { className: "oui-group", children: [
        (0, import_jsx_runtime5.jsxs)(
          Flex,
          {
            justify: "center",
            gap: 1,
            itemAlign: "center",
            className: "oui-cursor-pointer",
            onClick: toggleOpen,
            children: [
              (0, import_jsx_runtime5.jsx)(Divider, { className: "oui-flex-1" }),
              (0, import_jsx_runtime5.jsx)(
                ChevronDownIcon,
                {
                  size: 12,
                  color: "white",
                  className: M("oui-transition-transform", open2 && "oui-rotate-180")
                }
              ),
              (0, import_jsx_runtime5.jsx)(Divider, { className: "oui-flex-1" })
            ]
          }
        ),
        (0, import_jsx_runtime5.jsxs)(
          Box,
          {
            style: { transform: "translateZ(0)" },
            className: M(
              "oui-select-none oui-space-y-1.5 oui-overflow-hidden",
              "oui-transition-[max-height] oui-duration-150",
              "group-hover:oui-will-change-[max-height]",
              open2 ? showLTV ? "oui-max-h-[144px]" : "oui-max-h-[119px]" : "oui-max-h-0"
            ),
            children: [
              (0, import_jsx_runtime5.jsx)(
                AssetDetail,
                {
                  label: t("trading.asset.freeCollateral"),
                  description: t("trading.asset.freeCollateral.tooltip"),
                  formula: t("trading.asset.freeCollateral.formula"),
                  visible,
                  value: freeCollateral === 0 ? "0" : freeCollateral,
                  unit: "USDC"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                AssetDetail,
                {
                  label: t("trading.asset.marginRatio"),
                  description: t("trading.asset.marginRatio.tooltip"),
                  formula: t("trading.asset.marginRatio.formula"),
                  visible,
                  value: marginRatioVal,
                  isConnected,
                  rule: "percentages",
                  showPercentage: true,
                  placeholder: "--%"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                AssetDetail,
                {
                  label: t("trading.asset.maintenanceMarginRatio"),
                  description: t("trading.asset.maintenanceMarginRatio.tooltip"),
                  formula: t("trading.asset.maintenanceMarginRatio.formula"),
                  visible,
                  value: renderMMR,
                  rule: "percentages",
                  showPercentage: true,
                  placeholder: "--%"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                AssetDetail,
                {
                  label: t("trading.asset.maintenanceMargin"),
                  description: t("trading.asset.maintenanceMargin.tooltip"),
                  formula: t("trading.asset.maintenanceMargin.formula"),
                  visible,
                  value: maintenanceMargin,
                  unit: "USDC"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                AssetDetail,
                {
                  label: t("trading.asset.currentLeverage"),
                  description: t("trading.asset.currentLeverage.tooltip"),
                  formula: t("trading.asset.currentLeverage.formula"),
                  visible,
                  value: currentLeverage,
                  unit: "x"
                }
              ),
              showLTV && (0, import_jsx_runtime5.jsx)(LTVDetail, { visible, value: currentLtv })
            ]
          }
        )
      ] });
    };
    AssetView = ({
      networkId,
      isFirstTimeDeposit,
      totalValue,
      onDeposit,
      onWithdraw,
      onTransfer,
      toggleVisible,
      visible,
      freeCollateral,
      marginRatioVal,
      renderMMR,
      maintenanceMargin,
      currentLeverage,
      isConnected,
      isMainAccount,
      hasSubAccount,
      currentLtv
    }) => {
      const { title, description, titleColor, titleClsName } = useCurrentStatusText();
      const { t } = useTranslation();
      const transferButton = hasSubAccount && (0, import_jsx_runtime5.jsx)(
        Button,
        {
          fullWidth: true,
          color: "secondary",
          size: "md",
          onClick: onTransfer,
          "data-testid": "oui-testid-assetView-transfer-button",
          children: (0, import_jsx_runtime5.jsx)(Text2, { children: t("common.transfer") })
        }
      );
      const depositButton = isMainAccount && (0, import_jsx_runtime5.jsxs)(
        Button,
        {
          "data-testid": "oui-testid-assetView-deposit-button",
          fullWidth: true,
          size: "md",
          onClick: onDeposit,
          children: [
            !hasSubAccount && (0, import_jsx_runtime5.jsx)(ArrowDownShortIcon, { opacity: 1, className: "oui-text-primary-contrast" }),
            (0, import_jsx_runtime5.jsx)(Text2, { children: t("common.deposit") })
          ]
        }
      );
      const withdrawButton = isMainAccount && (0, import_jsx_runtime5.jsxs)(
        Button,
        {
          fullWidth: true,
          color: "secondary",
          size: "md",
          onClick: onWithdraw,
          "data-testid": "oui-testid-assetView-withdraw-button",
          children: [
            !hasSubAccount && (0, import_jsx_runtime5.jsx)(
              ArrowDownShortIcon,
              {
                color: "white",
                opacity: 1,
                className: "oui-rotate-180"
              }
            ),
            (0, import_jsx_runtime5.jsx)(Text2, { children: t("common.withdraw") })
          ]
        }
      );
      return (0, import_jsx_runtime5.jsxs)(Box, { className: "oui-relative", children: [
        title && description && (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", gap: 1, className: "oui-mb-[32px]", children: [
          (0, import_jsx_runtime5.jsx)(
            Text2,
            {
              size: "lg",
              weight: "bold",
              color: titleColor || "inherit",
              className: titleClsName,
              children: title
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            Text2,
            {
              size: "2xs",
              color: "neutral",
              weight: "semibold",
              className: "oui-text-center",
              children: description
            }
          )
        ] }),
        (0, import_jsx_runtime5.jsx)(
          AuthGuard,
          {
            networkId,
            buttonProps: { size: "md", fullWidth: true },
            children: isFirstTimeDeposit && isMainAccount ? (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
              (0, import_jsx_runtime5.jsx)(Box, { children: (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", gap: 1, className: "oui-mb-[32px]", children: [
                (0, import_jsx_runtime5.jsx)(Text2.gradient, { size: "lg", weight: "bold", color: "brand", children: t("trading.asset.startTrading") }),
                (0, import_jsx_runtime5.jsx)(Text2, { size: "2xs", color: "neutral", weight: "semibold", children: t("trading.asset.startTrading.description") })
              ] }) }),
              (0, import_jsx_runtime5.jsxs)(
                Button,
                {
                  "data-testid": "oui-testid-assetView-deposit-button",
                  fullWidth: true,
                  size: "md",
                  onClick: onDeposit,
                  children: [
                    (0, import_jsx_runtime5.jsx)(
                      ArrowDownShortIcon,
                      {
                        opacity: 1,
                        className: "oui-text-primary-contrast"
                      }
                    ),
                    (0, import_jsx_runtime5.jsx)(Text2, { children: t("common.deposit") })
                  ]
                }
              ),
              (0, import_jsx_runtime5.jsx)(Box, { className: "oui-mt-3", children: (0, import_jsx_runtime5.jsx)(FaucetWidget, {}) })
            ] }) : (0, import_jsx_runtime5.jsxs)(Box, { className: "oui-space-y-4", children: [
              (0, import_jsx_runtime5.jsx)(
                TotalValue,
                {
                  totalValue,
                  visible,
                  onToggleVisibility: toggleVisible
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                AssetValueList,
                {
                  visible,
                  freeCollateral,
                  marginRatioVal,
                  renderMMR,
                  maintenanceMargin,
                  currentLeverage,
                  isConnected,
                  currentLtv
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                Flex,
                {
                  gap: isMainAccount ? hasSubAccount ? 2 : 3 : 0,
                  itemAlign: "center",
                  children: isMainAccount ? (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
                    depositButton,
                    transferButton,
                    withdrawButton
                  ] }) : transferButton
                }
              ),
              isMainAccount && (0, import_jsx_runtime5.jsx)(FaucetWidget, {})
            ] })
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          "div",
          {
            className: M(
              "oui-absolute oui-inset-0 oui-rotate-180",
              "oui-pointer-events-none oui-rounded-2xl oui-blur-[200px]"
            ),
            style: {
              background: "conic-gradient(from -40.91deg at 40.63% 50.41%, rgba(159, 115, 241, 0) -48.92deg, rgba(242, 98, 181, 0) 125.18deg, #5FC5FF 193.41deg, #FFAC89 216.02deg, #8155FF 236.07deg, #789DFF 259.95deg, rgba(159, 115, 241, 0) 311.08deg, rgba(242, 98, 181, 0) 485.18deg)"
            }
          }
        )
      ] });
    };
  }
});
var useAssetViewScript;
var init_assetView_script = __esm2({
  "src/components/desktop/assetView/assetView.script.tsx"() {
    useAssetViewScript = () => {
      const { t } = useTranslation();
      const account = useAccountInstance();
      const ee = useEventEmitter();
      const { totalValue } = useCollateral({
        dp: 2
      });
      const networkId = useConfig("networkId");
      const { state, isMainAccount } = useAccount();
      const { freeCollateral } = useCollateral({
        dp: 2
      });
      const { marginRatio, mmr, maintenanceMargin, currentLeverage } = useMarginRatio();
      const isConnected = state.status >= AccountStatusEnum.Connected;
      const [{ aggregated }, positionsInfo] = usePositionStream();
      const marginRatioVal = (0, import_react8.useMemo)(() => {
        return Math.min(
          10,
          aggregated.notional === 0 ? positionsInfo["margin_ratio"](10) : marginRatio
        );
      }, [marginRatio, aggregated]);
      const openDepositAndWithdraw = (0, import_react8.useCallback)(
        (viewName) => {
          return modal.show(DepositAndWithdrawWithDialogId, {
            activeTab: viewName
          });
        },
        []
      );
      const onDeposit = (0, import_react8.useCallback)(async () => {
        return openDepositAndWithdraw("deposit");
      }, []);
      const onWithdraw = (0, import_react8.useCallback)(async () => {
        return openDepositAndWithdraw("withdraw");
      }, []);
      const onTransfer = (0, import_react8.useCallback)(async () => {
        return modal.show(TransferDialogId);
      }, []);
      const onSettle = (0, import_react8.useCallback)(async () => {
        return account.settle().catch((e) => {
          if (e.code === -1104) {
            Vt.error(t("settle.settlement.error"));
            return Promise.reject(e);
          }
          if (e.message.indexOf(
            "Signing off chain messages with Ledger is not yet supported"
          ) !== -1) {
            ee.emit("wallet:sign-message-with-ledger-error", {
              message: e.message,
              userAddress: account.address
            });
          }
        }).then((res) => {
          Vt.success(t("settle.settlement.requested"));
          return Promise.resolve(res);
        });
      }, [account, t]);
      const [visible, setVisible] = useLocalStorage(
        "orderly_assets_visible",
        true
      );
      const toggleVisible = (0, import_react8.useCallback)(() => {
        setVisible((visible2) => {
          return !visible2;
        });
      }, [visible]);
      const currentLtv = useComputedLTV();
      const _freeCollateral = useDataTap(freeCollateral) ?? void 0;
      const _marginRatioVal = useDataTap(marginRatioVal) ?? void 0;
      const _mmr = useDataTap(mmr) ?? void 0;
      const _maintenanceMargin = useDataTap(maintenanceMargin) ?? void 0;
      const _currentLeverage = useDataTap(currentLeverage) ?? void 0;
      const _totalValue = useDataTap(totalValue) ?? void 0;
      return {
        onDeposit,
        onWithdraw,
        onTransfer,
        onSettle,
        visible,
        toggleVisible,
        networkId,
        totalValue: _totalValue,
        status: state.status,
        freeCollateral: _freeCollateral,
        marginRatioVal: _marginRatioVal,
        renderMMR: _mmr,
        maintenanceMargin: _maintenanceMargin,
        currentLeverage: _currentLeverage,
        isConnected,
        isMainAccount,
        hasSubAccount: !!state.subAccounts?.length,
        currentLtv
      };
    };
  }
});
var AssetViewWidget;
var init_assetView_widget = __esm2({
  "src/components/desktop/assetView/assetView.widget.tsx"() {
    init_assetView_script();
    init_assetView_ui();
    AssetViewWidget = (props) => {
      const state = useAssetViewScript();
      return (0, import_jsx_runtime5.jsx)(AssetView, { ...state, isFirstTimeDeposit: props.isFirstTimeDeposit });
    };
  }
});
var assetView_exports = {};
__export2(assetView_exports, {
  AssetView: () => AssetView,
  AssetViewWidget: () => AssetViewWidget
});
var init_assetView = __esm2({
  "src/components/desktop/assetView/index.ts"() {
    init_assetView_ui();
    init_assetView_widget();
  }
});
var OrderBookContext;
var useOrderBookContext;
var ORDERBOOK_COIN_TYPE_KEY;
var ORDERBOOK_MOBILE_COIN_TYPE_KEY;
var OrderBookProvider;
var init_orderContext = __esm2({
  "src/components/base/orderBook/orderContext.tsx"() {
    OrderBookContext = (0, import_react8.createContext)({
      cellHeight: 22
    });
    useOrderBookContext = () => (0, import_react8.useContext)(OrderBookContext);
    ORDERBOOK_COIN_TYPE_KEY = "orderbook_coin_type";
    ORDERBOOK_MOBILE_COIN_TYPE_KEY = "orderbook_mobile_coin_unit";
    OrderBookProvider = (props) => {
      const [mode, setMode] = (0, import_react8.useState)("quantity");
      const [totalMode, setTotalMode] = (0, import_react8.useState)("quantity");
      const memoizedValue = import_react8.default.useMemo(() => {
        return {
          cellHeight: props.cellHeight,
          onItemClick: props.onItemClick,
          mode,
          totalMode: totalMode || "quantity",
          depth: props.depth,
          onModeChange: setMode,
          onTotalModeChange: setTotalMode,
          showTotal: props.showTotal || false,
          pendingOrders: props.pendingOrders,
          symbolInfo: props.symbolInfo,
          showBuySellRatio: props.showBuySellRatio ?? true,
          onShowBuySellRatioChange: props.onShowBuySellRatioChange
        };
      }, [
        mode,
        props.cellHeight,
        props.depth,
        props.onItemClick,
        props.pendingOrders,
        props.showTotal,
        props.symbolInfo,
        props.showBuySellRatio,
        props.onShowBuySellRatioChange,
        totalMode
      ]);
      return (0, import_jsx_runtime5.jsx)(OrderBookContext.Provider, { value: memoizedValue, children: props.children });
    };
  }
});
var init_types = __esm2({
  "src/components/base/orderBook/types.ts"() {
  }
});
var CellBar;
var init_cellBar = __esm2({
  "src/components/base/orderBook/cellBar.tsx"() {
    CellBar = (props) => {
      const {
        direction = 1
        /* RIGHT_TO_LEFT */
      } = props;
      const transform = (0, import_react8.useMemo)(() => {
        const x = Math.max(props.width, 0);
        if (direction === 0) {
          return { transform: `translateX(${x}%)` };
        }
        return { transform: `translateX(-${x}%)` };
      }, [props.width]);
      return (0, import_jsx_runtime5.jsx)(
        "div",
        {
          className: M(
            "oui-absolute oui-right-[-100%] oui-top-0 oui-h-full oui-w-full oui-transition-transform oui-pointer-events-none",
            direction === 0 && "oui-left-[-100%]",
            props.className
          ),
          style: transform
        }
      );
    };
  }
});
var DesktopOrderBookCell;
var init_cell_desktop = __esm2({
  "src/components/desktop/orderBook/cell.desktop.tsx"() {
    init_cellBar();
    init_orderContext();
    init_types();
    DesktopOrderBookCell = (props) => {
      const { cellHeight, showTotal, onItemClick, depth, pendingOrders } = useOrderBookContext();
      const {
        symbolInfo,
        currentHover,
        accumulated,
        accumulatedAmount,
        count,
        price,
        quantity,
        base,
        quote
      } = props;
      const { base_dp, quote_dp } = symbolInfo;
      const [coinType] = useLocalStorage(ORDERBOOK_COIN_TYPE_KEY, base);
      const width = Number.isNaN(price) ? 0 : accumulated / count * 100;
      const dp = (0, import_react8.useMemo)(() => {
        return getPrecisionByNumber(depth || `${quote_dp}`);
      }, [depth, quote_dp]);
      const totalAmount = Number.isNaN(accumulated) ? "-" : accumulatedAmount?.toString();
      const isPendingOrder = (0, import_react8.useMemo)(() => {
        const priceStr = parseNumber(price, { dp, padding: true });
        return pendingOrders.some(
          (item) => priceStr === parseNumber(item, { dp, padding: true })
        );
      }, [pendingOrders, price, dp]);
      return (0, import_jsx_runtime5.jsxs)(
        "div",
        {
          className: "oui-relative oui-flex oui-cursor-pointer oui-flex-row oui-justify-between oui-pl-3 oui-text-xs oui-tabular-nums oui-text-base-contrast-80",
          style: { height: `${cellHeight}px` },
          onClick: () => {
            if (Number.isNaN(price) || Number.isNaN(quantity)) {
              return;
            }
            onItemClick?.([price, quantity]);
          },
          onMouseEnter: props.onMouseEnter,
          onMouseLeave: props.onMouseLeave,
          children: [
            (0, import_jsx_runtime5.jsxs)(
              "div",
              {
                className: M(
                  "oui-mr-2 oui-flex oui-basis-7/12 oui-flex-row oui-items-center",
                  showTotal && "oui-basis-5/12"
                ),
                children: [
                  (0, import_jsx_runtime5.jsx)(
                    "div",
                    {
                      className: M(
                        "oui-flex-1 oui-text-left",
                        props.type === "ask" ? "oui-text-trade-loss" : "oui-text-trade-profit"
                      ),
                      children: (0, import_jsx_runtime5.jsx)(Text2.numeral, { dp, children: price })
                    }
                  ),
                  (0, import_jsx_runtime5.jsx)("div", { className: "oui-flex-1 oui-text-right oui-text-base-contrast-80", children: (0, import_jsx_runtime5.jsx)(Text2.numeral, { dp: base_dp, children: quantity }) })
                ]
              }
            ),
            (0, import_jsx_runtime5.jsxs)(
              "div",
              {
                className: M(
                  "oui-fex-row oui-relative oui-flex oui-basis-5/12 oui-items-center oui-justify-end oui-overflow-hidden",
                  showTotal && "oui-basis-7/12"
                ),
                children: [
                  showTotal ? (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
                    (0, import_jsx_runtime5.jsx)("div", { className: M("oui-flex-1 oui-pr-3 oui-text-right"), children: (0, import_jsx_runtime5.jsx)(Text2.numeral, { dp: base_dp, className: "oui-z-10", children: accumulated }) }),
                    (0, import_jsx_runtime5.jsx)("div", { className: M("oui-flex-1 oui-pr-3 oui-text-right"), children: (0, import_jsx_runtime5.jsx)(Text2.numeral, { dp: 0, className: "oui-z-10", children: totalAmount }) })
                  ] }) : (0, import_jsx_runtime5.jsxs)("div", { className: M("oui-flex-1 oui-pr-3 oui-text-right"), children: [
                    coinType === base && (0, import_jsx_runtime5.jsx)(Text2.numeral, { dp: base_dp, className: "oui-z-10", children: accumulated }),
                    coinType === quote && (0, import_jsx_runtime5.jsx)(Text2.numeral, { dp: 0, className: "oui-z-10", children: totalAmount })
                  ] }),
                  (0, import_jsx_runtime5.jsx)(
                    CellBar,
                    {
                      width,
                      direction: 0,
                      className: props.type === "ask" ? "oui-bg-trade-loss/10" : "oui-bg-trade-profit/10"
                    }
                  )
                ]
              }
            ),
            isPendingOrder && (0, import_jsx_runtime5.jsx)(
              "div",
              {
                className: M(
                  "oui-pointer-events-none oui-absolute oui-left-[4px] oui-size-[4px] oui-rounded-full",
                  props.type === "ask" && "oui-bg-trade-loss",
                  props.type === "bid" && "oui-bg-trade-profit"
                ),
                style: { top: `${cellHeight / 2 - 2}px` }
              }
            ),
            props.isHover && (0, import_jsx_runtime5.jsx)("div", { className: "oui-absolute oui-inset-0 oui-bg-white oui-opacity-[.12]" }),
            currentHover && (0, import_jsx_runtime5.jsx)(
              "div",
              {
                className: M(
                  "oui-absolute oui-inset-x-0",
                  props.type === "ask" && "oui-top-0",
                  props.type === "bid" && "oui-bottom-0"
                ),
                children: (0, import_jsx_runtime5.jsx)(
                  Divider,
                  {
                    lineStyle: "dashed",
                    className: M(
                      "oui-w-full",
                      props.type === "bid" && "oui-border-trade-profit",
                      props.type === "ask" && "oui-border-trade-loss"
                    )
                  }
                )
              }
            )
          ]
        }
      );
    };
  }
});
var calcHintInfo;
var DesktopListBox;
var Tip;
var Row2;
var init_listBox_desktop = __esm2({
  "src/components/desktop/orderBook/listBox.desktop.tsx"() {
    init_orderContext();
    init_types();
    init_cell_desktop();
    calcHintInfo = (item) => {
      if (!item) {
        return { sumQty: 0, sumQtyAmount: 0, avgPrice: 0 };
      }
      const [, , qty, amount] = item;
      const sumQty = !Number.isNaN(qty) ? qty : 0;
      const sumQtyAmount = !Number.isNaN(amount) ? amount : 0;
      return {
        sumQty,
        sumQtyAmount,
        avgPrice: sumQtyAmount === 0 || sumQty === 0 ? 0 : new decimal_default(sumQtyAmount).div(sumQty).toNumber()
      };
    };
    DesktopListBox = (props) => {
      const { data, type, countQty } = props;
      const { symbolInfo, depth } = useOrderBookContext();
      const findMaxItem = (0, import_react8.useCallback)(() => {
        if (!data?.length) {
          return null;
        }
        if (type === "ask") {
          const index = data.findIndex((item) => !Number.isNaN(item[0]));
          if (index !== -1) {
            return data[index];
          }
          return null;
        } else {
          const len = data.length;
          for (let index = len - 1; index >= 0; index--) {
            const item = data[index];
            if (!Number.isNaN(item[0])) {
              return item;
            }
          }
          return null;
        }
      }, [data, type]);
      const priceDp = (0, import_react8.useMemo)(() => {
        if (depth?.toString().includes(".")) {
          return depth.toString().split(".")[1].length;
        }
        return 0;
      }, [depth]);
      const maxQty = (0, import_react8.useMemo)(() => {
        return data.reduce((a, b) => Math.max(a, b[1]), 0);
      }, [data]);
      const [hoverIndex, setHoverIndex] = (0, import_react8.useState)(-1);
      return (0, import_jsx_runtime5.jsx)("div", { className: "oui-order-book-list oui-flex oui-flex-col oui-gap-px", children: data.map((item, index) => {
        return (0, import_jsx_runtime5.jsx)(
          Tip,
          {
            index,
            item,
            countQty,
            setHoverIndex,
            hoverIndex,
            type,
            maxQty,
            priceDp,
            symbolInfo,
            findMaxItem
          },
          index
        );
      }) });
    };
    Tip = (props) => {
      const {
        index,
        item,
        setHoverIndex,
        type,
        maxQty,
        hoverIndex,
        priceDp,
        countQty,
        symbolInfo,
        findMaxItem
      } = props;
      const { base, quote, base_dp: baseDp, quote_dp: quoteDp } = symbolInfo;
      const { t } = useTranslation();
      const isHover = hoverIndex !== -1 ? type === "ask" ? index >= hoverIndex : index <= hoverIndex : false;
      const [open2, setOpen] = (0, import_react8.useState)(false);
      const hintInfo = (0, import_react8.useMemo)(() => {
        const info = calcHintInfo(item);
        return info.avgPrice === 0 ? calcHintInfo(findMaxItem()) : info;
      }, [item, findMaxItem]);
      return (0, import_jsx_runtime5.jsxs)(TooltipRoot, { open: open2, onOpenChange: setOpen, children: [
        (0, import_jsx_runtime5.jsx)(TooltipTrigger, { children: (0, import_jsx_runtime5.jsx)(
          DesktopOrderBookCell,
          {
            background: "",
            price: item[0],
            quantity: item[1],
            accumulated: item[2],
            count: countQty,
            type,
            accumulatedAmount: item[3],
            maxQty,
            isHover,
            currentHover: hoverIndex === index,
            symbolInfo,
            base,
            quote,
            onMouseEnter: () => {
              setHoverIndex(index);
              setOpen(true);
            },
            onMouseLeave: () => {
              setHoverIndex(-1);
              setOpen(false);
            }
          }
        ) }),
        (0, import_jsx_runtime5.jsxs)(
          TooltipContent,
          {
            className: M(
              "oui-rounded-base oui-flex oui-w-full oui-max-w-[400px] oui-flex-col oui-gap-2 oui-bg-base-6 oui-p-3 oui-text-2xs oui-shadow-md"
              // type === OrderBookCellType.ASK ? `oui-mb-${30}` : "oui-mt-0"
            ),
            align: type === "ask" ? "end" : "start",
            alignOffset: -25.4,
            side: "left",
            sideOffset: 2,
            onPointerEnter: (e) => e.preventDefault(),
            children: [
              (0, import_jsx_runtime5.jsx)(
                Row2,
                {
                  title: `${t("common.avgPrice")}≈`,
                  content: hintInfo.avgPrice,
                  contentDp: quoteDp
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                Row2,
                {
                  title: `${t("trading.orderBook.sum")} (${base})`,
                  content: hintInfo.sumQty,
                  contentDp: baseDp
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                Row2,
                {
                  title: `${t("trading.orderBook.sum")} (${quote})`,
                  content: hintInfo.sumQtyAmount,
                  contentDp: quoteDp
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                TooltipArrow,
                {
                  className: "oui-fill-base-6",
                  style: {
                    transform: type === "ask" ? "translateX(80%)" : "translateX(-80%)"
                  }
                }
              )
            ]
          }
        )
      ] });
    };
    Row2 = (props) => {
      const { title, content, contentDp } = props;
      return (0, import_jsx_runtime5.jsxs)("div", { className: "oui-flex oui-flex-row oui-justify-between oui-gap-4", children: [
        (0, import_jsx_runtime5.jsx)("div", { className: "oui-select-none oui-text-base-contrast-36", children: title }),
        (0, import_jsx_runtime5.jsx)("div", { className: "oui-text-end", children: (0, import_jsx_runtime5.jsx)(Text2.numeral, { dp: contentDp, children: content }) })
      ] });
    };
  }
});
var DesktopAsks;
var init_asks_desktop = __esm2({
  "src/components/desktop/orderBook/asks.desktop.tsx"() {
    init_types();
    init_listBox_desktop();
    DesktopAsks = (props) => {
      const { data } = props;
      const countQty = (0, import_react8.useMemo)(() => {
        let max = Number.NaN;
        const len = data.length;
        let index = 0;
        while (Number.isNaN(max) && index < len) {
          max = data[index][2];
          index++;
        }
        return max;
      }, [data]);
      return (0, import_jsx_runtime5.jsx)(
        DesktopListBox,
        {
          type: "ask",
          data,
          countQty
        }
      );
    };
  }
});
var DesktopBids;
var init_bids_desktop = __esm2({
  "src/components/desktop/orderBook/bids.desktop.tsx"() {
    init_types();
    init_listBox_desktop();
    DesktopBids = (props) => {
      const { data } = props;
      const countQty = (0, import_react8.useMemo)(() => {
        let max = Number.NaN;
        let index = data.length - 1;
        while (Number.isNaN(max) && index > 0) {
          max = data[index][2];
          index--;
        }
        return max;
      }, [data]);
      return (0, import_jsx_runtime5.jsx)(
        DesktopListBox,
        {
          type: "bid",
          data,
          countQty
        }
      );
    };
  }
});
var BuySellRatioSettings;
var MoreIcon;
var init_buySellRatio = __esm2({
  "src/components/desktop/orderBook/buySellRatio.tsx"() {
    BuySellRatioSettings = ({
      showBuySellRatio,
      setShowBuySellRatio
    }) => {
      const { t } = useTranslation();
      const [settingsOpen, setSettingsOpen] = (0, import_react8.useState)(false);
      return (0, import_jsx_runtime5.jsxs)(PopoverRoot, { open: settingsOpen, onOpenChange: setSettingsOpen, children: [
        (0, import_jsx_runtime5.jsx)(PopoverTrigger, { asChild: true, children: (0, import_jsx_runtime5.jsx)(
          "button",
          {
            className: "oui-cursor-pointer oui-text-base-contrast-54 hover:oui-text-base-contrast oui-transition-colors",
            "aria-label": t("trading.orderBook.settings"),
            children: (0, import_jsx_runtime5.jsx)(MoreIcon, {})
          }
        ) }),
        (0, import_jsx_runtime5.jsx)(PopoverContent, { align: "end", sideOffset: 8, className: "oui-w-[200px] ", children: (0, import_jsx_runtime5.jsxs)(Flex, { itemAlign: "center", gap: 2, children: [
          (0, import_jsx_runtime5.jsx)(
            Checkbox,
            {
              id: "show-buy-sell-ratio",
              checked: showBuySellRatio,
              onCheckedChange: (checked) => {
                setShowBuySellRatio?.(checked === true);
                setSettingsOpen(false);
              }
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            "label",
            {
              htmlFor: "show-buy-sell-ratio",
              className: "oui-cursor-pointer oui-text-xs oui-text-base-contrast-54",
              children: t("trading.orderBook.showBuySellRatio")
            }
          )
        ] }) })
      ] });
    };
    MoreIcon = (props) => (0, import_jsx_runtime5.jsxs)(
      "svg",
      {
        xmlns: "http://www.w3.org/2000/svg",
        width: "16",
        height: "16",
        viewBox: "0 0 16 16",
        fill: "currentColor",
        ...props,
        children: [
          (0, import_jsx_runtime5.jsx)("path", { d: "M8.00521 6.66797C8.74161 6.66797 9.33854 7.26464 9.33854 8.0013C9.33854 8.73797 8.74161 9.33464 8.00521 9.33464C7.26881 9.33464 6.67188 8.73797 6.67188 8.0013C6.67188 7.26464 7.26881 6.66797 8.00521 6.66797Z" }),
          (0, import_jsx_runtime5.jsx)("path", { d: "M3.33333 6.66797C4.06973 6.66797 4.66667 7.26464 4.66667 8.0013C4.66667 8.73797 4.06973 9.33464 3.33333 9.33464C2.59693 9.33464 2 8.73797 2 8.0013C2 7.26464 2.59693 6.66797 3.33333 6.66797Z" }),
          (0, import_jsx_runtime5.jsx)("path", { d: "M12.6666 6.66797C13.403 6.66797 14 7.26464 14 8.0013C14 8.73797 13.403 9.33464 12.6666 9.33464C11.9302 9.33464 11.3333 8.73797 11.3333 8.0013C11.3333 7.26464 11.9302 6.66797 12.6666 6.66797Z" })
        ]
      }
    );
  }
});
var DesktopDepthSelect;
var init_depthSelect_desktop = __esm2({
  "src/components/desktop/orderBook/depthSelect.desktop.tsx"() {
    DesktopDepthSelect = (props) => {
      const options2 = (0, import_react8.useMemo)(() => {
        return props.depths.map((d) => ({
          value: d,
          label: `${d}`
        }));
      }, [props.depths]);
      return (0, import_jsx_runtime5.jsx)(Box, { pl: 3, width: 97, className: "oui-py-[10px]", children: (0, import_jsx_runtime5.jsx)(
        Select2.options,
        {
          options: options2,
          size: "xs",
          value: props.value,
          onValueChange: (value) => {
            props.onChange?.(value);
          }
        }
      ) });
    };
  }
});
var Option;
var DesktopHeader;
var Title;
var init_header_desktop = __esm2({
  "src/components/desktop/orderBook/header.desktop.tsx"() {
    init_orderContext();
    Option = (props) => {
      const { item, base, onClick } = props;
      const { t } = useTranslation();
      const [coinType, setCoinType] = useLocalStorage(
        ORDERBOOK_COIN_TYPE_KEY,
        base
      );
      const optimizedItem = (0, import_react8.useMemo)(() => {
        return optimizeSymbolDisplay(item);
      }, [item]);
      return (0, import_jsx_runtime5.jsxs)(
        Flex,
        {
          justify: "between",
          itemAlign: "center",
          className: M(
            "oui-w-full oui-px-2 oui-py-[3px]",
            "oui-cursor-pointer",
            "oui-text-xs",
            "oui-text-base-contrast-54",
            "hover:oui-bg-base-6",
            "oui-rounded-[3px]",
            "oui-transition-all",
            coinType === item && "oui-bg-base-5"
          ),
          onClick: (e) => {
            setCoinType(item);
            onClick(e);
          },
          children: [
            (0, import_jsx_runtime5.jsx)("div", { className: "oui-overflow-hidden oui-text-ellipsis ", children: `${t("common.total")}(${optimizedItem})` }),
            (0, import_jsx_runtime5.jsx)(
              "div",
              {
                className: M(
                  "oui-transition-all",
                  "oui-w-1",
                  "oui-h-1",
                  "oui-rounded-full",
                  "oui-bg-gradient-to-r",
                  coinType === item && "oui-from-[rgb(var(--oui-gradient-brand-start))] oui-to-[rgb(var(--oui-gradient-brand-end))]"
                )
              }
            )
          ]
        }
      );
    };
    DesktopHeader = (props) => {
      const { base, quote = "USDC" } = props;
      const { showTotal } = useOrderBookContext();
      const { t } = useTranslation();
      const [popoverOpen, setOpen] = import_react8.default.useState(false);
      const [coinType] = useLocalStorage(ORDERBOOK_COIN_TYPE_KEY, base);
      const TriggerIcon = popoverOpen ? CaretUpIcon : CaretDownIcon;
      const optimizedBase = (0, import_react8.useMemo)(() => {
        return optimizeSymbolDisplay(base);
      }, [base]);
      const optimizedCoinType = (0, import_react8.useMemo)(() => {
        return optimizeSymbolDisplay(coinType);
      }, [coinType]);
      return (0, import_jsx_runtime5.jsxs)(Flex, { pl: 3, justify: "between", className: "oui-py-[6px]", children: [
        (0, import_jsx_runtime5.jsxs)(
          Flex,
          {
            gap: 1,
            className: M("oui-basis-7/12", showTotal && "oui-basis-1/2"),
            children: [
              (0, import_jsx_runtime5.jsx)(Box, { width: "100%", children: (0, import_jsx_runtime5.jsx)(
                Title,
                {
                  id: "oui-order-book-header-price",
                  className: "oui-text-base-contrast-36",
                  children: `${t("common.price")}(${quote})`
                }
              ) }),
              (0, import_jsx_runtime5.jsx)(Box, { width: "100%", children: (0, import_jsx_runtime5.jsx)(
                Title,
                {
                  justifyEnd: true,
                  id: "oui-order-book-header-qty",
                  className: "oui-text-base-contrast-36",
                  children: `${t("common.qty")}(${optimizedBase})`
                }
              ) })
            ]
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          Flex,
          {
            gap: 1,
            pr: 3,
            className: M("oui-basis-5/12", showTotal && "oui-basis-1/2"),
            children: showTotal ? (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
              (0, import_jsx_runtime5.jsx)(Box, { className: "oui-text-base-contrast-36", width: "100%", children: (0, import_jsx_runtime5.jsx)(Title, { id: "oui-order-book-header-total-quote", justifyEnd: true, children: `${t("common.total")}(${optimizedBase})` }) }),
              (0, import_jsx_runtime5.jsx)(Box, { className: "oui-text-base-contrast-36", width: "100%", children: (0, import_jsx_runtime5.jsx)(Title, { justifyEnd: true, id: "oui-order-book-header-total-base", children: `${t("common.total")}(${quote})` }) })
            ] }) : (0, import_jsx_runtime5.jsx)(Box, { width: "100%", children: (0, import_jsx_runtime5.jsx)(
              Popover,
              {
                open: popoverOpen,
                onOpenChange: setOpen,
                contentProps: { className: M("oui-w-28 oui-p-1") },
                content: (0, import_jsx_runtime5.jsx)(
                  Flex,
                  {
                    direction: "column",
                    itemAlign: "start",
                    className: M("oui-w-full oui-gap-0.5"),
                    children: [base, quote].map((item) => {
                      return (0, import_jsx_runtime5.jsx)(
                        Option,
                        {
                          item,
                          base,
                          onClick: () => setOpen(false)
                        },
                        `type-${item}`
                      );
                    })
                  }
                ),
                children: (0, import_jsx_runtime5.jsxs)(
                  Flex,
                  {
                    justify: "end",
                    itemAlign: "center",
                    className: "oui-cursor-pointer oui-select-none oui-text-base-contrast-36 oui-transition-all hover:oui-text-base-contrast",
                    children: [
                      (0, import_jsx_runtime5.jsx)(Title, { justifyEnd: true, id: "oui-order-book-header-total-base", children: `${t("common.total")}(${optimizedCoinType})` }),
                      (0, import_jsx_runtime5.jsx)(
                        TriggerIcon,
                        {
                          color: "inherit",
                          className: "oui-size-4 oui-text-3xs"
                        }
                      )
                    ]
                  }
                )
              }
            ) })
          }
        )
      ] });
    };
    Title = (props) => {
      const { children, className, justifyEnd = false } = props;
      return (0, import_jsx_runtime5.jsx)(
        Flex,
        {
          id: props.id,
          className: M(
            className,
            "oui-items-end oui-text-xs",
            justifyEnd && "oui-justify-end"
          ),
          children
        }
      );
    };
  }
});
var MarkPriceView;
var DesktopLayout2;
var MobileLayout2;
var FlagIcon;
var init_markPrice = __esm2({
  "src/components/base/orderBook/markPrice.tsx"() {
    MarkPriceView = (props) => {
      const { isMobile } = useScreen();
      return isMobile ? (0, import_jsx_runtime5.jsx)(MobileLayout2, { ...props }) : (0, import_jsx_runtime5.jsx)(DesktopLayout2, { ...props });
    };
    DesktopLayout2 = (props) => {
      const { quote_dp, className, iconSize = 18 } = props;
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsx)(
        Tooltip,
        {
          content: t("trading.orderBook.markPrice.tooltip"),
          className: "oui-max-w-[270px]",
          children: (0, import_jsx_runtime5.jsxs)(
            Flex,
            {
              gap: 1,
              className: M("oui-cursor-pointer oui-text-2xs", className),
              children: [
                (0, import_jsx_runtime5.jsx)(FlagIcon, { size: iconSize }),
                (0, import_jsx_runtime5.jsx)(
                  Text2.numeral,
                  {
                    dp: quote_dp,
                    color: "warning",
                    className: "oui-underline oui-text-base oui-decoration-dashed oui-decoration-1 oui-underline-offset-4 oui-decoration-warning-darken",
                    children: props.markPrice
                  }
                )
              ]
            }
          )
        }
      );
    };
    MobileLayout2 = (props) => {
      const { quote_dp, className, iconSize = 18 } = props;
      const [open2, setOpen] = (0, import_react8.useState)(false);
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
        (0, import_jsx_runtime5.jsxs)(
          Flex,
          {
            gap: 1,
            className: M("oui-cursor-pointer oui-text-2xs", className),
            onClick: () => {
              setOpen(true);
            },
            children: [
              (0, import_jsx_runtime5.jsx)(FlagIcon, { size: iconSize }),
              (0, import_jsx_runtime5.jsx)(
                Text2.numeral,
                {
                  dp: quote_dp,
                  color: "warning",
                  className: "oui-underline oui-decoration-dashed oui-decoration-1 oui-underline-offset-4 oui-decoration-warning-darken",
                  children: props.markPrice
                }
              )
            ]
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          SimpleDialog,
          {
            size: "xs",
            open: open2,
            onOpenChange: setOpen,
            title: t("common.tips"),
            actions: {
              primary: {
                label: t("common.ok"),
                onClick: () => setOpen(false)
              }
            },
            children: (0, import_jsx_runtime5.jsx)(Text2, { children: t("trading.orderBook.markPrice.tooltip") })
          }
        )
      ] });
    };
    FlagIcon = (props) => {
      return (0, import_jsx_runtime5.jsx)(
        "svg",
        {
          width: props.size,
          height: props.size,
          viewBox: "0 0 18 18",
          fill: "currenColor",
          xmlns: "http://www.w3.org/2000/svg",
          className: "oui-fill-warning-darken",
          children: (0, import_jsx_runtime5.jsx)(
            "path",
            {
              fillRule: "evenodd",
              clipRule: "evenodd",
              d: "M3.75 1.5a.75.75 0 0 1 .75.75h6a.75.75 0 0 1 .75.75v1.5h3a.75.75 0 0 1 .75.75V12a.75.75 0 0 1-.75.75H7.5a.75.75 0 0 1-.75-.75v-1.5H4.5v5.25a.75.75 0 0 1-1.5 0V2.25a.75.75 0 0 1 .75-.75M4.5 9h5.25V3.75H4.5zm6.75-3v3.75a.75.75 0 0 1-.75.75H8.25v.75h5.25V6z"
            }
          )
        }
      );
    };
  }
});
var UNDERLINE_CLASS;
var MiddlePriceView;
var DesktopLayout3;
var MobileLayout3;
var init_midPriceView = __esm2({
  "src/components/base/orderBook/midPriceView.tsx"() {
    UNDERLINE_CLASS = "oui-cursor-pointer oui-underline oui-decoration-dashed oui-decoration-1 oui-underline-offset-4 oui-decoration-base-contrast oui-inline-flex";
    MiddlePriceView = (props) => {
      const { isMobile } = useScreen();
      return isMobile ? (0, import_jsx_runtime5.jsx)(MobileLayout3, { ...props }) : (0, import_jsx_runtime5.jsx)(DesktopLayout3, { ...props });
    };
    DesktopLayout3 = (props) => {
      const { lastPrice, quote_dp, className, iconSize = 18 } = props;
      const { t } = useTranslation();
      const [prevLastPrice, middlePrice] = lastPrice;
      const down = middlePrice < prevLastPrice;
      const up = middlePrice > prevLastPrice;
      return (0, import_jsx_runtime5.jsx)(
        Tooltip,
        {
          content: t("trading.orderBook.middlePrice.tooltip"),
          className: "oui-max-w-[240px]",
          children: (0, import_jsx_runtime5.jsx)("span", { className: UNDERLINE_CLASS, children: (0, import_jsx_runtime5.jsxs)(
            Flex,
            {
              gap: 1,
              className: M(
                up ? "oui-text-trade-profit" : down ? "oui-text-trade-loss" : "",
                className
              ),
              children: [
                (0, import_jsx_runtime5.jsx)(Text2.numeral, { dp: quote_dp, intensity: 98, children: middlePrice }),
                (0, import_jsx_runtime5.jsxs)(Box, { width: 19, children: [
                  down && (0, import_jsx_runtime5.jsx)(ArrowDownShortIcon, { size: iconSize, color: "danger", opacity: 1 }),
                  up && (0, import_jsx_runtime5.jsx)(ArrowUpShortIcon, { size: iconSize, color: "success", opacity: 1 })
                ] })
              ]
            }
          ) })
        }
      );
    };
    MobileLayout3 = (props) => {
      const { lastPrice, quote_dp, className, iconSize = 18 } = props;
      const [open2, setOpen] = (0, import_react8.useState)(false);
      const { t } = useTranslation();
      const [prevLastPrice, middlePrice] = lastPrice;
      const down = middlePrice < prevLastPrice;
      const up = middlePrice > prevLastPrice;
      const handleTriggerClick = () => setOpen(true);
      const handleKeyDown = (e) => {
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          setOpen(true);
        }
      };
      return (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
        (0, import_jsx_runtime5.jsx)(
          "span",
          {
            role: "button",
            tabIndex: 0,
            className: UNDERLINE_CLASS,
            onClick: handleTriggerClick,
            onKeyDown: handleKeyDown,
            "aria-label": t("trading.orderBook.middlePrice.tooltip"),
            children: (0, import_jsx_runtime5.jsxs)(
              Flex,
              {
                gap: 1,
                className: M(
                  up ? "oui-text-trade-profit" : down ? "oui-text-trade-loss" : "",
                  className
                ),
                children: [
                  (0, import_jsx_runtime5.jsx)(Text2.numeral, { dp: quote_dp, intensity: 98, children: middlePrice }),
                  (0, import_jsx_runtime5.jsxs)(Box, { width: 19, children: [
                    down && (0, import_jsx_runtime5.jsx)(ArrowDownShortIcon, { size: iconSize, color: "danger", opacity: 1 }),
                    up && (0, import_jsx_runtime5.jsx)(ArrowUpShortIcon, { size: iconSize, color: "success", opacity: 1 })
                  ] })
                ]
              }
            )
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          SimpleDialog,
          {
            size: "xs",
            open: open2,
            onOpenChange: setOpen,
            title: t("common.tips"),
            actions: {
              primary: {
                label: t("common.ok"),
                onClick: () => setOpen(false)
              }
            },
            children: (0, import_jsx_runtime5.jsx)(Text2, { children: t("trading.orderBook.middlePrice.tooltip") })
          }
        )
      ] });
    };
  }
});
var DesktopMarkPrice;
var Spread;
var init_markPrice_desktop = __esm2({
  "src/components/desktop/orderBook/markPrice.desktop.tsx"() {
    init_markPrice();
    init_midPriceView();
    init_orderContext();
    DesktopMarkPrice = (props) => {
      const { markPrice = 0, lastPrice, asks, bids, symbolInfo } = props;
      const { showTotal } = useOrderBookContext();
      return (0, import_jsx_runtime5.jsxs)("div", { className: "oui-flex oui-flex-row oui-pl-3 oui-tabular-nums oui-justify-between oui-text-base-contrast-80 oui-text-xs oui-relative oui-cursor-pointer oui-py-[6px]", children: [
        (0, import_jsx_runtime5.jsxs)(
          "div",
          {
            className: M(
              "oui-basis-7/12 oui-flex oui-flex-row oui-items-center oui-mr-2 oui-justify-between",
              showTotal && "oui-basis-5/12"
            ),
            children: [
              (0, import_jsx_runtime5.jsx)(
                MiddlePriceView,
                {
                  markPrice,
                  lastPrice,
                  quote_dp: symbolInfo.quote_dp,
                  className: "oui-text-base"
                }
              ),
              (0, import_jsx_runtime5.jsx)(MarkPriceView, { markPrice, quote_dp: symbolInfo.quote_dp })
            ]
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          "div",
          {
            className: M(
              "oui-basis-5/12 oui-flex oui-items-center oui-fex-row oui-overflow-hidden oui-relative oui-justify-end",
              showTotal && "oui-basis-7/12",
              "oui-pr-3"
            ),
            children: (0, import_jsx_runtime5.jsx)(Spread, { asks, bids })
          }
        )
      ] });
    };
    Spread = (props) => {
      const { asks, bids } = props;
      const { t } = useTranslation();
      const spread = (0, import_react8.useMemo)(() => {
        if (bids.length === 0 && asks.length === 0) {
          return 0;
        }
        const bid1 = Number.isNaN(bids[0][0]) ? 0 : bids[0][0];
        const index = asks.reverse().findIndex((item) => !Number.isNaN(item[0]));
        let ask1 = 0;
        if (index !== -1) {
          ask1 = Number.isNaN(asks[index][0]) ? 0 : asks[index][0];
        }
        const dValue = new decimal_default(ask1).sub(bid1).div(new decimal_default(ask1).add(bid1).div(2));
        return Math.ceil(dValue.toNumber() * 1e6 + 0.1) / 1e4;
      }, [asks, bids]);
      return (0, import_jsx_runtime5.jsx)("div", { children: (0, import_jsx_runtime5.jsx)(
        Tooltip,
        {
          content: t("trading.orderBook.spreadRatio.tooltip"),
          className: "oui-max-w-[240px]",
          children: (0, import_jsx_runtime5.jsx)(
            Text2,
            {
              size: "2xs",
              intensity: 36,
              className: "oui-cursor-pointer oui-underline oui-decoration-dashed oui-decoration-1 oui-underline-offset-4 oui-decoration-base-contrast-36",
              children: `${spread}%`
            }
          )
        }
      ) });
    };
  }
});
var DesktopOrderBook;
var init_index_desktop = __esm2({
  "src/components/desktop/orderBook/index.desktop.tsx"() {
    init_orderBook2();
    init_orderContext();
    init_asks_desktop();
    init_bids_desktop();
    init_buySellRatio();
    init_depthSelect_desktop();
    init_header_desktop();
    init_markPrice_desktop();
    DesktopOrderBook = (props) => {
      const {
        lastPrice,
        markPrice,
        quote,
        base,
        isLoading,
        onDepthChange,
        showBuySellRatio = true,
        setShowBuySellRatio,
        buySellRatio
      } = props;
      const { t } = useTranslation();
      const divRef = (0, import_react8.useRef)(null);
      const [showTotal, setShowTotal] = (0, import_react8.useState)(false);
      const [coinType, setCoinType] = useLocalStorage(
        ORDERBOOK_COIN_TYPE_KEY,
        base
      );
      (0, import_react8.useEffect)(() => {
        if (coinType !== quote && base) {
          setCoinType(base);
        }
      }, [base, quote]);
      (0, import_react8.useEffect)(() => {
        const resizeObserver = new ResizeObserver((entries) => {
          for (const entry of entries) {
            const { inlineSize: width } = entry.borderBoxSize[0];
            setShowTotal(width >= 360);
          }
        });
        const targetDiv = divRef.current;
        if (targetDiv) {
          resizeObserver.observe(targetDiv);
        }
        return () => {
          if (targetDiv) {
            resizeObserver.unobserve(targetDiv);
          }
        };
      }, []);
      return (0, import_jsx_runtime5.jsx)(
        OrderBookProvider,
        {
          cellHeight: props.cellHeight ?? 20,
          onItemClick: props.onItemClick,
          depth: props.activeDepth,
          showTotal,
          pendingOrders: props.pendingOrders || EMPTY_LIST,
          symbolInfo: props.symbolInfo,
          showBuySellRatio,
          onShowBuySellRatioChange: setShowBuySellRatio,
          children: (0, import_jsx_runtime5.jsxs)(
            Grid2,
            {
              cols: 1,
              rows: showBuySellRatio ? 6 : 5,
              id: "oui-orderbook-desktop",
              ref: divRef,
              className: M(
                "oui-relative oui-size-full",
                showBuySellRatio ? "oui-grid-rows-[auto,auto,1fr,auto,1fr,auto]" : "oui-grid-rows-[auto,auto,1fr,auto,1fr]"
              ),
              children: [
                (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", itemAlign: "center", className: "oui-pr-3", children: [
                  (0, import_jsx_runtime5.jsx)(
                    DesktopDepthSelect,
                    {
                      depths: props.depths,
                      value: props.activeDepth,
                      onChange: onDepthChange
                    }
                  ),
                  (0, import_jsx_runtime5.jsx)(
                    BuySellRatioSettings,
                    {
                      showBuySellRatio,
                      setShowBuySellRatio
                    }
                  )
                ] }),
                (0, import_jsx_runtime5.jsx)(DesktopHeader, { quote, base }),
                (0, import_jsx_runtime5.jsx)(DesktopAsks, { data: [...props.asks] }),
                (0, import_jsx_runtime5.jsx)(
                  DesktopMarkPrice,
                  {
                    lastPrice,
                    markPrice,
                    asks: [...props.asks],
                    bids: [...props.bids],
                    symbolInfo: props.symbolInfo
                  }
                ),
                (0, import_jsx_runtime5.jsx)(DesktopBids, { data: [...props.bids] }),
                showBuySellRatio && (0, import_jsx_runtime5.jsx)(
                  BuySellRatioBar,
                  {
                    ratio: buySellRatio || null,
                    className: "oui-text-2xs oui-px-3 oui-h-[38px]"
                  }
                ),
                isLoading && (0, import_jsx_runtime5.jsx)("div", { className: "oui-bg-bg-8/70 oui-absolute oui-inset-0 oui-z-10 oui-flex oui-items-center oui-justify-center", children: (0, import_jsx_runtime5.jsx)(Spinner, {}) })
              ]
            }
          )
        }
      );
    };
  }
});
var FundingRate;
var init_fundingRate_ui = __esm2({
  "src/components/mobile/fundingRate/fundingRate.ui.tsx"() {
    FundingRate = (props) => {
      const { data, onClick } = props;
      const predFundingRate = data.est_funding_rate;
      const countDown = data.countDown;
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", itemAlign: "start", pb: 2, children: [
        (0, import_jsx_runtime5.jsx)(
          Text2,
          {
            size: "2xs",
            intensity: 36,
            className: "oui-cursor-pointer oui-underline oui-decoration-line-16 oui-decoration-dashed oui-underline-offset-4",
            onClick,
            children: t("trading.fundingRate.predFundingRate")
          }
        ),
        predFundingRate === null ? "--" : (0, import_jsx_runtime5.jsxs)("div", { className: "orderly-flex orderly-gap-1 oui-text-2xs oui-text-base-contrast-36", children: [
          (0, import_jsx_runtime5.jsx)(Text2.numeral, { suffix: "%", dp: 4, intensity: 80, children: predFundingRate ?? "--" }),
          (0, import_jsx_runtime5.jsx)("span", { children: " in" }),
          (0, import_jsx_runtime5.jsx)("span", { children: " " + countDown })
        ] })
      ] });
    };
  }
});
var TIMEFRAME_CONFIG;
var useFundingRateModalScript;
var init_fundingRateModal_script = __esm2({
  "src/components/mobile/fundingRateModal/fundingRateModal.script.tsx"() {
    TIMEFRAME_CONFIG = [
      { key: "1H", hours: 1 },
      { key: "4H", hours: 4 },
      { key: "1D", hours: 24 },
      { key: "7D", hours: 168 },
      { key: "30D", hours: 720 },
      { key: "1Y", hours: 8760 }
    ];
    useFundingRateModalScript = (options2) => {
      const { data, isLoading } = useFundingDetails(options2.symbol);
      const {
        last_funding_rate,
        est_funding_rate,
        next_funding_time,
        last_funding_rate_timestamp
      } = useFundingRateBySymbol(options2.symbol) ?? {};
      const [{ aggregated, rows }] = usePositionStream(options2.symbol);
      const { notional } = aggregated ?? {};
      const fundingPeriod = (0, import_react8.useMemo)(() => {
        if (!data || isLoading) {
          return "-";
        }
        return `${data.funding_period}h`;
      }, [data, isLoading]);
      const capFunding = (0, import_react8.useMemo)(() => {
        if (!data || isLoading) {
          return "-";
        }
        return `${new decimal_default(data.cap_funding).mul(100).toNumber()}%`;
      }, [data, isLoading]);
      const floorFunding = (0, import_react8.useMemo)(() => {
        if (!data || isLoading) {
          return "-";
        }
        return `${new decimal_default(data.floor_funding).mul(100).toNumber()}%`;
      }, [data, isLoading]);
      const lastFundingRate = (0, import_react8.useMemo)(() => {
        if (!last_funding_rate) {
          return void 0;
        }
        return `${new decimal_default(last_funding_rate).mul(100).toNumber()}%`;
      }, [last_funding_rate]);
      const estFundingFee = (0, import_react8.useMemo)(() => {
        if (!est_funding_rate || !notional || rows.length === 0) {
          return "--";
        }
        return `${new decimal_default(est_funding_rate).mul(notional).todp(4).toNumber()}`;
      }, [est_funding_rate, notional, rows]);
      const calculateAnnualizedRate = (rate, intervalHours) => {
        if (!rate || !intervalHours) return void 0;
        const annualizedRate = new decimal_default(rate).mul(24).div(intervalHours).mul(365);
        return annualizedRate.todp(2, decimal_default.ROUND_DOWN).toNumber();
      };
      const lastFundingRateAnnualized = (0, import_react8.useMemo)(() => {
        if (!last_funding_rate || !data?.funding_period) {
          return void 0;
        }
        const rate = new decimal_default(last_funding_rate).mul(100).toNumber();
        const annualized = calculateAnnualizedRate(rate, data.funding_period);
        return annualized ? `${annualized}%` : void 0;
      }, [last_funding_rate, data?.funding_period]);
      const settlementIntervalHours = (0, import_react8.useMemo)(() => {
        if (!next_funding_time || !last_funding_rate_timestamp) return 8;
        const diff = next_funding_time - last_funding_rate_timestamp;
        return diff / 36e5;
      }, [next_funding_time, last_funding_rate_timestamp]);
      const estFundingRateByTimeframe = (0, import_react8.useMemo)(() => {
        if (est_funding_rate === void 0 || est_funding_rate === null || settlementIntervalHours <= 0) {
          return [];
        }
        return TIMEFRAME_CONFIG.map(({ key: key2, hours }) => {
          const rate = new decimal_default(est_funding_rate).mul(hours).div(settlementIntervalHours).mul(100);
          const str = rate.toFixed(5);
          const trimmed = str.replace(/\.?0+$/, "") || "0";
          return { timeframe: key2, value: `${trimmed}%` };
        });
      }, [est_funding_rate, settlementIntervalHours]);
      return {
        fundingPeriod,
        capFunding,
        floorFunding,
        lastFundingRate,
        estFundingFee,
        lastFundingRateAnnualized,
        estFundingRateByTimeframe
      };
    };
  }
});
var FundingRateModal;
var init_fundingRateModal_ui = __esm2({
  "src/components/mobile/fundingRateModal/fundingRateModal.ui.tsx"() {
    FundingRateModal = (props) => {
      const { t } = useTranslation();
      const {
        fundingPeriod,
        capFunding,
        floorFunding,
        lastFundingRate,
        estFundingFee,
        lastFundingRateAnnualized,
        estFundingRateByTimeframe
      } = props;
      const renderRow = (label, value, annualizedValue) => {
        if (!value) {
          return null;
        }
        return (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", itemAlign: "center", width: "100%", children: [
          (0, import_jsx_runtime5.jsx)(Text2, { intensity: 54, children: annualizedValue ? `${label}/ ${t("trading.fundingRate.annualized")}` : label }),
          (0, import_jsx_runtime5.jsxs)(Flex, { itemAlign: "end", gap: 1, className: "oui-text-base-contrast-80", children: [
            (0, import_jsx_runtime5.jsx)(Text2, { intensity: 80, children: value }),
            annualizedValue && " / ",
            annualizedValue && annualizedValue
          ] })
        ] });
      };
      return (0, import_jsx_runtime5.jsxs)(Flex, { width: "100%", itemAlign: "center", direction: "column", gap: 1, children: [
        (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", itemAlign: "center", width: "100%", children: [
          (0, import_jsx_runtime5.jsx)(Text2, { intensity: 54, children: t("trading.fundingRate.predFundingRate.interval") }),
          (0, import_jsx_runtime5.jsx)(Text2, { intensity: 80, children: fundingPeriod })
        ] }),
        (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", itemAlign: "center", width: "100%", children: [
          (0, import_jsx_runtime5.jsxs)(Text2, { intensity: 54, children: [
            t("trading.fundingRate.predFundingRate.cap"),
            " /",
            t("trading.fundingRate.predFundingRate.floor")
          ] }),
          (0, import_jsx_runtime5.jsxs)(Text2, { intensity: 80, children: [
            capFunding,
            " / ",
            floorFunding
          ] })
        ] }),
        renderRow(
          t("trading.fundingRate.lastFundingRate"),
          lastFundingRate,
          lastFundingRateAnnualized
        ),
        estFundingRateByTimeframe.map(
          ({ timeframe, value }) => renderRow(
            t("trading.fundingRate.estFundingRateWithTimeframe", {
              timeframe
            }),
            value
          )
        ),
        renderRow(t("trading.fundingRate.estimatedFundingFee"), estFundingFee),
        (0, import_jsx_runtime5.jsx)(Divider, { my: 4, className: "oui-w-full", intensity: 8 }),
        t("markets.symbolInfoBar.predFundingRate.tooltip")
      ] });
    };
  }
});
var FundingRateModalWidget;
var FundingRateDialogId;
var FundingRateSheetId;
var init_fundingRateModal_widget = __esm2({
  "src/components/mobile/fundingRateModal/fundingRateModal.widget.tsx"() {
    init_fundingRateModal_script();
    init_fundingRateModal_ui();
    FundingRateModalWidget = (props) => {
      const state = useFundingRateModalScript(props);
      return (0, import_jsx_runtime5.jsx)(FundingRateModal, { ...state });
    };
    FundingRateDialogId = "FundingRateDialogId";
    FundingRateSheetId = "FundingRateSheetId";
    registerSimpleDialog(FundingRateDialogId, FundingRateModalWidget, {
      size: "md",
      classNames: { content: "oui-border oui-border-line-6" },
      title: () => i18n_default.t("funding.fundingRate")
    });
    registerSimpleSheet(FundingRateSheetId, FundingRateModalWidget, {
      classNames: { content: "oui-text-sm" },
      title: () => i18n_default.t("funding.fundingRate")
    });
  }
});
var useFundingRateScript;
var init_fundingRate_script = __esm2({
  "src/components/mobile/fundingRate/fundingRate.script.tsx"() {
    init_fundingRateModal_widget();
    useFundingRateScript = (symbol) => {
      const data = useFundingRate(symbol);
      const onClick = (0, import_react8.useCallback)(() => {
        modal.show(FundingRateSheetId, { symbol });
      }, [symbol]);
      return {
        data,
        onClick
      };
    };
  }
});
var FundingRateWidget;
var init_fundingRate_widget = __esm2({
  "src/components/mobile/fundingRate/fundingRate.widget.tsx"() {
    init_fundingRate_script();
    init_fundingRate_ui();
    FundingRateWidget = (props) => {
      const state = useFundingRateScript(props.symbol);
      return (0, import_jsx_runtime5.jsx)(FundingRate, { ...state });
    };
  }
});
var init_fundingRate = __esm2({
  "src/components/mobile/fundingRate/index.ts"() {
    init_fundingRate_ui();
    init_fundingRate_widget();
    init_fundingRate_script();
  }
});
var OrderBookCell;
var init_cell = __esm2({
  "src/components/mobile/orderBook/cell.tsx"() {
    init_cellBar();
    init_orderContext();
    init_types();
    OrderBookCell = (props) => {
      const { accumulated, count, quantity, price, type, accumulatedAmount } = props;
      const width = accumulated / count * 100;
      const { cellHeight, onItemClick, depth, symbolInfo } = (0, import_react8.useContext)(OrderBookContext);
      const { base_dp, quote_dp, base, quote } = symbolInfo;
      const [coinUnit, setCoinUnit] = useLocalStorage(
        ORDERBOOK_MOBILE_COIN_TYPE_KEY,
        "qty"
      );
      const totalAmount = Number.isNaN(accumulated) ? "-" : accumulatedAmount?.toString();
      const dp = (0, import_react8.useMemo)(() => {
        return getPrecisionByNumber(depth || `${quote_dp}`);
      }, [depth, quote_dp]);
      const renderCell = () => {
        switch (coinUnit) {
          case "base":
            return (0, import_jsx_runtime5.jsx)(Text2.numeral, { className: "oui-text-base-contrast-80", dp: base_dp, children: accumulated });
          case "quote":
            return (0, import_jsx_runtime5.jsx)(Text2.numeral, { className: "oui-text-base-contrast-80", dp: 0, children: totalAmount });
          default:
            return (0, import_jsx_runtime5.jsx)(Text2.numeral, { className: "oui-text-base-contrast-80", dp: base_dp, children: Number.isNaN(quantity) ? "-" : quantity });
        }
      };
      return (0, import_jsx_runtime5.jsxs)(
        Box,
        {
          className: M(
            "oui-relative oui-w-full oui-cursor-pointer oui-overflow-hidden oui-text-2xs oui-tabular-nums"
          ),
          style: { height: `${cellHeight}px` },
          onClick: () => {
            if (Number.isNaN(price) || Number.isNaN(quantity)) {
              return;
            }
            onItemClick?.([price, quantity]);
          },
          children: [
            (0, import_jsx_runtime5.jsxs)(Flex, { itemAlign: "center", justify: "between", children: [
              (0, import_jsx_runtime5.jsx)(
                Text2.numeral,
                {
                  color: type === "bid" ? "buy" : "sell",
                  dp,
                  children: price
                }
              ),
              renderCell()
            ] }),
            Number.isNaN(width) ? null : (0, import_jsx_runtime5.jsx)(
              CellBar,
              {
                width,
                className: M(
                  type === "ask" ? "oui-bg-trade-loss/20" : "oui-bg-trade-profit/20"
                )
              }
            )
          ]
        }
      );
    };
  }
});
var ListBox;
var init_listBox = __esm2({
  "src/components/mobile/orderBook/listBox.tsx"() {
    init_orderContext();
    init_cell();
    ListBox = (props) => {
      const { data } = props;
      const { mode } = useOrderBookContext();
      return (0, import_jsx_runtime5.jsx)(Box, { className: "oui-order-book-list oui-flex oui-w-full oui-flex-col oui-gap-px", children: data.map((item, index) => {
        return (0, import_jsx_runtime5.jsx)(
          OrderBookCell,
          {
            background: "",
            price: item[0],
            quantity: item[1],
            accumulated: item[2],
            accumulatedAmount: item[3],
            count: props.countQty,
            type: props.type,
            mode
          },
          `item-${index}`
        );
      }) });
    };
  }
});
var Asks;
var init_asks = __esm2({
  "src/components/mobile/orderBook/asks.tsx"() {
    init_types();
    init_listBox();
    Asks = (props) => {
      const { data } = props;
      const countQty = (0, import_react8.useMemo)(() => {
        const len = data.length;
        let max = Number.NaN;
        let index = 0;
        while (Number.isNaN(max) && index < len) {
          max = data[index][2];
          index++;
        }
        return max;
      }, [data]);
      return (0, import_jsx_runtime5.jsx)(ListBox, { type: "ask", data, countQty });
    };
  }
});
var Bids;
var init_bids = __esm2({
  "src/components/mobile/orderBook/bids.tsx"() {
    init_types();
    init_listBox();
    Bids = (props) => {
      const { data } = props;
      const countQty = (0, import_react8.useMemo)(() => {
        let max = Number.NaN;
        let index = data.length - 1;
        while (Number.isNaN(max) && index > 0) {
          max = data[index][2];
          index--;
        }
        return max;
      }, [data]);
      return (0, import_jsx_runtime5.jsx)(ListBox, { type: "bid", data, countQty });
    };
  }
});
var DepthSelect;
var init_depthSelect = __esm2({
  "src/components/mobile/orderBook/depthSelect.tsx"() {
    DepthSelect = (props) => {
      const options2 = (0, import_react8.useMemo)(() => {
        return props.depth.map((d) => ({
          value: d,
          label: `${d}`
        }));
      }, [props.depth]);
      return (0, import_jsx_runtime5.jsx)(Box, { id: "oui-order-book-depth", className: "oui-w-full oui-pt-2", children: (0, import_jsx_runtime5.jsx)(
        Picker,
        {
          options: options2,
          fullWidth: true,
          size: "sm",
          value: props.value,
          className: "oui-text-2xs oui-text-base-contrast-54",
          onValueChange: (value) => {
            props.onChange?.(value);
          }
        }
      ) });
    };
  }
});
var Header2;
var init_header = __esm2({
  "src/components/mobile/orderBook/header.tsx"() {
    init_orderContext();
    Header2 = (props) => {
      const { t } = useTranslation();
      const { base, quote } = props;
      const [coinUnit, setCoinUnit] = useLocalStorage(
        ORDERBOOK_MOBILE_COIN_TYPE_KEY,
        "qty"
      );
      const options2 = (0, import_react8.useMemo)(() => {
        return [
          {
            value: "qty",
            label: `${t("common.quantity")}(${base})`,
            data: [t("common.quantity"), base]
          },
          {
            value: "base",
            label: `${t("common.total")}(${base})`,
            data: [t("common.total"), base]
          },
          {
            value: "quote",
            label: `${t("common.total")}(${quote})`,
            data: [t("common.total"), quote]
          }
        ];
      }, [t, base, quote]);
      return (0, import_jsx_runtime5.jsxs)(
        Flex,
        {
          justify: "between",
          width: "100%",
          className: "oui-py-[5px] oui-text-2xs oui-text-base-contrast-36",
          children: [
            (0, import_jsx_runtime5.jsxs)(
              Flex,
              {
                direction: "column",
                itemAlign: "start",
                id: "oui-order-book-header-price",
                children: [
                  (0, import_jsx_runtime5.jsx)(Text2, { children: t("common.price") }),
                  (0, import_jsx_runtime5.jsx)(Text2, { children: `(${quote})` })
                ]
              }
            ),
            (0, import_jsx_runtime5.jsx)(
              Picker,
              {
                size: "sm",
                value: coinUnit,
                onValueChange: setCoinUnit,
                options: options2,
                valueRenderer: (_, { open: open2, data }) => {
                  return (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", itemAlign: "center", gap: 1, children: [
                    Array.isArray(data) && (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", itemAlign: "end", children: [
                      data[0] && (0, import_jsx_runtime5.jsx)(Text2, { children: data[0] }),
                      data[1] && (0, import_jsx_runtime5.jsxs)(Text2, { children: [
                        "(",
                        data[1],
                        ")"
                      ] })
                    ] }),
                    open2 ? (0, import_jsx_runtime5.jsx)(CaretUpIcon, { size: 14, color: "inherit" }) : (0, import_jsx_runtime5.jsx)(CaretDownIcon, { size: 14, color: "inherit" })
                  ] });
                }
              }
            )
          ]
        }
      );
    };
  }
});
var MarkPrice;
var init_markPrice2 = __esm2({
  "src/components/mobile/orderBook/markPrice.tsx"() {
    init_markPrice();
    init_midPriceView();
    init_orderContext();
    MarkPrice = (props) => {
      const { symbolInfo } = useOrderBookContext();
      const { quote_dp } = symbolInfo;
      return (0, import_jsx_runtime5.jsxs)(
        Flex,
        {
          id: "oui-order-book-mark-price",
          className: "oui-py-[6px]",
          width: "100%",
          justify: "between",
          children: [
            (0, import_jsx_runtime5.jsx)(
              MiddlePriceView,
              {
                markPrice: props.markPrice,
                lastPrice: props.lastPrice,
                quote_dp,
                className: "oui-text-sm",
                iconSize: 14
              }
            ),
            (0, import_jsx_runtime5.jsx)(
              MarkPriceView,
              {
                markPrice: props.markPrice,
                quote_dp,
                className: "oui-text-2xs",
                iconSize: 12
              }
            )
          ]
        }
      );
    };
  }
});
var OrderBook;
var init_orderBook = __esm2({
  "src/components/mobile/orderBook/index.tsx"() {
    init_orderBook2();
    init_orderContext();
    init_buySellRatio();
    init_fundingRate();
    init_asks();
    init_bids();
    init_depthSelect();
    init_header();
    init_markPrice2();
    OrderBook = (props) => {
      const {
        lastPrice,
        markPrice,
        quote,
        base,
        isLoading,
        onDepthChange,
        showBuySellRatio = true,
        setShowBuySellRatio,
        buySellRatio
      } = props;
      const { t } = useTranslation();
      const [settingsOpen, setSettingsOpen] = (0, import_react8.useState)(false);
      const symbol = `PERP_${props.symbolInfo.base}_${props.symbolInfo.quote}`;
      const [coinUnit, setCoinUnit] = useLocalStorage(
        ORDERBOOK_MOBILE_COIN_TYPE_KEY,
        "qty"
      );
      return (0, import_jsx_runtime5.jsx)(
        OrderBookProvider,
        {
          cellHeight: props.cellHeight ?? 20,
          onItemClick: props.onItemClick,
          depth: props.activeDepth,
          pendingOrders: EMPTY_LIST,
          showTotal: false,
          symbolInfo: props.symbolInfo,
          showBuySellRatio,
          onShowBuySellRatioChange: setShowBuySellRatio,
          children: (0, import_jsx_runtime5.jsxs)(
            Flex,
            {
              direction: "column",
              p: 2,
              id: "oui-orderbook-mobile",
              className: M("oui-relative oui-size-full", props.className),
              justify: "start",
              itemAlign: "start",
              children: [
                (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", itemAlign: "center", className: "oui-w-full", children: [
                  (0, import_jsx_runtime5.jsx)(FundingRateWidget, { symbol }),
                  (0, import_jsx_runtime5.jsx)(
                    BuySellRatioSettings,
                    {
                      showBuySellRatio,
                      setShowBuySellRatio
                    }
                  )
                ] }),
                (0, import_jsx_runtime5.jsx)(Header2, { quote, base }),
                (0, import_jsx_runtime5.jsx)(Asks, { data: props.asks }),
                (0, import_jsx_runtime5.jsx)(MarkPrice, { lastPrice, markPrice }),
                (0, import_jsx_runtime5.jsx)(Bids, { data: props.bids }),
                (0, import_jsx_runtime5.jsx)(
                  DepthSelect,
                  {
                    depth: props.depths || EMPTY_LIST,
                    value: props.activeDepth,
                    onChange: onDepthChange
                  }
                ),
                showBuySellRatio && (0, import_jsx_runtime5.jsx)(
                  BuySellRatioBar,
                  {
                    ratio: buySellRatio || null,
                    className: "oui-px-0 oui-text-[8px] oui-h-6 oui-mt-2"
                  }
                ),
                isLoading && (0, import_jsx_runtime5.jsx)("div", { className: "oui-bg-base-800/70 oui-absolute oui-inset-0 oui-z-10 oui-flex oui-h-full oui-min-h-[420px] oui-items-center oui-justify-center", children: (0, import_jsx_runtime5.jsx)(Spinner, {}) })
              ]
            }
          )
        }
      );
    };
  }
});
var OrderBook2;
var init_orderBook_ui = __esm2({
  "src/components/base/orderBook/orderBook.ui.tsx"() {
    init_index_desktop();
    init_orderBook();
    OrderBook2 = (props) => {
      return (0, import_jsx_runtime5.jsx)(Box, { className: "oui-font-semibold", width: "100%", height: "100%", children: props.isMobile ? (0, import_jsx_runtime5.jsx)(
        OrderBook,
        {
          level: props.level,
          asks: props.asks,
          bids: props.bids,
          markPrice: props.markPrice,
          lastPrice: props.lastPrice,
          depths: props.depths,
          activeDepth: props.selDepth,
          base: props.base,
          quote: props.quote,
          isLoading: props.isLoading,
          onItemClick: props.onItemClick,
          cellHeight: props.cellHeight,
          onDepthChange: props.onDepthChange,
          className: props.className,
          symbolInfo: props.symbolInfo,
          showBuySellRatio: props.showBuySellRatio,
          setShowBuySellRatio: props.setShowBuySellRatio,
          buySellRatio: props.buySellRatio
        }
      ) : (0, import_jsx_runtime5.jsx)(
        DesktopOrderBook,
        {
          level: props.level,
          asks: props.asks,
          bids: props.bids,
          markPrice: props.markPrice,
          lastPrice: props.lastPrice,
          depths: props.depths,
          activeDepth: props.selDepth,
          base: props.base,
          quote: props.quote,
          isLoading: props.isLoading,
          onItemClick: props.onItemClick,
          cellHeight: props.cellHeight,
          onDepthChange: props.onDepthChange,
          className: props.className,
          pendingOrders: props.pendingOrders,
          symbolInfo: props.symbolInfo,
          showBuySellRatio: props.showBuySellRatio,
          setShowBuySellRatio: props.setShowBuySellRatio,
          buySellRatio: props.buySellRatio
        }
      ) });
    };
  }
});
var getBasicSymbolInfo;
var init_utils = __esm2({
  "src/utils/utils.ts"() {
    getBasicSymbolInfo = (symbolInfo) => {
      return {
        base_dp: symbolInfo("base_dp"),
        quote_dp: symbolInfo("quote_dp"),
        base_tick: symbolInfo("base_tick"),
        base: symbolInfo("base"),
        quote: symbolInfo("quote")
      };
    };
  }
});
var DEFAULT_CELL_HEIGHT;
var SPACE;
var BUY_SELL_RATIO_BAR_HEIGHT_MOBILE;
var BUY_SELL_RATIO_BAR_HEIGHT_DESKTOP;
var ORDERBOOK_SHOW_BUY_SELL_RATIO_KEY;
var useOrderBookScript;
var usePendingOrderStream;
var init_orderBook_script = __esm2({
  "src/components/base/orderBook/orderBook.script.tsx"() {
    init_utils();
    DEFAULT_CELL_HEIGHT = 20;
    SPACE = 104;
    BUY_SELL_RATIO_BAR_HEIGHT_MOBILE = 28;
    BUY_SELL_RATIO_BAR_HEIGHT_DESKTOP = 38;
    ORDERBOOK_SHOW_BUY_SELL_RATIO_KEY = "orderbook_show_buy_sell_ratio";
    useOrderBookScript = (props) => {
      const { symbol, height } = props;
      const symbolInfo = useSymbolsInfo()[symbol];
      const [cellHeight, setCellHeight] = (0, import_react8.useState)(DEFAULT_CELL_HEIGHT);
      const [level, setLevel] = (0, import_react8.useState)(10);
      const { base, quote, quote_dp } = getBasicSymbolInfo(symbolInfo);
      const [data, { onDepthChange, isLoading, onItemClick, depth, allDepths }] = useOrderbookStream(symbol, void 0, { level });
      const pendingOrders = usePendingOrderStream(symbol);
      const { isMobile } = useScreen();
      const [showBuySellRatio, setShowBuySellRatio] = useLocalStorage(
        ORDERBOOK_SHOW_BUY_SELL_RATIO_KEY,
        true
      );
      (0, import_react8.useEffect)(() => {
        if (height) {
          const availableSpace = SPACE + (showBuySellRatio ? isMobile ? BUY_SELL_RATIO_BAR_HEIGHT_MOBILE : BUY_SELL_RATIO_BAR_HEIGHT_DESKTOP : 0);
          const level2 = Math.floor(
            (height - availableSpace) / ((DEFAULT_CELL_HEIGHT + 1) * 2)
          );
          const cellsHeight = (DEFAULT_CELL_HEIGHT + 1) * 2 * level2;
          const restSpace = height - availableSpace - cellsHeight;
          if (restSpace > 10) {
            setCellHeight(DEFAULT_CELL_HEIGHT + restSpace / level2 / 2);
          } else {
            setCellHeight(DEFAULT_CELL_HEIGHT);
          }
          setLevel(level2);
        }
      }, [height, showBuySellRatio]);
      const selDepth = (0, import_react8.useMemo)(() => {
        if (typeof depth === "undefined" || typeof quote_dp === "undefined") {
          return void 0;
        }
        return removeTrailingZeros(depth);
      }, [depth, quote_dp, symbol]);
      const depths = (0, import_react8.useMemo)(() => {
        return allDepths?.map((e) => removeTrailingZeros(e)) || [];
      }, [allDepths, quote_dp]);
      const buySellRatio = (0, import_react8.useMemo)(() => {
        if (!data?.asks || !data?.bids || level === void 0) {
          return null;
        }
        const isValidNumber = (value) => {
          return typeof value === "number" && !Number.isNaN(value) && Number.isFinite(value) && value >= 0;
        };
        const visibleAsks = data.asks.slice(0, level);
        const visibleBids = data.bids.slice(-level);
        if (visibleAsks.length > 0 && visibleBids.length > 0) {
          const firstAsk = visibleAsks.find(
            (ask) => Array.isArray(ask) && ask.length === 4 && ask[3] !== void 0 && ask[3] !== null && isValidNumber(ask[3])
          );
          const lastBid = [...visibleBids].reverse().find(
            (bid) => Array.isArray(bid) && bid.length === 4 && bid[3] !== void 0 && bid[3] !== null && isValidNumber(bid[3])
          );
          if (firstAsk && lastBid) {
            const askAmount = new decimal_default(firstAsk[3]);
            const bidAmount = new decimal_default(lastBid[3]);
            const totalAmount = askAmount.add(bidAmount);
            const buyPercentage = bidAmount.div(totalAmount).mul(100).toNumber();
            const sellPercentage = 100 - buyPercentage;
            return {
              buyPercentage,
              sellPercentage,
              buyAmount: askAmount.toNumber(),
              sellAmount: bidAmount.toNumber()
            };
          }
        }
        return null;
      }, [data?.asks, data?.bids, level]);
      return {
        level,
        asks: data?.asks,
        bids: data?.bids,
        markPrice: data?.markPrice,
        lastPrice: data?.middlePrice,
        depths,
        selDepth,
        base,
        quote,
        isLoading,
        onItemClick,
        cellHeight,
        onDepthChange,
        pendingOrders,
        symbolInfo: getBasicSymbolInfo(symbolInfo),
        isMobile,
        showBuySellRatio,
        setShowBuySellRatio,
        buySellRatio
      };
    };
    usePendingOrderStream = (symbol) => {
      const [data] = useOrderStream({
        status: OrderStatus.INCOMPLETE,
        symbol
      });
      const pendingOrders = (0, import_react8.useMemo)(() => {
        const info = data?.filter((item) => item.symbol === symbol).reduce((a, b) => {
          const price = b.price || b.trigger_price || 0;
          return [...a, price];
        }, []);
        return info;
      }, [data, symbol]);
      return pendingOrders;
    };
  }
});
var OrderBookWidget;
var init_orderBook_widget = __esm2({
  "src/components/base/orderBook/orderBook.widget.tsx"() {
    init_orderBook_script();
    init_orderBook_ui();
    OrderBookWidget = (props) => {
      const { className, symbol, height } = props;
      const state = useOrderBookScript({ symbol, height });
      return (0, import_jsx_runtime5.jsx)(OrderBook2, { ...state, className });
    };
  }
});
var BuySellRatioBar;
var init_buySellRatioBar = __esm2({
  "src/components/base/orderBook/buySellRatioBar.tsx"() {
    BuySellRatioBar = (props) => {
      const { ratio, className } = props;
      const { buyPercentage, sellPercentage } = (0, import_react8.useMemo)(() => {
        if (!ratio) {
          return { buyPercentage: 50, sellPercentage: 50 };
        }
        const isValidNumber = (value) => {
          return typeof value === "number" && !Number.isNaN(value) && Number.isFinite(value) && value >= 0 && value <= 100;
        };
        const buyPct = isValidNumber(ratio.buyPercentage) ? ratio.buyPercentage : 50;
        const buyPctRounded = new decimal_default(buyPct).toDecimalPlaces(1, decimal_default.ROUND_HALF_UP).toNumber();
        const sellPctRounded = new decimal_default(100).sub(buyPctRounded).toDecimalPlaces(1, decimal_default.ROUND_HALF_UP).toNumber();
        return {
          buyPercentage: buyPctRounded,
          sellPercentage: sellPctRounded
        };
      }, [ratio]);
      return (0, import_jsx_runtime5.jsxs)(Flex, { className: M("oui-w-full", className), gap: 1, children: [
        (0, import_jsx_runtime5.jsxs)(Flex, { itemAlign: "center", gap: 1, children: [
          (0, import_jsx_runtime5.jsx)(Text2, { intensity: 80, children: "B" }),
          (0, import_jsx_runtime5.jsxs)(Text2, { color: "success", children: [
            buyPercentage.toFixed(1),
            "%"
          ] })
        ] }),
        (0, import_jsx_runtime5.jsxs)(
          "div",
          {
            style: {
              flex: 1,
              height: "4px",
              position: "relative",
              borderRadius: "2px",
              overflow: "hidden",
              backgroundColor: "rgb(var(--oui-line-4))"
            },
            children: [
              (0, import_jsx_runtime5.jsx)(
                "div",
                {
                  className: "oui-bg-trade-profit oui-mr-[2px] oui-rounded-sm",
                  style: {
                    position: "absolute",
                    left: 0,
                    top: 0,
                    width: `${buyPercentage}%`,
                    height: "100%"
                  }
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "div",
                {
                  className: "oui-bg-trade-loss oui-ml-[2px] oui-rounded-sm",
                  style: {
                    position: "absolute",
                    left: `${buyPercentage}%`,
                    top: 0,
                    width: `${sellPercentage}%`,
                    height: "100%"
                  }
                }
              )
            ]
          }
        ),
        (0, import_jsx_runtime5.jsxs)(Flex, { itemAlign: "center", gap: 1, children: [
          (0, import_jsx_runtime5.jsxs)(Text2, { color: "danger", children: [
            sellPercentage.toFixed(1),
            "%"
          ] }),
          (0, import_jsx_runtime5.jsx)(Text2, { intensity: 80, children: "S" })
        ] })
      ] });
    };
  }
});
var orderBook_exports = {};
__export2(orderBook_exports, {
  BuySellRatioBar: () => BuySellRatioBar,
  OrderBook: () => OrderBook2,
  OrderBookWidget: () => OrderBookWidget,
  useOrderBookScript: () => useOrderBookScript
});
var init_orderBook2 = __esm2({
  "src/components/base/orderBook/index.ts"() {
    init_orderBook_ui();
    init_orderBook_widget();
    init_orderBook_script();
    init_buySellRatioBar();
  }
});
var RiskRate;
var init_riskRate_ui = __esm2({
  "src/components/desktop/riskRate/riskRate.ui.tsx"() {
    init_assetView_ui();
    RiskRate = (props) => {
      const { riskRate, riskRateColor, currentLeverage, maxLeverage } = props;
      const { isHigh, isMedium, isLow } = riskRateColor;
      const { wrongNetwork } = useAppContext();
      const { t } = useTranslation();
      const textColor2 = wrongNetwork ? "" : isHigh ? "oui-text-danger" : isMedium ? "oui-text-warning-darken" : isLow ? gradientTextVariants({ color: "brand" }) : "";
      const boxClsName = wrongNetwork ? "oui-bg-gradient-to-r oui-opacity-20 oui-from-[#26fefe]  oui-via-[#ff7d00] oui-to-[#d92d6b] oui-h-1.5 oui-rounded-full" : isHigh ? "oui-bg-gradient-to-tr oui-from-[#791438] oui-to-[#ff447c] oui-h-1.5 oui-rounded-full" : isMedium ? "oui-bg-gradient-to-tr oui-from-[#792e00] oui-to-[#ffb65d] oui-h-1.5 oui-rounded-full" : isLow ? "oui-bg-gradient-to-tr oui-from-[#59b0fe] oui-to-[#26fefe] oui-h-1.5 oui-rounded-full" : "oui-bg-gradient-to-r oui-opacity-20 oui-from-[#26fefe]  oui-via-[#ff7d00] oui-to-[#d92d6b] oui-h-1.5 oui-rounded-full";
      return (0, import_jsx_runtime5.jsxs)(Box, { "data-risk": "", className: "oui-space-y-2", children: [
        (0, import_jsx_runtime5.jsx)(
          Flex,
          {
            itemAlign: "center",
            justify: "start",
            className: "oui-w-full oui-bg-base-6 oui-rounded-full oui-h-2 oui-px-[1px]",
            children: (0, import_jsx_runtime5.jsx)(
              Box,
              {
                className: boxClsName,
                style: riskRate && riskRate !== "--" ? { width: riskRate } : { width: "100%" }
              }
            )
          }
        ),
        (0, import_jsx_runtime5.jsxs)(Flex, { direction: "row", justify: "between", children: [
          (0, import_jsx_runtime5.jsx)(
            Tooltip,
            {
              content: (0, import_jsx_runtime5.jsx)(
                TooltipContent2,
                {
                  description: t("trading.riskRate.tooltip"),
                  formula: t("trading.riskRate.formula")
                }
              ),
              children: (0, import_jsx_runtime5.jsx)(
                Text2,
                {
                  size: "2xs",
                  color: "neutral",
                  weight: "semibold",
                  className: M(
                    "oui-cursor-pointer",
                    "oui-border-b oui-border-dashed oui-border-b-white/10"
                  ),
                  children: t("trading.riskRate")
                }
              )
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            Text2,
            {
              size: "xs",
              color: "neutral",
              weight: "semibold",
              className: M(textColor2),
              children: riskRate ?? "--"
            }
          )
        ] })
      ] });
    };
  }
});
var getRiskRateColor;
var useRiskRateScript;
var init_riskRate_script = __esm2({
  "src/components/desktop/riskRate/riskRate.script.tsx"() {
    getRiskRateColor = (riskRate) => {
      if (riskRate === null) {
        return { isHigh: false, isMedium: false, isLow: false, isDefault: true };
      }
      const isLow = riskRate < 40;
      const isMedium = riskRate >= 40 && riskRate < 80;
      const isHigh = riskRate >= 80;
      const isDefault = !isLow && !isMedium && !isHigh;
      return { isHigh, isMedium, isLow, isDefault };
    };
    useRiskRateScript = () => {
      const { state } = useAccount();
      const isConnected = state.status >= AccountStatusEnum.EnableTrading || state.status === AccountStatusEnum.EnableTradingWithoutConnected;
      const { marginRatio, currentLeverage, mmr } = useMarginRatio();
      const { curLeverage } = useLeverage();
      const riskRate = (0, import_react8.useMemo)(() => {
        if (!isConnected || marginRatio === null || mmr === null) {
          return "--";
        }
        if (marginRatio === 0 || mmr === 0) {
          return "0%";
        }
        const calculatedRiskRate = new decimal_default(mmr).div(marginRatio).mul(100).todp(2, decimal_default.ROUND_UP);
        return `${calculatedRiskRate.toString().replace(/\.?0+$/, "")}%`;
      }, [isConnected, marginRatio, mmr]);
      const riskRateNumber = riskRate === "--" ? null : parseFloat(riskRate);
      const riskRateColor = (0, import_react8.useMemo)(() => {
        return getRiskRateColor(riskRateNumber);
      }, [riskRateNumber]);
      const _curLeverage = useDataTap(currentLeverage);
      const _maxLeverage = useDataTap(curLeverage);
      const _riskRate = useDataTap(riskRate);
      return {
        riskRate: _riskRate,
        riskRateColor,
        isConnected,
        currentLeverage: _curLeverage,
        maxLeverage: _maxLeverage
      };
    };
  }
});
var RiskRateWidget;
var init_riskRate_widget = __esm2({
  "src/components/desktop/riskRate/riskRate.widget.tsx"() {
    init_riskRate_script();
    init_riskRate_ui();
    RiskRateWidget = () => {
      const state = useRiskRateScript();
      return (0, import_jsx_runtime5.jsx)(RiskRate, { ...state });
    };
  }
});
var riskRate_exports = {};
__export2(riskRate_exports, {
  RiskRate: () => RiskRate,
  RiskRateWidget: () => RiskRateWidget
});
var init_riskRate = __esm2({
  "src/components/desktop/riskRate/index.ts"() {
    init_riskRate_ui();
    init_riskRate_widget();
  }
});
var LazyLastTradesWidget;
var LazyOrderBookWidget;
var TwoColLayout;
var TabLayout;
var Title2;
var OrderBookAndTrades;
var init_orderBookAndTrades_ui = __esm2({
  "src/components/desktop/orderBookAndTrades/orderBookAndTrades.ui.tsx"() {
    LazyLastTradesWidget = import_react8.default.lazy(
      () => Promise.resolve().then(() => (init_lastTrades(), lastTrades_exports)).then((mod) => {
        return {
          default: mod.LastTradesWidget
        };
      })
    );
    LazyOrderBookWidget = import_react8.default.lazy(
      () => Promise.resolve().then(() => (init_orderBook2(), orderBook_exports)).then((mod) => {
        return {
          default: mod.OrderBookWidget
        };
      })
    );
    TwoColLayout = (props) => {
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsxs)(
        Grid2,
        {
          cols: 2,
          width: "100%",
          gap: 3,
          className: "oui-auto-rows-fr",
          style: {
            height: props.containerSize?.height
          },
          children: [
            (0, import_jsx_runtime5.jsxs)(
              Flex,
              {
                direction: "column",
                itemAlign: "start",
                pt: 3,
                pb: 3,
                r: "2xl",
                className: "oui-bg-base-9",
                children: [
                  (0, import_jsx_runtime5.jsx)(
                    Title2,
                    {
                      title: t("trading.orderBook"),
                      className: "oui-pl-3 oui-text-sm"
                    }
                  ),
                  (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
                    LazyOrderBookWidget,
                    {
                      symbol: props.symbol,
                      height: props.containerSize ? props.containerSize.height - 29 - 24 : void 0
                    }
                  ) })
                ]
              }
            ),
            (0, import_jsx_runtime5.jsxs)(
              Flex,
              {
                direction: "column",
                itemAlign: "start",
                py: 3,
                r: "2xl",
                className: "oui-bg-base-9 oui-h-full",
                children: [
                  (0, import_jsx_runtime5.jsx)(
                    Title2,
                    {
                      title: t("trading.lastTrades"),
                      className: "oui-text-sm oui-px-3"
                    }
                  ),
                  (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
                    LazyLastTradesWidget,
                    {
                      symbol: props.symbol,
                      style: {
                        height: props.containerSize && props.containerSize.height - 29 - 24
                      },
                      classNames: { listHeader: "oui-px-3", list: "oui-px-3" }
                    }
                  ) })
                ]
              }
            )
          ]
        }
      );
    };
    TabLayout = (props) => {
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsx)(
        Box,
        {
          pt: 3,
          pb: 3,
          pr: 0,
          r: "2xl",
          className: "oui-bg-base-9",
          style: { maxHeight: props.containerSize?.height },
          children: (0, import_jsx_runtime5.jsxs)(
            Tabs,
            {
              value: props.tab,
              variant: "contained",
              onValueChange: (tab) => {
                props.setTab(tab);
              },
              classNames: {
                tabsList: "oui-pl-3"
                // tabsContent: props.tab === "lastTrades" ? 'oui-pl-3' : ''
              },
              size: "lg",
              children: [
                (0, import_jsx_runtime5.jsx)(TabPanel, { value: "orderBook", title: t("trading.orderBook"), children: (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
                  LazyOrderBookWidget,
                  {
                    symbol: props.symbol,
                    height: props.containerSize ? props.containerSize.height - 29 - 18 : void 0
                  }
                ) }) }),
                (0, import_jsx_runtime5.jsx)(TabPanel, { value: "lastTrades", title: t("trading.lastTrades"), children: (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
                  LazyLastTradesWidget,
                  {
                    symbol: props.symbol,
                    style: {
                      height: props.containerSize && props.containerSize.height - 29 - 18
                    },
                    classNames: {
                      root: "oui-pt-[6px]",
                      listHeader: "oui-px-3",
                      list: "oui-px-3"
                    }
                  }
                ) }) })
              ]
            }
          )
        }
      );
    };
    Title2 = (props) => {
      return (0, import_jsx_runtime5.jsx)(
        Text2,
        {
          size: "base",
          intensity: 80,
          className: M("oui-pb-[5px]", props.className),
          children: props.title
        }
      );
    };
    OrderBookAndTrades = (props) => {
      const { containerRef, containerSize } = props;
      return (0, import_jsx_runtime5.jsx)("div", { ref: containerRef, className: "oui-h-full", children: (containerSize?.width ?? 0) >= 572 ? (0, import_jsx_runtime5.jsx)(TwoColLayout, { ...props }) : (0, import_jsx_runtime5.jsx)(TabLayout, { ...props }) });
    };
  }
});
var useOrderBookAndTradesScript;
var init_orderBookAndTrades_script = __esm2({
  "src/components/desktop/orderBookAndTrades/orderBookAndTrades.script.tsx"() {
    useOrderBookAndTradesScript = (symbol) => {
      const [containerSize, setContainerSize] = (0, import_react8.useState)(void 0);
      const [tab, setTab] = (0, import_react8.useState)("orderBook");
      const containerRef = (0, import_react8.useRef)(null);
      (0, import_react8.useEffect)(() => {
        const resizeObserver = new ResizeObserver((entries) => {
          for (const entry of entries) {
            const { width, height } = entry.contentRect;
            setContainerSize({
              width,
              height
            });
          }
        });
        const targetDiv = containerRef.current;
        if (targetDiv) {
          resizeObserver.observe(targetDiv);
        }
        return () => {
          if (targetDiv) {
            resizeObserver.unobserve(targetDiv);
          }
        };
      }, []);
      return {
        symbol,
        containerSize,
        containerRef,
        tab,
        setTab
      };
    };
  }
});
var OrderBookAndTradesWidget;
var init_orderBookAndTrades_widget = __esm2({
  "src/components/desktop/orderBookAndTrades/orderBookAndTrades.widget.tsx"() {
    init_orderBookAndTrades_script();
    init_orderBookAndTrades_ui();
    OrderBookAndTradesWidget = (props) => {
      const state = useOrderBookAndTradesScript(props.symbol);
      return (0, import_jsx_runtime5.jsx)(OrderBookAndTrades, { ...state });
    };
  }
});
var orderBookAndTrades_exports = {};
__export2(orderBookAndTrades_exports, {
  OrderBookAndTrades: () => OrderBookAndTrades,
  OrderBookAndTradesWidget: () => OrderBookAndTradesWidget,
  useOrderBookAndTradesScript: () => useOrderBookAndTradesScript
});
var init_orderBookAndTrades = __esm2({
  "src/components/desktop/orderBookAndTrades/index.ts"() {
    init_orderBookAndTrades_ui();
    init_orderBookAndTrades_widget();
    init_orderBookAndTrades_script();
  }
});
var switchLayout_exports = {};
__export2(switchLayout_exports, {
  LayoutIcon: () => LayoutIcon,
  MarketBottomIcon: () => MarketBottomIcon,
  MarketHideIcon: () => MarketHideIcon,
  MarketLeftIcon: () => MarketLeftIcon,
  MarketTopIcon: () => MarketTopIcon,
  OrderEntryIcon: () => OrderEntryIcon,
  SwitchLayout: () => SwitchLayout,
  SwitchLayoutDropDown: () => SwitchLayoutDropDown
});
var SwitchLayout;
var SwitchLayoutDropDown;
var LayoutIcon;
var OrderEntryIcon;
var MarketLeftIcon;
var MarketTopIcon;
var MarketBottomIcon;
var MarketHideIcon;
var init_switchLayout = __esm2({
  "src/components/desktop/layout/switchLayout.tsx"() {
    SwitchLayout = (props) => {
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsx)(SwitchLayoutDropDown, { ...props, children: (0, import_jsx_runtime5.jsxs)(
        Flex,
        {
          px: 3,
          className: M(
            "oui-rounded-md",
            "oui-h-[28px]",
            "oui-cursor-pointer oui-transition-all",
            "oui-bg-base-6 hover:oui-bg-base-4",
            "oui-text-base-contrast-54 hover:oui-text-base-contrast-80"
          ),
          gapX: 1,
          ml: 3,
          justify: "center",
          itemAlign: "center",
          children: [
            (0, import_jsx_runtime5.jsx)(LayoutIcon, {}),
            (0, import_jsx_runtime5.jsx)(Text2, { size: "2xs", weight: "semibold", children: t("trading.layout") })
          ]
        }
      ) });
    };
    SwitchLayoutDropDown = (props) => {
      const [open2, setOpen] = (0, import_react8.useState)(false);
      const [hoveredMarket, setHoveredMarket] = (0, import_react8.useState)(null);
      const { t } = useTranslation();
      const renderItem = (position) => {
        return (0, import_jsx_runtime5.jsxs)(
          Flex,
          {
            direction: "column",
            gapY: 2,
            onClick: () => {
              props.onLayout?.(position);
              setOpen(false);
            },
            className: "oui-group",
            children: [
              (0, import_jsx_runtime5.jsx)(
                Flex,
                {
                  justify: position === "right" ? "end" : "start",
                  className: M(
                    "oui-w-[148px] oui-h-[100px]",
                    "oui-bg-base-10 oui-rounded-[10px]",
                    "oui-border-[4px] oui-border-base-5 group-hover:oui-border-primary-light",
                    props.layout === position && "!oui-border-primary-light"
                  ),
                  children: (0, import_jsx_runtime5.jsx)(Box, { p: 1, children: (0, import_jsx_runtime5.jsx)(OrderEntryIcon, {}) })
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                Text2,
                {
                  size: "2xs",
                  intensity: 54,
                  className: M(
                    "oui-text-base-contrast-54 group-hover:oui-text-base-contrast-80",
                    props.layout === position && "oui-text-base-contrast-80"
                  ),
                  children: String(
                    position === "right" ? t("trading.layout.advanced.right") : t("trading.layout.advanced.left")
                  )
                }
              )
            ]
          }
        );
      };
      const renderMarketItem = (position) => {
        const getIcon = (isHovered) => {
          const isSelected = props.marketLayout === position;
          switch (position) {
            case "left":
              return (0, import_jsx_runtime5.jsx)(MarketLeftIcon, { isSelected, isHovered });
            case "top":
              return (0, import_jsx_runtime5.jsx)(MarketTopIcon, { isSelected, isHovered });
            case "bottom":
              return (0, import_jsx_runtime5.jsx)(MarketBottomIcon, { isSelected, isHovered });
            case "hide":
              return (0, import_jsx_runtime5.jsx)(MarketHideIcon, { isSelected, isHovered });
            default:
              return (0, import_jsx_runtime5.jsx)(MarketLeftIcon, { isSelected, isHovered });
          }
        };
        const getLabel = () => {
          switch (position) {
            case "left":
              return t("trading.layout.markets.left");
            case "top":
              return t("trading.layout.markets.top");
            case "bottom":
              return t("trading.layout.markets.bottom");
            case "hide":
              return t("trading.layout.markets.hide");
            default:
              return t("trading.layout.markets.left");
          }
        };
        return (0, import_jsx_runtime5.jsxs)(
          Flex,
          {
            direction: "column",
            gapY: 2,
            onClick: () => {
              props.onMarketLayout?.(position);
              setOpen(false);
            },
            onMouseEnter: () => setHoveredMarket(position),
            onMouseLeave: () => setHoveredMarket(null),
            className: "oui-group",
            children: [
              (0, import_jsx_runtime5.jsx)(Flex, { justify: "center", className: "oui-w-[148px] oui-h-[100px]", children: getIcon(hoveredMarket === position) }),
              (0, import_jsx_runtime5.jsx)(
                Text2,
                {
                  size: "2xs",
                  intensity: 54,
                  className: M(
                    "oui-text-base-contrast-54 group-hover:oui-text-base-contrast-80",
                    props.marketLayout === position && "oui-text-base-contrast-80"
                  ),
                  children: String(getLabel())
                }
              )
            ]
          }
        );
      };
      const content = (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
        (0, import_jsx_runtime5.jsxs)(
          Flex,
          {
            itemAlign: "center",
            justify: "between",
            mt: 3,
            className: "oui-mb-[10px] oui-min-w-[500px]",
            children: [
              (0, import_jsx_runtime5.jsx)(Text2, { size: "base", intensity: 98, children: t("trading.layout") }),
              (0, import_jsx_runtime5.jsx)(
                CloseIcon,
                {
                  size: 16,
                  className: "oui-text-base-contrast-80 oui-cursor-pointer",
                  opacity: 0.98,
                  onClick: () => {
                    setOpen(false);
                  }
                }
              )
            ]
          }
        ),
        (0, import_jsx_runtime5.jsx)(Divider, {}),
        (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", gapY: 2, mt: 5, itemAlign: "start", children: [
          (0, import_jsx_runtime5.jsx)(Text2, { size: "xs", intensity: 98, children: String(t("trading.layout.advanced")) }),
          (0, import_jsx_runtime5.jsxs)(Flex, { gapX: 6, children: [
            renderItem("left"),
            renderItem("right")
          ] })
        ] }),
        (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", gapY: 2, mt: 5, itemAlign: "start", children: [
          (0, import_jsx_runtime5.jsx)(Text2, { size: "xs", intensity: 98, children: String(t("trading.layout.markets")) }),
          (0, import_jsx_runtime5.jsxs)(Flex, { gapX: 6, children: [
            renderMarketItem("left"),
            renderMarketItem("top"),
            renderMarketItem("bottom"),
            renderMarketItem("hide")
          ] })
        ] })
      ] });
      return (0, import_jsx_runtime5.jsxs)(DropdownMenuRoot, { open: open2, onOpenChange: setOpen, children: [
        (0, import_jsx_runtime5.jsx)(DropdownMenuTrigger, { asChild: true, children: props.children }),
        (0, import_jsx_runtime5.jsx)(DropdownMenuPortal, { children: (0, import_jsx_runtime5.jsx)(
          DropdownMenuContent,
          {
            onCloseAutoFocus: (e) => e.preventDefault(),
            onClick: (e) => e.stopPropagation(),
            align: "end",
            className: M("oui-bg-base-8 oui-p-5 oui-pt-0 oui-font-semibold"),
            children: content
          }
        ) })
      ] });
    };
    LayoutIcon = (props) => (0, import_jsx_runtime5.jsx)(
      "svg",
      {
        width: "17",
        height: "16",
        viewBox: "0 0 17 16",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime5.jsx)("path", { d: "M3.832 1.994c-.736 0-1.333.597-1.333 1.333v9.334c0 .737.597 1.333 1.333 1.333H6.5c.737 0 1.333-.596 1.333-1.333V3.327c0-.736-.596-1.333-1.333-1.333zm6.667 0c-.737 0-1.333.597-1.333 1.333v2.667c0 .737.596 1.333 1.333 1.333h2.667c.736 0 1.333-.596 1.333-1.333V3.327c0-.736-.597-1.333-1.333-1.333zm.437 6.679a2.7 2.7 0 0 0-1.033.607.284.284 0 0 0-.061.339c.222.411-.01.851-.512.876a.29.29 0 0 0-.26.217c-.05.207-.07.38-.07.608 0 .19.02.407.06.599a.28.28 0 0 0 .252.217c.506.044.756.429.53.92a.28.28 0 0 0 .06.321c.296.273.635.466 1.034.59a.285.285 0 0 0 .312-.104c.31-.427.757-.428 1.05 0a.28.28 0 0 0 .313.113 2.8 2.8 0 0 0 1.042-.599.28.28 0 0 0 .06-.33c-.23-.466.035-.894.513-.902a.28.28 0 0 0 .269-.209c.048-.199.06-.372.06-.616q0-.316-.069-.616a.276.276 0 0 0-.27-.217c-.469-.001-.732-.463-.502-.868a.27.27 0 0 0-.053-.339 2.8 2.8 0 0 0-1.059-.607.274.274 0 0 0-.312.112c-.268.417-.77.425-1.033.009a.284.284 0 0 0-.321-.121m.842 1.536a1.111 1.111 0 1 1 0 2.222 1.111 1.111 0 0 1 0-2.222" })
      }
    );
    OrderEntryIcon = (props) => (0, import_jsx_runtime5.jsxs)(
      "svg",
      {
        width: "36",
        height: "84",
        viewBox: "0 0 36 84",
        fill: "none",
        xmlns: "http://www.w3.org/2000/svg",
        children: [
          (0, import_jsx_runtime5.jsx)("rect", { width: "36", height: "17", rx: "2", fill: "#181C23" }),
          (0, import_jsx_runtime5.jsx)("rect", { y: "19", width: "36", height: "54", rx: "2", fill: "#181C23" }),
          (0, import_jsx_runtime5.jsx)("rect", { y: "75", width: "36", height: "9", rx: "2", fill: "#181C23" }),
          (0, import_jsx_runtime5.jsx)(
            "rect",
            {
              x: "3",
              y: "79",
              width: "30",
              height: "1",
              rx: "0.5",
              fill: "url(#paint0_linear_17647_26849)"
            }
          ),
          (0, import_jsx_runtime5.jsx)("rect", { x: "3", y: "22", width: "14", height: "6", rx: "2", fill: "#008676" }),
          (0, import_jsx_runtime5.jsx)("rect", { x: "19", y: "22", width: "14", height: "6", rx: "2", fill: "#D92D6B" }),
          (0, import_jsx_runtime5.jsx)("rect", { x: "3", y: "11", width: "14", height: "3", rx: "1.5", fill: "#333948" }),
          (0, import_jsx_runtime5.jsx)("rect", { x: "19", y: "11", width: "14", height: "3", rx: "1.5", fill: "#335FFC" }),
          (0, import_jsx_runtime5.jsx)("rect", { x: "3", y: "62", width: "30", height: "8", rx: "2", fill: "#008676" }),
          (0, import_jsx_runtime5.jsx)("defs", { children: (0, import_jsx_runtime5.jsxs)(
            "linearGradient",
            {
              id: "paint0_linear_17647_26849",
              x1: "33",
              y1: "79.5",
              x2: "3",
              y2: "79.5",
              gradientUnits: "userSpaceOnUse",
              children: [
                (0, import_jsx_runtime5.jsx)("stop", { stopColor: "#779eff" }),
                (0, import_jsx_runtime5.jsx)("stop", { offset: "1", stopColor: "#26FEFE" })
              ]
            }
          ) })
        ]
      }
    );
    MarketLeftIcon = ({ isSelected, isHovered, ...props }) => {
      const getStrokeColor = () => {
        if (isSelected) return "rgb(var(--oui-color-primary-light))";
        if (isHovered) return "rgb(var(--oui-color-primary-light))";
        return "rgb(var(--oui-color-base-5))";
      };
      return (0, import_jsx_runtime5.jsxs)(
        "svg",
        {
          width: "148",
          height: "100",
          viewBox: "0 0 148 100",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg",
          ...props,
          children: [
            (0, import_jsx_runtime5.jsx)(
              "rect",
              {
                x: "2",
                y: "2",
                width: "144",
                height: "96",
                rx: "10",
                fill: "#07080A",
                stroke: getStrokeColor(),
                strokeWidth: "4"
              }
            ),
            (0, import_jsx_runtime5.jsx)("rect", { x: "8", y: "8", width: "24", height: "84", rx: "2", fill: "#181C23" }),
            (0, import_jsx_runtime5.jsx)("rect", { x: "16", y: "10", width: "4", height: "2", rx: "1", fill: "#333948" }),
            (0, import_jsx_runtime5.jsx)("rect", { x: "21", y: "10", width: "4", height: "2", rx: "1", fill: "#333948" }),
            (0, import_jsx_runtime5.jsx)("rect", { x: "26", y: "10", width: "4", height: "2", rx: "1", fill: "#333948" }),
            (0, import_jsx_runtime5.jsx)("rect", { x: "10", y: "10", width: "5", height: "2", rx: "1", fill: "#335FFC" }),
            (0, import_jsx_runtime5.jsx)("rect", { x: "10", y: "14", width: "20", height: "76", rx: "2", fill: "#282E3A" })
          ]
        }
      );
    };
    MarketTopIcon = ({ isSelected, isHovered, ...props }) => {
      const getStrokeColor = () => {
        if (isSelected) return "rgb(var(--oui-color-primary-light))";
        if (isHovered) return "rgb(var(--oui-color-primary-light))";
        return "rgb(var(--oui-color-base-5))";
      };
      return (0, import_jsx_runtime5.jsxs)(
        "svg",
        {
          width: "148",
          height: "100",
          viewBox: "0 0 148 100",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg",
          ...props,
          children: [
            (0, import_jsx_runtime5.jsx)(
              "rect",
              {
                x: "2",
                y: "2",
                width: "144",
                height: "96",
                rx: "10",
                fill: "#07080A",
                stroke: getStrokeColor(),
                strokeWidth: "4"
              }
            ),
            (0, import_jsx_runtime5.jsx)("rect", { x: "8", y: "8", width: "132", height: "8", rx: "2", fill: "#181C23" }),
            (0, import_jsx_runtime5.jsxs)("g", { clipPath: "url(#clip0_31319_74729)", children: [
              (0, import_jsx_runtime5.jsx)("rect", { x: "10", y: "10", width: "16", height: "4", rx: "2", fill: "#335FFC" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "28", y: "10", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "46", y: "10", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "64", y: "10", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "82", y: "10", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "100", y: "10", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "118", y: "10", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "136", y: "10", width: "16", height: "4", rx: "2", fill: "#333948" })
            ] }),
            (0, import_jsx_runtime5.jsx)("defs", { children: (0, import_jsx_runtime5.jsx)("clipPath", { id: "clip0_31319_74729", children: (0, import_jsx_runtime5.jsx)(
              "rect",
              {
                width: "130",
                height: "4",
                fill: "white",
                transform: "translate(10 10)"
              }
            ) }) })
          ]
        }
      );
    };
    MarketBottomIcon = ({ isSelected, isHovered, ...props }) => {
      const getStrokeColor = () => {
        if (isSelected) return "rgb(var(--oui-color-primary-light))";
        if (isHovered) return "rgb(var(--oui-color-primary-light))";
        return "rgb(var(--oui-color-base-5))";
      };
      return (0, import_jsx_runtime5.jsxs)(
        "svg",
        {
          width: "148",
          height: "100",
          viewBox: "0 0 148 100",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg",
          ...props,
          children: [
            (0, import_jsx_runtime5.jsx)(
              "rect",
              {
                x: "2",
                y: "2",
                width: "144",
                height: "96",
                rx: "10",
                fill: "#07080A",
                stroke: getStrokeColor(),
                strokeWidth: "4"
              }
            ),
            (0, import_jsx_runtime5.jsx)("rect", { x: "8", y: "84", width: "132", height: "8", rx: "2", fill: "#181C23" }),
            (0, import_jsx_runtime5.jsxs)("g", { clipPath: "url(#clip0_31319_74743)", children: [
              (0, import_jsx_runtime5.jsx)("rect", { x: "10", y: "86", width: "16", height: "4", rx: "2", fill: "#335FFC" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "28", y: "86", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "46", y: "86", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "64", y: "86", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "82", y: "86", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "100", y: "86", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "118", y: "86", width: "16", height: "4", rx: "2", fill: "#333948" }),
              (0, import_jsx_runtime5.jsx)("rect", { x: "136", y: "86", width: "16", height: "4", rx: "2", fill: "#333948" })
            ] }),
            (0, import_jsx_runtime5.jsx)("defs", { children: (0, import_jsx_runtime5.jsx)("clipPath", { id: "clip0_31319_74743", children: (0, import_jsx_runtime5.jsx)(
              "rect",
              {
                width: "130",
                height: "4",
                fill: "white",
                transform: "translate(10 86)"
              }
            ) }) })
          ]
        }
      );
    };
    MarketHideIcon = ({ isSelected, isHovered, ...props }) => {
      const getStrokeColor = () => {
        if (isSelected) return "rgb(var(--oui-color-primary-light))";
        if (isHovered) return "rgb(var(--oui-color-primary-light))";
        return "rgb(var(--oui-color-base-5))";
      };
      return (0, import_jsx_runtime5.jsxs)(
        "svg",
        {
          width: "148",
          height: "100",
          viewBox: "0 0 148 100",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg",
          ...props,
          children: [
            (0, import_jsx_runtime5.jsx)(
              "rect",
              {
                x: "2",
                y: "2",
                width: "144",
                height: "96",
                rx: "10",
                fill: "#07080A",
                stroke: getStrokeColor(),
                strokeWidth: "4"
              }
            ),
            (0, import_jsx_runtime5.jsxs)("g", { clipPath: "url(#clip0_31319_74757)", children: [
              (0, import_jsx_runtime5.jsx)("rect", { x: "8", y: "8", width: "132", height: "84", rx: "2", fill: "#181C23" }),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "66.8789",
                  y: "-76",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 66.8789 -76)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "73.9492",
                  y: "-68.929",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 73.9492 -68.929)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "81.0195",
                  y: "-61.8579",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 81.0195 -61.8579)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "88.0938",
                  y: "-54.7867",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 88.0938 -54.7867)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "95.1641",
                  y: "-47.7157",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 95.1641 -47.7157)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "102.234",
                  y: "-40.6447",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 102.234 -40.6447)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "109.305",
                  y: "-33.5736",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 109.305 -33.5736)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "116.375",
                  y: "-26.5026",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 116.375 -26.5026)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "123.449",
                  y: "-19.4315",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 123.449 -19.4315)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "130.52",
                  y: "-12.3604",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 130.52 -12.3604)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "137.59",
                  y: "-5.28931",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 137.59 -5.28931)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "144.66",
                  y: "1.78174",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 144.66 1.78174)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "151.73",
                  y: "8.85278",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 151.73 8.85278)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "158.805",
                  y: "15.9238",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 158.805 15.9238)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "165.875",
                  y: "22.995",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 165.875 22.995)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "172.945",
                  y: "30.066",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 172.945 30.066)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "180.016",
                  y: "37.1371",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 180.016 37.1371)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "187.086",
                  y: "44.2081",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 187.086 44.2081)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "194.156",
                  y: "51.2792",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 194.156 51.2792)",
                  fill: "#20252F"
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                "rect",
                {
                  x: "201.23",
                  y: "58.3503",
                  width: "4",
                  height: "188",
                  rx: "2",
                  transform: "rotate(45 201.23 58.3503)",
                  fill: "#20252F"
                }
              )
            ] }),
            (0, import_jsx_runtime5.jsx)("defs", { children: (0, import_jsx_runtime5.jsx)("clipPath", { id: "clip0_31319_74757", children: (0, import_jsx_runtime5.jsx)(
              "rect",
              {
                width: "132",
                height: "84",
                fill: "white",
                transform: "translate(8 8)"
              }
            ) }) })
          ]
        }
      );
    };
  }
});
var useTopTabScript;
var init_topTab_script = __esm2({
  "src/components/mobile/topTab/topTab.script.tsx"() {
    init_tradingPageContext();
    useTopTabScript = () => {
      const [tab, setTab] = (0, import_react8.useState)(
        "chart"
        /* chart */
      );
      const { symbol } = useTradingPageContext();
      const [visible, setVisible] = (0, import_react8.useState)(true);
      const toggleContentVisible = () => {
        setVisible((e) => !e);
      };
      return {
        tab,
        setTab,
        symbol,
        toggleContentVisible,
        setVisible,
        visible
      };
    };
  }
});
function useTradingviewScript2(props) {
  const [height, setHeight] = useLocalStorage(Key, MaxHeight);
  const [dragging, setDragging] = (0, import_react8.useState)(false);
  const dragRef = (0, import_react8.useRef)(null);
  const boxRef = (0, import_react8.useRef)(null);
  const [offsetY, setOffsetY] = (0, import_react8.useState)(0);
  const topRef = (0, import_react8.useRef)(0);
  const handleTouchStart = (0, import_react8.useCallback)((e) => {
    if (!boxRef.current) {
      return;
    }
    setDragging(true);
    const event = e.touches[0];
    const rect = boxRef.current.getBoundingClientRect();
    e.stopPropagation();
    e.preventDefault();
    const offsetY2 = event.clientY - rect.bottom;
    topRef.current = rect.top;
    setOffsetY(offsetY2);
  }, []);
  const handleTouchMove = (0, import_react8.useCallback)(
    (e) => {
      if (!boxRef.current) {
        return;
      }
      if (dragging) {
        e.stopPropagation();
        e.preventDefault();
        const event = e.touches[0];
        const newHeight = event.clientY - topRef.current - offsetY;
        setHeight(
          Math.min(Math.max(Math.round(newHeight), MinHeight), MaxHeight)
        );
        return false;
      }
    },
    [dragging, offsetY]
  );
  const handleTouchEnd = (0, import_react8.useCallback)(() => {
    setDragging(false);
  }, []);
  (0, import_react8.useEffect)(() => {
    const drag = dragRef.current;
    if (!drag) {
      return;
    }
    drag.addEventListener("touchstart", handleTouchStart);
    return () => {
      drag.removeEventListener("touchstart", handleTouchStart);
    };
  }, [handleTouchStart]);
  (0, import_react8.useEffect)(() => {
    document.addEventListener("touchmove", handleTouchMove, { passive: false });
    document.addEventListener("touchend", handleTouchEnd);
    return () => {
      document.removeEventListener("touchmove", handleTouchMove);
      document.removeEventListener("touchend", handleTouchEnd);
    };
  }, [dragging, handleTouchMove, handleTouchEnd]);
  return {
    ...props,
    height,
    dragging,
    dragRef,
    boxRef
  };
}
var MaxHeight;
var MinHeight;
var Key;
var init_tradingview_script = __esm2({
  "src/components/mobile/tradingview/tradingview.script.tsx"() {
    MaxHeight = 354;
    MinHeight = 234;
    Key = "TRADINGVIEW_MOBILE_HEIGHT";
  }
});
var KlineDragIcon;
var init_icons2 = __esm2({
  "src/components/base/icons.tsx"() {
    KlineDragIcon = (props) => (0, import_jsx_runtime5.jsx)(
      "svg",
      {
        width: "12",
        height: "12",
        viewBox: "0 0 12 12",
        fill: "currentColor",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: (0, import_jsx_runtime5.jsx)("g", { id: "Arrows-expand", children: (0, import_jsx_runtime5.jsx)(
          "path",
          {
            id: "Vector",
            d: "M5.9989 1.00708C5.8709 1.00708 5.7369 1.05008 5.6394 1.14758L3.7959 3.00708L4.4989 3.71008L5.4989 2.72607V4.50708C5.4989 4.78308 5.7229 5.00708 5.9989 5.00708C6.2749 5.00708 6.4989 4.78308 6.4989 4.50708V2.72607L7.4989 3.71008L8.2019 3.00708L6.3584 1.14758C6.2604 1.05008 6.1269 1.00708 5.9989 1.00708ZM5.9989 7.00707C5.7229 7.00707 5.4989 7.23107 5.4989 7.50707V9.28808L4.4989 8.30407L3.7959 9.00707L5.6394 10.8666C5.8349 11.0616 6.1629 11.0616 6.3584 10.8666L8.2019 9.00707L7.4989 8.30407L6.4989 9.28808V7.50707C6.4989 7.23107 6.2749 7.00707 5.9989 7.00707Z"
          }
        ) })
      }
    );
  }
});
var TradingviewUI2;
var init_tradingview_ui = __esm2({
  "src/components/mobile/tradingview/tradingview.ui.tsx"() {
    init_icons2();
    TradingviewUI2 = (props) => {
      return (0, import_jsx_runtime5.jsxs)(
        "div",
        {
          className: "oui-pb-1 oui-flex oui-flex-col oui-gap-1",
          style: { height: props.height },
          ref: props.boxRef,
          children: [
            (0, import_jsx_runtime5.jsx)("div", { className: "oui-w-full oui-h-full", children: (0, import_jsx_runtime5.jsx)(
              TradingviewWidget,
              {
                symbol: props.symbol,
                libraryPath: props.tradingViewConfig?.library_path,
                mode: 3,
                scriptSRC: props.tradingViewConfig?.scriptSRC,
                customCssUrl: props.tradingViewConfig?.customCssUrl,
                ...props.tradingViewConfig
              }
            ) }),
            (0, import_jsx_runtime5.jsx)("div", { className: "oui-relative oui-w-full", children: (0, import_jsx_runtime5.jsx)(
              "div",
              {
                ref: props.dragRef,
                className: M(
                  "oui-h-[1px] oui-absolute oui-left-0 oui-right-0 oui-bottom-0 oui-top-0 oui-z-10 oui-mt-[7px] oui-bg-base-contrast-12",
                  props.dragging && "oui-bg-primary "
                ),
                children: (0, import_jsx_runtime5.jsx)(
                  KlineDragIcon,
                  {
                    className: M(
                      "oui-w-3 oui-h-3 oui-absolute oui-left-1/2 -oui-top-[5px] -oui-translate-y-[0.5px] oui-text-base-contrast-12",
                      props.dragging && " oui-text-primary"
                    )
                  }
                )
              }
            ) })
          ]
        }
      );
    };
  }
});
var tradingview_widget_exports = {};
__export2(tradingview_widget_exports, {
  TradingviewWidget: () => TradingviewWidget3
});
var TradingviewWidget3;
var init_tradingview_widget = __esm2({
  "src/components/mobile/tradingview/tradingview.widget.tsx"() {
    init_tradingview_script();
    init_tradingview_ui();
    TradingviewWidget3 = (props) => {
      const state = useTradingviewScript2(props);
      return (0, import_jsx_runtime5.jsx)(TradingviewUI2, { ...state });
    };
  }
});
var lastTrades_widget_exports = {};
__export2(lastTrades_widget_exports, {
  MWebLastTrades: () => MWebLastTrades
});
var MWebLastTrades;
var init_lastTrades_widget2 = __esm2({
  "src/components/mobile/lastTrades/lastTrades.widget.tsx"() {
    init_lastTrades();
    MWebLastTrades = (props) => {
      return (0, import_jsx_runtime5.jsx)(
        LastTradesWidget,
        {
          symbol: props.symbol,
          classNames: {
            root: "oui-px-3 ",
            list: "oui-min-h-[141px] oui-max-h-[202px] oui-w-full",
            listHeader: "oui-text-xs oui-text-base-contrast-36",
            listItem: {
              left: "oui-text-xs",
              mid: "oui-text-xs",
              right: "oui-text-xs"
            }
          }
        }
      );
    };
  }
});
var TradeData;
var Row3;
var init_tradeData_ui = __esm2({
  "src/components/mobile/tradeData/tradeData.ui.tsx"() {
    TradeData = (props) => {
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", gap: 2, children: [
        (0, import_jsx_runtime5.jsx)(
          Row3,
          {
            title: t("common.markPrice"),
            value: props.ticker?.mark_price,
            dp: props.symbolInfo.quote_dp
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          Row3,
          {
            title: t("common.indexPrice"),
            value: props.ticker?.index_price,
            dp: props.symbolInfo.quote_dp
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          Row3,
          {
            title: t("markets.column.24hVolume"),
            value: props.vol_24h,
            dp: props.symbolInfo.quote_dp,
            rule: "human",
            showUSDC: true
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          Row3,
          {
            title: t("trading.column.24High"),
            value: props.ticker?.["24h_high"],
            dp: props.symbolInfo.quote_dp
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          Row3,
          {
            title: t("trading.column.24Low"),
            value: props.ticker?.["24h_low"],
            dp: props.symbolInfo.quote_dp
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          Row3,
          {
            title: t("markets.openInterest"),
            value: props.openInterest,
            dp: props.symbolInfo.quote_dp,
            rule: "human",
            showUSDC: true
          }
        )
      ] });
    };
    Row3 = (props) => {
      const { title, value, showUSDC, dp, rule = "price" } = props;
      return (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", width: "100%", className: "oui-text-xs", children: [
        (0, import_jsx_runtime5.jsx)(Text2, { intensity: 36, children: title }),
        (0, import_jsx_runtime5.jsxs)(Flex, { gap: 1, children: [
          (0, import_jsx_runtime5.jsx)(Text2.numeral, { rule, dp, intensity: 80, children: value }),
          showUSDC && (0, import_jsx_runtime5.jsx)(Text2, { intensity: 36, children: "USDC" })
        ] })
      ] });
    };
  }
});
var useTradeDataScript;
var init_tradeData_script = __esm2({
  "src/components/mobile/tradeData/tradeData.script.tsx"() {
    init_tradingPageContext();
    useTradeDataScript = (props) => {
      const { symbol } = props;
      const ticker = useTickerStream(symbol);
      const { symbolInfo } = useTradingPageContext();
      const vol_24h = (0, import_react8.useMemo)(() => {
        const close = ticker?.["24h_close"];
        const volume = ticker?.["24h_volume"];
        if (close && volume && !isNaN(close) && !isNaN(volume)) {
          return new decimal_default(close).mul(volume).toFixed(symbolInfo.quote_dp, decimal_default.ROUND_DOWN);
        }
        return void 0;
      }, [ticker]);
      const openInterest = (0, import_react8.useMemo)(() => {
        const markPrice = ticker?.["mark_price"];
        const openInterest2 = ticker?.["open_interest"];
        if (markPrice && openInterest2 && !isNaN(markPrice)) {
          return new decimal_default(markPrice).mul(Number(openInterest2)).toFixed(symbolInfo.quote_dp, decimal_default.ROUND_DOWN);
        }
        return void 0;
      }, [ticker]);
      return {
        ticker,
        symbolInfo,
        vol_24h,
        openInterest
      };
    };
  }
});
var TradeDataWidget;
var init_tradeData_widget = __esm2({
  "src/components/mobile/tradeData/tradeData.widget.tsx"() {
    init_tradeData_script();
    init_tradeData_ui();
    TradeDataWidget = (props) => {
      const state = useTradeDataScript(props);
      return (0, import_jsx_runtime5.jsx)(TradeData, { ...state });
    };
  }
});
var tradeData_exports = {};
__export2(tradeData_exports, {
  TradeData: () => TradeData,
  TradeDataWidget: () => TradeDataWidget,
  useTradeDataScript: () => useTradeDataScript
});
var init_tradeData = __esm2({
  "src/components/mobile/tradeData/index.ts"() {
    init_tradeData_ui();
    init_tradeData_widget();
    init_tradeData_script();
  }
});
var LazyTradingviewWidget;
var LazyMWebLastTrades;
var LazyTradeDataWidget;
var TopTab;
var ChevronIcon;
var init_topTab_ui = __esm2({
  "src/components/mobile/topTab/topTab.ui.tsx"() {
    init_tradingPageContext();
    init_topTab_script();
    LazyTradingviewWidget = import_react8.default.lazy(
      () => Promise.resolve().then(() => (init_tradingview_widget(), tradingview_widget_exports)).then((mod) => {
        return { default: mod.TradingviewWidget };
      })
    );
    LazyMWebLastTrades = import_react8.default.lazy(
      () => Promise.resolve().then(() => (init_lastTrades_widget2(), lastTrades_widget_exports)).then((mod) => {
        return { default: mod.MWebLastTrades };
      })
    );
    LazyTradeDataWidget = import_react8.default.lazy(
      () => Promise.resolve().then(() => (init_tradeData(), tradeData_exports)).then((mod) => {
        return { default: mod.TradeDataWidget };
      })
    );
    TopTab = (props) => {
      const { t } = useTranslation();
      const { tradingViewConfig } = useTradingPageContext();
      return (0, import_jsx_runtime5.jsxs)(
        Tabs,
        {
          variant: "contained",
          value: props.tab,
          contentVisible: props.visible,
          onValueChange: (e) => {
            props.setTab(e);
            props.setVisible(true);
          },
          className: props.className,
          classNames: {
            tabsList: "oui-p-2",
            tabsContent: "oui-min-h-[176px] oui-max-h-[396px]"
          },
          style: {
            marginBottom: props.tab === "chart" ? "8px" : 0
          },
          trailing: (0, import_jsx_runtime5.jsx)("button", { className: "oui-px-5", onClick: props.toggleContentVisible, children: (0, import_jsx_runtime5.jsx)(
            ChevronIcon,
            {
              className: props.visible ? "oui-rotate-0" : "oui-rotate-180"
            }
          ) }),
          children: [
            (0, import_jsx_runtime5.jsx)(TabPanel, { title: t("trading.tabs.chart"), value: "chart", children: (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
              LazyTradingviewWidget,
              {
                symbol: props.symbol,
                tradingViewConfig
              }
            ) }) }),
            (0, import_jsx_runtime5.jsx)(TabPanel, { title: t("trading.tabs.trades"), value: "trades", children: (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(LazyMWebLastTrades, { symbol: props.symbol }) }) }),
            (0, import_jsx_runtime5.jsx)(TabPanel, { title: t("trading.tabs.data"), value: "data", children: (0, import_jsx_runtime5.jsx)(Box, { px: 3, children: (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(LazyTradeDataWidget, { symbol: props.symbol }) }) }) })
          ]
        }
      );
    };
    ChevronIcon = (props) => {
      return (0, import_jsx_runtime5.jsxs)(
        "svg",
        {
          width: "12",
          height: "12",
          viewBox: "0 0 12 12",
          fill: "currentColor",
          xmlns: "http://www.w3.org/2000/svg",
          className: M(
            "oui-fill-base-contrast-54 hover:oui-fill-base-contrast-80",
            props.className
          ),
          children: [
            (0, import_jsx_runtime5.jsx)(
              "path",
              {
                d: "M5.721 4.585 2.726 6.578a.51.51 0 0 0-.14.7.51.51 0 0 0 .702.14l2.714-1.806 2.715 1.806c.23.153.549.089.702-.14a.51.51 0 0 0-.14-.7L6.283 4.585a.51.51 0 0 0-.562 0"
              }
            ),
            (0, import_jsx_runtime5.jsx)("defs", {})
          ]
        }
      );
    };
  }
});
var TopTabWidget;
var init_topTab_widget = __esm2({
  "src/components/mobile/topTab/topTab.widget.tsx"() {
    init_topTab_script();
    init_topTab_ui();
    TopTabWidget = (props) => {
      const state = useTopTabScript();
      return (0, import_jsx_runtime5.jsx)(TopTab, { className: props.className, ...state });
    };
  }
});
var topTab_exports = {};
__export2(topTab_exports, {
  TopTab: () => TopTab,
  TopTabWidget: () => TopTabWidget,
  useTopTabScript: () => useTopTabScript
});
var init_topTab = __esm2({
  "src/components/mobile/topTab/index.ts"() {
    init_topTab_ui();
    init_topTab_widget();
    init_topTab_script();
  }
});
var LazyOrderBookWidget2;
var OrderBookAndEntry;
var init_orderBookAndEntry_ui = __esm2({
  "src/components/mobile/orderBookAndEntry/orderBookAndEntry.ui.tsx"() {
    LazyOrderBookWidget2 = import_react8.default.lazy(
      () => Promise.resolve().then(() => (init_orderBook2(), orderBook_exports)).then((mod) => {
        return { default: mod.OrderBookWidget };
      })
    );
    OrderBookAndEntry = (props) => {
      const [height, setHeight] = (0, import_react8.useState)(0);
      const divRef = (0, import_react8.useRef)(null);
      (0, import_react8.useEffect)(() => {
        const div = divRef.current;
        if (!div) {
          return;
        }
        const resizeObserver = new ResizeObserver((entries) => {
          for (const entry of entries) {
            setHeight(entry.contentRect.height);
          }
        });
        resizeObserver.observe(div);
        return () => {
          resizeObserver.unobserve(div);
        };
      }, []);
      const orderBookColumn = (0, import_jsx_runtime5.jsx)(
        "div",
        {
          className: "oui-rounded-xl oui-bg-base-9",
          style: { height: `${height + 16}px` },
          children: (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
            LazyOrderBookWidget2,
            {
              symbol: props.symbol,
              height: height ? height - 44 : void 0
            }
          ) })
        }
      );
      const orderEntryColumn = (0, import_jsx_runtime5.jsx)("div", { className: "oui-rounded-xl oui-bg-base-9 oui-p-2", children: (0, import_jsx_runtime5.jsx)(OrderEntryWidget, { symbol: props.symbol, containerRef: divRef }) });
      return (0, import_jsx_runtime5.jsx)(
        "div",
        {
          className: M(
            "oui-mx-1 oui-grid oui-gap-1",
            props.layout === "right" ? "oui-grid-cols-[4fr,6fr]" : "oui-grid-cols-[6fr,4fr]",
            props.className
          ),
          children: props.layout === "right" ? (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
            orderBookColumn,
            orderEntryColumn
          ] }) : (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
            orderEntryColumn,
            orderBookColumn
          ] })
        }
      );
    };
  }
});
var ORDERLY_MWEB_ORDER_ENTRY_SIDE_MARKETS_LAYOUT;
var useOrderBookAndEntryScript;
var init_orderBookAndEntry_script = __esm2({
  "src/components/mobile/orderBookAndEntry/orderBookAndEntry.script.tsx"() {
    init_tradingPageContext();
    ORDERLY_MWEB_ORDER_ENTRY_SIDE_MARKETS_LAYOUT = "orderly_mweb_order_entry_side_markets_layout";
    useOrderBookAndEntryScript = () => {
      const { symbol } = useTradingPageContext();
      const [layout] = useLocalStorage(
        ORDERLY_MWEB_ORDER_ENTRY_SIDE_MARKETS_LAYOUT,
        "right"
      );
      return {
        symbol,
        layout
      };
    };
  }
});
var OrderBookAndEntryWidget;
var init_orderBookAndEntry_widget = __esm2({
  "src/components/mobile/orderBookAndEntry/orderBookAndEntry.widget.tsx"() {
    init_orderBookAndEntry_script();
    init_orderBookAndEntry_ui();
    OrderBookAndEntryWidget = (props) => {
      const state = useOrderBookAndEntryScript();
      return (0, import_jsx_runtime5.jsx)(OrderBookAndEntry, { className: props.className, ...state });
    };
  }
});
var orderBookAndEntry_exports = {};
__export2(orderBookAndEntry_exports, {
  OrderBookAndEntry: () => OrderBookAndEntry,
  OrderBookAndEntryWidget: () => OrderBookAndEntryWidget,
  useOrderBookAndEntryScript: () => useOrderBookAndEntryScript
});
var init_orderBookAndEntry = __esm2({
  "src/components/mobile/orderBookAndEntry/index.ts"() {
    init_orderBookAndEntry_ui();
    init_orderBookAndEntry_widget();
    init_orderBookAndEntry_script();
  }
});
var useDataListScript2;
var getDialogInfo;
var init_dataList_script2 = __esm2({
  "src/components/mobile/dataList/dataList.script.tsx"() {
    init_hooks();
    init_tradingPageContext();
    useDataListScript2 = (props) => {
      const { symbol, sharePnLConfig } = props;
      const [tab, setTab] = (0, import_react8.useState)(
        "Position"
        /* position */
      );
      const [subTab, setSubTab] = (0, import_react8.useState)(
        "Position history"
        /* positionHistory */
      );
      const { t } = useTranslation();
      const { onSymbolChange } = useTradingPageContext();
      const localStorage2 = useTradingLocalStorage();
      const [_, { cancelAllPendingOrders, cancelAllTPSLOrders }] = useOrderStream(
        {}
      );
      const { positionCount } = usePositionsCount(symbol);
      const { pendingOrderCount, tpSlOrderCount } = usePendingOrderCount(symbol);
      const onCloseAll = (type) => {
        const { title, content } = getDialogInfo(
          type,
          t,
          localStorage2.showAllSymbol ? void 0 : symbol
        );
        modal.confirm({
          title,
          content: (0, import_jsx_runtime5.jsx)(Text2, { size: "2xs", children: content }),
          onOk: async () => {
            try {
              if (tab === "TP/SL") {
                await cancelAllTPSLOrders(
                  localStorage2.showAllSymbol ? void 0 : symbol
                );
              } else {
                await cancelAllPendingOrders(
                  localStorage2.showAllSymbol ? void 0 : symbol
                );
              }
              return Promise.resolve(true);
            } catch (error) {
              if (error?.message !== void 0) {
                toast.error(error.message);
              }
              return Promise.resolve(false);
            } finally {
              Promise.resolve();
            }
          }
        });
      };
      return {
        tab,
        setTab,
        subTab,
        setSubTab,
        sharePnLConfig,
        symbol,
        positionCount,
        pendingOrderCount,
        tpSlOrderCount,
        ...localStorage2,
        onCloseAll,
        onSymbolChange
      };
    };
    getDialogInfo = (type, t, symbol) => {
      const formattedSymbol = symbol ? formatSymbol(symbol, "base") : symbol;
      switch (type) {
        case TabType.pending:
          if (symbol !== void 0) {
            return {
              title: t("orders.pending.cancelAll.forSymbol", {
                symbol: formattedSymbol
              }),
              content: t("orders.pending.cancelAll.forSymbol.description", {
                symbol: formattedSymbol
              })
            };
          }
          return {
            title: t("orders.pending.cancelAll"),
            content: t("orders.pending.cancelAll.description")
          };
        case TabType.tp_sl:
          if (symbol !== void 0) {
            return {
              title: t("orders.tpsl.cancelAll.forSymbol", {
                symbol: formattedSymbol
              }),
              content: t("orders.tpsl.cancelAll.forSymbol.description", {
                symbol: formattedSymbol
              })
            };
          }
          return {
            title: t("orders.tpsl.cancelAll"),
            content: t("orders.tpsl.cancelAll.description")
          };
        default:
          return {
            title: "",
            content: ""
          };
      }
    };
  }
});
var LazyPositionHeaderWidget2;
var SymbolControlHeader;
var OrdersView;
var PositionsView2;
var HistoryTab;
var DataList2;
var init_dataList_ui2 = __esm2({
  "src/components/mobile/dataList/dataList.ui.tsx"() {
    init_dataList_script2();
    LazyPositionHeaderWidget2 = import_react8.default.lazy(
      () => Promise.resolve().then(() => (init_positionHeader(), positionHeader_exports)).then((mod) => {
        return { default: mod.PositionHeaderWidget };
      })
    );
    SymbolControlHeader = (props) => {
      const { pendingOrderCount, tpSlOrderCount, type, symbol } = props;
      const { t } = useTranslation();
      const cancelAllDisabled = (0, import_react8.useMemo)(() => {
        if (type === TabType.pending) {
          return pendingOrderCount === 0;
        }
        if (type === TabType.tp_sl) {
          return tpSlOrderCount === 0;
        }
        return false;
      }, [pendingOrderCount, tpSlOrderCount, type]);
      const formattedSymbol = props.showAllSymbol ? void 0 : symbol ? formatSymbol(symbol, "base") : symbol;
      return (0, import_jsx_runtime5.jsxs)(
        Flex,
        {
          px: 2,
          py: 2,
          width: "100%",
          justify: "between",
          gap: 2,
          className: "oui-rounded-b-xl oui-bg-base-9",
          children: [
            (0, import_jsx_runtime5.jsxs)(Flex, { className: "oui-cursor-pointer oui-gap-[2px]", children: [
              (0, import_jsx_runtime5.jsx)(
                Checkbox,
                {
                  color: "white",
                  checked: !props.showAllSymbol,
                  onCheckedChange: (checked) => {
                    props.setShowAllSymbol(!checked);
                  }
                }
              ),
              (0, import_jsx_runtime5.jsx)(
                Text2,
                {
                  size: "2xs",
                  intensity: 54,
                  onClick: () => {
                    props.setShowAllSymbol(!props.showAllSymbol);
                  },
                  children: t("trading.hideOtherSymbols")
                }
              )
            ] }),
            (0, import_jsx_runtime5.jsx)(
              Button,
              {
                variant: "outlined",
                size: "xs",
                color: "secondary",
                onClick: () => props.onCloseAll(props.type),
                disabled: cancelAllDisabled,
                children: !props.showAllSymbol ? t("orders.cancelAll.ofSymbol", { symbol: formattedSymbol }) : t("orders.cancelAll")
              }
            )
          ]
        }
      );
    };
    OrdersView = (props) => {
      return (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", pb: 2, width: "100%", children: [
        (0, import_jsx_runtime5.jsx)(Divider, { className: "oui-w-full" }),
        props.type !== TabType.orderHistory && (0, import_jsx_runtime5.jsx)(SymbolControlHeader, { ...props }),
        (0, import_jsx_runtime5.jsx)(
          MobileOrderListWidget,
          {
            symbol: props.showAllSymbol ? void 0 : props.symbol,
            onSymbolChange: props.onSymbolChange,
            type: props.type,
            ordersStatus: props.ordersStatus,
            classNames: {
              root: "oui-w-full oui-hide-scrollbar oui-overflow-y-hidden",
              content: "!oui-space-y-1",
              cell: "oui-py-2 oui-bg-base-9 oui-p-2 oui-rounded-xl"
            },
            sharePnLConfig: props.sharePnLConfig,
            showFilter: props.type === TabType.orderHistory,
            filterConfig: { range: { from: void 0, to: void 0 } }
          }
        )
      ] });
    };
    PositionsView2 = (props) => {
      return (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", gap: 2, children: [
        (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
          LazyPositionHeaderWidget2,
          {
            pnlNotionalDecimalPrecision: props.pnlNotionalDecimalPrecision,
            setPnlNotionalDecimalPrecision: props.setPnlNotionalDecimalPrecision,
            symbol: props.showAllSymbol ? void 0 : props.symbol,
            unPnlPriceBasis: props.unPnlPriceBasis,
            setUnPnlPriceBasic: props.setUnPnlPriceBasic
          }
        ) }),
        (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
          MobilePositionsWidget,
          {
            symbol: props.showAllSymbol ? void 0 : props.symbol,
            onSymbolChange: props.onSymbolChange,
            sharePnLConfig: props.sharePnLConfig,
            pnlNotionalDecimalPrecision: props.pnlNotionalDecimalPrecision
          }
        ) })
      ] });
    };
    HistoryTab = (props) => {
      const { t } = useTranslation();
      return (0, import_jsx_runtime5.jsx)("div", { className: "oui-min-h-[300px]", children: (0, import_jsx_runtime5.jsxs)(
        Tabs,
        {
          value: props.subTab,
          onValueChange: (e) => props.setSubTab(e),
          size: "md",
          classNames: { tabsList: "oui-bg-base-9 oui-rounded-t-xl oui-p-2" },
          children: [
            (0, import_jsx_runtime5.jsx)(
              TabPanel,
              {
                title: t("positions.positionHistory"),
                value: "Position history",
                children: (0, import_jsx_runtime5.jsx)(
                  MobilePositionHistoryWidget,
                  {
                    symbol: props.showAllSymbol ? void 0 : props.symbol,
                    onSymbolChange: props.onSymbolChange,
                    classNames: { cell: "oui-p-2 oui-bg-base-9 oui-rounded-xl" },
                    sharePnLConfig: props.sharePnLConfig
                  }
                )
              }
            ),
            (0, import_jsx_runtime5.jsx)(
              TabPanel,
              {
                title: t("orders.orderHistory"),
                value: "Order history",
                children: (0, import_jsx_runtime5.jsx)(OrdersView, { type: TabType.orderHistory, ...props })
              }
            )
          ]
        }
      ) });
    };
    DataList2 = (props) => {
      const { t } = useTranslation();
      const {
        positionCount = 0,
        pendingOrderCount = 0,
        tpSlOrderCount = 0,
        showAllSymbol,
        symbol,
        tab,
        setTab,
        className
      } = props;
      const tabPanelItems = [
        {
          title: `${t("common.positions")} ${positionCount > 0 ? `(${positionCount})` : ""}`,
          value: "Position",
          content: (0, import_jsx_runtime5.jsx)(PositionsView2, { ...props })
        },
        {
          title: `${t("orders.status.pending")} ${pendingOrderCount > 0 ? `(${pendingOrderCount})` : ""}`,
          value: "Pending",
          content: (0, import_jsx_runtime5.jsx)(
            OrdersView,
            {
              type: TabType.pending,
              ordersStatus: OrderStatus.INCOMPLETE,
              ...props
            }
          )
        },
        {
          title: `${t("common.tpsl")} ${tpSlOrderCount > 0 ? `(${tpSlOrderCount})` : ""}`,
          value: "TP/SL",
          content: (0, import_jsx_runtime5.jsx)(
            OrdersView,
            {
              type: TabType.tp_sl,
              ordersStatus: OrderStatus.INCOMPLETE,
              ...props
            }
          )
        },
        {
          title: t("trading.history"),
          value: "History",
          content: (0, import_jsx_runtime5.jsx)(HistoryTab, { ...props })
        },
        {
          title: t("positions.liquidation"),
          value: "Liquidation",
          content: (0, import_jsx_runtime5.jsx)(
            MobileLiquidationWidget,
            {
              enableLoadMore: true,
              symbol: showAllSymbol ? void 0 : symbol,
              classNames: { cell: "oui-p-2 oui-bg-base-9 oui-rounded-xl" }
            }
          )
        },
        {
          title: t("common.assets"),
          value: "Assets",
          content: (0, import_jsx_runtime5.jsx)(assets_exports2.AssetsWidget, {})
        }
      ];
      return (0, import_jsx_runtime5.jsx)(
        Tabs,
        {
          value: tab,
          defaultValue: "Position",
          onValueChange: (e) => setTab(e),
          size: "lg",
          className,
          classNames: {
            tabsList: "oui-bg-base-9 oui-rounded-t-xl oui-p-2 oui-overflow-x-scroll oui-hide-scrollbar"
          },
          children: tabPanelItems.map((item) => {
            const { content, ...rest } = item;
            return (0, import_react8.createElement)(TabPanel, { ...rest, key: `item-${rest.value}` }, content);
          })
        }
      );
    };
  }
});
var DataListWidget2;
var init_dataList_widget2 = __esm2({
  "src/components/mobile/dataList/dataList.widget.tsx"() {
    init_dataList_script2();
    init_dataList_ui2();
    DataListWidget2 = (props) => {
      const state = useDataListScript2(props);
      return (0, import_jsx_runtime5.jsx)(DataList2, { ...state, className: props.className });
    };
  }
});
var dataList_exports2 = {};
__export2(dataList_exports2, {
  DataList: () => DataList2,
  DataListWidget: () => DataListWidget2,
  useDataListScript: () => useDataListScript2
});
var init_dataList2 = __esm2({
  "src/components/mobile/dataList/index.ts"() {
    init_dataList_ui2();
    init_dataList_widget2();
    init_dataList_script2();
  }
});
init_dataList();
init_lastTrades();
init_assetView();
init_orderBook2();
init_riskRate();
init_orderBookAndTrades();
var SplitLineBar = (props) => {
  const { onMouseDown, mode = "horizontal", ...rest } = props;
  const disable = (0, import_react8.useMemo)(
    () => props.className?.split(" ").includes("disable"),
    [props.className]
  );
  const filterCls = (0, import_react8.useMemo)(
    () => props.className?.split(" ").filter((cls) => cls !== "disable"),
    [props.className]
  );
  return (0, import_jsx_runtime5.jsx)(
    "div",
    {
      ...rest,
      className: M(
        filterCls,
        "!oui-transition-none",
        "!oui-shadow-none !oui-bg-transparent",
        "hover:!oui-bg-primary-light hover:!oui-shadow-[0px_0px_4px_0px] hover:!oui-shadow-primary-light/80",
        "active:!oui-bg-primary-light active:!oui-shadow-[0px_0px_4px_0px] active:!oui-shadow-primary-light/80",
        "focus:!oui-bg-primary-light focus:!oui-shadow-[0px_0px_4px_0px] focus:!oui-shadow-primary-light/80",
        mode === "horizontal" ? "!oui-w-[2px] !oui-min-w-[2px] !oui-mx-[3px]" : "!oui-h-[2px] !oui-min-h-[2px] !oui-my-[3px]",
        disable && "oui-pointer-events-none"
      ),
      children: (0, import_jsx_runtime5.jsx)(
        "div",
        {
          onMouseDown,
          className: M(
            "!oui-transition-none",
            mode === "horizontal" ? "after:!oui-w-[2px]" : "after:!oui-h-[2px]",
            "after:!oui-bg-transparent after:!oui-shadow-transparent"
          )
        }
      )
    }
  );
};
var SplitLayout = (0, import_react8.forwardRef)((props, ref) => {
  const { onSizeChange, ...rest } = props;
  return (0, import_jsx_runtime5.jsx)(
    Split,
    {
      ref,
      ...rest,
      lineBar: true,
      renderBar: (barProps) => (0, import_jsx_runtime5.jsx)(SplitLineBar, { ...barProps, mode: props.mode }),
      onDragEnd: (_, width) => onSizeChange?.(`${width}`)
    }
  );
});
init_tradingPageContext();
var HeadIcon = () => {
  return (0, import_jsx_runtime5.jsxs)(
    "svg",
    {
      width: "24",
      height: "24",
      viewBox: "0 0 24 24",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      className: "oui-rounded-full",
      children: [
        (0, import_jsx_runtime5.jsxs)("g", { clipPath: "url(#a)", children: [
          (0, import_jsx_runtime5.jsx)(
            "path",
            {
              d: "m31.002 14.493-8.326-22.51L.166.31l8.327 22.51z",
              fill: "#03435D"
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            "path",
            {
              d: "m4.456-8.716-18.03 15.84 15.84 18.03 18.03-15.84z",
              fill: "#157CF2"
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            "path",
            {
              d: "m17.548 38.67 22.825-7.416-7.416-22.825-22.826 7.416z",
              fill: "#F3E200"
            }
          )
        ] }),
        (0, import_jsx_runtime5.jsx)("defs", { children: (0, import_jsx_runtime5.jsx)("clipPath", { id: "a", children: (0, import_jsx_runtime5.jsx)("rect", { width: "24", height: "24", rx: "12", fill: "#fff" }) }) })
      ]
    }
  );
};
var CopyIcon = () => {
  return (0, import_jsx_runtime5.jsx)(
    "svg",
    {
      width: "20",
      height: "20",
      viewBox: "0 0 20 20",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      children: (0, import_jsx_runtime5.jsx)(
        "path",
        {
          d: "M5.832 2.492A3.333 3.333 0 0 0 2.5 5.826v5a3.333 3.333 0 0 0 3.333 3.333 3.333 3.333 0 0 0 3.334 3.333h5a3.333 3.333 0 0 0 3.333-3.333v-5a3.333 3.333 0 0 0-3.333-3.333 3.333 3.333 0 0 0-3.334-3.334zm8.334 5c.92 0 1.666.746 1.666 1.667v5c0 .92-.746 1.666-1.666 1.666h-5c-.92 0-1.667-.745-1.667-1.666h3.333a3.333 3.333 0 0 0 3.334-3.334z",
          fill: "rgb(var(--oui-color-primary))"
        }
      )
    }
  );
};
var USDCIcon = (props) => {
  const { size = 20 } = props;
  return (0, import_jsx_runtime5.jsxs)(
    "svg",
    {
      width: size,
      height: size,
      viewBox: "0 0 20 20",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      children: [
        (0, import_jsx_runtime5.jsxs)("g", { clipPath: "url(#a)", children: [
          (0, import_jsx_runtime5.jsx)(
            "path",
            {
              d: "M10 20c5.542 0 10-4.458 10-10S15.542 0 10 0 0 4.458 0 10s4.458 10 10 10",
              fill: "#2775CA"
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            "path",
            {
              d: "M13 11.473c0-1.452-.94-1.95-2.82-2.158-1.344-.166-1.613-.498-1.613-1.078s.448-.955 1.343-.955c.806 0 1.254.25 1.478.872.045.124.18.207.313.207h.717c.179 0 .313-.125.313-.29v-.042c-.179-.913-.985-1.618-2.015-1.701v-.996c0-.166-.134-.29-.358-.332h-.671c-.18 0-.314.124-.359.332v.954c-1.343.166-2.194.996-2.194 2.034 0 1.369.896 1.908 2.776 2.116 1.254.207 1.657.456 1.657 1.12s-.627 1.12-1.477 1.12c-1.165 0-1.568-.456-1.702-1.078a.32.32 0 0 0-.313-.25h-.762c-.179 0-.313.125-.313.291v.042c.179 1.037.895 1.784 2.373 1.991v.996c0 .166.134.29.358.332h.672c.18 0 .313-.124.358-.332v-.996C12.105 13.465 13 12.593 13 11.473",
              fill: "#fff",
              fillOpacity: ".98"
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            "path",
            {
              d: "M7.74 16.57c-3.458-1.215-5.23-4.986-3.945-8.324.665-1.821 2.127-3.208 3.944-3.859.178-.086.266-.216.266-.433v-.607c0-.174-.088-.304-.266-.347-.044 0-.133 0-.177.043-4.21 1.3-6.516 5.68-5.186 9.798.798 2.428 2.704 4.292 5.186 5.072.177.087.355 0 .399-.173.044-.043.044-.087.044-.173v-.608c0-.13-.133-.303-.266-.39m4.698-13.527c-.178-.086-.355 0-.4.174-.043.043-.043.086-.043.173v.607c0 .174.133.347.266.434 3.457 1.213 5.23 4.985 3.944 8.323-.664 1.821-2.127 3.209-3.944 3.859-.178.086-.266.216-.266.433v.607c0 .174.088.304.266.347.044 0 .133 0 .177-.043 4.21-1.3 6.515-5.68 5.186-9.798-.798-2.471-2.748-4.335-5.186-5.116",
              fill: "#fff"
            }
          )
        ] }),
        (0, import_jsx_runtime5.jsx)("defs", { children: (0, import_jsx_runtime5.jsx)("clipPath", { id: "a", children: (0, import_jsx_runtime5.jsx)("path", { fill: "#fff", d: "M0 0h20v20H0z" }) }) })
      ]
    }
  );
};
var OrderlyIcon = (props) => {
  const { size = 20 } = props;
  return (0, import_jsx_runtime5.jsxs)(
    "svg",
    {
      width: "20",
      height: "20",
      viewBox: "0 0 20 20",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      children: [
        (0, import_jsx_runtime5.jsxs)("g", { clipPath: "url(#clip0_6403_64483)", children: [
          (0, import_jsx_runtime5.jsx)(
            "path",
            {
              d: "M10.0245 19.9493C15.5198 19.9493 19.9747 15.4944 19.9747 9.99908C19.9747 4.50371 15.5198 0.0488281 10.0245 0.0488281C4.5291 0.0488281 0.0742188 4.50371 0.0742188 9.99908C0.0742188 15.4944 4.5291 19.9493 10.0245 19.9493Z",
              fill: "url(#paint0_linear_6403_64483)"
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            "path",
            {
              fillRule: "evenodd",
              clipRule: "evenodd",
              d: "M14.1553 4.89304C14.2199 4.94541 14.1821 5.04758 14.0989 5.04758H5.95035C5.86716 5.04758 5.82936 4.94541 5.894 4.89304C7.0223 3.97912 8.45953 3.43164 10.0246 3.43164C11.5898 3.43164 13.0269 3.97912 14.1553 4.89304Z",
              fill: "white"
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            "path",
            {
              fillRule: "evenodd",
              clipRule: "evenodd",
              d: "M7.47013 11.7869C7.42315 11.7199 7.34748 11.6777 7.26565 11.6777H3.82985C3.75113 11.6777 3.69375 11.7525 3.71563 11.8281C4.5078 14.565 7.0326 16.566 10.0247 16.566C13.017 16.566 15.5417 14.565 16.3339 11.8281C16.3558 11.7525 16.2984 11.6777 16.2197 11.6777H12.7839C12.702 11.6777 12.6264 11.7199 12.5795 11.7869C12.0155 12.5911 11.0815 13.1167 10.0248 13.1167C8.96803 13.1167 8.03406 12.5911 7.47013 11.7869Z",
              fill: "white"
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            "path",
            {
              fillRule: "evenodd",
              clipRule: "evenodd",
              d: "M12.3347 7.90519C12.3822 7.95756 12.449 7.9891 12.5196 7.9891H16.1137C16.1952 7.9891 16.253 7.90983 16.2261 7.83301C15.9724 7.10689 15.5948 6.43893 15.1187 5.85447C15.074 5.79961 15.0067 5.76855 14.9358 5.76855H5.11323C5.04247 5.76855 4.97512 5.79961 4.93044 5.85447C4.45429 6.43893 4.0767 7.10689 3.82306 7.83301C3.79622 7.90983 3.85398 7.9891 3.93536 7.9891H7.52952C7.60022 7.9891 7.66694 7.95756 7.71445 7.90519C8.28491 7.2763 9.10861 6.88136 10.0246 6.88136C10.9405 6.88136 11.7643 7.2763 12.3347 7.90519Z",
              fill: "white"
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            "path",
            {
              fillRule: "evenodd",
              clipRule: "evenodd",
              d: "M13.1164 10.9586C13.0553 10.9586 13.0118 10.899 13.0284 10.8401C13.1033 10.5722 13.1433 10.2898 13.1433 9.9981C13.1433 9.58758 13.064 9.19556 12.9199 8.83657C12.8954 8.77581 12.9392 8.70801 13.0046 8.70801H16.392C16.4349 8.70801 16.472 8.73831 16.4799 8.78052C16.5538 9.17511 16.5925 9.58216 16.5925 9.9982C16.5925 10.2978 16.5724 10.5926 16.5337 10.8815C16.5277 10.9258 16.4897 10.9586 16.4451 10.9586H13.1164ZM7.02223 10.8401C7.03868 10.899 6.99532 10.9586 6.93421 10.9586H3.60551C3.56084 10.9586 3.52285 10.9258 3.5169 10.8815C3.47807 10.5926 3.45801 10.2978 3.45801 9.9982C3.45801 9.58216 3.4967 9.17511 3.57069 8.78052C3.57859 8.73831 3.61563 8.70801 3.65856 8.70801H7.04593C7.11141 8.70801 7.15516 8.77581 7.13076 8.83657C6.98657 9.19556 6.90722 9.58758 6.90722 9.9981C6.90722 10.2898 6.94728 10.5722 7.02223 10.8401Z",
              fill: "white"
            }
          )
        ] }),
        (0, import_jsx_runtime5.jsxs)("defs", { children: [
          (0, import_jsx_runtime5.jsxs)(
            "linearGradient",
            {
              id: "paint0_linear_6403_64483",
              x1: "10.0245",
              y1: "0.0488561",
              x2: "10.0245",
              y2: "19.9493",
              gradientUnits: "userSpaceOnUse",
              children: [
                (0, import_jsx_runtime5.jsx)("stop", { stopColor: "#C750FF" }),
                (0, import_jsx_runtime5.jsx)("stop", { offset: "1", stopColor: "#5800E8" })
              ]
            }
          ),
          (0, import_jsx_runtime5.jsx)("clipPath", { id: "clip0_6403_64483", children: (0, import_jsx_runtime5.jsx)("rect", { width: "20", height: "20", fill: "white" }) })
        ] })
      ]
    }
  );
};
var AccountSheet = (props) => {
  const { t } = useTranslation();
  return (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", gap: 4, children: [
    (0, import_jsx_runtime5.jsx)(AccountInfo, { ...props }),
    (0, import_jsx_runtime5.jsx)(ReferralInfo, { ...props }),
    (0, import_jsx_runtime5.jsx)(TradingRewardsInfo, { ...props }),
    (0, import_jsx_runtime5.jsxs)(Flex, { gap: 3, width: "100%", justify: "center", children: [
      props.showGetTestUSDC && (0, import_jsx_runtime5.jsx)(
        Button,
        {
          variant: "outlined",
          size: "md",
          onClick: props.onGetTestUSDC,
          fullWidth: true,
          loading: props.gettingTestUSDC,
          className: "oui-border-primary-light oui-text-primary-light",
          children: t("trading.faucet.getTestUSDC")
        }
      ),
      (0, import_jsx_runtime5.jsx)(
        Button,
        {
          variant: "outlined",
          color: "danger",
          size: "md",
          onClick: props.onDisconnect,
          className: props.showGetTestUSDC ? "oui-w-full" : "oui-w-[50%]",
          children: t("connector.disconnect")
        }
      )
    ] })
  ] });
};
var AccountInfo = (props) => {
  return (0, import_jsx_runtime5.jsxs)(Flex, { width: "100%", justify: "between", children: [
    (0, import_jsx_runtime5.jsxs)(Flex, { gap: 3, children: [
      (0, import_jsx_runtime5.jsx)(HeadIcon, {}),
      (0, import_jsx_runtime5.jsxs)(
        Flex,
        {
          direction: "column",
          justify: "start",
          itemAlign: "start",
          className: "oui-gap-[2px]",
          children: [
            (0, import_jsx_runtime5.jsx)(Text2.formatted, { rule: "address", children: props.address }),
            (0, import_jsx_runtime5.jsx)(
              Text2.formatted,
              {
                size: "2xs",
                intensity: 80,
                prefix: (0, import_jsx_runtime5.jsx)("div", { className: "oui-h-1 oui-w-1 oui-rounded-full oui-bg-success oui-pr-1" }),
                children: props.chainName
              }
            )
          ]
        }
      )
    ] }),
    (0, import_jsx_runtime5.jsx)(
      "button",
      {
        className: "oui-cursor-pointer",
        onClick: () => {
          props.onCopyAddress();
        },
        children: (0, import_jsx_runtime5.jsx)(CopyIcon, {})
      }
    )
  ] });
};
var ReferralInfo = (props) => {
  const { t } = useTranslation();
  return (0, import_jsx_runtime5.jsxs)(
    Flex,
    {
      direction: "column",
      gap: 3,
      r: "lg",
      p: 3,
      className: "oui-bg-base-6",
      width: "100%",
      children: [
        (0, import_jsx_runtime5.jsx)(
          "button",
          {
            onClick: props.onClickReferral,
            className: "oui-cursor-pointer oui-w-full",
            children: (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", width: "100%", children: [
              (0, import_jsx_runtime5.jsx)(Text2, { size: "2xs", children: t("affiliate.referral") }),
              (0, import_jsx_runtime5.jsx)(ArrowRightShortIcon, { color: "white", opacity: 0.98, size: 16 })
            ] })
          }
        ),
        props.isAffiliate && (0, import_jsx_runtime5.jsxs)(
          Flex,
          {
            gradient: "primary",
            p: 4,
            r: "lg",
            width: "100%",
            angle: 180,
            justify: "between",
            children: [
              (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", itemAlign: "start", children: [
                (0, import_jsx_runtime5.jsx)(Text2, { size: "xs", children: t("common.affiliate") }),
                (0, import_jsx_runtime5.jsxs)(Text2, { size: "2xs", intensity: 54, children: [
                  "(",
                  t("affiliate.commission.30d"),
                  ")"
                ] })
              ] }),
              (0, import_jsx_runtime5.jsxs)(Flex, { className: "oui-gap-[6px]", children: [
                (0, import_jsx_runtime5.jsx)(USDCIcon, {}),
                (0, import_jsx_runtime5.jsx)(
                  Text2.numeral,
                  {
                    dp: 2,
                    padding: false,
                    rm: decimal_default.ROUND_DOWN,
                    rule: "price",
                    children: props.affiliateCommission30D ?? "--"
                  }
                )
              ] })
            ]
          }
        ),
        props.isTrader && (0, import_jsx_runtime5.jsxs)(
          Flex,
          {
            gradient: "success",
            p: 4,
            r: "lg",
            width: "100%",
            angle: 180,
            justify: "between",
            children: [
              (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", itemAlign: "start", children: [
                (0, import_jsx_runtime5.jsx)(Text2, { size: "xs", children: t("affiliate.trader") }),
                (0, import_jsx_runtime5.jsxs)(Text2, { size: "2xs", intensity: 54, children: [
                  "(",
                  t("affiliate.commission.30d"),
                  ")"
                ] })
              ] }),
              (0, import_jsx_runtime5.jsxs)(Flex, { className: "oui-gap-[6px]", children: [
                (0, import_jsx_runtime5.jsx)(USDCIcon, {}),
                (0, import_jsx_runtime5.jsx)(
                  Text2.numeral,
                  {
                    dp: 2,
                    padding: false,
                    rm: decimal_default.ROUND_DOWN,
                    rule: "price",
                    children: props.traderCommission30D ?? "--"
                  }
                )
              ] })
            ]
          }
        )
      ]
    }
  );
};
var TradingRewardsInfo = (props) => {
  const { t } = useTranslation();
  return (0, import_jsx_runtime5.jsxs)(
    Flex,
    {
      direction: "column",
      gap: 3,
      r: "lg",
      p: 3,
      className: "oui-bg-base-6",
      width: "100%",
      children: [
        (0, import_jsx_runtime5.jsx)(
          "button",
          {
            onClick: props.onClickTradingRewards,
            className: "oui-cursor-pointer oui-w-full",
            children: (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", width: "100%", children: [
              (0, import_jsx_runtime5.jsxs)(Flex, { gap: 1, className: "oui-text-base-contrast oui-text-2xs", children: [
                (0, import_jsx_runtime5.jsx)(Text2, { children: t("common.tradingRewards") }),
                (0, import_jsx_runtime5.jsxs)(Text2, { intensity: 54, children: [
                  "(",
                  (0, import_jsx_runtime5.jsx)(Text2, { children: `${t("tradingRewards.epoch")?.toLowerCase()} ` }),
                  (0, import_jsx_runtime5.jsx)(Text2, { intensity: 98, children: props.curEpochId }),
                  " )"
                ] })
              ] }),
              (0, import_jsx_runtime5.jsx)(ArrowRightShortIcon, { color: "white", opacity: 0.98, size: 16 })
            ] })
          }
        ),
        (0, import_jsx_runtime5.jsxs)(
          Flex,
          {
            p: 4,
            r: "lg",
            width: "100%",
            angle: 180,
            justify: "between",
            className: "oui-bg-gradient-to-t oui-from-[#2d0061] oui-to-[#bd6bed]",
            children: [
              (0, import_jsx_runtime5.jsx)(Text2, { size: "2xs", intensity: 80, children: t("tradingRewards.myEstRewards") }),
              (0, import_jsx_runtime5.jsxs)(Flex, { className: "oui-gap-[6px]", children: [
                (0, import_jsx_runtime5.jsx)(OrderlyIcon, {}),
                (0, import_jsx_runtime5.jsx)(
                  Text2.numeral,
                  {
                    dp: 2,
                    padding: false,
                    rm: decimal_default.ROUND_DOWN,
                    rule: "price",
                    children: props.estRewards
                  }
                )
              ] })
            ]
          }
        )
      ]
    }
  );
};
var useAccountSheetScript = (props) => {
  const { t } = useTranslation();
  const [linkDeviceStorage] = useLocalStorage("orderly_link_device", {});
  const { account, state } = useAccount();
  const accountId = account.accountId;
  const address = account.address;
  const { hide } = useModal();
  const config = useConfig();
  const { connectedChain, disconnect, namespace } = useWalletConnector();
  const chainId = account.chainId || connectedChain?.id || linkDeviceStorage?.chainId;
  const showGetTestUSDC = (0, import_react8.useMemo)(() => {
    if (chainId) {
      return (state.status === AccountStatusEnum.EnableTrading || state.status === AccountStatusEnum.EnableTradingWithoutConnected) && // @ts-ignore
      isTestnet(parseInt(chainId));
    }
    return false;
  }, [state.status, chainId]);
  const chainName = useGetChains(chainId);
  const operatorUrl = config.get("operatorUrl");
  const onCopyAddress = () => {
    navigator.clipboard.writeText(address ?? "");
    Vt.success(t("common.copy.copied"));
  };
  const {
    affiliateCommission30D,
    traderCommission30D,
    isAffiliate,
    isTrader,
    onClickReferral
  } = useReferral(props.onClickReferral);
  const { curEpochId, estRewards, onClickTradingRewards } = useTradingRewards(
    props.onClickTradingRewards
  );
  const onDisconnect = async () => {
    localStorage.removeItem("orderly_link_device");
    await disconnect({
      label: state.connectWallet?.name
    });
    await account.disconnect();
    hide();
  };
  const [getTestUSDC, { isMutating: gettingTestUSDC }] = useMutation(
    `${operatorUrl}/v1/faucet/usdc`
  );
  const onGetTestUSDC = () => {
    if (state.status < AccountStatusEnum.EnableTrading && state.status !== AccountStatusEnum.EnableTradingWithoutConnected) {
      return;
    }
    const message = t("trading.faucet.getTestUSDC.success", {
      quantity: namespace === ChainNamespace.solana ? "100" : "1,000"
    });
    return getTestUSDC({
      chain_id: chainId?.toString(),
      user_address: state.address,
      broker_id: config.get("brokerId")
    }).then(
      (res) => {
        if (res.success) {
          return modal.alert({
            title: t("trading.faucet.getTestUSDC"),
            message,
            onOk: () => {
              return Promise.resolve(true);
            }
          });
        }
        res.message && Vt.error(res.message);
      },
      (error) => {
        Vt.error(error.message);
      }
    );
  };
  return {
    accountId,
    address,
    chainId,
    chainName,
    onCopyAddress,
    affiliateCommission30D,
    onClickReferral,
    isAffiliate,
    isTrader,
    traderCommission30D,
    curEpochId,
    onClickTradingRewards,
    estRewards,
    onDisconnect,
    showGetTestUSDC,
    onGetTestUSDC,
    gettingTestUSDC
  };
};
var useReferral = (_onClickReferral) => {
  const { data, isLoading, isAffiliate, isTrader } = useReferralInfo();
  const affiliateCommission30D = (0, import_react8.useMemo)(() => {
    if (isAffiliate) {
      return data?.referrer_info?.["30d_referrer_rebate"];
    }
    return void 0;
  }, [data, isAffiliate]);
  const traderCommission30D = (0, import_react8.useMemo)(() => {
    if (isTrader) {
      return data?.referee_info?.["30d_referee_rebate"];
    }
    return void 0;
  }, [data, isTrader]);
  const onClickReferral = () => {
    _onClickReferral?.();
  };
  return {
    onClickReferral,
    affiliateCommission30D,
    traderCommission30D,
    isAffiliate,
    isTrader
    // WARNING: test data
    // affiliateCommission30D: 1234.43,
    // traderCommission30D:44343.33,
    // isAffiliate: true,
    // isTrader: true,
  };
};
var useTradingRewards = (_onClick) => {
  const [curEpochEstimate] = useCurEpochEstimate(TWType.normal);
  const [list, curEpoch] = useEpochInfo(TWType.normal);
  const curEpochId = (0, import_react8.useMemo)(() => {
    return curEpoch.curEpochInfo?.epoch_id ?? "--";
  }, [curEpoch]);
  const estRewards = (0, import_react8.useMemo)(() => {
    if (typeof curEpochEstimate?.est_r_wallet === "undefined") {
      return "--";
    }
    return curEpochEstimate?.est_r_wallet;
  }, [curEpochEstimate]);
  const onClickTradingRewards = () => {
    _onClick?.();
  };
  return {
    curEpochId,
    estRewards,
    onClickTradingRewards
  };
};
function useGetChains(chainId) {
  const [mainChains, { findByChainId }] = useChains("mainnet", {
    pick: "network_infos",
    filter: (chain) => chain.network_infos?.bridge_enable || chain.network_infos?.bridgeless
  });
  const chainName = (0, import_react8.useMemo)(() => {
    const chain = findByChainId(parseInt(chainId), "network_infos");
    if (!chain) {
      return "Unknown";
    }
    return chain.name;
  }, [chainId, findByChainId]);
  return chainName;
}
var AccountSheetWidget = (props) => {
  const state = useAccountSheetScript(props);
  return (0, import_jsx_runtime5.jsx)(AccountSheet, { ...state });
};
var useAccountScript = () => {
  const { t } = useTranslation();
  const { referral, tradingRewards, bottomSheetLeading } = useTradingPageContext();
  const { account, state } = useAccount();
  const onShowAccountSheet = () => {
    modal.sheet({
      title: t("common.account"),
      leading: bottomSheetLeading,
      content: (0, import_jsx_runtime5.jsx)(AccountSheetWidget, { ...referral, ...tradingRewards })
    });
  };
  return {
    onShowAccountSheet,
    address: account.address,
    state
  };
};
var Account = (props) => {
  return (0, import_jsx_runtime5.jsx)(
    AuthGuard,
    {
      buttonProps: {
        size: "sm"
      },
      children: (0, import_jsx_runtime5.jsx)(
        Button,
        {
          variant: "gradient",
          size: "sm",
          className: "oui-max-w-[83px]",
          onClick: (e) => {
            props.onShowAccountSheet();
          },
          children: formatAddress(props.address, [4, 4])
        }
      )
    }
  );
};
installExtension({
  name: "mobile-account-menu",
  scope: ["*"],
  positions: [ExtensionPositionEnum.MobileAccountMenu],
  builder: useAccountScript,
  __isInternal: true
})((props) => {
  return (0, import_jsx_runtime5.jsx)(Account, { ...props });
});
var MobileAccountMenuExtension = () => {
  return (0, import_jsx_runtime5.jsx)(ExtensionSlot, { position: ExtensionPositionEnum.MobileAccountMenu });
};
var Balance = () => {
  return null;
};
var useBalanceScript = () => {
  return {};
};
var BalanceWidget = () => {
  const state = useBalanceScript();
  return (0, import_jsx_runtime5.jsx)(Balance, { ...state });
};
var Chain = (props) => {
  const { t } = useTranslation();
  return (0, import_jsx_runtime5.jsx)(
    "button",
    {
      onClick: (e) => {
        modal.show(ChainSelectorSheetId, {
          // networkId: props.networkId,
          bridgeLessOnly: false,
          isWrongNetwork: props.wrongNetwork
        }).then(
          (r) => {
            if (r?.chainId) {
              props.setCurrentChainId(r?.chainId);
            }
            Vt.success(t("connector.networkSwitched"));
          },
          (error) => void 0
        );
      },
      children: (0, import_jsx_runtime5.jsxs)(Box, { className: "oui-relative oui-rounded-t-[6px] oui-rounded-bl-[6px] oui-rounded-br-[3px] oui-bg-base-5 oui-px-2 oui-h-7 oui-flex oui-items-center", children: [
        (0, import_jsx_runtime5.jsx)(ChainIcon, { chainId: props.currentChainId, size: "2xs" }),
        (0, import_jsx_runtime5.jsx)("div", { className: "oui-absolute oui-right-0 oui-bottom-0", children: (0, import_jsx_runtime5.jsxs)(
          "svg",
          {
            width: "9",
            height: "9",
            viewBox: "0 0 9 9",
            fill: "none",
            xmlns: "http://www.w3.org/2000/svg",
            children: [
              (0, import_jsx_runtime5.jsx)("path", { d: "M9 6V0L0 9h6a3 3 0 0 0 3-3", fill: "url(#a)" }),
              (0, import_jsx_runtime5.jsx)("defs", { children: (0, import_jsx_runtime5.jsxs)(
                "linearGradient",
                {
                  id: "a",
                  x1: "9",
                  y1: "4.5",
                  x2: "0",
                  y2: "4.5",
                  gradientUnits: "userSpaceOnUse",
                  children: [
                    (0, import_jsx_runtime5.jsx)("stop", { stopColor: "rgb(var(--oui-gradient-brand-end))" }),
                    (0, import_jsx_runtime5.jsx)(
                      "stop",
                      {
                        offset: "1",
                        stopColor: "rgb(var(--oui-gradient-brand-start))"
                      }
                    )
                  ]
                }
              ) })
            ]
          }
        ) })
      ] })
    }
  );
};
var useChainScript = () => {
  const config = useConfig();
  const { wrongNetwork, currentChainId, setCurrentChainId } = useAppContext();
  const networkId = config.get("networkId");
  return {
    currentChainId,
    setCurrentChainId,
    networkId,
    wrongNetwork
  };
};
var ChainWidget = () => {
  const state = useChainScript();
  return (0, import_jsx_runtime5.jsx)(Chain, { ...state });
};
var BottomNavBar = (props) => {
  const renderContent = () => {
    if (props.wrongNetwork) {
      return null;
    }
    if (!props.disabledConnect && props.status === AccountStatusEnum.EnableTradingWithoutConnected) {
      return (0, import_jsx_runtime5.jsx)(LinkDevice, { onDisconnect: props.onDisconnect });
    }
    return (0, import_jsx_runtime5.jsx)(ChainWidget, {});
  };
  const showScanQRCode = !props.disabledConnect && props.status !== AccountStatusEnum.EnableTradingWithoutConnected && props.status < AccountStatusEnum.EnableTrading;
  return (0, import_jsx_runtime5.jsxs)("div", { className: "oui-bg-base-9 oui-border-t oui-border-line-4", children: [
    (0, import_jsx_runtime5.jsxs)(
      Flex,
      {
        height: 64,
        gap: 1,
        justify: "end",
        itemAlign: "center",
        className: "oui-px-[14px]",
        children: [
          (0, import_jsx_runtime5.jsx)(BalanceWidget, {}),
          (0, import_jsx_runtime5.jsxs)(Flex, { gap: 2, children: [
            showScanQRCode && (0, import_jsx_runtime5.jsx)(ScanQRCodeWidget, {}),
            renderContent(),
            (0, import_jsx_runtime5.jsx)(MobileAccountMenuExtension, {})
          ] })
        ]
      }
    ),
    (0, import_jsx_runtime5.jsx)("div", { className: "oui-h-[env(safe-area-inset-bottom)]" })
  ] });
};
var LinkDevice = (props) => {
  const [open2, setOpen] = (0, import_react8.useState)(false);
  const { t } = useTranslation();
  const showDialog = () => {
    setOpen(true);
  };
  const hideDialog = () => {
    setOpen(false);
  };
  return (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
    (0, import_jsx_runtime5.jsx)(
      SimpleDialog,
      {
        open: open2,
        onOpenChange: setOpen,
        title: t("common.tips"),
        size: "xs",
        actions: {
          secondary: {
            label: t("common.cancel"),
            onClick: hideDialog,
            size: "md",
            fullWidth: true
          },
          primary: {
            label: t("connector.disconnect"),
            onClick: async () => {
              await props.onDisconnect();
              hideDialog();
            },
            size: "md",
            variant: "outlined",
            color: "danger"
          }
        },
        children: (0, import_jsx_runtime5.jsx)(Text2, { intensity: 54, size: "sm", children: t("linkDevice.scanQRCode.connected.description") })
      }
    ),
    (0, import_jsx_runtime5.jsxs)(
      Flex,
      {
        className: "oui-text-base-contrast oui-px-[6px]",
        intensity: 500,
        height: 28,
        r: "md",
        onClick: showDialog,
        children: [
          (0, import_jsx_runtime5.jsx)(DesktopIcon, {}),
          (0, import_jsx_runtime5.jsx)(Dot, {}),
          (0, import_jsx_runtime5.jsx)(LinkIcon, {}),
          (0, import_jsx_runtime5.jsx)(Dot, {}),
          (0, import_jsx_runtime5.jsx)(MobileIcon, {})
        ]
      }
    )
  ] });
};
var Dot = () => {
  return (0, import_jsx_runtime5.jsxs)(Flex, { className: "oui-gap-x-[1px] oui-px-[1px]", children: [
    (0, import_jsx_runtime5.jsx)(DotIcon, {}),
    (0, import_jsx_runtime5.jsx)(DotIcon, {}),
    (0, import_jsx_runtime5.jsx)(DotIcon, {})
  ] });
};
var DesktopIcon = (props) => (0, import_jsx_runtime5.jsx)(
  "svg",
  {
    width: "18",
    height: "18",
    viewBox: "0 0 18 18",
    fill: "currentColor",
    xmlns: "http://www.w3.org/2000/svg",
    ...props,
    children: (0, import_jsx_runtime5.jsx)("path", { d: "M4.5 2.93a2.25 2.25 0 0 0-2.25 2.25v5.25a.75.75 0 0 0-.75.75v1.5c0 1.272.93 2.25 2.25 2.25h10.5c1.318 0 2.25-.978 2.25-2.25v-1.5a.75.75 0 0 0-.75-.75V5.18a2.25 2.25 0 0 0-2.25-2.25zm0 1.5h9a.75.75 0 0 1 .75.75v5.25H3.75V5.18a.75.75 0 0 1 .75-.75M3 11.93h12v.75c0 .46-.277.75-.75.75H3.75c-.474 0-.75-.29-.75-.75z" })
  }
);
var MobileIcon = (props) => (0, import_jsx_runtime5.jsx)(
  "svg",
  {
    width: "18",
    height: "18",
    viewBox: "0 0 18 18",
    fill: "currentColor",
    xmlns: "http://www.w3.org/2000/svg",
    ...props,
    children: (0, import_jsx_runtime5.jsx)("path", { d: "M13.498 3.715a2.25 2.25 0 0 0-2.25-2.25h-4.5a2.25 2.25 0 0 0-2.25 2.25v10.5a2.25 2.25 0 0 0 2.25 2.25h4.5a2.25 2.25 0 0 0 2.25-2.25zm-1.5 0v9.75h-6v-9.75a.75.75 0 0 1 .75-.75h4.5a.75.75 0 0 1 .75.75m-2.25 11.25a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0" })
  }
);
var LinkIcon = (props) => (0, import_jsx_runtime5.jsxs)(
  "svg",
  {
    width: "12",
    height: "12",
    viewBox: "0 0 12 12",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    children: [
      (0, import_jsx_runtime5.jsx)(
        "path",
        {
          fillRule: "evenodd",
          clipRule: "evenodd",
          d: "M1.007 6a5 5 0 1 1 10 0 5 5 0 0 1-10 0m7.486-2.344A.6.6 0 0 1 8.91 3.5c.15 0 .305.048.418.156a.55.55 0 0 1 0 .798L5.254 8.337a.62.62 0 0 1-.837 0L2.67 6.673a.55.55 0 0 1 0-.798.62.62 0 0 1 .837 0l1.329 1.266z",
          fill: "url(#a)"
        }
      ),
      (0, import_jsx_runtime5.jsx)("defs", { children: (0, import_jsx_runtime5.jsxs)(
        "linearGradient",
        {
          id: "a",
          x1: "11.007",
          y1: "5.999",
          x2: "1.007",
          y2: "5.999",
          gradientUnits: "userSpaceOnUse",
          children: [
            (0, import_jsx_runtime5.jsx)("stop", { stopColor: "rgb(var(--oui-gradient-brand-end))" }),
            (0, import_jsx_runtime5.jsx)("stop", { offset: "1", stopColor: "rgb(var(--oui-gradient-brand-start))" })
          ]
        }
      ) })
    ]
  }
);
var DotIcon = (props) => (0, import_jsx_runtime5.jsxs)(
  "svg",
  {
    width: "2",
    height: "2",
    viewBox: "0 0 2 2",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    children: [
      (0, import_jsx_runtime5.jsx)(
        "path",
        {
          d: "M.667.334a.667.667 0 1 1 0 1.333.667.667 0 0 1 0-1.333",
          fill: "url(#a)"
        }
      ),
      (0, import_jsx_runtime5.jsx)("defs", { children: (0, import_jsx_runtime5.jsxs)(
        "linearGradient",
        {
          id: "a",
          x1: "1.333",
          y1: "1.001",
          x2: "0",
          y2: "1.001",
          gradientUnits: "userSpaceOnUse",
          children: [
            (0, import_jsx_runtime5.jsx)("stop", { stopColor: "rgb(var(--oui-gradient-brand-end))" }),
            (0, import_jsx_runtime5.jsx)("stop", { offset: "1", stopColor: "rgb(var(--oui-gradient-brand-start))" })
          ]
        }
      ) })
    ]
  }
);
var useBottomNavBarScript = () => {
  const { wrongNetwork, disabledConnect } = useAppContext();
  const { account, state } = useAccount();
  const onDisconnect = async () => {
    localStorage.removeItem("orderly_link_device");
    await account.disconnect();
  };
  return {
    wrongNetwork,
    disabledConnect,
    status: state.status,
    onDisconnect
  };
};
var BottomNavBarWidget = () => {
  const state = useBottomNavBarScript();
  return (0, import_jsx_runtime5.jsx)(BottomNavBar, { ...state });
};
init_hooks();
var usePortfolioSheetScript = () => {
  const { state, account, isMainAccount } = useAccount();
  const assets = useAssets();
  const marginRatio = useMarginRatioAndLeverage();
  const ee = useEventEmitter();
  const { t } = useTranslation();
  const subAccounts = state.subAccounts ?? [];
  const [showSliderTip, setShowSliderTip] = (0, import_react8.useState)(false);
  const onSettlePnL = (0, import_react8.useCallback)(async () => {
    return account.settle().catch((e) => {
      if (e.code == -1104) {
        Vt.error(t("settle.settlement.error"));
        return Promise.reject(e);
      }
      if (e.message.indexOf(
        "Signing off chain messages with Ledger is not yet supported"
      ) !== -1) {
        ee.emit("wallet:sign-message-with-ledger-error", {
          message: e.message,
          userAddress: account.address
        });
        return Promise.reject(e);
      }
      if (e?.code === "ACTION_REJECTED") {
        Vt.error(t("connector.userRejected"));
        return Promise.reject(e);
      }
    }).then((res) => {
      Vt.success(t("settle.settlement.requested"));
      return Promise.resolve(res);
    });
  }, [account, t]);
  const onDeposit = (0, import_react8.useCallback)(() => {
    modal.show(DepositAndWithdrawWithSheetId, {
      activeTab: "deposit"
    });
  }, []);
  const onWithdraw = (0, import_react8.useCallback)(() => {
    modal.show(DepositAndWithdrawWithSheetId, {
      activeTab: "withdraw"
    });
  }, []);
  const onTransfer = (0, import_react8.useCallback)(() => {
    modal.show(TransferSheetId);
  }, []);
  return {
    ...assets,
    ...marginRatio,
    onSettlePnL,
    onDeposit,
    onWithdraw,
    showSliderTip,
    setShowSliderTip,
    isMainAccount,
    onTransfer,
    hasSubAccount: subAccounts?.length > 0
  };
};
var useAssets = () => {
  const { hideAssets, setHideAssets } = useTradingLocalStorage();
  const toggleHideAssets = () => {
    setHideAssets(!hideAssets);
  };
  const { totalCollateral, freeCollateral, totalValue, availableBalance } = useCollateral({
    dp: 2
  });
  return {
    hideAssets,
    toggleHideAssets,
    totalCollateral,
    freeCollateral,
    totalValue,
    availableBalance
  };
};
var useMarginRatioAndLeverage = () => {
  const { t } = useTranslation();
  const [{ aggregated, totalUnrealizedROI }, positionsInfo] = usePositionStream();
  const { marginRatio, currentLeverage, mmr } = useMarginRatio();
  const marginRatioVal = (0, import_react8.useMemo)(() => {
    return Math.min(
      10,
      aggregated.notional === 0 ? positionsInfo["margin_ratio"](10) : marginRatio
    );
  }, [marginRatio, aggregated]);
  const { update, curLeverage, maxLeverage, leverageLevers } = useLeverage();
  const marks = (0, import_react8.useMemo)(() => {
    return leverageLevers?.map((e) => ({
      label: `${e}x`,
      value: e
    }));
  }, [leverageLevers]);
  const [leverage, setLeverage] = (0, import_react8.useState)(curLeverage ?? 0);
  const step = 100 / ((marks?.length || 0) - 1);
  const onLeverageChange = (leverage2) => {
    setLeverage(leverage2);
  };
  const onSave = async (leverage2) => {
    try {
      update({ leverage: leverage2 }).then(
        () => {
          Vt.success(t("leverage.updated"));
        },
        (err) => {
          Vt.error(err.message);
        }
      );
    } catch {
    }
  };
  const onValueCommit = (0, import_react8.useCallback)((value) => {
    onSave(Array.isArray(value) ? value[0] : value);
  }, []);
  const debouncedCommit = c(onValueCommit, 500);
  const onInputChange = (0, import_react8.useCallback)(
    (e) => {
      const parsed = Number.parseInt(e.target.value, 10);
      const raw = Number.isNaN(parsed) ? 0 : parsed;
      const clamped = Math.min(Math.max(raw, 1), maxLeverage);
      setLeverage(clamped);
      debouncedCommit(clamped);
    },
    [debouncedCommit, maxLeverage]
  );
  const onLeverageIncrease = () => {
    setLeverage((prev) => {
      onSave(prev + 1);
      return prev + 1;
    });
  };
  const onLeverageReduce = () => {
    setLeverage((prev) => {
      onSave(prev - 1);
      return prev - 1;
    });
  };
  return {
    aggregated,
    totalUnrealizedROI,
    positionsInfo,
    marginRatio,
    marginRatioVal,
    mmr,
    currentLeverage,
    step,
    marks,
    onLeverageChange,
    onLeverageIncrease,
    onLeverageReduce,
    onInputChange,
    isReduceDisabled: leverage <= 1,
    isIncreaseDisabled: leverage >= maxLeverage,
    onValueCommit,
    value: leverage,
    maxLeverage,
    onSaveLeverage: onSave
  };
};
function getMarginRatioColor(marginRatio, mmr) {
  if (mmr === null) {
    return { isRed: false, isYellow: false, isGreen: true };
  }
  const imr = mmr * 2;
  const high = marginRatio <= imr;
  const mid = marginRatio > imr && marginRatio < 1;
  const low = marginRatio >= 1;
  return { high, mid, low };
}
var RiskIndicator = (props) => {
  const { size = 20, className } = props;
  return (0, import_jsx_runtime5.jsx)(
    "div",
    {
      className: "oui-flex oui-justify-center oui-items-end",
      style: {
        background: "url(/images/riskLevelBg.png)",
        backgroundSize: "contain",
        backgroundRepeat: "no-repeat",
        backgroundPosition: "center bottom",
        width: `${size}px`,
        height: `14px`
      },
      children: (0, import_jsx_runtime5.jsx)(
        "div",
        {
          className: M(
            "oui-relative oui-w-[2px] oui-h-[2px] oui-rounded-full oui-bg-white after:oui-block after:oui-content-[''] after:oui-absolute after:oui-right-0 after:oui-w-[10px] after:oui-h-[2px] after:oui-bg-white/50",
            className
            // {
            //   "oui-rotate-0": value > 1,
            //   "oui-rotate-90": value >= 0.05 && value <= 1,
            //   "oui-rotate-180": value < 0.05,
            // }
          )
        }
      )
    }
  );
};
var PortfolioSheet = (props) => {
  return (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", gap: 4, width: "100%", children: [
    (0, import_jsx_runtime5.jsx)(Asset, { ...props }),
    (0, import_jsx_runtime5.jsx)(Divider, { className: "oui-w-full" }),
    (0, import_jsx_runtime5.jsx)(MarginRatio, { ...props }),
    (0, import_jsx_runtime5.jsx)(Buttons, { ...props })
  ] });
};
var Asset = (props) => {
  const { t } = useTranslation();
  const onUnsettleClick = (0, import_react8.useCallback)(() => {
    return modal.confirm({
      title: t("settle.settlePnl"),
      // maxWidth: "xs",
      content: (0, import_jsx_runtime5.jsx)(Text2, { intensity: 54, size: "xs", children: (0, import_jsx_runtime5.jsx)(Trans, { i18nKey: "settle.settlePnl.description" }) }),
      onCancel: () => {
        return Promise.reject();
      },
      onOk: () => {
        if (typeof props.onSettlePnL !== "function") {
          return Promise.resolve();
        }
        return props.onSettlePnL().catch((e) => {
        });
      }
    });
  }, [t]);
  const clsName = props.totalUnrealizedROI > 0 ? "oui-text-success-darken" : "oui-text-danger-darken";
  return (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", gap: 3, width: "100%", children: [
    (0, import_jsx_runtime5.jsxs)(Flex, { direction: "column", itemAlign: "start", width: "100%", children: [
      (0, import_jsx_runtime5.jsx)(
        Text2.formatted,
        {
          size: "2xs",
          intensity: 36,
          suffix: props.hideAssets ? (0, import_jsx_runtime5.jsx)(
            EyeIcon,
            {
              opacity: 1,
              size: 16,
              className: "oui-text-primary-light"
            }
          ) : (0, import_jsx_runtime5.jsx)(
            EyeCloseIcon,
            {
              opacity: 1,
              size: 16,
              className: "oui-text-primary-light"
            }
          ),
          onClick: () => {
            props.toggleHideAssets();
          },
          className: "oui-cursor-pointer",
          children: `${t("common.totalValue")} (USDC)`
        }
      ),
      (0, import_jsx_runtime5.jsx)(
        Text2.numeral,
        {
          size: "base",
          dp: 2,
          padding: false,
          visible: !props.hideAssets,
          children: props.totalValue ?? "--"
        }
      )
    ] }),
    (0, import_jsx_runtime5.jsxs)(Grid2, { cols: 2, rows: 1, width: "100%", children: [
      (0, import_jsx_runtime5.jsx)(
        Statistic,
        {
          label: `${t("common.unrealizedPnl")} (USDC)`,
          classNames: {
            label: "oui-text-2xs oui-text-base-contrast-36"
          },
          children: (0, import_jsx_runtime5.jsxs)(Flex, { gap: 1, children: [
            (0, import_jsx_runtime5.jsx)(
              Text2.pnl,
              {
                size: "xs",
                coloring: true,
                dp: 2,
                padding: false,
                visible: !props.hideAssets,
                children: props.aggregated.total_unreal_pnl
              }
            ),
            !props.hideAssets && (0, import_jsx_runtime5.jsx)(
              Text2.roi,
              {
                size: "xs",
                dp: 2,
                padding: false,
                rule: "percentages",
                prefix: "(",
                suffix: ")",
                className: clsName,
                children: props.totalUnrealizedROI
              }
            )
          ] })
        }
      ),
      (0, import_jsx_runtime5.jsx)(
        Statistic,
        {
          label: `${t("trading.asset.unsettledPnl")} (USDC)`,
          classNames: {
            label: "oui-text-2xs oui-text-base-contrast-36"
          },
          children: (0, import_jsx_runtime5.jsxs)(Flex, { justify: "between", width: "100%", children: [
            (0, import_jsx_runtime5.jsx)(
              Text2.pnl,
              {
                size: "xs",
                coloring: true,
                dp: 2,
                padding: false,
                visible: !props.hideAssets,
                children: props.aggregated.total_unsettled_pnl
              }
            ),
            (0, import_jsx_runtime5.jsxs)(
              "button",
              {
                className: "oui-flex oui-gap-1 oui-items-center",
                onClick: onUnsettleClick,
                children: [
                  (0, import_jsx_runtime5.jsx)(
                    RefreshIcon,
                    {
                      opacity: 1,
                      size: 12,
                      className: "oui-text-primary-light"
                    }
                  ),
                  (0, import_jsx_runtime5.jsx)(Text2, { size: "2xs", color: "primary", children: t("settle.settlePnl") })
                ]
              }
            )
          ] })
        }
      )
    ] })
  ] });
};
var MarginRatio = (props) => {
  const { t } = useTranslation();
  const { high, mid, low } = getMarginRatioColor(
    props.marginRatioVal,
    props.mmr
  );
  return (0, import_jsx_runtime5.jsxs)(Grid2, { cols: 2, rows: 1, width: "100%", children: [
    (0, import_jsx_runtime5.jsx)(
      Statistic,
      {
        label: t("trading.asset.marginRatio"),
        classNames: {
          label: "oui-text-2xs oui-text-base-contrast-36"
        },
        children: (0, import_jsx_runtime5.jsxs)(Flex, { gap: 2, children: [
          (0, import_jsx_runtime5.jsx)(
            Text2.numeral,
            {
              size: "xs",
              rule: "percentages",
              color: "primary",
              dp: 2,
              padding: false,
              visible: !props.hideAssets,
              children: props.marginRatioVal
            }
          ),
          !props.hideAssets && (0, import_jsx_runtime5.jsx)(
            RiskIndicator,
            {
              className: low ? "oui-rotate-0" : mid ? "oui-rotate-90" : high ? "oui-rotate-180" : ""
            }
          )
        ] })
      }
    ),
    (0, import_jsx_runtime5.jsx)(
      Statistic,
      {
        label: `${t("trading.asset.free&TotalCollateral")} (USDC)`,
        classNames: {
          label: "oui-text-2xs oui-text-base-contrast-36"
        },
        children: (0, import_jsx_runtime5.jsxs)(Flex, { justify: "start", width: "100%", gap: 1, children: [
          (0, import_jsx_runtime5.jsx)(
            Text2.collateral,
            {
              size: "xs",
              dp: 2,
              padding: false,
              visible: !props.hideAssets,
              children: props.freeCollateral
            }
          ),
          (0, import_jsx_runtime5.jsx)(Text2, { size: "xs", children: "/" }),
          (0, import_jsx_runtime5.jsx)(
            Text2.collateral,
            {
              size: "xs",
              dp: 2,
              padding: false,
              visible: !props.hideAssets,
              children: props.totalCollateral
            }
          )
        ] })
      }
    )
  ] });
};
var Buttons = (props) => {
  const { t } = useTranslation();
  if (props.isMainAccount) {
    return (0, import_jsx_runtime5.jsxs)(
      Grid2,
      {
        cols: props.hasSubAccount ? 3 : 2,
        rows: 1,
        gap: 3,
        className: "oui-grid-row-[1fr,1fr]",
        width: "100%",
        pt: 2,
        pb: 4,
        children: [
          (0, import_jsx_runtime5.jsx)(
            Button,
            {
              icon: (0, import_jsx_runtime5.jsx)(ArrowDownShortIcon, { color: "white", opacity: 0.8 }),
              size: "md",
              fullWidth: true,
              onClick: props.onDeposit,
              children: t("common.deposit")
            }
          ),
          props.hasSubAccount && (0, import_jsx_runtime5.jsx)(
            Button,
            {
              icon: (0, import_jsx_runtime5.jsx)(ArrowLeftRightIcon, { color: "white", opacity: 0.8 }),
              color: "gray",
              size: "md",
              onClick: props.onTransfer,
              "data-testid": "oui-testid-assetView-transfer-button",
              children: (0, import_jsx_runtime5.jsx)(Text2, { children: t("common.transfer") })
            }
          ),
          (0, import_jsx_runtime5.jsx)(
            Button,
            {
              icon: (0, import_jsx_runtime5.jsx)(ArrowUpShortIcon, { color: "white", opacity: 0.8 }),
              size: "md",
              fullWidth: true,
              className: "oui-bg-base-2 hover:oui-bg-base-2/50",
              onClick: props.onWithdraw,
              children: t("common.withdraw")
            }
          )
        ]
      }
    );
  }
  return (0, import_jsx_runtime5.jsx)(
    Button,
    {
      fullWidth: true,
      color: "secondary",
      size: "md",
      onClick: props.onTransfer,
      "data-testid": "oui-testid-assetView-transfer-button",
      children: (0, import_jsx_runtime5.jsx)(Text2, { children: t("common.transfer") })
    }
  );
};
var PortfolioSheetWidget = () => {
  const state = usePortfolioSheetScript();
  return (0, import_jsx_runtime5.jsx)(PortfolioSheet, { ...state });
};
init_fundingRate();
init_fundingRateModal_script();
init_fundingRateModal_ui();
init_fundingRateModal_widget();
var SortablePanel = (props) => {
  const { showIndicator, dragOverlay } = props;
  const nodeRef = (0, import_react8.useRef)(null);
  const [dimensions, setDimensions] = import_react8.default.useState(null);
  const sortableResult = useSortable({
    id: props.id
  });
  const {
    setNodeRef,
    attributes,
    listeners,
    transform,
    transition,
    isDragging,
    setActivatorNodeRef
  } = sortableResult;
  (0, import_react8.useEffect)(() => {
    if (isDragging && nodeRef.current && !dimensions) {
      const rect = nodeRef.current.getBoundingClientRect();
      setDimensions({
        width: rect.width,
        height: rect.height
      });
    } else if (!isDragging && dimensions) {
      setDimensions(null);
    }
  }, [isDragging, dimensions]);
  const combinedRef = (node) => {
    setNodeRef(node);
    nodeRef.current = node;
  };
  const style = {
    transform: CSS.Transform.toString(transform),
    transition
    // Ensure the item maintains its box model during drag
    // boxSizing: "border-box" as const,
  };
  if (isDragging && dimensions && !dragOverlay) {
    return (0, import_jsx_runtime5.jsx)(
      Box,
      {
        intensity: 900,
        r: "2xl",
        p: 3,
        className: M("oui-relative"),
        style: {
          ...style,
          width: dimensions.width,
          height: dimensions.height,
          minWidth: dimensions.width,
          minHeight: dimensions.height,
          maxWidth: dimensions.width,
          maxHeight: dimensions.height,
          // Placeholder styling with subtle visual feedback
          border: "1px solid rgb(var(--oui-color-primary))",
          backgroundImage: `repeating-linear-gradient(135deg, rgb(var(--oui-color-base-6)) 0px, rgb(var(--oui-color-base-6)) 4px, transparent 4px, transparent 8px)`
        }
      }
    );
  }
  return (0, import_jsx_runtime5.jsxs)(
    Box,
    {
      intensity: 900,
      r: "2xl",
      p: 3,
      width: "100%",
      className: M(
        "oui-relative",
        props.className
        // dragOverlay && "oui-scale-105",
      ),
      ref: combinedRef,
      style,
      children: [
        (0, import_jsx_runtime5.jsx)(
          "div",
          {
            className: M(
              "inner-content oui-transition-transform",
              dragOverlay && "oui-scale-95"
            ),
            children: props.children
          }
        ),
        showIndicator && (0, import_jsx_runtime5.jsx)(
          "button",
          {
            ...attributes,
            ...listeners,
            className: "oui-absolute oui-right-0 oui-top-4 oui-cursor-move oui-py-1",
            style: { touchAction: "none" },
            ref: setActivatorNodeRef,
            children: (0, import_jsx_runtime5.jsx)(
              IndicatorIcon,
              {
                className: M(
                  "oui-text-base-contrast-20 hover:oui-text-base-contrast-80"
                )
              }
            )
          }
        )
      ]
    }
  );
};
var IndicatorIcon = (props) => (0, import_jsx_runtime5.jsxs)(
  "svg",
  {
    width: "10",
    height: "16",
    viewBox: "0 0 10 16",
    fill: "currentColor",
    xmlns: "http://www.w3.org/2000/svg",
    ...props,
    children: [
      (0, import_jsx_runtime5.jsx)("rect", { x: "2", y: "2", width: "6", height: "2", rx: "1" }),
      (0, import_jsx_runtime5.jsx)("rect", { x: "2", y: "7", width: "6", height: "2", rx: "1" }),
      (0, import_jsx_runtime5.jsx)("rect", { x: "2", y: "12", width: "6", height: "2", rx: "1" })
    ]
  }
);
var toastId = "rwaOutsideMarketHoursNotify";
var RwaOutsideMarketHoursNotify = () => {
  const { t } = useTranslation();
  const closeNotify = () => {
    Vt.remove(toastId);
  };
  const { isMobile } = useScreen();
  (0, import_react8.useEffect)(() => {
    if (isMobile) {
      setTimeout(() => {
        Vt.dismiss(toastId);
      }, 3e3);
    }
  }, [isMobile]);
  return (0, import_jsx_runtime5.jsxs)(Flex, { r: "lg", className: "oui-bg-base-6 oui-relative oui-max-w-[348px]", children: [
    (0, import_jsx_runtime5.jsx)(
      Flex,
      {
        direction: "column",
        justify: "center",
        itemAlign: "center",
        className: "oui-bg-warning-darken oui-px-[5px] oui-h-full oui-rounded-tl-[10px] oui-rounded-bl-[10px]",
        children: (0, import_jsx_runtime5.jsx)(
          ExclamationFillIcon,
          {
            opacity: 1,
            className: "oui-text-base-6",
            size: 18
          }
        )
      }
    ),
    (0, import_jsx_runtime5.jsx)(Flex, { p: 4, mr: 1, children: (0, import_jsx_runtime5.jsx)(Text2, { size: "sm", intensity: 80, weight: "semibold", children: t("trading.rwa.outsideMarketHours.notify") }) }),
    (0, import_jsx_runtime5.jsx)(
      "button",
      {
        className: "oui-w-6 oui-h-6 oui-right-0 oui-top-0 oui-absolute oui-bg-base-8 oui-rounded-tr-[10px] oui-rounded-bl-[10px] oui-inline-flex oui-justify-center oui-items-center oui-gap-2.5 oui-cursor-pointer",
        onClick: closeNotify,
        children: (0, import_jsx_runtime5.jsx)(CloseIcon, { opacity: 1, className: "oui-text-base-contrast", size: 16 })
      }
    )
  ] });
};
var showRwaOutsideMarketHoursNotify = () => {
  Vt.custom((0, import_jsx_runtime5.jsx)(RwaOutsideMarketHoursNotify, {}), {
    id: toastId,
    duration: 3e3
  });
};
RwaOutsideMarketHoursNotify.displayName = "RwaOutsideMarketHoursNotify";
var useShowRwaCountdown = (symbol) => {
  const [showCountdown, setShowCountdown] = (0, import_react8.useState)(
    void 0
  );
  const [manulClose, setManulClose] = (0, import_react8.useState)(false);
  const { isRwa, open: open2, closeTimeInterval } = useGetRwaSymbolCloseTimeInterval(symbol);
  (0, import_react8.useEffect)(() => {
    if (manulClose) {
      return;
    }
    setShowCountdown(isRwa && open2 && !!closeTimeInterval);
  }, [isRwa, open2, closeTimeInterval, manulClose]);
  (0, import_react8.useEffect)(() => {
    return () => {
      setShowCountdown(void 0);
      setManulClose(false);
    };
  }, [symbol]);
  const closeCountdown = () => {
    setShowCountdown(false);
    setManulClose(true);
  };
  return (0, import_react8.useMemo)(() => {
    return {
      showCountdown,
      closeCountdown
    };
  }, [showCountdown, closeCountdown]);
};
var useSplitPersistent = (key2, defaulValue, dep) => {
  const size = (0, import_react8.useMemo)(() => {
    const size2 = localStorage.getItem(key2);
    if (size2) {
      return `${size2}%`;
    }
    return defaulValue;
  }, [key2, defaulValue, dep]);
  const setSize = (size2) => {
    localStorage.setItem(key2, size2);
  };
  return [size, setSize];
};
init_hooks();
init_tradingPageContext();
var useFirstTimeDeposit = () => {
  const { state } = useAccount();
  const { wrongNetwork, disabledConnect } = useAppContext();
  const { totalValue } = useCollateral({
    dp: 2
  });
  const unavailable = wrongNetwork || disabledConnect || state.status < AccountStatusEnum.EnableTrading && state.status !== AccountStatusEnum.EnableTradingWithoutConnected;
  const { startTime, endTime } = (0, import_react8.useMemo)(() => {
    const d = /* @__PURE__ */ new Date();
    const today = new Date(
      d.getFullYear(),
      d.getMonth(),
      d.getDate(),
      23,
      59,
      59
    );
    const endTime2 = today.getTime();
    const startTime2 = endTime2 - 90 * 24 * 60 * 60 * 1e3;
    return {
      startTime: startTime2,
      endTime: endTime2
    };
  }, []);
  const [_, { meta }] = useAssetsHistory(
    {
      startTime,
      endTime,
      page: 1,
      pageSize: 5,
      side: AssetHistorySideEnum.DEPOSIT,
      status: AssetHistoryStatusEnum.COMPLETED
    },
    {
      shouldUpdateOnWalletChanged: (data) => data.side === AssetHistorySideEnum.DEPOSIT && data.transStatus === AssetHistoryStatusEnum.COMPLETED
    }
  );
  return !unavailable && totalValue === 0 && meta?.total === 0;
};
var scrollBarWidth = 6;
var topBarHeight = 48;
var bottomBarHeight = 29;
var space = 8;
var symbolInfoBarHeight = 54;
var orderEntryMinWidth = 280;
var orderEntryMaxWidth = 360;
var orderbookMinWidth = 280;
var orderbookMaxWidth = 732;
var orderbookMinHeight = 464;
var orderbookMaxHeight = 728;
var tradindviewMinHeight = 320;
var tradingViewMinWidth = 540;
var dataListMaxHeight = 800;
var dataListInitialHeight = 350;
var ORDERLY_ORDER_ENTRY_SIDE_MARKETS_LAYOUT = "orderly_order_entry_side_markets_layout";
var ORDERLY_SIDE_MARKETS_MODE_KEY = "orderly_side_markets_mode";
var ORDERLY_HORIZONTAL_MARKETS_LAYOUT = "orderly_horizontal_markets_layout";
var useTradingScript = () => {
  const [openMarketsSheet, setOpenMarketsSheet] = (0, import_react8.useState)(false);
  const props = useTradingPageContext();
  const { state } = useAccount();
  const { t } = useTranslation();
  const { wrongNetwork, disabledConnect, restrictedInfo, onRouteChange } = useAppContext();
  const { hideAssets, setHideAssets } = useTradingLocalStorage();
  const isFirstTimeDeposit = useFirstTimeDeposit();
  const { totalValue } = useCollateral();
  const total = useDataTap(totalValue);
  const max2XL = useMediaQuery("(max-width: 1279px)");
  const min3XL = useMediaQuery("(min-width: 1440px)");
  const max4XL = useMediaQuery("(max-width: 1680px)");
  const [layout, setLayout] = useLocalStorage(
    ORDERLY_ORDER_ENTRY_SIDE_MARKETS_LAYOUT,
    "right"
  );
  const [marketLayout, setMarketLayout] = useLocalStorage(
    ORDERLY_HORIZONTAL_MARKETS_LAYOUT,
    "left"
  );
  const canTrade = (0, import_react8.useMemo)(() => {
    return !wrongNetwork && !disabledConnect && (state.status >= AccountStatusEnum.EnableTrading || state.status === AccountStatusEnum.EnableTradingWithoutConnected);
  }, [state.status, wrongNetwork, disabledConnect]);
  const onShowPortfolioSheet = () => {
    if (canTrade) {
      modal.sheet({
        title: t("trading.asset&Margin"),
        leading: props.bottomSheetLeading,
        content: (0, import_jsx_runtime5.jsx)(PortfolioSheetWidget, {})
      });
    }
  };
  const horizontalDraggable = (0, import_react8.useMemo)(() => min3XL, [min3XL]);
  const positionsState = useOrderEntryPositions({
    canTrade,
    isFirstTimeDeposit
  });
  const marketsCollapseState = useMarketsCollapse({ resizeable: min3XL });
  const observerState = useObserverOrderEntry({ max2XL });
  const marketsWidth = (0, import_react8.useMemo)(() => {
    switch (marketsCollapseState.panelSize) {
      case "small":
        return 0;
      case "middle":
        return 70;
      case "large":
        return 280;
      default:
        return 0;
    }
  }, [marketsCollapseState.panelSize]);
  const tradindviewMaxHeight = max2XL ? 1200 : 600;
  const dataListMinHeight = canTrade ? 379 : 277;
  const splitSizeState = useSplitSize({ dep: layout });
  const tradingViewHeightState = useExtraHeight({
    orderEntryViewRef: observerState.orderEntryViewRef,
    tradindviewMaxHeight,
    dataListMinHeight
  });
  const navigateToPortfolio = typeof onRouteChange === "function" ? () => onRouteChange({ href: "/portfolio", name: t("common.portfolio") }) : void 0;
  const map = {
    layout,
    onLayout: setLayout,
    marketLayout,
    onMarketLayout: setMarketLayout,
    max2XL,
    min3XL,
    max4XL,
    canTrade,
    openMarketsSheet,
    onOpenMarketsSheetChange: setOpenMarketsSheet,
    horizontalDraggable,
    ...marketsCollapseState,
    ...positionsState,
    ...splitSizeState,
    ...observerState,
    restrictedInfo,
    ...tradingViewHeightState,
    marketsWidth,
    tradindviewMaxHeight,
    dataListMinHeight,
    total,
    hideAssets,
    setHideAssets,
    onShowPortfolioSheet,
    navigateToPortfolio,
    isFirstTimeDeposit,
    symbolInfoBarHeight
  };
  return { ...props, ...map };
};
var useMarketsCollapse = (options2) => {
  const { resizeable } = options2;
  const [animating, setAnimating] = (0, import_react8.useState)(false);
  const [panelSize, setPanelSize] = useLocalStorage(ORDERLY_SIDE_MARKETS_MODE_KEY, "large");
  const onPanelSizeChange = (collapsed) => {
    setPanelSize(collapsed);
    setAnimating(true);
  };
  const memoizedPanelSize = (0, import_react8.useMemo)(() => {
    const normalized = panelSize === "large" ? "large" : "middle";
    return resizeable ? normalized : "middle";
  }, [resizeable, panelSize]);
  return {
    resizeable,
    panelSize: memoizedPanelSize,
    onPanelSizeChange,
    animating,
    setAnimating
  };
};
var useOrderEntryPositions = (options2) => {
  const { canTrade, isFirstTimeDeposit } = options2;
  const [positions, setPositions] = useLocalStorage(
    "orderly_assets_orderEntry_margin_positions",
    [0, 1, 2]
  );
  const updatePositions = (currentIdx, targetIdx) => {
    const pos2 = [...positions];
    if (currentIdx === 0 && targetIdx === pos2.length - 1) {
      pos2[targetIdx] = positions[currentIdx];
      for (let i = 0; i < pos2.length - 1; i++) {
        pos2[i] = positions[i + 1];
      }
    } else if (currentIdx === pos2.length - 1 && targetIdx === 0) {
      pos2[targetIdx] = positions[currentIdx];
      for (let i = 1; i < pos2.length; i++) {
        pos2[i] = positions[i - 1];
      }
    } else {
      [pos2[currentIdx], pos2[targetIdx]] = [pos2[targetIdx], pos2[currentIdx]];
    }
    setPositions(pos2);
  };
  const showPositionIcon = (0, import_react8.useMemo)(
    () => canTrade && !isFirstTimeDeposit,
    [canTrade, isFirstTimeDeposit]
  );
  const pos = (0, import_react8.useMemo)(() => {
    return showPositionIcon ? positions : [0, 1, 2];
  }, [showPositionIcon, positions]);
  return {
    positions: pos,
    showPositionIcon,
    updatePositions
  };
};
var useSplitSize = (options2) => {
  const { dep } = options2;
  const [mainSplitSize, setMainSplitSize] = useSplitPersistent(
    "orderly_main_split_size",
    `${orderEntryMinWidth}px`,
    dep
  );
  const [dataListSplitSize, setDataListSplitSize] = useSplitPersistent(
    "orderly_datalist_split_size",
    `${dataListInitialHeight}px`
    // undefined,
  );
  const [orderBookSplitSize, setOrderbookSplitSize] = useSplitPersistent(
    "orderly_orderbook_split_size",
    "280px",
    dep
  );
  const [dataListSplitHeightSM, setDataListSplitHeightSM] = useSplitPersistent(
    "orderly_datalist_split_height_sm",
    "350px"
  );
  const [orderBookSplitHeightSM, setOrderbookSplitHeightSM] = useSplitPersistent("orderly_orderbook_split_height_sm", "280px");
  return {
    orderBookSplitSize,
    setOrderbookSplitSize,
    dataListSplitSize,
    setDataListSplitSize,
    mainSplitSize,
    setMainSplitSize,
    dataListSplitHeightSM,
    setDataListSplitHeightSM,
    orderBookSplitHeightSM,
    setOrderbookSplitHeightSM
  };
};
var useObserverOrderEntry = (options2) => {
  const { max2XL } = options2;
  const [orderEntryHeight, setOrderEntryHeight] = (0, import_react8.useState)(0);
  const orderEntryViewRef = (0, import_react8.useRef)(null);
  (0, import_react8.useEffect)(() => {
    const element = orderEntryViewRef.current;
    if (!element || !max2XL) {
      return;
    }
    const resizeObserver = new ResizeObserver((entries) => {
      for (const entry of entries) {
        const height = entry.contentRect.height;
        if (height) {
          setOrderEntryHeight(height);
        }
      }
    });
    resizeObserver.observe(element);
    return () => {
      resizeObserver.unobserve(element);
    };
  }, [orderEntryViewRef, max2XL]);
  return {
    orderEntryViewRef,
    orderEntryHeight
  };
};
function getOffsetSizeNum(size) {
  if (size) {
    return `${100 - Math.min(Number(size), 100)}`;
  }
  return "";
}
function useExtraHeight(options2) {
  const { tradindviewMaxHeight, dataListMinHeight } = options2;
  const tradingviewAndOrderbookSplitRef = (0, import_react8.useRef)(null);
  const max2XLSplitRef = (0, import_react8.useRef)(null);
  const [extraHeight, setExtraHeight] = useLocalStorage(
    "orderly_order_entry_extra_height",
    0
  );
  const space2 = 10 + 12;
  const [dataListHeight, setDataListHeight] = useLocalStorage(
    "orderly_trading_data_list_height",
    dataListMinHeight
  );
  const onTradingviewAndOrderbookDragging = (preSize, nextSize) => {
    const boxHeight = tradingviewAndOrderbookSplitRef?.current?.boxHeight;
    if (!boxHeight) {
      return;
    }
    const splitTradingviewHeight = boxHeight * preSize / 100;
    const splitOrderbookHeight = boxHeight * nextSize / 100;
    const tradingviewHeight = Math.min(
      Math.max(splitTradingviewHeight, tradindviewMinHeight),
      tradindviewMaxHeight
    );
    const orderbookHeight = Math.min(
      Math.max(splitOrderbookHeight, orderbookMinHeight),
      orderbookMaxHeight
    );
    const orderEntryHeight = options2.orderEntryViewRef.current?.clientHeight || 0;
    if (splitOrderbookHeight >= orderbookHeight) {
      const offset = splitOrderbookHeight - orderbookHeight;
      setExtraHeight(Math.max(0, extraHeight - offset));
    } else if (tradingviewHeight + orderbookHeight < tradindviewMaxHeight + orderbookMaxHeight) {
      const height = tradingviewHeight + orderbookHeight + space2 + symbolInfoBarHeight;
      const offset = Math.max(0, height - orderEntryHeight);
      setExtraHeight(extraHeight + offset);
    }
  };
  const onDataListSplitHeightDragging = (preSize, nextSize) => {
    const boxHeight = max2XLSplitRef?.current?.boxHeight;
    if (!boxHeight) {
      return;
    }
    const splitDataListHeight = boxHeight * nextSize / 100;
    if (splitDataListHeight >= dataListMinHeight && splitDataListHeight <= dataListMaxHeight) {
      setDataListHeight(splitDataListHeight);
      const offset = splitDataListHeight - dataListHeight;
      if (offset > 0) {
        setExtraHeight(Math.max(0, extraHeight - offset));
      }
    }
  };
  return {
    max2XLSplitRef,
    tradingviewAndOrderbookSplitRef,
    onTradingviewAndOrderbookDragging,
    onDataListSplitHeightDragging,
    extraHeight,
    dataListHeight
  };
}
var LazyRiskRateWidget = import_react8.default.lazy(
  () => Promise.resolve().then(() => (init_riskRate(), riskRate_exports)).then((mod) => {
    return {
      default: mod.RiskRateWidget
    };
  })
);
var LazyAssetViewWidget = import_react8.default.lazy(
  () => Promise.resolve().then(() => (init_assetView(), assetView_exports)).then((mod) => {
    return {
      default: mod.AssetViewWidget
    };
  })
);
var LazyDataListWidget = import_react8.default.lazy(
  () => Promise.resolve().then(() => (init_dataList(), dataList_exports)).then((mod) => {
    return {
      default: mod.DataListWidget
    };
  })
);
var LazySwitchLayout = import_react8.default.lazy(
  () => Promise.resolve().then(() => (init_switchLayout(), switchLayout_exports)).then((mod) => {
    return {
      default: mod.SwitchLayout
    };
  })
);
var LazyOrderBookAndTradesWidget = import_react8.default.lazy(
  () => Promise.resolve().then(() => (init_orderBookAndTrades(), orderBookAndTrades_exports)).then((mod) => {
    return {
      default: mod.OrderBookAndTradesWidget
    };
  })
);
var DesktopLayout4 = (props) => {
  const {
    resizeable,
    panelSize,
    onPanelSizeChange,
    layout,
    onLayout,
    marketLayout,
    onMarketLayout,
    orderBookSplitSize,
    setOrderbookSplitSize,
    dataListSplitSize,
    setDataListSplitSize,
    mainSplitSize,
    setMainSplitSize,
    dataListSplitHeightSM,
    setDataListSplitHeightSM,
    orderBookSplitHeightSM,
    setOrderbookSplitHeightSM,
    max2XL,
    max4XL,
    animating,
    setAnimating,
    updatePositions,
    showPositionIcon,
    horizontalDraggable,
    marketsWidth,
    tradindviewMaxHeight,
    dataListMinHeight
  } = props;
  const { showCountdown, closeCountdown } = useShowRwaCountdown(props.symbol);
  const symbolInfoBarHeight2 = (0, import_react8.useMemo)(() => {
    return showCountdown ? 104 : 56;
  }, [showCountdown]);
  const { isRwa, open: open2 } = useGetRwaSymbolOpenStatus(props.symbol);
  (0, import_react8.useEffect)(() => {
    if (isRwa && !open2) {
      showRwaOutsideMarketHoursNotify();
    }
  }, [isRwa, open2, props.symbol]);
  const [tradingViewFullScreen] = useLocalStorage(
    TradingviewFullscreenKey,
    false
  );
  const [sortableItems, setSortableItems] = useLocalStorage(
    OrderEntrySortKeys,
    ["margin", "assets", "orderEntry"]
  );
  const dropAnimationConfig = (0, import_react8.useMemo)(() => {
    return {
      keyframes({
        transform
      }) {
        return [
          {
            transform: CSS.Transform.toString({
              ...transform.initial,
              scaleX: 1.05,
              scaleY: 1.05
            })
          },
          {
            transform: CSS.Transform.toString({
              ...transform.final,
              scaleX: 1,
              scaleY: 1
              // scaleX: 0.85,
              // scaleY: 0.85,
            })
          }
        ];
      },
      sideEffects: ({ active, dragOverlay }) => {
        active.node.style.opacity = "0";
        const innerElement = dragOverlay.node.querySelector(".inner-content");
        if (innerElement) {
          innerElement.classList.add("oui-animate-shake");
        }
        return () => {
          active.node.style.opacity = "";
        };
      }
    };
  }, []);
  const sensors = useSensors(
    useSensor(PointerSensor, {
      activationConstraint: {
        distance: 5
      }
    }),
    useSensor(KeyboardSensor, {
      coordinateGetter: sortableKeyboardCoordinates
    })
  );
  const [activeId, setActiveId] = (0, import_react8.useState)(null);
  function handleDragStart(event) {
    setActiveId(event.active.id);
  }
  function handleDragEnd(event) {
    const { active, over } = event;
    if (active.id !== over?.id && over) {
      const oldIndex = sortableItems.indexOf(active.id);
      const newIndex = sortableItems.indexOf(over.id);
      if (oldIndex !== -1 && newIndex !== -1) {
        const newItems = arrayMove(sortableItems, oldIndex, newIndex);
        setSortableItems(newItems);
      }
    }
    setActiveId(null);
  }
  const minScreenHeight = (0, import_react8.useMemo)(() => {
    return tradingViewFullScreen ? 0 : symbolInfoBarHeight2 + orderbookMaxHeight + dataListInitialHeight + space * 4;
  }, [tradingViewFullScreen]);
  const minScreenHeightSM = topBarHeight + bottomBarHeight + symbolInfoBarHeight2 + tradindviewMinHeight + orderbookMinHeight + dataListMinHeight + space * 4;
  const horizontalMarketsView = (0, import_jsx_runtime5.jsx)(
    HorizontalMarketsWidget,
    {
      symbol: props.symbol,
      onSymbolChange: props.onSymbolChange,
      maxItems: -1,
      dropdownPos: marketLayout === "bottom" ? "top" : "bottom"
    }
  );
  const containerPaddingX = (0, import_react8.useMemo)(() => max2XL ? 12 : 8, [max2XL]);
  const stickyHorizontalMarketsView = (0, import_jsx_runtime5.jsx)(
    Box,
    {
      className: M(
        "oui-bg-base-10",
        // -8 is for reducing the container's padding
        "oui-sticky oui-z-30 oui-mb-[-8px] oui-py-2",
        // Split line disabled for > 2xl screens
        !max2XL && "oui-mt-[-8px]"
      ),
      style: {
        bottom: 0,
        minWidth: (max2XL ? 1024 : 1440) - scrollBarWidth - containerPaddingX * 2
      },
      children: horizontalMarketsView
    }
  );
  const marketsWidget = (0, import_jsx_runtime5.jsx)(
    SideMarketsWidget,
    {
      resizeable,
      panelSize,
      onPanelSizeChange,
      symbol: props.symbol,
      onSymbolChange: props.onSymbolChange
    }
  );
  const marketsView = (0, import_jsx_runtime5.jsx)(
    Box,
    {
      intensity: 900,
      pt: 3,
      r: "2xl",
      height: "100%",
      width: marketsWidth,
      style: { minWidth: marketsWidth },
      className: "oui-transition-all oui-duration-150",
      onTransitionEnd: () => setAnimating(false),
      children: !animating && marketLayout === "left" && marketsWidget
    }
  );
  const symbolInfoBarView = (0, import_jsx_runtime5.jsx)(
    Box,
    {
      intensity: 900,
      r: "2xl",
      px: 3,
      width: "100%",
      style: {
        minHeight: symbolInfoBarHeight2,
        height: symbolInfoBarHeight2
      },
      children: (0, import_jsx_runtime5.jsx)(
        SymbolInfoBarFullWidget,
        {
          symbol: props.symbol,
          onSymbolChange: props.onSymbolChange,
          closeCountdown,
          showCountdown,
          trailing: (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
            LazySwitchLayout,
            {
              layout,
              onLayout,
              marketLayout,
              onMarketLayout
            }
          ) })
        }
      )
    }
  );
  const { library_path, ...restTradingViewConfig } = props.tradingViewConfig;
  const tradingviewWidget = (0, import_jsx_runtime5.jsx)(
    TradingviewWidget,
    {
      classNames: {
        root: M(
          tradingViewFullScreen ? "!oui-absolute oui-top-0 oui-left-0 oui-right-0 oui-bottom-0 oui-z-[40] oui-bg-base-10" : "oui-z-1"
        ),
        content: M(
          tradingViewFullScreen ? "oui-top-3 oui-bottom-3 oui-left-3 oui-right-3 oui-bg-base-9 oui-rounded-[16px] oui-overflow-hidden" : ""
        )
      },
      symbol: props.symbol,
      ...restTradingViewConfig,
      libraryPath: library_path
    }
  );
  const tradingView = (0, import_jsx_runtime5.jsx)(
    Box,
    {
      width: "100%",
      height: "100%",
      intensity: 900,
      r: "2xl",
      style: { flex: 1, minWidth: tradingViewMinWidth },
      className: "oui-overflow-hidden",
      children: tradingviewWidget
    }
  );
  const orderbookWidget = (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(LazyOrderBookAndTradesWidget, { symbol: props.symbol }) });
  const orderbookView = (0, import_jsx_runtime5.jsx)(
    Box,
    {
      r: "2xl",
      height: "100%",
      style: {
        minWidth: orderbookMinWidth,
        maxWidth: horizontalDraggable ? orderbookMaxWidth : orderbookMinWidth,
        width: orderBookSplitSize
      },
      className: "oui-overflow-hidden",
      children: orderbookWidget
    }
  );
  const dataListWidget = (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
    LazyDataListWidget,
    {
      current: void 0,
      symbol: props.symbol,
      sharePnLConfig: props.sharePnLConfig
    }
  ) });
  const dataListView = (0, import_jsx_runtime5.jsx)(
    Box,
    {
      intensity: 900,
      r: "2xl",
      p: 2,
      style: {
        height: dataListSplitSize,
        // height: `calc(100% - ${symbolInfoBarHeight}px - ${orderbookMaxHeight}px - ${space}px)`,
        minHeight: dataListInitialHeight
        // minHeight: `max(${dataListMinHeight}px, calc(100vh - ${symbolInfoBarHeight}px - ${orderbookMaxHeight}px - ${space}px))`,
      },
      className: "oui-overflow-hidden",
      children: dataListWidget
    }
  );
  const orderInteractionWidgets = (0, import_react8.useMemo)(() => {
    return {
      margin: {
        className: "",
        element: (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(LazyRiskRateWidget, {}) })
      },
      assets: {
        className: "oui-border oui-border-line-12",
        element: (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
          (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
            LazyAssetViewWidget,
            {
              isFirstTimeDeposit: props.isFirstTimeDeposit
            }
          ) }),
          (0, import_jsx_runtime5.jsx)(
            DepositStatusWidget,
            {
              className: "oui-mt-3 oui-gap-y-2",
              onClick: props.navigateToPortfolio
            }
          )
        ] })
      },
      orderEntry: {
        className: "",
        element: (0, import_jsx_runtime5.jsx)(
          OrderEntryWidget,
          {
            symbol: props.symbol,
            disableFeatures: props.disableFeatures
          }
        )
      }
    };
  }, [
    props.isFirstTimeDeposit,
    props.disableFeatures,
    props.navigateToPortfolio,
    props.symbol
  ]);
  const orderEntryView = (0, import_jsx_runtime5.jsx)(
    Flex,
    {
      gapY: 2,
      direction: "column",
      height: "100%",
      style: {
        minWidth: orderEntryMinWidth,
        maxWidth: horizontalDraggable ? orderEntryMaxWidth : orderEntryMinWidth,
        width: mainSplitSize
      },
      children: sortableItems.map((key2) => {
        return (0, import_jsx_runtime5.jsx)(
          SortablePanel,
          {
            id: key2,
            showIndicator: showPositionIcon,
            className: orderInteractionWidgets[key2].className,
            children: orderInteractionWidgets[key2].element
          },
          key2
        );
      })
    }
  );
  const renderTradingView = () => {
    if (max4XL && layout === "right") {
      return (0, import_jsx_runtime5.jsxs)(
        Flex,
        {
          gap: 2,
          className: "oui-flex-1 oui-overflow-hidden",
          style: { minWidth: marketsWidth + tradingViewMinWidth + space },
          children: [
            marketLayout === "left" && marketsView,
            tradingView
          ]
        }
      );
    }
    return tradingView;
  };
  const tradingViewAndOrderbookView = (0, import_jsx_runtime5.jsxs)(
    SplitLayout,
    {
      style: {
        // the style width is not set, and a child node style needs to be set to flex: 1 to adapt
        flex: 1,
        minHeight: orderbookMinHeight
        // maxHeight: orderbookMaxHeight,
      },
      onSizeChange: setOrderbookSplitSize,
      disable: !horizontalDraggable,
      children: [
        renderTradingView(),
        orderbookView
      ]
    }
  );
  const renderTradingViewAndOrderbookView = () => {
    if (max4XL && layout === "left") {
      return (0, import_jsx_runtime5.jsxs)(Flex, { gapX: 2, style: { minHeight: orderbookMinHeight }, height: "100%", children: [
        tradingViewAndOrderbookView,
        marketLayout === "left" && marketsView
      ] });
    }
    return tradingViewAndOrderbookView;
  };
  const mainView = (0, import_jsx_runtime5.jsxs)(
    Flex,
    {
      direction: "column",
      className: "oui-flex-1 oui-overflow-hidden",
      gap: 2,
      style: {
        minWidth: max4XL ? marketsWidth + tradingViewMinWidth + orderbookMinWidth + space * 2 : tradingViewMinWidth + orderbookMinWidth + space
      },
      children: [
        symbolInfoBarView,
        (0, import_jsx_runtime5.jsxs)(
          SplitLayout,
          {
            style: {
              // height: orderbookMaxHeight + dataListInitialHeight + space,
              maxHeight: `calc(100% - ${symbolInfoBarHeight2}px - ${space}px)`
            },
            className: "oui-w-full",
            mode: "vertical",
            onSizeChange: setDataListSplitSize,
            children: [
              renderTradingViewAndOrderbookView(),
              dataListView
            ]
          }
        )
      ]
    }
  );
  const onSizeChange = (width) => layout === "left" ? setMainSplitSize(getOffsetSizeNum(width)) : setMainSplitSize(width);
  if (max2XL) {
    return (0, import_jsx_runtime5.jsxs)(
      DndContext,
      {
        sensors,
        collisionDetection: closestCenter,
        onDragStart: handleDragStart,
        onDragEnd: handleDragEnd,
        modifiers: [restrictToVerticalAxis],
        children: [
          (0, import_jsx_runtime5.jsx)(
            SortableContext,
            {
              items: sortableItems,
              strategy: verticalListSortingStrategy,
              children: (0, import_jsx_runtime5.jsxs)(Box, { height: "100%", children: [
                marketLayout === "top" && (0, import_jsx_runtime5.jsx)(
                  Box,
                  {
                    className: M("oui-mt-2 oui-max-h-8 oui-px-3", props.className),
                    children: horizontalMarketsView
                  }
                ),
                (0, import_jsx_runtime5.jsxs)(
                  SplitLayout,
                  {
                    ref: props.max2XLSplitRef,
                    style: {
                      minHeight: minScreenHeightSM,
                      minWidth: 1024 - scrollBarWidth
                      // height: props.extraHeight ? props.extraHeight : undefined,
                    },
                    className: M(
                      "oui-flex oui-flex-1",
                      "oui-size-full oui-min-w-[1018px]",
                      "oui-px-3 oui-py-2",
                      props.className
                    ),
                    onSizeChange: setDataListSplitHeightSM,
                    onDragging: props.onDataListSplitHeightDragging,
                    mode: "vertical",
                    children: [
                      (0, import_jsx_runtime5.jsxs)(
                        Flex,
                        {
                          gapX: 2,
                          itemAlign: "stretch",
                          className: M(
                            "oui-flex-1",
                            layout === "left" && "oui-flex-row-reverse"
                          ),
                          style: {
                            minHeight: Math.max(
                              symbolInfoBarHeight2 + tradindviewMinHeight + orderbookMinHeight + space * 2,
                              props.orderEntryHeight
                            ),
                            maxHeight: symbolInfoBarHeight2 + tradindviewMaxHeight + orderbookMaxHeight + space * 2
                          },
                          children: [
                            (0, import_jsx_runtime5.jsxs)(
                              Flex,
                              {
                                height: "100%",
                                className: "oui-w-[calc(100%_-_280px_-_12px)] oui-flex-1",
                                direction: "column",
                                gapY: 2,
                                children: [
                                  symbolInfoBarView,
                                  (0, import_jsx_runtime5.jsxs)(
                                    Flex,
                                    {
                                      width: "100%",
                                      height: "100%",
                                      gapX: 2,
                                      itemAlign: "stretch",
                                      style: {
                                        minHeight: tradindviewMinHeight + orderbookMinHeight + space,
                                        maxHeight: tradindviewMaxHeight + orderbookMaxHeight + space
                                      },
                                      className: M(
                                        "oui-flex-1",
                                        layout === "left" && "oui-flex-row-reverse"
                                      ),
                                      children: [
                                        marketLayout === "left" && (0, import_jsx_runtime5.jsx)(
                                          Box,
                                          {
                                            intensity: 900,
                                            pt: 3,
                                            r: "2xl",
                                            width: marketsWidth,
                                            style: {
                                              minHeight: tradindviewMinHeight + orderbookMinHeight + space,
                                              maxHeight: tradindviewMaxHeight + orderbookMaxHeight + space
                                            },
                                            children: marketsWidget
                                          }
                                        ),
                                        (0, import_jsx_runtime5.jsxs)(
                                          SplitLayout,
                                          {
                                            ref: props.tradingviewAndOrderbookSplitRef,
                                            mode: "vertical",
                                            style: { width: `calc(100% - ${marketsWidth}px)` },
                                            className: "oui-flex-1",
                                            onSizeChange: setOrderbookSplitHeightSM,
                                            onDragging: props.onTradingviewAndOrderbookDragging,
                                            children: [
                                              (0, import_jsx_runtime5.jsx)(
                                                Box,
                                                {
                                                  width: "100%",
                                                  intensity: 900,
                                                  r: "2xl",
                                                  style: {
                                                    minHeight: tradindviewMinHeight,
                                                    maxHeight: tradindviewMaxHeight,
                                                    height: 1200
                                                  },
                                                  children: tradingviewWidget
                                                }
                                              ),
                                              (0, import_jsx_runtime5.jsx)(
                                                Box,
                                                {
                                                  r: "2xl",
                                                  height: "100%",
                                                  width: "100%",
                                                  style: {
                                                    minHeight: orderbookMinHeight,
                                                    maxHeight: orderbookMaxHeight,
                                                    height: orderBookSplitHeightSM
                                                  },
                                                  className: "oui-flex-1",
                                                  children: orderbookWidget
                                                }
                                              )
                                            ]
                                          }
                                        )
                                      ]
                                    }
                                  )
                                ]
                              }
                            ),
                            (0, import_jsx_runtime5.jsxs)(
                              Flex,
                              {
                                ref: props.orderEntryViewRef,
                                id: "orderEntryView",
                                gapY: 3,
                                direction: "column",
                                className: "oui-relative",
                                style: {
                                  width: orderEntryMinWidth,
                                  // force order entry render actual content height
                                  height: "max-content"
                                  // height:
                                  //   props.extraHeight && props.extraHeight > 100
                                  //     ? undefined
                                  // : "max-content",
                                },
                                children: [
                                  (0, import_jsx_runtime5.jsx)(
                                    Flex,
                                    {
                                      gapY: 2,
                                      direction: "column",
                                      height: "100%",
                                      style: {
                                        minWidth: orderEntryMinWidth,
                                        maxWidth: horizontalDraggable ? orderEntryMaxWidth : orderEntryMinWidth,
                                        width: mainSplitSize
                                      },
                                      children: sortableItems.map((key2) => {
                                        return (0, import_jsx_runtime5.jsx)(
                                          SortablePanel,
                                          {
                                            id: key2,
                                            showIndicator: showPositionIcon,
                                            className: orderInteractionWidgets[key2].className,
                                            children: orderInteractionWidgets[key2].element
                                          },
                                          key2
                                        );
                                      })
                                    }
                                  ),
                                  (0, import_jsx_runtime5.jsx)(Box, { height: props.extraHeight })
                                ]
                              }
                            )
                          ]
                        }
                      ),
                      (0, import_jsx_runtime5.jsx)(
                        Box,
                        {
                          intensity: 900,
                          r: "2xl",
                          p: 2,
                          style: {
                            height: dataListSplitHeightSM,
                            minHeight: Math.max(dataListMinHeight, props.dataListHeight),
                            maxHeight: dataListMaxHeight
                          },
                          className: "oui-overflow-hidden",
                          children: dataListWidget
                        }
                      ),
                      marketLayout === "bottom" && stickyHorizontalMarketsView
                    ]
                  }
                )
              ] })
            }
          ),
          (0, import_jsx_runtime5.jsx)(DragOverlay, { dropAnimation: dropAnimationConfig, children: activeId ? (0, import_jsx_runtime5.jsx)(
            SortablePanel,
            {
              id: activeId,
              showIndicator: showPositionIcon,
              dragOverlay: true,
              className: `${orderInteractionWidgets[activeId].className} oui-shadow-lg oui-shadow-base-9`,
              children: orderInteractionWidgets[activeId].element
            }
          ) : null })
        ]
      }
    );
  }
  return (0, import_jsx_runtime5.jsxs)(
    DndContext,
    {
      sensors,
      collisionDetection: closestCenter,
      onDragStart: handleDragStart,
      onDragEnd: handleDragEnd,
      modifiers: [restrictToVerticalAxis],
      children: [
        (0, import_jsx_runtime5.jsx)(
          SortableContext,
          {
            items: sortableItems,
            strategy: verticalListSortingStrategy,
            children: (0, import_jsx_runtime5.jsxs)(
              Flex,
              {
                style: {
                  minHeight: minScreenHeight,
                  minWidth: 1440 - scrollBarWidth
                },
                className: M(
                  props.className,
                  "oui-justify-start",
                  tradingViewFullScreen && "oui-relative oui-h-[calc(100vh-80px)] oui-w-screen oui-overflow-hidden !oui-p-0"
                ),
                width: "100%",
                p: 2,
                gap: 2,
                itemAlign: "stretch",
                direction: "column",
                children: [
                  marketLayout === "top" && horizontalMarketsView,
                  (0, import_jsx_runtime5.jsxs)(
                    Flex,
                    {
                      className: M(
                        "oui-flex-1 oui-overflow-hidden",
                        layout === "left" && "oui-flex-row-reverse"
                      ),
                      gap: 2,
                      children: [
                        !max4XL && marketLayout === "left" && marketsView,
                        (0, import_jsx_runtime5.jsxs)(
                          SplitLayout,
                          {
                            className: M("oui-flex oui-flex-1 oui-overflow-hidden"),
                            onSizeChange,
                            disable: !horizontalDraggable,
                            children: [
                              layout === "left" && orderEntryView,
                              mainView,
                              layout === "right" && orderEntryView
                            ]
                          }
                        )
                      ]
                    }
                  ),
                  marketLayout === "bottom" && stickyHorizontalMarketsView
                ]
              }
            )
          }
        ),
        (0, import_jsx_runtime5.jsx)(
          DragOverlay,
          {
            dropAnimation: dropAnimationConfig,
            children: activeId ? (0, import_jsx_runtime5.jsx)(
              SortablePanel,
              {
                id: activeId,
                showIndicator: showPositionIcon,
                dragOverlay: true,
                className: `${orderInteractionWidgets[activeId].className} oui-shadow-lg oui-shadow-base-9`,
                children: orderInteractionWidgets[activeId].element
              }
            ) : null
          }
        )
      ]
    }
  );
};
var Countdown = ({
  timeInterval
}) => {
  const { t } = useTranslation();
  if (!timeInterval) {
    return null;
  }
  if (timeInterval >= 1800) {
    return null;
  }
  const [days, hours, minutes, seconds] = breakDownSeconds(timeInterval);
  const daysStr = days.toString().padStart(2, "0");
  const hoursStr = hours.toString().padStart(2, "0");
  const minutesStr = minutes.toString().padStart(2, "0");
  const secondsStr = seconds.toString().padStart(2, "0");
  const renderStr = () => {
    if (days > 0) {
      return {
        s1: daysStr,
        s2: hoursStr,
        c1: t("common.dayShort"),
        c2: t("common.hourShort")
      };
    }
    if (hours > 0) {
      return {
        s1: hoursStr,
        s2: minutesStr,
        c1: t("common.hourShort"),
        c2: t("common.minuteShort")
      };
    }
    return {
      s1: minutesStr,
      s2: secondsStr,
      c1: t("common.minuteShort"),
      c2: t("common.secondShort")
    };
  };
  const { s1, s2, c1, c2 } = renderStr();
  return (0, import_jsx_runtime5.jsxs)(Flex, { className: "oui-text-base-contrast-54 oui-text-xs oui-font-normal", children: [
    (0, import_jsx_runtime5.jsx)(
      Box,
      {
        ml: 2,
        className: "oui-bg-base-7 oui-px-1 oui-rounded-md oui-text-base-contrast oui-min-w-[22px] oui-text-center oui-text-xs",
        children: s1
      }
    ),
    c1,
    " : ",
    (0, import_jsx_runtime5.jsx)(
      Box,
      {
        ml: 1,
        className: "oui-bg-base-7 oui-px-1 oui-rounded-md oui-text-base-contrast oui-min-w-[22px] oui-text-center oui-text-xs",
        children: s2
      }
    ),
    c2
  ] });
};
function breakDownSeconds(total) {
  const SEC_PER_DAY = 86400;
  const SEC_PER_HOUR = 3600;
  const SEC_PER_MIN = 60;
  const days = Math.floor(total / SEC_PER_DAY);
  const remAfterDays = total % SEC_PER_DAY;
  const hours = Math.floor(remAfterDays / SEC_PER_HOUR);
  const remAfterHours = remAfterDays % SEC_PER_HOUR;
  const mins = Math.floor(remAfterHours / SEC_PER_MIN);
  const secs = remAfterHours % SEC_PER_MIN;
  return [days, hours, mins, secs];
}
var LazyTopTabWidget = import_react8.default.lazy(
  () => Promise.resolve().then(() => (init_topTab(), topTab_exports)).then((mod) => {
    return { default: mod.TopTabWidget };
  })
);
var LazyOrderBookAndEntryWidget = import_react8.default.lazy(
  () => Promise.resolve().then(() => (init_orderBookAndEntry(), orderBookAndEntry_exports)).then((mod) => {
    return { default: mod.OrderBookAndEntryWidget };
  })
);
var LazyDataListWidget2 = import_react8.default.lazy(
  () => Promise.resolve().then(() => (init_dataList2(), dataList_exports2)).then((mod) => {
    return { default: mod.DataListWidget };
  })
);
var MaybeEqual = () => {
  return (0, import_jsx_runtime5.jsx)(
    "svg",
    {
      width: "8",
      height: "8",
      viewBox: "0 0 8 8",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      children: (0, import_jsx_runtime5.jsx)(
        "path",
        {
          d: "M6.456 3.48a3.5 3.5 0 0 0 .431-.567 9 9 0 0 0 .361-.654l-.648-.66-.107.185q-.071.12-.142.244a3 3 0 0 1-.27.396 1.4 1.4 0 0 1-.318.29.67.67 0 0 1-.38.114q-.307 0-.666-.194t-.742-.42q-.383-.227-.777-.42a1.7 1.7 0 0 0-.771-.194q-.401.001-.72.154a2.1 2.1 0 0 0-.57.404 2.6 2.6 0 0 0-.43.574 5 5 0 0 0-.307.646l.649.66q.165-.437.464-.833.298-.395.742-.396.347 0 .7.194.354.193.721.42.37.227.763.42.396.195.826.195.374.001.665-.155.29-.151.526-.404m.352 2.941a3.5 3.5 0 0 0 .431-.566q.196-.315.361-.654l-.648-.66-.107.184-.142.244a3 3 0 0 1-.27.396 1.4 1.4 0 0 1-.318.29.67.67 0 0 1-.38.115q-.307 0-.666-.195-.36-.193-.742-.42-.383-.226-.778-.42a1.7 1.7 0 0 0-.77-.194q-.401.001-.72.154a2.1 2.1 0 0 0-.57.405 2.6 2.6 0 0 0-.43.574 5 5 0 0 0-.307.646l.649.66q.165-.437.464-.833.297-.395.742-.396.347 0 .7.194.354.193.721.42.369.226.763.42.396.194.826.194.374.001.665-.154.29-.152.526-.404",
          fill: "#fff",
          fillOpacity: ".54"
        }
      )
    }
  );
};
var MobileLayout4 = (props) => {
  const { t } = useTranslation();
  const { isRwa, open: open2, closeTimeInterval } = useGetRwaSymbolInfo(props.symbol);
  (0, import_react8.useEffect)(() => {
    if (isRwa && !open2) {
      showRwaOutsideMarketHoursNotify();
    }
  }, [isRwa, open2, props.symbol]);
  const rwaStatusBar = (0, import_react8.useMemo)(() => {
    if (!isRwa) {
      return null;
    }
    if (closeTimeInterval === void 0) {
      return null;
    }
    const thresholdTime = 30 * 60;
    if ((closeTimeInterval ?? 0) > thresholdTime) {
      return null;
    }
    return (0, import_jsx_runtime5.jsxs)(
      Flex,
      {
        gap: 1,
        p: 2,
        justify: "start",
        itemAlign: "center",
        r: "lg",
        mt: 2,
        className: "oui-bg-success/15 oui-text-xs oui-text-base-contrast-54",
        children: [
          (0, import_jsx_runtime5.jsx)(NewsFillIcon, { color: "success", size: 16, className: "oui-flex-shrink-0" }),
          (0, import_jsx_runtime5.jsx)(Flex, { className: "oui-flex-1 oui-text-success", children: (0, import_jsx_runtime5.jsx)(Text2, { children: open2 ? t("trading.rwa.mWeb.outsideMarketHours.desc") : t("trading.rwa.mWeb.insideMarketHours.desc") }) }),
          (0, import_jsx_runtime5.jsx)(Countdown, { timeInterval: closeTimeInterval })
        ]
      }
    );
  }, [isRwa, open2, closeTimeInterval]);
  const symbolInfoBar = (0, import_jsx_runtime5.jsx)(
    SymbolInfoBarWidget,
    {
      symbol: props.symbol,
      onSymbol: () => props.onOpenMarketsSheetChange(true),
      trailing: (0, import_jsx_runtime5.jsxs)(
        Flex,
        {
          direction: "column",
          itemAlign: "end",
          className: "oui-cursor-pointer oui-text-[11px]",
          onClick: props.onShowPortfolioSheet,
          children: [
            (0, import_jsx_runtime5.jsxs)(Flex, { children: [
              (0, import_jsx_runtime5.jsx)(Text2, { intensity: 54, children: t("common.totalValue") }),
              (0, import_jsx_runtime5.jsx)(
                "button",
                {
                  onClick: (e) => {
                    e.stopPropagation();
                    e.preventDefault();
                    props.setHideAssets(!props.hideAssets);
                  },
                  className: "oui-px-1",
                  children: props.hideAssets ? (0, import_jsx_runtime5.jsx)(
                    EyeIcon,
                    {
                      color: "primary",
                      opacity: 1,
                      size: 16,
                      className: "oui-text-primary-light"
                    }
                  ) : (0, import_jsx_runtime5.jsx)(
                    EyeCloseIcon,
                    {
                      color: "primary",
                      opacity: 1,
                      size: 16,
                      className: "oui-text-primary-light"
                    }
                  )
                }
              ),
              (0, import_jsx_runtime5.jsx)(MaybeEqual, {})
            ] }),
            (0, import_jsx_runtime5.jsx)(
              Text2.numeral,
              {
                suffix: (0, import_jsx_runtime5.jsx)(Text2, { intensity: 20, children: " USDC" }),
                dp: 2,
                visible: !props.hideAssets,
                children: props.canTrade ? props.total ?? "--" : "--"
              }
            )
          ]
        }
      )
    }
  );
  const topBar = (0, import_jsx_runtime5.jsxs)(Box, { intensity: 900, className: "oui-rounded-xl", mx: 1, px: 3, py: 2, children: [
    symbolInfoBar,
    (0, import_jsx_runtime5.jsx)(
      SimpleSheet,
      {
        open: props.openMarketsSheet,
        onOpenChange: props.onOpenMarketsSheetChange,
        classNames: {
          body: "oui-h-full oui-pb-0",
          content: "!oui-w-[372px] !oui-max-w-[372px] !oui-p-0"
        },
        contentProps: { side: "left", closeable: false },
        children: (0, import_jsx_runtime5.jsx)(
          MarketsSheetWidget,
          {
            symbol: props.symbol,
            onSymbolChange: (symbol) => {
              props.onOpenMarketsSheetChange(false);
              props.onSymbolChange?.(symbol);
            }
          }
        )
      }
    ),
    rwaStatusBar
  ] });
  return (0, import_jsx_runtime5.jsx)("div", { className: "oui-relative oui-grid oui-gap-1 oui-bg-base-10", children: (0, import_jsx_runtime5.jsxs)("main", { className: "oui-hide-scrollbar oui-space-y-1 oui-overflow-y-auto", children: [
    topBar,
    (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(LazyTopTabWidget, { className: "oui-mx-1 oui-rounded-xl oui-bg-base-9" }) }),
    (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(LazyOrderBookAndEntryWidget, {}) }),
    (0, import_jsx_runtime5.jsx)(import_react8.default.Suspense, { fallback: null, children: (0, import_jsx_runtime5.jsx)(
      LazyDataListWidget2,
      {
        symbol: props.symbol,
        className: "oui-mx-1 oui-rounded-xl",
        sharePnLConfig: props.sharePnLConfig
      }
    ) })
  ] }) });
};
var Trading = (props) => {
  const { isMobile } = useScreen();
  if (isMobile) {
    return (0, import_jsx_runtime5.jsx)(MobileLayout4, { ...props });
  }
  return (0, import_jsx_runtime5.jsx)(
    DesktopLayout4,
    {
      className: "oui-h-[calc(100vh_-_48px_-_29px)] oui-bg-base-10",
      ...props
    }
  );
};
var TradingWidget = () => {
  const state = useTradingScript();
  return (0, import_jsx_runtime5.jsx)(Trading, { ...state });
};
init_utils();
init_tradingPageContext();
var TradingPageProvider = (props) => {
  const { symbol, children } = props;
  const symbolInfo = useSymbolsInfo()[symbol];
  const memoizedValue = (0, import_react8.useMemo)(() => {
    const basicSymbol = getBasicSymbolInfo(symbolInfo);
    return {
      ...props,
      symbolInfo: {
        ...basicSymbol,
        symbol
      }
    };
  }, [props, symbol, symbolInfo]);
  return (0, import_jsx_runtime5.jsx)(TradingPageContext.Provider, { value: memoizedValue, children });
};
var TradingPage = (props) => {
  return (0, import_jsx_runtime5.jsx)(
    TradingPageProvider,
    {
      symbol: props.symbol,
      tradingViewConfig: props.tradingViewConfig,
      onSymbolChange: props.onSymbolChange,
      disableFeatures: props.disableFeatures,
      overrideFeatures: props.overrideFeatures,
      referral: props.referral,
      tradingRewards: props.tradingRewards,
      bottomSheetLeading: props.bottomSheetLeading,
      sharePnLConfig: props.sharePnLConfig,
      children: (0, import_jsx_runtime5.jsx)(TradingWidget, {})
    }
  );
};
init_tradingPageContext();
init_hooks();
export {
  AssetView,
  AssetViewWidget,
  BottomNavBar,
  BottomNavBarWidget,
  BuySellRatioBar,
  DataList,
  DataListWidget,
  FundingRate,
  FundingRateDialogId,
  FundingRateModal,
  FundingRateModalWidget,
  FundingRateSheetId,
  FundingRateWidget,
  LastTrades,
  LastTradesWidget,
  OrderBook2 as OrderBook,
  OrderBookAndTrades,
  OrderBookAndTradesWidget,
  OrderBookWidget,
  PortfolioSheet,
  PortfolioSheetWidget,
  RiskRate,
  RiskRateWidget,
  SplitLayout,
  Trading,
  TradingPage,
  TradingPageContext,
  TradingPageProvider,
  TradingWidget,
  useBottomNavBarScript,
  useFundingRateModalScript,
  useFundingRateScript,
  useOrderBookAndTradesScript,
  useOrderBookScript,
  usePendingOrderCount,
  usePortfolioSheetScript,
  usePositionsCount,
  useTradingLocalStorage,
  useTradingPageContext,
  useTradingScript
};
//# sourceMappingURL=@orderly__network_trading.js.map
