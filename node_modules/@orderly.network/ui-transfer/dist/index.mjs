import { i18n, useTranslation, Trans } from '@orderly.network/i18n';
import { registerSimpleDialog, registerSimpleSheet, Tabs, TabPanel, ArrowDownSquareFillIcon, ArrowUpSquareFillIcon, ExtensionSlot, ExtensionPositionEnum, Box, textVariants, Flex, toast, Text, cn, Button, Spinner, Select, Input, inputFormatter, useScreen, modal, Tooltip, Tips, AlertDialog, EditIcon, ArrowLeftRightIcon, WalletIcon, DropdownMenuRoot, DropdownMenuTrigger, DropdownMenuPortal, DropdownMenuContent, ScrollArea, ChainIcon, Badge, CaretUpIcon, CaretDownIcon, SimpleDialog, CloseRoundFillIcon, ExclamationFillIcon, TokenIcon, Checkbox, CloseIcon } from '@orderly.network/ui';
import { forwardRef, useState, useMemo, useEffect, useCallback, useRef } from 'react';
import { useConfig, useWalletConnector, useLocalStorage, useConvert, useOdosQuote, useComputedLTV, useAccount, useAppStore, useTransfer, useSubAccountDataObserver, useSubAccountMaxWithdrawal, useTokensInfo, useEventEmitter, usePositionStream, useIndexPricesStream, useOrderlyContext, useDeposit, useAssetsHistory, useChains, useMemoizedFn, useWithdraw, useWalletTopic, useQuery, useSWR, useTokenInfo, useHoldingStream, useInternalTransfer, useDebouncedCallback, useTransferHistory, useBalanceTopic } from '@orderly.network/hooks';
import { Decimal, zero, praseChainIdToNumber, int2hex, toNonExponential, formatWithPrecision, isSolana } from '@orderly.network/utils';
import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
import { AccountStatusEnum, AssetHistoryStatusEnum, AssetHistorySideEnum, Arbitrum, EMPTY_OBJECT, isNativeTokenChecker, ChainNamespace, ABSTRACT_CHAIN_ID_MAP, nativeTokenAddress } from '@orderly.network/types';
import { AuthGuard, useAuthGuard } from '@orderly.network/ui-connector';
import { useAppContext, useAppConfig } from '@orderly.network/react-app';
import { account } from '@orderly.network/perp';
import { utils } from '@orderly.network/core';
import { ChainSelectorDialogId } from '@orderly.network/ui-chain-selector';
import { PublicKey } from '@solana/web3.js';
import { ethers } from 'ethers';
import { DefaultSolanaWalletAdapter } from '@orderly.network/default-solana-adapter';

// src/components/depositForm/depositForm.ui.tsx
var useLtvScript = () => {
  const { data, error, isLoading } = useQuery(
    "/v1/public/auto_convert_threshold",
    {
      errorRetryCount: 3,
      revalidateOnFocus: false
    }
  );
  const ltvThreshold = useMemo(() => {
    if (!data || isLoading) {
      return "-";
    }
    return `${new Decimal(data.ltv_threshold).mul(100).toNumber()}%`;
  }, []);
  return {
    ltv_threshold: ltvThreshold,
    negative_usdc_threshold: data?.negative_usdc_threshold,
    isLoading,
    error
  };
};
var TooltipIcon = forwardRef((props, ref) => {
  return /* @__PURE__ */ jsx(
    "svg",
    {
      width: 12,
      height: 12,
      viewBox: "0 0 12 12",
      fill: "currentColor",
      xmlns: "http://www.w3.org/2000/svg",
      focusable: false,
      ref,
      ...props,
      children: /* @__PURE__ */ jsx("path", { d: "M5.999 1.007a5 5 0 1 0 0 10 5 5 0 0 0 0-10m0 2.5a.5.5 0 1 1 0 1 .5.5 0 0 1 0-1m0 1.5a.5.5 0 0 1 .5.5v2.5a.5.5 0 0 1-1 0v-2.5a.5.5 0 0 1 .5-.5" })
    }
  );
});
var calculateTextColor = (val) => {
  if (val >= 0 && val < 50) {
    return "oui-text-success";
  } else if (val >= 50 && val < 80) {
    return "oui-text-warning";
  } else if (val >= 80) {
    return "oui-text-danger";
  } else {
    return "";
  }
};
var TooltipContent = (props) => {
  const { isLoading, ltv_threshold } = props;
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Box, { className: "oui-w-72 oui-max-w-72", children: [
    /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 80, children: t("transfer.LTV.description", {
      threshold: isLoading ? "-" : ltv_threshold
    }) }),
    " ",
    /* @__PURE__ */ jsx(
      "a",
      {
        href: "https://orderly.network/docs/introduction/trade-on-orderly/multi-collateral",
        target: "_blank",
        rel: "noreferrer",
        className: "oui-border-b oui-border-dashed oui-border-line-12 oui-text-2xs oui-text-primary",
        children: t("tradingLeaderboard.learnMore")
      }
    )
  ] });
};
var LtvUI = (props) => {
  const { t } = useTranslation();
  const { currentLtv, nextLTV, showDiff, ltv_threshold, isLoading } = props;
  return /* @__PURE__ */ jsxs(Flex, { width: "100%", itemAlign: "center", justify: "between", children: [
    /* @__PURE__ */ jsxs(Flex, { justify: "start", itemAlign: "center", children: [
      /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: t("transfer.LTV") }),
      /* @__PURE__ */ jsx(
        Tooltip,
        {
          className: "oui-p-2",
          content: /* @__PURE__ */ jsx(
            TooltipContent,
            {
              isLoading,
              ltv_threshold
            }
          ),
          children: /* @__PURE__ */ jsx(TooltipIcon, { className: "oui-ml-[2px] oui-cursor-pointer oui-text-base-contrast-36" })
        }
      )
    ] }),
    showDiff ? /* @__PURE__ */ jsxs(Flex, { itemAlign: "center", justify: "between", gap: 1, children: [
      /* @__PURE__ */ jsxs(
        Text,
        {
          size: "2xs",
          className: cn("oui-font-semibold", calculateTextColor(currentLtv)),
          children: [
            currentLtv,
            "%"
          ]
        }
      ),
      "\u2192",
      /* @__PURE__ */ jsxs(
        Text,
        {
          size: "2xs",
          className: cn("oui-font-semibold", calculateTextColor(nextLTV)),
          children: [
            nextLTV,
            "%"
          ]
        }
      )
    ] }) : /* @__PURE__ */ jsxs(
      Text,
      {
        size: "2xs",
        className: cn("oui-font-semibold", calculateTextColor(currentLtv)),
        children: [
          currentLtv,
          "%"
        ]
      }
    )
  ] });
};
var LtvWidget = (props) => {
  const state = useLtvScript();
  return /* @__PURE__ */ jsx(LtvUI, { ...props, ...state });
};

// src/types.ts
var DepositAction = /* @__PURE__ */ ((DepositAction2) => {
  DepositAction2[DepositAction2["Deposit"] = 0] = "Deposit";
  DepositAction2[DepositAction2["Approve"] = 1] = "Approve";
  DepositAction2[DepositAction2["ApproveAndDeposit"] = 2] = "ApproveAndDeposit";
  return DepositAction2;
})(DepositAction || {});
var WithdrawTo = /* @__PURE__ */ ((WithdrawTo2) => {
  WithdrawTo2["Wallet"] = "wallet";
  WithdrawTo2["Account"] = "accountId";
  return WithdrawTo2;
})(WithdrawTo || {});
var ActionButton = (props) => {
  const {
    disabled,
    loading,
    actionType,
    symbol = "USDC",
    onDeposit,
    onApprove,
    onApproveAndDeposit,
    networkId
  } = props;
  const { t } = useTranslation();
  const buttonParams = useMemo(() => {
    const params = {
      // [DepositAction.Approve]: {
      //   // Approve & Deposit
      //   children: t("transfer.deposit.approve"),
      //   onClick: onApprove,
      //   // approve not disabled button
      //   disabled: false,
      //   "data-testid": "oui-testid-deposit-dialog-approve-btn",
      // },
      [2 /* ApproveAndDeposit */]: {
        children: `${t("transfer.deposit.approve")} & ${t("common.deposit")}`,
        onClick: onApproveAndDeposit
        // approve not disabled button
        // disabled: false,
      },
      // [DepositAction.Increase]: {
      //   children: t("transfer.deposit.increase.symbol", { symbol }),
      //   onClick: onApprove,
      //   "data-testid": "oui-testid-deposit-dialog-increase-btn",
      // },
      [0 /* Deposit */]: {
        children: t("common.deposit"),
        onClick: onDeposit,
        "data-testid": "oui-testid-deposit-dialog-deposit-btn"
      }
    };
    return params[actionType];
  }, [onApprove, onDeposit, actionType, symbol, t]);
  const buttonSize = { initial: "md", lg: "lg" };
  return /* @__PURE__ */ jsx(Box, { className: "oui-w-full lg:oui-w-auto lg:oui-min-w-[184px]", children: /* @__PURE__ */ jsx(
    AuthGuard,
    {
      status: AccountStatusEnum.EnableTrading,
      networkId,
      buttonProps: {
        fullWidth: true,
        size: buttonSize
      },
      children: /* @__PURE__ */ jsx(
        Button,
        {
          fullWidth: true,
          disabled,
          loading,
          size: buttonSize,
          ...buttonParams
        }
      )
    }
  ) });
};
var AvailableTooltipMessage = ({
  tokenSymbol,
  decimals
}) => {
  const { t } = useTranslation();
  const { maxAmount } = useWithdraw({
    token: tokenSymbol,
    decimals
  });
  const amountText = useMemo(() => {
    if (maxAmount === void 0 || maxAmount === null) return "--";
    return maxAmount.toString();
  }, [maxAmount]);
  return /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 80, children: t("transfer.withdraw.available.tooltip", { amount: amountText }) });
};
var AvailableQuantity = (props) => {
  const { quantity, maxQuantity, token, loading } = props;
  const { t } = useTranslation();
  const { isMobile } = useScreen();
  const { getIndexPrice } = useIndexPricesStream();
  const name = token?.display_name || token?.symbol || "";
  const dp = token?.precision ?? token?.decimals ?? 2;
  const notional = useMemo(() => {
    if (props.notional !== void 0 && props.notional !== null) {
      return props.notional;
    }
    if (quantity && token?.symbol) {
      return new Decimal(quantity).mul(getIndexPrice(token?.symbol) || 1).toNumber();
    }
    return 0;
  }, [quantity, token?.symbol, props.notional]);
  return /* @__PURE__ */ jsxs(Flex, { px: 2, children: [
    /* @__PURE__ */ jsxs(Text, { size: "2xs", intensity: 36, children: [
      "$",
      /* @__PURE__ */ jsx(Text.numeral, { dp: 2, padding: false, rm: Decimal.ROUND_DOWN, children: notional })
    ] }),
    /* @__PURE__ */ jsxs(Flex, { gapX: 2, itemAlign: "center", className: "oui-ml-auto", children: [
      /* @__PURE__ */ jsxs(Flex, { gapX: 1, itemAlign: "center", children: [
        props.tooltipContent ? isMobile ? /* @__PURE__ */ jsx(
          "button",
          {
            type: "button",
            className: "oui-p-0",
            onClick: () => {
              if (token?.symbol) {
                const anyToken = token;
                modal.alert({
                  title: t("common.tips"),
                  message: /* @__PURE__ */ jsx(
                    AvailableTooltipMessage,
                    {
                      tokenSymbol: token.symbol,
                      decimals: anyToken?.token_decimal ?? token.decimals ?? token.precision
                    }
                  )
                });
              } else {
                modal.alert({
                  title: t("common.tips"),
                  message: props.tooltipContent
                });
              }
            },
            children: /* @__PURE__ */ jsx(
              Text,
              {
                size: "2xs",
                intensity: 36,
                className: "oui-cursor-pointer oui-border-b oui-border-dashed oui-border-line-12",
                children: `${t("common.available")}: `
              }
            )
          }
        ) : /* @__PURE__ */ jsx(
          Tooltip,
          {
            content: props.tooltipContent,
            className: "oui-max-w-[274px]",
            children: /* @__PURE__ */ jsx(
              Text,
              {
                size: "2xs",
                intensity: 36,
                className: "oui-cursor-pointer oui-border-b oui-border-dashed oui-border-line-12",
                children: `${t("common.available")}: `
              }
            )
          }
        ) : /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: `${t("common.available")}: ` }),
        loading ? /* @__PURE__ */ jsx(Spinner, { size: "sm" }) : /* @__PURE__ */ jsx(
          Text.numeral,
          {
            size: "2xs",
            intensity: 36,
            rm: Decimal.ROUND_DOWN,
            dp,
            padding: false,
            "data-testid": "oui-testid-withdraw_deposit-dialog-available-value",
            children: maxQuantity
          }
        ),
        /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: ` ${name}` })
      ] }),
      /* @__PURE__ */ jsx(
        Text,
        {
          size: "2xs",
          color: "primary",
          className: "oui-cursor-pointer oui-select-none",
          onClick: props.onClick,
          children: t("common.max")
        }
      )
    ] })
  ] });
};
var BrokerWallet = () => {
  const { t } = useTranslation();
  const { appIcons } = useAppConfig();
  const brokerName = useConfig("brokerName");
  const icon = useMemo(() => {
    const { secondary } = appIcons || {};
    if (!secondary?.img && secondary?.component) return null;
    if (secondary?.img) {
      return /* @__PURE__ */ jsx("img", { src: secondary?.img, className: "oui-w-5 oui-h-5" });
    }
    if (secondary?.component) {
      return /* @__PURE__ */ jsx(Fragment, { children: secondary.component });
    }
  }, [appIcons]);
  return /* @__PURE__ */ jsxs(Flex, { justify: "between", children: [
    /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 98, children: t("transfer.brokerAccount", { brokerName }) }),
    icon
  ] });
};
var ExchangeIcon = (props) => /* @__PURE__ */ jsx(
  "svg",
  {
    width: "12",
    height: "12",
    viewBox: "0 0 12 12",
    fill: "currentColor",
    xmlns: "http://www.w3.org/2000/svg",
    ...props,
    children: /* @__PURE__ */ jsx("path", { d: "M10.997 8.004a.5.5 0 0 0-.14-.36l-1.86-1.843-.703.703.984 1h-7.28a.5.5 0 0 0 0 1h7.28l-.984 1 .703.703 1.86-1.844a.5.5 0 0 0 .14-.36m-.5-4a.5.5 0 0 0-.5-.5H2.716l.984-1-.703-.703-1.859 1.843a.515.515 0 0 0 0 .719l1.86 1.844.702-.703-.984-1h7.281a.5.5 0 0 0 .5-.5" })
  }
);
var ArrowDownIcon = (props) => /* @__PURE__ */ jsx(
  "svg",
  {
    width: "20",
    height: "21",
    viewBox: "0 0 20 21",
    fill: "currentColor",
    xmlns: "http://www.w3.org/2000/svg",
    ...props,
    children: /* @__PURE__ */ jsx("path", { d: "M9.994 5.51a.83.83 0 0 0-.832.833v6.295l-2.498-2.471-1.17 1.17 3.902 3.929a.84.84 0 0 0 .599.244.84.84 0 0 0 .597-.244l3.903-3.928-1.171-1.171-2.498 2.471V6.343a.83.83 0 0 0-.832-.833" })
  }
);
var RefreshIcon = (props) => /* @__PURE__ */ jsx(
  "svg",
  {
    width: "12",
    height: "12",
    viewBox: "0 0 12 12",
    fill: "currentColor",
    xmlns: "http://www.w3.org/2000/svg",
    ...props,
    children: /* @__PURE__ */ jsx("path", { d: "M6.00342 1.49561C4.97642 1.49561 3.99542 1.84011 3.20642 2.46461C2.98992 2.63561 2.95692 2.9511 3.12842 3.1676C3.29992 3.3841 3.61492 3.4171 3.83142 3.2456C4.44492 2.7601 5.20392 2.4956 6.00342 2.4956C7.93642 2.4956 9.50342 4.0626 9.50342 5.9956H8.50342L10.0034 7.9956L11.5034 5.9956H10.5034C10.5034 3.5101 8.48892 1.49561 6.00342 1.49561ZM2.00342 3.9956L0.503418 5.9956H1.50342C1.50342 8.4811 3.51792 10.4956 6.00342 10.4956C7.03092 10.4956 8.01142 10.1516 8.80042 9.52659C9.01692 9.35559 9.04992 9.0401 8.87842 8.8236C8.70692 8.6071 8.39191 8.5741 8.17542 8.7456C7.56142 9.2316 6.80342 9.4956 6.00342 9.4956C4.07042 9.4956 2.50342 7.9286 2.50342 5.9956H3.50342L2.00342 3.9956Z" })
  }
);
var TransferVerticalIcon = (props) => /* @__PURE__ */ jsx(
  "svg",
  {
    width: "20",
    height: "21",
    viewBox: "0 0 20 21",
    fill: "currentColor",
    xmlns: "http://www.w3.org/2000/svg",
    ...props,
    children: /* @__PURE__ */ jsx("path", { d: "M12.385 4.565a.6.6 0 0 0-.427.167L9.764 6.944l.837.837 1.19-1.17v8.66a.595.595 0 0 0 1.19 0v-8.66l1.189 1.17.836-.837-2.193-2.212a.6.6 0 0 0-.428-.167m-4.758.595a.595.595 0 0 0-.595.595v8.661l-1.19-1.17-.836.836L7.2 16.294a.61.61 0 0 0 .855 0l2.194-2.212-.837-.836-1.19 1.17V5.755a.595.595 0 0 0-.594-.595" })
  }
);
var ChainSelect = (props) => {
  const { chains, value, wrongNetwork, loading, disabled } = props;
  const [open, setOpen] = useState(false);
  const { t } = useTranslation();
  const selectable = wrongNetwork || chains?.length > 1;
  const chainIcon = wrongNetwork ? /* @__PURE__ */ jsx(
    Flex,
    {
      width: 18,
      height: 18,
      intensity: 100,
      r: "full",
      justify: "center",
      itemAlign: "center",
      children: /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 80, children: "U" })
    }
  ) : /* @__PURE__ */ jsx(ChainIcon, { className: "oui-size-[18px]", chainId: value?.id });
  const chainName = wrongNetwork ? "Unknown" : value?.info?.network_infos?.name;
  const currentChain = chains.find((chain) => chain.chain_id === value?.id);
  const extendedCurrentChain = currentChain;
  const isCurrentChainSupported = !currentChain || extendedCurrentChain?.isSupported !== false;
  const renderRightIcon = () => {
    if (loading) {
      return /* @__PURE__ */ jsx(Spinner, { size: "sm" });
    }
    if (selectable) {
      return /* @__PURE__ */ jsx(ExchangeIcon, { className: "oui-text-base-contrast-54" });
    }
  };
  const trigger = /* @__PURE__ */ jsxs(
    Flex,
    {
      intensity: 500,
      className: cn(
        "oui-rounded-t-xl oui-rounded-b-sm oui-border oui-border-line",
        disabled ? "oui-cursor-not-allowed" : selectable ? "oui-cursor-pointer" : "oui-cursor-auto"
      ),
      height: 54,
      px: 3,
      justify: "between",
      itemAlign: "center",
      children: [
        /* @__PURE__ */ jsxs("div", { children: [
          /* @__PURE__ */ jsx(Flex, { children: /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 54, children: t("transfer.network") }) }),
          /* @__PURE__ */ jsxs(Flex, { gapX: 1, itemAlign: "center", children: [
            chainIcon,
            /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 80, children: chainName }),
            !isCurrentChainSupported && /* @__PURE__ */ jsx(Badge, { color: "danger", size: "xs", children: t("common.notSupported") })
          ] })
        ] }),
        renderRightIcon()
      ]
    }
  );
  const content = chains.map((chain, index) => {
    const extendedChain = chain;
    const isActive = chain.chain_id === value?.id;
    const isSupported = extendedChain.isSupported !== false;
    return /* @__PURE__ */ jsxs(
      Flex,
      {
        px: 2,
        r: "base",
        justify: "between",
        itemAlign: "center",
        className: cn(
          "oui-deposit-network-select-item",
          "oui-h-[30px]",
          isSupported ? "hover:oui-bg-base-5 oui-cursor-pointer" : "oui-cursor-not-allowed",
          isActive && "oui-bg-base-5",
          index !== 0 && "oui-mt-[2px]"
        ),
        onClick: async () => {
          if (!isSupported) return;
          setOpen(false);
          await props.onValueChange(chain);
        },
        children: [
          /* @__PURE__ */ jsxs(Flex, { gapX: 1, itemAlign: "center", children: [
            /* @__PURE__ */ jsx(
              ChainIcon,
              {
                className: cn("oui-size-[18px]", !isSupported && "oui-opacity-50"),
                chainId: chain.chain_id
              }
            ),
            /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: isSupported ? 54 : 36, children: chain.name }),
            !isSupported && /* @__PURE__ */ jsx(Badge, { color: "danger", size: "xs", children: t("common.notSupported") })
          ] }),
          isActive && /* @__PURE__ */ jsx(
            Box,
            {
              width: 4,
              height: 4,
              r: "full",
              className: "oui-deposit-network-select-active-dot oui-bg-[linear-gradient(270deg,#59B0FE_0%,#26FEFE_100%)]"
            }
          )
        ]
      },
      chain.chain_id
    );
  });
  return /* @__PURE__ */ jsxs(DropdownMenuRoot, { open: selectable ? open : false, onOpenChange: setOpen, children: [
    /* @__PURE__ */ jsx(DropdownMenuTrigger, { disabled, asChild: true, children: trigger }),
    /* @__PURE__ */ jsx(DropdownMenuPortal, { children: /* @__PURE__ */ jsx(
      DropdownMenuContent,
      {
        onCloseAutoFocus: (e) => e.preventDefault(),
        align: "start",
        sideOffset: 2,
        className: cn(
          "oui-deposit-token-select-dropdown-menu-content",
          "oui-bg-base-8 oui-p-1",
          "oui-w-[var(--radix-dropdown-menu-trigger-width)]",
          "oui-rounded-md oui-select-none"
        ),
        children: /* @__PURE__ */ jsx(ScrollArea, { children: /* @__PURE__ */ jsx("div", { className: "oui-max-h-[254px]", children: content }) })
      }
    ) })
  ] });
};
var CollateralContribution = (props) => {
  const { t } = useTranslation();
  const { value, precision = 6 } = props;
  return /* @__PURE__ */ jsxs(Flex, { width: "100%", itemAlign: "center", justify: "between", children: [
    /* @__PURE__ */ jsx(Flex, { itemAlign: "center", justify: "start", children: /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: t("transfer.deposit.collateralContribution") }) }),
    /* @__PURE__ */ jsxs(Flex, { itemAlign: "center", justify: "end", gap: 1, children: [
      /* @__PURE__ */ jsx(
        Text.numeral,
        {
          dp: precision,
          size: "2xs",
          rm: Decimal.ROUND_DOWN,
          className: cn("oui-font-semibold"),
          padding: false,
          children: value
        }
      ),
      /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, className: "oui-select-none", children: "USDC" })
    ] })
  ] });
};
var TooltipContent2 = () => {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(Box, { className: "oui-w-72 oui-max-w-72", children: [
    /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 80, children: t("portfolio.overview.column.collateralRatio.explain") }),
    " ",
    /* @__PURE__ */ jsx(
      "a",
      {
        href: "https://orderly.network/docs/introduction/trade-on-orderly/multi-collateral",
        target: "_blank",
        rel: "noreferrer",
        className: "oui-border-b oui-border-dashed oui-border-line-12 oui-text-2xs oui-text-primary",
        children: t("tradingLeaderboard.learnMore")
      }
    )
  ] });
};
var CollateralRatioUI = (props) => {
  const { t } = useTranslation();
  const { value } = props;
  return /* @__PURE__ */ jsxs(Flex, { width: "100%", itemAlign: "center", justify: "between", children: [
    /* @__PURE__ */ jsxs(Flex, { justify: "start", itemAlign: "center", children: [
      /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: t("portfolio.overview.column.collateralRatio") }),
      /* @__PURE__ */ jsx(Tooltip, { className: "oui-p-2", content: /* @__PURE__ */ jsx(TooltipContent2, {}), children: /* @__PURE__ */ jsx(TooltipIcon, { className: "oui-ml-[2px] oui-cursor-pointer oui-text-base-contrast-36" }) })
    ] }),
    /* @__PURE__ */ jsx(
      Text.numeral,
      {
        dp: 2,
        rm: Decimal.ROUND_DOWN,
        coloring: true,
        className: "oui-font-semibold",
        rule: "percentages",
        children: value
      }
    )
  ] });
};
var CollateralRatioWidget = (props) => {
  return /* @__PURE__ */ jsx(CollateralRatioUI, { ...props });
};
var ExchangeDivider = ({ icon }) => {
  return /* @__PURE__ */ jsxs(Flex, { height: 40, gapX: 3, children: [
    /* @__PURE__ */ jsx(Flex, { height: 1, className: "oui-flex-1 oui-bg-base-contrast-12" }),
    icon || /* @__PURE__ */ jsx(ArrowDownIcon, { className: "oui-text-primary-light" }),
    /* @__PURE__ */ jsx(Flex, { height: 1, className: "oui-flex-1 oui-bg-base-contrast-12" })
  ] });
};
var Fee = (props) => {
  const { dstGasFee, feeQty, feeAmount, dp, nativeSymbol } = props;
  const { t } = useTranslation();
  const onShowFee = () => {
    modal.alert({
      title: t("transfer.deposit.estGasFee"),
      message: /* @__PURE__ */ jsx(Text, { intensity: 36, size: "2xs", children: t("transfer.deposit.estGasFee.tooltip") })
    });
  };
  const showFeeQty = !!dstGasFee && dstGasFee !== "0";
  return /* @__PURE__ */ jsxs(
    Text,
    {
      size: "2xs",
      intensity: 36,
      className: "oui-cursor-pointer oui-border-b oui-border-dashed oui-border-line-12",
      onClick: onShowFee,
      children: [
        `${t("transfer.deposit.estGasFee")} \u2248 `,
        /* @__PURE__ */ jsxs(Text, { size: "2xs", intensity: 80, children: [
          "$",
          /* @__PURE__ */ jsx(Text.numeral, { dp: 2, padding: false, rm: Decimal.ROUND_UP, children: feeAmount }),
          " "
        ] }),
        showFeeQty && /* @__PURE__ */ jsxs("span", { children: [
          "(",
          /* @__PURE__ */ jsxs(Text, { intensity: 54, children: [
            /* @__PURE__ */ jsx(Text.numeral, { dp, padding: false, rm: Decimal.ROUND_UP, children: feeQty }),
            nativeSymbol
          ] }),
          ")"
        ] })
      ]
    }
  );
};
var MinimumReceived = (props) => {
  const { t } = useTranslation();
  const { value, symbol, precision = 6 } = props;
  return /* @__PURE__ */ jsxs(Flex, { width: "100%", itemAlign: "center", justify: "between", children: [
    /* @__PURE__ */ jsx(Flex, { itemAlign: "center", justify: "start", children: /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: t("transfer.swapDeposit.minimumReceived") }) }),
    /* @__PURE__ */ jsxs(Flex, { itemAlign: "center", justify: "end", gap: 1, children: [
      /* @__PURE__ */ jsx(
        Text.numeral,
        {
          dp: precision,
          size: "2xs",
          className: "oui-select-none",
          intensity: 80,
          children: value
        }
      ),
      /* @__PURE__ */ jsx(Text, { size: "2xs", className: "oui-select-none", intensity: 36, children: symbol })
    ] })
  ] });
};

// src/constants/yieldBearingAssets.ts
var YIELD_BEARING_ASSETS = [
  {
    symbol: "YUSD",
    displayName: "YUSD",
    apyApiUrl: "https://api.aegis.im/api/project-stats",
    apyPath: "data.efficient_apr",
    externalUrl: "https://aegis.im/"
  },
  {
    symbol: "deUSD",
    displayName: "deUSD",
    apyApiUrl: "https://api-deusd-prod-public.elixir.xyz/public/deusd_apy",
    apyPath: "deusd_apy",
    externalUrl: "https://www.elixir.xyz/deusd"
  }
];
function isYieldBearingAsset(symbol) {
  if (!symbol) return false;
  return YIELD_BEARING_ASSETS.some(
    (asset) => asset.symbol.toLowerCase() === symbol.toLowerCase()
  );
}
function getYieldBearingAsset(symbol) {
  if (!symbol) return void 0;
  return YIELD_BEARING_ASSETS.find(
    (asset) => asset.symbol.toLowerCase() === symbol.toLowerCase()
  );
}
function useYieldAPY(symbol) {
  const [apy, setApy] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);
  const assetConfig = getYieldBearingAsset(symbol);
  const fetchAPY = useCallback(async () => {
    if (!assetConfig) {
      setApy(null);
      setError(null);
      return;
    }
    setLoading(true);
    setError(null);
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), 1e4);
      const response = await fetch(assetConfig.apyApiUrl, {
        signal: controller.signal,
        headers: {
          Accept: "application/json"
        }
      });
      clearTimeout(timeoutId);
      if (!response.ok) {
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      const data = await response.json();
      const apyValue = getNestedValue(data, assetConfig.apyPath);
      if (typeof apyValue === "number" && !isNaN(apyValue)) {
        setApy(apyValue);
        setError(null);
      } else {
        throw new Error("Invalid APY data format");
      }
    } catch (err) {
      console.error(`[useYieldAPY] Failed to fetch APY for ${symbol}:`, err);
      setApy(null);
      if (err instanceof Error) {
        if (err.name === "AbortError") {
          setError("timeout");
        } else {
          setError(err.message);
        }
      } else {
        setError("unknown_error");
      }
    } finally {
      setLoading(false);
    }
  }, [symbol, assetConfig]);
  useEffect(() => {
    setApy(null);
    setError(null);
    if (assetConfig?.symbol === "deUSD") {
      fetchAPY();
    } else {
      setLoading(false);
    }
  }, [assetConfig, fetchAPY]);
  return {
    apy,
    loading,
    error,
    externalUrl: assetConfig?.externalUrl || null
  };
}
function getNestedValue(obj, path) {
  return path.split(".").reduce((current, key) => {
    return current?.[key];
  }, obj);
}
var TokenOption = (props) => {
  const {
    token,
    isActive,
    displayType,
    onTokenChange,
    open,
    isLoading,
    showBalance
  } = props;
  const { symbol, precision, insufficientBalance } = token;
  const { apy } = useYieldAPY(symbol);
  const showAPY = isYieldBearingAsset(symbol) && apy !== null;
  const { t } = useTranslation();
  if (displayType === "vaultBalance" && insufficientBalance) {
    return /* @__PURE__ */ jsx(
      Flex,
      {
        itemAlign: "center",
        justify: "between",
        px: 2,
        r: "base",
        className: cn(
          "group",
          "oui-h-[30px]",
          "oui-text-2xs oui-font-semibold",
          isActive && "oui-bg-base-5",
          props.index !== 0 && "oui-mt-[2px]",
          "oui-cursor-not-allowed"
        ),
        children: /* @__PURE__ */ jsxs(Flex, { itemAlign: "center", gapX: 1, children: [
          /* @__PURE__ */ jsx(
            TokenIcon,
            {
              url: token.logo_uri,
              name: symbol,
              className: "oui-size-[16px] oui-opacity-50"
            }
          ),
          /* @__PURE__ */ jsx(Text, { intensity: 36, className: "oui-max-w-[200px] oui-truncate", children: token.label }),
          /* @__PURE__ */ jsx(Badge, { color: "neutral", size: "xs", children: t("transfer.withdraw.InsufficientVaultBalance") })
        ] })
      },
      symbol
    );
  }
  const renderValue = () => {
    if (!showBalance) {
      return null;
    }
    if (isLoading && !token.amount) {
      return /* @__PURE__ */ jsx(Spinner, { size: "sm" });
    }
    return /* @__PURE__ */ jsx(
      Text.numeral,
      {
        rule: "price",
        dp: precision,
        rm: Decimal.ROUND_DOWN,
        ignoreDP: true,
        className: cn(
          "oui-text-base-contrast-80 group-hover:oui-text-base-contrast-54",
          isActive && "oui-text-base-contrast-54"
        ),
        children: token.amount
      }
    );
  };
  return /* @__PURE__ */ jsxs(
    Flex,
    {
      justify: "between",
      px: 2,
      r: "base",
      className: cn(
        "group",
        "oui-h-[30px] hover:oui-bg-base-5",
        "oui-text-2xs oui-font-semibold",
        "oui-cursor-pointer",
        isActive && "oui-bg-base-5",
        props.index !== 0 && "oui-mt-[2px]"
      ),
      onClick: () => {
        onTokenChange?.(token);
      },
      children: [
        /* @__PURE__ */ jsxs(Flex, { gapX: 1, itemAlign: "center", children: [
          /* @__PURE__ */ jsx(
            TokenIcon,
            {
              name: symbol,
              url: token.logo_uri,
              className: "oui-size-[16px]"
            }
          ),
          /* @__PURE__ */ jsx(
            Text,
            {
              className: cn(
                "oui-text-base-contrast-54 group-hover:oui-text-base-contrast-80",
                isActive && "oui-text-base-contrast-80",
                "oui-max-w-[200px] oui-truncate"
              ),
              children: token.label
            }
          ),
          showAPY && /* @__PURE__ */ jsx(
            Box,
            {
              height: 18,
              px: 2,
              r: "sm",
              className: "oui-rounded-[4px] oui-bg-success/[0.15]",
              children: /* @__PURE__ */ jsxs(Text, { size: "2xs", className: "oui-text-success", children: [
                apy.toFixed(1),
                "% APY"
              ] })
            }
          )
        ] }),
        renderValue()
      ]
    },
    symbol
  );
};
var QuantityInput = (props) => {
  const {
    token,
    tokens = [],
    classNames,
    label,
    status,
    hintMessage,
    hintSuffix,
    value,
    onValueChange,
    onTokenChange,
    loading,
    placeholder,
    formatters,
    vaultBalanceList,
    displayType,
    tokenValueFormatter,
    tokenShowCaret,
    balancesRevalidating,
    showBalance,
    ...rest
  } = props;
  const { t } = useTranslation();
  const inputRef = useRef(null);
  const [open, setOpen] = useState(false);
  const [width, setWidth] = useState(0);
  const tokenOptions = useMemo(() => {
    return tokens.map((token2) => {
      const currentToken = vaultBalanceList?.find(
        (item) => item.token === token2.symbol
      );
      const insufficientBalance = currentToken ? new Decimal(currentToken.balance ?? 0).lt(value ? Number(value) : 0) : false;
      return {
        ...token2,
        name: token2.display_name || token2.symbol,
        insufficientBalance
      };
    });
  }, [tokens, value, vaultBalanceList]);
  useEffect(() => {
    const rect = inputRef?.current?.getBoundingClientRect();
    setWidth(rect?.width || 0);
  }, [inputRef]);
  const _onTokenChange = (value2) => {
    const find = tokens.find((item) => item.symbol === value2);
    if (find) {
      onTokenChange?.(find);
    }
  };
  const selectable = tokens.length > 1;
  const selectOpen = selectable ? open : false;
  const optionRenderer = (item) => {
    const isActive = item.symbol === token?.symbol;
    return /* @__PURE__ */ jsx(
      TokenOption,
      {
        token: item,
        isActive,
        displayType,
        onTokenChange: (item2) => {
          onTokenChange?.(item2);
          setOpen(false);
        },
        open: selectOpen,
        isLoading: balancesRevalidating,
        showBalance
      }
    );
  };
  const prefix = /* @__PURE__ */ jsxs(Box, { children: [
    /* @__PURE__ */ jsx(Box, { className: "oui-absolute oui-top-0", children: /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: label || t("common.quantity") }) }),
    loading && /* @__PURE__ */ jsx(Box, { className: "oui-absolute oui-bottom-1", children: /* @__PURE__ */ jsx(Spinner, { size: "sm" }) })
  ] });
  const suffix = /* @__PURE__ */ jsx("div", { className: "oui-absolute oui-right-0", children: /* @__PURE__ */ jsx(
    Select.tokens,
    {
      open: selectOpen,
      onOpenChange: setOpen,
      disabled: rest.disabled,
      variant: "text",
      tokens: tokenOptions,
      value: token?.display_name || token?.symbol,
      size: rest.size,
      onValueChange: _onTokenChange,
      showIcon: true,
      valueFormatter: tokenValueFormatter,
      showCaret: tokenShowCaret,
      optionRenderer,
      contentProps: {
        onCloseAutoFocus: (event) => {
          event.preventDefault();
          inputRef.current?.focus();
        },
        onClick: (event) => {
          event.preventDefault();
          inputRef.current?.focus();
        },
        style: { width },
        align: "end",
        sideOffset: 5,
        className: "oui-border oui-border-line-6"
      }
    }
  ) });
  const message = /* @__PURE__ */ jsxs(Flex, { mt: 1, gapX: 1, px: 1, justify: "between", itemAlign: "center", children: [
    /* @__PURE__ */ jsxs(Flex, { gapX: 1, itemAlign: "center", children: [
      /* @__PURE__ */ jsx(
        Box,
        {
          width: 4,
          height: 4,
          r: "full",
          className: cn(
            status === "error" && "oui-bg-danger-light",
            status === "warning" && "oui-bg-warning-light"
          )
        }
      ),
      /* @__PURE__ */ jsx(
        Text,
        {
          size: "2xs",
          className: cn(
            status === "error" && "oui-text-danger-light",
            status === "warning" && "oui-text-warning-light"
          ),
          children: hintMessage
        }
      )
    ] }),
    hintSuffix && /* @__PURE__ */ jsx(Box, { className: "oui-flex oui-items-center oui-gap-1", children: hintSuffix })
  ] });
  const _placeholder = placeholder ?? (loading ? "" : "0");
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      Input,
      {
        ref: inputRef,
        autoComplete: "off",
        placeholder: _placeholder,
        prefix,
        suffix,
        value,
        onValueChange: (value2) => {
          onValueChange?.(value2);
        },
        formatters: formatters || [
          inputFormatter.numberFormatter,
          inputFormatter.dpFormatter(token?.precision ?? 2),
          inputFormatter.currencyFormatter
        ],
        ...rest,
        classNames: {
          ...classNames,
          root: cn(
            "oui-relative oui-h-[54px] oui-px-3",
            "oui-rounded-lg oui-border oui-border-line",
            status === "error" && "oui-outline-danger-light focus-within:oui-outline-danger-light",
            status === "warning" && "oui-outline-warning-light focus-within:oui-outline-warning-light",
            props.readOnly ? "oui-border-none oui-bg-base-6 focus-within:oui-outline-0" : "oui-bg-base-5",
            classNames?.root
          ),
          input: cn("oui-absolute oui-bottom-0", classNames?.input)
        }
      }
    ),
    hintMessage && message
  ] });
};
var options = [0.5, 1, 2];
var Slippage = (props) => {
  const { min = 0.2, max = 10, dp = 2 } = props;
  const [value, setValue] = useState();
  const [customValue, setCustomValue] = useState("");
  const [open, setOpen] = useState(false);
  const { t } = useTranslation();
  useEffect(() => {
    if (props.value && !options.includes(props.value)) {
      setCustomValue(props.value.toString());
    } else {
      setValue(props.value);
    }
  }, [props.value, open]);
  const showSlippage = () => {
    setOpen(true);
  };
  const hideSlippage = () => {
    setOpen(false);
  };
  const onClick = (val) => {
    setValue(val);
    setCustomValue("");
  };
  const onValueChange = (val) => {
    if (!val || Number(val) === 0) {
      setCustomValue(val);
      return;
    }
    const d = new Decimal(val);
    setValue(void 0);
    if (d.lt(min)) {
      setCustomValue(min.toString());
    } else if (d.gt(max)) {
      setCustomValue(max.toString());
    } else {
      setCustomValue(val);
    }
  };
  const getValue = () => customValue ? new Decimal(customValue)?.toNumber() : value;
  const onConfirm = () => {
    const val = getValue();
    if (!val) return Promise.resolve(true);
    props.onValueChange?.(val);
    hideSlippage();
    if (options.includes(val)) {
      setCustomValue("");
    }
    return Promise.resolve(true);
  };
  const disabled = !getValue();
  const errorMessage = useMemo(() => {
    return props.validate?.(getValue());
  }, [value, customValue, props.validate]);
  const content = /* @__PURE__ */ jsxs("div", { className: "oui-text-2xs", children: [
    /* @__PURE__ */ jsxs(Flex, { gapX: 1, itemAlign: "center", children: [
      /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 54, children: t("transfer.slippage.slippageTolerance") }),
      /* @__PURE__ */ jsx(
        Tips,
        {
          content: t("transfer.slippage.slippageTolerance.description"),
          title: t("common.tips"),
          classNamss: {
            trigger: "oui-mt-[2px] oui-size-4"
          }
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(Flex, { gapX: 2, mt: 3, children: [
      options.map((item) => {
        const isActive = value === item;
        return /* @__PURE__ */ jsx(
          SlippageItem,
          {
            value: item,
            isActive,
            onClick: () => {
              onClick(item);
            }
          },
          item
        );
      }),
      /* @__PURE__ */ jsx(
        Input,
        {
          suffix: "%",
          formatters: [
            inputFormatter.numberFormatter,
            inputFormatter.dpFormatter(dp)
          ],
          value: customValue,
          onValueChange,
          classNames: {
            root: cn(
              "oui-rounded-md oui-bg-base-6",
              "oui-h-[40px] oui-w-[74px]"
            ),
            input: "oui-text-base-contrast",
            additional: "oui-pl-1",
            suffix: "oui-text-base-contrast-36"
          }
        }
      )
    ] }),
    errorMessage && /* @__PURE__ */ jsx(Flex, { mt: 6, children: /* @__PURE__ */ jsx(
      Text,
      {
        size: "2xs",
        color: "warning",
        className: "oui-w-full oui-text-center",
        children: errorMessage
      }
    ) })
  ] });
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsx(
      AlertDialog,
      {
        open,
        onOpenChange: setOpen,
        title: t("transfer.slippage"),
        okLabel: t("common.save"),
        message: content,
        onOk: onConfirm,
        actions: { primary: { disabled } }
      }
    ),
    /* @__PURE__ */ jsxs(
      Flex,
      {
        width: "100%",
        justify: "between",
        className: "oui-cursor-pointer oui-select-none",
        onClick: showSlippage,
        children: [
          /* @__PURE__ */ jsx(Text, { intensity: 36, size: "2xs", children: t("transfer.slippage") }),
          /* @__PURE__ */ jsxs(Flex, { gapX: 1, children: [
            /* @__PURE__ */ jsxs(Text, { size: "2xs", color: "primaryLight", children: [
              props.value,
              "%"
            ] }),
            /* @__PURE__ */ jsx(EditIcon, { className: "oui-size-3 oui-text-primary-light" })
          ] })
        ]
      }
    )
  ] });
};
var SlippageItem = ({ value, isActive, onClick }) => {
  return /* @__PURE__ */ jsx(
    Flex,
    {
      intensity: 600,
      justify: "center",
      itemAlign: "center",
      r: "md",
      width: 74,
      height: 40,
      className: cn(
        "oui-cursor-pointer oui-select-none",
        isActive ? "oui-bg-primary-darken oui-text-primary-contrast/80" : "oui-text-base-contrast-80"
      ),
      onClick,
      children: /* @__PURE__ */ jsxs(Text, { size: "sm", children: [
        value,
        "%"
      ] })
    }
  );
};
var Web3Wallet = () => {
  const { t } = useTranslation();
  const { wallet, connectedChain } = useWalletConnector();
  const { state: accountState, account: account2 } = useAccount();
  const address = useMemo(() => {
    let address2 = accountState.address;
    if (connectedChain?.id && ABSTRACT_CHAIN_ID_MAP.has(parseInt(connectedChain?.id))) {
      address2 = account2.getAdditionalInfo()?.AGWAddress;
    }
    return address2;
  }, [wallet, accountState, account2, connectedChain]);
  return /* @__PURE__ */ jsxs(Flex, { justify: "between", children: [
    /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 98, children: t("transfer.web3Wallet.your") }),
    /* @__PURE__ */ jsxs(Flex, { gapX: 1, children: [
      /* @__PURE__ */ jsx(WalletIcon, { size: "xs", name: wallet?.label ?? "" }),
      /* @__PURE__ */ jsx(Text.formatted, { size: "sm", intensity: 54, rule: "address", children: address })
    ] })
  ] });
};
var BaseReminder = ({
  apy,
  loading,
  externalUrl,
  className,
  testId
}) => {
  const { t } = useTranslation();
  const formattedAPY = apy !== null ? apy.toFixed(1) : null;
  return /* @__PURE__ */ jsxs(
    Box,
    {
      className: cn(
        "oui-border-[0.5px] oui-border-primary oui-bg-primary/[0.12]",
        className
      ),
      px: 3,
      py: 2,
      r: "lg",
      width: "100%",
      children: [
        /* @__PURE__ */ jsxs(Flex, { direction: "column", itemAlign: "start", children: [
          formattedAPY !== null ? /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 98, children: /* @__PURE__ */ jsx(
            Trans,
            {
              i18nKey: "transfer.deposit.yieldReminder.earnAPY",
              values: { apy: formattedAPY },
              components: [/* @__PURE__ */ jsx("span", { className: "oui-text-success" }, "0")]
            }
          ) }) : /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 98, children: t("transfer.deposit.yieldReminder.earnRewards") }),
          /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 54, className: "oui-font-normal", children: t("transfer.deposit.yieldReminder.distribution") })
        ] }),
        /* @__PURE__ */ jsxs(Flex, { justify: "between", itemAlign: "center", mt: 3, children: [
          formattedAPY !== null && /* @__PURE__ */ jsxs(Flex, { gap: 1, children: [
            /* @__PURE__ */ jsx(TooltipIcon, {}),
            /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, className: "oui-font-normal", children: t("transfer.deposit.yieldReminder.disclaimer") })
          ] }),
          externalUrl && /* @__PURE__ */ jsxs(
            "a",
            {
              href: externalUrl,
              target: "_blank",
              rel: "noopener noreferrer",
              className: "oui-inline-flex oui-items-center oui-gap-[2px]  oui-font-normal oui-no-underline oui-transition-opacity hover:oui-opacity-80",
              children: [
                t("common.details"),
                /* @__PURE__ */ jsx(
                  "svg",
                  {
                    width: "14",
                    height: "14",
                    viewBox: "0 0 14 14",
                    fill: "none",
                    xmlns: "http://www.w3.org/2000/svg",
                    children: /* @__PURE__ */ jsx(
                      "path",
                      {
                        d: "M3.507 6.996c0-.322.26-.583.583-.583h4.407l-1.73-1.748.82-.82 2.749 2.732a.6.6 0 0 1 .17.42.6.6 0 0 1-.17.418l-2.75 2.731-.82-.82 1.73-1.747H4.09a.583.583 0 0 1-.583-.583",
                        fill: "#fff",
                        fillOpacity: ".54"
                      }
                    )
                  }
                )
              ]
            }
          )
        ] })
      ]
    }
  );
};
var DeUsdReminder = (props) => {
  return /* @__PURE__ */ jsx(BaseReminder, { ...props, testId: "deusd-yield-reminder" });
};
var YusdReminder = (props) => {
  return /* @__PURE__ */ jsx(BaseReminder, { ...props, testId: "yusd-yield-reminder" });
};
var YieldBearingReminder = ({
  symbol,
  className
}) => {
  const { apy, loading, externalUrl } = useYieldAPY(symbol);
  if (!isYieldBearingAsset(symbol)) {
    return null;
  }
  const isDeUsd = symbol?.toLowerCase() === "deusd";
  if (isDeUsd) {
    return /* @__PURE__ */ jsx(
      DeUsdReminder,
      {
        apy,
        loading,
        externalUrl,
        className
      }
    );
  }
  return /* @__PURE__ */ jsx(
    YusdReminder,
    {
      apy,
      loading,
      externalUrl,
      className
    }
  );
};
var ConvertRate = (props) => {
  const {
    sourceSymbol,
    targetSymbol,
    swapPrice,
    precision = 6,
    swapPriceInUSD
  } = props;
  const { t } = useTranslation();
  const formatSwapPrice = useMemo(() => {
    return formatWithPrecision(swapPrice, precision);
  }, [swapPrice, precision]);
  const suffix = swapPriceInUSD ? /* @__PURE__ */ jsxs("div", { children: [
    "($",
    /* @__PURE__ */ jsx(Text, { children: swapPriceInUSD }),
    ")"
  ] }) : void 0;
  return /* @__PURE__ */ jsxs(Flex, { width: "100%", itemAlign: "center", justify: "between", children: [
    /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: t("transfer.deposit.convertRate") }),
    /* @__PURE__ */ jsxs(
      Flex,
      {
        itemAlign: "center",
        gap: 1,
        className: cn(
          props.className,
          "oui-text-2xs oui-text-base-contrast-36"
        ),
        children: [
          /* @__PURE__ */ jsx(Text, { intensity: 80, children: "1" }),
          /* @__PURE__ */ jsx("span", { children: sourceSymbol }),
          /* @__PURE__ */ jsx("span", { children: "=" }),
          /* @__PURE__ */ jsx(Text, { intensity: 80, children: formatSwapPrice }),
          /* @__PURE__ */ jsx("span", { children: targetSymbol }),
          suffix
        ]
      }
    )
  ] });
};
var DepositTokenValueFormatter = ({ value, userMaxQty }) => {
  const { t } = useTranslation();
  const tipContent = /* @__PURE__ */ jsxs(Flex, { direction: "column", itemAlign: "start", children: [
    /* @__PURE__ */ jsxs(Text, { size: "2xs", weight: "semibold", intensity: 36, children: [
      t("transfer.depositCap.tooltip"),
      /* @__PURE__ */ jsxs(Text, { as: "span", size: "2xs", weight: "semibold", intensity: 80, children: [
        value,
        "."
      ] })
    ] }),
    /* @__PURE__ */ jsx(
      "a",
      {
        href: "https://orderly.network/docs/introduction/trade-on-orderly/multi-collateral#max-deposits-user",
        target: "_blank",
        rel: "noopener noreferrer",
        className: "oui-text-2xs oui-text-primary",
        children: t("common.learnMore")
      }
    )
  ] });
  return /* @__PURE__ */ jsxs(Flex, { direction: "column", itemAlign: "end", gapY: 1, children: [
    /* @__PURE__ */ jsxs(Flex, { gapX: 1, itemAlign: "center", children: [
      /* @__PURE__ */ jsx(TokenIcon, { name: value, className: "oui-size-[16px]" }),
      /* @__PURE__ */ jsx(Text, { weight: "semibold", intensity: 54, children: value }),
      /* @__PURE__ */ jsx(
        CaretDownIcon,
        {
          size: 12,
          className: "oui-text-base-contrast-54",
          opacity: 1
        }
      )
    ] }),
    /* @__PURE__ */ jsxs(Flex, { itemAlign: "center", className: "oui-gap-[2px]", children: [
      /* @__PURE__ */ jsxs(
        Text,
        {
          size: "2xs",
          intensity: 36,
          weight: "regular",
          className: "oui-leading-[10px]",
          children: [
            t("transfer.depositCap", "Deposit cap"),
            " ",
            /* @__PURE__ */ jsx(
              Text.numeral,
              {
                as: "span",
                size: "2xs",
                intensity: 80,
                weight: "regular",
                className: "oui-leading-[10px]",
                dp: 0,
                children: userMaxQty ?? 0
              }
            )
          ]
        }
      ),
      /* @__PURE__ */ jsx(Tips, { content: tipContent, title: t("common.tips") })
    ] })
  ] });
};
var Notice = (props) => {
  const { message, wrongNetwork } = props;
  const { t } = useTranslation();
  const content = useMemo(() => {
    if (wrongNetwork) {
      return t("connector.wrongNetwork.tooltip");
    }
    return message;
  }, [message, wrongNetwork, t]);
  if (content) {
    return /* @__PURE__ */ jsx(
      Box,
      {
        mb: 3,
        className: "oui-text-center oui-text-xs oui-text-warning-darken",
        children: content
      }
    );
  }
  return null;
};
var DepositForm = (props) => {
  const {
    sourceToken,
    targetToken,
    sourceTokens,
    targetTokens,
    onSourceTokenChange,
    onTargetTokenChange,
    quantity,
    collateralContributionQuantity,
    maxQuantity,
    maxDepositAmount,
    onQuantityChange,
    hintMessage,
    inputStatus,
    targetInputStatus,
    targetHintMessage,
    chains,
    currentChain,
    settingChain,
    onChainChange,
    actionType,
    onDeposit,
    onApprove,
    onApproveAndDeposit,
    wrongNetwork,
    balanceRevalidating,
    loading,
    disabled,
    networkId,
    fee,
    collateralRatio: collateralRatio2,
    currentLTV,
    nextLTV,
    slippage,
    onSlippageChange,
    swapMinReceived,
    needSwap,
    swapPrice,
    swapPriceInUSD,
    warningMessage,
    usdcToken,
    targetQuantity,
    targetQuantityLoading,
    batchBalancesRevalidating,
    showSourceDepositCap,
    showTargetDepositCap,
    slippageValidate,
    quantityNotional
  } = props;
  const { t } = useTranslation();
  const tokenValueFormatter = (value) => /* @__PURE__ */ jsx(
    DepositTokenValueFormatter,
    {
      value,
      userMaxQty: targetToken?.user_max_qty
    }
  );
  const renderContent = () => {
    return /* @__PURE__ */ jsxs(Flex, { direction: "column", itemAlign: "start", mt: 2, gap: 1, children: [
      needSwap && /* @__PURE__ */ jsx(
        ConvertRate,
        {
          sourceSymbol: sourceToken?.display_name || sourceToken?.symbol,
          targetSymbol: targetToken?.display_name || targetToken?.symbol,
          precision: targetToken?.precision,
          swapPrice,
          swapPriceInUSD
        }
      ),
      /* @__PURE__ */ jsx(CollateralRatioWidget, { value: collateralRatio2 }),
      /* @__PURE__ */ jsx(
        CollateralContribution,
        {
          precision: usdcToken?.precision,
          value: collateralContributionQuantity
        }
      ),
      /* @__PURE__ */ jsx(
        LtvWidget,
        {
          showDiff: typeof quantity !== "undefined" && Number(quantity) > 0,
          currentLtv: currentLTV,
          nextLTV
        }
      ),
      needSwap && /* @__PURE__ */ jsxs(Fragment, { children: [
        /* @__PURE__ */ jsx(
          Slippage,
          {
            value: slippage,
            onValueChange: onSlippageChange,
            min: 0.01,
            max: 50,
            validate: slippageValidate
          }
        ),
        /* @__PURE__ */ jsx(
          MinimumReceived,
          {
            value: swapMinReceived,
            symbol: targetToken?.symbol ?? "",
            precision: targetToken?.precision
          }
        )
      ] }),
      /* @__PURE__ */ jsx(Fee, { ...fee, nativeSymbol: props.nativeSymbol })
    ] });
  };
  return /* @__PURE__ */ jsxs(Box, { id: "oui-deposit-form", className: textVariants({ weight: "semibold" }), children: [
    /* @__PURE__ */ jsxs(Box, { className: "oui-mb-6 lg:oui-mb-8", children: [
      /* @__PURE__ */ jsx(Web3Wallet, {}),
      /* @__PURE__ */ jsxs(Box, { mt: 3, mb: 1, children: [
        /* @__PURE__ */ jsx(
          ChainSelect,
          {
            chains,
            value: currentChain,
            onValueChange: onChainChange,
            wrongNetwork,
            loading: settingChain,
            disabled: !props.isLoggedIn
          }
        ),
        /* @__PURE__ */ jsx(
          QuantityInput,
          {
            "data-testId": "oui-testid-deposit-dialog-quantity-input",
            classNames: {
              root: "oui-mt-[2px] oui-rounded-t-sm oui-rounded-b-xl"
            },
            value: quantity,
            onValueChange: onQuantityChange,
            token: sourceToken,
            tokens: sourceTokens,
            onTokenChange: onSourceTokenChange,
            status: inputStatus,
            hintMessage,
            tokenShowCaret: !showSourceDepositCap && sourceTokens?.length > 1,
            tokenValueFormatter: showSourceDepositCap ? tokenValueFormatter : void 0,
            disabled: !props.isLoggedIn,
            balancesRevalidating: batchBalancesRevalidating,
            showBalance: true
          }
        )
      ] }),
      /* @__PURE__ */ jsx(
        AvailableQuantity,
        {
          token: sourceToken,
          quantity,
          maxQuantity,
          notional: quantityNotional,
          loading: balanceRevalidating,
          onClick: () => {
            onQuantityChange(maxDepositAmount);
          }
        }
      ),
      /* @__PURE__ */ jsx(
        YieldBearingReminder,
        {
          symbol: targetToken?.symbol,
          className: "oui-mt-3"
        }
      ),
      /* @__PURE__ */ jsx(ExchangeDivider, {}),
      /* @__PURE__ */ jsx(BrokerWallet, {}),
      /* @__PURE__ */ jsx(
        QuantityInput,
        {
          classNames: {
            root: "oui-mt-3 oui-border-transparent focus-within:oui-outline-transparent"
          },
          token: targetToken,
          tokens: targetTokens,
          onTokenChange: onTargetTokenChange,
          value: targetQuantity,
          loading: targetQuantityLoading,
          disabled: !props.isLoggedIn,
          readOnly: true,
          status: targetInputStatus,
          hintMessage: targetHintMessage,
          tokenShowCaret: !showTargetDepositCap && targetTokens?.length > 1,
          tokenValueFormatter: showTargetDepositCap ? tokenValueFormatter : void 0
        }
      ),
      renderContent()
    ] }),
    /* @__PURE__ */ jsx(Notice, { message: warningMessage, wrongNetwork }),
    /* @__PURE__ */ jsx(Flex, { justify: "center", children: /* @__PURE__ */ jsx(
      ActionButton,
      {
        actionType,
        symbol: sourceToken?.symbol,
        disabled,
        loading,
        onDeposit,
        onApprove,
        onApproveAndDeposit,
        networkId
      }
    ) })
  ] });
};
function useActionType(options2) {
  const { allowance, quantity, maxQuantity } = options2;
  const actionType = useMemo(() => {
    const allowanceNum = Number(allowance);
    if (allowanceNum <= 0) {
      return 2 /* ApproveAndDeposit */;
    }
    const qty = Number(quantity);
    const maxQty = Number(maxQuantity);
    if (allowanceNum < qty && qty <= maxQty) {
      return 2 /* ApproveAndDeposit */;
    }
    return 0 /* Deposit */;
  }, [allowance, quantity, maxQuantity]);
  return actionType;
}
function useChainSelect() {
  const { t } = useTranslation();
  const networkId = useConfig("networkId");
  const [linkDeviceStorage] = useLocalStorage("orderly_link_device", {});
  const { connectedChain, settingChain, setChain } = useWalletConnector();
  const [chains, { findByChainId }] = useChains(networkId, {
    pick: "network_infos",
    filter: (chain) => chain.network_infos?.bridge_enable || chain.network_infos?.bridgeless
  });
  const currentChain = useMemo(() => {
    const chainId = connectedChain ? praseChainIdToNumber(connectedChain.id) : parseInt(linkDeviceStorage?.chainId);
    if (!chainId) return null;
    const chain = findByChainId(chainId);
    return {
      ...connectedChain,
      id: chainId,
      info: chain
    };
  }, [findByChainId, connectedChain, linkDeviceStorage]);
  const onChainChange = useCallback(
    async (chain) => {
      const chainInfo = findByChainId(chain.chain_id);
      if (!connectedChain) {
        return;
      }
      if (!chainInfo || chainInfo.network_infos?.chain_id === currentChain?.id) {
        return Promise.resolve();
      }
      return setChain({
        chainId: int2hex(Number(chainInfo.network_infos?.chain_id))
      }).then((switched) => {
        if (switched) {
          toast.success(t("connector.networkSwitched"));
        } else {
          toast.error(t("connector.switchChain.failed"));
        }
      }).catch((error) => {
        if (error && error.message) {
          toast.error(
            `${t("connector.switchChain.failed")}: ${error.message}`
          );
        }
      });
    },
    [currentChain, setChain, findByChainId]
  );
  return {
    chains,
    currentChain,
    settingChain,
    onChainChange
  };
}
var { collateralRatio, collateralContribution } = account;
var useCollateralValue = (params) => {
  const { sourceToken, targetToken, indexPrice } = params;
  const quantity = Number(params.quantity);
  const memoizedCollateralRatio = useMemo(() => {
    return collateralRatio({
      baseWeight: targetToken?.base_weight ?? 0,
      discountFactor: targetToken?.discount_factor ?? 0,
      collateralQty: quantity,
      collateralCap: sourceToken?.user_max_qty ?? quantity,
      indexPrice: indexPrice ?? 0
    });
  }, [targetToken, quantity, sourceToken?.user_max_qty, indexPrice]);
  const collateralContributionQuantity = useMemo(() => {
    return collateralContribution({
      collateralQty: quantity,
      collateralCap: sourceToken?.user_max_qty ?? quantity,
      collateralRatio: memoizedCollateralRatio.toNumber(),
      indexPrice: indexPrice ?? 0
    });
  }, [
    quantity,
    sourceToken?.user_max_qty,
    memoizedCollateralRatio,
    indexPrice
  ]);
  const currentLTV = useComputedLTV();
  const nextLTV = useComputedLTV({
    input: quantity,
    token: sourceToken?.symbol
  });
  return {
    collateralRatio: memoizedCollateralRatio.toNumber(),
    collateralContributionQuantity,
    currentLTV,
    nextLTV
  };
};
var useConvertThreshold = () => {
  const { data, error, isLoading } = useQuery(
    "/v1/public/auto_convert_threshold",
    {
      revalidateOnFocus: true,
      errorRetryCount: 3
    }
  );
  return {
    ltv_threshold: data?.ltv_threshold,
    negative_usdc_threshold: data?.negative_usdc_threshold,
    isLoading,
    error
  };
};
function sortTokensWithUSDCFirst(tokens = []) {
  const sortedTokens = [...tokens];
  sortedTokens.sort((a, b) => {
    if (a.symbol === "USDC") return -1;
    if (b.symbol === "USDC") return 1;
    return 0;
  });
  return sortedTokens;
}
var getUSDCToken = (tokens = []) => {
  let first;
  let usdce;
  let usdbc;
  for (const token of tokens) {
    if (first === void 0) first = token;
    if (token.symbol === "USDC") return token;
    if (token.symbol === "USDC.e") usdce = token;
    if (token.symbol === "USDbC") usdbc = token;
  }
  return usdce ?? usdbc ?? first;
};
var feeDecimalsOffset = (origin) => {
  return (origin ?? 2) + 3;
};
function checkIsAccountId(accountId) {
  const regex = /^0x[a-fA-F0-9]{64}$/;
  return regex.test(accountId);
}
function getTransferErrorMessage(errorCode) {
  if (errorCode === 34) {
    return i18n.t("transfer.internalTransfer.error.transferInProgress");
  }
  if (errorCode === 17) {
    return i18n.t("transfer.internalTransfer.error.withdrawalInProgress");
  }
  if (errorCode === 35) {
    return i18n.t("transfer.internalTransfer.error.accountIdNotExist");
  }
  if (errorCode === 37) {
    return i18n.t("transfer.internalTransfer.error.transferToSelf");
  }
  if (errorCode === 46) {
    return i18n.t("transfer.internalTransfer.error.transferToSubAccount");
  }
  return i18n.t("transfer.internalTransfer.error.default");
}
var DEPOSIT_ERROR_CODE_MAP = {
  AccessControlBadConfirmation: "0x6697b232",
  AccessControlUnauthorizedAccount: "0xe2517d3f",
  AccountIdInvalid: "0xc7ee9ce6",
  AddressZero: "0x9fabe1c1",
  BalanceNotEnough: "0x4b3815a6",
  BrokerNotAllowed: "0x59d9b863",
  CeffuAddressMismatch: "0xf67c6d78",
  DepositExceedLimit: "0xd969df24",
  EnumerableSetError: "0xa65b249b",
  InvalidSwapSignature: "0x06a0cf4a",
  InvalidTokenAddress: "0x1eb00b06",
  NativeTokenDepositAmountMismatch: "0xfa7c7537",
  NotRebalanceEnableToken: "0xad674ae6",
  NotZeroCodeLength: "0x623793c9",
  OnlyCrossChainManagerCanCall: "0x833d33e7",
  ReentrancyGuardReentrantCall: "0x3ee5aeb5",
  SwapAlreadySubmitted: "0x0b693144",
  TokenNotAllowed: "0xa29c4986",
  ZeroCodeLength: "0x30773dbb",
  ZeroDeposit: "0x56316e87",
  ZeroDepositFee: "0x93d3bb4d"
};
function getDepositKnownErrorMessage(message) {
  if (!message) {
    return "";
  }
  for (const key of Object.keys(DEPOSIT_ERROR_CODE_MAP)) {
    const value = DEPOSIT_ERROR_CODE_MAP[key];
    if (message.includes(value)) {
      return `${key} (${value})`;
    }
  }
  return "";
}
function useDepositAction(options2) {
  const { quantity, approve, deposit, swapDeposit, onSuccess, needSwap } = options2;
  const [isMutating, setIsMutating] = useState(false);
  const [depositError, setDepositError] = useState("");
  const ee = useEventEmitter();
  const { t } = useTranslation();
  const doDeposit = useCallback(async () => {
    try {
      await deposit();
      setDepositError("");
    } catch (err) {
      console.error("orderly deposit error", err);
      const knownErrorMessage = getDepositKnownErrorMessage(err.message);
      if (knownErrorMessage) {
        setDepositError(knownErrorMessage);
        toast.error(
          /* @__PURE__ */ jsxs("div", { children: [
            t("common.somethingWentWrong"),
            /* @__PURE__ */ jsx("br", {}),
            /* @__PURE__ */ jsxs("div", { className: "orderly-text-white/[0.54] orderly-text-xs", children: [
              t("common.details"),
              ": ",
              knownErrorMessage
            ] })
          ] })
        );
      } else {
        toast.error(err.message || t("common.somethingWentWrong"));
      }
      throw err;
    }
  }, [deposit, onSuccess, t, ee]);
  const onDeposit = useCallback(async () => {
    const num = Number(quantity);
    if (isNaN(num) || num <= 0) {
      toast.error(t("transfer.quantity.invalid"));
      return;
    }
    if (isMutating) return;
    setIsMutating(true);
    try {
      if (needSwap) {
        await swapDeposit?.();
      } else {
        await doDeposit();
      }
      toast.success(t("transfer.deposit.requested"));
      ee.emit("deposit:requested");
      onSuccess?.();
    } catch (err) {
    } finally {
      setIsMutating(false);
    }
  }, [quantity, isMutating, needSwap, doDeposit, swapDeposit, t]);
  const onApprove = useCallback(async () => {
    if (isMutating) return;
    setIsMutating(true);
    try {
      await approve(quantity);
      toast.success(t("transfer.deposit.approve.success"));
    } catch (err) {
      console.error("approve error", err);
      toast.error(
        err.message || err?.errorCode || t("transfer.deposit.approve.failed")
      );
      throw err;
    } finally {
      setIsMutating(false);
    }
  }, [approve, isMutating, quantity, t]);
  const onApproveAndDeposit = useCallback(async () => {
    if (isMutating) return;
    setIsMutating(true);
    try {
      await onApprove();
      await onDeposit();
    } catch (err) {
      console.error("approve and deposit error", err);
    } finally {
      setIsMutating(false);
    }
  }, [isMutating, onApprove, onDeposit]);
  return {
    isMutating,
    depositError,
    setDepositError,
    onApprove,
    onDeposit,
    onApproveAndDeposit
  };
}
function useDepositFee(options2) {
  const { nativeSymbol, depositFee = 0, getIndexPrice } = options2;
  const { account: account2 } = useAccount();
  const tokenInfo = useTokenInfo(nativeSymbol);
  const feeProps = useMemo(() => {
    const decimals = account2.walletAdapter?.chainNamespace === ChainNamespace.solana ? 9 : 18;
    const dstGasFee = new Decimal(depositFee.toString()).div(new Decimal(10).pow(decimals)).toString();
    const indexPrice = getIndexPrice(nativeSymbol);
    const feeAmount = new Decimal(dstGasFee).mul(indexPrice || 0).toString();
    return {
      dstGasFee,
      feeQty: dstGasFee,
      feeAmount,
      dp: tokenInfo?.decimals || 8
    };
  }, [depositFee, tokenInfo]);
  return feeProps;
}
var useMaxDepositAmount = (sourceToken, balance) => {
  const maxDepositAmount = useMemo(() => {
    const balanceDecimal = new Decimal(balance || 0).todp(
      sourceToken?.precision ?? 2,
      Decimal.ROUND_DOWN
    );
    if (sourceToken?.user_max_qty === -1 || sourceToken?.user_max_qty === void 0) {
      return balanceDecimal.toString();
    }
    if (sourceToken?.user_max_qty === 0) {
      return "0";
    }
    const userMaxQty = new Decimal(sourceToken.user_max_qty).todp(
      sourceToken?.precision ?? 2,
      Decimal.ROUND_DOWN
    );
    return balanceDecimal.lt(userMaxQty) ? balanceDecimal.toString() : userMaxQty.toString();
  }, [sourceToken, balance]);
  return maxDepositAmount;
};

// src/components/depositForm/hooks/useDepositQuantities.ts
var useDepositFormQuantities = ({
  sourceToken,
  targetToken,
  balance,
  quantity,
  needSwap,
  swapQuantity,
  swapPrice,
  getIndexPrice
}) => {
  const maxQuantity = useMemo(
    () => new Decimal(balance || 0).todp(sourceToken?.precision ?? 2, Decimal.ROUND_DOWN).toString(),
    [balance, sourceToken?.precision]
  );
  const maxDepositAmount = useMaxDepositAmount(sourceToken, balance);
  const targetQuantity = useMemo(() => {
    if (needSwap) {
      return swapQuantity ? new Decimal(swapQuantity)?.todp(targetToken?.precision ?? 6, Decimal.ROUND_DOWN).toString() : void 0;
    }
    return quantity;
  }, [needSwap, swapQuantity, quantity, targetToken?.precision]);
  const swapPriceInUSD = useMemo(() => {
    if (swapPrice && targetToken?.symbol) {
      const indexPrice2 = getIndexPrice(targetToken.symbol);
      if (!indexPrice2) return void 0;
      const num = new Decimal(swapPrice).mul(indexPrice2);
      return formatWithPrecision(num, 2);
    }
  }, [swapPrice, targetToken?.symbol, getIndexPrice]);
  const quantityNotional = useMemo(() => {
    const indexPrice2 = needSwap ? swapPrice : getIndexPrice(sourceToken?.symbol);
    if (quantity && indexPrice2) {
      return new Decimal(quantity).mul(indexPrice2).toNumber();
    }
    return 0;
  }, [quantity, sourceToken?.symbol, swapPrice, needSwap, getIndexPrice]);
  const indexPrice = useMemo(() => {
    return getIndexPrice(sourceToken?.symbol);
  }, [quantity, sourceToken?.symbol]);
  const swapIndexPrice = useMemo(() => {
    return getIndexPrice(sourceToken?.symbol);
  }, [swapPrice, sourceToken?.symbol]);
  return {
    maxQuantity,
    maxDepositAmount,
    targetQuantity,
    swapPriceInUSD,
    quantityNotional,
    indexPrice,
    swapIndexPrice
  };
};
var useDepositValidation = (options2) => {
  const {
    sourceToken,
    targetToken,
    quantity,
    targetQuantity,
    maxQuantity,
    isNativeToken,
    depositFee,
    depositFeeRevalidating,
    nativeBalanceRevalidating,
    dstGasFee,
    nativeSymbol,
    nativeBalance,
    account: account2,
    currentChain,
    depositError,
    needSwap
  } = options2;
  const { t } = useTranslation();
  const insufficientBalance = useMemo(() => {
    if (quantity && Number(quantity) > 0) {
      return new Decimal(quantity).gt(maxQuantity);
    }
    return false;
  }, [quantity, maxQuantity]);
  const insufficientNativeTotal = useMemo(() => {
    if (isNativeToken && quantity && Number(quantity) > 0 && !depositFeeRevalidating && !insufficientBalance) {
      const totalNeeded = new Decimal(quantity).add(dstGasFee || 0);
      return totalNeeded.gt(maxQuantity || 0);
    }
    return false;
  }, [
    isNativeToken,
    quantity,
    depositFeeRevalidating,
    insufficientBalance,
    dstGasFee,
    maxQuantity
  ]);
  const isExceedSourceTokenCap = useMemo(() => {
    if (sourceToken?.symbol !== "USDC" && sourceToken?.symbol === targetToken?.symbol && sourceToken?.is_collateral && sourceToken?.user_max_qty !== void 0 && sourceToken?.user_max_qty !== -1 && quantity && !isNaN(Number(quantity))) {
      return new Decimal(quantity).gt(sourceToken.user_max_qty);
    }
    return false;
  }, [sourceToken, targetToken, quantity]);
  const isExceedTargetTokenCap = useMemo(() => {
    if (targetToken?.symbol !== "USDC" && sourceToken?.symbol !== targetToken?.symbol && targetToken?.is_collateral && targetToken?.user_max_qty !== void 0 && targetToken?.user_max_qty !== -1 && targetQuantity && !isNaN(Number(targetQuantity))) {
      return new Decimal(targetQuantity).gte(
        new Decimal(targetToken.user_max_qty).mul(0.9)
      );
    }
    return false;
  }, [targetToken, targetQuantity]);
  const showSourceDepositCap = !needSwap && sourceToken?.user_max_qty !== void 0 && sourceToken?.user_max_qty !== -1;
  const showTargetDepositCap = needSwap && targetToken?.user_max_qty !== void 0 && targetToken?.user_max_qty !== -1;
  const depositExceedLimit = useMemo(() => {
    return depositError?.includes(DEPOSIT_ERROR_CODE_MAP.DepositExceedLimit);
  }, [depositError]);
  const insufficientGasMessage = useMemo(() => {
    if (nativeSymbol && quantity && Number(quantity) > 0 && // when insufficient balance, the input status is error, so we don't need to check gas balance
    !insufficientBalance && !depositFeeRevalidating && !nativeBalanceRevalidating && (account2.walletAdapter?.chainNamespace === ChainNamespace.solana || dstGasFee)) {
      const notEnoughGas = new Decimal(nativeBalance || 0).lt(dstGasFee || 0);
      const isNotSolBalance = Number(dstGasFee) === 0 && Number(nativeBalance) == Number(dstGasFee);
      if (notEnoughGas || isNotSolBalance) {
        return t("transfer.deposit.notEnoughGas", {
          token: nativeSymbol
        });
      }
    }
  }, [
    dstGasFee,
    quantity,
    nativeBalanceRevalidating,
    depositFeeRevalidating,
    t,
    nativeSymbol,
    insufficientBalance,
    account2,
    nativeBalance
  ]);
  const gasFeeMessage = useMemo(() => {
    if (insufficientNativeTotal) {
      return t("transfer.deposit.gasFee.error", {
        token: sourceToken?.symbol
      });
    }
  }, [t, insufficientNativeTotal, sourceToken?.symbol]);
  const globalMaxQtyMessage = useMemo(() => {
    if (sourceToken?.symbol !== "USDC" && sourceToken?.symbol === targetToken?.symbol && sourceToken?.is_collateral && depositExceedLimit) {
      return /* @__PURE__ */ jsx(
        Trans,
        {
          i18nKey: "transfer.deposit.globalMaxQty.error",
          values: {
            token: sourceToken?.symbol,
            chain: currentChain?.info?.network_infos?.name || ""
          },
          components: [
            /* @__PURE__ */ jsx(
              "a",
              {
                href: "https://orderly.network/docs/introduction/trade-on-orderly/multi-collateral#max-deposits-global",
                target: "_blank",
                rel: "noopener noreferrer",
                className: "oui-text-primary"
              },
              "0"
            )
          ]
        }
      );
    }
  }, [
    depositExceedLimit,
    sourceToken?.symbol,
    sourceToken?.is_collateral,
    targetToken?.symbol,
    currentChain,
    t
  ]);
  const feeWarningMessage = useMemo(() => {
    if (quantity && Number(quantity) > 0 && depositFee === 0n && !depositFeeRevalidating && account2.walletAdapter?.chainNamespace !== ChainNamespace.solana) {
      return t("transfer.deposit.feeUnavailable");
    }
  }, [t, quantity, depositFee, depositFeeRevalidating, account2]);
  const { inputStatus, hintMessage } = useMemo(() => {
    if (!quantity) {
      return { inputStatus: "default", hintMessage: "" };
    }
    if (insufficientBalance) {
      return {
        inputStatus: "error",
        hintMessage: t("transfer.insufficientBalance")
      };
    }
    if (isExceedSourceTokenCap) {
      return {
        inputStatus: "error",
        hintMessage: t("transfer.deposit.exceedCap")
      };
    }
    return { inputStatus: "default", hintMessage: "" };
  }, [t, insufficientBalance, isExceedSourceTokenCap]);
  const { targetInputStatus, targetHintMessage } = useMemo(() => {
    if (!targetQuantity || inputStatus === "error") {
      return { targetInputStatus: "default", targetHintMessage: "" };
    }
    if (isExceedTargetTokenCap) {
      return {
        targetInputStatus: "error",
        targetHintMessage: t("transfer.deposit.closeToMaxLimit")
      };
    }
    return { targetInputStatus: "default", targetHintMessage: "" };
  }, [t, targetQuantity, isExceedTargetTokenCap, inputStatus]);
  const slippageValidate = useCallback(
    (value) => {
      if (value && value >= 5) {
        return t("transfer.slippage.error.high");
      }
      return "";
    },
    [t]
  );
  const validationMessage = globalMaxQtyMessage || gasFeeMessage || feeWarningMessage || insufficientGasMessage;
  const depositDisabled = inputStatus === "error" || // if exceed collateral cap, disable deposit button
  isExceedSourceTokenCap || !!feeWarningMessage || !!insufficientNativeTotal || !!insufficientGasMessage;
  return {
    validationMessage,
    hintMessage,
    inputStatus,
    targetInputStatus,
    targetHintMessage,
    depositDisabled,
    showSourceDepositCap,
    showTargetDepositCap,
    slippageValidate
  };
};
function useNativeBalance(options2) {
  const { targetChain, fetchBalance } = options2;
  const decimal = targetChain?.network_infos?.currency_decimal;
  const key = useMemo(() => {
    const chainId = targetChain?.network_infos?.chain_id;
    if (!chainId || !decimal) {
      return null;
    }
    return ["nativeBalance", chainId, decimal];
  }, [fetchBalance, targetChain, decimal]);
  const fetcher2 = async () => {
    const balance2 = await fetchBalance(nativeTokenAddress, decimal);
    return balance2;
  };
  const { data: balance, isLoading } = useSWR(key, fetcher2, {
    revalidateOnFocus: true,
    refreshInterval: 3e3,
    errorRetryCount: 3
  });
  return { balance, isLoading };
}
var useOrderlyTokens = (currentChain) => {
  const [orderlyTokens, setOrderlyTokens] = useState([]);
  const onChainInited = useCallback((chainInfo) => {
    if (chainInfo && chainInfo?.token_infos?.length > 0) {
      const orderlyTokens2 = chainInfo.token_infos;
      const tokens = sortTokensWithUSDCFirst(orderlyTokens2);
      setOrderlyTokens(tokens);
    }
  }, []);
  useEffect(() => {
    onChainInited(currentChain?.info);
  }, [currentChain]);
  return orderlyTokens;
};
var useToken = (orderlyTokens) => {
  const [sourceToken, setSourceToken] = useState();
  const [targetToken, setTargetToken] = useState();
  const [sourceTokens, setSourceTokens] = useState([]);
  const [targetTokens, setTargetTokens] = useState([]);
  const networkId = useConfig("networkId");
  useEffect(() => {
    const usdcToken = orderlyTokens[0];
    if (!usdcToken) {
      return;
    }
    setSourceToken(usdcToken);
    setTargetToken(usdcToken);
    setSourceTokens(orderlyTokens);
    setTargetTokens([usdcToken]);
  }, [orderlyTokens]);
  useEffect(() => {
    if (!sourceToken || !sourceTokens.length) {
      return;
    }
    if (sourceToken.symbol === "USDC") {
      setTargetToken(sourceToken);
      setTargetTokens([sourceToken]);
      return;
    }
    const collateralTokens = sourceTokens.filter((item) => item.is_collateral);
    const usdcToken = sourceTokens.find((item) => item.symbol === "USDC");
    if (sourceToken.is_collateral) {
      if (networkId === "mainnet" && sourceToken.swap_enable) {
        setTargetToken(sourceToken);
        setTargetTokens([
          sourceToken,
          ...collateralTokens.filter(
            (item) => item.symbol !== sourceToken.symbol
          )
        ]);
      } else {
        setTargetToken(sourceToken);
        setTargetTokens([sourceToken]);
      }
      return;
    }
    setTargetToken(usdcToken || collateralTokens[0]);
    setTargetTokens(collateralTokens);
  }, [networkId, sourceToken, sourceTokens]);
  return {
    sourceToken,
    targetToken,
    sourceTokens,
    targetTokens,
    setSourceTokens,
    onSourceTokenChange: setSourceToken,
    onTargetTokenChange: setTargetToken
  };
};
var fetcher = (url) => fetch(url).then((res) => res.json());
function mergeTokens(orderlyTokens, swapTokens) {
  const tokens = orderlyTokens.map((item) => {
    return {
      ...item,
      swap_enable: swapTokens.some((swapToken) => {
        return swapToken.address?.toLowerCase() === item.address?.toLowerCase();
      })
    };
  });
  swapTokens.forEach((swapToken) => {
    const orderlyToken = orderlyTokens.find((item) => {
      return item.address?.toLowerCase() === swapToken.address?.toLowerCase() || item.symbol === swapToken.symbol;
    });
    if (!orderlyToken) {
      tokens.push(swapToken);
    }
  });
  return tokens;
}
function filterAndSortTokens(orderlyTokens = [], swapTokens = [], tokenBalances = {}, getIndexPrice) {
  const mergedTokens = mergeTokens(orderlyTokens, swapTokens);
  const filteredTokens = mergedTokens.map((item) => {
    const quantity = tokenBalances[item.symbol];
    const amount = new Decimal(quantity || 0).todp(item.precision || 6, Decimal.ROUND_DOWN).toNumber();
    return {
      ...item,
      quantity,
      amount
    };
  }).filter((item) => {
    return item.is_collateral || item.amount > 0;
  });
  const list = filteredTokens.map((item) => {
    const indexPrice = getIndexPrice(item.symbol);
    const balance = new Decimal(item.amount || 0).mul(indexPrice || 1).todp(item.precision || 2).toNumber();
    return {
      ...item,
      balance,
      isNativeToken: isNativeTokenChecker(item.address)
    };
  });
  const STABLECOIN_ORDER = {
    USDC: 0,
    "USDC.e": 1,
    USDT: 2
  };
  const getStablecoinRank = (symbol) => symbol !== void 0 && symbol in STABLECOIN_ORDER ? STABLECOIN_ORDER[symbol] : Number.MAX_SAFE_INTEGER;
  return list.sort((a, b) => {
    if (a.is_collateral !== b.is_collateral) {
      return a.is_collateral ? -1 : 1;
    }
    if (a.is_collateral && b.is_collateral) {
      const rankA = getStablecoinRank(a.symbol);
      const rankB = getStablecoinRank(b.symbol);
      if (rankA !== rankB) return rankA - rankB;
      if (a.isNativeToken !== b.isNativeToken) {
        return a.isNativeToken ? -1 : 1;
      }
    }
    return (a.symbol || "").localeCompare(b.symbol || "");
  });
}

// src/components/depositForm/hooks/useTokenBalances.ts
var useTokenBalances = (options2) => {
  const { orderlyTokens, swapTokens, fetchBalances } = options2;
  const tokens = useMemo(
    () => mergeTokens(orderlyTokens, swapTokens),
    [orderlyTokens, swapTokens]
  );
  const key = useMemo(() => {
    if (tokens?.length > 0) {
      return ["tokenBalances", tokens.map((item) => item.symbol).join(",")];
    }
    return null;
  }, [tokens]);
  const fetcher2 = async () => {
    if (!tokens || tokens.length === 0) {
      return EMPTY_OBJECT;
    }
    const balances2 = await fetchBalances(tokens);
    return balances2;
  };
  const { data: balances = EMPTY_OBJECT, isLoading } = useSWR(key, fetcher2, {
    revalidateOnFocus: true,
    refreshInterval: 1e4,
    errorRetryInterval: 3e3,
    errorRetryCount: 3
  });
  const loading = useMemo(() => {
    if (Object.keys(balances).length === 0) {
      return true;
    }
    return isLoading;
  }, [isLoading, balances]);
  return { balances, isLoading: loading };
};
var SUPPORTED_CHAINS_URL = "https://api.woofi.com/woofi_dex/depositor/supported_chains";
var useSwapSupportedChains = (chainId) => {
  const { data, isLoading } = useSWR(
    SUPPORTED_CHAINS_URL,
    fetcher,
    {
      revalidateOnFocus: false
    }
  );
  const chains = useMemo(() => {
    if (!data || data.status !== "ok") return [];
    return data.data?.perp_vault?.filter((c) => c.same_chain_deposit === true);
  }, [data]);
  const chain = useMemo(() => {
    if (!chainId || !chains?.length) return null;
    return chains.find((c) => c.chain_id === parseInt(chainId));
  }, [chains, chainId]);
  return {
    chains,
    chain,
    isLoading
  };
};

// src/components/depositForm/swap/use1inchTokens.ts
var useSwapTokens = (chainId, enableSwapDeposit) => {
  const { chain: swapSupportedChain } = useSwapSupportedChains(chainId);
  const url = useMemo(() => {
    if (enableSwapDeposit && swapSupportedChain) {
      return `https://widget-api.woofi.com/1inch_tokens?network=${swapSupportedChain.chain_key}`;
    }
    return null;
  }, [chainId, enableSwapDeposit, swapSupportedChain]);
  const { data } = useSWR(url, fetcher, {
    revalidateOnFocus: false
  });
  const tokens = useMemo(() => {
    const tokenMap = data?.tokens;
    return Object.values(tokenMap || {}).map((item) => ({
      symbol: item.symbol,
      address: item.address,
      decimals: item.decimals,
      // default precision is 8 for 1inch tokens
      precision: 8,
      swap_enable: true,
      // need to show token icon
      logo_uri: item.logoURI
    }));
  }, [data]);
  if (!enableSwapDeposit) {
    return [];
  }
  return tokens;
};

// src/components/depositForm/swap/perp_vault_depositor.json
var perp_vault_depositor_default = [
  {
    inputs: [],
    stateMutability: "nonpayable",
    type: "constructor"
  },
  {
    inputs: [],
    name: "BridgeTokenNotEnough",
    type: "error"
  },
  {
    inputs: [],
    name: "DepositAmountExceed",
    type: "error"
  },
  {
    inputs: [],
    name: "DstDepositorNotSet",
    type: "error"
  },
  {
    inputs: [],
    name: "EnforcedPause",
    type: "error"
  },
  {
    inputs: [],
    name: "ExpectedPause",
    type: "error"
  },
  {
    inputs: [],
    name: "FromNotPair",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidBridgeAmount",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidBridgeSlippage",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidCaller",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidDepositor",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidDstToToken",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidInitialization",
    type: "error"
  },
  {
    inputs: [
      {
        internalType: "uint16",
        name: "optionType",
        type: "uint16"
      }
    ],
    name: "InvalidOptionType",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidPerpVault",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidReceiver",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidSrcBridgeToken",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidSrcFromAmount",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidSrcFromToken",
    type: "error"
  },
  {
    inputs: [],
    name: "InvalidWOOFiRouter",
    type: "error"
  },
  {
    inputs: [],
    name: "NotInitializing",
    type: "error"
  },
  {
    inputs: [
      {
        internalType: "address",
        name: "owner",
        type: "address"
      }
    ],
    name: "OwnableInvalidOwner",
    type: "error"
  },
  {
    inputs: [
      {
        internalType: "address",
        name: "account",
        type: "address"
      }
    ],
    name: "OwnableUnauthorizedAccount",
    type: "error"
  },
  {
    inputs: [],
    name: "ReentrancyGuardReentrantCall",
    type: "error"
  },
  {
    inputs: [
      {
        internalType: "uint8",
        name: "bits",
        type: "uint8"
      },
      {
        internalType: "uint256",
        name: "value",
        type: "uint256"
      }
    ],
    name: "SafeCastOverflowedUintDowncast",
    type: "error"
  },
  {
    anonymous: false,
    inputs: [
      {
        indexed: false,
        internalType: "uint64",
        name: "version",
        type: "uint64"
      }
    ],
    name: "Initialized",
    type: "event"
  },
  {
    anonymous: false,
    inputs: [
      {
        indexed: false,
        internalType: "uint32",
        name: "srcEid",
        type: "uint32"
      },
      {
        indexed: true,
        internalType: "bytes32",
        name: "guid",
        type: "bytes32"
      },
      {
        indexed: true,
        internalType: "address",
        name: "sender",
        type: "address"
      },
      {
        indexed: true,
        internalType: "address",
        name: "to",
        type: "address"
      },
      {
        indexed: false,
        internalType: "address",
        name: "bridgeToken",
        type: "address"
      },
      {
        indexed: false,
        internalType: "uint256",
        name: "bridgeAmount",
        type: "uint256"
      },
      {
        indexed: false,
        internalType: "uint256",
        name: "swapBridgeAmount",
        type: "uint256"
      },
      {
        indexed: false,
        internalType: "address",
        name: "toToken",
        type: "address"
      },
      {
        indexed: false,
        internalType: "uint256",
        name: "minToAmount",
        type: "uint256"
      },
      {
        indexed: false,
        internalType: "uint256",
        name: "toAmount",
        type: "uint256"
      },
      {
        indexed: false,
        internalType: "address",
        name: "realToToken",
        type: "address"
      },
      {
        indexed: false,
        internalType: "uint256",
        name: "realToAmount",
        type: "uint256"
      },
      {
        indexed: false,
        internalType: "enum IOrderlyDepositor.SwapType",
        name: "swapType",
        type: "uint8"
      },
      {
        indexed: false,
        internalType: "bool",
        name: "ableToDeposit",
        type: "bool"
      }
    ],
    name: "OrderlyCrossDepositOnDstChain",
    type: "event"
  },
  {
    anonymous: false,
    inputs: [
      {
        indexed: false,
        internalType: "uint32",
        name: "dstEid",
        type: "uint32"
      },
      {
        indexed: true,
        internalType: "bytes32",
        name: "guid",
        type: "bytes32"
      },
      {
        indexed: true,
        internalType: "address",
        name: "sender",
        type: "address"
      },
      {
        indexed: true,
        internalType: "address",
        name: "to",
        type: "address"
      },
      {
        indexed: false,
        internalType: "address",
        name: "fromToken",
        type: "address"
      },
      {
        indexed: false,
        internalType: "uint256",
        name: "fromAmount",
        type: "uint256"
      },
      {
        indexed: false,
        internalType: "address",
        name: "bridgeToken",
        type: "address"
      },
      {
        indexed: false,
        internalType: "uint256",
        name: "minBridgeAmount",
        type: "uint256"
      },
      {
        indexed: false,
        internalType: "uint256",
        name: "realBridgeAmount",
        type: "uint256"
      },
      {
        indexed: false,
        internalType: "enum IOrderlyDepositor.SwapType",
        name: "swapType",
        type: "uint8"
      }
    ],
    name: "OrderlyCrossDepositOnSrcChain",
    type: "event"
  },
  {
    anonymous: false,
    inputs: [
      {
        indexed: true,
        internalType: "address",
        name: "sender",
        type: "address"
      },
      {
        indexed: true,
        internalType: "address",
        name: "to",
        type: "address"
      },
      {
        indexed: false,
        internalType: "address",
        name: "fromToken",
        type: "address"
      },
      {
        indexed: false,
        internalType: "uint256",
        name: "fromAmount",
        type: "uint256"
      },
      {
        indexed: false,
        internalType: "address",
        name: "toToken",
        type: "address"
      },
      {
        indexed: false,
        internalType: "uint256",
        name: "minToAmount",
        type: "uint256"
      },
      {
        indexed: false,
        internalType: "enum IOrderlyDepositor.SwapType",
        name: "swapType",
        type: "uint8"
      }
    ],
    name: "OrderlyDeposit",
    type: "event"
  },
  {
    anonymous: false,
    inputs: [
      {
        indexed: true,
        internalType: "address",
        name: "previousOwner",
        type: "address"
      },
      {
        indexed: true,
        internalType: "address",
        name: "newOwner",
        type: "address"
      }
    ],
    name: "OwnershipTransferred",
    type: "event"
  },
  {
    anonymous: false,
    inputs: [
      {
        indexed: false,
        internalType: "address",
        name: "account",
        type: "address"
      }
    ],
    name: "Paused",
    type: "event"
  },
  {
    anonymous: false,
    inputs: [
      {
        indexed: false,
        internalType: "address",
        name: "account",
        type: "address"
      }
    ],
    name: "Unpaused",
    type: "event"
  },
  {
    inputs: [],
    name: "BRIDGE_SLIPPAGE_BASE",
    outputs: [
      {
        internalType: "uint256",
        name: "",
        type: "uint256"
      }
    ],
    stateMutability: "view",
    type: "function"
  },
  {
    inputs: [],
    name: "ETH_PLACEHOLDER",
    outputs: [
      {
        internalType: "address",
        name: "",
        type: "address"
      }
    ],
    stateMutability: "view",
    type: "function"
  },
  {
    inputs: [],
    name: "bridgeSlippage",
    outputs: [
      {
        internalType: "uint256",
        name: "",
        type: "uint256"
      }
    ],
    stateMutability: "view",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "address",
        name: "receiver",
        type: "address"
      },
      {
        components: [
          {
            internalType: "address",
            name: "fromToken",
            type: "address"
          },
          {
            internalType: "address",
            name: "bridgeToken",
            type: "address"
          },
          {
            internalType: "uint256",
            name: "fromAmount",
            type: "uint256"
          },
          {
            internalType: "uint256",
            name: "minBridgeAmount",
            type: "uint256"
          }
        ],
        internalType: "struct IOrderlyDepositor.SrcInfo",
        name: "srcInfo",
        type: "tuple"
      },
      {
        components: [
          {
            internalType: "uint32",
            name: "eid",
            type: "uint32"
          },
          {
            internalType: "address",
            name: "bridgeToken",
            type: "address"
          },
          {
            internalType: "address",
            name: "toToken",
            type: "address"
          },
          {
            internalType: "uint256",
            name: "minToAmount",
            type: "uint256"
          },
          {
            internalType: "uint128",
            name: "orderlyBridgeFee",
            type: "uint128"
          },
          {
            internalType: "uint128",
            name: "dstGasForCall",
            type: "uint128"
          }
        ],
        internalType: "struct IOrderlyDepositor.DstInfo",
        name: "dstInfo",
        type: "tuple"
      },
      {
        components: [
          {
            internalType: "address",
            name: "swapRouter",
            type: "address"
          },
          {
            internalType: "bytes",
            name: "data",
            type: "bytes"
          }
        ],
        internalType: "struct IOrderlyDepositor.SrcExtSwapInfo",
        name: "srcExtSwapInfo",
        type: "tuple"
      },
      {
        components: [
          {
            internalType: "address",
            name: "swapRouter",
            type: "address"
          },
          {
            internalType: "uint256",
            name: "swapAmount",
            type: "uint256"
          },
          {
            internalType: "bytes",
            name: "data",
            type: "bytes"
          }
        ],
        internalType: "struct IOrderlyDepositor.DstExtSwapInfo",
        name: "dstExtSwapInfo",
        type: "tuple"
      },
      {
        components: [
          {
            internalType: "bytes32",
            name: "accountId",
            type: "bytes32"
          },
          {
            internalType: "bytes32",
            name: "brokerHash",
            type: "bytes32"
          },
          {
            internalType: "bytes32",
            name: "tokenHash",
            type: "bytes32"
          }
        ],
        internalType: "struct IOrderlyDepositor.PerpAccountInfo",
        name: "dstPerpAccountInfo",
        type: "tuple"
      }
    ],
    name: "crossDeposit",
    outputs: [],
    stateMutability: "payable",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "address",
        name: "receiver",
        type: "address"
      },
      {
        internalType: "uint128",
        name: "orderlyBridgeFee",
        type: "uint128"
      },
      {
        components: [
          {
            internalType: "address",
            name: "fromToken",
            type: "address"
          },
          {
            internalType: "address",
            name: "bridgeToken",
            type: "address"
          },
          {
            internalType: "uint256",
            name: "fromAmount",
            type: "uint256"
          },
          {
            internalType: "uint256",
            name: "minBridgeAmount",
            type: "uint256"
          }
        ],
        internalType: "struct IOrderlyDepositor.SrcInfo",
        name: "info",
        type: "tuple"
      },
      {
        components: [
          {
            internalType: "address",
            name: "swapRouter",
            type: "address"
          },
          {
            internalType: "bytes",
            name: "data",
            type: "bytes"
          }
        ],
        internalType: "struct IOrderlyDepositor.SrcExtSwapInfo",
        name: "extSwapInfo",
        type: "tuple"
      },
      {
        components: [
          {
            internalType: "bytes32",
            name: "accountId",
            type: "bytes32"
          },
          {
            internalType: "bytes32",
            name: "brokerHash",
            type: "bytes32"
          },
          {
            internalType: "bytes32",
            name: "tokenHash",
            type: "bytes32"
          }
        ],
        internalType: "struct IOrderlyDepositor.PerpAccountInfo",
        name: "perpAccountInfo",
        type: "tuple"
      }
    ],
    name: "deposit",
    outputs: [],
    stateMutability: "payable",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "uint32",
        name: "",
        type: "uint32"
      }
    ],
    name: "depositors",
    outputs: [
      {
        internalType: "address",
        name: "",
        type: "address"
      }
    ],
    stateMutability: "view",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "address",
        name: "_stuckToken",
        type: "address"
      },
      {
        internalType: "uint256",
        name: "_amount",
        type: "uint256"
      }
    ],
    name: "inCaseTokenGotStuck",
    outputs: [],
    stateMutability: "nonpayable",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "address",
        name: "_owner",
        type: "address"
      },
      {
        internalType: "address",
        name: "_weth",
        type: "address"
      },
      {
        internalType: "address",
        name: "_woofiRouter",
        type: "address"
      },
      {
        internalType: "address",
        name: "_sgInfo",
        type: "address"
      }
    ],
    name: "initialize",
    outputs: [],
    stateMutability: "nonpayable",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "address",
        name: "_from",
        type: "address"
      },
      {
        internalType: "bytes32",
        name: "_guid",
        type: "bytes32"
      },
      {
        internalType: "bytes",
        name: "_message",
        type: "bytes"
      },
      {
        internalType: "address",
        name: "",
        type: "address"
      },
      {
        internalType: "bytes",
        name: "",
        type: "bytes"
      }
    ],
    name: "lzCompose",
    outputs: [],
    stateMutability: "payable",
    type: "function"
  },
  {
    inputs: [],
    name: "owner",
    outputs: [
      {
        internalType: "address",
        name: "",
        type: "address"
      }
    ],
    stateMutability: "view",
    type: "function"
  },
  {
    inputs: [],
    name: "pause",
    outputs: [],
    stateMutability: "nonpayable",
    type: "function"
  },
  {
    inputs: [],
    name: "paused",
    outputs: [
      {
        internalType: "bool",
        name: "",
        type: "bool"
      }
    ],
    stateMutability: "view",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "uint32",
        name: "",
        type: "uint32"
      }
    ],
    name: "perpVaults",
    outputs: [
      {
        internalType: "address",
        name: "",
        type: "address"
      }
    ],
    stateMutability: "view",
    type: "function"
  },
  {
    inputs: [
      {
        components: [
          {
            internalType: "address",
            name: "fromToken",
            type: "address"
          },
          {
            internalType: "address",
            name: "bridgeToken",
            type: "address"
          },
          {
            internalType: "uint256",
            name: "fromAmount",
            type: "uint256"
          },
          {
            internalType: "uint256",
            name: "minBridgeAmount",
            type: "uint256"
          }
        ],
        internalType: "struct IOrderlyDepositor.SrcInfo",
        name: "srcInfo",
        type: "tuple"
      },
      {
        components: [
          {
            internalType: "uint32",
            name: "eid",
            type: "uint32"
          },
          {
            internalType: "address",
            name: "bridgeToken",
            type: "address"
          },
          {
            internalType: "address",
            name: "toToken",
            type: "address"
          },
          {
            internalType: "uint256",
            name: "minToAmount",
            type: "uint256"
          },
          {
            internalType: "uint128",
            name: "orderlyBridgeFee",
            type: "uint128"
          },
          {
            internalType: "uint128",
            name: "dstGasForCall",
            type: "uint128"
          }
        ],
        internalType: "struct IOrderlyDepositor.DstInfo",
        name: "dstInfo",
        type: "tuple"
      },
      {
        internalType: "bytes",
        name: "composeMsg",
        type: "bytes"
      }
    ],
    name: "quoteLayerZeroFee",
    outputs: [
      {
        internalType: "uint256",
        name: "",
        type: "uint256"
      },
      {
        internalType: "uint256",
        name: "",
        type: "uint256"
      }
    ],
    stateMutability: "view",
    type: "function"
  },
  {
    inputs: [],
    name: "renounceOwnership",
    outputs: [],
    stateMutability: "nonpayable",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "uint256",
        name: "_bridgeSlippage",
        type: "uint256"
      }
    ],
    name: "setBridgeSlippage",
    outputs: [],
    stateMutability: "nonpayable",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "uint16",
        name: "_chainId",
        type: "uint16"
      },
      {
        internalType: "address",
        name: "_depositor",
        type: "address"
      }
    ],
    name: "setDepositor",
    outputs: [],
    stateMutability: "nonpayable",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "uint16",
        name: "_chainId",
        type: "uint16"
      },
      {
        internalType: "address",
        name: "_perpVault",
        type: "address"
      }
    ],
    name: "setPerpVault",
    outputs: [],
    stateMutability: "nonpayable",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "address",
        name: "_woofiRouter",
        type: "address"
      }
    ],
    name: "setWOOFiRouter",
    outputs: [],
    stateMutability: "nonpayable",
    type: "function"
  },
  {
    inputs: [],
    name: "sgInfo",
    outputs: [
      {
        internalType: "contract ISgInfoV2",
        name: "",
        type: "address"
      }
    ],
    stateMutability: "view",
    type: "function"
  },
  {
    inputs: [
      {
        internalType: "address",
        name: "newOwner",
        type: "address"
      }
    ],
    name: "transferOwnership",
    outputs: [],
    stateMutability: "nonpayable",
    type: "function"
  },
  {
    inputs: [],
    name: "unpause",
    outputs: [],
    stateMutability: "nonpayable",
    type: "function"
  },
  {
    inputs: [],
    name: "weth",
    outputs: [
      {
        internalType: "address",
        name: "",
        type: "address"
      }
    ],
    stateMutability: "view",
    type: "function"
  },
  {
    inputs: [],
    name: "woofiRouter",
    outputs: [
      {
        internalType: "contract IWOOFiRouterV2",
        name: "",
        type: "address"
      }
    ],
    stateMutability: "view",
    type: "function"
  },
  {
    stateMutability: "payable",
    type: "receive"
  }
];
var ORDERLY_SWAP_DEPOSIT_SLIPPAGE_KEY = "orderly_swap_deposit_slippage";
var useSlippage = () => {
  const [slippage, setSlippage] = useLocalStorage(
    ORDERLY_SWAP_DEPOSIT_SLIPPAGE_KEY,
    1
  );
  return { slippage, onSlippageChange: setSlippage };
};
var useSwapQuote = (options2) => {
  const { sourceToken, targetToken, quantity, chainId, slippage } = options2;
  const { chain: swapSupportedChain } = useSwapSupportedChains(chainId);
  const url = useMemo(() => {
    const chainKey = swapSupportedChain?.chain_key;
    const urlSearchParams = new URLSearchParams();
    const isSwapDeposit = sourceToken?.swap_enable && sourceToken?.address && targetToken?.address && sourceToken?.address !== targetToken?.address;
    if (!isSwapDeposit || !chainKey || !quantity || !slippage) {
      return null;
    }
    const amount = new Decimal(quantity).mul(Math.pow(10, sourceToken.decimals)).toFixed(0);
    urlSearchParams.set("from_token", sourceToken.address);
    urlSearchParams.set("to_token", targetToken.address);
    urlSearchParams.set("from_amount", amount);
    urlSearchParams.set("network", chainKey);
    urlSearchParams.set("slippage", slippage.toString());
    urlSearchParams.set("extra_fee_rate", "0");
    return `https://api.woofi.com/v2/swap?${urlSearchParams.toString()}`;
  }, [
    sourceToken,
    targetToken,
    quantity,
    chainId,
    slippage,
    swapSupportedChain
  ]);
  const { data, isLoading: swapPriceRevalidating } = useSWR(
    url,
    fetcher,
    {
      revalidateOnFocus: false,
      refreshInterval: 1e4,
      shouldRetryOnError: false
    }
  );
  const swapData = useMemo(() => {
    if (!data) return;
    if (data.status === "ok") {
      return data.data;
    }
  }, [data]);
  const error = useMemo(() => {
    if (!data) return;
    if (data.status === "fail") {
      return data.error?.message?.["1inch"]?.error?.description;
    }
  }, [data]);
  const { swapQuantity, swapMinReceived } = useMemo(() => {
    if (!swapData) return {};
    const decimals = targetToken?.decimals || 0;
    const swapQuantity2 = new Decimal(swapData.outcomes.amount || 0).div(Math.pow(10, decimals)).toString();
    const swapMinReceived2 = new Decimal(swapData.infos.min_to_amount || 0).div(Math.pow(10, decimals)).toString();
    return { swapQuantity: swapQuantity2, swapMinReceived: swapMinReceived2 };
  }, [swapData, targetToken?.decimals]);
  return {
    swapData,
    swapPrice: swapData?.price,
    swapMinReceived,
    swapQuantity,
    swapPriceRevalidating,
    error
  };
};

// src/components/depositForm/swap/useSwapDeposit.ts
var SWAP_CONTRACT_ADDRESS = "0xe18f1051d1cCeCb61572eb686e1beb34A8612bf3";
var useSwapDeposit = (options2) => {
  const { sourceToken, targetToken, currentChain, quantity, depositFee } = options2;
  const { account: account2 } = useAccount();
  const brokerId = useConfig("brokerId");
  const { slippage, onSlippageChange } = useSlippage();
  const { t } = useTranslation();
  const {
    swapData,
    swapPrice,
    swapQuantity,
    swapMinReceived,
    swapPriceRevalidating,
    error
  } = useSwapQuote({
    sourceToken,
    targetToken,
    quantity,
    chainId: currentChain?.id,
    slippage
  });
  const onSwapDeposit = useCallback(async () => {
    if (!account2.address || !account2.walletAdapter || !swapData || !sourceToken || !targetToken || !brokerId) {
      return;
    }
    const userAddress = account2.address;
    const receiver = userAddress;
    const orderlyBridgeFee = depositFee;
    const info = {
      fromToken: swapData.infos.from_token,
      bridgeToken: swapData.infos.to_token,
      fromAmount: swapData.infos.from_amount,
      minBridgeAmount: swapData.infos.min_to_amount
    };
    const extSwapInfo = {
      swapRouter: swapData["1inch"].swap_router,
      data: swapData["1inch"].data
    };
    const perpAccountInfo = {
      accountId: account2.accountIdHashStr,
      brokerHash: utils.parseBrokerHash(brokerId),
      tokenHash: utils.parseTokenHash(targetToken.symbol)
    };
    const value = isNativeTokenChecker(sourceToken.address) ? BigInt(swapData.infos.from_amount) + orderlyBridgeFee : orderlyBridgeFee;
    const payload = {
      from: userAddress,
      to: SWAP_CONTRACT_ADDRESS,
      data: [receiver, orderlyBridgeFee, info, extSwapInfo, perpAccountInfo],
      value
    };
    console.info("swap deposit payload", payload);
    try {
      const result = await account2.walletAdapter.sendTransaction(
        SWAP_CONTRACT_ADDRESS,
        "deposit",
        payload,
        {
          abi: perp_vault_depositor_default
        }
      );
      console.info("swap deposit result", result);
      return result;
    } catch (err) {
      console.error("swap deposit error", err);
      toast.error(err.message || t("common.somethingWentWrong"));
      throw err;
    }
  }, [account2, brokerId, swapData, sourceToken, targetToken, depositFee]);
  return {
    swapPrice,
    swapQuantity,
    swapMinReceived,
    swapPriceRevalidating,
    slippage,
    onSlippageChange,
    onSwapDeposit,
    error
  };
};

// src/components/depositForm/depositForm.script.tsx
var useDepositFormScript = (options2) => {
  const { wrongNetwork } = useAppContext();
  const { enableSwapDeposit } = useOrderlyContext();
  const { account: account2 } = useAccount();
  const networkId = useConfig("networkId");
  const { chains, currentChain, settingChain, onChainChange } = useChainSelect();
  const swapTokens = useSwapTokens(currentChain?.id, enableSwapDeposit);
  const orderlyTokens = useOrderlyTokens(currentChain);
  const {
    sourceToken,
    targetToken,
    sourceTokens,
    targetTokens,
    onSourceTokenChange,
    setSourceTokens,
    onTargetTokenChange
  } = useToken(orderlyTokens);
  const { getIndexPrice } = useIndexPricesStream();
  const {
    balance,
    allowance,
    depositFee,
    balanceRevalidating,
    depositFeeRevalidating,
    quantity,
    setQuantity,
    approve,
    deposit,
    fetchBalance,
    fetchBalances,
    targetChain,
    isNativeToken
  } = useDeposit({
    address: sourceToken?.address,
    decimals: sourceToken?.decimals,
    srcChainId: currentChain?.id,
    srcToken: sourceToken?.symbol,
    dstToken: targetToken?.symbol,
    depositorAddress: enableSwapDeposit ? SWAP_CONTRACT_ADDRESS : void 0
  });
  const { balance: nativeBalance, isLoading: nativeBalanceRevalidating } = useNativeBalance({
    fetchBalance,
    targetChain
  });
  const { balances: tokenBalances, isLoading: batchBalancesRevalidating } = useTokenBalances({ orderlyTokens, swapTokens, fetchBalances });
  useEffect(() => {
    const sortedTokens = filterAndSortTokens(
      orderlyTokens,
      swapTokens,
      tokenBalances,
      getIndexPrice
    );
    setSourceTokens(sortedTokens);
  }, [orderlyTokens, swapTokens, tokenBalances]);
  const needSwap = useMemo(() => {
    return !!sourceToken?.symbol && !!targetToken?.symbol && sourceToken.symbol !== targetToken?.symbol;
  }, [sourceToken, targetToken]);
  const {
    swapPrice,
    swapQuantity,
    swapMinReceived,
    swapPriceRevalidating,
    slippage,
    onSlippageChange,
    onSwapDeposit,
    error: swapErrorMessage
  } = useSwapDeposit({
    sourceToken,
    targetToken,
    currentChain,
    quantity,
    depositFee
  });
  const {
    maxQuantity,
    maxDepositAmount,
    targetQuantity,
    swapPriceInUSD,
    quantityNotional,
    indexPrice,
    swapIndexPrice
  } = useDepositFormQuantities({
    sourceToken,
    targetToken,
    balance,
    quantity,
    needSwap,
    swapQuantity,
    swapPrice,
    getIndexPrice
  });
  const nativeSymbol = useMemo(() => {
    return currentChain?.info?.nativeToken?.symbol;
  }, [currentChain]);
  const onDepositSuccess = useCallback(() => {
    setQuantity("");
    options2.close?.();
  }, []);
  const {
    isMutating: depositRevalidating,
    depositError,
    setDepositError,
    onApprove,
    onDeposit,
    onApproveAndDeposit
  } = useDepositAction({
    quantity,
    approve,
    deposit,
    needSwap,
    swapDeposit: onSwapDeposit,
    onSuccess: onDepositSuccess
  });
  useEffect(() => {
    setQuantity("");
    setDepositError("");
  }, [sourceToken, currentChain?.id]);
  const fee = useDepositFee({ nativeSymbol, depositFee, getIndexPrice });
  const {
    inputStatus,
    hintMessage,
    validationMessage,
    depositDisabled,
    targetInputStatus,
    targetHintMessage,
    showSourceDepositCap,
    showTargetDepositCap,
    slippageValidate
  } = useDepositValidation({
    sourceToken,
    targetToken,
    quantity,
    targetQuantity,
    maxQuantity,
    isNativeToken,
    depositFee,
    depositFeeRevalidating,
    nativeBalanceRevalidating,
    dstGasFee: fee.dstGasFee,
    nativeSymbol,
    nativeBalance,
    account: account2,
    currentChain,
    depositError,
    needSwap
  });
  const usdcToken = useMemo(() => {
    return sourceTokens?.find((item) => item.symbol === "USDC");
  }, [sourceTokens]);
  const actionType = useActionType({ allowance, quantity, maxQuantity });
  const isLoggedIn = useAuthGuard();
  const {
    collateralRatio: collateralRatio2,
    collateralContributionQuantity,
    currentLTV,
    nextLTV
  } = useCollateralValue({
    sourceToken,
    targetToken,
    quantity,
    indexPrice: needSwap ? swapIndexPrice : indexPrice
  });
  const {
    ltv_threshold,
    negative_usdc_threshold,
    isLoading: isConvertThresholdLoading
  } = useConvertThreshold();
  const loading = depositFeeRevalidating || depositRevalidating || swapPriceRevalidating;
  const disabled = !sourceToken || !quantity || Number(quantity) === 0 || depositDisabled || loading || !!swapErrorMessage;
  const targetQuantityLoading = swapPriceRevalidating;
  const warningMessage = validationMessage || swapErrorMessage;
  return {
    sourceToken,
    targetToken,
    sourceTokens,
    targetTokens,
    onSourceTokenChange,
    onTargetTokenChange,
    nativeSymbol,
    isNativeToken,
    quantity,
    collateralContributionQuantity,
    maxQuantity,
    maxDepositAmount,
    onQuantityChange: setQuantity,
    hintMessage,
    inputStatus,
    targetInputStatus,
    targetHintMessage,
    chains,
    currentChain,
    settingChain,
    onChainChange,
    actionType,
    onDeposit,
    onApprove,
    onApproveAndDeposit,
    wrongNetwork,
    balanceRevalidating,
    batchBalancesRevalidating,
    loading,
    disabled,
    networkId,
    fee,
    collateralRatio: collateralRatio2,
    currentLTV,
    nextLTV,
    ltv_threshold,
    negative_usdc_threshold,
    isConvertThresholdLoading,
    slippage,
    onSlippageChange,
    slippageValidate,
    swapMinReceived,
    usdcToken,
    needSwap,
    swapPrice,
    swapPriceInUSD,
    warningMessage,
    targetQuantity,
    targetQuantityLoading,
    isLoggedIn,
    showSourceDepositCap,
    showTargetDepositCap,
    quantityNotional
  };
};
var DepositFormWidget = (props) => {
  const state = useDepositFormScript(props);
  return /* @__PURE__ */ jsx(DepositForm, { ...state });
};
var TextAreaInput = (props) => {
  const { t } = useTranslation();
  const {
    value,
    onChange,
    status,
    hintMessage,
    placeholder,
    label,
    enableAccountLookup,
    accountInfo,
    accountDropdownOpen,
    setAccountDropdownOpen
  } = props;
  const textareaRef = useRef(null);
  const [selectedAccount, setSelectedAccount] = useState(
    null
  );
  const displayStatus = status;
  const displayHint = hintMessage;
  const handleChange = (e) => {
    const nextValue = e.target.value;
    if (selectedAccount) {
      setSelectedAccount(null);
    }
    onChange?.(nextValue);
    const textarea = textareaRef.current;
    if (textarea) {
      textarea.style.height = "auto";
      textarea.style.height = `${textarea.scrollHeight}px`;
    }
  };
  useEffect(() => {
    const textarea = textareaRef.current;
    if (textarea) {
      textarea.style.height = "auto";
      textarea.style.height = `${textarea.scrollHeight}px`;
    }
  }, [value, selectedAccount?.address]);
  const prefix = /* @__PURE__ */ jsx("div", { className: "oui-absolute oui-left-3 oui-top-0.5 oui-z-[1]", children: /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: label }) });
  const message = /* @__PURE__ */ jsxs(Flex, { mb: 1, gapX: 1, px: 1, children: [
    /* @__PURE__ */ jsx(
      Box,
      {
        width: 4,
        height: 4,
        r: "full",
        className: cn(
          displayStatus === "error" && "oui-bg-danger-light",
          displayStatus === "warning" && "oui-bg-warning-light"
        )
      }
    ),
    /* @__PURE__ */ jsx(
      Text,
      {
        size: "2xs",
        className: cn(
          displayStatus === "error" && "oui-text-danger-light",
          displayStatus === "warning" && "oui-text-warning-light"
        ),
        children: displayHint
      }
    )
  ] });
  const textareaNode = /* @__PURE__ */ jsxs("div", { className: "oui-relative", children: [
    prefix,
    /* @__PURE__ */ jsxs(
      "div",
      {
        className: cn(
          "oui-relative oui-w-full oui-rounded-lg oui-border oui-border-line oui-mb-1",
          "oui-bg-base-5 oui-text-sm oui-text-base-contrast",
          "focus-within:oui-border-primary-light",
          displayStatus === "error" && "oui-border-danger-light focus-within:oui-border-danger-light",
          displayStatus === "warning" && "oui-border-warning-light focus-within:oui-border-warning-light",
          "disabled:oui-cursor-not-allowed"
        ),
        children: [
          /* @__PURE__ */ jsx(
            "textarea",
            {
              ref: textareaRef,
              placeholder,
              className: cn(
                // hide resize height control and scrollbar
                "oui-resize-none oui-overflow-y-hidden",
                "oui-block oui-w-full oui-bg-transparent",
                "oui-px-3 oui-pt-5",
                selectedAccount?.address ? "oui-pb-0" : "oui-pb-2",
                "oui-text-sm oui-text-base-contrast",
                "oui-rounded-lg oui-outline-none",
                "placeholder:oui-text-base-contrast-20",
                props.className
              ),
              rows: 1,
              value,
              onChange: handleChange,
              onFocus: () => {
                if (accountInfo) {
                  setAccountDropdownOpen?.(true);
                }
              },
              disabled: props.disabled
            }
          ),
          selectedAccount?.address && /* @__PURE__ */ jsx("div", { className: "oui-flex oui-items-center oui-justify-between oui-px-3 oui-pb-2 oui-pt-1", children: /* @__PURE__ */ jsxs(
            Text,
            {
              size: "2xs",
              intensity: 36,
              className: "oui-truncate oui-leading-[15px]",
              children: [
                "(",
                t("common.address"),
                ":",
                " ",
                /* @__PURE__ */ jsx(Text.formatted, { as: "span", rule: "address", range: [6, 4], children: selectedAccount.address }),
                ")"
              ]
            }
          ) }),
          enableAccountLookup && /* @__PURE__ */ jsx(DropdownMenuTrigger, { asChild: true, children: /* @__PURE__ */ jsx("span", { className: "oui-absolute oui-left-0 oui-bottom-0 oui-w-0 oui-h-0" }) })
        ]
      }
    )
  ] });
  if (!enableAccountLookup) {
    return /* @__PURE__ */ jsxs("div", { className: "oui-relative", children: [
      textareaNode,
      displayHint && message
    ] });
  }
  return /* @__PURE__ */ jsxs(
    DropdownMenuRoot,
    {
      open: !!(accountInfo && accountDropdownOpen),
      onOpenChange: (open) => {
        if (!open) {
          setAccountDropdownOpen?.(false);
        }
      },
      children: [
        textareaNode,
        displayHint && message,
        /* @__PURE__ */ jsx(DropdownMenuTrigger, { asChild: true, children: /* @__PURE__ */ jsx("span", { className: "oui-invisible oui-w-0 oui-h-0 oui-overflow-hidden" }) }),
        accountInfo && accountDropdownOpen && /* @__PURE__ */ jsx(DropdownMenuPortal, { children: /* @__PURE__ */ jsx(
          DropdownMenuContent,
          {
            align: "start",
            className: cn(
              "oui-bg-base-8",
              "oui-rounded-md oui-shadow-lg",
              "oui--mt-1 oui-w-[378px] oui-p-1"
            ),
            ...{
              onOpenAutoFocus: (event) => event.preventDefault()
            },
            children: /* @__PURE__ */ jsx(
              AccountResultItem,
              {
                item: accountInfo,
                disabled: props.disabled,
                selected: !!selectedAccount && selectedAccount.accountId === accountInfo.accountId,
                onSelect: (account2) => {
                  onChange?.(account2.accountId);
                  setSelectedAccount(account2);
                  setAccountDropdownOpen?.(false);
                }
              }
            )
          }
        ) })
      ]
    }
  );
};
var AccountResultItem = ({
  item,
  disabled,
  selected,
  onSelect
}) => {
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: cn(
        "oui-flex oui-w-full oui-items-center oui-justify-between",
        "oui-gap-1.5 oui-rounded-[4px] oui-px-2 oui-py-1.5",
        "oui-cursor-pointer hover:oui-bg-base-5",
        disabled && "oui-cursor-not-allowed oui-opacity-50"
      ),
      onClick: () => !disabled && onSelect(item),
      children: [
        /* @__PURE__ */ jsxs("div", { className: "oui-flex oui-flex-col oui-items-start oui-gap-0.5", children: [
          /* @__PURE__ */ jsxs(
            Text,
            {
              size: "sm",
              intensity: 54,
              weight: "semibold",
              className: "oui-tracking-wide",
              children: [
                t("common.accountId"),
                ":",
                " ",
                /* @__PURE__ */ jsx(
                  Text.formatted,
                  {
                    rule: "address",
                    range: [6, 4],
                    as: "span",
                    className: "oui-text-primary-light",
                    children: item.accountId
                  }
                )
              ]
            }
          ),
          /* @__PURE__ */ jsxs(Text, { size: "2xs", intensity: 36, children: [
            "(",
            item.address,
            ")"
          ] })
        ] }),
        /* @__PURE__ */ jsx(Checkbox, { checked: !!selected })
      ]
    }
  );
};
var UnsettlePnlInfo = (props) => {
  const {
    hasPositions,
    unsettledPnl,
    onSettlePnl,
    tooltipContent,
    dialogContent
  } = props;
  const { t } = useTranslation();
  const { isMobile } = useScreen();
  const renderUnsettledLabel = () => {
    return /* @__PURE__ */ jsxs(Flex, { itemAlign: "center", justify: "start", gap: 1, children: [
      /* @__PURE__ */ jsx(ExclamationFillIcon, { size: 14, className: "oui-text-warning-darken" }),
      /* @__PURE__ */ jsx(Text, { className: "oui-cursor-pointer oui-border-b oui-border-dashed oui-border-line-12", children: `${t("settle.unsettled")}:` })
    ] });
  };
  if (unsettledPnl === 0 && !hasPositions) {
    return /* @__PURE__ */ jsx(Fragment, {});
  }
  const settlePnlDialog = () => {
    modal.confirm({
      title: t("settle.settlePnl"),
      content: dialogContent,
      onOk: () => {
        return onSettlePnl();
      }
    });
  };
  return /* @__PURE__ */ jsxs(Flex, { justify: "between", className: "oui-text-2xs oui-text-base-contrast-36", children: [
    /* @__PURE__ */ jsxs(Flex, { itemAlign: "center", justify: "start", gap: 1, children: [
      tooltipContent ? isMobile ? /* @__PURE__ */ jsx(
        "button",
        {
          type: "button",
          className: "oui-p-0",
          onClick: () => {
            modal.alert({
              title: t("common.tips"),
              message: tooltipContent
            });
          },
          children: renderUnsettledLabel()
        }
      ) : /* @__PURE__ */ jsx(
        Tooltip,
        {
          className: "oui-max-w-[274px] oui-font-semibold",
          content: tooltipContent,
          children: renderUnsettledLabel()
        }
      ) : renderUnsettledLabel(),
      /* @__PURE__ */ jsx(
        Text.numeral,
        {
          showIdentifier: true,
          coloring: true,
          weight: "semibold",
          dp: 6,
          "data-testid": "oui-testid-withdraw-dialog-unsettledPnl-value",
          children: unsettledPnl ?? "--"
        }
      ),
      /* @__PURE__ */ jsx(Text, { children: "USDC" })
    ] }),
    /* @__PURE__ */ jsxs(Flex, { itemAlign: "center", gap: 1, className: "oui-cursor-pointer", children: [
      /* @__PURE__ */ jsx(RefreshIcon, { className: "oui-text-primary" }),
      /* @__PURE__ */ jsx(
        Text,
        {
          "data-testid": "oui-testid-withdraw-dialog-settle-text",
          size: "2xs",
          color: "primary",
          className: " oui-select-none",
          onClick: settlePnlDialog,
          children: t("common.settle")
        }
      )
    ] })
  ] });
};
var CrossWithdrawConfirm = ({
  address,
  amount,
  currentChain
}) => {
  const { t } = useTranslation();
  const networkName = useMemo(() => {
    if (currentChain && currentChain.info && currentChain.info.network_infos) {
      return currentChain.info.network_infos.name;
    }
    return void 0;
  }, [currentChain]);
  return /* @__PURE__ */ jsxs(
    Flex,
    {
      direction: "column",
      itemAlign: "start",
      justify: "start",
      className: "oui-gap-1 lg:oui-gap-3",
      children: [
        /* @__PURE__ */ jsxs(
          Flex,
          {
            direction: "column",
            itemAlign: "start",
            mb: 5,
            className: "oui-gap-3 lg:oui-gap-4",
            children: [
              /* @__PURE__ */ jsxs(Flex, { direction: "column", justify: "start", itemAlign: "start", children: [
                /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: t("transfer.withdraw.crossChain.recipientAddress") }),
                /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 98, className: "oui-break-all", children: address })
              ] }),
              /* @__PURE__ */ jsxs(Flex, { direction: "column", justify: "start", itemAlign: "start", children: [
                /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: t("transfer.withdraw.crossChain.recipientNetwork") }),
                /* @__PURE__ */ jsxs(Flex, { gap: 1, children: [
                  /* @__PURE__ */ jsx(
                    ChainIcon,
                    {
                      className: "oui-h-[18px] oui-w-[18px]",
                      size: "sm",
                      chainId: currentChain.id
                    }
                  ),
                  /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 98, children: networkName })
                ] })
              ] }),
              /* @__PURE__ */ jsxs(Flex, { direction: "column", justify: "start", itemAlign: "start", children: [
                /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: `${t("transfer.withdraw.crossChain.withdrawAmount")} (USDC)` }),
                /* @__PURE__ */ jsx(Text.numeral, { size: "sm", intensity: 98, dp: 2, children: amount })
              ] })
            ]
          }
        ),
        /* @__PURE__ */ jsx(
          Flex,
          {
            justify: "center",
            className: "oui-text-warning-darken oui-text-2xs lg:oui-text-xs oui-text-center",
            mb: 3,
            children: t("transfer.withdraw.crossChain.warning")
          }
        )
      ]
    }
  );
};
function SwitchChainButton(props) {
  const { t } = useTranslation();
  const switchChain = () => {
    modal.show(ChainSelectorDialogId, {
      networkId: props.networkId,
      bridgeLessOnly: true
    }).then(
      (r) => {
        toast.success(t("connector.networkSwitched"));
      },
      (error) => console.log("[switchChain error]", error)
    );
  };
  return /* @__PURE__ */ jsx(Flex, { direction: "column", children: /* @__PURE__ */ jsx(
    Button,
    {
      color: "warning",
      size: props.size,
      fullWidth: true,
      onClick: () => {
        switchChain();
      },
      children: t("connector.switchNetwork")
    }
  ) });
}
var WithdrawAction = (props) => {
  const {
    disabled,
    loading,
    onWithdraw,
    networkId,
    crossChainWithdraw,
    address,
    currentChain,
    quantity,
    fee,
    checkIsBridgeless,
    onTransfer
  } = props;
  const { t } = useTranslation();
  const amount = useMemo(() => {
    if (!quantity) {
      return 0;
    }
    return new Decimal(quantity).minus(fee ?? 0).toNumber();
  }, [quantity, fee]);
  const preWithdraw = () => {
    if (crossChainWithdraw) {
      modal.confirm({
        title: t("transfer.withdraw.crossChain.confirmWithdraw"),
        content: /* @__PURE__ */ jsx(
          CrossWithdrawConfirm,
          {
            address,
            amount,
            currentChain
          }
        ),
        classNames: {
          content: "oui-font-semibold",
          body: "!oui-pb-0",
          footer: "!oui-pt-0"
        },
        onOk: async () => {
          onWithdraw();
        }
      });
      return;
    }
    onWithdraw();
  };
  const buttonSize = { initial: "md", lg: "lg" };
  const renderButton = () => {
    if (props.withdrawTo === "accountId" /* Account */) {
      return /* @__PURE__ */ jsx(
        Button,
        {
          fullWidth: true,
          disabled,
          loading,
          onClick: onTransfer,
          size: buttonSize,
          children: t("common.withdraw")
        }
      );
    }
    if (checkIsBridgeless) {
      return /* @__PURE__ */ jsx(
        Button,
        {
          "data-testid": "oui-testid-withdraw-dialog-withdraw-btn",
          fullWidth: true,
          disabled,
          loading,
          onClick: preWithdraw,
          size: buttonSize,
          children: t("common.withdraw")
        }
      );
    }
    return /* @__PURE__ */ jsx(SwitchChainButton, { networkId, size: buttonSize });
  };
  return /* @__PURE__ */ jsx(Box, { className: "oui-w-full lg:oui-w-auto lg:oui-min-w-[184px]", children: /* @__PURE__ */ jsx(
    AuthGuard,
    {
      status: AccountStatusEnum.EnableTrading,
      networkId,
      bridgeLessOnly: true,
      buttonProps: { fullWidth: true, size: buttonSize },
      children: renderButton()
    }
  ) });
};
var WithdrawWarningMessage = (props) => {
  const { crossChainTrans, qtyGreaterThanMaxAmount } = props;
  const { t } = useTranslation();
  const { state } = useAccount();
  const renderContent = () => {
    if (state.status === AccountStatusEnum.NotConnected) {
      return null;
    }
    if (crossChainTrans) {
      return t("transfer.withdraw.crossChain.process");
    }
    if (qtyGreaterThanMaxAmount) {
      return t("transfer.insufficientBalance");
    }
    return props.message;
  };
  const content = renderContent();
  if (!content) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    Flex,
    {
      my: 4,
      className: "oui-justify-center oui-text-center oui-text-xs oui-text-warning-darken",
      children: content
    }
  );
};
var useSettlePnl = (options2) => {
  const { accountId } = options2 || {};
  const { t } = useTranslation();
  const ee = useEventEmitter();
  const { account: account2, state } = useAccount();
  const [positionData] = usePositionStream();
  const hasPositions = useMemo(
    () => !!positionData?.rows?.length,
    [positionData]
  );
  const onSettlePnl = async () => {
    const isSubAccount = accountId && state.mainAccountId !== accountId;
    const settleFn = isSubAccount ? account2.settleSubAccount({ subAccountId: accountId }) : account2.settle({ accountId });
    return settleFn.then((res) => {
      toast.success(t("settle.settlement.requested"));
      return Promise.resolve(res);
    }).catch((e) => {
      if (e.code == -1104) {
        toast.error(t("settle.settlement.error"));
      } else if (e.message.indexOf(
        "Signing off chain messages with Ledger is not yet supported"
      ) !== -1) {
        ee.emit("wallet:sign-message-with-ledger-error", {
          message: e.message,
          userAddress: account2.address
        });
      } else if (e.message.indexOf("user rejected") !== -1) {
        toast.error(t("transfer.rejectTransaction"));
      } else {
        toast.error(e.message);
        return Promise.reject(e);
      }
    });
  };
  return {
    hasPositions,
    onSettlePnl
  };
};
var cctpSupportedChains = [43114, 1, 137, 42161, 10, 8453, 1329];
var cctpSupportedTokens = ["USDC"];
var useVaultBalance = (inputs) => {
  const {
    currentChain,
    sourceToken,
    withdrawTo,
    quantity,
    qtyGreaterThanMaxAmount
  } = inputs;
  const { t } = useTranslation();
  const { data: vaultBalanceData } = useQuery(
    `/v1/public/vault_balance`,
    {
      revalidateOnMount: true,
      errorRetryCount: 3
    }
  );
  const vaultBalanceList = useMemo(() => {
    if (withdrawTo === "accountId" /* Account */ || !currentChain || !Array.isArray(vaultBalanceData)) {
      return [];
    }
    return vaultBalanceData.filter(
      (item) => Number.parseInt(item.chain_id) === currentChain?.id
    );
  }, [vaultBalanceData, currentChain, withdrawTo]);
  const chainVaultBalance = useMemo(() => {
    const vaultBalance = vaultBalanceList?.find(
      (item) => item.token === sourceToken?.symbol
    );
    return vaultBalance?.balance;
  }, [vaultBalanceList, sourceToken]);
  const qtyGreaterThanVault = useMemo(() => {
    if (!quantity || Number.isNaN(quantity) || !chainVaultBalance || Number.isNaN(chainVaultBalance)) {
      return false;
    }
    return new Decimal(quantity).gt(chainVaultBalance);
  }, [quantity, chainVaultBalance, withdrawTo]);
  const crossChainWithdraw = useMemo(() => {
    const canCCTP = currentChain?.id && sourceToken?.symbol ? cctpSupportedChains.includes(currentChain?.id) && cctpSupportedTokens.includes(sourceToken?.symbol) : false;
    return qtyGreaterThanVault && canCCTP && !qtyGreaterThanMaxAmount;
  }, [qtyGreaterThanVault, qtyGreaterThanMaxAmount, currentChain, sourceToken]);
  const chainName = useMemo(() => {
    if (currentChain && currentChain.info && currentChain.info.network_infos) {
      return currentChain.info.network_infos.name;
    }
    return "";
  }, [currentChain]);
  const vaultBalanceMessage = useMemo(() => {
    if (qtyGreaterThanVault && !crossChainWithdraw && chainName) {
      return t("transfer.withdraw.vaultWarning", {
        tokenName: sourceToken?.symbol,
        chainName,
        balance: chainVaultBalance
      });
    }
    return "";
  }, [
    qtyGreaterThanVault,
    crossChainWithdraw,
    chainName,
    sourceToken,
    chainVaultBalance
  ]);
  const vaultBalanceListFilterCCTP = useMemo(() => {
    if (cctpSupportedChains.includes(currentChain?.id)) {
      return vaultBalanceList?.filter(
        (item) => !cctpSupportedTokens.includes(item.token)
      );
    }
    return vaultBalanceList;
  }, [vaultBalanceList, currentChain]);
  return {
    vaultBalanceList: vaultBalanceListFilterCCTP,
    // chainVaultBalance,
    // if the token is cross chain withdraw supported, the qtyGreaterThanVault will be false
    qtyGreaterThanVault: qtyGreaterThanVault && !crossChainWithdraw,
    crossChainWithdraw,
    vaultBalanceMessage
  };
};
function useWithdrawAccountId(options2) {
  const { token, quantity, setQuantity, close, setLoading, decimals } = options2;
  const { t } = useTranslation();
  const [toAccountId, setToAccountId] = useState("");
  const [inputStatus, setInputStatus] = useState("default");
  const [hintMessage, setHintMessage] = useState();
  const [accountInfo, setAccountInfo] = useState(null);
  const [isDropdownOpen, setIsDropdownOpen] = useState(false);
  const latestLookupInputRef = useRef("");
  const config = useConfig();
  const brokerId = config.get("brokerId");
  const apiBaseUrl = config.get("apiBaseUrl");
  const { transfer, submitting } = useInternalTransfer();
  const onTransfer = useCallback(() => {
    const num = Number(quantity);
    if (Number.isNaN(num) || num <= 0) {
      toast.error(t("transfer.quantity.invalid"));
      return;
    }
    if (submitting || !toAccountId) {
      return;
    }
    setLoading(true);
    transfer({
      token,
      receiver: toAccountId,
      amount: quantity,
      decimals
    }).then(() => {
      toast.success(t("transfer.internalTransfer.success"));
      setQuantity("");
      close?.();
    }).catch((err) => {
      console.log("transfer error", err);
      const errorMsg = getTransferErrorMessage(err.code);
      toast.error(errorMsg);
    }).finally(() => {
      setLoading(false);
    });
  }, [t, quantity, token, submitting, toAccountId, transfer]);
  useEffect(() => {
    if (!toAccountId) {
      setInputStatus("default");
      setHintMessage("");
      setAccountInfo(null);
      setIsDropdownOpen(false);
      return;
    }
    if (checkIsAccountId(toAccountId)) {
      setInputStatus("default");
      setHintMessage("");
      return;
    }
  }, [toAccountId]);
  const walletLookup = async (input, network) => {
    if (!network || !brokerId) return null;
    const chainType = network;
    const res = await fetch(
      `${apiBaseUrl}/v1/get_account?address=${encodeURIComponent(
        input
      )}&broker_id=${encodeURIComponent(
        brokerId
      )}&chain_type=${encodeURIComponent(chainType)}`
    );
    const json = await res.json();
    if (res.ok && json?.success && json?.data?.account_id) {
      return {
        accountId: json.data.account_id,
        address: input
      };
    }
    return null;
  };
  const accountLookup = async (input) => {
    const res = await fetch(
      `${apiBaseUrl}/v1/public/account?account_id=${encodeURIComponent(input)}`
    );
    const json = await res.json();
    if (res.ok && json?.success && json?.data?.address && (!brokerId || json.data.broker_id === brokerId)) {
      return {
        accountId: input,
        address: json.data.address
      };
    }
    if (res.ok && json?.success && brokerId) {
      console.log(
        "This account belongs to a different broker and cannot be used here."
      );
    }
    return null;
  };
  const performLookup = useCallback(
    async (rawInput) => {
      const input = rawInput.trim();
      if (!input || input !== latestLookupInputRef.current) {
        return;
      }
      const { valid, network } = validateWalletAddress(input);
      try {
        let resolved = null;
        if (valid) {
          resolved = await walletLookup(input, network);
        } else {
          resolved = await accountLookup(input);
        }
        if (!resolved) {
          setAccountInfo(null);
          setIsDropdownOpen(false);
          setInputStatus("error");
          setHintMessage(t("transfer.withdraw.accountId.invalid"));
        } else {
          setAccountInfo(resolved);
          setIsDropdownOpen(true);
          setInputStatus("default");
          setHintMessage("");
        }
      } catch (error) {
        console.log("Failed to search account. Please try again.", error);
        setAccountInfo(null);
        setIsDropdownOpen(false);
        setInputStatus("error");
        setHintMessage(t("transfer.withdraw.accountId.invalid"));
      }
    },
    [brokerId, t]
  );
  const debouncedLookup = useDebouncedCallback((input) => {
    void performLookup(input);
  }, 500);
  const handleAccountIdChange = useCallback(
    (val) => {
      setToAccountId(val);
      const trimmed = val.trim();
      if (!trimmed) {
        setAccountInfo(null);
        setIsDropdownOpen(false);
        latestLookupInputRef.current = "";
        return;
      }
      latestLookupInputRef.current = trimmed;
      if (checkIsAccountId(trimmed) && accountInfo?.accountId === trimmed) {
        setIsDropdownOpen(false);
        setInputStatus("default");
        setHintMessage("");
        return;
      }
      debouncedLookup(trimmed);
    },
    [debouncedLookup, accountInfo]
  );
  return {
    toAccountId,
    setToAccountId: handleAccountIdChange,
    onTransfer,
    internalWithdrawSubmitting: submitting,
    toAccountIdInputStatus: inputStatus,
    toAccountIdHintMessage: hintMessage,
    toAccountInfo: accountInfo,
    toAccountInfoDropdownOpen: isDropdownOpen,
    setToAccountInfoDropdownOpen: setIsDropdownOpen
  };
}
function useWithdrawFee(options2) {
  const { crossChainWithdraw, currentChain, token, withdrawTo } = options2;
  const tokenInfo = useTokenInfo(token);
  const fee = useMemo(() => {
    if (withdrawTo === "accountId" /* Account */) {
      return 0;
    }
    if (!currentChain) {
      return 0;
    }
    const item = tokenInfo?.chain_details?.find(
      (chain) => Number.parseInt(chain.chain_id) === currentChain.id
    );
    if (!item) {
      return 0;
    }
    if (crossChainWithdraw) {
      return (item.withdrawal_fee || 0) + (item.cross_chain_withdrawal_fee || 0);
    }
    return item.withdrawal_fee || 0;
  }, [tokenInfo, token, currentChain, crossChainWithdraw, withdrawTo]);
  return fee;
}
var useWithdrawLTV = (params) => {
  const { token, quantity } = params;
  const { ltv_threshold } = useConvertThreshold2();
  const { t } = useTranslation();
  const currentLTV = useComputedLTV();
  const nextLTV = useComputedLTV({
    input: quantity ? -Number(quantity) : 0,
    token
  });
  const ltvWarningMessage = useMemo(() => {
    if (ltv_threshold && nextLTV) {
      const threshold = ltv_threshold * 100;
      if (nextLTV > threshold) {
        return t("transfer.withdraw.LTV.error", {
          threshold
        });
      }
    }
    return "";
  }, [nextLTV, ltv_threshold]);
  return {
    currentLTV,
    nextLTV,
    ltvWarningMessage,
    t
  };
};
var useConvertThreshold2 = () => {
  const { data } = useQuery(
    "/v1/public/auto_convert_threshold",
    {
      revalidateOnFocus: false
    }
  );
  return {
    ltv_threshold: data?.ltv_threshold,
    negative_usdc_threshold: data?.negative_usdc_threshold
  };
};
var useToken2 = (currentChain, filter = () => true) => {
  const [sourceToken, setSourceToken] = useState();
  const [targetToken, setTargetToken] = useState();
  const [sourceTokens, setSourceTokens] = useState([]);
  const [targetTokens, setTargetTokens] = useState([]);
  const sourceTokenUpdatedRef = useRef(false);
  const networkId = useConfig("networkId");
  const onChainInited = useCallback((chainInfo) => {
    if (chainInfo && chainInfo?.token_infos?.length > 0) {
      const tokens = chainInfo.token_infos?.filter(filter);
      if (tokens?.length) {
        tokens.sort((a, b) => {
          if (a.symbol === "USDC") return -1;
          if (b.symbol === "USDC") return 1;
          return 0;
        });
      }
      const usdcToken = getUSDCToken(tokens);
      if (!usdcToken) {
        return;
      }
      setSourceToken(usdcToken);
      setTargetToken(usdcToken);
      setSourceTokens(tokens);
      setTargetTokens([usdcToken]);
    }
  }, []);
  useEffect(() => {
    onChainInited(currentChain?.info);
  }, [currentChain, onChainInited]);
  useEffect(() => {
    if (!sourceToken || !sourceTokens.length) {
      return;
    }
    if (sourceToken.symbol === "USDC") {
      setTargetToken(sourceToken);
      setTargetTokens([sourceToken]);
      return;
    }
    const usdc = sourceTokens.find((t) => t.symbol === "USDC");
    if (sourceToken.is_collateral) {
      if (networkId === "mainnet" && sourceToken.swap_enable) {
        setTargetToken(sourceToken);
        setTargetTokens([sourceToken, usdc]);
      } else {
        setTargetToken(sourceToken);
        setTargetTokens([sourceToken]);
      }
      return;
    }
    setTargetToken(usdc);
    setTargetTokens([usdc]);
  }, [networkId, sourceToken, sourceTokens]);
  const onSourceTokenChange = useCallback((token) => {
    sourceTokenUpdatedRef.current = true;
    setSourceToken(token);
  }, []);
  useEffect(() => {
    sourceTokenUpdatedRef.current = false;
  }, [currentChain?.id]);
  return {
    sourceToken,
    targetToken,
    sourceTokens,
    targetTokens,
    onSourceTokenChange,
    setSourceToken,
    onTargetTokenChange: setTargetToken,
    sourceTokenUpdatedRef
  };
};

// src/components/withdrawForm/hooks/useWithdrawToken.ts
function useWithdrawToken(params) {
  const { currentChain, withdrawTo } = params;
  const [token, setToken] = useState();
  const [tokens, setTokens] = useState([]);
  const tokensInfo = useTokensInfo();
  const { data: holdings } = useHoldingStream();
  const { sourceToken, onSourceTokenChange, sourceTokens } = useToken2(
    currentChain,
    (token2) => token2.symbol === "USDC" || token2.is_collateral
  );
  const allTokens = useMemo(() => {
    if (!tokensInfo?.length) return [];
    const holdingMap = /* @__PURE__ */ new Map();
    holdings?.forEach((item) => {
      holdingMap.set(item.token, item);
    });
    const mappedTokens = tokensInfo.map((item) => ({
      ...item,
      symbol: item.token,
      token_decimal: item.decimals,
      precision: item.decimals
    }));
    const list = mappedTokens.filter((token2) => {
      const holding = holdingMap.get(token2.symbol);
      if (!holdings) return true;
      return !!holding && holding.holding > 0;
    });
    if (list.length === 0) {
      const usdcToken = mappedTokens.find((t) => t.symbol === "USDC");
      if (usdcToken) {
        return [usdcToken];
      }
    }
    list.sort((a, b) => {
      if (a.symbol === "USDC") return -1;
      if (b.symbol === "USDC") return 1;
      return 0;
    });
    return list;
  }, [tokensInfo, holdings]);
  useEffect(() => {
    if (!allTokens.length) return;
    const usdcToken = getUSDCToken(allTokens);
    setToken(usdcToken || allTokens?.[0]);
    setTokens(allTokens);
  }, [allTokens]);
  const isTokenSupportedOnChain = useMemoizedFn(
    (tokenSymbol) => {
      if (!currentChain?.info?.token_infos) {
        return false;
      }
      return currentChain.info.token_infos.some(
        (token2) => token2.symbol === tokenSymbol
      );
    }
  );
  const handleSourceTokenChange = useMemoizedFn((newToken) => {
    if (withdrawTo === "wallet" /* Wallet */ && currentChain && newToken.symbol) {
      const matchingToken = sourceTokens.find(
        (t) => t.symbol === newToken.symbol
      );
      if (matchingToken) {
        onSourceTokenChange(matchingToken);
      } else {
        onSourceTokenChange(newToken);
      }
      return;
    }
    onSourceTokenChange(newToken);
  });
  const syncToken = useMemoizedFn(() => {
    if (withdrawTo === "accountId" /* Account */) {
      const findToken = tokens.find(
        (item) => item.symbol === sourceToken?.symbol
      );
      if (findToken) {
        setToken(findToken);
      }
    }
  });
  useEffect(() => {
    if (withdrawTo !== "wallet" /* Wallet */ || !allTokens.length) return;
    const holdingSymbols = new Set(allTokens.map((t) => t.symbol));
    if (!sourceToken?.symbol || !holdingSymbols.has(sourceToken.symbol)) {
      const defaultToken = getUSDCToken(allTokens);
      if (defaultToken) {
        handleSourceTokenChange(defaultToken);
      }
    }
  }, [withdrawTo, allTokens, sourceToken?.symbol, handleSourceTokenChange]);
  useEffect(() => {
    syncToken();
  }, [
    withdrawTo,
    allTokens,
    sourceToken,
    sourceTokens,
    currentChain,
    syncToken
  ]);
  if (withdrawTo === "accountId" /* Account */) {
    return {
      sourceToken: token,
      onSourceTokenChange: setToken,
      sourceTokens: tokens,
      isTokenSupportedOnChain
    };
  }
  return {
    sourceToken,
    onSourceTokenChange: handleSourceTokenChange,
    sourceTokens: allTokens,
    isTokenSupportedOnChain
  };
}

// src/components/withdrawForm/withdrawForm.script.tsx
var validateWalletAddress = (address) => {
  if (ethers.isAddress(address)) {
    return { valid: true, network: "EVM" };
  }
  try {
    const pubKey = new PublicKey(address);
    if (PublicKey.isOnCurve(pubKey.toBytes())) {
      return { valid: true, network: "SOL" };
    }
  } catch {
  }
  return { valid: false };
};
var markPrice = 1;
var useWithdrawFormScript = (options2) => {
  const { t } = useTranslation();
  const [crossChainTrans, setCrossChainTrans] = useState(false);
  const [loading, setLoading] = useState(false);
  const [assetHistory] = useAssetsHistory(
    {
      page: 1,
      pageSize: 1,
      side: AssetHistorySideEnum.WITHDRAW,
      status: AssetHistoryStatusEnum.PENDING_REBALANCE
    },
    // update when withdraw status changed
    {
      shouldUpdateOnWalletChanged: (data) => data.side === AssetHistorySideEnum.WITHDRAW
    }
  );
  const config = useConfig();
  const brokerName = config.get("brokerName");
  const networkId = config.get("networkId");
  const ee = useEventEmitter();
  const [quantity, setQuantity] = useState("");
  const [inputStatus, setInputStatus] = useState("default");
  const [hintMessage, setHintMessage] = useState();
  const [withdrawTo, setWithdrawTo] = useState("wallet" /* Wallet */);
  const { wrongNetwork, widgetConfigs } = useAppContext();
  const { account: account2, state } = useAccount();
  const [chains, { findByChainId }] = useChains(networkId, {
    filter: (chain) => chain.network_infos?.bridge_enable || chain.network_infos?.bridgeless
  });
  const [linkDeviceStorage] = useLocalStorage("orderly_link_device", {});
  const {
    connectedChain,
    wallet,
    setChain: switchChain,
    settingChain
  } = useWalletConnector();
  const isLoggedIn = useAuthGuard();
  const [pendingTokenSymbol, setPendingTokenSymbol] = useState();
  const currentChain = useMemo(() => {
    const chainId = connectedChain ? praseChainIdToNumber(connectedChain.id) : Number.parseInt(linkDeviceStorage?.chainId);
    if (!chainId) {
      return null;
    }
    const chain = findByChainId(chainId);
    return {
      ...connectedChain,
      id: chainId,
      info: chain
    };
  }, [findByChainId, connectedChain, linkDeviceStorage]);
  const {
    sourceToken,
    onSourceTokenChange: _OnSourceTokenChange,
    sourceTokens,
    isTokenSupportedOnChain
  } = useWithdrawToken({
    currentChain,
    withdrawTo
  });
  const onSourceTokenChange = useMemoizedFn((token) => {
    setQuantity("");
    _OnSourceTokenChange(token);
    setPendingTokenSymbol(token.symbol);
  });
  const tokenChains = useMemo(() => {
    if (!chains) return [];
    const list = chains.map((chain) => {
      const isSupported = chain.token_infos?.some(
        (token) => token.symbol === sourceToken?.symbol
      );
      return {
        ...chain.network_infos,
        isSupported
      };
    });
    list.sort((a, b) => {
      const aSupported = !!a.isSupported;
      const bSupported = !!b.isSupported;
      if (aSupported === bSupported) return 0;
      return aSupported ? -1 : 1;
    });
    return list;
  }, [chains, sourceToken?.symbol]);
  const enableWithdrawToExternalWallet = widgetConfigs?.withdraw?.enableWithdrawToExternalWallet ?? false;
  const { walletName, address } = useMemo(
    () => ({
      walletName: wallet?.label,
      address: wallet?.accounts?.[0].address
    }),
    [wallet]
  );
  const [externalWallets, setExternalWallets] = useLocalStorage("orderly_external_wallets", []);
  const [selectedWalletAddress, setSelectedWalletAddress] = useState();
  useEffect(() => {
    setSelectedWalletAddress(void 0);
  }, [currentChain?.namespace]);
  useEffect(() => {
    if (address && !selectedWalletAddress) {
      setSelectedWalletAddress(address);
    }
  }, [address, selectedWalletAddress]);
  const onSelectWallet = (address2) => {
    setSelectedWalletAddress(address2);
  };
  const onAddExternalWallet = (addr, network) => {
    const normalizedAddr = addr.trim();
    if (!normalizedAddr) return;
    const connectedAddress = address?.trim();
    if (connectedAddress && connectedAddress.toLowerCase() === normalizedAddr.toLowerCase()) {
      setSelectedWalletAddress(normalizedAddr);
      return;
    }
    const exists = (externalWallets || []).some((w) => {
      return w.address?.trim().toLowerCase() === normalizedAddr.toLowerCase();
    });
    if (!exists) {
      setExternalWallets([
        ...externalWallets || [],
        { address: normalizedAddr, network }
      ]);
    }
    setSelectedWalletAddress(normalizedAddr);
  };
  const onQuantityChange = (qty) => {
    setQuantity(qty);
  };
  const amount = useMemo(() => {
    return new Decimal(quantity || 0).mul(markPrice).toNumber();
  }, [quantity, markPrice]);
  const { withdraw, maxAmount, unsettledPnL } = useWithdraw({
    token: sourceToken?.symbol,
    decimals: sourceToken?.token_decimal
  });
  const qtyGreaterThanMaxAmount = useMemo(() => {
    if (!quantity || Number.isNaN(quantity)) {
      return false;
    }
    if (!maxAmount || Number.isNaN(maxAmount)) {
      return true;
    }
    return new Decimal(quantity).gt(maxAmount);
  }, [quantity, maxAmount]);
  const {
    vaultBalanceList,
    qtyGreaterThanVault,
    crossChainWithdraw,
    vaultBalanceMessage
  } = useVaultBalance({
    currentChain,
    sourceToken,
    withdrawTo,
    quantity,
    qtyGreaterThanMaxAmount
  });
  const withdrawAccountIdState = useWithdrawAccountId({
    token: sourceToken?.symbol,
    decimals: sourceToken?.token_decimal,
    quantity,
    setQuantity,
    close: options2.close,
    setLoading
  });
  const checkIsBridgeless = useMemo(() => {
    if (wrongNetwork) {
      return false;
    }
    if (!currentChain) {
      return false;
    }
    if (networkId === "testnet") {
      return true;
    }
    if (!currentChain.info) {
      return false;
    }
    if (!currentChain.info.network_infos || !currentChain.info.network_infos.bridgeless) {
      return false;
    }
    return true;
  }, [currentChain, wrongNetwork]);
  const cleanData = () => {
    setQuantity("");
  };
  const onChainChange = useCallback(
    async (chain) => {
      const chainInfo = findByChainId(chain.chain_id);
      if (!chainInfo || chainInfo.network_infos?.chain_id === currentChain?.id) {
        return Promise.resolve();
      }
      return switchChain?.({
        chainId: int2hex(Number(chainInfo.network_infos?.chain_id))
      }).then((switched) => {
        if (switched) {
          toast.success(t("connector.networkSwitched"));
          cleanData();
        } else {
          toast.error(t("connector.switchChain.failed"));
        }
      }).catch((error) => {
        toast.error(`${t("connector.switchChain.failed")}: ${error.message}`);
      });
    },
    [currentChain, switchChain, findByChainId, t]
  );
  useEffect(() => {
    if (!pendingTokenSymbol || !sourceTokens || sourceTokens.length === 0) {
      return;
    }
    const matchedToken = sourceTokens.find(
      (token) => token.symbol === pendingTokenSymbol
    );
    if (matchedToken) {
      onSourceTokenChange(matchedToken);
    }
  }, [pendingTokenSymbol, sourceTokens, onSourceTokenChange, currentChain]);
  const onWithdraw = async () => {
    if (loading) {
      return;
    }
    if (inputStatus !== "default") {
      return;
    }
    setLoading(true);
    return withdraw({
      amount: quantity,
      token: sourceToken?.symbol,
      chainId: currentChain?.id,
      allowCrossChainWithdraw: crossChainWithdraw,
      receiver: selectedWalletAddress
    }).then((res) => {
      toast.success(t("transfer.withdraw.requested"));
      ee.emit("withdraw:requested");
      options2.close?.();
      setQuantity("");
    }).catch((e) => {
      if (e.message.indexOf("user rejected") !== -1) {
        toast.error(t("transfer.rejectTransaction"));
        return;
      }
      if (e.message.indexOf(
        "Signing off chain messages with Ledger is not yet supported"
      ) !== -1) {
        ee.emit("wallet:sign-message-with-ledger-error", {
          message: e.message,
          userAddress: account2.address
        });
        return;
      }
      toast.error(e.message);
    }).finally(() => {
      setLoading(false);
    });
  };
  const fee = useWithdrawFee({
    crossChainWithdraw,
    currentChain,
    token: sourceToken?.symbol,
    withdrawTo
  });
  const minAmountWarningMessage = useMemo(() => {
    const minAmount = new Decimal(
      sourceToken?.minimum_withdraw_amount ?? 0
    ).add(fee);
    if (quantity && new Decimal(quantity).lt(minAmount)) {
      return t("transfer.withdraw.minAmount.error", {
        minAmount: toNonExponential(minAmount.toNumber()),
        currency: sourceToken?.symbol
      });
    }
  }, [quantity, sourceToken?.minimum_withdraw_amount, fee, t]);
  const showQty = useMemo(() => {
    if (!quantity) {
      return "";
    }
    const value = new Decimal(quantity).sub(fee ?? 0);
    if (value.isNegative()) {
      return "";
    }
    return toNonExponential(value.toNumber());
  }, [fee, quantity]);
  const isTokenUnsupported = useMemo(() => {
    if (withdrawTo !== "wallet" /* Wallet */ || !currentChain || !sourceToken?.symbol) {
      return false;
    }
    return !isTokenSupportedOnChain(sourceToken.symbol);
  }, [withdrawTo, currentChain, sourceToken?.symbol, isTokenSupportedOnChain]);
  useEffect(() => {
    if (isTokenUnsupported && sourceToken?.symbol) {
      setInputStatus("error");
      setHintMessage(
        t("transfer.withdraw.unsupported.token", { token: sourceToken.symbol })
      );
      return;
    }
    if (!quantity) {
      setInputStatus("default");
      setHintMessage("");
      return;
    }
    const qty = new Decimal(quantity ?? 0);
    if (unsettledPnL < 0) {
      if (qty.gt(maxAmount)) {
        setInputStatus("error");
        setHintMessage(t("transfer.insufficientBalance"));
      } else {
        setInputStatus("default");
        setHintMessage("");
      }
    }
  }, [
    quantity,
    maxAmount,
    unsettledPnL,
    crossChainTrans,
    isTokenUnsupported,
    sourceToken?.symbol,
    t
  ]);
  const disabled = crossChainTrans || !quantity || Number(quantity) === 0 || ["error", "warning"].includes(inputStatus) || withdrawTo === "accountId" /* Account */ && !withdrawAccountIdState.toAccountId || qtyGreaterThanMaxAmount || qtyGreaterThanVault || !!minAmountWarningMessage || isTokenUnsupported;
  useEffect(() => {
    setCrossChainTrans(!!assetHistory?.length);
  }, [assetHistory?.length]);
  const handleWalletTopic = useMemoizedFn((data) => {
    if (!crossChainTrans) {
      return;
    }
    const txId = assetHistory?.[0]?.tx_id;
    const { trxId, transStatus } = data;
    if (trxId === txId && transStatus === "COMPLETED") {
      setCrossChainTrans(false);
    }
  });
  useWalletTopic({
    onMessage: handleWalletTopic
  });
  const { hasPositions, onSettlePnl } = useSettlePnl();
  const { currentLTV, nextLTV, ltvWarningMessage } = useWithdrawLTV({
    token: sourceToken?.symbol,
    quantity
  });
  const warningMessage = ltvWarningMessage || minAmountWarningMessage || vaultBalanceMessage;
  useMemo(() => {
    if (withdrawTo === "accountId" /* Account */) {
      return [];
    }
    return vaultBalanceList?.filter(
      (item) => Number.parseInt(item.chain_id) === currentChain?.id
    );
  }, [vaultBalanceList, currentChain, withdrawTo]);
  const onSwitchToSupportedNetwork = useMemoizedFn(async () => {
    if (!tokenChains || tokenChains.length === 0) return;
    const targetNetwork = tokenChains.find((item) => item.isSupported) ?? tokenChains[0];
    await onChainChange(targetNetwork);
  });
  return {
    walletName,
    address,
    quantity,
    onQuantityChange,
    sourceToken,
    onSourceTokenChange,
    sourceTokens,
    inputStatus,
    hintMessage,
    amount,
    balanceRevalidating: false,
    maxQuantity: maxAmount,
    disabled,
    loading,
    unsettledPnL,
    wrongNetwork,
    settingChain,
    tokenChains,
    currentChain,
    onChainChange,
    onWithdraw,
    fee,
    crossChainWithdraw,
    crossChainTrans,
    showQty,
    networkId,
    checkIsBridgeless,
    hasPositions,
    onSettlePnl,
    brokerName,
    qtyGreaterThanMaxAmount,
    vaultBalanceList,
    ...withdrawAccountIdState,
    withdrawTo,
    setWithdrawTo,
    currentLTV,
    nextLTV,
    warningMessage,
    isLoggedIn,
    isTokenUnsupported,
    onSwitchToSupportedNetwork,
    enableWithdrawToExternalWallet,
    externalWallets,
    selectedWalletAddress: selectedWalletAddress || address,
    onSelectWallet,
    onAddExternalWallet,
    setExternalWallets,
    isEnableTrading: state.status >= AccountStatusEnum.EnableTrading || state.status === AccountStatusEnum.EnableTradingWithoutConnected
  };
};
var AddWalletDialog = ({
  open,
  onOpenChange,
  onConfirm,
  chain
}) => {
  const { t } = useTranslation();
  const [address, setAddress] = useState("");
  const [isValidating, setIsValidating] = useState(false);
  const [validationResult, setValidationResult] = useState(null);
  const checkAddress = useDebouncedCallback((addr) => {
    if (!addr) {
      setValidationResult(null);
      setIsValidating(false);
      return;
    }
    const result = validateWalletAddress(addr);
    setValidationResult(result);
    setIsValidating(false);
  }, 500);
  const onAddressChange = (val) => {
    setAddress(val);
    if (!val) {
      setValidationResult(null);
      setIsValidating(false);
      return;
    }
    setIsValidating(true);
    setValidationResult(null);
    checkAddress(val);
  };
  const onClear = () => {
    setAddress("");
    setValidationResult(null);
    setIsValidating(false);
  };
  useEffect(() => {
    if (!open) onClear();
  }, [open]);
  const requiredNetwork = chain?.namespace === ChainNamespace.solana ? "SOL" : chain?.namespace === ChainNamespace.evm ? "EVM" : void 0;
  const requiredNetworkLabel = requiredNetwork === "SOL" ? "Solana" : requiredNetwork === "EVM" ? "EVM" : "";
  const hasValidation = !!validationResult;
  const isValid = !!validationResult?.valid;
  const isInvalidFormat = hasValidation && !validationResult.valid;
  const isNetworkMismatch = isValid && !!requiredNetwork && validationResult.network !== requiredNetwork;
  const showBorderDanger = !isValidating && hasValidation && (isInvalidFormat || isNetworkMismatch);
  const showProtocolLabel = !isValidating && isValid;
  const showInlineInvalid = !isValidating && isInvalidFormat;
  const showErrorHint = !isValidating && isNetworkMismatch;
  const handleConfirm = () => {
    if (!address || isValidating || !isValid || isNetworkMismatch) return;
    onConfirm(address, validationResult.network === "SOL" ? "SOL" : "EVM");
    onOpenChange(false);
  };
  return /* @__PURE__ */ jsx(
    SimpleDialog,
    {
      open,
      onOpenChange,
      title: t("transfer.withdraw.addExternalWallet"),
      size: "sm",
      children: /* @__PURE__ */ jsxs(Box, { className: "oui-flex oui-flex-col oui-gap-6 oui-font-semibold oui-tracking-[0.03em]", children: [
        /* @__PURE__ */ jsxs(Flex, { justify: "between", children: [
          /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 54, children: t("common.network") }),
          /* @__PURE__ */ jsxs(Flex, { gapX: 1, children: [
            /* @__PURE__ */ jsx(ChainIcon, { chainId: "" + chain?.id, className: "oui-w-4 oui-h-4" }),
            /* @__PURE__ */ jsx(Text, { size: "sm", children: chain?.info?.network_infos?.shortName })
          ] })
        ] }),
        /* @__PURE__ */ jsxs(Flex, { direction: "column", itemAlign: "start", gapY: 2, children: [
          /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 54, children: t("transfer.withdraw.addExternalWallet.addressDescription") }),
          requiredNetworkLabel && /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 54, children: t("transfer.withdraw.addExternalWallet.addressWarning", {
            networkLabel: requiredNetworkLabel
          }) })
        ] }),
        /* @__PURE__ */ jsxs(Box, { className: "oui-flex oui-flex-col oui-gap-1", children: [
          /* @__PURE__ */ jsxs(Flex, { justify: "between", children: [
            /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 54, children: t("transfer.withdraw.addExternalWallet.label") }),
            isValidating && /* @__PURE__ */ jsx(Spinner, { className: "oui-w-3.5 oui-h-3.5 oui-text-base-contrast-36" }),
            showProtocolLabel && /* @__PURE__ */ jsx(
              Text,
              {
                size: "2xs",
                className: cn(
                  isNetworkMismatch ? "oui-text-danger" : "oui-text-primary-light"
                ),
                children: validationResult.network === "EVM" ? "EVM" : "Solana"
              }
            ),
            showInlineInvalid && /* @__PURE__ */ jsx(Text, { size: "2xs", className: "oui-text-danger", children: t("common.invalid") })
          ] }),
          /* @__PURE__ */ jsx(
            Input,
            {
              value: address,
              onValueChange: onAddressChange,
              autoFocus: true,
              color: showBorderDanger ? "danger" : void 0,
              className: cn("oui-bg-transparent oui-bg-base-6"),
              classNames: { input: "oui-text-white" },
              suffix: address && /* @__PURE__ */ jsx(Box, { className: "oui-ml-2.5 oui-mr-2 oui-cursor-pointer", children: /* @__PURE__ */ jsx(
                CloseRoundFillIcon,
                {
                  size: 16,
                  onClick: onClear,
                  className: "oui-text-base-contrast-20 hover:oui-text-base-contrast-54"
                }
              ) })
            }
          ),
          showErrorHint && /* @__PURE__ */ jsx(Flex, { gapX: 1, px: 1, justify: "between", itemAlign: "center", children: /* @__PURE__ */ jsxs(Flex, { gapX: 1, itemAlign: "center", children: [
            /* @__PURE__ */ jsx(
              Box,
              {
                width: 4,
                height: 4,
                r: "full",
                className: "oui-bg-danger-light"
              }
            ),
            /* @__PURE__ */ jsx(Text, { size: "2xs", className: "oui-text-danger-light", children: t("transfer.withdraw.addExternalWallet.networkMismatch", {
              networkLabel: requiredNetworkLabel
            }) })
          ] }) })
        ] }),
        /* @__PURE__ */ jsx(
          Button,
          {
            variant: "contained",
            fullWidth: true,
            onClick: handleConfirm,
            disabled: !address || isValidating || !isValid || isNetworkMismatch,
            className: "oui-mt-2",
            children: t("common.confirm")
          }
        )
      ] })
    }
  );
};
var AddIcon = (props) => /* @__PURE__ */ jsx(
  "svg",
  {
    width: "18",
    height: "18",
    viewBox: "0 0 18 18",
    fill: "currentColor",
    xmlns: "http://www.w3.org/2000/svg",
    ...props,
    children: /* @__PURE__ */ jsx("path", { d: "M8.99316 2.19731C8.57916 2.19731 8.24316 2.53331 8.24316 2.94731V8.19731H2.99316C2.57916 8.19731 2.24316 8.53331 2.24316 8.94731C2.24316 9.36131 2.57916 9.69731 2.99316 9.69731H8.24316V14.9473C8.24316 15.3613 8.57916 15.6973 8.99316 15.6973C9.40716 15.6973 9.74316 15.3613 9.74316 14.9473V9.69731H14.9932C15.4072 9.69731 15.7432 9.36131 15.7432 8.94731C15.7432 8.53331 15.4072 8.19731 14.9932 8.19731H9.74316V2.94731C9.74316 2.53331 9.40716 2.19731 8.99316 2.19731Z" })
  }
);
var WalletMenuItem = ({
  onClick,
  children,
  className
}) => {
  return /* @__PURE__ */ jsx(
    "button",
    {
      type: "button",
      onClick,
      className: cn(
        "oui-flex oui-items-center oui-gap-2 oui-py-1.5 oui-px-2 hover:oui-bg-base-5",
        "oui-rounded-[4px]",
        "oui-font-semibold oui-tracking-[0.03em]",
        className
      ),
      children
    }
  );
};
var WalletSelector = ({
  connectedWallet,
  externalWallets,
  selectedAddress,
  onSelect,
  onAddExternalWallet
}) => {
  const { t } = useTranslation();
  const [isOpen, setIsOpen] = useState(false);
  const currentNetwork = useMemo(() => {
    if (!connectedWallet?.namespace) return void 0;
    const ns = connectedWallet.namespace.toLowerCase();
    if (ns.includes("sol")) return "SOL";
    return "EVM";
  }, [connectedWallet?.namespace]);
  const filteredExternalWallets = useMemo(() => {
    if (!currentNetwork) return externalWallets;
    return externalWallets.filter(
      (wallet) => wallet.network === currentNetwork
    );
  }, [externalWallets, currentNetwork]);
  const selectedWalletOpt = useMemo(() => {
    if (connectedWallet && selectedAddress === connectedWallet.address) {
      return {
        address: connectedWallet.address,
        name: connectedWallet.name,
        network: currentNetwork
      };
    }
    return filteredExternalWallets.find(
      (w) => w.address === selectedAddress
    );
  }, [
    connectedWallet,
    currentNetwork,
    filteredExternalWallets,
    selectedAddress
  ]);
  const hasExternalWallets = filteredExternalWallets.length > 0;
  const showConnectedItem = !!connectedWallet && hasExternalWallets;
  const getChainLabel = (network) => {
    if (!network) return "";
    if (network === "SOL") return "Solana";
    return "EVM";
  };
  const handleSelectWallet = (address) => {
    onSelect(address);
    setIsOpen(false);
  };
  const handleAddExternalWalletClick = () => {
    onAddExternalWallet();
    setIsOpen(false);
  };
  const dropdownContent = /* @__PURE__ */ jsx(DropdownMenuPortal, { children: /* @__PURE__ */ jsxs(
    DropdownMenuContent,
    {
      align: "end",
      className: "oui-max-h-[240px] oui-overflow-y-auto oui-custom-scrollbar",
      children: [
        showConnectedItem && connectedWallet && /* @__PURE__ */ jsx(
          WalletMenuItem,
          {
            onClick: () => handleSelectWallet(connectedWallet.address),
            className: "oui-w-[368px]",
            children: /* @__PURE__ */ jsxs(Flex, { direction: "column", itemAlign: "start", gapY: 1, children: [
              /* @__PURE__ */ jsx(Text, { size: "2xs", className: "oui-text-base-contrast-54", children: t("common.myWallet") }),
              /* @__PURE__ */ jsx(
                Text,
                {
                  size: "2xs",
                  intensity: 54,
                  className: "oui-text-base-contrast-36 oui-leading-[15px]",
                  children: `(${getChainLabel(currentNetwork)}) ${connectedWallet.address}`
                }
              )
            ] })
          }
        ),
        hasExternalWallets && /* @__PURE__ */ jsx(Fragment, { children: filteredExternalWallets.map((wallet) => /* @__PURE__ */ jsx(
          WalletMenuItem,
          {
            onClick: () => handleSelectWallet(wallet.address),
            className: "oui-w-[368px]",
            children: /* @__PURE__ */ jsxs(Flex, { direction: "column", itemAlign: "start", gapY: 1, children: [
              /* @__PURE__ */ jsx(Text, { size: "2xs", className: "oui-text-base-contrast-54", children: t("common.externalWallet") }),
              /* @__PURE__ */ jsx(
                Text,
                {
                  size: "2xs",
                  intensity: 54,
                  className: "oui-text-base-contrast-36 oui-leading-[15px]",
                  children: `(${getChainLabel(wallet.network)}) ${wallet.address}`
                }
              )
            ] })
          },
          wallet.address
        )) }),
        /* @__PURE__ */ jsx(
          WalletMenuItem,
          {
            onClick: handleAddExternalWalletClick,
            className: cn(
              "oui-justify-center",
              hasExternalWallets && "oui-w-full",
              !hasExternalWallets && "oui-w-[190px]"
            ),
            children: /* @__PURE__ */ jsxs(
              Flex,
              {
                itemAlign: "center",
                gapX: 1,
                className: "oui-text-primary oui-font-semibold oui-tracking-[0.03em]",
                children: [
                  /* @__PURE__ */ jsx(AddIcon, { className: "oui-size-[18px]" }),
                  /* @__PURE__ */ jsx(Text, { size: "2xs", children: t("transfer.withdraw.addExternalWallet") })
                ]
              }
            )
          }
        )
      ]
    }
  ) });
  return /* @__PURE__ */ jsxs(Flex, { justify: "between", className: "oui-w-full oui-mb-3", children: [
    /* @__PURE__ */ jsx(Text, { size: "2xs", intensity: 36, children: t("common.wallet") }),
    /* @__PURE__ */ jsxs(DropdownMenuRoot, { open: isOpen, onOpenChange: setIsOpen, children: [
      /* @__PURE__ */ jsx(DropdownMenuTrigger, { asChild: true, children: /* @__PURE__ */ jsxs("div", { className: "oui-flex oui-items-center oui-gap-1 oui-cursor-pointer oui-text-base-contrast-54 hover:oui-text-base-contrast-80", children: [
        selectedWalletOpt?.name && /* @__PURE__ */ jsx(WalletIcon, { name: selectedWalletOpt.name, size: "3xs" }),
        /* @__PURE__ */ jsx(Text.formatted, { size: "2xs", intensity: 54, rule: "address", children: selectedWalletOpt?.address || "" }),
        /* @__PURE__ */ jsx(
          Text,
          {
            size: "2xs",
            intensity: 54,
            className: "oui-text-base-contrast-36",
            children: ` (${getChainLabel(selectedWalletOpt?.network)})`
          }
        ),
        isOpen ? /* @__PURE__ */ jsx(CaretUpIcon, { size: 12, className: "oui-text-inherit" }) : /* @__PURE__ */ jsx(CaretDownIcon, { size: 12, className: "oui-text-inherit" })
      ] }) }),
      dropdownContent
    ] })
  ] });
};
var WithdrawForm = (props) => {
  const {
    address,
    walletName,
    loading,
    disabled,
    quantity,
    onQuantityChange,
    sourceToken,
    amount,
    maxQuantity,
    tokenChains,
    currentChain,
    fee,
    settingChain,
    crossChainTrans,
    checkIsBridgeless,
    withdrawTo,
    sourceTokens,
    onSourceTokenChange,
    vaultBalanceList,
    qtyGreaterThanMaxAmount,
    isTokenUnsupported,
    onSwitchToSupportedNetwork,
    externalWallets,
    selectedWalletAddress,
    onSelectWallet,
    onAddExternalWallet,
    isEnableTrading,
    enableWithdrawToExternalWallet
  } = props;
  const { t } = useTranslation();
  const [addWalletOpen, setAddWalletOpen] = useState(false);
  const handleAddExternalWallet = (address2, network) => {
    onAddExternalWallet?.(address2, network);
  };
  const internalWithdrawPanel = /* @__PURE__ */ jsxs(
    TabPanel,
    {
      title: t("transfer.withdraw.otherAccount", {
        brokerName: props.brokerName
      }),
      value: "accountId" /* Account */,
      children: [
        /* @__PURE__ */ jsx(
          TextAreaInput,
          {
            label: t("common.accountId"),
            value: props.toAccountId,
            onChange: props.setToAccountId,
            status: props.toAccountIdInputStatus,
            hintMessage: props.toAccountIdHintMessage,
            disabled: !props.isLoggedIn,
            placeholder: t("transfer.withdraw.accountIdOrAddress.placeholder"),
            enableAccountLookup: true,
            accountInfo: props.toAccountInfo,
            accountDropdownOpen: props.toAccountInfoDropdownOpen,
            setAccountDropdownOpen: props.setToAccountInfoDropdownOpen
          }
        ),
        /* @__PURE__ */ jsx(Box, { mb: 1, px: 2, children: /* @__PURE__ */ jsx(Text, { size: "xs", intensity: 54, children: t("transfer.withdraw.accountIdOrAddress.hint") }) })
      ]
    }
  );
  return /* @__PURE__ */ jsxs(
    Box,
    {
      id: "oui-withdraw-form",
      className: textVariants({ weight: "semibold" }),
      children: [
        /* @__PURE__ */ jsxs(Box, { className: "oui-mb-6 lg:oui-mb-8", children: [
          /* @__PURE__ */ jsx(BrokerWallet, {}),
          /* @__PURE__ */ jsx(Box, { mt: 3, mb: 1, children: /* @__PURE__ */ jsx(
            QuantityInput,
            {
              value: quantity,
              onValueChange: onQuantityChange,
              token: sourceToken,
              tokens: sourceTokens,
              onTokenChange: onSourceTokenChange,
              status: props.inputStatus,
              hintMessage: props.hintMessage,
              hintSuffix: isTokenUnsupported ? /* @__PURE__ */ jsxs(
                "button",
                {
                  type: "button",
                  onClick: onSwitchToSupportedNetwork,
                  className: "oui-inline-flex oui-items-center oui-gap-1 oui-text-2xs oui-font-semibold oui-text-primary",
                  children: [
                    t("common.switch"),
                    /* @__PURE__ */ jsx(
                      ArrowLeftRightIcon,
                      {
                        size: 16,
                        className: "oui-text-primary oui-mt-0.5",
                        opacity: 1
                      }
                    )
                  ]
                }
              ) : void 0,
              vaultBalanceList,
              testId: "oui-testid-withdraw-dialog-quantity-input",
              displayType: "vaultBalance",
              disabled: !props.isLoggedIn
            }
          ) }),
          /* @__PURE__ */ jsx(
            AvailableQuantity,
            {
              token: sourceToken,
              quantity: amount,
              maxQuantity: maxQuantity.toString(),
              loading: props.balanceRevalidating,
              onClick: () => {
                onQuantityChange(maxQuantity.toString());
              },
              tooltipContent: t("transfer.withdraw.available.tooltip", {
                amount: maxQuantity.toString()
              })
            }
          ),
          /* @__PURE__ */ jsx(Box, { mx: 2, mt: 1, children: /* @__PURE__ */ jsx(
            UnsettlePnlInfo,
            {
              unsettledPnl: props.unsettledPnL,
              hasPositions: props.hasPositions,
              onSettlePnl: props.onSettlePnl,
              tooltipContent: t("settle.unsettled.tooltip"),
              dialogContent: /* @__PURE__ */ jsx(Trans, { i18nKey: "settle.settlePnl.description" })
            }
          ) }),
          /* @__PURE__ */ jsx(ExchangeDivider, {}),
          /* @__PURE__ */ jsxs(
            Tabs,
            {
              value: withdrawTo,
              onValueChange: props.setWithdrawTo,
              variant: "contained",
              size: "lg",
              classNames: {
                tabsList: "oui-px-0",
                tabsContent: "oui-pt-3"
              },
              children: [
                /* @__PURE__ */ jsxs(
                  TabPanel,
                  {
                    title: t("transfer.web3Wallet.my"),
                    icon: props.walletName ? /* @__PURE__ */ jsx(WalletIcon, { size: "xs", name: props.walletName ?? "" }) : void 0,
                    value: "wallet" /* Wallet */,
                    children: [
                      isEnableTrading && enableWithdrawToExternalWallet && /* @__PURE__ */ jsx(
                        WalletSelector,
                        {
                          connectedWallet: address ? {
                            name: walletName || t("common.wallet"),
                            address,
                            namespace: currentChain?.namespace
                          } : void 0,
                          externalWallets: externalWallets || [],
                          selectedAddress: selectedWalletAddress ?? "",
                          onSelect: onSelectWallet,
                          onAddExternalWallet: () => setAddWalletOpen(true)
                        }
                      ),
                      /* @__PURE__ */ jsx(Box, { mb: 1, children: /* @__PURE__ */ jsx(
                        ChainSelect,
                        {
                          chains: tokenChains,
                          value: currentChain,
                          onValueChange: props.onChainChange,
                          wrongNetwork: props.wrongNetwork,
                          loading: settingChain,
                          disabled: !props.isLoggedIn
                        }
                      ) }),
                      /* @__PURE__ */ jsx(
                        QuantityInput,
                        {
                          classNames: {
                            root: "oui-mt-[2px] oui-rounded-t-sm oui-rounded-b-xl"
                          },
                          token: sourceToken,
                          value: props.showQty,
                          readOnly: true
                        }
                      )
                    ]
                  }
                ),
                internalWithdrawPanel
              ]
            }
          ),
          /* @__PURE__ */ jsx(Box, { mt: 2, px: 2, children: /* @__PURE__ */ jsx(
            LtvWidget,
            {
              showDiff: typeof quantity !== "undefined" && Number(quantity) > 0,
              currentLtv: props.currentLTV,
              nextLTV: props.nextLTV
            }
          ) }),
          /* @__PURE__ */ jsx(Flex, { direction: "column", mt: 1, gapY: 1, px: 2, itemAlign: "start", children: /* @__PURE__ */ jsxs(Text, { size: "xs", intensity: 36, children: [
            t("common.fee"),
            withdrawTo === "wallet" /* Wallet */ ? " \u2248 " : " = ",
            /* @__PURE__ */ jsx(Text, { size: "xs", intensity: 80, children: fee })
          ] }) })
        ] }),
        /* @__PURE__ */ jsx(
          WithdrawWarningMessage,
          {
            checkIsBridgeless,
            crossChainTrans,
            qtyGreaterThanMaxAmount,
            message: props.warningMessage
          }
        ),
        /* @__PURE__ */ jsx(Flex, { justify: "center", children: /* @__PURE__ */ jsx(
          WithdrawAction,
          {
            checkIsBridgeless,
            networkId: props.networkId,
            disabled,
            loading,
            onWithdraw: props.onWithdraw,
            crossChainWithdraw: props.crossChainWithdraw,
            currentChain,
            address,
            quantity,
            fee,
            withdrawTo,
            onTransfer: props.onTransfer
          }
        ) }),
        enableWithdrawToExternalWallet && /* @__PURE__ */ jsx(
          AddWalletDialog,
          {
            open: addWalletOpen,
            onOpenChange: setAddWalletOpen,
            onConfirm: handleAddExternalWallet,
            chain: currentChain
          }
        )
      ]
    }
  );
};
var WithdrawFormWidget = (props) => {
  const state = useWithdrawFormScript(props);
  return /* @__PURE__ */ jsx(WithdrawForm, { ...state });
};
var DepositSlot = (props) => {
  return /* @__PURE__ */ jsx(
    ExtensionSlot,
    {
      position: ExtensionPositionEnum.DepositForm,
      defaultWidget: DepositFormWidget,
      ...props
    }
  );
};
var WithdrawSlot = (props) => {
  return /* @__PURE__ */ jsx(
    ExtensionSlot,
    {
      position: ExtensionPositionEnum.WithdrawForm,
      defaultWidget: WithdrawFormWidget,
      ...props
    }
  );
};
var DepositAndWithdrawWithDialogId = "DepositAndWithdrawWithDialogId";
var DepositAndWithdrawWithSheetId = "DepositAndWithdrawWithSheetId";
var DepositAndWithdraw = (props) => {
  const [activeTab, setActiveTab] = useState(
    props.activeTab || "deposit"
  );
  const { t } = useTranslation();
  return /* @__PURE__ */ jsxs(
    Tabs,
    {
      value: activeTab,
      onValueChange: setActiveTab,
      variant: "contained",
      size: "lg",
      classNames: {
        tabsList: "oui-px-0",
        tabsContent: "oui-pt-5"
      },
      children: [
        /* @__PURE__ */ jsx(
          TabPanel,
          {
            title: t("common.deposit"),
            icon: /* @__PURE__ */ jsx(ArrowDownSquareFillIcon, {}),
            value: "deposit",
            children: /* @__PURE__ */ jsx(DepositSlot, { close: props.close })
          }
        ),
        /* @__PURE__ */ jsx(
          TabPanel,
          {
            title: t("common.withdraw"),
            icon: /* @__PURE__ */ jsx(ArrowUpSquareFillIcon, {}),
            value: "withdraw",
            children: /* @__PURE__ */ jsx(WithdrawSlot, { close: props.close })
          }
        )
      ]
    }
  );
};
registerSimpleDialog(DepositAndWithdrawWithDialogId, DepositAndWithdraw, {
  size: "md",
  classNames: {
    content: "oui-border oui-border-line-6"
  }
});
registerSimpleSheet(DepositAndWithdrawWithSheetId, DepositAndWithdraw);
var AccountSelect = (props) => {
  const { subAccounts = [], value } = props;
  const [open, setOpen] = useState(false);
  const selectable = subAccounts.length > 1;
  const trigger = /* @__PURE__ */ jsxs(
    Flex,
    {
      intensity: 500,
      className: cn(
        "oui-rounded-b-sm oui-rounded-t-xl oui-border oui-border-line",
        selectable ? "oui-cursor-pointer" : "oui-cursor-auto oui-bg-base-6"
      ),
      height: 54,
      px: 3,
      justify: "between",
      itemAlign: "center",
      children: [
        /* @__PURE__ */ jsxs(Flex, { direction: "column", itemAlign: "start", children: [
          /* @__PURE__ */ jsx(
            Text.formatted,
            {
              size: "xs",
              intensity: 80,
              rule: value?.description ? void 0 : "address",
              children: value?.description || value?.id
            }
          ),
          /* @__PURE__ */ jsxs(Text, { intensity: 36, size: "2xs", children: [
            `ID: `,
            /* @__PURE__ */ jsx(Text.formatted, { rule: "address", children: value?.id })
          ] })
        ] }),
        selectable && /* @__PURE__ */ jsx(ExchangeIcon, { className: "oui-text-base-contrast-54" })
      ]
    }
  );
  const content = subAccounts.map((subAccount, index) => {
    const isActive = subAccount.id === value?.id;
    const asset = subAccount?.holding?.find(
      (item) => item.token === "USDC"
    )?.holding;
    return /* @__PURE__ */ jsxs(
      Flex,
      {
        px: 2,
        r: "base",
        justify: "between",
        className: cn(
          "oui-transfer-form-account-select-item",
          "oui-cursor-pointer oui-py-[6px] hover:oui-bg-base-5",
          isActive && "oui-bg-base-5",
          index !== 0 && "oui-mt-[2px]"
        ),
        onClick: async () => {
          setOpen(false);
          props.onValueChange?.(subAccount);
        },
        children: [
          /* @__PURE__ */ jsx(Flex, { gapX: 1, itemAlign: "center", children: /* @__PURE__ */ jsxs(Flex, { direction: "column", itemAlign: "start", className: "oui-text-2xs", children: [
            /* @__PURE__ */ jsx(
              Text.formatted,
              {
                rule: subAccount?.description ? void 0 : "address",
                intensity: 80,
                children: subAccount?.description || subAccount?.id
              }
            ),
            /* @__PURE__ */ jsxs(Text, { intensity: 36, size: "2xs", children: [
              `ID: `,
              /* @__PURE__ */ jsx(Text.formatted, { rule: "address", children: subAccount?.id })
            ] })
          ] }) }),
          /* @__PURE__ */ jsxs(Flex, { gapX: 3, children: [
            /* @__PURE__ */ jsx(Text.numeral, { size: "2xs", intensity: 54, unit: " USDC", children: asset || 0 }),
            isActive && /* @__PURE__ */ jsx(
              Box,
              {
                width: 4,
                height: 4,
                r: "full",
                className: "oui-transfer-form-account-select-active-dot oui-bg-[linear-gradient(270deg,#59B0FE_0%,#26FEFE_100%)]"
              }
            )
          ] })
        ]
      },
      subAccount.id
    );
  });
  return /* @__PURE__ */ jsxs(DropdownMenuRoot, { open: selectable ? open : false, onOpenChange: setOpen, children: [
    /* @__PURE__ */ jsx(DropdownMenuTrigger, { asChild: true, children: trigger }),
    /* @__PURE__ */ jsx(DropdownMenuPortal, { children: /* @__PURE__ */ jsx(
      DropdownMenuContent,
      {
        onCloseAutoFocus: (e) => e.preventDefault(),
        align: "start",
        sideOffset: 2,
        className: cn(
          "oui-transfer-form-account-select-dropdown-menu-content",
          "oui-font-semibold",
          "oui-bg-base-8 oui-p-1",
          "oui-w-[var(--radix-dropdown-menu-trigger-width)]",
          "oui-select-none oui-rounded-md"
        ),
        children: /* @__PURE__ */ jsx(ScrollArea, { children: /* @__PURE__ */ jsxs("div", { className: "oui-max-h-[254px]", children: [
          content,
          " "
        ] }) })
      }
    ) })
  ] });
};
var TransferForm = (props) => {
  const {
    networkId,
    disabled,
    onTransfer,
    quantity,
    onQuantityChange,
    amount,
    tokens,
    token,
    onTokenChange,
    maxQuantity,
    submitting,
    hintMessage,
    inputStatus,
    hasPositions,
    onSettlePnl,
    unsettledPnL,
    toAccountAsset,
    fromAccounts,
    toAccounts,
    fromAccount,
    onFromAccountChange,
    toAccount,
    onToAccountChange,
    onExchange
  } = props;
  const { t } = useTranslation();
  const buttonSize = { initial: "md", lg: "lg" };
  return /* @__PURE__ */ jsxs(Box, { id: "oui-deposit-form", className: textVariants({ weight: "semibold" }), children: [
    /* @__PURE__ */ jsxs(Box, { className: "oui-mb-6 lg:oui-mb-8", children: [
      /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 98, children: t("transfer.internalTransfer.from") }),
      /* @__PURE__ */ jsxs(Box, { mt: 1, mb: 1, children: [
        /* @__PURE__ */ jsx(
          AccountSelect,
          {
            subAccounts: fromAccounts,
            value: fromAccount,
            onValueChange: onFromAccountChange
          }
        ),
        /* @__PURE__ */ jsx(
          QuantityInput,
          {
            classNames: {
              root: "oui-mt-[2px] oui-rounded-t-sm oui-rounded-b-xl"
            },
            value: quantity,
            onValueChange: onQuantityChange,
            tokens,
            token,
            onTokenChange,
            hintMessage,
            status: inputStatus
          }
        )
      ] }),
      /* @__PURE__ */ jsx(
        AvailableQuantity,
        {
          token,
          quantity: amount,
          maxQuantity,
          onClick: () => {
            onQuantityChange(maxQuantity.toString());
          }
        }
      ),
      /* @__PURE__ */ jsx(Box, { mx: 2, mt: 1, children: /* @__PURE__ */ jsx(
        UnsettlePnlInfo,
        {
          unsettledPnl: unsettledPnL,
          hasPositions,
          onSettlePnl,
          tooltipContent: t("transfer.internalTransfer.unsettled.tooltip"),
          dialogContent: (
            // @ts-ignore
            /* @__PURE__ */ jsx(Trans, { i18nKey: "transfer.internalTransfer.settlePnl.description" })
          )
        }
      ) }),
      /* @__PURE__ */ jsx(
        ExchangeDivider,
        {
          icon: /* @__PURE__ */ jsx(
            TransferVerticalIcon,
            {
              className: "oui-cursor-pointer oui-text-primary",
              onClick: onExchange
            }
          )
        }
      ),
      /* @__PURE__ */ jsx(Text, { size: "sm", intensity: 98, children: t("transfer.internalTransfer.to") }),
      /* @__PURE__ */ jsxs(Box, { mt: 1, children: [
        /* @__PURE__ */ jsx(
          AccountSelect,
          {
            subAccounts: toAccounts,
            value: toAccount,
            onValueChange: onToAccountChange
          }
        ),
        /* @__PURE__ */ jsxs(
          Flex,
          {
            className: cn(
              "oui-mt-[2px] oui-h-[31px] oui-text-base-contrast-54",
              "oui-rounded-b-xl oui-rounded-t-sm"
            ),
            justify: "between",
            itemAlign: "center",
            px: 3,
            intensity: 600,
            children: [
              /* @__PURE__ */ jsx(Text, { size: "2xs", children: t("transfer.internalTransfer.currentAssetValue") }),
              /* @__PURE__ */ jsx(
                Text.numeral,
                {
                  size: "2xs",
                  intensity: 54,
                  unit: ` ${token.symbol}`,
                  dp: token?.precision,
                  padding: false,
                  children: toAccountAsset
                }
              )
            ]
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsx(Flex, { justify: "center", children: /* @__PURE__ */ jsx(Box, { className: "oui-w-full lg:oui-w-auto lg:oui-min-w-[184px]", children: /* @__PURE__ */ jsx(
      AuthGuard,
      {
        networkId,
        buttonProps: {
          fullWidth: true,
          size: buttonSize
        },
        children: /* @__PURE__ */ jsx(
          Button,
          {
            fullWidth: true,
            disabled,
            loading: submitting,
            size: buttonSize,
            onClick: onTransfer,
            children: t("common.transfer")
          }
        )
      }
    ) }) })
  ] });
};
var DEFAULT_TOKEN = {
  symbol: "USDC"
};
var useTransferFormScript = (options2) => {
  const { t } = useTranslation();
  const [quantity, setQuantity] = useState("");
  const [fromAccount, setFromAccount] = useState();
  const [toAccount, setToAccount] = useState();
  const [mainAccount, setMainAccount] = useState();
  const [tokens, setTokens] = useState([DEFAULT_TOKEN]);
  const [token, setToken] = useState(DEFAULT_TOKEN);
  const [holdingMap, setHoldingMap] = useState(
    {}
  );
  const networkId = useConfig("networkId");
  const { state, isMainAccount, subAccount } = useAccount();
  const tokensInfo = useAppStore((state2) => state2.tokensInfo);
  const {
    transfer,
    submitting,
    maxAmount: currentMaxAmount,
    unsettledPnL: currentUnsettledPnL
  } = useTransfer({ fromAccountId: fromAccount?.id, token: token.symbol });
  const subAccounts = state.subAccounts;
  const mainAccountId = state.mainAccountId;
  const accountId = state.accountId;
  const observerAccountId = isMainAccount ? fromAccount?.id !== mainAccountId ? fromAccount?.id : void 0 : fromAccount?.id;
  const { hasPositions: currentHasPositions, onSettlePnl } = useSettlePnl({
    accountId: fromAccount?.id
  });
  const { portfolio, positions } = useSubAccountDataObserver(observerAccountId);
  const subAccountMaxAmount = useSubAccountMaxWithdrawal({
    token: token.symbol,
    unsettledPnL: portfolio?.unsettledPnL,
    freeCollateral: portfolio?.freeCollateral ?? zero,
    holdings: portfolio?.holding
  });
  const formHasPositions = useMemo(
    () => !!positions?.rows?.length,
    [positions]
  );
  const hasPositions = useMemo(() => {
    return observerAccountId ? formHasPositions : currentHasPositions;
  }, [observerAccountId, formHasPositions, currentHasPositions]);
  const { unsettledPnL, maxQuantity } = useMemo(() => {
    if (observerAccountId) {
      return {
        unsettledPnL: portfolio?.unsettledPnL,
        maxQuantity: subAccountMaxAmount
      };
    }
    return {
      unsettledPnL: currentUnsettledPnL,
      maxQuantity: currentMaxAmount
    };
  }, [
    observerAccountId,
    currentUnsettledPnL,
    currentMaxAmount,
    portfolio?.unsettledPnL,
    subAccountMaxAmount
  ]);
  const { inputStatus, hintMessage } = useInputStatus({
    quantity,
    maxQuantity,
    unsettledPnL
  });
  const onTransfer = useCallback(() => {
    const num = Number(quantity);
    if (isNaN(num) || num <= 0) {
      toast.error(t("transfer.quantity.invalid"));
      return;
    }
    if (submitting || !toAccount) return;
    transfer(token.symbol, {
      account_id: toAccount.id,
      amount: new Decimal(quantity).toNumber()
    }).then(() => {
      toast.success(t("transfer.internalTransfer.success"));
      setQuantity("");
      options2.close?.();
    }).catch((err) => {
      console.error("transfer error: ", err);
      const errorMsg = getTransferErrorMessage(err.code);
      toast.error(errorMsg);
    });
  }, [t, token, quantity, submitting, transfer, toAccount]);
  const disabled = submitting || !quantity || inputStatus === "error";
  const amount = useMemo(() => {
    const markPrice2 = 1;
    return new Decimal(quantity || 0).mul(markPrice2).toNumber();
  }, [quantity]);
  const toAccountAsset = useMemo(() => {
    if (!toAccount?.id) {
      return 0;
    }
    const holdings = holdingMap[toAccount.id];
    const asset = holdings?.find((item) => item.token === token.symbol);
    return asset?.holding || 0;
  }, [toAccount, token, holdingMap]);
  const { fromAccounts, toAccounts } = useMemo(() => {
    if (isMainAccount) {
      return {
        fromAccounts: mainAccount ? [mainAccount, ...subAccounts || []] : [],
        toAccounts: (
          // only from account is main account, can transfer to other sub accounts
          fromAccount?.id === mainAccountId ? subAccounts : []
        )
      };
    }
    return {
      fromAccounts: [],
      toAccounts: []
    };
  }, [isMainAccount, mainAccountId, mainAccount, subAccounts, fromAccount]);
  useEffect(() => {
    if (!mainAccountId) return;
    const _mainAccount = {
      id: mainAccountId,
      description: t("common.mainAccount"),
      holding: []
    };
    setMainAccount(_mainAccount);
    subAccount.refresh().then((res) => {
      setHoldingMap(res);
      setMainAccount({
        ..._mainAccount,
        holding: res[mainAccountId]
      });
    });
  }, [t, mainAccountId]);
  useEffect(() => {
    setFromAccount(
      isMainAccount ? mainAccount : subAccounts?.find((item) => item.id === accountId)
    );
  }, [isMainAccount, mainAccount, subAccounts, accountId]);
  useEffect(() => {
    if (isMainAccount) {
      const firstSubAccount = subAccounts?.[0];
      const selectAccount = options2.toAccountId ? subAccounts?.find((item) => item.id === options2.toAccountId) || firstSubAccount : firstSubAccount;
      if (selectAccount) {
        setToAccount(selectAccount);
      }
    } else {
      setToAccount(mainAccount);
    }
  }, [options2?.toAccountId, isMainAccount, mainAccount, subAccounts]);
  useEffect(() => {
    const tokens2 = tokensInfo?.map((item) => ({
      symbol: item.token,
      precision: item.decimals
    }));
    if (tokens2?.length) {
      tokens2.sort((a, b) => {
        if (a.symbol === "USDC") return -1;
        if (b.symbol === "USDC") return 1;
        return 0;
      });
      const targetToken = tokens2?.find((item) => item.symbol === options2.token);
      setTokens(tokens2);
      setToken(targetToken || tokens2?.[0] || DEFAULT_TOKEN);
    }
  }, [tokensInfo, options2.token]);
  const onFromAccountChange = useCallback(
    (account2) => {
      setFromAccount(account2);
      const firstSubAccount = subAccounts?.[0];
      if (account2?.id === mainAccountId) {
        setToAccount(firstSubAccount);
        return;
      }
      if (account2?.id && account2?.id !== mainAccountId) {
        setToAccount(mainAccount);
        return;
      }
    },
    [mainAccountId, mainAccount, subAccounts]
  );
  const onToAccountChange = useCallback((account2) => {
    setToAccount(account2);
  }, []);
  const onExchange = useCallback(() => {
    setFromAccount(toAccount);
    setToAccount(fromAccount);
  }, [fromAccount, toAccount]);
  return {
    networkId,
    onTransfer,
    quantity,
    amount,
    onQuantityChange: setQuantity,
    maxQuantity,
    tokens,
    token,
    onTokenChange: setToken,
    disabled,
    submitting,
    hintMessage,
    inputStatus,
    hasPositions,
    onSettlePnl,
    unsettledPnL,
    toAccountAsset,
    fromAccount,
    toAccount,
    fromAccounts,
    onFromAccountChange,
    toAccounts,
    onToAccountChange,
    onExchange,
    isMainAccount
  };
};
function useInputStatus(options2) {
  const { quantity, maxQuantity, unsettledPnL = 0 } = options2;
  const { t } = useTranslation();
  const [inputStatus, setInputStatus] = useState("default");
  const [hintMessage, setHintMessage] = useState();
  useEffect(() => {
    if (!quantity) {
      setInputStatus("default");
      setHintMessage("");
      return;
    }
    const qty = new Decimal(quantity);
    if (unsettledPnL < 0) {
      if (qty.gt(maxQuantity)) {
        setInputStatus("error");
        setHintMessage(t("transfer.insufficientBalance"));
      } else {
        setInputStatus("default");
        setHintMessage("");
      }
    } else {
      if (qty.gt(maxQuantity)) {
        setInputStatus("error");
        setHintMessage(t("transfer.insufficientBalance"));
      } else if (qty.gt(new Decimal(maxQuantity).minus(unsettledPnL)) && qty.lessThanOrEqualTo(maxQuantity)) {
        setInputStatus("warning");
        setHintMessage(t("settle.settlePnl.warning"));
      } else {
        setInputStatus("default");
        setHintMessage("");
      }
    }
  }, [quantity, maxQuantity]);
  return { inputStatus, hintMessage };
}
var TransferDialogId = "TransferDialogId";
var TransferSheetId = "TransferSheetId";
var TransferFormWidget = (props) => {
  const state = useTransferFormScript({
    toAccountId: props.toAccountId,
    token: props.token,
    close: props.close
  });
  return /* @__PURE__ */ jsx(TransferForm, { ...state });
};
var TransferWidget = (props) => {
  return /* @__PURE__ */ jsx(
    TransferFormWidget,
    {
      close: props.close,
      toAccountId: props.toAccountId,
      token: props.token
    }
  );
};
registerSimpleDialog(TransferDialogId, TransferWidget, {
  size: "md",
  classNames: {
    content: "oui-border oui-border-line-6"
  },
  title: () => i18n.t("common.transfer")
});
registerSimpleSheet(TransferSheetId, TransferWidget, {
  title: () => i18n.t("common.transfer")
});
var ConvertAction = (props) => {
  const { disabled, loading, networkId, onConvert } = props;
  const { t } = useTranslation();
  return /* @__PURE__ */ jsx(Box, { className: "oui-w-full lg:oui-w-auto lg:oui-min-w-[184px]", children: /* @__PURE__ */ jsx(
    AuthGuard,
    {
      status: AccountStatusEnum.EnableTrading,
      networkId,
      buttonProps: { fullWidth: true, size: { initial: "md", lg: "lg" } },
      children: /* @__PURE__ */ jsx(
        Button,
        {
          fullWidth: true,
          disabled,
          loading,
          size: { initial: "md", lg: "lg" },
          onClick: onConvert,
          children: t("transfer.convert")
        }
      )
    }
  ) });
};
var SwapCoin = (props) => {
  const {
    sourceSymbol,
    targetSymbol,
    indexPrice,
    precision = 6,
    suffix
  } = props;
  return /* @__PURE__ */ jsxs(
    Flex,
    {
      itemAlign: "center",
      gap: 1,
      className: cn(props.className, "oui-text-2xs oui-text-base-contrast-36"),
      children: [
        /* @__PURE__ */ jsx(Text, { intensity: 80, children: "1" }),
        /* @__PURE__ */ jsx("span", { children: sourceSymbol }),
        /* @__PURE__ */ jsx("span", { children: "=" }),
        /* @__PURE__ */ jsx(Text.numeral, { intensity: 80, dp: precision, padding: false, children: indexPrice }),
        /* @__PURE__ */ jsx("span", { children: targetSymbol }),
        suffix
      ]
    }
  );
};
var splitTokenBySymbol = (items) => {
  return items.reduce(
    (result, item) => {
      if (item.token?.toUpperCase() === "USDC") {
        result.usdc.push(item);
      } else {
        result.others.push(item);
      }
      return result;
    },
    { usdc: [], others: [] }
  );
};
var findChainInfo = (tokenInfo) => {
  const arbitrumChainInfo = tokenInfo.chain_details.find(
    (item) => parseInt(item.chain_id) === Arbitrum.id
  );
  const nativeTokenChainInfo = tokenInfo.chain_details.find(
    (item) => !item.contract_address
  );
  const nativeTokenAddress2 = "0x0000000000000000000000000000000000000000";
  if (arbitrumChainInfo) {
    return {
      contract_address: arbitrumChainInfo.contract_address || nativeTokenAddress2,
      quoteChainId: arbitrumChainInfo.chain_id,
      decimals: arbitrumChainInfo.decimals
    };
  }
  if (nativeTokenChainInfo) {
    return {
      contract_address: nativeTokenAddress2,
      quoteChainId: nativeTokenChainInfo.chain_id,
      decimals: nativeTokenChainInfo.decimals
    };
  }
  return {
    contract_address: tokenInfo.chain_details[0]?.contract_address,
    quoteChainId: tokenInfo.chain_details[0]?.chain_id,
    decimals: tokenInfo.chain_details[0]?.decimals
  };
};
var useToken3 = (options2) => {
  const { defaultValue } = options2;
  const [sourceToken, setSourceToken] = useState();
  const [targetToken, setTargetToken] = useState();
  const [sourceTokens, setSourceTokens] = useState([]);
  const tokensInfo = useTokensInfo();
  const newTokensInfo = useMemo(() => {
    const filteredTokensInfo = tokensInfo.filter((item) => item.on_chain_swap);
    return filteredTokensInfo.map((item) => {
      const chainInfo = findChainInfo(item);
      return {
        ...item,
        symbol: item.token,
        precision: item.decimals ?? 6,
        ...chainInfo
      };
    });
  }, [tokensInfo]);
  useEffect(() => {
    const { usdc, others } = splitTokenBySymbol(newTokensInfo);
    setSourceToken(() => {
      if (defaultValue) {
        const defaultToken = others.find(({ token }) => token === defaultValue);
        return defaultToken ? defaultToken : others[0];
      }
      return others[0];
    });
    setSourceTokens(others);
    setTargetToken(usdc[0]);
  }, [defaultValue, newTokensInfo]);
  const targetChainInfo = useMemo(() => {
    const info = targetToken?.chain_details?.find(
      (item) => item.chain_id === sourceToken?.quoteChainId
    );
    return {
      ...info,
      precision: targetToken?.precision
    };
  }, [sourceToken, targetToken]);
  return {
    sourceToken,
    sourceTokens,
    onSourceTokenChange: setSourceToken,
    targetToken,
    targetChainInfo
  };
};

// src/components/convertForm/convertForm.script.tsx
var { calcMinimumReceived } = account;
var ORDERLY_CONVERT_SLIPPAGE_KEY = "orderly_convert_slippage";
var normalizeAmount = (amount, decimals) => {
  return new Decimal(amount).mul(new Decimal(10).pow(decimals)).toFixed(0);
};
var unnormalizeAmount = (amount, decimals) => {
  return new Decimal(amount).div(new Decimal(10).pow(decimals)).toString();
};
var useConvertFormScript = (options2) => {
  const { token: defaultToken, close } = options2;
  const { t } = useTranslation();
  const [loading, setLoading] = useState(false);
  const config = useConfig();
  const networkId = config.get("networkId");
  const [quantity, setQuantity] = useState("");
  const { wrongNetwork } = useAppContext();
  const { wallet } = useWalletConnector();
  const {
    sourceToken,
    sourceTokens,
    onSourceTokenChange,
    targetToken,
    targetChainInfo
  } = useToken3({ defaultValue: defaultToken });
  const { walletName, address } = useMemo(
    () => ({
      walletName: wallet?.label,
      address: wallet?.accounts?.[0].address
    }),
    [wallet]
  );
  const onQuantityChange = (qty) => {
    setQuantity(qty);
  };
  const [slippage, setSlippage] = useLocalStorage(
    ORDERLY_CONVERT_SLIPPAGE_KEY,
    1
  );
  const { maxAmount, convert } = useConvert({ token: sourceToken?.token });
  const onConvert = async () => {
    if (loading) {
      return;
    }
    setLoading(true);
    return convert({
      amount: Number(quantity),
      slippage: new Decimal(slippage).div(100).toNumber()
    }).then(() => {
      toast.success(t("transfer.convert.completed"));
      close?.();
      setQuantity("");
    }).catch((err) => {
      toast.error(
        err.message?.includes("user rejected") ? t("transfer.convert.failed") : err.message
      );
    }).finally(() => {
      setLoading(false);
    });
  };
  const [postQuote, { data: quoteData, isMutating: isQuoteLoading }] = useOdosQuote();
  useEffect(() => {
    const { quoteChainId, contract_address, decimals } = sourceToken || {};
    const targetAddress = targetChainInfo?.contract_address;
    if (quantity && quoteChainId && contract_address && targetAddress) {
      postQuote({
        chainId: parseInt(quoteChainId),
        inputTokens: [
          {
            amount: normalizeAmount(quantity, decimals),
            tokenAddress: contract_address
          }
        ],
        outputTokens: [
          {
            proportion: 1,
            tokenAddress: targetAddress
          }
        ]
        // simple: true,
      });
    }
  }, [quantity, sourceToken, targetChainInfo, postQuote]);
  const memoizedOutAmounts = useMemo(() => {
    if (!quoteData || isQuoteLoading) {
      return "-";
    }
    return quoteData?.outAmounts[0];
  }, [quoteData, isQuoteLoading]);
  const memoizedConvertRate = useMemo(() => {
    if (!quoteData || isQuoteLoading) {
      return "-";
    }
    const rate = new Decimal(
      unnormalizeAmount(
        quoteData.outAmounts[0],
        targetChainInfo?.decimals ?? 6
      )
    ).div(
      unnormalizeAmount(quoteData.inAmounts[0], sourceToken?.decimals ?? 6)
    ).toString();
    return rate;
  }, [isQuoteLoading, quoteData, sourceToken, targetChainInfo]);
  const memoizedMinimumReceived = useMemo(() => {
    if (!quoteData || isQuoteLoading) {
      return 0;
    }
    return calcMinimumReceived({
      amount: quoteData?.outAmounts[0],
      slippage: Number(slippage)
    });
  }, [quoteData, isQuoteLoading, slippage]);
  const currentLtv = useComputedLTV();
  const nextLTV = useComputedLTV({
    input: Number(quantity),
    token: sourceToken?.token
  });
  const disabled = !quantity || Number(quantity) === 0;
  const { hasPositions, onSettlePnl } = useSettlePnl();
  return {
    walletName,
    address,
    quantity,
    onQuantityChange,
    token: sourceToken,
    sourceTokens,
    onSourceTokenChange,
    targetToken,
    balanceRevalidating: false,
    maxQuantity: maxAmount,
    disabled,
    loading,
    wrongNetwork,
    onConvert,
    hasPositions,
    onSettlePnl,
    networkId,
    slippage,
    onSlippageChange: setSlippage,
    convertRate: memoizedConvertRate,
    minimumReceived: memoizedMinimumReceived,
    outAmounts: memoizedOutAmounts,
    isQuoteLoading,
    currentLTV: currentLtv,
    nextLTV,
    targetChainInfo
  };
};
var ConvertFormUI = (props) => {
  const {
    loading,
    disabled,
    quantity,
    onQuantityChange,
    maxQuantity,
    token,
    targetToken,
    sourceTokens,
    onSourceTokenChange,
    onConvert,
    slippage,
    onSlippageChange,
    convertRate,
    outAmounts,
    minimumReceived,
    isQuoteLoading,
    currentLTV,
    nextLTV,
    networkId,
    balanceRevalidating,
    targetChainInfo
  } = props;
  return /* @__PURE__ */ jsxs(Box, { className: textVariants({ weight: "semibold" }), children: [
    /* @__PURE__ */ jsxs(Box, { className: "oui-mb-6 lg:oui-mb-8", children: [
      /* @__PURE__ */ jsx(Box, { mt: 3, mb: 1, children: /* @__PURE__ */ jsx(
        QuantityInput,
        {
          value: quantity,
          onValueChange: onQuantityChange,
          token,
          tokens: sourceTokens,
          onTokenChange: onSourceTokenChange
        }
      ) }),
      /* @__PURE__ */ jsx(
        AvailableQuantity,
        {
          token,
          quantity,
          maxQuantity: maxQuantity.toString(),
          loading: balanceRevalidating,
          onClick: () => {
            onQuantityChange(maxQuantity.toString());
          }
        }
      ),
      /* @__PURE__ */ jsx(ExchangeDivider, {}),
      /* @__PURE__ */ jsx(
        QuantityInput,
        {
          readOnly: true,
          loading: isQuoteLoading,
          token: targetToken,
          value: isQuoteLoading || !quantity || Number.isNaN(Number(outAmounts)) ? "" : unnormalizeAmount(outAmounts, targetChainInfo?.decimals ?? 6)
        }
      ),
      /* @__PURE__ */ jsxs(Flex, { direction: "column", itemAlign: "start", mt: 2, gap: 1, children: [
        /* @__PURE__ */ jsx(
          SwapCoin,
          {
            indexPrice: isQuoteLoading || !quantity || Number.isNaN(Number(convertRate)) ? "-" : convertRate,
            sourceSymbol: token?.token,
            targetSymbol: targetToken?.token
          }
        ),
        /* @__PURE__ */ jsx(Slippage, { value: slippage, onValueChange: onSlippageChange }),
        /* @__PURE__ */ jsx(
          MinimumReceived,
          {
            symbol: targetToken?.token || "",
            precision: targetChainInfo?.precision ?? 6,
            value: isQuoteLoading || !quantity || Number.isNaN(Number(minimumReceived)) ? "-" : unnormalizeAmount(
              minimumReceived.toString(),
              targetChainInfo?.decimals ?? 6
            )
          }
        ),
        /* @__PURE__ */ jsx(
          LtvWidget,
          {
            showDiff: typeof quantity !== "undefined" && Number(quantity) > 0,
            currentLtv: currentLTV,
            nextLTV
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsx(Flex, { itemAlign: "center", justify: "center", children: /* @__PURE__ */ jsx(
      ConvertAction,
      {
        networkId,
        disabled,
        loading,
        onConvert
      }
    ) })
  ] });
};
var ConvertDialogId = "ConvertDialogId";
var ConvertSheetId = "ConvertSheetId";
var ConvertFormWidget = (props) => {
  const state = useConvertFormScript(props);
  return /* @__PURE__ */ jsx(ConvertFormUI, { ...state });
};
registerSimpleDialog(ConvertDialogId, ConvertFormWidget, {
  size: "md",
  classNames: { content: "oui-border oui-border-line-6" },
  title: () => i18n.t("transfer.convert")
});
registerSimpleSheet(ConvertSheetId, ConvertFormWidget, {
  title: () => i18n.t("transfer.convert")
});
var DepositStatus = (props) => {
  const { canTrade, className, classNames, pendingCount, completedCount } = props;
  if (!canTrade || pendingCount === 0 && completedCount === 0) {
    return null;
  }
  return /* @__PURE__ */ jsxs(
    Flex,
    {
      direction: "column",
      gapY: 1,
      width: "100%",
      className: cn(className, classNames?.root),
      children: [
        /* @__PURE__ */ jsx(
          DepositStatusContent,
          {
            className: classNames?.items,
            count: pendingCount,
            status: AssetHistoryStatusEnum.PENDING,
            onClick: props.onClick,
            estimatedTime: props.estimatedTime,
            onClose: props.onClose
          }
        ),
        /* @__PURE__ */ jsx(
          DepositStatusContent,
          {
            className: classNames?.items,
            count: completedCount,
            status: AssetHistoryStatusEnum.COMPLETED,
            onClick: props.onClick,
            onClose: props.onClose
          }
        )
      ]
    }
  );
};
var DepositStatusContent = (props) => {
  const { t } = useTranslation();
  const { count, status, estimatedTime } = props;
  const content = useMemo(() => {
    if (status === AssetHistoryStatusEnum.PENDING) {
      return count > 1 ? t("transfer.deposit.status.pending.multiple") : t("transfer.deposit.status.pending.one");
    }
    return count > 1 ? t("transfer.deposit.status.completed.multiple") : t("transfer.deposit.status.completed.one");
  }, [t, status, count]);
  if (count === 0) {
    return null;
  }
  const clickable = typeof props.onClick === "function";
  const renderContent = () => {
    if (status === AssetHistoryStatusEnum.PENDING && count === 1 && estimatedTime) {
      return /* @__PURE__ */ jsxs(Text, { size: "2xs", color: "primary", children: [
        "~ ",
        estimatedTime
      ] });
    }
    return /* @__PURE__ */ jsx(Badge3, { children: props.count });
  };
  return /* @__PURE__ */ jsxs(
    Flex,
    {
      intensity: 900,
      gapX: 1,
      width: "100%",
      r: "full",
      className: cn(
        "oui-px-3 lg:oui-px-0",
        "oui-py-2 lg:oui-py-0",
        props.className
      ),
      children: [
        /* @__PURE__ */ jsxs(
          Flex,
          {
            justify: "between",
            itemAlign: "center",
            className: cn(
              "oui-font-normal",
              "oui-text-base-contrast-80 hover:oui-text-base-contrast",
              clickable && "oui-cursor-pointer"
            ),
            width: "100%",
            onClick: props.onClick,
            children: [
              /* @__PURE__ */ jsxs(Flex, { gapX: 1, children: [
                /* @__PURE__ */ jsx(
                  Dot,
                  {
                    className: status === AssetHistoryStatusEnum.COMPLETED ? "oui-bg-success" : "oui-bg-primary"
                  }
                ),
                /* @__PURE__ */ jsx(Text, { size: "2xs", children: content })
              ] }),
              renderContent()
            ]
          }
        ),
        /* @__PURE__ */ jsx(
          CloseIcon,
          {
            size: 14,
            opacity: 1,
            className: "oui-text-base-contrast-54 hover:oui-text-base-contrast",
            onClick: (e) => {
              e.preventDefault();
              e.stopPropagation();
              props.onClose(status);
            }
          }
        )
      ]
    }
  );
};
var Badge3 = (props) => {
  return /* @__PURE__ */ jsx(
    Flex,
    {
      justify: "center",
      itemAlign: "center",
      r: "full",
      width: 18,
      height: 18,
      className: cn("oui-bg-line-12", props.className),
      children: /* @__PURE__ */ jsx(Text, { size: "3xs", intensity: 80, children: props.children })
    }
  );
};
var Dot = (props) => {
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: cn(
        "oui-mx-[5px] oui-size-2 oui-shrink-0 oui-rounded-full",
        props.className
      )
    }
  );
};
var blockCount = 5;
async function getEvmBlockTime(chain) {
  const provider = new ethers.JsonRpcProvider(
    chain.network_infos.public_rpc_url
  );
  const latest = await provider.getBlockNumber();
  const blocks = await Promise.all(
    Array.from({ length: blockCount }, (_, i) => provider.getBlock(latest - i))
  );
  const timestamps = blocks.filter((b) => b?.timestamp != null).map((b) => Number(b?.timestamp));
  if (timestamps.length < 2) {
    return 0;
  }
  let sum = 0;
  for (let i = 0; i < timestamps.length - 1; i++) {
    const diff = timestamps[i] - timestamps[i + 1];
    sum += diff;
  }
  return sum / (timestamps.length - 1);
}
async function getSolanaBlockTime(chain, wallet) {
  if (!wallet) {
    return 0;
  }
  const walletAdapter = new DefaultSolanaWalletAdapter();
  walletAdapter.active({
    address: wallet.accounts[0].address,
    chain: { id: chain.network_infos.chain_id },
    provider: wallet.provider
  });
  const samples = await walletAdapter.connection.getRecentPerformanceSamples(60);
  let totalBlockTime = 0;
  let validSamples = 0;
  for (const sample of samples) {
    if (sample.numSlots > 0 && sample.samplePeriodSecs > 0) {
      const blockTime = sample.samplePeriodSecs / sample.numSlots;
      totalBlockTime += blockTime;
      validSamples++;
    }
  }
  return totalBlockTime / validSamples;
}

// src/contract/getBlockTime.ts
async function getBlockTime(inputs) {
  const { chainId, chain, wallet } = inputs;
  if (isSolana(chainId)) {
    return getSolanaBlockTime(chain, wallet);
  }
  return getEvmBlockTime(chain);
}

// src/contract/abi/getAppUlnConfigAbi.json
var getAppUlnConfigAbi_default = [
  {
    inputs: [
      {
        internalType: "address",
        name: "_oapp",
        type: "address"
      },
      {
        internalType: "uint32",
        name: "_remoteEid",
        type: "uint32"
      }
    ],
    name: "getAppUlnConfig",
    outputs: [
      {
        components: [
          {
            internalType: "uint64",
            name: "confirmations",
            type: "uint64"
          },
          {
            internalType: "uint8",
            name: "requiredDVNCount",
            type: "uint8"
          },
          {
            internalType: "uint8",
            name: "optionalDVNCount",
            type: "uint8"
          },
          {
            internalType: "uint8",
            name: "optionalDVNThreshold",
            type: "uint8"
          },
          {
            internalType: "address[]",
            name: "requiredDVNs",
            type: "address[]"
          },
          {
            internalType: "address[]",
            name: "optionalDVNs",
            type: "address[]"
          }
        ],
        internalType: "struct UlnConfig",
        name: "",
        type: "tuple"
      }
    ],
    stateMutability: "view",
    type: "function"
  }
];

// src/contract/endpointId.ts
var CHAIN_ID_TO_ENDPOINT_ID = {
  // Ethereum and L2s
  1: 30101,
  // Ethereum Mainnet
  10: 30111,
  // Optimism Mainnet
  42161: 30110,
  // Arbitrum Mainnet
  137: 30109,
  // Polygon Mainnet
  8453: 30184,
  // Base Mainnet
  59144: 30183,
  // Linea Mainnet
  534352: 30214,
  // Scroll Mainnet
  324: 30165,
  // zkSync Era Mainnet
  1101: 30158,
  // Polygon zkEVM Mainnet
  169: 30217,
  // Manta Pacific Mainnet
  5e3: 30181,
  // Mantle Mainnet
  81457: 30243,
  // Blast Mainnet
  252: 30255,
  // Fraxtal Mainnet
  34443: 30260,
  // Mode Mainnet
  167e3: 30290,
  // Taiko Mainnet
  7777777: 30195,
  // Zora Mainnet
  // Testnets
  11155420: 40232,
  // Optimism Sepolia Testnet
  421614: 40231,
  // Arbitrum Sepolia Testnet
  84532: 40245,
  // Base Sepolia Testnet
  300: 40305,
  // zkSync Sepolia Testnet
  11155111: 40161,
  // Ethereum Sepolia Testnet
  17e3: 40217,
  // Ethereum Holesky Testnet
  // BNB Chain and related
  56: 30102,
  // BNB Smart Chain (BSC) Mainnet
  204: 30202,
  // opBNB Mainnet
  97: 40102,
  // BNB Smart Chain (BSC) Testnet
  // Avalanche
  43114: 30106,
  // Avalanche Mainnet
  43113: 40106,
  // Avalanche Fuji Testnet
  // Fantom
  250: 30112,
  // Fantom Mainnet
  // Gnosis
  100: 30145,
  // Gnosis Mainnet
  // Celo
  42220: 30125,
  // Celo Mainnet
  // Harmony
  16666e5: 30116,
  // Harmony Mainnet
  // Cronos
  25: 30359,
  // Cronos EVM Mainnet
  388: 30360,
  // Cronos zkEVM Mainnet
  // Metis
  1088: 30151,
  // Metis Mainnet
  // Moonbeam/Moonriver
  1284: 30126,
  // Moonbeam Mainnet
  1285: 30167,
  // Moonriver Mainnet
  // Astar
  592: 30210,
  // Astar Mainnet
  3776: 30257,
  // Astar zkEVM Mainnet
  // Canto
  7700: 30159,
  // Canto Mainnet
  // Fuse
  122: 30138,
  // Fuse Mainnet
  // Kava
  2222: 30177,
  // Kava Mainnet
  // Meter
  82: 30176,
  // Meter Mainnet
  // Near Aurora
  1313161554: 30211,
  // Near Aurora Mainnet
  // DFK Chain
  53935: 30115,
  // DFK Chain
  // DOS Chain
  7979: 30149,
  // DOS Chain Mainnet
  // Degen
  666666666: 30267,
  // Degen Mainnet
  // Dexalot Subnet
  432204: 30118,
  // Dexalot Subnet Mainnet
  // Homeverse
  19011: 30265,
  // Homeverse Mainnet
  // Horizen EON
  7332: 30215,
  // Horizen EON Mainnet
  // Hubble
  1992: 30182,
  // Hubble Mainnet
  // Tenet
  1559: 30173,
  // Tenet Mainnet
  // TelosEVM
  40: 30199,
  // TelosEVM Mainnet
  // Tiltyard
  710420: 30238,
  // Tiltyard Mainnet
  // Viction
  88: 30196,
  // Viction Mainnet
  // XPLA
  37: 30216,
  // XPLA Mainnet
  // Xai
  660279: 30236,
  // Xai Mainnet
  // re.al
  111188: 30237,
  // re.al Mainnet
  // inEVM
  2525: 30234,
  // inEVM Mainnet
  // Rari Chain
  1380012617: 30235,
  // Rari Chain Mainnet
  // Shimmer
  148: 30230,
  // Shimmer Mainnet
  // Skale
  2046399126: 30273,
  // Skale Mainnet
  // Orderly
  291: 30213,
  // Orderly Mainnet
  // CoreDAO
  1116: 30153,
  // CoreDAO Mainnet
  // Conflux eSpace
  1030: 30212,
  // Conflux eSpace Mainnet
  // Flare
  14: 30295,
  // Flare Mainnet
  // Gravity
  1625: 30294,
  // Gravity Mainnet
  // Japan Open Chain
  81: 30285,
  // Japan Open Chain Mainnet
  // Kaia (formerly Klaytn)
  8217: 30150,
  // Kaia Mainnet (formerly Klaytn)
  // OKX
  66: 30155,
  // OKX Mainnet
  // Beam
  4337: 30198,
  // Beam Mainnet
  // Loot
  5151706: 30197,
  // Loot Mainnet
  // Lyra
  957: 30311,
  // Lyra Mainnet
  // Morph
  2818: 30322,
  // Morph Mainnet
  // Movement
  "": 30325,
  // Movement Mainnet (no chain ID)
  // Bouncebit
  6001: 30293,
  // Bouncebit Mainnet
  // Cyber
  7560: 30283,
  // Cyber Mainnet
  // Iota
  8822: 30284,
  // Iota Mainnet
  // Sanko
  1996: 30278,
  // Sanko Mainnet
  // Sei
  1329: 30280,
  // Sei Mainnet
  // BOB
  60808: 30279,
  // BOB Mainnet
  // Bahamut
  5165: 30363,
  // Bahamut Mainnet
  // Animechain
  69e3: 30372,
  // Animechain Mainnet
  // Ape
  33139: 30312,
  // Ape Mainnet
  // Berachain
  80094: 30362,
  // Berachain Mainnet
  // Bevm
  11501: 30317,
  // Bevm Mainnet
  // Bitlayer
  200901: 30314,
  // Bitlayer Mainnet
  // Botanix
  3637: 30376,
  // Botanix
  // Codex
  81224: 30323,
  // Codex Mainnet
  // Concrete
  12739: 30366,
  // Concrete
  // DM2 Verse
  68770: 30315,
  // DM2 Verse Mainnet
  // EDU Chain
  41923: 30328,
  // EDU Chain Mainnet
  // EVM on Flow
  747: 30336,
  // EVM on Flow Mainnet
  // Etherlink
  42793: 30292,
  // Etherlink Mainnet
  // Glue
  1300: 30342,
  // Glue Mainnet
  // Goat
  2345: 30361,
  // Goat Mainnet
  // Gunz
  43419: 30371,
  // Gunz Mainnet
  // Hedera
  295: 30316,
  // Hedera Mainnet
  // Hemi
  43111: 30329,
  // Hemi Mainnet
  // HyperEVM
  999: 30367,
  // HyperEVM Mainnet
  // Initia
  // "": 30326, // Initia Mainnet (no chain ID)
  // Ink
  57073: 30339,
  // Ink Mainnet
  // Katana
  747474: 30375,
  // Katana
  // Lens
  232: 30373,
  // Lens Mainnet
  // Lightlink
  1890: 30309,
  // Lightlink Mainnet
  // Lisk
  1135: 30321,
  // Lisk Mainnet
  // Merlin
  4200: 30266,
  // Merlin Mainnet
  // Nibiru
  6900: 30369,
  // Nibiru Mainnet
  // Otherworld Space
  8227: 30341,
  // Otherworld Space Mainnet
  // Peaq
  3338: 30302,
  // Peaq Mainnet
  // Plume
  98866: 30370,
  // Plume Mainnet
  // Reya
  1729: 30313,
  // Reya Mainnet
  // Rootstock
  30: 30333,
  // Rootstock Mainnet
  // Soneium
  1868: 30340,
  // Soneium Mainnet
  // Sonic
  146: 30332,
  // Sonic Mainnet
  // Sophon
  50104: 30334,
  // Sophon Mainnet
  // Story
  1514: 30364,
  // Story Mainnet
  // Subtensor EVM
  964: 30374,
  // Subtensor EVM Mainnet
  // Superposition
  55244: 30327,
  // Superposition Mainnet
  // Swell
  1923: 30335,
  // Swell Mainnet
  // Tac
  239: 30377,
  // Tac
  // TON
  // "": 30343, // TON Mainnet (no chain ID)
  // Unichain
  130: 30320,
  // Unichain Mainnet
  // Vana
  1480: 30330,
  // Vana Mainnet
  // Worldchain
  480: 30319,
  // Worldchain Mainnet
  // X Layer
  196: 30274,
  // X Layer Mainnet
  // XChain
  94524: 30291,
  // XChain Mainnet
  // XDC
  50: 30365,
  // XDC Mainnet
  // Zircuit
  48900: 30303,
  // Zircuit Mainnet
  // zkLink
  810180: 30301,
  // zkLink Mainnet
  // Abstract
  2741: 30324,
  // Abstract Mainnet
  // Aptos (non-EVM)
  // 1: 30108, // Aptos Mainnet
  // Arbitrum Nova
  42170: 30175,
  // Arbitrum Nova Mainnet
  // Corn
  21e6: 30331,
  // Corn Mainnet
  // Initia Testnet
  // "": 40326, // Initia Testnet (no chain ID)
  // HyperEVM Testnet
  998: 40362,
  // HyperEVM Testnet
  // Berachain Bepolia Testnet
  80069: 40371,
  // Berachain Bepolia Testnet
  // Monad Testnet
  10143: 40204,
  // Monad Testnet
  // Polygon Amoy Testnet
  80002: 40267,
  // Polygon Amoy Testnet
  // Solana (non-EVM)
  101: 30168,
  // Solana Mainnet
  103: 40168,
  // Solana Devnet
  900900900: 30168,
  // Solana Mainnet
  901901901: 40168,
  // Solana Devnet
  // Tron
  728126428: 30420,
  // Tron Mainnet
  2494104990: 40420
  // Tron Testnet
};
function getEndpointId(chainId) {
  return CHAIN_ID_TO_ENDPOINT_ID[chainId];
}

// src/contract/getChainConfirmations.ts
async function getChainConfirmations(chain) {
  const chainId = chain.network_infos.chain_id;
  const isMainnet = chain.network_infos.mainnet;
  const publicRpcUrl = isMainnet ? "https://rpc.orderly.network" : "https://testnet-rpc.orderly.org";
  const contractAddress = isMainnet ? "0xCFf08a35A5f27F306e2DA99ff198dB90f13DEF77" : "0x3013C32e5F45E69ceA9baD4d96786704C2aE148c";
  const endpointId = getEndpointId(chainId);
  const address = "0x0000000000000000000000000000000000000000";
  const provider = new ethers.JsonRpcProvider(publicRpcUrl);
  const contract = new ethers.Contract(
    contractAddress,
    // full abi: https://explorer-orderly-mainnet-0.t.conduit.xyz/address/0xCFf08a35A5f27F306e2DA99ff198dB90f13DEF77?tab=contract_abi
    // this only need to use getAppUlnConfig abi, other functions are not needed
    getAppUlnConfigAbi_default,
    provider
  );
  const result = await contract.getAppUlnConfig(address, endpointId);
  return Number(result.confirmations || 0);
}

// src/contract/useTransactionTime.ts
var useTransactionTime = (chainId) => {
  const [blockTime, setBlockTime] = useState(0);
  const [confirmations, setConfirmations] = useState(0);
  const timeMap = useRef({});
  const confirmationsMap = useRef({});
  const [, { findByChainId }] = useChains();
  const { wallet } = useWalletConnector();
  const chain = useMemo(() => {
    if (!chainId) {
      return;
    }
    const id = typeof chainId === "number" ? chainId : Number.parseInt(chainId);
    return findByChainId(id);
  }, [chainId, findByChainId]);
  useEffect(() => {
    if (!chain || !wallet) {
      return;
    }
    const chainId2 = chain.network_infos.chain_id;
    if (timeMap.current[chainId2]) {
      setBlockTime(timeMap.current[chainId2]);
    } else {
      getBlockTime({
        chainId: chainId2,
        chain,
        wallet
      }).then((time) => {
        console.log("average block time", chainId2, time);
        setBlockTime(time);
        timeMap.current[chainId2] = time;
      }).catch((error) => {
        console.error("getBlockTime error", error);
      });
    }
    if (confirmationsMap.current[chainId2]) {
      setConfirmations(confirmationsMap.current[chainId2]);
    } else {
      getChainConfirmations(chain).then((confirmations2) => {
        console.log("confirmations", chainId2, confirmations2);
        setConfirmations(confirmations2);
        confirmationsMap.current[chainId2] = confirmations2;
      }).catch((error) => {
        console.error("getChainConfirmations error", error);
      });
    }
  }, [chain, wallet]);
  const transactionTime = useMemo(() => {
    if (blockTime && confirmations) {
      return blockTime * confirmations;
    }
    return 0;
  }, [blockTime, confirmations]);
  return transactionTime;
};

// src/components/depostiStatus/depostiStatus.script.ts
function getTimeRange() {
  const endTime = Date.now();
  const startTime = endTime - 1e3 * 60 * 60;
  return [startTime, endTime];
}
function useDepositStatusScript() {
  const [depositDataRange, setDepositDataRange] = useState(getTimeRange());
  const [transferDataRange, setTransferDataRange] = useState(getTimeRange());
  const [depositPending, setDepositPending] = useState(0);
  const [depositCompleted, setDepositCompleted] = useState(0);
  const [transferCompleted, setTransferCompleted] = useState(0);
  const [pendingList, setPendingList] = useState([]);
  const [hideNotificationTimeMap, setHideNotificationTimeMap] = useLocalStorage(
    "orderly_deposit_status_hide_notification_time",
    {
      [AssetHistoryStatusEnum.PENDING]: 0,
      [AssetHistoryStatusEnum.COMPLETED]: 0
    }
  );
  const { state } = useAccount();
  const canTrade = state.status === AccountStatusEnum.EnableTrading || state.status === AccountStatusEnum.EnableTradingWithoutConnected;
  const { chainId, blockTime } = useMemo(() => {
    if (canTrade && pendingList.length === 1) {
      return {
        chainId: pendingList[0].chain_id,
        blockTime: pendingList[0].block_time
      };
    }
    return {
      chainId: void 0,
      blockTime: void 0
    };
  }, [canTrade, pendingList]);
  const transactionTime = useTransactionTime(chainId);
  const estimatedTime = useMemo(() => {
    if (transactionTime && blockTime) {
      const seconds = blockTime / 1e3 + transactionTime - Date.now() / 1e3;
      console.log("transactionTime", transactionTime, seconds);
      return formatEstimatedTime(seconds);
    }
    return 0;
  }, [transactionTime, blockTime]);
  const [assetHistory, { isLoading }] = useAssetsHistory(
    {
      startTime: depositDataRange[0],
      endTime: depositDataRange[1],
      page: 1,
      pageSize: 200,
      side: "DEPOSIT"
    },
    {
      shouldUpdateOnWalletChanged: () => false
    }
  );
  const [transferHistory] = useTransferHistory({
    dataRange: transferDataRange,
    side: "IN",
    size: 200,
    page: 1,
    main_sub_only: false
  });
  const updateDepositTimeRange = useDebouncedCallback((data) => {
    setDepositDataRange(getTimeRange());
  }, 300);
  useWalletTopic({
    onMessage(data) {
      const { side, transStatus } = data;
      if (side === AssetHistorySideEnum.DEPOSIT && (transStatus === AssetHistoryStatusEnum.PENDING || transStatus === AssetHistoryStatusEnum.COMPLETED)) {
        console.log("deposit status updated", data);
        updateDepositTimeRange(data);
      }
    }
  });
  const updateTransferTimeRange = useDebouncedCallback((data) => {
    setTransferDataRange(getTimeRange());
  }, 300);
  useBalanceTopic({
    onMessage(data) {
      console.log("balance updated", data);
      updateTransferTimeRange(data);
    }
  });
  useEffect(() => {
    if (!assetHistory || isLoading) {
      return;
    }
    const hidePendingNotificationTime = hideNotificationTimeMap[AssetHistoryStatusEnum.PENDING];
    const hideCompletedNotificationTime = hideNotificationTimeMap[AssetHistoryStatusEnum.COMPLETED];
    let pendingList2 = assetHistory.filter(
      (item) => item.trans_status === AssetHistoryStatusEnum.PENDING
    );
    if (hidePendingNotificationTime) {
      pendingList2 = pendingList2.filter(
        (item) => item.created_time > hidePendingNotificationTime
      );
    }
    let completedList = assetHistory.filter(
      (item) => item.trans_status === AssetHistoryStatusEnum.COMPLETED
    );
    if (hideCompletedNotificationTime) {
      completedList = completedList.filter(
        (item) => item.created_time > hideCompletedNotificationTime
      );
    }
    setDepositPending(pendingList2?.length || 0);
    setDepositCompleted(completedList?.length || 0);
    setPendingList(pendingList2 || []);
  }, [assetHistory, isLoading, hideNotificationTimeMap]);
  useEffect(() => {
    if (!transferHistory) {
      return;
    }
    const hideCompletedNotificationTime = hideNotificationTimeMap[AssetHistoryStatusEnum.COMPLETED];
    const completedList = hideCompletedNotificationTime ? transferHistory.filter(
      (item) => item.created_time > hideCompletedNotificationTime
    ) : transferHistory;
    setTransferCompleted(completedList.length);
  }, [transferHistory, hideNotificationTimeMap]);
  const completedCount = useMemo(() => {
    return depositCompleted + transferCompleted;
  }, [depositCompleted, transferCompleted]);
  const onClose = useCallback(
    (status) => {
      setHideNotificationTimeMap({
        ...hideNotificationTimeMap,
        [status]: Date.now()
      });
    },
    [hideNotificationTimeMap]
  );
  return {
    pendingCount: depositPending,
    completedCount,
    canTrade,
    estimatedTime,
    onClose
  };
}
function formatEstimatedTime(totalSeconds) {
  const sec = Math.max(30, totalSeconds);
  let minutes = Math.floor(sec / 60);
  let seconds = sec % 60;
  if (seconds > 0 && seconds <= 30) {
    seconds = 30;
  } else if (seconds > 30) {
    minutes += 1;
    seconds = 0;
  }
  if (minutes > 0) {
    return seconds > 0 ? `${minutes} m ${seconds} s` : `${minutes} m`;
  }
  return `${seconds} s`;
}
var DepositStatusWidget = (props) => {
  const state = useDepositStatusScript();
  return /* @__PURE__ */ jsx(DepositStatus, { ...state, ...props });
};

export { ActionButton, AvailableQuantity, BrokerWallet, ChainSelect, ConvertFormUI, ConvertFormWidget, DEPOSIT_ERROR_CODE_MAP, DepositAction, DepositAndWithdraw, DepositAndWithdrawWithDialogId, DepositAndWithdrawWithSheetId, DepositForm, DepositFormWidget, DepositStatus, DepositStatusWidget, ExchangeDivider, Fee, QuantityInput, SwapCoin, TransferDialogId, TransferForm, TransferFormWidget, TransferSheetId, TransferWidget, Web3Wallet, WithdrawForm, WithdrawFormWidget, WithdrawTo, YIELD_BEARING_ASSETS, YieldBearingReminder, checkIsAccountId, feeDecimalsOffset, getDepositKnownErrorMessage, getTransferErrorMessage, getUSDCToken, getYieldBearingAsset, isYieldBearingAsset, sortTokensWithUSDCFirst, useConvertFormScript as useConvertForm, useDepositFormScript, useDepositStatusScript, useTransferFormScript, useWithdrawFormScript };
//# sourceMappingURL=index.mjs.map
//# sourceMappingURL=index.mjs.map