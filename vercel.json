{
  "install": {
    "runtime": "nodejs20.x",
    "command": "yarn config set registry https://registry.npmjs.org && yarn install"
  },
  "build": {
    "env": {
      "NPM_CONFIG_REGISTRY": "https://registry.npmjs.org",
      "YARN_REGISTRY": "https://registry.npmjs.org"
    }
  }
}
